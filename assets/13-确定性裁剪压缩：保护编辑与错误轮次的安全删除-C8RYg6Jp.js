const t='当长会话的上下文利用率越过 0.70 时，MiniCode 会进入一条**不调用模型、纯算法驱动**的压缩路径——`snipCompactConversation`。它与 LLM 摘要压缩（autoCompact）的本质区别在于：裁剪决策完全由确定性的区间扫描与保护规则决定，同一输入必然产生同一输出，且**绝不触碰文件编辑轮次与错误诊断轮次**。本文将剖析这条路径的分组模型、保护规则、安全区间选取与验证护栏。\n\n## 压缩管线中的定位：零成本的第一道防线\n\nMiniCode 的上下文管理是一条按利用率阈值分级触发的管线，`snipCompact` 是其中唯一一个"不花一分钱 token"的确定性机制：\n\n```mermaid\nflowchart TD\n    A[代理循环每轮<br/>agent-loop] --> B{利用率 ≥ 0.70?}\n    B -- 是 --> C[snipCompact<br/>确定性中段删除 · 无模型调用]\n    B -- 否 --> E2[跳过 snip]\n    C --> D{利用率 ≥ 0.50?}\n    E2 --> D\n    D -- 是 --> E[微压缩 microcompact<br/>旧工具结果占位清理]\n    D -- 否 --> F2[跳过微压缩]\n    E --> F{利用率 ≥ 0.75?}\n    F2 --> F\n    F -- 是 --> G[上下文折叠投影<br/>可摘要区间替换]\n    F -- 否 --> H2[跳过折叠]\n    G --> H{step==0 且<br/>利用率 ≥ 0.85?}\n    H2 --> H\n    H -- 是 --> I[autoCompact<br/>LLM 摘要压缩]\n    H -- 否 --> J[继续模型调用]\n    I --> J\n```\n\n在 `agent-loop.ts` 的每轮循环中，执行顺序为：**snipCompact → microcompact → context-collapse → autoCompact**，且 `snippedThisTurn` 标志保证一轮内 snip 至多执行一次；autoCompact 仅在 `step === 0` 且警告级别达到 critical/blocked 时才尝试。这意味着只要 snip 成功释放了足够上下文，后续昂贵的 LLM 摘要路径就可能根本不会被触发——测试 `autoCompact uses post-snip stats instead of stale pre-snip usage` 专门验证了这一效果：snip 之后保留消息的供应商用量被标记为 stale，`autoCompact` 不再基于过期的用量数字发起多余的模型调用。\n\nSources: [agent-loop.ts](src/agent-loop.ts#L174-L230)、[test/snip-compact.test.ts](test/snip-compact.test.ts#L473-L503)\n\n## 触发条件与常量体系\n\nsnip 的触发与目标全部由 `constants.ts` 中的五个常量决定：\n\n| 常量 | 值 | 语义 |\n|---|---|---|\n| `SNIP_COMPACT_THRESHOLD` | 0.70 | 利用率达到该值才触发 |\n| `SNIP_TARGET_USAGE` | 0.60 | 裁剪后的目标利用率 |\n| `SNIP_MIN_MESSAGES_TO_REMOVE` | 6 | 单次至少删除 6 条消息 |\n| `SNIP_KEEP_RECENT_MESSAGES` | 12 | 尾部 12 条消息无条件保留 |\n| `SNIP_MIN_TOKENS_TO_FREE` | 2_000 | 净释放量下限（tokens） |\n\n入口函数首先计算利用率：`triggerTokens / effectiveInput`（`effectiveInput` 优先取供应商上报的 `effectiveInput`，否则回退到模型上下文窗口）。若低于 0.70，立即返回 `below_threshold` 的 `noSnipResult`。注意这是一个**软性护栏**——真正的安全保证来自后续的候选区间划定与分组保护，阈值只是成本控制的第一道闸门。\n\nSources: [constants.ts](src/compact/constants.ts#L7-L11)、[snipCompact.ts](src/compact/snipCompact.ts#L402-L425)\n\n## 候选区间划定：三明治式保留结构\n\n`findCandidateRange` 的职责是框出"中间可以删、两头必须留"的候选区间，它同时保护**尾部最近消息**与**最近一次用户请求**：\n\n```\n[messages.length - 12]  ← keepRecentStart（尾部保留线的起点）\n          ↓\nend = min(keepRecentStart, lastUserIndex)\n          ↓\n[start, end)  ← 候选区间\n          ↓\nstart = 越过最后一个边界消息（system / context_summary / snip_boundary）\n```\n\n三个边界条件依次生效：\n\n1. **消息总量下限**：`length ≤ 12 + 6 = 18` 时直接拒绝（`too_few_messages`），保证即使删掉 6 条也仍有足够的保留空间。\n2. **尾部保留**：`keepRecentStart = length - 12`，最近 12 条消息永不进入候选区间。\n3. **最新用户请求保护**：从尾部向前扫描最后一条 `user` 消息，`end = min(keepRecentStart, lastUserIndex)`——这确保当前活跃任务（最新的用户指令及其后的所有消息）绝不被删。测试 `does not delete the latest user message or messages after it` 专门验证了这一点。\n4. **起点锚定**：`start` 会推进到最后一个边界消息之后，保证 system、历史 context_summary、已存在的 snip_boundary 不会被纳入删除范围。\n\n若 `end - start < 6`，返回 `candidate_range_too_small` 拒绝。候选区间划定之后，真正的安全分析才在分组层面展开。\n\nSources: [snipCompact.ts](src/compact/snipCompact.ts#L237-L275)、[test/snip-compact.test.ts](test/snip-compact.test.ts#L132-L178)\n\n## 消息分组：工具调用对的完整性\n\n`buildMessageGroups` 先把线性消息流折叠为**原子分组**，每组要么整体删除、要么整体保留，这是"不产生孤儿工具调用"的根基：\n\n| 消息形态 | 分组行为 | 初始保护状态 |\n|---|---|---|\n| `assistant_tool_call` + 紧邻且 `toolUseId` 匹配的 `tool_result` | 合并为一个双消息组 | 未保护 |\n| `assistant_tool_call` 后无匹配结果 | 单消息组 | **保护**（`unclosed_tool_call`） |\n| 孤立的 `tool_result` | 单消息组 | **保护**（`orphan_tool_result`） |\n| user / assistant / system 等 | 单消息组 | 未保护 |\n\n分组逻辑只向前看一步：若 `messages[i+1]` 是 `tool_result` 且 `toolUseId` 与当前调用一致，则两消息绑定为一组；否则调用单独成组并被标记为受保护——因为删除一个未闭合的调用会把后续依赖它的结果变成孤儿。测试 `does not split tool_call and tool_result pairs` 断言：被删除的 ID 集合中 `m-3` 与 `m-4`（调用与结果）要么同时存在、要么同时不存在；`assertNoToolOrphans` 则从正反两个方向校验调用集合与结果集合完全一致。\n\nSources: [snipCompact.ts](src/compact/snipCompact.ts#L148-L195)、[test/snip-compact.test.ts](test/snip-compact.test.ts#L75-L97)\n\n## 保护规则：编辑轮次与错误轮次的不可触碰区\n\n分组之后，`markProtectedGroups` 对候选区间内的每组施加**三类保护标记**，保护一旦生效即向相邻分组扩散（`protectNearbyGroups` 保护自身及前后各一组）：\n\n```mermaid\nflowchart LR\n    subgraph 候选区间内的分组\n        G1[普通对话组] --> G2{groupHasProtectedTool?}\n        G1 --> G3{groupHasImportantError?}\n        G1 --> G4{含边界消息?}\n        G2 -- 是 --> P1[保护自身 ± 邻居<br/>near_file_edit]\n        G3 -- 是 --> P2[保护自身 ± 邻居<br/>near_important_error]\n        G4 -- 是 --> P3[保护该组<br/>boundary_message]\n    end\n    subgraph 候选区间外\n        G5[尾部最近消息等] --> P4[outside_candidate_range]\n    end\n```\n\n**编辑保护**：`isProtectedToolName` 先精确匹配 `edit_file`、`modify_file`、`patch_file`、`write_file`、`apply_patch` 五个工具名，再做模糊匹配（名称含 `patch` / `write` / `edit` / `modify` 任意子串即命中）。命中组及其前后邻居整体标为 `near_file_edit`。测试 `protects patch/edit tool groups and their neighboring messages` 验证：`patch_file` 的调用-结果对以及前后的 "Keep before patch" / "Keep after patch" 文本全部保留。\n\n**错误保护**：判定分两层——`toolResultLooksImportantError` 检查 `isError` 标志或结果内容中的错误标记；`messageTextLooksImportantError` 对 user / assistant / assistant_progress / context_summary / snip_boundary 的文本内容做同样的标记扫描。`ERROR_MARKERS` 包含 `error`、`failed`、`failure`、`exception`、`traceback`、`permission denied` 六个词。命中的分组连同邻居标为 `near_important_error`。测试同时覆盖了"工具结果中的错误"（`Traceback: exception failed`）与"普通文本中的错误分析"（`Traceback analysis: exception failed in prior command`）两种形态。\n\n**边界保护**：组内含 `system`、`context_summary`、`snip_boundary` 任一角色即标为 `boundary_message`。这保证了历史摘要与既有裁剪占位符永远不会被二次删除。\n\nSources: [snipCompact.ts](src/compact/snipCompact.ts#L51-L66)、[snipCompact.ts](src/compact/snipCompact.ts#L96-L146)、[snipCompact.ts](src/compact/snipCompact.ts#L197-L235)、[test/snip-compact.test.ts](test/snip-compact.test.ts#L235-L340)\n\n## 安全区间与确定性择优\n\n保护标记完成后，`findSafeRuns` 把未保护的分组连续段切分为若干"安全运行段"，随后进入一个**全确定性的择优流程**：\n\n1. **过滤**：丢弃消息数 < 6 或 tokens < 2,000 的运行段。\n2. **排序**：`compareRuns` 按三键降序——先比 tokens 释放量，再比消息条数，最后比起始位置靠前（`a.start - b.start`）。三键排序保证在任意输入下都产生唯一的最优解。\n3. **截取**：`selectDeletionFromRun` 从最优运行段的起点开始逐组累积，直到 `tokens ≥ desiredTokensToFree` **且** `messagesCount ≥ 6` 同时满足；其中 `desiredTokensToFree = max(2_000, triggerTokens - effectiveInput × 0.60)`——即"至少要释放 2,000 tokens，理想情况下把利用率拉回 0.60 目标线"。\n\n若过滤后无任何运行段，返回 `no_safe_interval`。这解释了测试 `returns didSnip=false when there is no safe interval` 的场景：当旧区间全部由 `edit_file` 调用-结果对构成时，每个分组都被保护，连续未保护段不复存在，裁剪被安全拒绝。\n\nSources: [snipCompact.ts](src/compact/snipCompact.ts#L277-L346)、[snipCompact.ts](src/compact/snipCompact.ts#L430-L450)、[test/snip-compact.test.ts](test/snip-compact.test.ts#L390-L411)\n\n## 边界占位与验证护栏\n\n删除不是简单的 splice——被删区间会替换为一个 `snip_boundary` 占位消息，承载裁剪元数据：\n\n```typescript\n{\n  id: `snip-${timestamp}-${firstRemoved}`,\n  role: \'snip_boundary\',\n  content: buildSnipBoundaryContent({ removedCount, tokensFreed }),\n  removedMessageIds: [...],   // 被删消息 ID 列表\n  removedCount, tokensFreed, timestamp,\n}\n```\n\n占位文本 `[Snipped earlier conversation segment]` 明确告知后续模型"中段对话已被移除，近期对话与当前任务上下文完整保留"；同时 `removedMessageIds` 记录了被删消息的精确 ID 列表，为会话日志的断点恢复提供依据。\n\n`snipCompactConversation` 在返回成功前设置**四道验证护栏**，任何一道失败都会降级为 `noSnipResult`：\n\n| 护栏 | 检查点 | 失败原因 |\n|---|---|---|\n| 消息数 | 截取段 `messagesCount ≥ 6` | `below_min_messages` |\n| 净释放量 | `deletion.tokens - boundaryTokens ≥ 2,000` | `below_min_tokens` |\n| 实际削减 | `tokensAfter < tokensBefore` | `no_token_reduction` |\n| 用量新鲜度 | 保留消息标记 `usageStale` | 触发记账回退到估算 |\n\n最后一道护栏 `markRetainedUsageStale` 尤其精妙：snip 后保留的 assistant 系消息如果仍带供应商上报的 usage，其 token 数已不再代表真实上下文，因此统一标记 `usageStale = true` 与原因 "conversation was snip-compacted after this provider usage was recorded"，使后续 `computeContextStats` 回退到估算而非陈旧的精确用量——这正是防止 autoCompact 误触发的关键。\n\nSources: [snipCompact.ts](src/compact/snipCompact.ts#L348-L399)、[snipCompact.ts](src/compact/snipCompact.ts#L447-L499)、[token-estimator.ts](src/utils/token-estimator.ts#L158-L175)、[types.ts](src/types.ts#L48-L55)\n\n## 与周边机制的协同\n\nsnip 的产物是一个一等公民的 `snip_boundary` 角色消息，整个系统对它一视同仁：\n\n- **会话持久化**：`wrapEvent` 为 snip_boundary 附加 `snipMetadata`（含 `removedMessageIds`）；`appendSnipBoundary` 以独立事件追加到 JSONL 日志，`reconstructSnippedEvents` 在恢复时把占位符插回被删消息的原始逻辑位置。\n- **Anthropic 适配器**：`snip_boundary` 被翻译为一条 user 消息（`buildAnthropicSnipBoundaryText` 的精简版文本），模型端只能看到"一段早期对话被移除"的声明。\n- **微压缩协同**：microcompact 只替换 `tool_result` 的 content 为 `CLEAR_MARKER`，从不触碰 snip_boundary——测试 `microcompact does not clear snip_boundary messages` 显式锁定该行为。\n- **上下文折叠与摘要压缩**：`context-collapse` 与 `compact` 均将 snip_boundary 视为不可折叠的边界角色。\n- **手动触发**：`/snip` 斜杠命令在 TUI 中直接调用同一入口，成功后同步 `args.messages`、重置 `alreadySavedCount` 与折叠状态，并在状态栏报告释放的 token 数。\n\nSources: [session.ts](src/session.ts#L93-L160)、[session.ts](src/session.ts#L252-L286)、[anthropic-adapter.ts](src/anthropic-adapter.ts#L286-L291)、[microcompact.ts](src/compact/microcompact.ts#L30-L49)、[compact.ts](src/compact/compact.ts#L114-L116)、[tty-app.ts](src/tty-app.ts#L1096-L1131)\n\n## 确定性保证清单\n\n| 不变式 | 机制 | 验证测试 |\n|---|---|---|\n| 系统消息永不删除 | `isBoundaryMessage` + 起点锚定 | `does not delete system messages` |\n| 历史摘要永不删除 | `boundary_message` 保护 | `does not delete context_summary messages` |\n| 已存在的裁剪占位永不删除 | `boundary_message` 保护 | `does not delete an existing snip_boundary` |\n| 最近 12 条消息保留 | `keepRecentStart` 下限 | `does not delete the most recent kept messages` |\n| 最新用户请求及后续保留 | `lastUserIndex` 截断 | `does not delete the latest user message` |\n| 工具调用-结果对不拆分 | 分组原子性 | `does not split tool_call and tool_result pairs` |\n| 未闭合调用/孤儿结果不删除 | 初始保护标记 | `does not delete unclosed tool calls` |\n| 编辑类工具轮次保留 | 名称匹配 + 邻居扩散 | `protects patch/edit tool groups` |\n| 错误轮次保留 | 标记扫描 + 邻居扩散 | `protects important error tool results` |\n| 同输入必同输出 | 纯算法 + 三键排序 | —（结构性保证） |\n\n所有保护理由（`unclosed_tool_call`、`orphan_tool_result`、`outside_candidate_range`、`boundary_message`、`near_file_edit`、`near_important_error`）都被记录在分组上，任何拒绝路径都返回可诊断的 `reason` 字符串——这套"可解释的确定性"正是它区别于黑盒 LLM 摘要的关键价值。\n\nSources: [snipCompact.ts](src/compact/snipCompact.ts#L23-L49)、[snipCompact.ts](src/compact/snipCompact.ts#L68-L82)、[test/snip-compact.test.ts](test/snip-compact.test.ts#L99-L149)\n\n## 下一步阅读\n\nsnip 只是上下文压缩体系的一环。理解它与相邻机制的边界，建议按目录顺序继续：\n\n- [自动压缩：触发阈值与失败熔断](12-zi-dong-ya-suo-hong-fa-yu-zhi-yu-shi-bai-rong-duan)——snip 在 0.70 拦截后，0.85 的 LLM 摘要路径如何处理剩余负载\n- [上下文折叠投影：可摘要区间识别与替换](14-shang-xia-wen-zhe-die-tou-ying-ke-zhai-yao-qu-jian-shi-bie-yu-ti-huan)——0.75 层级的可摘要区间机制与 snip 的保护语义如何对齐\n- [微压缩：旧工具结果的占位清理](15-wei-ya-suo-jiu-gong-ju-jie-guo-de-zhan-wei-qing-li)——snip_boundary 为何在微压缩中豁免\n- [追加式 JSONL 事件日志与断点恢复](19-zhui-jia-shi-jsonl-shi-jian-ri-zhi-yu-duan-dian-hui-fu)——`snipMetadata` 与 `reconstructSnippedEvents` 的持久化语义';export{t as default};

const n='本文剖析 HappyClaw 中 IM 渠道挂载（Mount）与会话绑定（Binding）的边界机制：一个外部聊天如何被登记为 `RegisteredGroup`、绑定到工作区或具体会话、在原生话题容器下升级为 `thread_map` 路由，以及解绑时如何原子地恢复账号默认目标。全文面向需要修改或扩展路由逻辑的高级开发者，聚焦**边界不变量**——哪些状态互斥、哪些路径 fail-closed、哪些写操作必须原子完成。\n\n## 问题域：三层边界的划分\n\n渠道挂载系统需要同时维持三条彼此正交的边界：\n\n1. **地址边界**——外部聊天在系统内的标识（JID）如何在 provider、外部聊天、渠道账号、线程四个维度上分层，且历史遗留格式可被规范化；\n2. **绑定边界**——一个外部聊天挂载到哪个逻辑目标（工作区主对话 or 具体会话），两个目标互斥；\n3. **所有权边界**——会话由哪个 IM 源"粘性"持有，Web 注入或其他连接器都不能静默转移。\n\n这三条边界分别由 `channel-address.ts`、`channel-mount-service.ts`、`channel-session-owner.ts` 承载，并由 `resolveEffectiveChatJid` 在入站路由时统一裁决。理解边界的正确姿势是：**地址决定"这是谁"、挂载决定"去往哪里"、所有权决定"回复从哪发出"**。\n\nSources: [channel-address.ts](src/channel-address.ts#L1-L115)、[channel-mount-service.ts](src/channel-mount-service.ts#L1-L36)、[channel-session-owner.ts](src/channel-session-owner.ts#L1-L36)\n\n## 地址边界：JID 的四层结构\n\nJID 由 `shared/channel-prefixes.ts` 中七个 provider 前缀（`feishu:`、`telegram:`、`qq:`、`wechat:`、`dingtalk:`、`discord:`、`whatsapp:`）起始，其后以 `#` 分隔多个片段。`parseChannelAddress` 将 JID 解析为结构化地址：\n\n```mermaid\ngraph LR\n    subgraph JID["jid 字符串"]\n        A["feishu:oc_xxx"] --> B["#account:bot-a"]\n        B --> C["#thread:t1"]\n        C --> D["#root:r1"]\n    end\n    subgraph 语义["parseChannelAddress 输出"]\n        E["provider = \'feishu\'"]\n        F["externalChatId = \'oc_xxx\'"]\n        G["channelAccountId = \'bot-a\'"]\n        H["threadId / rootMessageId"]\n    end\n    A -.-> E\n    A -.-> F\n    B -.-> G\n    C -.-> H\n    D -.-> H\n```\n\n三个关键规范化函数定义了地址的合法变换：\n\n- `channelConversationJid(jid)` 剥离 thread/root 片段，保留 provider + 外部聊天 + 账户片段，是**会话所有权与挂载的粒度基准**——所有权按对话持有，而非按线程持有；\n- `scopeChannelJid(jid, accountId)` 为遗留 JID 补挂账户片段（`account:`），不触碰 provider 原生路由片段；\n- `toProviderJid(jid)` 反方向剥离 HappyClaw 的账户片段，恢复为传给 provider SDK 的原始 JID。\n\n没有 `account:` 片段的 JID 被标记为 `legacy: true`，对应历史"未按账号划分"的记录，注册时归入渠道账号的默认（或 legacy 默认）账户。**地址变换必须保持可逆且幂等**——同一外部聊天在迁移前后必须能规范化为同一 `channelConversationJid`。\n\nSources: [shared/channel-prefixes.ts](shared/channel-prefixes.ts#L1-L19)、[channel-address.ts](src/channel-address.ts#L1-L115)\n\n## 绑定边界：工作区挂载与会话绑定的互斥模型\n\n`RegisteredGroup` 上的两个目标字段构成互斥的绑定模型（见 [types.ts](src/types.ts#L160-L215)）：\n\n| 字段 | 语义 | 对应 routing_mode |\n|---|---|---|\n| `target_agent_id` | 绑定到工作区内**具体会话**（`kind === \'conversation\'` 的 Agent） | `single_session` |\n| `target_main_jid` | 绑定到**工作区主对话**（`web:{uuid}` 规范 JID） | `single_session` 或 `thread_map` |\n| 两者皆无 | 未挂载；普通群聊可能持有账号默认工作区回退 | — |\n\n互斥性由 `hasSessionMountConflict` / `hasWorkspaceMountConflict` 显式维护：会话绑定存在时不允许再设工作区目标，反之亦然；唯一例外是**同一工作区的旧格式 `web:{folder}` 与新格式 `web:{uuid}` 被视为等价**（`matchesWorkspaceMount` 双值比较），这是迁移期的兼容通道。\n\n挂载更新全部通过三个纯函数生成新状态，绝不直接改字段：\n\n- `buildSessionMountUpdate(group, sessionId, options)` — 置 `target_agent_id`、清 `target_main_jid`、`binding_mode: \'single_context\'`；\n- `buildWorkspaceMountUpdate(group, workspaceJid, routingMode, options)` — 反向清 `target_agent_id`，`binding_mode` 依 routingMode 取 `thread_map` 或 `single_context`；\n- `buildUnmountUpdate(group, options)` — 清空两个目标（仅用于内部全量清除；用户侧解绑走默认恢复路径，见下文）。\n\n回复策略（`reply_policy: \'source_only\' | \'mirror\'`）、激活模式、受众模式与 `owner_im_id` 作为渠道级策略随挂载保留，不在目标切换时丢失。\n\nSources: [channel-mount-service.ts](src/channel-mount-service.ts#L437-L560)、[types.ts](src/types.ts#L160-L215)\n\n### channel_mounts 规范化镜像\n\n`registered_groups` 是路由的**唯一事实源**，但查询热路径需要规范化投影，因此 `setRegisteredGroup` 在单事务内双写 `channel_mounts` 表（含 `agent_channel_mounts` 的派生同步），`commitChannelMountUpdate` 同时更新内存路由缓存并清空该渠道的失败计数。投影函数 `normalizeChannelMountFromGroup` 将两个目标字段折叠为单一 `ChannelMount` 记录：会话绑定投影出 `session_id`，工作区绑定投影出 `workspace_jid` 与 `routing_mode`；投影失败（目标缺失）返回 `null` 并**跳过镜像写**，而非写入残缺行。\n\n```mermaid\ngraph LR\n    A["registered_groups<br/>(事实源)"] -->|"normalizeChannelMountFromGroup"| B["channel_mounts<br/>(规范化镜像)"]\n    B -->|"resolveChannelMountTarget"| C["路由目标解析"]\n    A -->|"setRegisteredGroup 单事务"| D["内存 registeredGroups 缓存"]\n    B -->|"upsertChannelMount 同步"| E["agent_channel_mounts"]\n```\n\nSources: [channel-mount-service.ts](src/channel-mount-service.ts#L353-L437)、[db.ts](src/db.ts#L10790-L10860)\n\n## 路由决策：resolveEffectiveChatJid 的优先级序列\n\n入站消息的主路由函数 `buildResolveEffectiveChatJid`（注册于所有连接器的 `resolveEffectiveChatJid` 回调）按固定优先级裁决目标，**任何 stale 状态都 fail-closed（返回 null，消息不路由）**：\n\n```mermaid\nflowchart TD\n    A["入站 chatJid"] --> B{"registeredGroups 中存在?"}\n    B -- 否 --> X1["null（不路由）"]\n    B -- 是 --> C{"检测到原生线程?"}\n    C -- 是 --> D["ensureNativeContextChannelMount 升级"]\n    D -- conflict --> X2["null（fail-closed）"]\n    D -- ok --> E["channel_mounts 镜像存在?"]\n    C -- 否 --> E\n    E -- 是 --> F["resolveChannelMountTarget"]\n    F -- stale --> X3["null + warn"]\n    F -- session 绑定 --> G["effectiveJid = chat_jid#agent:id"]\n    F -- workspace 且 thread_map --> H["resolveOrCreateNativeThreadAgent"]\n    F -- workspace 普通 --> I["effectiveJid = workspaceJid"]\n    E -- 否 --> J{"target_agent_id?"}\n    J -- 是 --> G\n    J -- 否 --> K{"binding_mode=thread_map 且有原生线程?"}\n    K -- 是 --> H\n    K -- 否 --> L{"target_main_jid?"}\n    L -- 是 --> I\n    L -- 否 --> X4["null（无绑定）"]\n```\n\n决策序列的要点：\n\n1. **原生线程检测先行**——飞书话题/Telegram Forum 的首条消息触发 `ensureNativeContextChannelMount`，把普通绑定升级为 `thread_map`；升级失败（如已绑定固定会话）立即 fail-closed 并持久化能力标记供 UI 诊断；\n2. **镜像优先于遗留列**——`channel_mounts` 存在时以 `resolveChannelMountTarget` 为准，该函数能识别 `missing_session` / `missing_workspace` 两类 stale，并上报 `workspaceMismatch`（镜像的工作区与会话属主工作区不一致时以**会话属主**为准，仅告警不阻断）；\n3. **遗留列作为兼容回退**——`target_agent_id` → `thread_map` → `target_main_jid` 依次尝试，`resolveWorkspaceJid` 负责把 `web:{folder}` 旧格式折叠到 `web:{uuid}`。\n\nSources: [src/index.ts](src/index.ts#L18609-L18756)、[channel-mount-service.ts](src/channel-mount-service.ts#L460-L530)\n\n## 对话分类：绑定策略的准入前提\n\n绑定目标类型与对话身份（direct/group）存在硬性约束，由 `channel-conversation-kind.ts` 裁决：\n\n| 对话分类 | 允许的绑定目标 | 判定依据 |\n|---|---|---|\n| `group` | 仅工作区 | 飞书 `group`/`topic`、QQ `qq:group:`、Telegram 负 ID、Discord 非 DM 等 |\n| `direct` | 仅会话 | 飞书 `p2p`、QQ `qq:c2c:`、Telegram 正 ID、WhatsApp `@s.whatsapp.net` 等 |\n| `unknown` | 一律拒绝 | 无法判定时 fail-closed |\n\n飞书是特例：其外部聊天使用同一套 `oc_*` 标识符承载 P2P 与群聊，**JID 本身不可猜测对话类型**，必须以 durable `feishu_chat_mode` 或 live `chat_mode` 元数据为准（`resolveChannelConversationKind` 对飞书分支只信任元数据）。`conversationBindingPolicyError` 把上述约束转成面向用户的错误文案，REST 端点据此在写入前拦截非法绑定。\n\nSources: [channel-conversation-kind.ts](src/channel-conversation-kind.ts#L1-L91)\n\n## 原生上下文容器的挂载升级\n\n`isNativeContextContainer` 判定一个外部聊天是否是"原生上下文容器"（一个容器承载多个平台会话）：飞书 topic 群、thread 型群、需要 @ 激活的普通飞书群，以及 Telegram Forum（`thread_capable`）。容器**必须**以 `thread_map` 路由——这是不变量，不是偏好：\n\n- `upgradeNativeContextChannelMount` 拒绝 `bound_to_session`（容器绑死单会话是非法状态）与 `missing_workspace`；\n- 升级以 `buildWorkspaceMountUpdate(group, workspaceJid, \'thread_map\')` 原子提交，不删除已有会话/上下文映射——**重新绑定同一话题渠道可恢复既有历史**；\n- 容器与其工作区的导航状态联动：`markThreadMapWorkspace` 置 `conversation_source: \'native_thread\'` + `conversation_nav_mode: \'vertical_threads\'`；`detachThreadMapWorkspace` 只在 `hasRemainingThreadMapMount` 判定**最后一个 thread_map 源离开后**才把工作区还原为 `manual`/`horizontal`，避免误伤共享同一工作区的多个话题群。\n\n一个值得注意的兼容迁移：飞书普通群在 mention 模式下按话题隔离会话，但若该群在旧配置下被绑定了固定会话（`target_agent_id`），`ensureNativeContextChannelMount` 会把该会话提升为其属主工作区的 `thread_map` 绑定——固定会话无法承载多个话题会话，提升是唯一不丢消息的路径。\n\nSources: [channel-mount-service.ts](src/channel-mount-service.ts#L80-L130)、[channel-mount-service.ts](src/channel-mount-service.ts#L253-L310)、[src/index.ts](src/index.ts#L18514-L18600)\n\n## 所有权边界：会话的粘性归属\n\n`resolveStickyChannelOwner` 定义会话的 IM 所有权规则，核心不变量是**所有权按对话粒度持有、按消息粒度跟随回复锚点**：\n\n- Web 表面可以向 IM 所属会话注入输入，但**不改变传输所有权**（`web:*` 源永远不能成为 owner）；\n- 同一 provider 的第二个连接器观察到后续消息时，**不能**静默把会话转移给那个 Bot（跨 `channel_account_id` 保持原 owner）；\n- 在同一对话（`channelConversationJid` 相等）内，回复锚点跟随当前消息——修复了"首条消息带 `root_id` 就冻结整个路由"的回归：后来顶层级消息不会被永远钉进那个线程；\n- 跨对话、跨账户、跨 provider 的消息保持原 owner。\n\n首次绑定是幂等的：Web 创建的会话在收到第一条具体 IM 源时一次性采纳 owner（`setSessionChannelOwnerOnce`），此后粘性生效。该函数在入站回复路由（`replySourceImJid`）与 IPC 回复转发路由（`activeImReplyRoutes`）两处消费，保证"回复从哪个渠道发出"与所有权一致。\n\nSources: [channel-session-owner.ts](src/channel-session-owner.ts#L1-L36)、[tests/channel-session-owner.test.ts](tests/channel-session-owner.test.ts#L1-L47)、[src/index.ts](src/index.ts#L5670-L5695)\n\n## 解绑与默认恢复：原子性边界\n\n用户侧解绑（`/unbind` 斜杠命令与 REST DELETE）的语义与直觉相反：**不是清除路由，而是恢复渠道账号的默认工作区**。`buildRestoreDefaultChannelMountUpdate` 按确定性顺序解析目标：\n\n1. 校验账户一致性（存储账户 vs JID 编码账户 vs 当前用户，任一不匹配返回 `unavailable: account_mismatch`）；\n2. 取账号 `default_workspace_jid`（须存在、未被排除、`created_by` 属于有效 owner）；\n3. 失败则回退到 owner 的 home 工作区；\n4. 仍无则 `unavailable: missing_default_workspace`。\n\n**原子性保证**：`restoreDefaultChannelMount` 先纯解析、后 `commitChannelMountUpdate` 单次提交——解析失败绝不产生"渠道暂时无绑定"的中间态；`resolveDefaultChannelMountForWorkspaceDeletion` 更进一步，在工作区删除前为所有受影响渠道预解析全部 reroute，再与删除同事务持久化。测试契约明确断言：`/unbind` 路径只允许 `restoreDefaultChannelMount`，不允许 `buildUnmountUpdate`（后者仅用于彻底删除记录的场景）。\n\n```mermaid\nstateDiagram-v2\n    [*] --> Bound: 绑定/挂载\n    Bound --> Restoring: /unbind 或健康修复\n    Restoring --> Resolved: 账号默认或 home 可用\n    Restoring --> Bound: 解析失败（保留旧路由）\n    Resolved --> Detached: 最后 thread_map 源离开\n    Detached --> [*]: 工作区导航还原 manual/horizontal\n```\n\nSources: [channel-mount-service.ts](src/channel-mount-service.ts#L148-L253)、[src/index.ts](src/index.ts#L2003-L2060)、[tests/channel-unbind-default-contract.test.ts](tests/channel-unbind-default-contract.test.ts#L10-L36)\n\n## REST 边界与并发安全\n\n绑定变更面集中在 `routes/agents.ts` 与 `routes/config.ts` 两个端点族，共享同一组挂载构建器与 `restoreDefaultChannelMount`（契约测试强制这一共享，防止两处语义漂移）：\n\n| 端点 | 语义 | 关键校验 |\n|---|---|---|\n| `PUT /api/groups/:jid/sessions/:sessionId/im-binding` | 工作区会话 ← IM 群 | `conversationBindingPolicyError(kind,\'session\')`；native 容器拒绝；冲突 409 |\n| `PUT /api/groups/:jid/agents/:agentId/im-binding` | 同上（Agent 别名路径） | 同左 |\n| `PUT /api/groups/:jid/im-binding` | 工作区主对话 ← IM 群 | `conversationBindingPolicyError(kind,\'workspace\')`；`threadCapable` 决定 routing_mode |\n| `DELETE .../im-binding/:imJid` | 解绑 → 恢复默认 | 复用 `restoreDefaultChannelMount` |\n\n端点实现体现了**跨 await 并发安全**这一高级约束：`fetchLiveChatInfo` 是真实网络调用（如飞书 `getFeishuChatInfo`），await 期间路由器的 owner 学习路径或并发绑定请求可能已提交新挂载——因此 await 之后必须基于 `freshImGroup` 重读、重跑 `canModifyGroup` / `hasConsistentChannelAccount` 校验，再用新鲜快照构建更新。注释明确警告：用 await 前的旧快照构建 update 会**静默覆盖并发写入**、绕过冲突检查，甚至把已升级为 thread 能力的容器绑成固定会话，破坏其话题隔离。\n\nSources: [routes/agents.ts](src/routes/agents.ts#L1208-L1470)、[routes/agents.ts](src/routes/agents.ts#L1735-L1921)、[tests/channel-binding-rest-contract.test.ts](tests/channel-binding-rest-contract.test.ts#L8-L121)\n\n## 渠道能力矩阵与前端契约\n\n前端 `im-capabilities.ts` 的能力表与后端校验一一对应（契约测试逐项镜像断言）：七个渠道全部 `can_bind_workspace: true`；仅飞书与 Telegram 声明 `supports_thread_map: true`；激活模式（`always`/`when_mentioned`/`owner_mentioned`/`auto`/`disabled`）只有具备真实入站门控的飞书、钉钉、Discord、WhatsApp 支持，Telegram/QQ/微信连接器无门控概念故不声明。设置页 `BindingsSection` 按 `conversation_kind` 区分目标：群聊只呈现"绑定到此工作区"，私聊才呈现会话绑定入口，与后端 `conversationBindingPolicyError` 的约束同源。\n\nSources: [web/src/constants/im-capabilities.ts](web/src/constants/im-capabilities.ts#L1-L101)、[tests/channel-mount-service.test.ts](tests/channel-mount-service.test.ts#L165-L190)\n\n## 边界汇总：不可违反的不变量\n\n| 不变量 | 违规后果 | 强制位置 |\n|---|---|---|\n| `target_agent_id` 与 `target_main_jid` 互斥 | 路由歧义 | `hasSessionMountConflict` / `hasWorkspaceMountConflict` |\n| 原生上下文容器只能 `thread_map`，不可绑单会话 | 话题会话丢失 | `upgradeNativeContextChannelMount` + REST 400 |\n| direct 只能绑会话、group 只能绑工作区 | 非法路由 | `conversationBindingPolicyError` |\n| 会话所有权粘性：Web/他连接器不转移 owner | 回复渠道错乱 | `resolveStickyChannelOwner` |\n| 解绑必须原子恢复默认，失败保留旧路由 | 渠道悬空 | `buildRestoreDefaultChannelMountUpdate` 先解析后提交 |\n| stale 挂载 fail-closed 不路由 | 消息错投 | `resolveChannelMountTarget` + `buildResolveEffectiveChatJid` |\n| 跨 await 写入必须基于新鲜快照 | 并发覆盖/绕过冲突检查 | routes/agents.ts fresh re-read |\n\n进一步阅读：挂载的目标工作区与会话源自 [智能体-工作区-会话三层模型](6-zhi-neng-ti-gong-zuo-qu-hui-hua-san-ceng-mo-xing)；原生话题容器的会话创建细节见 [飞书会话语义与原生话题映射](15-fei-shu-hui-hua-yu-yi-yu-yuan-sheng-hua-ti-ying-she)；入站准入与 `/pair` 配对协议属于 [IM 命令系统与消息分发](17-im-ming-ling-xi-tong-yu-xiao-xi-fen-fa) 的边界；渠道账号的连接与授权前置条件见 [IM 渠道架构与多账号连接池](13-im-qu-dao-jia-gou-yu-duo-zhang-hao-lian-jie-chi)。';export{n as default};

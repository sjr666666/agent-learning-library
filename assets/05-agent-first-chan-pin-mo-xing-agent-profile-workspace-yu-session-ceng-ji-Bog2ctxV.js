const e='本页解释 Miniclaw 的核心产品抽象：**Agent Profile** 作为顶层身份与策略所有者、**Workspace** 作为文件系统与执行隔离边界、**Runtime Session** 作为工作区内的执行记录。我们将梳理三层的职责划分、持久化载体、身份一致性机制，以及新旧两套模型之间的兼容映射。\n\n## 为什么需要三层产品模型\n\nMiniclaw 在一次显式架构迁移中确立了 Agent-first 的产品表面，同时保留了既有群组/会话运行时的兼容性。设计上有一个关键原则：**产品模型与持久化的运行时模型是刻意分离的**——Agent 是顶层产品身份和策略所有者；Workspace 是由创建者拥有的私有文件系统、路由与 host/container 隔离边界；而运行时会话只是工作区内部的执行记录，不是另一个产品级 Agent [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L12-L20)。\n\n目标层级结构如下：Agent 持有身份提示词、Claude 预设包含策略、用户 Skill/MCP 策略与渠道挂载，其下拥有若干 Workspace；每个 Workspace 是运行时隔离边界，默认含一个主会话，可选地派生会话或任务运行时会话 [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L22-L34)。这个结构直接映射到数据库中的四张核心表：`agent_profiles`、`workspaces`、`workspace_agent_profiles`（桥接表）与 `workspace_runtime_sessions` [src/db.ts](src/db.ts#L736-L765)。\n\n```mermaid\nflowchart TB\n    subgraph AGENT["Agent Profile —— 顶层身份"]\n        AP["agent_profiles<br/>四段提示词 · runtime_policy · version"]\n        APV["agent_profile_prompt_versions<br/>修订快照历史"]\n    end\n\n    subgraph WS["Workspace —— 隔离边界"]\n        W["workspaces（canonical）<br/>folder · owner_user_id · is_home"]\n        B["workspace_agent_profiles<br/>Workspace ↔ Agent 绑定桥"]\n        RS["workspace_runtime_sessions<br/>SDK 恢复元数据投影"]\n        MS["主会话 Main Session"]\n        DS["派生会话<br/>子代理 / 定时任务 / 渠道会话"]\n    end\n\n    subgraph CH["渠道入口"]\n        M["agent_channel_mounts<br/>channel_jid → Agent → Workspace/Session"]\n    end\n\n    AP == "一对多拥有" ==> W\n    APV -.版本快照.-> AP\n    B -.桥接.- AP\n    B -.桥接.- W\n    MS -.身份快照写入.-> RS\n    DS -.身份快照写入.-> RS\n    M -->|"session_id 为空：主会话"| MS\n    M -->|"session_id 非空：指定会话"| DS\n```\n\n三层职责对照：\n\n| 维度 | Agent Profile | Workspace | Runtime Session |\n|---|---|---|---|\n| 产品语义 | 顶层身份与策略所有者 | 私有文件系统与执行隔离边界 | 工作区内的执行记录 |\n| 持久化载体 | `agent_profiles` | `workspaces` + 兼容镜像 | `sessions` + `workspace_runtime_sessions` 投影 |\n| 归属关系 | 属于一个 owner 用户 | 通过桥接表绑定到一个 Agent | 从属于一个 Workspace |\n| 关键标识 | id + identity_hash + version | jid + folder | group_folder + agent_id + SDK session_id |\n| 可数量关系 | 一个用户多个 Agent（仅一个默认） | 一个 Agent 多个 Workspace | 一个 Workspace 一个主会话 + N 个派生 |\n\nSources: [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L12-L34) [src/db.ts](src/db.ts#L736-L765)\n\n## Agent Profile：顶层身份与策略所有者\n\n`agent_profiles` 表是整个产品模型的根。每行代表一个可独立编辑的智能体身份，字段包括 `owner_user_id`（所有者）、四段提示词（`identity_prompt`/`soul_prompt`/`agents_prompt`/`tools_prompt`）、`prompt_mode`（append 或 replace）、头像三元组、`model_config_id`、JSON 序列化的 `runtime_policy`、`identity_hash` 与递增的 `version`，以及 `is_default` 和 `status` [src/db.ts](src/db.ts#L919-L945)。注意代码注释中刻意区分了这张表与旧版 `agents` 表——后者存储的是工作区内的会话/任务/spawn 记录，不是顶层 Agent 概念（下文详述）[src/db.ts](src/db.ts#L889-L917)。\n\n四段提示词各有明确语义边界，这是 Agent 人格的第一等公民表达方式：\n\n| 提示词段 | 字段 | 语义定义 |\n|---|---|---|\n| IDENTITY | `identity_prompt` | 简明的角色定位与公开身份 |\n| SOUL | `soul_prompt` | 价值观、性格气质与持久的判断原则 |\n| AGENTS | `agents_prompt` | 操作规则、工作流与协作行为 |\n| TOOLS | `tools_prompt` | 工具选择与工具使用指引 |\n\nSources: [src/types.ts](src/types.ts#L284-L292)\n\n**prompt_mode 决定了这四段提示词与 Claude Code 原生预设的组合方式**：`append` 表示在预设之后追加（旧字段 `include_claude_preset=true` 是它的兼容别名），`replace` 则完全替换预设但保留平台运行时指令 [src/types.ts](src/types.ts#L293-L311)。组合语义在设计文档中有精确定义——append 时上下文为「Claude Code 预设 + Agent 身份提示词 + 工作区/上下文提示词 + 消息历史」，replace 时去掉第一层 [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L160-L172)。\n\n`runtime_policy` 是版本化的 JSON 策略对象，**刻意比"完全资源所有权"更窄**：Provider、模型与凭据始终由系统统一管理，Agent 策略不选择 Provider。策略只覆盖四个安全可圈定的维度：\n\n| 策略键 | 取值 | 含义 |\n|---|---|---|\n| `reasoning.effort` | inherit / low / medium / high / xhigh / max | 推理力度，先继承 Provider 自定义环境再落到 SDK 默认 |\n| `context.source` | managed / host_claude | 上下文来源；host_claude 为管理员专属能力 |\n| `skills` (+`skills.host`) | inherit / custom / disabled + ids | 用户 Skill 选择策略；host 子键控制原生 `~/.claude/skills` |\n| `mcp` | inherit / custom / disabled + ids | 用户 MCP 服务器选择策略 |\n\nSources: [src/types.ts](src/types.ts#L349-L374) [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L248-L267)\n\n每个用户保证存在且仅存在一个默认 Agent。`getOrCreateDefaultAgentProfile` 在读取时惰性创建默认档案（`is_default=1`），并通过部分唯一索引 `idx_agent_profiles_default` 强制「每用户至多一个激活的默认 Agent」这一不变量；若检测到历史遗留命名还会自动迁移名称并递增版本 [src/db.ts](src/db.ts#L8646-L8730) [src/db.ts](src/db.ts#L941-L943)。此外，系统内置的 Miniclaw 平台身份不由任何可编辑字段承载，而是通过 `MINICLAW_PLATFORM_IDENTITY_VERSION` 对默认档案的 identity_hash 做二次 SHA-256 绑定——受保护的身份契约变化时只需递增该版本号，现有 SDK 会话即会在其下重启 [src/agent-profile-runtime.ts](src/agent-profile-runtime.ts#L19-L33)。\n\n## 身份哈希与版本化：让每一次变更可追溯\n\n**identity_hash 是贯穿三层的身份一致性锚点**。它由四段提示词、生效的运行时策略与 Agent 名称共同计算得出；任何一处修改都会产生新哈希并使 `version` 递增 [src/db.ts](src/db.ts#L8163-L8171)。每次提示词变更都会向 `agent_profile_prompt_versions` 表写入一条不可变快照，记录当时的完整四段提示词、哈希与变更来源（`create`/`update`/`restore`/`migration`），并以 `(agent_profile_id, version)` 唯一约束保证同一版本号不会被覆写 [src/db.ts](src/db.ts#L947-L964) [src/types.ts](src/types.ts#L329-L338)。\n\n这套机制解决的是分布式系统中经典的**陈旧身份泄漏问题**：暖 Runner 在进程启动时捕获了 `containerInput.agentProfile`，如果 Agent 提示词或预设策略随后发生变化，IPC 注入的消息仍可能被旧的内存态 Runner 处理。因此设计文档将「运行时一致性」列为 Phase 0、最高风险迁移点——任何身份变更必须先停掉关联 Workspace 的全部暖 Runner 才能提交 [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L26-L33)。\n\n```mermaid\nflowchart LR\n    A["编辑 Agent 提示词/策略"] --> B["计算新 identity_hash<br/>version += 1"]\n    B --> C{"遍历绑定的<br/>Workspace"}\n    C --> D["停止暖 Runner<br/>（第一遍）"]\n    D --> E["同步提交 DB 变更"]\n    E --> F["再次停止运行时 JID<br/>（第二遍，关闭 TOCTOU 窗口）"]\n    F --> G["下次冷启动<br/>携带新身份快照"]\n```\n\nSources: [src/db.ts](src/db.ts#L8163-L8171) [src/db.ts](src/db.ts#L947-L964)\n\n## Workspace：文件系统与执行的隔离边界\n\n`workspaces` 表是新模型的规范元数据表：`jid` 为主键，`folder` 是文件系统隔离键，配合 `owner_user_id`、`name`、`status` 与 `is_home` 标记 [src/db.ts](src/db.ts#L736-L747)。**folder 是持久化标识符而非任意文件系统路径**——它必须匹配正则 `/^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/`，注册与所有运行时路径构建器共享这一契约，确保创建时接受的 folder 在清理阶段也能被安全处理，杜绝路径穿越 [src/workspace-folder.ts](src/workspace-folder.ts#L5-L14)。物理布局上，每个 folder 在数据目录中派生出独立的 `groups/{folder}/`、`sessions/{folder}/.claude/` 与 `ipc/{folder}/` 子树 [CLAUDE.md](CLAUDE.md#L220-L238)。\n\nWorkspace 与 Agent 的归属关系由桥接表 `workspace_agent_profiles` 承载：以 `group_folder` 为主键映射到 `agent_profile_id`，同时记录该绑定上的交互契约 `interaction_mode`（`assistant` 被动应答或 `proactive` 主动介入，CHECK 约束限定取值）[src/db.ts](src/db.ts#L986-L996) [src/types.ts](src/types.ts#L340-L347)。治理视角则通过 `/api/workspaces` 暴露：序列化函数按权限裁剪输出——能检查治理信息的调用者可见 `execution_mode`、`interaction_mode`、嵌入的 Agent 快照（id/name/version/hash/policy）、运行时会话数与渠道挂载数，普通成员只能看到基础摘要 [src/routes/workspaces.ts](src/routes/workspaces.ts#L103-L137)。\n\n值得注意的是访问判定中的一个防御细节：Workspace 解析要求 `jid` 以 `web:` 前缀开头，host 执行模式的 Workspace 仅管理员或 home 创建者可见，最终还要通过 `canAccessGroup` 的 ACL 校验——**结构性可见性与授权是两层独立的检查** [src/routes/workspaces.ts](src/routes/workspaces.ts#L28-L45)。\n\nSources: [src/db.ts](src/db.ts#L736-L747) [src/workspace-folder.ts](src/workspace-folder.ts#L5-L14) [src/db.ts](src/db.ts#L986-L996)\n\n## Runtime Session：执行记录而非第二个 "Agent"\n\n这是三层模型中最容易被误解的一层，代码库用命名纪律反复强调它的定位。`workspace_runtime_sessions` 表的建表注释明确写道：这些行只是从旧 `sessions` 表投影出的 **SDK/Provider 恢复状态**，被刻意不命名为 sessions——因为产品级对话会话存在于别处，二者不能混淆 [src/db.ts](src/db.ts#L748-L763)。API 层面同样贯彻这一纪律：对外统一使用 `runtime_sessions`、`runtime_session_count` 与 `/api/workspaces/:jid/runtime-sessions` 命名，避免与产品会话混淆 [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L226-L228)。\n\n每个 Workspace 默认拥有一个主会话；额外的运行时会话为三类场景而存在：spawn 出的子代理、定时任务，以及需要的渠道特定对话 [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L116-L124)。底层由旧 `sessions` 表承载，主键为 `(group_folder, agent_id)`——即同一个 folder 内按 `agent_id` 区分主会话与各派生会话，迁移后追加了 `agent_profile_id`/`agent_profile_version`/`identity_hash` 三个身份列 [src/db.ts](src/db.ts#L665-L670) [src/db.ts](src/db.ts#L1964-L1966)。\n\n**每次会话落库时都会原子写入一份身份快照**。`setSession` 在单个事务内完成三件事：upsert 会话行、写入 `{agentProfileId, agentProfileVersion, identityHash}` 身份三元组、调用 `syncWorkspaceRuntimeSessionProjection` 将行同步投影到规范镜像表 [src/db.ts](src/db.ts#L7784-L7815)。当后续运行发现快照哈希与 Agent 当前哈希不一致时，既有的 mismatch 重置逻辑会在下一次冷运行时清理会话行并注入近期历史，而不是急切删除数据 [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L31-L33)。\n\n定时任务的上下文语义也遵循同样的隔离哲学：默认 `isolated` 模式复用源 Workspace 的目录、环境、Agent 身份与执行模式，但创建 run 范围的队列 JID、Claude 会话与 IPC 命名空间，运行后清理以防污染主会话；`group` 模式则直接把消息注入源 Workspace 主会话 [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L269-L279)。\n\nSources: [src/db.ts](src/db.ts#L748-L763) [src/db.ts](src/db.ts#L7784-L7815) [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L116-L124)\n\n## 渠道挂载：外部消息如何进入层级\n\n渠道是外部消息入口，在目标模型中**挂载点属于 Agent 层级**，再路由到具体的 Workspace/会话。规范的镜像表 `agent_channel_mounts` 以 `channel_jid` 为主键，记录 `agent_profile_id`、`owner_user_id`、`workspace_jid/folder`、`session_id` 以及路由模式、回复策略、激活模式、受众模式等路由属性，并在 profile、workspace、session 三个方向都建有索引 [src/db.ts](src/db.ts#L766-L784)。迁移期间兼容字段保持双写——工作区群聊挂载走 `channel_mounts`，Agent/会话挂载走 `agent_channel_mounts`，规则要求不得只更新其中一侧 [CLAUDE.md](CLAUDE.md#L201-L205)。\n\n挂载的目标解析有两条路径，取决于 `session_id` 是否非空：非空时是会话级挂载，解析出形如 `{chat_jid}#agent:{sessionId}` 的 effectiveJid 并校验会话与其所属 Workspace 的真实存在性（缺失则返回 stale 状态及原因）；为空时是工作区级挂载，直接落在主会话上 [src/channel-mount-service.ts](src/channel-mount-service.ts#L412-L467)。进入会话后的所有权遵循**会话粒度的粘性归属**：Web 表面可以向 IM 拥有的会话注入输入，但不能改变传输所有权；同一会话内回复锚点跟随当前消息（避免首个消息恰好带 thread ID 就永久锁死话题），而跨会话、跨账号或跨 Provider 仍保持原始所有者不变 [src/channel-session-owner.ts](src/channel-session-owner.ts#L5-L35)。\n\nSources: [src/db.ts](src/db.ts#L766-L784) [src/channel-mount-service.ts](src/channel-mount-service.ts#L412-L467)\n\n## 一致性保障：热 Runner 静默、串行锁与策略收敛\n\n围绕 Agent 的变更操作建立了一套三重一致性机制。第一重是**两遍静默协议**：`quiesceWorkspaceRunnersAroundCommit` 先停止受影响运行时，同步提交身份/所有权变更，然后对相同（加上新发现的）运行时 JID 再停一遍——第二遍关闭了"先停后写"的 TOCTOU 窗口；配合引用计数的变更暂停门，第一遍之后新建的 Runner 要么在第二遍开始时已存在并被等待退出，要么只在暂停释放后才启动从而观察到新持久化状态 [src/agent-profile-runtime.ts](src/agent-profile-runtime.ts#L240-L258)。失败路径也有明确分级：提交前失败抛出 `pre_commit` 错误，提交成功但清理失败则标记 `post_commit` 并允许调用方安装持久化的 fail-closed 队列门 [src/agent-profile-runtime.ts](src/agent-profile-runtime.ts#L94-L111)。\n\n第二重是 **profile 粒度的串行锁**。`withAgentProfileLocks` 按 AgentProfile 序列化成员/配置变更，多把锁总是按字典序获取，因此相反方向的 A→B/B→A 迁移不会死锁；引用计数包含排队持有者，异常路径也会释放 [src/agent-profile-runtime.ts](src/agent-profile-runtime.ts#L154-L179)。\n\n第三重是**运行时的策略收敛**，这是最容易被忽视的安全设计：`resolveEffectiveAgentProfile` 在执行时才解析安全敏感策略——持久化的档案数据永远不足以构成授权，用户角色降级无需重写每条历史档案即可立即生效。具体而言，非活跃管理员失去 host 资源使用权后，其 `context.source` 强制回落 `managed`、请求中的 host Skills 被 fail-closed 地置为 disabled（但保留所请求的 id 供审计与编辑），最后基于生效策略重算 identity_hash [src/agent-profile-runtime.ts](src/agent-profile-runtime.ts#L44-L92)。收敛后的身份经由 stdin 引导载荷注入 Runner——`ContainerInput.agentProfile` 携带 id、name、version、identityHash、identityPrompt、includeClaudePreset、modelConfigId 与 runtimePolicy 全量快照 [src/container-runner.ts](src/container-runner.ts#L381-L393)，Runner 侧将其包裹为 `<agent-identity>` XML 结构注入系统提示词，并声明四段提示词不能覆盖平台安全规则与工具约束 [container/agent-runner/src/index.ts](container/agent-runner/src/index.ts#L300-L317)；同一份哈希还进入运行时上下文审计记录，附带对 runtimePolicy 单独计算的 SHA-256 [container/agent-runner/src/index.ts](container/agent-runner/src/index.ts#L345-L362)。\n\nSources: [src/agent-profile-runtime.ts](src/agent-profile-runtime.ts#L240-L258) [src/agent-profile-runtime.ts](src/agent-profile-runtime.ts#L154-L179) [src/container-runner.ts](src/container-runner.ts#L381-L393)\n\n## 新旧模型的兼容映射\n\n迁移采取"规范镜像 + 写穿兼容"策略：新表作为 canonical 读镜像，旧字段在所有读取方完成迁移前仍是路由事实来源，两侧必须同事务更新。核心映射关系如下：\n\n| 旧模型（兼容层） | 新模型 | 迁移状态 |\n|---|---|---|\n| `registered_groups.folder` | `workspaces` 规范行（jid/folder 键） | 回填完成，双写保留 |\n| `registered_groups.target_main_jid` / `reply_policy` 等 | `agent_channel_mounts` 规范镜像 | 绑定/解绑经单一提交服务同步两侧并刷新路由缓存 |\n| `sessions` 行 | `workspace_runtime_sessions` 投影 | 由 `setSession` 同步维护 |\n| `workspace_agent_profiles` | Workspace→Agent 桥接（本身即新表） | 迁移期一次性回填 |\n| 旧 `agents` 表 | 保留为会话/任务/spawn 模型，非顶层 Agent | 明确区隔，不复用 |\n| 无 | Workspace 迁移需预检、非默认 Agent 删除前须先迁走全部 Workspace | 治理 UI 强制 |\n\nSources: [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L214-L245) [docs/agent-first-architecture-plan.md](docs/agent-first-architecture-plan.md#L200-L212)\n\n## 小结\n\nMiniclaw 用三个各司其职的抽象构成了清晰的产品骨架：**Agent Profile** 以四段提示词加收窄的运行时策略定义"它是谁"，以 identity_hash 与版本链保证"它何时变了"；**Workspace** 以受验证的 folder 契约实现文件系统与执行的双重隔离；**Runtime Session** 则严格定位为执行记录，通过身份快照与投影同步维持可恢复性。三者之间的每一次跨层变更都被两遍静默协议、字典序串行锁与运行时策略收敛三重机制保护，而新旧模型间的兼容映射保证了迁移过程零破坏。理解了这套层级，后续章节的执行引擎、渠道路由与权限体系都将在此骨架上展开。\n\n建议按以下路径继续阅读：先回到全局看四方的边界划分 [整体架构：Backend、Pi Runner、Workspace 与客户端的边界划分](4-zheng-ti-jia-gou-backend-pi-runner-workspace-yu-ke-hu-duan-de-bian-jie-hua-fen)，再深入本文多次引用的四段提示词组装细节 [四段 Prompt 体系：IDENTITY、SOUL、AGENTS、TOOLS 的组装与上下文预算](12-si-duan-prompt-ti-xi-identity-soul-agents-tools-de-zu-zhuang-yu-shang-xia-wen-yu-suan)；想了解本文涉及的核心表族的全貌请读 [SQLite 持久化：Schema 版本化、迁移策略与核心表族](9-sqlite-chi-jiu-hua-schema-ban-ben-hua-qian-yi-ce-lue-yu-he-xin-biao-zu)，渠道挂载与多账号绑定边界的展开则在 [多账号管理与绑定边界：群聊挂载、私聊 Session 与渠道身份归属](16-duo-zhang-hao-guan-li-yu-bang-ding-bian-jie-qun-liao-gua-zai-si-liao-session-yu-qu-dao-shen-fen-gui-shu)。';export{e as default};

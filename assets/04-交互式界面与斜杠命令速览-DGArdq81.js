const n='当你运行 `minicode` 时，看到的不是一行简单的问答提示符，而是一个完整的全屏终端交互界面（TUI）。这一页带你快速认识这个界面的每个区域、常用键盘操作，以及全部斜杠命令的分类与用途。读完这一页，你就能自信地在 MiniCode 里开始第一次真正的对话。\n\n## 界面长什么样：全屏 TUI 的四个区域\n\nMiniCode 检测到标准输入和输出都是 TTY 时，会进入全屏交互模式：先切换到终端的备用屏幕（alternate screen），隐藏光标，并开启鼠标追踪，然后绘制整个界面。退出时会恢复原屏幕并打印 session ID 供以后恢复。如果不在 TTY 中（例如管道输入），则退化为逐行 readline 模式，行为保持兼容。\n\nSources: [src/index.ts](src/index.ts#L60-L135)、[src/tui/screen.ts](src/tui/screen.ts#L30-L40)、[src/index.ts](src/index.ts#L152-L273)\n\n整个画面自上而下由四个面板组成，渲染代码按固定顺序拼接每一帧，并通过「逐行 diff」只重绘发生变化的行，避免整屏闪烁：\n\n```mermaid\nflowchart TD\n    A["Header 面板（MiniCode 横幅）"] --> B["Session feed 面板（对话流）"]\n    B --> C["Permission 摘要行"]\n    C --> D["Prompt 面板（输入行 + slash 菜单）"]\n    D --> E["Footer 状态栏"]\n    A --> A1["徽章：session / provider / model / messages / events / ctx / skills / mcp"]\n    B --> B1["按 事件类型 着色：you / Minicode / tool / progress"]\n    E --> E1["左侧：Ready/Thinking/工具状态 + 右侧：tools / skills / mcp / shells"]\n```\n\n- **Header 横幅**：一行彩色徽章展示当前会话的即时快照 —— `session`（本地会话）、`provider`（供应商域名）、`model`（模型名）、`messages`（消息数）、`events`（事件数）、`ctx`（上下文占用百分比与来源标记 `usage`/`usage+est`/`est`）、`skills`（技能数）、`mcp`（已连接/总数）。上下文占用率用 10 格进度条表示，颜色随 warning 级别从绿到红变化。\n- **Session feed 面板**：即对话流（transcript）。用户消息以 `you` 开头并带底色，助手回复以 `Minicode` 开头，工具调用显示 `tool <名称> running/ok/err`，特殊阶段显示 `progress`。条目之间用蓝色圆点分隔，超长工具输出会被折叠成预览并在末尾标注截断。\n- **Permission 摘要行**：以暗色小字显示当前额外的允许目录与危险命令 allowlist 概况。\n- **Prompt 面板**：绿色的 `mini-code prompt>` 提示符、光标与输入文本，以及当输入以 `/` 开头时自动弹出的斜杠命令菜单。\n- **Footer 状态栏**：左侧显示状态（空闲时 `Ready` 带轮播圆点，思考中显示 `Thinking` 并附带已耗时秒数，工具执行中显示 `Running <tool>...`）；右侧显示 tools/skills 开关、MCP 服务器连接状况、后台 shell 数量以及压缩状态提示。\n\nSources: [src/tty-app.ts](src/tty-app.ts#L746-L852)、[src/tui/chrome.ts](src/tui/chrome.ts#L249-L301)、[src/tui/transcript.ts](src/tui/transcript.ts#L161-L198)、[src/tui/chrome.ts](src/tui/chrome.ts#L370-L408)、[src/tui/chrome.ts](src/tui/chrome.ts#L214-L247)\n\n启动时界面还会播放一段 ASCII 奶酪小鼠动画作为欢迎仪式（`Welcome back!~`），随后在对话流中展示已加载的 memory 指令文件列表，以及发现的已保存会话（提示输入 `/resume` 恢复）。\n\nSources: [src/tty-app.ts](src/tty-app.ts#L156-L222)、[src/tty-app.ts](src/tty-app.ts#L534-L590)、[src/tty-app.ts](src/tty-app.ts#L1704-L1750)\n\n## 输入行与斜杠命令菜单：边输入边补全\n\n输入行是交互的核心入口。它的工作方式非常接近现代终端工具：\n\n- 空输入时右侧会轮播提示语，例如「Enter send; /help commands; Esc clear; Ctrl+C exit」；一旦有输入，提示语自动消失。\n- 当你输入 `/` 开头的文本时，界面会在输入行下方弹出**斜杠命令菜单**：根据已输入的前缀实时过滤匹配的命令（例如输入 `/mo` 会列出 `/model` 和 `/model <model-name>`）。\n- 菜单中的当前选中项用反色高亮。按 `↑`/`↓` 切换选中项，按 `Tab` 或 `Enter` 把选中的命令用法回填到输入行，再按 `Enter` 执行；若 `Enter` 时输入内容已是完整命令用法，则直接提交。\n\nSources: [src/tui/input.ts](src/tui/input.ts#L9-L41)、[src/tui/chrome.ts](src/tui/chrome.ts#L410-L429)、[src/tty-app.ts](src/tty-app.ts#L2183-L2271)\n\n`Enter` 提交后，输入解析器负责把终端原始字节流翻译成结构化事件：它能识别方向键、PageUp/PageDown、Home/End、Ctrl 组合键（`Ctrl+A/E/N/P/U/C`）与 Meta 修饰键，还能解析鼠标点击、拖拽与滚轮事件。特别地，**多行粘贴**会被识别出来——粘贴内容里的换行会被当作普通文本而非「回车提交」，确保整段代码原样进入输入框。\n\nSources: [src/tui/input-parser.ts](src/tui/input-parser.ts#L211-L288)、[src/tui/input-parser.ts](src/tui/input-parser.ts#L46-L48)、[test/input-parser.test.ts](test/input-parser.test.ts#L6-L28)\n\n## 键盘与鼠标快捷键速查\n\n| 按键 | 作用 | 说明 |\n| --- | --- | --- |\n| `Enter` | 提交输入 / 回填选中命令 | 若斜杠菜单有选中项则先回填用法，再按一次执行 |\n| `Tab` | 自动补全选中命令 | 直接填入菜单中高亮的命令用法 |\n| `↑` / `↓` | 切换斜杠菜单项；否则浏览输入历史 | 菜单打开时优先切换菜单 |\n| `Meta+↑` / `Meta+↓` | 滚动对话流 | Alt/Option + 方向键 |\n| `PageUp` / `PageDown` | 对话流上/下翻页 | 每页滚动 8 行 |\n| `Ctrl+P` / `Ctrl+N` | 上一条/下一条输入历史 | 与方向键互补 |\n| `Ctrl+A` / `Ctrl+E` | 光标移到行首/行尾 | 输入为空时改为跳转对话流顶部/底部 |\n| `Ctrl+U` | 清空当前输入 | 快速重来 |\n| `←` / `→` | 光标左/右移动 | 支持行内编辑 |\n| `Backspace` / `Delete` | 删除光标前/后字符 | |\n| `Esc` | 清空输入 | 也用于取消会话选择器 |\n| `Ctrl+C` | 退出 MiniCode | 任何界面（含审批弹窗）下均有效 |\n| 鼠标滚轮 | 滚动对话流 | 依赖终端开启鼠标追踪 |\n| 鼠标拖拽 | 选中对话流文本并复制 | 松手后自动写入系统剪贴板 |\n\n历史记录按工作目录与会话持久化，`↑`/`↓` 导航时会保留你正在输入但未提交的草稿（`historyDraft`），回退到历史后再向下走会回到草稿，不会丢失。\n\nSources: [src/tty-app.ts](src/tty-app.ts#L2094-L2395)、[src/tty-app.ts](src/tty-app.ts#L465-L492)、[src/tty-app.ts](src/tty-app.ts#L352-L379)、[src/tty-app.ts](src/tty-app.ts#L2110-L2168)\n\n## 斜杠命令全景：四类命令一次看全\n\n斜杠命令的注册表集中在 `SLASH_COMMANDS` 数组中，每个命令由 `name`、`usage`、`description` 三要素组成；`/help` 与输入裸 `/` 都会打印全部命令的用法清单。命令按职责可以分为四类：\n\n| 类别 | 命令 | 作用 |\n| --- | --- | --- |\n| **信息查询** | `/help` | 展示全部可用斜杠命令 |\n| | `/tools` | 列出当前可用的工具及本地快捷指令 |\n| | `/status` | 显示当前模型、baseUrl、认证方式与配置来源 |\n| | `/model` | 查看当前模型 |\n| | `/model <name>` | 把模型覆盖写入 `~/.mini-code/settings.json` 持久化 |\n| | `/config-paths` | 显示 mini-code 与 Claude 兼容回退的配置路径 |\n| | `/permissions` | 显示权限存储路径与当前权限摘要 |\n| | `/skills` | 列出发现的 SKILL.md 工作流 |\n| | `/mcp` | 显示配置的 MCP 服务器及连接状态、工具/资源/提示数量 |\n| **文件与命令快捷** | `/ls [path]` | 直接列出目录文件 |\n| | `/grep <pattern>::[path]` | 直接搜索文本 |\n| | `/read <path>` | 直接读取文件 |\n| | `/write <path>::<content>` | 直接写入文件 |\n| | `/modify <path>::<content>` | 替换文件，先展示可审阅的 diff |\n| | `/edit <path>::<search>::<replace>` | 精确替换单处内容 |\n| | `/patch <path>::<s1>::<r1>::<s2>::<r2>...` | 一次对同一文件做多处替换 |\n| | `/cmd [cwd::]<command> [args...]` | 直接执行受允许的开发命令，可指定目录 |\n| **会话管理** | `/resume` / `/resume <id>` | 打开会话选择器 / 直接恢复指定会话 |\n| | `/rename <name>` | 重命名当前会话 |\n| | `/new` | 清空当前会话，开启全新对话 |\n| | `/fork` | 把当前会话分叉为独立副本 |\n| | `/exit` | 退出 MiniCode |\n| **上下文管理** | `/compact` | 手动压缩对话上下文，释放 context window |\n| | `/collapse` | 把安全的旧段落投影为摘要，保留原始 transcript |\n| | `/snip` | 不调用模型，确定性移除中间段安全上下文 |\n| **项目与记忆** | `/init` | 为当前项目生成 `.mini-code/`、`.gitignore` 条目与 `MINI.md`（幂等） |\n| | `/memory` | 显示当前加载进系统提示的指令文件 |\n\nSources: [src/cli-commands.ts](src/cli-commands.ts#L19-L155)、[src/cli-commands.ts](src/cli-commands.ts#L157-L165)\n\n其中「文件与命令快捷」这类命令的解析逻辑独立于命令注册表，位于 `local-tool-shortcuts.ts`：它们把 `/read`、`/edit`、`/cmd` 这类输入直接翻译成对内置工具的调用并立即执行，绕过了模型，适合「我只想快速看一眼文件/改一行字」的场景。详细用法见 [内置工具与本地快捷指令](5-nei-zhi-gong-ju-yu-ben-di-kuai-jie-zhi-ling)。\n\nSources: [src/local-tool-shortcuts.ts](src/local-tool-shortcuts.ts#L17-L134)\n\n## 命令分发：输入提交后发生了什么\n\n你按下 `Enter` 后，MiniCode 按固定优先级依次判断输入属于哪一类——本地命令优先于模型推理，模型只处理最终落到「普通对话」的输入。完整分发流程如下：\n\n```mermaid\nflowchart TD\n    A["提交输入"] --> B{"输入 === /exit ?"}\n    B -->|是| Z["退出 TUI"]\n    B -->|否| C{"压缩类命令<br/>/compact /collapse /snip ?"}\n    C -->|是| C1["本地处理上下文<br/>并写会话日志"]\n    C -->|否| D{"会话类命令<br/>/rename /resume /new /fork ?"}\n    D -->|是| D1["本地处理会话<br/>/resume 打开选择器"]\n    D -->|否| E["记录输入历史"]\n    E --> F{"/tools ?"}\n    F -->|是| F1["列出工具清单"]\n    F -->|否| G{"本地查询命令<br/>/help /status /mcp ... ?"}\n    G -->|是| G1["tryHandleLocalCommand<br/>返回文本结果"]\n    G -->|否| H{"工具快捷指令<br/>/ls /read /edit /cmd ... ?"}\n    H -->|是| H1["解析为工具调用<br/>executeToolShortcut"]\n    H -->|否| I{"以 / 开头 ?"}\n    I -->|是| I1["未识别 → 前缀匹配<br/>模糊提示最接近命令"]\n    I -->|否| J["作为用户消息<br/>进入 model → tool → model 代理循环"]\n```\n\n- **压缩类命令**（`/compact`、`/collapse`、`/snip`）在 `handleInput` 里独立处理，结果（节省的 token、移除的消息数）会以助手消息的形式回写到对话流，并在底部状态栏短暂显示如 `ctx -35% (saved 12K tokens)` 的压缩报告。\n- **会话类命令**（`/rename`、`/resume`、`/new`、`/fork`）同样在本地完成：`/resume` 无参数时弹出全屏会话选择器，支持 `↑/↓` 选择、`Enter` 恢复、`d` 删除、`Tab` 切换到「所有项目」视图、`Esc` 取消。\n- **未识别斜杠命令**不会进入模型，而是用前缀匹配给出最接近的候选（例如输入 `/coll` 会提示 `/collapse`）。\n- **普通文本**才会真正作为用户消息发送给模型，进入代理循环执行多步工具调用。\n\nSources: [src/tty-app.ts](src/tty-app.ts#L1011-L1131)、[src/tty-app.ts](src/tty-app.ts#L1133-L1369)、[src/tty-app.ts](src/tty-app.ts#L783-L817)、[src/tty-app.ts](src/tty-app.ts#L1323-L1345)\n\n## 状态反馈：随时知道程序在干什么\n\n界面通过底部状态栏与头部徽章持续反馈运行状态，避免你在「卡住」和「正在思考」之间产生困惑：\n\n- 空闲时左侧显示 `Ready`（圆点每 1 秒轮播一次）；思考中显示黄色的 `Thinking` 并附带已耗秒数（例如 `Thinking(12s)`）；工具执行时显示 `Running <tool>...`，同屏还会出现一个 `activity` 面板列出正在运行的工具。\n- 右侧的 `tools` / `skills` 徽章以绿/红显示开关状态，`mcp srv` 显示已连接/总数与暴露的工具数，有后台 shell 时显示 `shells N`，压缩刚完成时短暂显示压缩百分比。\n- 工具面板（activity）会保留最近 5 个工具的执行结果（`ok`/`err`），让你快速确认刚才哪一步成功了、哪一步失败了。\n\nSources: [src/tui/chrome.ts](src/tui/chrome.ts#L329-L368)、[src/tty-app.ts](src/tty-app.ts#L294-L301)、[src/tty-app.ts](src/tty-app.ts#L1383-L1402)\n\n## 下一步阅读\n\n你已经掌握了界面的骨架和命令的地图。建议按这个顺序继续深入：\n\n- [内置工具与本地快捷指令](5-nei-zhi-gong-ju-yu-ben-di-kuai-jie-zhi-ling)——`/ls`、`/edit`、`/cmd` 等快捷指令对应的底层工具与参数详解\n- [会话管理入门：保存、恢复、重命名与分叉](6-hui-hua-guan-li-ru-men-bao-cun-hui-fu-zhong-ming-ming-yu-fen-cha)——`/resume`、`/fork` 背后的持久化机制\n- [TUI 架构：状态机驱动的终端界面](25-tui-jia-gou-zhuang-tai-ji-qu-dong-de-zhong-duan-jie-mian)——本页界面的状态机与渲染管线深挖\n- [输入解析器：转义序列、鼠标事件与多行粘贴](26-shu-ru-jie-xi-qi-zhuan-yi-xu-lie-shu-biao-shi-jian-yu-duo-xing-nian-tie)——输入如何从字节流变成结构化事件';export{n as default};

const n="# MCP 客户端池 | lukilabs/craft-agents-oss\n\n\n---\nMCP 客户端池是 Craft Agents 中所有模型上下文协议 (MCP) 来源集成的集中式连接管理器。它运行在主进程（Electron 或无头服务器）中，并作为每个远程 MCP 服务器和进程内 API 来源的唯一管理节点。该池并未让每个 Agent 后端独立管理各自的 MCP 连接，而是提供了一个共享的多路复用层，后端通过代理工具定义来使用该层。\n\n来源：[mcp-pool.ts](packages/shared/src/mcp/mcp-pool.ts#L1-L15)\n\n## 架构概览[](#架构概览)\n\n该池解决了三个基础问题：**连接去重**（多个会话复用同一个 Linear 或 GitHub 连接）、**凭证安全**（主进程直接持有令牌，不使用凭证缓存文件）以及**传输协议无关性**（HTTP、SSE、stdio 和进程内来源均汇聚在同一接口之后）。该设计遵循代理模式——每个来源的工具会以命名空间标识符 (`mcp__{slug}__{toolName}`) 的形式重新导出，因此后端无需了解传输机制。\n\n来源：[mcp-pool.ts](packages/shared/src/mcp/mcp-pool.ts#L101-L127), [pool-server.ts](packages/shared/src/mcp/pool-server.ts#L1-L21)\n\n## 模块结构[](#模块结构)\n\nMCP 子系统组织在 `packages/shared/src/mcp/` 目录下，包含四个文件，各自在连接生命周期中承担不同的职责：\n\n文件\n\n职责\n\n[mcp-pool.ts](packages/shared/src/mcp/mcp-pool.ts)\n\n核心池——连接生命周期、同步对账、工具发现、执行路由\n\n[client.ts](packages/shared/src/mcp/client.ts)\n\n`CraftMcpClient` —— HTTP/SSE/stdio 传输客户端；`PoolClient` 接口；环境变量安全\n\n[api-source-pool-client.ts](packages/shared/src/mcp/api-source-pool-client.ts)\n\n`ApiSourcePoolClient` —— 通过 `InMemoryTransport` 实现的进程内 MCP 服务器桥接\n\n[pool-server.ts](packages/shared/src/mcp/pool-server.ts)\n\n`McpPoolServer` —— 向外部 SDK 子进程暴露池工具的 HTTP 端点\n\n第五个文件 [validation.ts](packages/shared/src/mcp/validation.ts) 提供了连接前的健康检查，使用 Claude Agent SDK 的 `mcpServerStatus()` 方法和直接的工具列表功能——它会根据 Anthropic 的属性命名约束验证 schema，并为 UI 生成带类型的错误结果。\n\n来源：[index.ts](packages/shared/src/mcp/index.ts#L1-L5)\n\n## PoolClient 接口[](#poolclient-接口)\n\n远程和进程内客户端都实现了 `PoolClient` 接口，这也是该池所依赖的唯一契约。这种抽象意味着，添加新的传输类型（如 WebSocket、gRPC 等）只需实现三个方法，并通过现有的 `connect` / `connectInProcess` 入口点进行注册即可。\n\nTYPESCRIPT\n\nCopy code\n\n```\nexport interface PoolClient {\n  listTools(): Promise<Tool[]>;\n  callTool(name: string, args: Record<string, unknown>): Promise<unknown>;\n  close(): Promise<void>;\n}\n```\n\n这两个具体实现在建立传输连接的方式上存在根本区别：\n\n**CraftMcpClient** 封装了官方的 `@modelcontextprotocol/sdk` 的 `Client` 类，对于远程服务器使用 `StreamableHTTPClientTransport`，对于基于本地子进程的服务器则使用 `StdioClientTransport`。它在连接时通过列出工具来执行健康检查，并在 `close()` 时干净地拆除连接。一个关键的安全细节：在生成 stdio 传输时，客户端会在将 `process.env` 传递给子进程之前，过滤掉敏感的环境变量（API 密钥、令牌、AWS 凭证）。`BLOCKED_ENV_VARS` 列表与 `session-tools-core` 中的副本保持同步 ([client.ts](packages/shared/src/mcp/client.ts#L43-L60))。\n\n**ApiSourcePoolClient** 使用 `InMemoryTransport.createLinkedPair()` 来桥接进程内的 `McpServer` 实例（通过 `createSdkMcpServer` 从 OpenAPI 定义创建）。客户端和服务器端分别连接到各自在内存管道中的对应端，从而为 API 来源实现零网络开销的工具执行。\n\n来源：[client.ts](packages/shared/src/mcp/client.ts#L62-L70), [client.ts](packages/shared/src/mcp/client.ts#L72-L153), [api-source-pool-client.ts](packages/shared/src/mcp/api-source-pool-client.ts#L15-L52)\n\n## 连接生命周期[](#连接生命周期)\n\n该池通过五个内部映射表管理连接，这些映射表共同构成了其完整的状态：\n\n映射表\n\n键\n\n值\n\n用途\n\n`clients`\n\n来源 slug\n\n`PoolClient`\n\n活跃连接\n\n`activeConfigs`\n\n来源 slug\n\n`SdkMcpServerConfig`\n\n用于变更检测的配置快照\n\n`toolCache`\n\n来源 slug\n\n`Tool[]`\n\n缓存的工具列表（避免重复拉取）\n\n`proxyTools`\n\n`mcp__{slug}__{name}`\n\n`{ slug, originalName }`\n\n用于工具执行的反向路由\n\n连接通过三个入口点建立。`connect(slug, config)` 根据一个 `SdkMcpServerConfig` 创建 `CraftMcpClient`，通过 `sdkConfigToClientConfig()` 转换配置，将 HTTP/SSE 统一规范化为单一的 `http` 传输类型。`connectInProcess(slug, mcpServer)` 使用 `ApiSourcePoolClient` 封装一个 `McpServer` 实例。两者都会委托给共享的 `registerClient()` 方法，该方法会调用 `listTools()`（触发延迟连接）、缓存结果，并构建代理工具名称映射。\n\n断开连接同样简洁：`disconnect(slug)` 会关闭客户端，移除该 slug 的所有代理工具条目，并清除配置和工具缓存。`disconnectAll()` 会在所有活跃客户端上并行执行关闭调用。\n\n来源：[mcp-pool.ts](packages/shared/src/mcp/mcp-pool.ts#L101-L112), [mcp-pool.ts](packages/shared/src/mcp/mcp-pool.ts#L155-L209)\n\n## 同步：声明式对账[](#同步声明式对账)\n\n`sync()` 方法是保持池与用户配置来源对齐的主要 API。`sync()` 无需调用方手动跟踪需要添加或移除的来源，而是接收_期望_状态并执行三阶段对账：\n\n1.  **断开已移除的来源** —— 拆除当前已连接但不在期望集合中的 slug。\n2.  **连接新来源** —— 建立尚未连接的 slug。对于已连接的来源，会通过 `mcpConfigChanged()` 检查其配置是否发生变更（URL 变更、令牌刷新），该方法会比较类型、URL 和 `Authorization` 头部，但会刻意跳过 stdio 来源，因为它们不携带 OAuth 令牌。\n3.  **连接新的 API 来源** —— 进程内服务器遵循相同的幂等的“若不存在则连接”逻辑。\n\n该方法返回一个连接失败的 slug 数组，使调用方能够展示有针对性的错误消息。它还会在最后调用 `onToolsChanged` 回调，以此向下游消费者（后端、池服务器）发出工具格局已发生变化的信号。\n\n`sync()` 方法会基于 `isLocalMcpEnabled(workspaceRootPath)` 过滤 stdio 来源——禁用了本地 MCP 执行的工作区将静默跳过所有 stdio 类型的来源且不报错。这是一个工作区范围的安全门控，而非全局设置。\n\n来源：[mcp-pool.ts](packages/shared/src/mcp/mcp-pool.ts#L236-L308), [mcp-pool.ts](packages/shared/src/mcp/mcp-pool.ts#L85-L99)\n\n## 工具发现与代理注册[](#工具发现与代理注册)\n\n后端完全不与池的传输层交互。相反，它们接收的是**代理工具定义**——包含命名空间名称、描述和 JSON Schema 的轻量级对象——并将其注册到自己的工具执行系统中。\n\n`getProxyToolDefs(slugs?)` 会遍历工具缓存并生成 `ProxyToolDef` 对象。每个定义都会从输入 schema 中剥离 `$schema` 键（这会导致 Pi agent 子进程中的 AJV 验证失败），并且在未提供 schema 时，替换为空的 `{ type: 'object', properties: {} }` schema。`mcp__{slug}__{toolName}` 的命名约定至关重要：Claude Agent SDK 会添加自己的 `mcp__{serverKey}__` 前缀，因此池将来源 slug 作为服务器键并配合原始工具名称使用，从而生成正确的最终名称，例如 `mcp__linear__createIssue`。\n\n对于 ClaudeAgent，`createSourceProxyServers()` 函数 ([claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L406-L440)) 会使用从 MCP 工具的 JSON Schema 派生出的 Zod schema，将这些代理定义封装为 SDK 的 `tool()` 实例。对于 PiAgent，代理定义会被序列化并通过 JSONL 发送到子进程以供模型感知，其执行则通过池的 `callTool()` 方法路由回传。\n\n来源：[mcp-pool.ts](packages/shared/src/mcp/mcp-pool.ts#L339-L358), [claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L400-L440)\n\n## 工具执行流水线[](#工具执行流水线)\n\n当后端调用 `callTool(proxyName, args)` 时，池会通过 `proxyTools` 映射表解析代理名称以恢复原始的 slug 和工具名称，然后委托给相应的 `PoolClient`。执行流水线包含三个后处理阶段：\n\n**内容块标准化** —— MCP 工具结果可能包含混合的内容块（文本、图像、音频）。文本块会被作为字符串收集（对于不合规的服务器，会回退使用 JSON.stringify）。二进制块（图像/音频）会进行 base64 解码，通过 `detectExtensionFromMagic()` 利用魔数字节识别其类型，经过清理后保存到会话的下载目录中。\n\n**大型响应保护** —— 如果合并后的文本超过阈值，`guardLargeResult()` 将被触发。它要么将响应保存到磁盘并返回截断后的路径引用，要么调用 `summarizeCallback`（通过 `setSummarizeCallback()` 设置）以生成 LLM 生成的摘要。这可以防止因 MCP 来源返回大型负载而导致上下文窗口溢出。\n\n**错误归因** —— 失败结果中会包含 `sourceSlug`，使 UI 能够将错误归因到特定来源，并提供有针对性的修复措施（重新认证、禁用来源等）。\n\n来源：[mcp-pool.ts](packages/shared/src/mcp/mcp-pool.ts#L368-L451)\n\n## McpPoolServer：面向外部 SDK 的 HTTP 桥接[](#mcppoolserver面向外部-sdk-的-http-桥接)\n\n`McpPoolServer` 使用 Streamable HTTP 协议，通过 HTTP 将池的工具作为标准 MCP 服务器暴露出来。它的存在专门是为了服务于通过基于 POST 的 JSON-RPC 而非进程内 API 进行连接的外部 SDK 子进程（如 Codex、Copilot）。\n\n该服务器出于安全考虑，会绑定到 `127.0.0.1` 并在随机端口 (`listen(0)`) 上启动。它运行在**无状态模式**下——不进行会话跟踪，每个请求都是独立的。这种简化是可行的，因为来源变更已经会触发完整的重连周期 (`regenCodexConfigAndReconnect()`)，因此外部客户端会在每次启动时重新发现工具。\n\n命名转换层负责处理前缀差异：池内部使用 `mcp__craft__search_spaces`，服务器剥离 `mcp__` 前缀以暴露 `craft__search_spaces`，而 Codex 会添加自己的 `mcp__sources__` 前缀，从而生成最终的 `mcp__sources__craft__search_spaces`。在接收到传入的工具调用时，服务器会在通过池进行路由之前重新添加 `mcp__` 前缀。\n\n在无状态模式下，`notifyToolsChanged()` 方法被有意设计为空操作。外部 SDK 子进程不会接收推送通知——它们通过在来源配置更新触发服务器重启后重新连接来发现工具变更。\n\n来源：[pool-server.ts](packages/shared/src/mcp/pool-server.ts#L32-L178)\n\n## 验证：连接前健康检查[](#验证连接前健康检查)\n\n在来源进入池之前，可以通过 [validation.ts](packages/shared/src/mcp/validation.ts) 中的两条独立路径对其进行验证：\n\n**HTTP/SSE 验证** (`validateMcpConnection`) 使用 Claude Agent SDK 的 `query()` 函数配合 `mcpServerStatus()`，在不执行工具的情况下检查连通性。它返回一个包含带类型错误类别的 `McpValidationResult`：`failed`（连接错误）、`needs-auth`（401/403）、`pending`（来源仍在初始化）、`invalid-schema`（工具 schema 违反了 Anthropic 的属性名称模式）、`disabled`（被显式关闭）或 `unknown`。成功时，它会填充 `serverInfo`（名称、版本）和 `tools`（可用工具名称）。\n\n**Stdio 验证** (`validateStdioMcpConnection`) 采取了不同的方法——它实际上通过 `child_process.spawn` 生成 MCP 服务器进程，通过 stdio 传输进行连接，列出工具，并针对 `ANTHROPIC_PROPERTY_NAME_PATTERN` (`/^[a-zA-Z0-9_.-]{1,64}$/`) 验证每个工具的输入 schema。这种模式是 Claude 特有的约束，在 MCP 规范本身中并不存在。结果中包含 `invalidProperties`，提供用于 UI 渲染的精确路径/键信息。\n\n来源：[validation.ts](packages/shared/src/mcp/validation.ts#L19-L54), [validation.ts](packages/shared/src/mcp/validation.ts#L119-L143), [validation.ts](packages/shared/src/mcp/validation.ts#L345-L362)\n\n## 集成点[](#集成点)\n\n该池在三个层面集成到 Agent 系统中，对应于架构图中展示的三种消费者类型：\n\n消费者\n\n集成方式\n\n工具流转方式\n\nClaudeAgent\n\n`createSourceProxyServers()` + SDK `mcpServers` 选项\n\n池工具成为 SDK MCP 服务器；SDK 添加 `mcp__{key}__` 前缀\n\nPiAgent\n\n`registerPoolToolsWithSubprocess()` JSONL\n\n池工具定义被序列化并发送到子进程；执行通过 `callTool()` 路由回传\n\n外部 SDK\n\n`McpPoolServer` HTTP 端点\n\n工具作为标准 MCP 服务器暴露；客户端独立连接和发现\n\n这三条路径最终都汇聚于同一个 `McpClientPool.callTool()` 方法，从而确保了一致的执行语义，包括内容标准化、二进制处理和大型响应保护。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L406-L440), [pi-agent.ts](packages/shared/src/agent/pi-agent.ts#L475-L500), [pool-server.ts](packages/shared/src/mcp/pool-server.ts#L107-L143)\n\n## 后续步骤[](#后续步骤)\n\n要继续探索该架构，可以查看池的工具如何在 [权限模式系统](/lukilabs/craft-agents-oss/13-permission-mode-system) 中与 Agent 执行生命周期交互，或者检查来源在进入池之前是如何在 [来源与技能](/lukilabs/craft-agents-oss/19-sources-and-skills) 中被构建和配置的。如需了解 ClaudeAgent 后端的完整工具集成，请参阅 [ClaudeAgent SDK 集成](/lukilabs/craft-agents-oss/10-claudeagent-sdk-integration) 。";export{n as default};

const n="# BaseAgent 抽象 | lukilabs/craft-agents-oss\n\n\n---\nAgent 后端系统是 Craft Agents 的架构骨干——它定义了用户消息如何流入 AI 推理流程、工具如何进行权限校验与执行，以及流式事件如何回传至各个宿主端（Electron、CLI、WebUI）。其核心是 **`BaseAgent`**，一个抽象类，它捕获了所有提供商后端共有的横切关注点，仅将特定于提供商的 SDK 管道工作保留为抽象方法。\n\n## 架构概述[](#架构概述)\n\n后端系统遵循**模板方法**模式：`BaseAgent.chat()` 是唯一的入口点，在委托给抽象的 `chatImpl()` 之前，它会处理技能提及解析、前置条件强制执行、分支种子注入和指令前置——而 `chatImpl()` 是每个提供商必须以不同方式实现的唯一方法。\n\nMaximum text size in diagram exceeded\n\n工厂函数 `createBackend()`（别名为 `createAgent()`）会根据 `BackendConfig` 中的 `provider` 字段解析正确的子类——目前 `'anthropic'` 映射到 `ClaudeAgent`，而 `'pi'` 映射到 `PiAgent`。来源：[factory.ts](/packages/shared/src/agent/backend/factory.ts#L132-L152)\n\n## AgentBackend 契约[](#agentbackend-契约)\n\n`backend/types.ts` 中的 `AgentBackend` 接口定义了每个宿主端（Electron SessionManager、CLI 控制器、无头服务器）编程时所依赖的完整公共表面。它被有意设计为**与提供商无关**——没有任何 Anthropic 或 Pi 的类型会泄漏出此边界。来源：[types.ts](/packages/shared/src/agent/backend/types.ts#L307-L571)\n\n### 生命周期方法[](#生命周期方法)\n\n方法\n\n用途\n\nBaseAgent 中的默认实现\n\n`postInit()`\n\n在回调接线完成后进行鉴权注入和配置生成\n\n空操作（返回 `{ authInjected: true }`）\n\n`destroy()` / `dispose()`\n\n释放所有资源——MCP 连接、监听器、白名单\n\n停止配置监听器，清除白名单，断开 MCP 连接池\n\n`ensureBranchReady()`\n\n在首条消息前为分支会话准备后端\n\n空操作\n\n`applyBridgeUpdates(ctx)`\n\n会话中的配置变更（来源启用、令牌刷新）\n\n空操作\n\n来源：[base-agent.ts](/packages/shared/src/agent/base-agent.ts#L809-L858)\n\n### 聊天与控制流[](#聊天与控制流)\n\n方法\n\n用途\n\n`chat(message, attachments?, options?)`\n\n模板方法入口点——解析技能，注入指令，委托给 `chatImpl()`\n\n`abort(reason?)`\n\n优雅的异步中止（用户发起的停止）\n\n`forceAbort(reason)`\n\n带有类型化 `AbortReason` 枚举的硬停止\n\n`interruptForHandoff(reason)`\n\n暂停以进行 UI 交接（计划审查、鉴权对话框）——默认调用 `forceAbort()`\n\n`redirect(message)`\n\n流式消息注入——如果被原生引导则返回 `true`，如果中止并重新排队则返回 `false`\n\n`BaseAgent` 中的 `chat()` 方法在委托之前执行四个步骤：(1) 解析 `[skill:slug]` 提及并解析 `SKILL.md` 路径；(2) 通过 `PrerequisiteManager` 注册技能前置条件；(3) 如果存在分支种子或转移的会话上下文，则将其注入；(4) 前置一个读取指令，强制模型在执行操作前消费技能文件。来源：[base-agent.ts](/packages/shared/src/agent/base-agent.ts#L956-L993)\n\n### 回调属性[](#回调属性)\n\n回调由会话/外观层在构造后接线——`BaseAgent` 永远不会导入应用层代码：\n\n回调\n\n触发条件\n\n`onPermissionRequest`\n\n工具需要用户批准（bash、文件写入、MCP 修改操作）\n\n`onPlanSubmitted`\n\nAgent 调用 `SubmitPlan` 工具——触发计划审查 UI\n\n`onAuthRequest`\n\n来源需要 OAuth 或凭证输入\n\n`onSourceChange`\n\n来源配置在磁盘上被修改\n\n`onSourcesListChange`\n\n工作区中添加/移除了来源\n\n`onPermissionModeChange`\n\n用户切换权限模式\n\n`onDebug`\n\nAgent 内部的调试日志输出\n\n`onSourceActivationRequest`\n\nAgent 尝试使用非活动来源的工具\n\n`onUsageUpdate`\n\n每轮的 Token 用量统计\n\n`onBackendAuthRequired`\n\n后端需要重新鉴权（例如，OAuth 过期）\n\n`onSpawnSession`\n\nAgent 请求创建子会话\n\n来源：[base-agent.ts](/packages/shared/src/agent/base-agent.ts#L203-L214)\n\n## 核心模块[](#核心模块)\n\n`BaseAgent` 组合了来自 `agent/core/` 的六个管理器类，每个类负责单一领域。这些是 `protected` 字段，子类可以访问，但对使用者透明。\n\n### PermissionManager[](#permissionmanager)\n\n根据当前的权限模式（`safe` → `ask` → `allow-all`）和自定义的 `permissions.json` 规则评估每次工具调用。它维护会话作用域的白名单，用于针对特定命令和域的“始终允许”决策，并将危险命令检测委托给一个共享集合（`rm`、`sudo`、`git push` 等）。来源：[permission-manager.ts](/packages/shared/src/agent/core/permission-manager.ts#L68-L384)\n\n从 `BaseAgent` 委托的关键方法：\n\n-   `getPermissionMode()` / `setPermissionMode()` / `cyclePermissionMode()` —— 带有 UI 通知的模式转换\n-   `evaluateToolCall(toolName, toolInput)` —— 为任何工具调用返回 `{ allowed, blockReason }`\n-   `whitelistCommand(baseCommand)` / `whitelistDomain(domain)` —— 会话作用域的“始终允许”\n\n### SourceManager[](#sourcemanager)\n\n跟踪活动、预期和非活动来源。将来源状态格式化为注入到用户消息中的 XML 上下文块，以便模型知道哪些工具可用。处理首次遇到时带有来源标语的“未见”状态。来源：[source-manager.ts](/packages/shared/src/agent/core/source-manager.ts#L40-L387)\n\n### PromptBuilder[](#promptbuilder)\n\n构建伴随用户消息的上下文块——工作区能力、日期/时间上下文（用于提示词缓存）、工作目录状态和偏好格式化。提供 `buildContextParts()` 方法供子类调用以组装其提示词。来源：[prompt-builder.ts](/packages/shared/src/agent/core/prompt-builder.ts#L44-L243)\n\n### UsageTracker[](#usagetracker)\n\n记录单条消息和累计会话的 Token 用量（输入、输出、缓存读/写）。发出带有上下文窗口填充百分比和缓存命中率的 `UsageUpdate` 事件——供 UI 消费以显示用量统计。在上下文填充达到 80% 和 95% 时触发阈值警告。来源：[usage-tracker.ts](/packages/shared/src/agent/core/usage-tracker.ts#L103-L290)\n\n### PrerequisiteManager[](#prerequisitemanager)\n\n强制执行“先读后用”契约：当来源被激活时，必须先由模型读取其 `guide.md`，然后才能执行其任何 MCP 工具。类似地，`[skill:slug]` 提及会将 `SKILL.md` 文件注册为前置条件。在上下文压缩后（当模型丢失之前读取的内容时），`resetReadState()` 会强制重新读取。来源：[prerequisite-manager.ts](/packages/shared/src/agent/core/prerequisite-manager.ts#L64-L272)\n\n### ConfigWatcherManager[](#configwatchermanager)\n\n监听工作区配置文件（来源、权限）以便在会话期间进行热重载。触发 `onSourceChange`、`onSourcesListChange` 和 `onConfigValidationError` 回调，使运行中的 Agent 能够在无需重启的情况下适应配置更改。仅在非无头模式下启动。来源：[base-agent.ts](/packages/shared/src/agent/base-agent.ts#L280-L321)\n\n## BackendConfig 与提供商解析[](#backendconfig-与提供商解析)\n\n`BackendConfig` 使用特定于提供商的字段扩展了 `CoreBackendConfig`。`CoreBackendConfig` 被有意设计为与提供商无关——它包含工作区、会话、模型、MCP 连接池、自动化系统以及分支/恢复回调。特定于提供商的路由（鉴权类型、运行时路径）存在于 `BackendConfig.provider` 和 `BackendConfig.runtime` 中。来源：[types.ts](/packages/shared/src/agent/backend/types.ts#L156-L623)\n\nSyntax error in textmermaid version 11.6.0\n\n工厂使用**驱动注册表**模式，每个提供商注册一个 `ProviderDriver`，用于解析运行时路径、验证凭证并获取模型。目前已注册 `anthropicDriver` 和 `piDriver`。来源：[factory.ts](/packages/shared/src/agent/backend/factory.ts#L63-L68)\n\n**后端能力**在 `BACKEND_CAPABILITIES` 中以声明式方式定义（例如 `needsHttpPoolServer`），使得会话层无需检查提供商字符串即可做出决策。来源：[factory.ts](/packages/shared/src/agent/backend/factory.ts#L585-L595)\n\n## 抽象方法——提供商契约[](#抽象方法提供商契约)\n\n`BaseAgent` 定义了七个抽象方法，每个提供商必须实现这些方法。它们代表了允许存在特定于提供商的 SDK 逻辑的唯一位置：\n\n抽象方法\n\nClaudeAgent 实现\n\nPiAgent 实现\n\n`chatImpl()`\n\n带有流式传输的 Claude Agent SDK `query()`\n\n向子进程发送 JSONL 消息，从事件队列中 yield\n\n`abort()`\n\nAbortController 信号\n\n子进程 `kill('SIGTERM')`\n\n`forceAbort()`\n\n终止控制器 + 关闭 SDK\n\n子进程 `kill('SIGKILL')`\n\n`isProcessing()`\n\n检查 SDK 查询是否处于活动状态\n\n检查子进程状态标志\n\n`respondToPermission()`\n\n解析来自权限检查的待处理 Promise\n\n向子进程发送权限响应\n\n`runMiniCompletion()`\n\n带有空系统提示词的 SDK `query()`\n\n子进程 `mini_completion` 请求\n\n`queryLlm()`\n\n带有自定义系统提示词的 SDK `query()`\n\n子进程 `mini_completion` 请求\n\n来源：[base-agent.ts](/packages/shared/src/agent/base-agent.ts#L999-L1078)\n\n## 会话恢复与分支[](#会话恢复与分支)\n\n`BaseAgent` 提供了所有后端使用的统一恢复逻辑。当 SDK 会话恢复失败（空响应、未找到线程）时，`buildRecoveryContext()` 会将先前的消息格式化为 `<conversation_recovery>` XML 块，并将其前置到用户消息中，从而允许 Agent 自然地继续。来源：[base-agent.ts](/packages/shared/src/agent/base-agent.ts#L717-L783)\n\n对于会话分支，`buildBranchSeedContext()` 会根据分支截止点之前的父消息创建一个 `<branch_seed_context>` 块。这是一次性注入——`markBranchSeedApplied()` 确保它仅在第一轮触发。ClaudeAgent 还额外支持原生 SDK 级别的分支；如果失败，它会回退到通过 `runMiniCompletion()` 总结父会话。来源：[base-agent.ts](/packages/shared/src/agent/base-agent.ts#L751-L774)\n\n## Mini Agent 模式[](#mini-agent-模式)\n\n当 `systemPromptPreset` 为 `'mini'` 时，Agent 会进入专为快速配置编辑而设计的受限模式。`BaseAgent` 集中管理 mini agent 配置——允许的工具（`Read`、`Edit`、`Write`、`Glob`、`Grep`、`Bash`）、MCP 服务器键（仅限 `session`）以及最小化思考。每个后端会适当地解释这些常量：ClaudeAgent 使用它们来过滤 SDK 工具并设置 `maxThinkingTokens: 0`，而 PiAgent 使用它们来过滤子进程工具注册。来源：[base-agent.ts](/packages/shared/src/agent/base-agent.ts#L133-L704)\n\n## 会话 MCP 工具补全[](#会话-mcp-工具补全)\n\n会话作用域的工具（`SubmitPlan`、`source_oauth_trigger`、`source_credential_prompt`）在外部 MCP 服务器子进程中运行。由于该子进程拥有自己的内存且无法访问主进程的回调注册表，因此 `BaseAgent.handleSessionMcpToolCompletion()` 提供了一个共享方法，将工具名称映射到回调：`SubmitPlan` → `onPlanSubmitted`，鉴权工具 → `onAuthRequest`。ClaudeAgent 不使用此路径，因为其会话作用域的工具是通过 Claude Agent SDK 在进程内运行的。来源：[base-agent.ts](/packages/shared/src/agent/base-agent.ts#L377-L423)\n\n## 总结[](#总结)\n\n`BaseAgent` 是 Craft Agents 后端系统的架构支点。它通过将每个共享关注点——权限、来源、提示词、用量跟踪、前置条件、配置监听、技能解析、标题生成和会话恢复——提取到可复用的核心模块中，消除了 `ClaudeAgent`（约 2680 行）和 `PiAgent`（约 2248 行）之间的重复代码。抽象契约很窄（仅七个方法）且稳定，这使得添加新的提供商后端变得十分简单，只需实现特定于 SDK 的管道工作即可继承完整的行为栈。\n\n## 后续步骤[](#后续步骤)\n\n-   **[ClaudeAgent SDK 集成](/lukilabs/craft-agents-oss/10-claudeagent-sdk-integration)** —— 深入探讨 ClaudeAgent 如何封装 Anthropic Claude Agent SDK，包括权限钩子、事件流和 `ClaudeEventAdapter` 管道。\n-   **[PiAgent 子进程协议](/lukilabs/craft-agents-oss/11-piagent-subprocess-protocol)** —— 探索 JSONL 子进程通信模型、会话管理和 `PiEventAdapter` 事件转换层。\n-   **[MCP 客户端连接池](/lukilabs/craft-agents-oss/12-mcp-client-pool)** —— 了解集中式的 `McpClientPool` 如何管理跨 Agent 后端共享的来源工具连接。";export{n as default};

const e='Miniclaw 的定时任务是连接"Agent 长期自主运转"与"用户可信赖的自动化"的核心子系统。它要同时回答四个问题：**什么时候触发**（cron 表达式、固定间隔、一次性时间点）、**触发后如何保证恰好执行一次**（数据库租约与幂等物化）、**进程重启后怎么办**（周期任务记为 missed 而不重放，一次性任务仍补跑）、以及**结果如何可靠送达**（通知账本的持久重试）。本文剖析 `src/task-scheduler.ts`（约 3500 行）与其依赖的持久层，覆盖调度类型解析、调度泵循环、occurrence 物化、重启恢复策略和执行管线。\n\n## 双层数据模型：任务定义与运行实例\n\n理解调度器的第一步是区分两张表。`scheduled_tasks` 是**任务定义表**，保存 prompt、投递目标、调度类型与调度值，其中 `next_run` 字段扮演"调度游标"角色——它既表示下一次触发时刻，也是并发控制中的乐观锁比较值；`task_runs` 是 **V2 运行实例表**，每次实际触发都会物化一行，携带 `occurrence_key` 唯一键、触发类型、**定义快照**（`definition_snapshot`，把执行瞬间的 prompt/路由/执行方式冻结下来，之后修改任务不影响已在排队的运行）以及完整的租约与通知字段 [db.ts](src/db.ts#L574-L657)。这一"定义 + 快照"的分层意味着任务编辑与正在执行的运行彼此隔离。\n\n```mermaid\nerDiagram\n    scheduled_tasks ||--o{ task_runs : "物化为"\n    scheduled_tasks {\n        text id PK\n        text schedule_type "cron | interval | once"\n        text schedule_value "表达式或毫秒或时间点"\n        text next_run "调度游标"\n        text status "active/paused/completed/parsing"\n        int revision "乐观并发版本"\n        text delivery_route_jid "精确投递绑定"\n    }\n    task_runs {\n        text id PK\n        text occurrence_key UK "taskId:scheduledFor"\n        text trigger_type "scheduled/manual/backfill"\n        text definition_snapshot "定义快照 JSON"\n        text status "queued...delivered"\n        int attempt "已被认领次数"\n        text lease_owner "租约持有者"\n    }\n```\n\n运行实例的状态机比直觉丰富：除常规的排队/运行/成功/失败外，还有 `retry_wait`（等待重试）、`missed`（被判定跳过的 occurrence）和 `delivered`（group 模式下已移交群队列投递）。每个状态迁移都由带租约校验的 DB 函数完成 [types.ts](src/types.ts#L544-L554)。\n\n| 状态 | 含义 | 进入方式 |\n|---|---|---|\n| `queued` | 已物化待执行 | `materializeTaskOccurrence` / `createTaskRun` |\n| `running` | 已被某 runner 认领 | `claimNextTaskRun`（attempt+1，租约生效） |\n| `retry_wait` | 执行前失败，定时重试 | `releaseTaskRunForRetry` |\n| `success` / `failed` / `cancelled` | 终态 | `completeTaskRun`（fence 校验后写入） |\n| `missed` | 有意跳过（冲突/离线错过/非法计划） | 物化时直接落终态行 |\n| `delivered` | group 模式移交群会话队列 | 群模式完成路径 |\n\nSources: [db.ts](src/db.ts#L574-L657)、[types.ts](src/types.ts#L505-L554)\n\n## 三种调度类型与 next_run 游标计算\n\n三种调度类型的语义差异集中在 `next_run` 如何推导。**cron** 使用 `cron-parser` 库按 `TIMEZONE` 时区解析（默认取 `TZ` 环境变量，回退系统时区再回退 `Asia/Shanghai`）；**interval** 是毫秒数；**once** 是本地时间字符串，按进程本地时区解释后统一转 UTC 存储 [config.ts](src/config.ts#L52-L57)。\n\n| 维度 | cron | interval | once |\n|---|---|---|---|\n| `schedule_value` 形态 | 5/6 字段 cron 表达式 | 毫秒数字符串（≥60000） | 本地时间串 |\n| 最小频率防线 | 秒字段必须解析为单值 | 数值下限校验 | 不适用 |\n| 游标锚点 | 上一次 next_run | 上一次 next_run（ceil 补齐） | 无下游 |\n| 到期后 | 计算下一匹配时刻 | 锚点 + N×周期 | 结束生命周期 |\n\n代码里有三个容易混淆的函数，职责边界值得记住。`computeNextRunForSchedule` 服务于**新建/修改任务**，以"当前时刻"为锚点算第一次触发；`computeNextRun` 服务于**调度循环**，以任务的既有 `next_run` 为锚点推进周期——对 interval 类型它用 `Math.ceil(elapsed/ms)` 补齐错过的整周期数而不是简单加一个周期，避免长期漂移；`computeNextRunForTaskResume` 则在重新启用 paused 任务时重建被清除的游标，并拒绝启用执行时间已过的一次性任务 [task-scheduler.ts](src/task-scheduler.ts#L682-L719)。\n\n最小频率防线是刻意做成**确定性验证**而非"看未来两次触发"的启发式：五字段 cron 天然分钟级；六字段 cron 只有秒字段解析为单一值时才安全——`*/30 * * * * *` 这类秒级循环会在 `validateCronMinimumInterval` 处直接抛错。interval 的下限校验甚至在 `computeNextRun` 内部重复一遍，因为手工 SQL 或旧版数据可能绕过 REST 校验，此时抛错会把该次触发送进 missed+pause 路径而不是放行一个毫秒级死循环 [task-scheduler.ts](src/task-scheduler.ts#L624-L658)。\n\nSources: [task-scheduler.ts](src/task-scheduler.ts#L610-L719)、[config.ts](src/config.ts#L52-L57)\n\n## 调度泵：事件驱动的唤醒循环\n\n调度器不是固定频率轮询，而是**事件驱动 + 兜底对账**的混合模式。`armSchedulerFromStore` 每轮结束时从数据库查询两个"最近唤醒时刻"：所有活跃任务的最早 `next_run`（`getNextScheduledTaskWakeAt`），以及 task_runs 侧最近的可用/租约到期/通知重试时刻（`getNextTaskRunWakeAt`），取二者较小值精确设定 setTimeout；同时用 `SCHEDULER_RECONCILE_MS = 60s` 封顶，作为时钟跳变、丢失唤醒通知等异常的兜底对账 [task-scheduler.ts](src/task-scheduler.ts#L2209-L2223)。任何任务定义变更都会调用 `notifyTaskSchedulerChanged` 立即唤醒泵，让新设置的更早计划无需等待当前定时器到期。\n\n```mermaid\nflowchart TD\n    A["startSchedulerLoop"] --> B["记录 startedAtMs<br/>清空进程内保留集合"]\n    B --> C["pumpTaskScheduler"]\n    C --> D1["failExpiredStartedTaskRuns<br/>过期已启动 run 标记 failed"]\n    D1 --> D2["finalizeExpired...Notification<br/>回收超时通知租约"]\n    D2 --> E["materializeDueOccurrences<br/>物化到期 occurrence"]\n    E --> F["pumpTaskNotificationRetries<br/>认领失败通知重试"]\n    F --> G{"claimNextTaskRun"}\n    G -- "拿到 run" --> H["executeClaimedTaskRun"]\n    H --> G\n    G -- "无候选" --> I["armSchedulerFromStore<br/>按最近唤醒时刻设定时器"]\n```\n\n每轮泵最多认领 `MAX_CLAIMS_PER_PUMP = 32` 个运行实例，防止一轮阻塞过久；泵本身有 `schedulerPumping` 重入保护，且在优雅停机时拒绝再次布防定时器 [task-scheduler.ts](src/task-scheduler.ts#L3204-L3236)。停机侧的 `stopSchedulerLoop` 遵循明确契约：先停止物化新 occurrence，再分轮 drain 那些**脱离群队列管理**的脚本执行与通知重试 Promise（每轮重读活跃集合，因为完成一个运行可能入队它的后续通知），轮数与时钟双封顶避免单个卡死项阻塞退出 [task-scheduler.ts](src/task-scheduler.ts#L3238-L3279)。\n\nSources: [task-scheduler.ts](src/task-scheduler.ts#L2125-L2228)、[task-scheduler.ts](src/task-scheduler.ts#L3204-L3279)、[db.ts](src/db.ts#L7512-L7594)\n\n## Occurrence 物化：游标推进与运行创建的原子性\n\n"任务到期"到"运行实例存在"之间由 `materializeTaskOccurrence` 一个事务完成三件事：以 `WHERE next_run = ?` 的乐观并发条件锁定定义行、插入 `occurrence_key = taskId:scheduledFor` 的运行行、原子地把 `next_run` 推进到下一个周期 [db.ts](src/db.ts#L5206-L5286)。这个设计一次性消灭了三类经典 bug：两个调度进程重复物化同一时刻（occurrence_key 唯一约束）、物化成功但游标未推进导致死循环（同事务保证）、以及编辑竞态（游标已被别人改过则整个物化放弃）。\n\n物化时还有一个重要的**背压规则**：如果该任务的上一个 occurrence 仍在 `queued/running/retry_wait`，本次到期不会排队第二个运行（每任务同时只允许一个非终态运行，由部分唯一索引 `idx_task_runs_one_nonterminal` 兜底），而是直接落一行 `missed` 终态记录，错误信息写明被哪个运行阻塞，然后照常推进游标——慢任务不会堆积雪崩，但跳过事实完全可审计 [db.ts](src/db.ts#L5237-L5267)。`materializeDueOccurrences` 在此之上补充两条策略分支：schedule 解析失败的任务记 missed 后**暂停整个定义**等待修复；一次性任务过期触发时标记为 `backfill` 触发类型 [task-scheduler.ts](src/task-scheduler.ts#L2926-L2962)。\n\nSources: [db.ts](src/db.ts#L5206-L5286)、[task-scheduler.ts](src/task-scheduler.ts#L2926-L2962)\n\n## 重启补跑：周期任务记为 Missed，一次性任务仍执行\n\n这是标题里最微妙的语义。判定函数 `shouldSkipBackfill` 只有一行核心逻辑：**计划的触发时刻早于本进程启动时刻（`schedulerStartedAtMs`）的周期性工作，一律不补跑**；`startedAtMs <= 0`（测试场景）或无法解析的时刻则放行 [task-scheduler.ts](src/task-scheduler.ts#L610-L622)。设计动机写在注释里：周期任务错过的这一次通常已经失去时效（比如"每小时汇报"停机两小时后没必要连发两份），而**一次性任务是显式的用户承诺，重启后必须兑现**——所以 `materializeDueOccurrences` 里只有周期任务命中 skip 判定时才标记 `backfill` + `"Missed while scheduler was offline by Ns"`，once 过期任务虽然也标 backfill 但照样进入执行队列 [task-scheduler.ts](src/task-scheduler.ts#L2946-L2960)。\n\n| 场景 | 周期任务 | 一次性任务 |\n|---|---|---|\n| 正常到期 | `scheduled` 触发，正常执行 | `scheduled` 触发，正常执行 |\n| 停机期间到期，重启后发现 | 记 `missed`（backfill 触发 + 离线原因），游标继续推进 | **仍然执行**（backfill 触发） |\n| 上个运行还没跑完又到期 | 记 `missed`（前序运行占用），不排队 | 同左 |\n| schedule 数据损坏 | 记 `missed` 并暂停定义 | 记 `missed` 并转 completed |\n\n启动路径还有三道配套清理。其一，进程内保留集合（`runningTaskIds` 等）重启后必然失效，直接清空；但 V2 的数据库租约**刻意不清**，因为它们靠到期自然回收，清掉反而会破坏滚动重启时的接管语义。其二，legacy 运行日志没有租约字段，启动时残留的 running 状态必属崩溃孤儿，统一标记为 failed。其三，上一会话崩溃遗留的已完成 once-task 专属 `task-*` 工作区会被删除 [task-scheduler.ts](src/task-scheduler.ts#L3281-L3342)。\n\n另一条重启相关的安全边界是 `failExpiredStartedTaskRuns`：**已经跨过执行边界的运行**（`started_at` 非空）租约过期后被标记为 `failed` 而非重新排队——注释明确说明这是 at-most-once 取舍："进程停止后才重试可能重复外部副作用"。与之相对，**尚未** markStarted 的运行（如刚认领还没进执行函数就崩溃）可以被安全接管重试 [db.ts](src/db.ts#L5407-L5458)。这套策略全部有契约测试锁定：`shouldSkipBackfill` 的五个边界用例（含"恰好在启动时刻不算离线"）直接从生产代码导入断言，防止实现漂移 [task-restart-recovery.test.ts](tests/task-restart-recovery.test.ts#L189-L230)；启动恢复测试则验证旧日志关闭、租约保留与损坏 interval 的 pause 行为 [task-scheduler-startup.test.ts](tests/task-scheduler-startup.test.ts#L33-L75)。\n\nSources: [task-scheduler.ts](src/task-scheduler.ts#L610-L622)、[task-scheduler.ts](src/task-scheduler.ts#L3281-L3342)、[db.ts](src/db.ts#L5407-L5458)、[tests/task-restart-recovery.test.ts](tests/task-restart-recovery.test.ts#L73-L230)\n\n## 执行管线：租约、心跳与三种执行模式\n\n运行实例从队列到执行的入口是 `claimNextTaskRun`：在一个事务里选出最早的候选（`queued/retry_wait` 且到达可用时间，或 running 且租约过期**且从未 markStarted**），随后用 `UPDATE ... WHERE <同样的候选条件>` 做条件更新，写入新的 `lease_owner`、递增 `lease_token` 和 `attempt`。`lease_token` 单调递增是全部写操作的 fence 凭据——过期但未被接管的租约仍允许原 worker 提交自己的结果（它可能已经把输出发给用户了），而一旦有人接管，token 变化就让旧 worker 的一切后续写入失效 [db.ts](src/db.ts#L5460-L5495)。\n\n执行侧 `executeClaimedTaskRun` 按 `execution_type` 与 `context_mode` 分三条路：**script**（宿主机脚本，需管理员权限与 host 执行许可）、**group**（群聊上下文模式，最终移交群队列产出 `delivered` 态）、**isolated**（默认，独立虚拟会话 `workspace#task:{runId}` 中运行 Agent，不污染主聊天）。三者共享同一套租约纪律：认领后先调 `markTaskRunExecutionStarted` 划下不可逆边界，然后心跳定时器每 `TASK_RUN_LEASE_MS/3`（20 秒）续租一次，续租失败立即中止执行防止僵尸双跑 [task-scheduler.ts](src/task-scheduler.ts#L2266-L2298)、[task-scheduler.ts](src/task-scheduler.ts#L2637-L2836)。\n\n| 失败时机 | 处理方式 | 是否消耗 attempt |\n|---|---|---|\n| 入队前被丢弃 | 手动触发→cancelled；否则安全预启动重试 | 否 |\n| 进程停机打断（未开始） | 释放预算，下个进程直接接手 | 否 |\n| 工作区变更中（mutation paused） | 1 秒后重试，不消耗预算 | 否 |\n| 一般预启动失败 | 指数退避 `min(60s, 2^n)`，上限 5 次 | 是 |\n\n预启动重试的退避细节体现了运维考量：`MAX_SAFE_PRESTART_ATTEMPTS = 5` 次普通重试后落终态 failed，但**因停机导致的释放必须归还预算**（`countsAsAttempt: false` 时 attempt 做 `MAX(0, attempt-1)` 回退），否则连续五次例行滚动重启就会永久杀死一个从未真正执行过的 occurrence [task-scheduler.ts](src/task-scheduler.ts#L2230-L2264)、[db.ts](src/db.ts#L5497-L5526)。执行结束后的落账由 `finishDurableRunFromLegacyLog` 统一裁决 success/failed/delivered，并把 legacy 日志与 V2 账本对齐 [task-scheduler.ts](src/task-scheduler.ts#L2583-L2635)。\n\nSources: [db.ts](src/db.ts#L5344-L5526)、[task-scheduler.ts](src/task-scheduler.ts#L2637-L2836)\n\n## 结果通知的持久重试\n\n运行结束后要把结果送达 IM 渠道或 Web 工作区，这条链路同样按"可崩溃"标准设计。每次投递尝试的 payload 先持久化到 task_runs 的 `notification_payload` 等字段，投递收据（成功/部分失败/失败的渠道清单）也落库；失败的渠道被筛出构造**只含失败目标**的重试 payload，由独立的 `claimNextTaskRunNotification` 带租约认领，上限 `MAX_TASK_NOTIFICATION_ATTEMPTS = 5` 次，退避同样是 `min(60s, 1s × 2^(n-1))` [task-scheduler.ts](src/task-scheduler.ts#L2964-L3003)、[db.ts](src/db.ts#L7313-L7320)。关键的不变量是"存储与通知分离"：第一次尝试已把结果写入工作区的，重试 payload 会带 `skipStore: true` 只补投递，绝不重复写库或刷屏 [task-scheduler.ts](src/task-scheduler.ts#L2483-L2495)。通知重试属于前文提到的 detached work，停机 drain 时会被显式等待。\n\nSources: [task-notification.ts](src/task-notification.ts#L88-L144)、[task-scheduler.ts](src/task-scheduler.ts#L3141-L3202)、[db.ts](src/db.ts#L7313-L7320)\n\n## 任务生命周期入口：REST 创建、AI 解析与手动触发\n\n任务有三个创建入口，共享同一套 schedule 校验。REST `POST /api/tasks` 在落库前调用 `computeNextRunForSchedule`，非法 cron/过短 interval 直接返回 400，保证**不存在无效计划的 active 任务**；同时按来源工作区继承 host/container 执行模式，script 类型仅限管理员 [routes/tasks.ts](src/routes/tasks.ts#L186-L319)。AI 自然语言创建走两段式：先用占位 cron（`0 0 * * *`）+ `status: \'parsing\'` + 空 `next_run` 立即落库（此时永远不会被调度），后台用 SDK 解析出结构化的 schedule 后经同样的校验管线激活，解析失败或超时则停在 paused 等待人工修整——乐观并发 revision 保证解析期间用户的编辑不会被解析结果覆盖 [routes/tasks.ts](src/routes/tasks.ts#L1108-L1247)。\n\n手动"立即运行"（`triggerTaskNow`）有两个精心设计的差异：它**不推进也不读取 next_run**（正常计划不受影响），并通过可选 `idempotencyKey` 构造幂等键防重复提交；若该任务已有活跃运行则返回 `active_conflict` 而不是排队第二个。取消侧 `cancelTaskRunNow` 先用 DB 取消 fence 掉迟到的完成写入，再中断进程内活跃 worker，还专门处理了 group 模式已 delivered 运行的取消 [task-scheduler.ts](src/task-scheduler.ts#L3344-L3401)。最后值得一提的投递精度保障是 `resolveTargetGroupJid`：运行要么送达创建时记录的 `delivery_route_jid` 绑定（含飞书话题等完整路由信息），要么终结性失败，**永不静默降级**到同 folder 的其他群或 Web——这正是 CLAUDE.md 6.4 节"响应目标不得降级"原则的实现 [task-scheduler.ts](src/task-scheduler.ts#L131-L158)。调度器整体在主服务启动流程末尾拉起，并把 `triggerTaskNow`/`cancelTaskRunNow` 注入 Web 依赖供 HTTP 层使用 [index.ts](src/index.ts#L21074-L21084)。\n\nSources: [routes/tasks.ts](src/routes/tasks.ts#L186-L319)、[routes/tasks.ts](src/routes/tasks.ts#L1108-L1247)、[task-scheduler.ts](src/task-scheduler.ts#L3344-L3401)、[index.ts](src/index.ts#L21074-L21084)\n\n## 小结\n\nMiniclaw 定时任务调度器的设计主线可以压缩成三个词：**游标化**（next_run 既是计划又是乐观锁）、**实例化**（一切执行都物化为可审计的 task_runs 行，定义快照隔离编辑竞态）、**差异化恢复**（周期任务宁缺毋滥记 missed，一次性任务承诺必达，已开始的执行宁报失败不盲目重放）。配合租约 token fencing、预启动预算归还和通知持久账本，整套系统在不引入外部消息队列的前提下，用 SQLite 事务达成了接近 at-least-once 投递 + at-most-once 执行的组合语义。想继续深入，可以沿目录阅读执行引擎侧的 [会话串行队列：Runner 生命周期、重试退避与卡死恢复](14-hui-hua-chuan-xing-dui-lie-runner-sheng-ming-zhou-qi-zhong-shi-tui-bi-yu-qia-si-hui-fu) 了解 GroupQueue 与运行的衔接，以及 [可靠性状态机：Inbox、Turn、Outbox 与 Streaming Card 的持久投递](17-ke-kao-xing-zhuang-tai-ji-inbox-turn-outbox-yu-streaming-card-de-chi-jiu-tou-di) 了解任务结果送达 IM 后的可靠性机制。';export{e as default};

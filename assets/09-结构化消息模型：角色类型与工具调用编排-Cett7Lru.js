const t='在 MiniCode 中，`ChatMessage` 是贯穿"模型适配器 → 代理循环 → 工具执行 → 压缩器 → 会话持久化"全链路的唯一事实源（single source of truth）。本文剖析这一结构化消息模型的九种角色类型，解释它们如何在 Anthropic API 线格式与内部表示之间双向映射，以及代理循环如何基于该模型完成工具调用的编排、配对与记账。理解本页内容是阅读 [代理循环](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng) 与 [Anthropic 适配器](10-anthropic-gua-pei-qi-zhong-shi-tui-bi-xian-liu-yu-si-kao-kuai-bao-liu) 的必备前置。\n\n## 一、消息角色的完整图谱\n\n`ChatMessage` 是一个按 `role` 字段区分的**可辨识联合类型**（discriminated union），共九种角色，全部附带可选的 `MessageIdentity`（`id` 字段，用于跨压缩与持久化的消息追踪）：\n\n| 角色 | 核心字段 | 用途 | 是否携带用量元数据 |\n|---|---|---|---|\n| `system` | `content: string` | 系统提示词，通常只有一条 | 否 |\n| `user` | `content: string` | 用户输入或续接提示 | 否 |\n| `assistant_thinking` | `blocks: ProviderThinkingBlock[]` | 以**不透明块**保留供应商的推理内容 | 否 |\n| `assistant` | `content: string` | 模型最终文本回复 | 是（`ProviderUsageMetadata`） |\n| `assistant_progress` | `content: string` | 回合内的中间进度文本 | 是（`ProviderUsageMetadata`） |\n| `assistant_tool_call` | `toolUseId`、`toolName`、`input` | 模型发起的一次工具调用请求 | 是（`ProviderUsageMetadata`） |\n| `tool_result` | `toolUseId`、`toolName`、`content`、`isError` | 工具执行结果，通过 `toolUseId` 与调用配对 | 否 |\n| `context_summary` | `content`、`compressedCount`、`timestamp` | LLM 压缩（auto-compact / 上下文折叠）生成的摘要 | 否 |\n| `snip_boundary` | `content`、`removedMessageIds`、`removedCount`、`tokensFreed`、`timestamp` | 确定性裁剪（snip-compact）留下的边界占位 | 否 |\n\n定义见 [types.ts](src/types.ts#L23-L55)。两个设计要点值得注意：其一，`assistant_thinking` 不存储文本而存储 `ProviderThinkingBlock[]`，意味着推理内容对 MiniCode 本身是不透明的，仅作原样透传（round-trip）——这正是 [Anthropic 适配器](10-anthropic-gua-pei-qi-zhong-shi-tui-bi-xian-liu-yu-si-kao-kuai-bao-liu) 中"思考块保留"的根基；其二，`assistant_progress` 与 `assistant` 的分离，让"回合进行中"与"回合结束"在消息历史里拥有可辨别的结构，而非依赖文本内容猜测（[types.ts](src/types.ts#L27-L28)）。\n\n围绕这一核心类型，还有三个配套结构：\n\n- **`ToolCall`**（`id`、`toolName`、`input`）：适配器从 API 响应中解析出的工具调用，与 `assistant_tool_call` 消息一一对应（[types.ts](src/types.ts#L57-L61)）。\n- **`AgentStep`**：`ModelAdapter.next()` 的返回值，仅有 `assistant` 与 `tool_calls` 两种判别分支——前者携带 `content` 与 `kind`（`\'final\' | \'progress\'`），后者携带 `calls: ToolCall[]` 与可选 `content`、`contentKind`（[types.ts](src/types.ts#L69-L86)）。`ModelAdapter` 接口签名是 `next(messages: ChatMessage[]): Promise<AgentStep>`（[types.ts](src/types.ts#L88-L90)），即**整个消息历史作为输入、单步结果作为输出**，这正是代理循环每一步的契约。\n- **`CompressionResult`**：压缩器产出的 `messages + summary + 统计量`，其中 `summary` 被限定为 `context_summary` 角色（[types.ts](src/types.ts#L92-L98)）。\n\n## 二、从内部模型到 API 线格式：双向映射\n\n`ChatMessage` 是内部表示，而供应商 API 使用另一种线格式。Anthropic 适配器定义了 `AnthropicContentBlock`（`text` / `tool_use` / `tool_result` / 其他不透明块）与 `AnthropicMessage`（仅 `user` / `assistant` 两种角色、content 为块数组）（[anthropic-adapter.ts](src/anthropic-adapter.ts#L18-L27)）。转换的核心是 `toAnthropicMessages`，九种内部角色被映射到三种外部形态：\n\n```mermaid\nflowchart LR\n    subgraph internal["内部 ChatMessage 角色"]\n        A1[system] --> S[system 字段<br/>join \'\\n\\n\']\n        A2[user] --> U[user 文本块]\n        A3[assistant_thinking] --> T[assistant 消息<br/>thinking 块透传]\n        A4[assistant] --> AU[assistant 文本块]\n        A5[assistant_progress] --> AP[assistant 文本块<br/>包裹 &lt;progress&gt;]\n        A6[assistant_tool_call] --> TC[assistant 消息<br/>tool_use 块]\n        A7[tool_result] --> TR[user 消息<br/>tool_result 块]\n        A8[context_summary] --> US1[user 文本块<br/>Context Summary 前缀]\n        A9[snip_boundary] --> US2[user 文本块<br/>Snipped 提示]\n    end\n    subgraph wire["Anthropic API 消息"]\n        S\n        U\n        T\n        AU\n        AP\n        TC\n        TR\n        US1\n        US2\n    end\n```\n\n映射规则定义于 [anthropic-adapter.ts](src/anthropic-adapter.ts#L234-L302)，其中最关键的是 `pushAnthropicMessage` 的**同角色合并**行为：当相邻消息同为 `user` 或同为 `assistant` 时，新块被追加到上一条消息的 content 数组中，而不是新建消息（[anthropic-adapter.ts](src/anthropic-adapter.ts#L220-L232)）。这保证了三个 API 语义约束：\n\n1. **thinking 块必须紧邻其后的 tool_use 块、处于同一条 assistant 消息**——因此 `assistant_thinking` 与紧随的 `assistant_tool_call` 会被合并进同一条 assistant 消息（[anthropic-adapter.ts](src/anthropic-adapter.ts#L253-L257) 与 [anthropic-adapter.ts](src/anthropic-adapter.ts#L269-L277)）。\n2. **tool_result 必须位于 user 消息**——`tool_result` 内部角色被映射为 `tool_use_id` 块推入 user 消息（[anthropic-adapter.ts](src/anthropic-adapter.ts#L293-L298)）。\n3. **压缩产物不得破坏对话连续性**——`context_summary` 与 `snip_boundary` 都以 user 文本块形式注入，并分别带上前缀 `[Context Summary from earlier conversation]` 与精简版 Snipped 提示（[anthropic-adapter.ts](src/anthropic-adapter.ts#L279-L291)、[snipCompact.ts](src/compact/snipCompact.ts#L365-L372)）。\n\n反向映射发生在响应解析阶段：适配器遍历响应 content 块，将 `text` 块拼接、`tool_use` 块收集为 `ToolCall[]`、`thinking` / `redacted_thinking` 块收进 `thinkingBlocks`，最终根据"是否有 tool_use"决定返回 `tool_calls` 还是 `assistant` 类型的 `AgentStep`（[anthropic-adapter.ts](src/anthropic-adapter.ts#L375-L436)）。\n\n### 进度标记协议：`<progress>` / `<final>`\n\n映射中存在一层"文本协议"：`parseAssistantText` 识别 `content` 开头的 `<final>`、`[FINAL]`、`<progress>`、`[PROGRESS]` 标记，将其剥离并归类为 `kind: \'final\' | \'progress\'`（[anthropic-adapter.ts](src/anthropic-adapter.ts#L153-L187)）；反向发送时，`assistant_progress` 消息会被重新包裹为 `<progress>\\n...\\n</progress>` 文本块（[anthropic-adapter.ts](src/anthropic-adapter.ts#L210-L218)）。这一协议在系统提示词中显式告知模型（[prompt.ts](src/prompt.ts#L25-L30)），形成"模型用标记自我声明回合状态 → 适配器解析 → 代理循环按角色分派"的闭环。\n\n## 三、工具调用编排：一次完整的工具回合\n\n当 `AgentStep.type === \'tool_calls\'` 时，代理循环进入工具编排分支。整个流程可分为"执行"与"回写消息历史"两个阶段：\n\n```mermaid\nsequenceDiagram\n    participant Model as ModelAdapter\n    participant Loop as runAgentTurn\n    participant Reg as ToolRegistry\n    participant Storage as tool-result-storage\n\n    Loop->>Model: next(messages)\n    Model-->>Loop: AgentStep { type:\'tool_calls\', calls:[...] }\n    Loop->>Loop: appendThinkingBlocks(thinkingBlocks)\n    Note over Loop: 若 step.content 存在：<br/>progress → assistant_progress + 续接提示<br/>final 文本 → assistant 消息\n    loop 每个 call\n        Loop->>Reg: execute(toolName, input, {cwd, permissions})\n        Reg->>Reg: zod safeParse(input) 校验\n        Reg-->>Loop: ToolResult {ok, output, awaitUser?}\n        Loop->>Storage: replaceLargeToolResult(...) 落盘替换\n    end\n    Loop->>Storage: applyToolResultBudget(批量预算)\n    Loop->>Loop: 构造 assistant_tool_call 消息数组\n    Loop->>Loop: 追加 tool_result 消息数组\n    Note over Loop: messages = [...messages, ...toolCalls, ...toolResults]\n```\n\n**执行阶段**（[agent-loop.ts](src/agent-loop.ts#L374-L406)）：循环遍历 `next.calls`，对每个调用执行 `args.tools.execute()`。`ToolRegistry.execute` 内部先通过名称查找工具定义，再用 `tool.schema.safeParse(input)` 做本地 Zod 校验——校验失败、工具抛异常都被吞进 `ToolResult` 的 `ok: false` 分支而非向上抛出（[tool.ts](src/tool.ts#L95-L124)）。每个结果随后经过 `replaceLargeToolResult` 处理：超过 50K 字符的输出被写入磁盘、以 `<persisted-output>` 预览标签替换，且同一 `toolUseId` 的替换结果会被缓存复用（[tool-result-storage.ts](src/utils/tool-result-storage.ts#L142-L196)）。批处理后 `applyToolResultBudget` 再对整批结果施加 200K 字符的可见预算，从大到小把超预算项落盘（[tool-result-storage.ts](src/utils/tool-result-storage.ts#L198-L292)）。\n\n**回写阶段**（[agent-loop.ts](src/agent-loop.ts#L416-L437)）：循环为每个 call 构造一条 `assistant_tool_call` 消息（`toolUseId`、`toolName`、`input` 原样保留），为每个结果构造一条 `tool_result` 消息（携带 `isError` 标志），然后整体追加到消息历史。这里有一个精妙的记账细节：**用量元数据只挂在最后一个 `assistant_tool_call` 上**——`withProviderUsage(toolCallMessage, i === calls.length - 1 ? next.usage : undefined)`（[agent-loop.ts](src/agent-loop.ts#L424-L427)）。因为供应商的 usage 是针对整次 API 请求的，多次工具调用共享同一份用量，挂在最后一条即可避免重复计数，同时保证 `tokenCountWithEstimation` 从尾部扫描时能命中。\n\n两种特殊终止路径也值得留意：若某个工具结果标记 `awaitUser`（即 `ask_user` 工具），循环把问题文本作为 `assistant` 消息回写后立即结束回合（[agent-loop.ts](src/agent-loop.ts#L439-L453)）；若达到 `maxSteps` 上限，则追加一条中文终止说明的 `assistant` 消息（[agent-loop.ts](src/agent-loop.ts#L456-L464)）。\n\n## 四、工具调用配对的完整性约束\n\n`assistant_tool_call` 与 `tool_result` 通过 `toolUseId` 一一配对，这是消息模型最重要的结构不变量。整个代码库中有四处独立机制共同维护它：\n\n| 机制 | 位置 | 策略 |\n|---|---|---|\n| API 轮次分组 | [compact.ts](src/compact/compact.ts#L11-L44) | `groupMessagesByApiRound` 把 `assistant_thinking` + 连续 `assistant_tool_call` + 连续 `tool_result` 视为一个"API 轮次"整体，压缩边界对齐时不会切开轮次（[compact.ts](src/compact/compact.ts#L46-L56)） |\n| 孤儿保护 | [snipCompact.ts](src/compact/snipCompact.ts#L148-L195) | `buildMessageGroups` 将未闭合的 `assistant_tool_call`（无匹配 result）标记为 `unclosed_tool_call` 并保护；孤立的 `tool_result`（无对应 call）标记为 `orphan_tool_result` 并保护，二者都不可被 snip 删除 |\n| 闭合性校验 | [context-collapse.ts](src/compact/context-collapse.ts#L328-L353) | `toolGroupIsClosed` 用两个 Set（calls 与 results 的 toolUseId）做双向包含校验，任何一侧缺失即视为未闭合、该组受保护 |\n| 可清理工具白名单 | [context.ts](src/utils/context.ts#L118-L124) 与 [microcompact.ts](src/compact/microcompact.ts#L6-L50) | 微压缩只清空 `read_file`、`run_command`、`search_files` 等只读工具的结果内容（替换为 `[Output cleared for context space]` 标记），编辑类工具永远不被触碰 |\n\n这套约束的意义在于：任何压缩/裁剪操作都必须**以消息为原子单位**或**以完整配对为单位**执行，防止把"调用存在但结果丢失"或"结果存在但调用丢失"的半截序列喂给模型——那会直接破坏 Anthropic API 的 `tool_use`/`tool_result` 交替要求。\n\n## 五、角色元数据与上下文记账\n\n结构化消息模型不只是"能表达"，还为上下文管理提供了精确的记账能力。`ProviderUsageMetadata`（`providerUsage` + `usageStale` + `usageStaleReason`）只附着于 `assistant`、`assistant_progress`、`assistant_tool_call` 三种角色（[types.ts](src/types.ts#L8-L12)）。`tokenCountWithEstimation` 从消息尾部向前扫描，命中最近的带用量消息后，以其 `totalTokens` 为精确基准、对更晚的消息按角色估算——因此只要消息流末尾有 assistant 类消息，用量记账就是"供应商精确值 + 尾部估算值"的混合模式（[token-estimator.ts](src/utils/token-estimator.ts#L92-L103)、[token-estimator.ts](src/utils/token-estimator.ts#L125-L156)）。\n\n每种角色的估算采用不同的"字符/令牌"比例，反映了不同内容形态的令牌密度差异：\n\n| 角色 | 字符/令牌 | 说明 |\n|---|---|---|\n| `system` / `assistant` / `context_summary` / `snip_boundary` | 3.5 | 长文本、自然语言密度高 |\n| `user` / `assistant_thinking` / `assistant_progress` | 3.0 | 常规密度 |\n| `assistant_tool_call` | 2.5 | JSON 结构输入，令牌效率高 |\n| `tool_result` | 2.0 | 命令输出等原始文本，令牌效率最高 |\n\n见 [token-estimator.ts](src/utils/token-estimator.ts#L34-L44)。`markProviderUsageStale` 是配套的另一半：压缩、snip、上下文折叠发生之后，保留消息上原有的用量数据会失真（因为前面被压缩的内容已不在请求中），此时将 `usageStale` 置位并记录原因（如 `conversation was context-collapsed in the model-visible projection...`，见 [context-collapse.ts](src/compact/context-collapse.ts#L78-L79)），后续记账便回退到纯估算模式。关于记账与估算的完整讨论见 [供应商用量优先的上下文记账与令牌估算](11-gong-ying-shang-yong-liang-you-xian-de-shang-xia-wen-ji-zhang-yu-ling-pai-gu-suan)。\n\n## 六、角色在持久化与 UI 层的映射\n\n消息模型最终还要落到 JSONL 会话日志与终端界面。`session.ts` 的 `roleToType` 将内部角色映射为事件类型：\n\n| 内部角色 | 事件类型 | UI 呈现 |\n|---|---|---|\n| `system` | `system` | 不可见 |\n| `user` | `user` | 用户气泡 |\n| `assistant` | `assistant` | 助手回复 |\n| `assistant_thinking` | `thinking` | 不可见（透传块） |\n| `assistant_progress` | `progress` | 进度文本 |\n| `assistant_tool_call` | `tool_call` | 工具调用卡片 |\n| `tool_result` | `tool_result` | 工具输出 |\n| `context_summary` | `summary` | `[Context summary: N messages compressed]` |\n| `snip_boundary` | `snip_boundary` | `Snipped earlier context: removed N messages, freed ~X tokens` |\n\n角色映射见 [session.ts](src/session.ts#L61-L74)，UI 渲染见 [tty-app.ts](src/tty-app.ts#L975-L998)。值得注意的是会话恢复（`resumeSession`）时，系统消息会被提取、重建为当前提示词后置于头部，其余角色原样恢复（[tty-app.ts](src/tty-app.ts#L955-L967)）。`snip_boundary` 事件额外携带 `snipMetadata`，使被删消息的 ID 集合得以在日志中留痕，供恢复时重建边界（[session.ts](src/session.ts#L93-L102)）。持久化机制的整体设计见 [追加式 JSONL 事件日志与断点恢复](19-zhui-jia-shi-jsonl-shi-jian-ri-zhi-yu-duan-dian-hui-fu)。\n\n## 总结与下一步\n\nMiniCode 的结构化消息模型用九个角色覆盖了对话的全部形态——从系统指令、用户输入、透明的推理块、进度与最终回复，到可配对的工具调用/结果，再到两种压缩产物。它通过"可辨识联合类型 + 角色专属元数据"为代理循环提供了稳定的执行契约：适配器负责双向映射并维护 API 语义，循环负责编排与回写，压缩器与裁剪器则通过配对约束保证消息历史的完整性。\n\n建议的后续阅读路径：\n\n1. **[代理循环](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng)**——本页的编排细节正是该循环的分支实现；\n2. **[Anthropic 适配器](10-anthropic-gua-pei-qi-zhong-shi-tui-bi-xian-liu-yu-si-kao-kuai-bao-liu)**——深入思考块保留与重试限流机制；\n3. **[统一工具协议](8-tong-gong-ju-xie-yi-zhu-ce-schema-xiao-yan-yu-ji-zhong-zhi-xing)**——`ToolDefinition` 与 `ToolRegistry` 的完整设计；\n4. **[确定性裁剪压缩](13-que-ding-xing-cai-jian-ya-suo-bao-hu-bian-ji-yu-cuo-wu-lun-ci-de-an-quan-shan-chu)** 与 **[上下文折叠投影](14-shang-xia-wen-zhe-die-tou-ying-ke-zhai-yao-qu-jian-shi-bie-yu-ti-huan)**——配对约束在压缩器中的具体应用。';export{t as default};

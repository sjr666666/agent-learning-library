const e='Miniclaw 的用量与计费体系回答一个核心问题：当多个成员各自拥有工作区、Agent 与 IM 渠道时，系统如何把每一次模型调用折算成可审计的金额，如何在余额不足或配额触顶时拦截新的执行，以及如何把这些数据以可信口径呈现给用户和管理员。整套体系由四个协作层构成：Runner 侧的**token 采集**、后端的**确定性计价**、单事务提交的**原子账本**，以及贯穿消息入口的**配额门控**。本文逐一拆解这四层的实现机制与一致性设计。\n\n## 体系总览：四层职责划分\n\n理解这套体系的第一步是厘清职责边界：采集层只负责把 SDK 返回的累计计数器变成可靠的增量事件；计价层把 token 数量换算成美元金额；账本层在单个 SQLite 事务中落盘所有维度；门控层则在每次消息进入执行管线之前做前置校验。SDK 自带的 `costUSD` 估算被有意排除在记账权威之外——它既不是身份的一部分，也不是金额的来源，仅在流式展示前被替换为账本口径的数值。\n\n```mermaid\nflowchart TB\n    subgraph COLLECT["① 采集层"]\n        R["Pi Agent Runner<br/>result-usage.ts<br/>modelUsage 高水位 delta"]\n    end\n    subgraph PRICE["② 计价层"]\n        K["kaboo-pricing.ts<br/>五类 token 单价 × 分位取整"]\n        U["usage-service.ts<br/>eventId 身份 + shouldCharge 判定"]\n    end\n    subgraph LEDGER["③ 原子账本层（db.ts 单事务）"]\n        E["usage_events<br/>事件幂等主键"]\n        M["usage_records<br/>event_id+model 唯一"]\n        Q["monthly_usage / daily_usage<br/>配额台账"]\n        W["user_balances /<br/>balance_transactions<br/>钱包扣费"]\n    end\n    subgraph GATE["④ 门控与展示层"]\n        G["billing.ts<br/>checkBillingAccess 三态拦截"]\n        API["routes/usage.ts + routes/billing.ts"]\n        FE["Web UsagePage / 飞书卡片用量注脚"]\n    end\n    R -->|"StreamEvent(type=usage)<br/>eventId + batchIndex"| U\n    U --> K\n    U --> E & M & Q & W\n    G -->|"拒绝/放行"| R\n    Q --> G\n    W --> G\n    E & M --> API --> FE\n```\n\n这一分层意味着任何一层都不持有"真相"的独占权：token 数量的权威是 Runner 归一化后的 modelUsage 行，金额的权威是 Kaboo 对齐的本地计价函数，而两者的绑定关系由事件级 `eventId` 在整个链路中保持不变。\n\nSources: [usage-service.ts](src/usage-service.ts#L113-L121)、[result-usage.ts](container/agent-runner/src/result-usage.ts#L1-L14)\n\n## 用量采集：从累计计数器到幂等事件\n\nClaude Agent SDK 的 result 消息携带的是**累计**的 `modelUsage` 计数器，而一次长会话可能产出多个 result。Runner 侧的 `extractResultUsage` 因此采用高水位差值策略：对每个模型的每一类 token 计数器维护快照，新结果到来时只取 `current - previous` 的增量；若计数器不增反降，则视作上游开启了新纪元（例如切换供应商后重置），直接把当前值当作增量，避免丢失切换后的首个结果。没有产生可计费增量的模型不会生成虚假的模型调用记录。\n\nSources: [result-usage.ts](container/agent-runner/src/result-usage.ts#L85-L107)、[result-usage.ts](container/agent-runner/src/result-usage.ts#L148-L184)\n\n采集到的用量通过 StreamEvent 的 `usage` 载荷传递回主进程，其中包含稳定的事件标识 `eventId` 以及批次位置字段 `batchIndex`/`batchCount`（用于把一次逻辑运行的多条 result 归并为一条账目）。当调用方未携带显式 `eventId` 时，后端用 SHA-256 对 `userId + groupFolder + agentId + messageId + source + usage` 指纹做确定性派生——注意指纹载荷刻意排除了 `costUSD`，因为 SDK 的美元估算不属于逻辑事件的同一性定义，同一消息即使估算值不同也必须是同一个事件。\n\nSources: [stream-event.ts](shared/stream-event.ts#L369-L389)、[usage-service.ts](src/usage-service.ts#L93-L111)\n\n主进程唯一的落库入口是 `writeUsageRecords` 包装函数，它在两条消息处理管线的 `eventType === \'usage\'` 分支上被调用。这里有一个关键的展示一致性设计：**在广播流式事件之前**，先把用量写入账本，再用账本返回的 `providerEstimatedCostUSD` 覆盖 `streamEvent.usage.costUSD`——这样 Web 与飞书卡片上实时跳动的金额和事后用量页面的统计永远同口径；后续持久化若因重放再次触发，会被 `eventId` 幂等去重。对于一次回合内多条 result 的情况，主进程用 `mergeHeldUsage` 把中间批次的用量逐类累加，直到最后一个批次才向外发布完整总量。\n\nSources: [index.ts](src/index.ts#L2344-L2383)、[index.ts](src/index.ts#L7212-L7253)、[index.ts](src/index.ts#L1369-L1412)\n\n## Kaboo 对齐计价：五类 token 与分位取整边界\n\n计价模块 `kaboo-pricing.ts` 是一套与外部 Kaboo 计费系统的种子数据保持一致的确定性价格表，其文件头注明了对应的 SQL 迁移与 Go 服务源。每条规则按模型名模式匹配五种 token 分类：普通输入、普通输出、缓存读取、缓存写入与推理 token。匹配算法复刻了 Kaboo 的语义——大小写不敏感的子串包含、忽略尾部 `%` 通配符、最长模式优先；没有任何规则命中时回落到 Claude Sonnet 档单价（$3/$15 每百万 token），这是一个刻意选择的安全兜底。\n\n| Token 分类 | Sonnet 4.5 单价（$/百万 token） | 说明 |\n|---|---|---|\n| 输入 inputTokens | 3.00 | 非缓存的普通输入 |\n| 输出 outputTokens | 15.00 | 普通输出 |\n| 缓存读取 cacheRead | 0.30 | 命中 prompt cache |\n| 缓存写入 cacheCreation | 3.75 | 输入价的 1.25 倍 |\n| 推理 reasoningTokens | 15.00 | 与输出同价 |\n\nSources: [kaboo-pricing.ts](src/kaboo-pricing.ts#L1-L65)、[kaboo-pricing.ts](src/kaboo-pricing.ts#L92-L200)、[kaboo-pricing.ts](src/kaboo-pricing.ts#L232-L247)\n\n金额换算有一条严格的取整纪律：所有成本先以未取整浮点参与计算，随后统一 `Math.round(cost * 100)` 折算成整数分，只有过了这个分位边界才允许转换回美元。由于一次逻辑事件可能由多次小规模的 API 调用累积而成（尤其流式场景），直接对每次调用单独取分会造成亚分损耗。为此系统引入 **UTC 半小时计价桶**：写入某模型行之前，先查询同一 `userId + groupFolder + source + model` 在当前半小时桶内的已有 token 总量，分别计算"桶内旧总量"和"桶内新旧合计"的分位成本，两者之差即为该事件的增量金额。这样桶内全部增量之和恰好等于 Kaboo 对整个桶一次性取整的结果，亚分进位得以在小调用之间正确传递，且残差不会跨过桶边界污染下一个时段。\n\nSources: [kaboo-pricing.ts](src/kaboo-pricing.ts#L249-L280)、[db.ts](src/db.ts#L3554-L3603)、[usage-service.ts](src/usage-service.ts#L155-L192)\n\n另一个重要规则是**以 modelUsage 重建事件根**：`priceKabooUsageByModel` 收到事件根用量与逐模型用量两份数据时，总是先用清洗后的模型行重新加总出事件根，仅当模型行为空时才把根用量归入 `unknown` 模型。这保证了事件、模型、配额与钱包四本账永远共享完全相同的 token 基数，不会因根声明与明细不一致而出现口径分裂。\n\nSources: [kaboo-pricing.ts](src/kaboo-pricing.ts#L282-L348)、[kaboo-pricing.test.ts](tests/kaboo-pricing.test.ts#L87-L163)\n\n## 原子账本：单事务写入五本账\n\n`recordUsageEventBatch` 是整个体系的持久化核心，它在一个 better-sqlite3 事务中完成五件事，任一环节失败则整体回滚：\n\n```mermaid\nsequenceDiagram\n    participant S as usage-service\n    participant T as recordUsageEventBatch（单事务）\n    participant E as usage_events\n    participant R as usage_records + daily_summary\n    participant Q as monthly/daily_usage\n    participant W as user_balances\n\n    S->>T: eventId + 模型行 + trackBillingUsage + chargeBalance\n    T->>E: INSERT OR IGNORE（幂等主键）\n    alt changes === 0（重放）\n    T-->>S: { inserted: false }，其余步骤全部跳过\n    else 首次写入\n    T->>R: 逐模型插入明细 + UPSERT 日汇总\n    T->>Q: incrementUsageBoth（月/日配额同事务双写）\n    T->>W: adjustUserBalance(-billed, idempotency_key)\n    T-->>S: { inserted: true }\n    end\n```\n\n第一道防线是 `INSERT OR IGNORE usage_events`：事件表以 `eventId` 为主键，若插入影响行数为零说明是重放，函数立即返回且**连配额计数器都不会重复累加**。第二层保护是 `idx_usage_event_model` 唯一部分索引（`WHERE event_id IS NOT NULL`），从数据库层面杜绝同一事件的同一模型行被写入两次。v51 迁移为存量数据库补齐了 `event_id`、两个成本列与 `usage_date` 物化日期列，并对旧行回填稳定的派生事件 ID。\n\nSources: [db.ts](src/db.ts#L3633-L3774)、[db.ts](src/db.ts#L1217-L1234)、[schema-v51-usage.test.ts](tests/schema-v51-usage.test.ts#L22-L60)\n\n进入配额台账的 token 定义值得注意：`billableInput` 是输入、缓存读取与缓存写入三类之和，`billableOutput` 是输出与推理之和，而入账金额使用乘以套餐倍率后的 `billedCostUSD`。这意味着**即使某个事件计价为零成本（如全免费模型或管理员账户），它的 token 数量依然会计入用户的日/月配额**——配额限制的是资源消耗而非仅仅是金钱支出。\n\nSources: [db.ts](src/db.ts#L3734-L3751)、[usage-accounting.test.ts](tests/usage-accounting.test.ts#L164-L230)\n\n钱包扣费同样嵌在同一事务中：`adjustUserBalance` 支持幂等键，扣费固定使用 `usage_event_${eventId}` 作为键，配合 `balance_transactions.idempotency_key` 上的唯一部分索引，使同一事件的重放扣费直接返回既有交易记录。扣费方向区分处理：充值累加 `total_deposited_usd`，消费累加 `total_consumed_usd`；用量扣费允许余额转负（`allowNegative: true`），而管理员充值类操作禁止负余额。`usage-service.recordUsageEvent` 在事务外还有一次兼容性的 `deductUsageCost` 调用，它因共享相同幂等键而不会二次扣款，作用只是触发既有的计费审计与访问状态翻转钩子（`wallet_blocked` / `wallet_unblocked`）。\n\nSources: [db.ts](src/db.ts#L13557-L13635)、[usage-service.ts](src/usage-service.ts#L224-L234)、[billing.ts](src/billing.ts#L664-L706)\n\n## 订阅套餐与配额门控\n\n计费的开关与参数来自系统设置：`billingEnabled` 默认关闭，另有最低起用余额 `billingMinStartBalanceUsd`（默认 $0.01）与展示币种配置。开启后，一切门控围绕**有效套餐**展开：优先取用户真实的活跃订阅，若无则回落到标记为默认的 `billing_plans` 行（构造一个虚拟的 `fallback_` 订阅），完全没有可用套餐时用户会被彻底拦下。\n\nSources: [billing.ts](src/billing.ts#L44-L81)、[runtime-config.ts](src/runtime-config.ts#L3965-L3975)\n\n套餐表的字段决定了系统能表达的限制粒度：\n\n| 字段族 | 代表字段 | 作用 |\n|---|---|---|\n| 三窗口费用配额 | `daily_cost_quota` / `weekly_cost_quota` / `monthly_cost_quota` | 各时间窗内的美元消费上限 |\n| 三窗口 token 配额 | `daily_token_quota` / `weekly_token_quota` / `monthly_token_quota` | 各时间窗内的 token 总量上限 |\n| 计价倍率 | `rate_multiplier` | billed 成本的乘数，可实现折扣或加价 |\n| 资源上限 | `max_groups` / `max_im_channels` / `max_mcp_servers` / `max_storage_mb` / `max_concurrent_containers` | 工作区数量、渠道数、MCP 数、存储与并发容器 |\n| 商业属性 | `trial_days` / `display_price` / `highlight` / `is_default` | 试用天数、展示价、推荐标记、默认套餐 |\n\nSources: [db.ts](src/db.ts#L1000-L1046)、[billing.ts](src/billing.ts#L472-L575)\n\n配额判定的核心是 `checkQuota`：管理员与未开启计费两种情况直接放行；其余结果经内存 LRU 缓存加速（容量 500 条、TTL 30 秒，Map 插入序模拟淘汰）。内部按**日 → 周 → 月**顺序依次检查费用与 token 两类上限，第一个触顶的窗口决定拒绝理由与重置时间（次日零点、下周一零点、下月一号）；全部通过时汇总各窗口的使用百分比取最大值作为预警指标。\n\nSources: [billing.ts](src/billing.ts#L137-L202)、[billing.ts](src/billing.ts#L361-L470)\n\n对外统一的准入判断是 `checkBillingAccess`，它把三种阻断形态收敛为一个带原因的结构化结果，并支持绕过缓存的新鲜版本供强一致场景使用：\n\n```mermaid\nflowchart TD\n    A["checkBillingAccess"] --> B{"role=admin 或<br/>计费未开启？"}\n    B -->|是| OK["allowed=true"]\n    B -->|否| C["读取余额与有效套餐"]\n    C --> D{"无有效套餐？"}\n    D -->|是| P["plan_inactive<br/>未找到可用套餐"]\n    D -->|否| E{"余额 < minStartBalanceUsd？"}\n    E -->|是| F["insufficient_balance<br/>请联系管理员充值"]\n    E -->|否| G{"checkQuota 触顶？"}\n    G -->|是| H["quota_exceeded<br/>附 resetAt 重置提示"]\n    G -->|否| OK2["allowed=true<br/>附 warningPercent 预警"]\n```\n\nSources: [billing.ts](src/billing.ts#L204-L334)\n\n这个判定被嵌入三个消息入口，形成完整的执行前置闸门：\n\n| 入口 | 位置 | 校验时机 | 拒绝后的行为 |\n|---|---|---|---|\n| IM 渠道消息 | `processGroupMessages` | 按 workspace 真实 owner 校验 | 经持久投递路由发送 `__billing__` 系统通知，未被确认则保留游标待重试 |\n| Web 聊天消息 | `web.ts` 消息注入 | 缓存版 `checkBillingAccess` | 落库系统消息并 WebSocket 广播，排队消息标记为 released |\n| 定时任务调度 | 任务执行前 | 新鲜版 `checkBillingAccessFresh` | run log 写入「计费限制」错误，任务不启动 |\n\n三个入口共同遵循一条原则：**校验对象是工作区的真实创建者而非操作发起者**，因此管理员代建的跨组任务仍然消耗并受限于所属成员的套餐。IM 入口使用新鲜版检查是为了避免 30 秒缓存让刚充值的用户继续被拒。\n\nSources: [index.ts](src/index.ts#L7009-L7055)、[web.ts](src/web.ts#L606-L650)、[task-scheduler.ts](src/task-scheduler.ts#L1029-L1055)\n\n## 钱包运营：调整、兑换码与审计\n\n管理员的资金操作集中在 `applyAdminBalanceAdjustment`：调整前后各做一次访问状态快照，状态发生翻转时写入 `wallet_blocked` 或 `wallet_unblocked` 审计事件，让"何时因何被断供/恢复"有据可查。用户自助通道则是兑换码：`redeemCode` 支持三种类型——余额型直接入账、订阅型激活指定套餐、试用型在现有试用期基础上顺延。发放核销采用**乐观锁**：先原子递增 `used_count` 并登记使用者，成功后才执行入账或开套餐，且入账同样携带幂等键防止并发重复到账。\n\nSources: [billing.ts](src/billing.ts#L577-L617)、[billing.ts](src/billing.ts#L710-L816)\n\n所有敏感动作（分配套餐、取消订阅、手动充值/扣减、兑换码核销、计费设置变更、钱包封锁/解封）统一经过 `logBillingAudit` 写入 `billing_audit_log` 表，形成与资金流水并列的操作审计线。\n\nSources: [db.ts](src/db.ts#L1110-L1119)、[billing.ts](src/billing.ts#L119-L133)\n\n## 查询与呈现：分析 API 与双端展示\n\n用量查询基于物化的 `usage_records` 明细表实时聚合，`getUsageAnalytics` 一次返回四组数据：summary 汇总（其中**运行次数定义为 `COUNT(DISTINCT event_id)`**，与模型调用次数 `COUNT(*)` 严格区分）、按日期×模型×用户×Agent×工作区×来源的 breakdown、纯日期维度的 daily 序列，以及 models/agents/workspaces/sources 四个维度的归因榜单。API 层强制非管理员只能查看自己的数据，管理员可传入 `userId` 下钻；CSV 导出限制单次一万行并对以 `=+-@` 开头的单元格加引号前缀，防止电子表格公式注入。\n\nSources: [db.ts](src/db.ts#L3967-L4036)、[routes/usage.ts](src/routes/usage.ts#L21-L98)、[routes/usage.ts](src/routes/usage.ts#L185-L235)\n\n`/api/usage/stats` 响应中的 `billing` 元数据块明确声明了两套金额的语义——`providerEstimateCostUSD` 是 "kaboo-utc-30m-rounded-estimate"（供应侧估算），`billedCostUSD` 是 "actual-charge"（实际扣费），前端据此决定是否向成员展示计费列。用户侧的账务信息由 `/api/billing/my/*` 端点族提供（订阅、余额、当月用量、交易流水、配额进度、准入状态）；管理侧端点则统一挂在 `manage_billing` 权限之后，代码注释特别指出计费配置刻意独立于通用系统设置权限——`manage_system_config` 不得隐含计费控制能力。\n\nSources: [routes/usage.ts](src/routes/usage.ts#L117-L134)、[routes/billing.ts](src/routes/billing.ts#L76-L132)、[routes/billing.ts](src/routes/billing.ts#L150-L232)\n\n前端对应两个懒加载页面：`UsagePage` 提供趋势图/表格双视图、7/14/30/90 天周期切换、四维归因排序与 CSV 导出；`BillingPage` 承载套餐与钱包信息。IM 侧的呈现由 `feishu-usage-display.ts` 完成，它把五类 token 汇总为一行权威总数（如 `12.3K tokens（输入 1.2K · 输出 800 · 缓存读取 10.1K）`），并规定全零信封必须显示「Token 未上报」而非误导性的 0。\n\nSources: [UsagePage.tsx](web/src/pages/UsagePage.tsx#L29-L77)、[feishu-usage-display.ts](src/feishu-usage-display.ts#L26-L73)\n\n## 周期任务：过期、对账与清理\n\n三个后台定时器维持账本的长期健康：每小时调用 `expireSubscriptions` 使到期订阅失效并清空相关缓存；每 6 小时对全体非管理员活跃用户执行**月度用量对账**——将 `monthly_usage` 与 `daily_usage` 按月聚合对比，漂移超过 $0.01 时以日聚合为基准修正月度台账，这是防止进程在历史双写间隙崩溃留下永久偏差的兜底安全网；每 24 小时清理过期的日用量与审计日志行。\n\nSources: [index.ts](src/index.ts#L20542-L20591)、[billing.ts](src/billing.ts#L854-L910)\n\n## 设计要点回顾\n\n回顾全文，这套体系的一致性建立在四个可验证的不变量之上：其一，**单一权威**——token 以 modelUsage 行为权威、金额以本地 Kaboo 计价为权威，SDK 估算被系统性排除；其二，**事件幂等**——从事件表主键、明细唯一索引到钱包幂等键的三层数据库约束，使任意重放都收敛为无操作；其三，**单事务多账本**——明细、配额、钱包在同一个 SQLite 事务中成败与共，不存在中间态；其四，**前置门控**——三条消息入口在工作开始前统一按工作区真实 owner 做准入裁决。理解这些不变量后，排查"金额对不上""配额没释放"一类问题时，可以沿着 eventId 从 `usage_events` 出发逐层核对四本账。\n\n如果想继续深入相邻机制，推荐阅读 [SQLite 持久化：Schema 版本化、迁移策略与核心表族](9-sqlite-chi-jiu-hua-schema-ban-ben-hua-qian-yi-ce-lue-yu-he-xin-biao-zu) 了解账本表的版本演进，[StreamEvent 流式契约：三端共享类型与同步机制](25-streamevent-liu-shi-qi-yue-san-duan-gong-xiang-lei-xing-yu-tong-bu-ji-zhi) 了解 usage 事件的传输协议，以及 [定时任务调度器：Cron、固定间隔、一次性任务与重启补跑](21-ding-shi-ren-wu-diao-du-qi-cron-gu-ding-jian-ge-ci-xing-ren-wu-yu-zhong-qi-bu-pao) 了解计费门控如何约束自动化执行。';export{e as default};

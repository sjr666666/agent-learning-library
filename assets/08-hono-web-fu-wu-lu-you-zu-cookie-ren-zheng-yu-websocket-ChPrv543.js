const e='Miniclaw 的对外服务面收敛在 `src/web.ts` 这一个文件里（约 3400 行）：它用 Hono 组装出全部 HTTP API，用 `@hono/node-server` 绑定端口，再用 `ws` 库在同一条 Node HTTP Server 上挂载 WebSocket 通道。本文从三个维度剖析这条服务面——**路由族的挂载结构**、**基于 HMAC 签名 Cookie 的认证中间件**、以及**带独立认证路径的 WebSocket 协议**——帮助你在阅读具体业务路由之前，先建立"请求如何进来、如何被验证、事件如何推出去"的整体心智模型。\n\n## 应用组装：单文件内的三层职责\n\n`src/web.ts` 的顶部同时承担了三种角色：创建带类型化上下文的 Hono 实例、持有终端会话的双向映射、以及声明对核心运行时的依赖接口。Hono 实例通过泛型 `{ Variables: Variables }` 注入类型——`c.get(\'user\')` 与 `c.get(\'sessionId\')` 在编译期就有确定形状，这是整个认证体系的类型锚点。紧随其后的两个 `Map` 构成了容器终端的所有权模型：`wsTerminals` 记录"哪条连接打开了哪个工作区的终端"，`terminalOwners` 反向记录"哪个工作区的终端当前归谁所有"，两者共同保证一个终端同一时刻只有一个控制者。\n\n```mermaid\nflowchart LR\n    subgraph Client["客户端"]\n        FE["Web 前端<br/>React SPA"]\n    end\n    subgraph Node["Node HTTP Server (@hono/node-server)"]\n        subgraph Hono["Hono App (src/web.ts)"]\n            COMP["compress()"]\n            CORS["cors() 仅挂 /api/*"]\n            subgraph Routes["路由族 app.route()"]\n                R1["/api/auth"]\n                R2["/api/groups ..."]\n                R3["其余 17 个子模块"]\n            end\n            INLINE["内联路由<br/>POST /api/messages 等"]\n            STATIC["serveStatic + SPA 兜底"]\n        end\n        WSUP["server.on(\'upgrade\')<br/>setupWebSocket"]\n    end\n    subgraph Core["核心运行时 (index.ts 注入)"]\n        Q["GroupQueue"]\n        DB[("SQLite db.ts")]\n        IM["IM 渠道管理器"]\n    end\n    FE -- "HTTP /api/* (Cookie)" --> COMP\n    FE -- "WS /ws (Upgrade)" --> WSUP\n    COMP --> CORS --> Routes\n    Routes --> Q & DB & IM\n```\n\n真正的运行时能力并不在 `web.ts` 内部实现，而是通过 `WebDeps` 接口在启动时一次性注入——队列、游标推进、渠道重连等约三十个回调构成了 Web 层与核心层的唯一边界。这种"窄接口 + 宽注入"的设计让路由层可以独立测试：测试工具 `createAppForTest` 只需传入一套伪造的 `WebDeps` 就能拿到完整装配好的 Hono 应用。\n\nSources: [web.ts](src/web.ts#L142-L145)、[web-context.ts](src/web-context.ts#L31-L120)、[web.ts](src/web.ts#L244-L249)、[web.ts](src/web.ts#L3263-L3273)、[index.ts](src/index.ts#L20348-L20375)\n\n## 全局中间件与服务器启动参数\n\n中间件链刻意保持极简：全局只挂了一个 `compress()`（gzip 压缩 API JSON 与静态资源），CORS 则只作用于 `/api/*`。来源校验函数 `isAllowedOrigin` 实现了三层放行逻辑——同源请求无 `Origin` 头直接放行；`localhost`/`127.0.0.1` 任意端口默认放行（可用 `CORS_ALLOW_LOCALHOST=false` 关闭）；其余来源必须命中 `CORS_ALLOWED_ORIGINS` 环境变量的逗号白名单，设为 `\'*\'` 则完全关闭防御。`credentials: true` 是必需配置，因为整套认证依赖 Cookie 跨请求携带。\n\n服务器启动由 `startWebServer(webDeps)` 完成：`serve()` 将 Hono 的 `app.fetch` 桥接到 Node HTTP Server 并绑定 `WEB_PORT`（默认 3000），随后立即调用 `setupWebSocket` 复用同一个 server 实例。这里有两个值得注意的运维取舍被写进了注释：`requestTimeout` 从 Node 默认的 5 分钟放宽到 10 分钟，以支持慢速网络下的大文件上传下载，代价是 slow-POST 连接占用窗口同步翻倍；`headersTimeout` 保持 60 秒只防 header-slowloris，慢速 body 的防护被明确交给前置反向代理。启动流程还会向队列注册三个回调（容器退出时清理终端、Runner 状态变化、查询开始/结束），并每 5 秒广播一次系统状态快照。\n\nSources: [web.ts](src/web.ts#L205-L259)、[web.ts](src/web.ts#L225-L245)、[web.ts](src/web.ts#L3275-L3336)、[config.ts](src/config.ts#L60-L60)\n\n## 路由族全景：19 个子模块的挂载图谱\n\n路由采用"子模块自建 Hono 实例 + 主应用按前缀挂载"的模式，全部集中在 `app.route()` 一段代码中完成。这张挂载表是理解 API 面的第一张地图：\n\n| 挂载前缀 | 路由模块 | 职责域 |\n|---|---|---|\n| `/api/auth` | `routes/auth.ts` | 登录、注册、会话管理、头像 |\n| `/api/groups` | `routes/groups.ts` | 工作区 CRUD 与会话操作 |\n| `/api/groups` | `routes/files.ts` | 工作区文件读写 |\n| `/api/groups` | `routes/agents.ts` | Agent 子会话（兼容 `/agents` 别名） |\n| `/api/groups` | `routes/workspace-config.ts` | 工作区级配置 |\n| `/api/memory` | `routes/memory.ts` | Workspace Memory v2 |\n| `/api/config` | `routes/config.ts` | 系统/Provider 配置 |\n| `/api/tasks` | `routes/tasks.ts` | 定时任务 |\n| `/api/skills` | `routes/skills.ts` | 技能导入与管理 |\n| `/api/admin` | `routes/admin.ts` | 用户与审计管理 |\n| `/api/browse` | `routes/browse.ts` | 宿主目录浏览 |\n| `/api/mcp-servers` | `routes/mcp-servers.ts` | MCP 服务器配置 |\n| `/api/plugins` | `routes/plugins.ts` | 插件市场与安装 |\n| `/api/agent-profiles` | `routes/agent-profiles.ts` | Agent Profile 管理 |\n| `/api/workspaces` | `routes/workspaces.ts` | 新版工作区接口 |\n| `/api`（根） | `routes/monitor.ts` | 监控探针 |\n| `/api/usage`、`/api/billing` | `usage.ts`、`billing.ts` | 用量与计费 |\n| `/api/bug-report`、`/api/channel-accounts` | 对应模块 | 错误上报、多账号管理 |\n\n两个设计细节值得展开。其一，`/api/groups` 前缀被四个模块共享挂载——这是历史演进的痕迹：Agent 会话路由最初挂在 `/agents` 下，后迁移为工作区子路径但保留了兼容别名；workspace-config 也归入工作区命名空间。其二，认证不是全局中间件，而是**逐路由显式追加**的（如 `groupRoutes.get(\'/\', authMiddleware, ...)`），公共端点（如登录、初始化检查）天然不经过认证链。除挂载的路由外，`web.ts` 还内联实现了最核心的两条业务端点：`POST /api/messages` 承接前端发消息的主通路（含 Zod 校验、群组访问检查、宿主模式权限、`/clear` 重置分支），`GET /api/follow-ups` 与 `POST /api/follow-ups/:messageId/action` 管理追问队列。\n\nSources: [web.ts](src/web.ts#L265-L286)、[web.ts](src/web.ts#L290-L399)、[web.ts](src/web.ts#L409-L457)、[groups.ts](src/routes/groups.ts#L536-L547)\n\n## 静态资源与 SPA 兜底：缓存策略的三层分级\n\n前端产物的服务逻辑体现了精细的缓存分级。第一层是自定义的 `conditionalStatic` 中间件——因为 `@hono/node-server` 的 `serveStatic` 不支持条件请求（带 `If-None-Match` 也永远返回 200 全量），这里按文件的 `size + mtime` 生成弱 ETag，命中即返回 304 空体，并内置路径穿越防护（解析后的绝对路径必须仍在 dist 根内）。第二层是 Cache-Control 分级：`/assets/*` 与 `/fonts/*` 设为一年 immutable（内容哈希文件名保证不可变），图标 30 天；而 SPA shell、Service Worker 清理脚本和 manifest 强制 `no-cache`，保证发版即生效。第三层是兜底路由：`rewriteRequestPath` 把所有不带扩展名且非 `/api`、`/ws` 的路径改写到 `/index.html`，完成 SPA history 路由的回退。\n\nSources: [web.ts](src/web.ts#L1258-L1299)、[web.ts](src/web.ts#L1302-L1337)、[web.ts](src/web.ts#L1340-L1370)\n\n## Cookie 认证基石：HMAC 签名令牌与双 Cookie 名\n\n会话凭证的设计目标是：即使数据库或备份泄漏，raw session token 也无法被冒用为登录 Cookie。为此每个会话包含两层——数据库中存储 64 位十六进制随机 token（`crypto.randomBytes(32)`），而发给浏览器的 Cookie 值是 `token.HMAC-SHA256(token, secret)` 的拼接形式。校验方用 `timingSafeEqual` 做常数时间签名比对，防止逐字节探测签名。\n\n签名密钥 `WEB_SESSION_SECRET` 按三级优先级解析：环境变量最高（方便容器化部署覆盖），其次是数据目录下持久化的 `session-secret.key` 文件（权限 0600，保证重启后旧 Cookie 不失效），最后才是临时生成。Cookie 名称则按请求是否安全动态选择：\n\n| 维度 | HTTPS 场景 | HTTP 场景（本地开发等） |\n|---|---|---|\n| Cookie 名 | `__Host-miniclaw_session` | `miniclaw_session` |\n| Secure 标志 | 有 | 无 |\n| 共同属性 | `HttpOnly; SameSite=Strict; Path=/; Max-Age=2592000` | 同左 |\n\n`__Host-` 前缀是浏览器强制的安全标记（要求 Secure、Path=/、禁止 Domain 属性），能有效防御子域名劫持类攻击；是否使用它由 `isSecureRequest` 逐请求判定——信任代理时看 `x-forwarded-proto`，否则解析请求 URL 的协议。\n\nSources: [auth.ts](src/auth.ts#L27-L38)、[auth.ts](src/auth.ts#L54-L81)、[auth.ts](src/auth.ts#L84-L98)、[config.ts](src/config.ts#L62-L102)、[utils.ts](src/utils.ts#L157-L169)\n\n## authMiddleware：一次请求要过的五道关卡\n\n`authMiddleware` 是所有受保护路由的单一入口，它的执行链条值得逐段拆解：\n\n1. **收集重复 Cookie**。浏览器在同名 Cookie 新旧并存时会发送多个值（典型于签名升级迁移期），`getAllCookieValues` 解析原始 Cookie 头取出某名称的**全部**取值，并按"新 Secure 名 → 普通名 → 两个 Legacy 名"的优先级排列。\n2. **逐一验签**。`tryVerifyAny` 对每个候选值尝试 HMAC 验签，任一通过即采纳。若命中未签名的历史 Cookie 且环境变量 `ALLOW_LEGACY_UNSIGNED_COOKIE=true`，会接受它并标记 `legacy=true`——该开关默认关闭，注释明确说明永久接受未签名 Cookie 会让签名机制形同虚设。\n3. **会话缓存查找**。`getCachedSessionWithUser` 提供 30 秒 TTL 的内存缓存，避免每个请求都打 SQLite；查不到则失效缓存并返回 401。\n4. **过期与账号状态**。过期会话直接删除数据库行并失效缓存；`disabled`/`deleted` 状态分别返回 403。\n5. **强制改密门控与活跃度更新**。若 `must_change_password` 为真，仅放行 `/api/auth/me`、`/password`、`/logout`、`/profile`、`/sessions*` 这几个完成改密流程所必需的端点，其余一律 403 `PASSWORD_CHANGE_REQUIRED`；通过后按 5 分钟去抖间隔低频更新 `last_active_at`。\n\n通过全部关卡后，中间件将 `AuthUser`（含角色与权限集）写入 Hono 上下文。在此之上有六个细粒度门控中间件可叠加：`requirePermission` / `requireAnyPermission` 做权限位检查，另有 `adminRoleMiddleware`、`systemConfigMiddleware`、`groupEnvMiddleware`、`usersManageMiddleware` 等语义化包装，供各路由按需组合。\n\n```mermaid\nflowchart TD\n    A["收到 /api/* 请求"] --> B{"存在任何会话 Cookie?"}\n    B -- 否 --> X["401 Unauthorized"]\n    B -- 是 --> C{"tryVerifyAny<br/>HMAC 验签通过?"}\n    C -- 否 --> X\n    C -- "是(legacy)" --> D["响应头补发签名 Cookie"]\n    C -- 是 --> E{"30s 缓存命中<br/>且会话存在?"}\n    E -- 否 --> X\n    E -- 是 --> F{"未过期 且<br/>status == active?"}\n    F -- 过期 --> Y["删会话 + 401 Session expired"]\n    F -- 禁用/删除 --> Z["403"]\n    F -- 是 --> G{"must_change_password?"}\n    G -- 是且非白名单路径 --> W["403 PASSWORD_CHANGE_REQUIRED"]\n    G -- 否或白名单 --> H["c.set(\'user\') 放行<br/>+ 5min 去抖更新活跃时间"]\n```\n\nSources: [middleware/auth.ts](src/middleware/auth.ts#L63-L150)、[middleware/auth.ts](src/middleware/auth.ts#L32-L61)、[middleware/auth.ts](src/middleware/auth.ts#L152-L191)、[web-context.ts](src/web-context.ts#L276-L301)、[auth.ts](src/auth.ts#L50-L51)\n\n## 登录端点：暴力破解的三重防线\n\n`POST /api/login` 集中了教科书级的登录防御。第一重是**双层速率限制**：按"用户名+IP"和"全局用户名"两个窗口分别计数（后者阈值是前者 4 倍、窗口 1 小时，用于对抗分布式撞库），超限返回 429 并附带重试秒数；限流记录的内存回收定时器刻意以首次尝试时间为准，防止攻击者靠暂停来重置配额。第二重是**常数时间密码比对**：即使用户名不存在，也会对一个内置的合法 bcrypt 哈希跑完整的 `bcrypt.compare`，抹平"用户不存在"与"密码错误"之间的响应时差。第三重是**审计日志**：成功与失败（含细分原因：用户不存在/账号停用/密码错误/被限流）都写入 `logAuthEvent`。\n\n登录成功后创建 32 字节随机会话写入 SQLite（含 IP、UA、过期时间），清理该用户的限流计数，回填用户主页工作区，最终以手写的 `Set-Cookie` 头返回——注意这里是构造原生 `Response` 而非 `c.json`，因为需要精确控制响应头的组合。整个认证族还包含登出（删除会话行）、`GET /me`、改密码（会吊销其他会话）、会话列表与远程吊销、头像上传（独立的 body 限额中间件）等端点。\n\nSources: [routes/auth.ts](src/routes/auth.ts#L235-L358)、[auth.ts](src/auth.ts#L170-L210)、[routes/auth.ts](src/routes/auth.ts#L136-L139)、[routes/auth.ts](src/routes/auth.ts#L529-L553)\n\n## WebSocket 升级握手：绕过 Hono 的第二条认证路径\n\nWebSocket 无法复用 Hono 中间件链——协议升级发生在 Node HTTP Server 层，根本不会进入 `app.fetch`。因此 `setupWebSocket` 在 `server.on(\'upgrade\')` 上重建了一条平行的认证路径，其严格度与 `authMiddleware` 对齐但顺序不同：\n\n```mermaid\nsequenceDiagram\n    participant FE as 浏览器\n    participant S as Node HTTP Server\n    participant WSS as WebSocketServer (noServer)\n    FE->>S: GET /ws (Upgrade + Cookie + Origin)\n    S->>S: pathname !== \'/ws\' ? destroy\n    S->>S: Origin 校验：同源放行<br/>否则查白名单（CSWSH 防御）\n    S->>S: 403 Forbidden + destroy（不在白名单）\n    S->>S: 收集全部 Cookie 值 + tryVerifyAny 验签\n    S->>S: 401 + destroy（无 Cookie / 验签失败）\n    S->>S: 缓存查会话 + 过期/状态检查\n    S->>S: 403 Password Change Required（强制改密）\n    S->>WSS: handleUpgrade → emit \'connection\'\n    WSS-->>FE: 连接建立\n    WSS-->>FE: active_run_snapshot（权威运行态）\n    WSS-->>FE: stream_snapshot ×N（可访问的流式快照）\n```\n\n三个细节体现了这里的工程考量。首先是 **CSWSH（跨站 WebSocket 劫持）纵深防御**：浏览器对 WebSocket 不发 CORS 预检，CORS 中间件天然失效，因此升级处理器手动比较 `Origin.host` 与 `Host` 头判断同源——同源放行、缺失放行（非浏览器客户端）、其余必须命中白名单，拒绝的 Origin 会按去重集合只警告一次，避免反向代理场景下刷屏。其次是**帧大小上限**：`maxPayload` 从 ws 库默认的 100 MiB 收紧到 8 MiB，注释算了一笔账——这足以覆盖"10 张 5MB 图片 base64 后"的合法上限，同时阻止认证用户用单帧把服务器 OOM。最后是 **Legacy Cookie 的降级处理**：HTTP 响应可以携带 `Set-Cookie` 完成透明升级，但 WebSocket 升级握手不能，所以这里接受 legacy Cookie 但不做升级，推迟到下一次普通 HTTP 请求时再换发签名 Cookie。\n\nSources: [web.ts](src/web.ts#L1379-L1491)、[web.ts](src/web.ts#L1397-L1433)、[web.ts](src/web.ts#L1435-L1486)、[web.ts](src/web.ts#L1374-L1377)\n\n## 连接生命周期：注册表、双快照重放与入站契约\n\n连接建立后，客户端信息以 `WsClientInfo { sessionId, userId, role }` 存入全局注册表 `wsClients`，这是后续所有广播过滤的数据基础。服务端随即主动推送两份状态：先是 `active_run_snapshot`（遍历队列中所有该用户可见的工作区，标注每个正在运行的 query 的 `runId` 与 phase），再是各工作区的 `stream_snapshot`（超过 30 分钟的陈旧快照会被清除，空快照跳过）。注释解释了这个顺序的必要性：暖 Runner 场景下进程可能活跃但没有 query 在跑，仅有进程存活信号不足以恢复前端的输入框与流式卡片，必须以逻辑运行为准，且 `active_run_snapshot` 必须先于 `stream_snapshot` 到达，客户端才能把每个投影围栏到确切的尝试上。\n\n入站消息契约 `WsMessageIn` 只有五个类型，全部走同一条消息级复验流程——每条消息到达时都会重新查会话缓存、重新检查过期与强制改密状态（管理员重置密码后，已建立的 WS 连接会在下一条消息时被 `close(1008)` 踢出）：\n\n| 入站类型 | 用途 | 服务端防线 |\n|---|---|---|\n| `send_message` | 发送用户消息 | MessageCreateSchema 重校验 + `canAccessGroup` + 宿主模式要求 admin |\n| `terminal_start` | 打开容器终端 | 尺寸钳制 + 单一所有权接管 |\n| `terminal_input` | 写入终端 | 必须是当前所有者连接 |\n| `terminal_resize` | 调整尺寸 | 所有者检查 |\n| `terminal_stop` | 关闭终端 | 所有者检查 |\n\n终端的所有权模型由开头提到的两个 Map 支撑：新连接打开终端时，若目标工作区已有其他所有者，旧终端被停止并向旧连接推送 `terminal_stopped`（原因："终端被其他连接接管"）；`terminal_input` 则双向校验——连接绑定的 JID 和所有者映射必须同时匹配，否则回复"终端会话已失效"。这保证了终端这种有状态的稀缺资源不会被并发破坏。\n\nSources: [web.ts](src/web.ts#L1493-L1578)、[web.ts](src/web.ts#L1620-L1655)、[types.ts](src/types.ts#L1042-L1058)、[web.ts](src/web.ts#L1664-L1712)、[web.ts](src/web.ts#L1940-L1995)、[web.ts](src/web.ts#L2013-L2042)、[web-context.ts](src/web-context.ts#L25-L29)\n\n## 广播管线：safeBroadcast 的三条过滤规则\n\n服务端到客户端的全部推送都经过 `safeBroadcast` 这一个收口函数，它按序执行三条过滤规则。第一条是**会话有效性复查**：每次广播都对每个客户端重新查会话缓存，发现过期则顺带删除数据库会话行，无效连接一律 `close(1008, \'Unauthorized\')` 并移出注册表——这意味着吊销会话后推送通道几乎实时失效。第二条是 `adminOnly` 标记，用于 Docker 拉取日志这类管理员专属事件。第三条是**组隔离白名单**：`allowedUserIds` 参数为 `Set` 时只有成员能收到，为 `undefined` 时表示不启用此过滤，而为 `null` 时语义是"归属无法解析 → 默认拒绝所有人（包括 admin）"——宁可漏发也不意外泄漏，这是 fail-closed 原则在推送侧的体现。\n\n在进入广播前还有一层 JID 归一化：`normalizeHomeJid` 把 IM 渠道的聊天 JID 归属到它实际绑定的 Web 工作区 JID，未绑定时才回退扫描同 folder 的 `web:` JID，避免另一个工作区的事件被打上错误标签出现在错误的客户端里。流式事件的推送入口 `broadcastStreamEvent` 则再加两道工序：其一，`context_audit` 类型的事件完全不发给前端，只在服务端落盘为运行上下文快照（防止暴露路径与框架术语）；其二，每个事件先经 `streamRunFence` 围栏裁决——被更新的查询尝试取代的迟到事件（如 A→B 替换后 A 的残留回调）会被丢弃并记录 debug 日志，通过的事件才广播并在 `streamingSnapshots` 里累积成可重放的快照。这套机制与断线重连的快照重放配合，构成了前端流式渲染的状态一致性基座。\n\nSources: [web.ts](src/web.ts#L2145-L2217)、[web.ts](src/web.ts#L2219-L2266)、[web.ts](src/web.ts#L2899-L2963)、[web.ts](src/web.ts#L2875-L2897)、[types.ts](src/types.ts#L856-L1041)\n\n## 优雅退出与可测试性\n\n关闭路径 `shutdownWebServer` 按依赖逆序拆除：停掉状态广播定时器 → 逐个以 `close(1001, \'Server shutting down\')` 关闭客户端 → 关闭 WebSocketServer → 最后关闭 HTTP Server。终端进程则有独立的 `shutdownTerminals` 出口，由更上层的优雅退出编排统一调度。可测试性方面，除了前述 `createAppForTest`，导出面还包含了十余个 `broadcast*` 函数（新消息、打字指示、Agent 状态、标题生成、Runner 状态、run 开始/结束、计费更新、渠道账号状态等），使上层编排器无需感知 WebSocket 的存在就能触发前端更新。\n\nSources: [web.ts](src/web.ts#L3340-L3370)、[web.ts](src/web.ts#L2268-L2332)、[web.ts](src/web.ts#L3036-L3210)\n\n## 小结与延伸阅读\n\nMiniclaw 的 Web 服务面呈现出清晰的对称结构：HTTP 与 WebSocket 两条路径各自维护了一套认证逻辑，但共享同一套凭证（HMAC 签名 Cookie）、同一份会话存储（SQLite + 30 秒缓存）和同一套账号状态机；推送侧则以 `safeBroadcast` 单点收口，用 fail-closed 白名单保证事件不越界。理解了这条骨架，再去读任何一条具体业务路由都不会迷路。\n\n建议按以下顺序继续深入：想了解 Web 层背后的执行引擎如何排队执行，请阅读[会话串行队列：Runner 生命周期、重试退避与卡死恢复](14-hui-hua-chuan-xing-dui-lie-runner-sheng-ming-zhou-qi-zhong-shi-tui-bi-yu-qia-si-hui-fu)；想理解 `stream_event` 中流动的具体事件类型，请阅读[StreamEvent 流式契约：三端共享类型与同步机制](25-streamevent-liu-shi-qi-yue-san-duan-gong-xiang-lei-xing-yu-tong-bu-ji-zhi)；想弄清 `canAccessGroup` 与 `allowedUserIds` 背后的授权模型，请阅读[ACL 权限矩阵：层次化授权与无 admin 旁路的资源隔离](19-acl-quan-xian-ju-zhen-ceng-ci-hua-shou-quan-yu-wu-admin-pang-lu-de-zi-yuan-ge-chi)；前端如何消费这些接口与推送，见[Web 前端架构：React 19、Zustand 状态管理与页面路由](24-web-qian-duan-jia-gou-react-19-zustand-zhuang-tai-guan-li-yu-ye-mian-lu-you)；会话数据的落地细节见[SQLite 持久化：Schema 版本化、迁移策略与核心表族](9-sqlite-chi-jiu-hua-schema-ban-ben-hua-qian-yi-ce-lue-yu-he-xin-biao-zu)。';export{e as default};

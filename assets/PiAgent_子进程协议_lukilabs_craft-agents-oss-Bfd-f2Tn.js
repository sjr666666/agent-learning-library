const n="# PiAgent 子进程协议 | lukilabs/craft-agents-oss\n\n\n---\nPiAgent 子进程协议是一种架构机制，Craft Agents 通过该机制将 `@mariozechner/pi-coding-agent` SDK——一个包含近原生模块的庞大 ESM 依赖树——隔离到专用的子进程中。宿主（Electron 主进程或 CLI）与子进程之间的通信完全通过 **基于 stdio 的 JSONL（JSON Lines）** 进行，从而划定了一个清晰的边界，既避免了 Electron 打包冲突，又实现了与 Claude agent 后端的功能对等。本文档将介绍该协议的线路格式、生命周期状态机、权限握手机制以及性能优化。\n\n## 架构动机[](#架构动机)\n\nPi SDK（`@mariozechner/pi-coding-agent` v0.66.1）依赖于纯 ESM 模块、动态导入（例如通过 `import(\"./amazon-bedrock.js\")` 解析 Bedrock 提供商）以及复杂的传递依赖图，这些与 Electron 的打包器流水线从根本上是不兼容的。架构并未通过扭曲构建流程来适应这些限制，而是将整个 Pi SDK 的接口提取到 `packages/pi-agent-server` 中——这是一个使用 Bun 构建的独立 Node.js 进程——并通过 `stdin` 和 `stdout` 上的换行符分隔的 JSON 消息与之通信。调试输出被路由到 `stderr`，以保持协议通道的纯净。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L1-L15)、[package.json](packages/pi-agent-server/package.json#L12-L13)\n\n## 协议拓扑[](#协议拓扑)\n\n宿主进程将 `pi-agent-server` 作为子进程生成，并将 `InboundMessage` 对象写入其 `stdin`。子进程解析每一行，分派给相应的处理器，并将 `OutboundMessage` 对象写入 `stdout`。所有调试日志均输出到 `stderr`。此外还存在一个辅助 HTTP 通道，用于处理来自会话 MCP 服务器的基于回调的 `call_llm` 请求。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L17-L19)、[index.ts](packages/pi-agent-server/src/index.ts#L239-L247)\n\n## 线路协议规范[](#线路协议规范)\n\n### 入站消息（宿主 → 子进程）[](#入站消息宿主--子进程)\n\n`InboundMessage` 可辨识联合类型定义了宿主可以发送的每条命令。每条消息都是以换行符终止的单个 JSON 对象。\n\n`type`\n\n用途\n\n关键字段\n\n异步响应？\n\n`init`\n\n使用会话配置引导子进程\n\n`apiKey`、`model`、`cwd`、`sessionId`、`piAuth`、`baseUrl`、`customEndpoint`\n\n→ `ready`\n\n`prompt`\n\n提交用户消息以供 LLM 处理\n\n`id`、`message`、`systemPrompt`、`images?`\n\n→ `event` 流\n\n`register_tools`\n\n注册 MCP/会话代理工具定义\n\n`tools: ProxyToolDef[]`\n\n无（状态更新）\n\n`tool_execute_response`\n\n返回代理工具执行结果\n\n`requestId`、`result: {content, isError}`\n\n解析待处理的 Promise\n\n`pre_tool_use_response`\n\n返回权限决策\n\n`requestId`、`action: allow|block|modify`、`input?`、`reason?`\n\n解析待处理的 Promise\n\n`abort`\n\n取消正在进行的 LLM 生成\n\n—\n\n立即\n\n`mini_completion`\n\n请求轻量级 LLM 查询（摘要）\n\n`id`、`prompt`\n\n→ `mini_completion_result` 或 `error`\n\n`ensure_session_ready`\n\n预热 Pi 会话\n\n`id`\n\n→ `ensure_session_ready_result`\n\n`set_model`\n\n在会话中途切换活动模型\n\n`model`\n\n无\n\n`set_thinking_level`\n\n调整推理深度\n\n`level`\n\n无\n\n`compact`\n\n触发手动上下文压缩\n\n`id`、`customInstructions?`\n\n→ `compact_result`\n\n`set_auto_compaction`\n\n切换自动压缩\n\n`id`、`enabled`\n\n→ `set_auto_compaction_result`\n\n`steer`\n\n注入生成中途的引导消息\n\n`message`\n\n无\n\n`token_update`\n\n刷新身份验证凭据\n\n`piAuth: {provider, credential}`\n\n无\n\n`shutdown`\n\n优雅地终止子进程\n\n—\n\n进程退出\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L110-L125)\n\n### 出站消息（子进程 → 宿主）[](#出站消息子进程--宿主)\n\n`type`\n\n用途\n\n关键字段\n\n`ready`\n\n标识子进程初始化完成\n\n`sessionId`、`callbackPort`\n\n`event`\n\n转发 Pi SDK 会话事件\n\n`event: AgentSessionEvent`\n\n`pre_tool_use_request`\n\n向宿主请求权限决策\n\n`requestId`、`toolName`、`input`、`toolCallId?`\n\n`tool_execute_request`\n\n将工具执行委托给宿主\n\n`requestId`、`toolName`、`args`\n\n`session_tool_completed`\n\n通知宿主 MCP 工具已完成\n\n`toolName`、`args`、`isError`\n\n`mini_completion_result`\n\n返回摘要结果\n\n`id`、`text`\n\n`ensure_session_ready_result`\n\n确认会话已预热\n\n`id`、`sessionId`\n\n`compact_result`\n\n返回压缩结果\n\n`id`、`success`、`result?`、`errorMessage?`\n\n`set_auto_compaction_result`\n\n确认自动压缩状态\n\n`id`、`success`、`enabled`\n\n`session_id_update`\n\n传播 SDK 分配的会话 ID\n\n`sessionId`\n\n`error`\n\n报告协议级错误\n\n`message`、`code?`\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L147-L189)\n\n### 凭据类型[](#凭据类型)\n\n`PiCredential` 联合类型支持三种身份验证策略，使子进程能够对接多种 LLM 提供商，而无需理解其特定的身份验证流程：\n\n类型\n\n字段\n\n使用者\n\n`api_key`\n\n`key: string`\n\nAnthropic、OpenAI、Google、自定义端点\n\n`oauth`\n\n`access`、`refresh`、`expires`\n\nGitHub Copilot、Google OAuth\n\n`iam`\n\n`accessKeyId`、`secretAccessKey`、`region?`、`sessionToken?`\n\nAWS Bedrock\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L75-L78)\n\n## 子进程生命周期[](#子进程生命周期)\n\n### 初始化阶段[](#初始化阶段)\n\n在收到 `init` 消息后，子进程会执行多步引导操作：(1) 销毁任何先前的会话并将 `moduleAuthStorage` 重置为 `null`；(2) 在模块作用域存储 `InitMessage` 配置；(3) 在适用时设置 Azure 特定的环境变量（`AZURE_OPENAI_BASE_URL`）；(4) 在随机端口上启动回调 HTTP 服务器；(5) 发送包含回调端口的 `ready` 消息。会话创建被**延迟**到第一次 `prompt` 或 `ensure_session_ready` 调用，这使宿主能够在会话实例化之前注册代理工具。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L1172-L1201)\n\n### 会话创建（懒加载）[](#会话创建懒加载)\n\n`ensureSession()` 函数是会话工厂。它通过 `createAuthenticatedRegistry()` 构建经过身份验证的 `PiModelRegistry`，将来自 `initConfig.piAuth` 的凭据注入到内存中的 `PiAuthStorage` 中。工具注册遵循两层模型：内置的编码工具和 Web 工具（搜索、网页抓取）通过 `wrapToolsWithHooks()` 包装了权限钩子，而通过 `register_tools` 消息接收的代理工具定义则通过 `buildProxyTools()` 成为可执行的存根。组合后的工具集通过内部的 `_baseToolsOverride` 机制注入到 Pi 会话中，因为 Pi SDK 的 `createAgentSession` 会忽略外部提供的 `AgentTool` 对象。\n\n为了在子进程重启之间保持会话持久化，`PiSessionManager.continueRecent()` 会从每个 Craft 会话目录中加载最新的 `.jsonl` 会话文件。分支处理使用 `PiSessionManager.forkFrom()`，并可选地结合 `branch()` 以实现精确的轮次级别截断。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L486-L646)\n\n## 权限握手协议[](#权限握手协议)\n\n权限系统作为基于 JSONL 通道的**异步请求-响应握手**运行。当任何工具（编码工具或代理工具）即将执行时，子进程会向宿主发送 `pre_tool_use_request` 并阻塞，直到收到匹配的 `pre_tool_use_response`。这种设计将所有权限逻辑（模式检查、来源激活、输入转换）集中到了宿主进程中。\n\n待处理的 Promise 被跟踪在两个以 `requestId` 为键的 `Map` 结构中——`pendingPreToolUse` 和 `pendingToolExecutions`。在触发 `abort` 时，所有待处理的工具使用前 Promise 都会以 `{action: 'block', reason: 'Aborted'}` 解决，以解除任何等待工具执行代码的阻塞，同时推测性预取缓存也会被清空。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L663-L687)、[index.ts](packages/pi-agent-server/src/index.ts#L1349-L1366)\n\n### 工具名称规范化[](#工具名称规范化)\n\nPi SDK 工具使用小写名称（`read`、`write`、`bash`、`grep`），但 Craft 权限系统期望的是帕斯卡命名法（`Read`、`Write`、`Bash`、`Grep`）。`PI_TOOL_NAME_MAP` 常量弥合了这一差异，在请求权限时以及当 `PiEventAdapter` 为 UI 层规范化事件时都会应用此映射。\n\n来源：[constants.ts](packages/shared/src/agent/backend/pi/constants.ts#L31-L46)\n\n## 代理工具执行[](#代理工具执行)\n\n必须在宿主进程中执行的工具（MCP 会话工具、需要宿主级别访问权限的文件系统操作）通过 `register_tools` 注册为仅包含 `name`、`description` 和 `inputSchema` 的 `ProxyToolDef` 描述符。子进程通过 `buildProxyTools()` 构建可执行的 `AgentTool` 存根，将执行委托回宿主：\n\n1.  存根的 `execute()` 发送带有唯一 `requestId` 的 `tool_execute_request`\n2.  宿主执行工具并使用 `tool_execute_response` 进行响应\n3.  存根通过 `pendingToolExecutions` 映射等待响应并返回结果\n\n代理工具定义按名称合并（新工具替换同名现有工具），并且如果在会话期间修改了工具，`toolsChanged` 标志会触发在下一次提示词时重建会话。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L779-L838)、[index.ts](packages/pi-agent-server/src/index.ts#L1312-L1327)\n\n**并行工具调用的推测性预取**：当 Pi SDK 在单条助手消息中发出 2 个及以上的 `call_llm` 工具调用时，子进程会在 `message_end` 时检测到这一点，并在 SDK 的顺序 `executeToolCalls` 循环开始之前**并行**向宿主触发所有请求。随后，每个代理工具的 `execute()` 都会命中 `prefetchCache`，而不是发送重复请求，从而有效地将顺序的 SDK 语义转换为并行执行。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L216-L226)、[index.ts](packages/pi-agent-server/src/index.ts#L1103-L1127)\n\n## 临时 LLM 查询（`queryLlm`）[](#临时-llm-查询queryllm)\n\n迷你补全（用于摘要生成、大响应处理以及 `call_llm` MCP 工具）通过 `queryLlm()` 运行，该函数会创建一个**无持久化的临时内存 Pi 会话**。该函数包含一个复杂的模型回退链：它首先尝试配置的迷你模型，然后在遇到 `model_not_found` 错误时回退到候选列表（按提供商兼容性过滤）。`miniModel` 会根据经过身份验证的提供商进行验证，以防止跨提供商模型选择导致的“No API key found”错误。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L845-L1043)\n\n## 回调 HTTP 服务器[](#回调-http-服务器)\n\n对于源自会话 MCP 服务器（运行在单独的进程中）的 `call_llm` 请求，子进程会在 `127.0.0.1:{random_port}` 上启动一个带有单一 `POST /call-llm` 端点的 HTTP 服务器。该端口通过 `ready` 消息的 `callbackPort` 字段传递给宿主。此服务器为 `queryLlm` 流水线提供了同步 HTTP 接口，使会话 MCP 服务器无需理解 JSONL 协议即可调用 LLM 查询。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L268-L313)\n\n## 错误恢复与弹性[](#错误恢复与弹性)\n\n### 上下文溢出恢复[](#上下文溢出恢复)\n\n当提示词触发上下文窗口溢出时，`handlePrompt` 会捕获错误并自动执行压缩-重试序列：调用 `session.compact()`，通过 `waitForCompaction()` 等待任何正在进行的自动压缩完成，然后重新发送提示词。如果恢复失败，它会发出带有 `prompt_overflow_recovery_failed` 错误码的 `error` 消息，随后发出一个合成的 `agent_end` 事件以解除宿主事件队列的阻塞。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L1276-L1309)\n\n### 压缩竞态保护[](#压缩竞态保护)\n\n`waitForCompaction()` 轮询循环可防止竞态条件（被追踪为 craft-agents-oss#464），即并发的 `_runAutoCompaction` 调用因共享 `AbortController` 而崩溃。在每次提示词之前，子进程会以 200 毫秒的间隔和 60 秒的超时时间轮询 `session.isCompacting`。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L1220-L1234)\n\n### 关闭清理[](#关闭清理)\n\n`handleShutdown` 遵循严格的拆卸序列：取消订阅事件 → 销毁会话 → 停止回调服务器 → 以描述性原因拒绝所有待处理的 Promise → `process.exit(0)`。这确保了子进程中不会残留悬空的 Promise 或打开的句柄。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L1496-L1526)\n\n## 自定义端点支持[](#自定义端点支持)\n\n子进程通过合成的提供商注册系统支持任意兼容 OpenAI 和 Anthropic 的端点。当 `init` 包含 `baseUrl` 和 `customEndpoint.api` 时，子进程会使用默认具有 128K 上下文窗口（可通过 `customModels` 数组覆盖）的模型定义调用 `modelRegistry.registerProvider('custom-endpoint', ...)`。当 `shouldPreferCustomEndpoint()` 返回 true 时，模型解析会优先选择自定义端点。通过 `set_model` 进行的会话中切换模型会为自定义端点提供商动态注册以前未知的模型。\n\n自定义端点的 `PiCredential` 对于经过身份验证的服务可以是 `api_key`，而对于本地端点（Ollama、LM Studio）可以为空——如果非本地端点没有 API 密钥，子进程会发出警告。\n\n来源：[index.ts](packages/pi-agent-server/src/index.ts#L358-L437)、[index.ts](packages/pi-agent-server/src/index.ts#L1440-L1471)\n\n## 运行时驱动集成[](#运行时驱动集成)\n\n从宿主的角度来看，`piDriver`（位于 `internal/drivers/pi.ts`）会构建一个 `BackendRuntimePayload`，其中包含 `piServer`、`interceptor` 和 `node` 二进制文件的解析后文件系统路径，以及特定于提供商的配置（`piAuthProvider`、`baseUrl`、`customEndpoint`、`customModels`）。此负载流经 [BaseAgent 抽象层](/lukilabs/craft-agents-oss/9-baseagent-abstraction) 以配置子进程的生成参数。\n\n来源：[pi.ts](packages/shared/src/agent/backend/internal/drivers/pi.ts#L227-L242)、[driver-types.ts](packages/shared/src/agent/backend/internal/driver-types.ts#L14-L32)\n\n## 阅读进阶[](#阅读进阶)\n\n本页介绍了线路协议和子进程内部机制。要了解 PiAgent 如何融入后端选择层级的更广泛背景，请参阅 [BaseAgent 抽象层](/lukilabs/craft-agents-oss/9-baseagent-abstraction) 。要了解 Pi SDK 事件如何转换为 UI 所消费的统一 Craft 事件模型，请参阅 [ClaudeAgent SDK 集成](/lukilabs/craft-agents-oss/10-claudeagent-sdk-integration) 中的事件适配器。要了解宿主在响应 `pre_tool_use_request` 时运行的权限逻辑，请参阅 [权限模式系统](/lukilabs/craft-agents-oss/13-permission-mode-system) 。";export{n as default};

const n='MiniCode 的全屏终端界面（TUI）是一个**不依赖任何渲染框架**（无 Ink/React）的自研实现。它的设计核心是一句话：**原始字节流进，结构化事件分发，单一状态源驱动帧合成，增量 diff 写屏**。整个体系分成两个层次——`src/tui/*` 是七个纯函数渲染/解析组件（无业务逻辑），`src/tty-app.ts` 是唯一持有状态与模式分派逻辑的编排层。理解本页，等于掌握了 MiniCode 交互体验的全部骨架。\n\n## 整体架构：一条单向数据管道\n\nTUI 不是"事件驱动"而是**单向数据流管道**：stdin 的原始字节经过状态化解析器变成结构化事件，事件被 Promise 链串行化后交给模式分派器修改 `ScreenState`，任何状态变更都通过渲染调度器合并成一次帧重绘，最终由 diff 写入器只更新变化行。\n\n```mermaid\nflowchart LR\n    STDIN["stdin 原始字节流"] --> PARSER["parseInputChunk<br/>带 remainder 的状态化解析"]\n    PARSER --> EVENTS["ParsedInputEvent[]<br/>key / text / wheel / mouse"]\n    EVENTS --> CHAIN["eventChain Promise<br/>串行化事件处理"]\n    CHAIN --> SM["handleEvent 模式分派<br/>（Approval / Picker / Normal）"]\n    SM --> STATE["ScreenState<br/>单一状态源"]\n    STATE --> RENDER["scheduleRender<br/>setImmediate 合并调度"]\n    RENDER --> FRAME["renderScreen 帧合成"]\n    FRAME --> DIFF["renderTerminalFrame<br/>行级 diff 增量绘制"]\n    DIFF --> TERM["终端屏幕"]\n    AGENT["runAgentTurn 回调<br/>onToolStart / onToolResult / onAssistantMessage..."] -->|"更新状态并触发重绘"| STATE\n```\n\n这条管道的两个端点值得注意。入口端，`runTtyApp` 启动时调用 `enterAlternateScreen()` 进入备选屏幕、`setRawMode(true)` 关闭行缓冲、`hideCursor()` 隐藏光标——终端被整个接管为"画布"；出口端，`renderTerminalFrame` 用行级 diff 只重写变化行，把闪烁降到最低。全屏交互节奏是项目五大设计原则之一，见 [ARCHITECTURE_ZH.md](ARCHITECTURE_ZH.md#L12-L15)。\n\nSources: [screen.ts](src/tui/screen.ts#L22-L40)、[tty-app.ts](src/tty-app.ts#L1647-L1651)、[ARCHITECTURE_ZH.md](ARCHITECTURE_ZH.md#L12-L15)\n\n## 模块分层：纯组件库与编排层的边界\n\n`src/tui/*` 刻意保持"哑组件"性质：每个模块只做**字符串变换**，不持有会话状态、不发起副作用、不知道 agent 循环的存在。`src/ui.ts` 只是历史遗留的再导出门面。真正把组件组装成应用的是 `src/tty-app.ts`——它 import 全部渲染函数，用 `ScreenState` 把零散的渲染结果拼接成完整帧。\n\n| 模块 | 职责 | 规模 | 关键导出 |\n|---|---|---|---|\n| `tui/types.ts` | `TranscriptEntry` 联合类型（user/assistant/progress/tool） | 28 行 | `TranscriptEntry` |\n| `tui/screen.ts` | 备选屏幕、光标控制、帧 diff 写入 | 86 行 | `renderTerminalFrame`、`enterAlternateScreen` |\n| `tui/input.ts` | 输入提示行渲染（含光标反显与轮换 hint） | 42 行 | `renderInputPrompt` |\n| `tui/input-parser.ts` | 字节流 → 结构化事件，跨 chunk 保持 remainder | 291 行 | `parseInputChunk`、`ParsedInputEvent` |\n| `tui/chrome.ts` | 面板、banner、footer、权限提示、工具面板 | 747 行 | `renderPanel`、`renderBanner`、`renderPermissionPrompt` |\n| `tui/transcript.ts` | 会话流渲染、滚动窗口、选区高亮与文本提取 | 304 行 | `renderTranscript`、`extractSelectedText` |\n| `tui/markdown.ts` | 轻量 Markdown 上色（代码块/表格/标题/粗体） | 64 行 | `renderMarkdownish` |\n| `tui/index.ts` | 门面再导出 | 26 行 | — |\n\n组件库内部也有依赖层次：`transcript.ts` 依赖 `chrome.ts` 的宽度计算与折行函数（`charDisplayWidth`/`stringDisplayWidth`/`wrapPanelBodyLine`），以及 `markdown.ts` 的上色器——可见"显示宽度"这个关注点被收敛在 `chrome.ts` 中，全库统一遵守东亚洲宽字符（CJK）宽度为 2 的约定，这为鼠标选区的列坐标计算提供了唯一的事实来源。\n\nSources: [tui/index.ts](src/tui/index.ts#L1-L26)、[transcript.ts](src/tui/transcript.ts#L1-L3)、[chrome.ts](src/tui/chrome.ts#L27-L56)、[ui.ts](src/ui.ts#L1-L29)\n\n## 单一状态源：ScreenState 与模式分派\n\n编排层的核心是一个约 27 字段的 `ScreenState`。它同时承载三类信息：**编辑器状态**（`input`/`cursorOffset`/`history`）、**会话展示状态**（`transcript`/`transcriptScrollOffset`/`selection`）、**模式状态**（`pendingApproval`/`sessionPicker`/`isBusy`）以及**动画节拍**（`statusAnimationFrame`/`inputHintFrame`/`welcomeAnimation`）。\n\n| 字段组 | 代表性字段 | 说明 |\n|---|---|---|\n| 输入编辑 | `input`、`cursorOffset`、`history`、`historyIndex`、`historyDraft` | 行内编辑与历史导航（Ctrl+P/N） |\n| 会话展示 | `transcript`、`transcriptScrollOffset`、`selection`、`mouseDown` | 滚动窗口与鼠标框选 |\n| 模式 | `pendingApproval`、`sessionPicker`、`isBusy` | 决定 `handleEvent` 的分派分支 |\n| 状态反馈 | `status`、`activeTool`、`recentTools`、`compressionStatus` | footer 与工具面板数据 |\n| 动画 | `statusAnimationFrame`、`inputHintFrame`、`welcomeAnimation` | 三个定时器各自推进的节拍器 |\n| 布局缓存 | `transcriptBodyStartY`、`transcriptBodyLines` | 鼠标坐标 → 内容行索引的映射依据 |\n\n模式分派发生在 `handleEvent`（主循环内的事件处理函数）的开头，按**优先级顺序**检查：`pendingApproval` 存在则进入审批模式 → `sessionPicker` 存在则进入会话选择模式 → 否则进入普通输入模式。注意 `isBusy` 不是独立模式而是一个**跨模式标志**：busy 期间输入框仍然可编辑，但提交（Enter）会被拦截并改为显示 "Current turn is still running..." 状态。\n\n```mermaid\nstateDiagram-v2\n    [*] --> Normal: runTtyApp 启动\n    Normal --> Busy: Enter 提交进入 agent 回合\n    Busy --> Normal: 回合结束 (isBusy=false)\n    Normal --> Approval: PermissionManager 触发审批\n    Approval --> Normal: 选择决策 / 反馈 / Esc 拒绝一次\n    Normal --> Picker: /resume 无参数\n    Picker --> Normal: 选中会话 / Esc 取消\n    Picker --> Picker: Tab 切换 all-projects 视图\n    Normal --> Normal: 输入编辑 / 滚动 / 鼠标选择\n```\n\n审批模式与选择模式有一个共同的设计手法：**用 Promise 挂起异步流程**。`createPermissionPromptHandler` 把审批请求包装成 `new Promise(resolve => { state.pendingApproval = {...resolve} })`——agent 回合在权限检查处暂停，直到用户在 UI 中做出选择后 `resolve` 被调用，回合才继续。会话选择器同理：`/resume` 无参数时挂起直到用户选定会话。这让模式切换不需要额外的事件总线，只靠状态字段 + Promise 回调即可完成同步。\n\nSources: [tty-app.ts](src/tty-app.ts#L122-L148)、[tty-app.ts](src/tty-app.ts#L1017-L1025)、[tty-app.ts](src/tty-app.ts#L1626-L1644)、[tty-app.ts](src/tty-app.ts#L1236-L1248)\n\n## 输入管线：从字节流到结构化事件\n\n`process.stdin` 的 `data` 事件以任意 chunk 边界到达，而转义序列（如 SGR 鼠标事件 `ESC[<0;10;20M`）可能被拆散到多个 chunk。因此解析器是**有状态的**：`onData` 把上次残留的 `inputRemainder` 与当前 chunk 拼接后交给 `parseInputChunk`，未消费的尾部作为 `rest` 留待下次。解析出的所有事件被追加到 `eventChain` Promise 上串行执行，保证 `handleEvent` 的异步副作用（如 `await clearSession`）不会交错。\n\n`parseInputChunk` 的主循环按序识别五类输入，每类映射为一种结构化事件：\n\n| 输入形态 | 判定 | 事件 |\n|---|---|---|\n| 命名键（方向键/Home/End/PgUp 等） | CSI `ESC[` 与 SS3 `ESCO` 序列 | `{kind:\'key\', name, ctrl, meta}` |\n| SGR 鼠标/滚轮 | `ESC[<b;x;yM/m` | `{kind:\'mouse\'|\'wheel\'}` |\n| 控制字符 | `\\u0001`–`\\u001a` 映射表 | `{kind:\'text\', text, ctrl:true}` |\n| 换行 | `\\r`/`\\n` | 默认 `{kind:\'key\', name:\'return\'}`；粘贴场景则转成 `\\n` 文本 |\n| 可打印字符 | 其余 | `{kind:\'text\', text, ctrl:false}` |\n\n最精妙的是**多行粘贴判定**：`isMultilinePasteChunk` 检查当前 chunk 是否"同时含换行与非换行字符"——是则判定为粘贴，换行全部转为 `\\n` 文本而**不会触发提交**；否则是单次按键，`\\r\\n` 归一化为一次 return。这解决了终端粘贴多行代码时被误当成多次回车提交的问题（测试见 [input-parser.test.ts](test/input-parser.test.ts#L6-L28)）。\n\n转义序列解析还有一个防呆细节：`maybeNeedMoreForEscapeSequence` 只对**已知不完整的 CSI 前缀**（如孤立的 `ESC`、`ESC[`、`ESC[<`）等待更多数据，若出现异常字符（如 `ESC[\\r`）则立即放行让解析器回退，避免解析器被垃圾字节永久阻塞。\n\nSources: [tui/input-parser.ts](src/tui/input-parser.ts#L211-L288)、[tui/input-parser.ts](src/tui/input-parser.ts#L46-L48)、[tui/input-parser.ts](src/tui/input-parser.ts#L61-L77)、[tty-app.ts](src/tty-app.ts#L2409-L2427)\n\n## 渲染管线：帧合成、调度合并与增量绘制\n\n渲染分三级：\n\n**第一级——帧合成**。`renderScreen` 按当前模式拼接不同帧。普通模式自上而下为：header banner 面板 → transcript 面板（`session feed`，无边框）→ 权限摘要行 → 输入提示面板（含斜杠菜单）→ footer 状态栏；审批模式替换为 approval 面板 + activity 工具面板；选择模式替换为 sessions/projects 面板。每帧渲染时同步缓存 `transcriptBodyStartY`（banner 高度 + 4）与 `transcriptBodyLines`（由终端行数反推剩余高度），供鼠标坐标反查内容行使用。\n\n**第二级——调度合并**。`createRenderScheduler` 用 `setImmediate` 做节流：同一 tick 内多次 `scheduleRender()` 只触发一次真实渲染。agent 回合的回调（`onToolStart`、`onToolResult`、`onAssistantMessage` 等）在几毫秒内可能连续触发十几次状态更新，合并调度把这一连串变更折叠成一个帧，避免中间态闪烁。\n\n**第三级——行级 diff**。`renderTerminalFrame` 缓存上一帧的行数组与终端尺寸；新帧逐行比对，只对变化行输出 `\\x1b[{n};1H`（定位）+ `\\x1b[2K`（清行）+ 新内容。首次渲染或检测到终端尺寸变化（`columns`/`rows` 与缓存不一致）才强制全量重绘。这是全屏刷新不闪烁的关键。\n\n```mermaid\nflowchart TD\n    A["状态变更 (任意来源)"] --> B["scheduleRender()"]\n    B --> C{"本 tick 已调度?"}\n    C -->|"是"| D["丢弃，等待 setImmediate"]\n    C -->|"否"| E["setImmediate 排程"]\n    E --> F["renderScreen 合成帧"]\n    F --> G{"终端尺寸/首帧?"}\n    G -->|"是"| H["全量重绘 (HOME + ERASE_TO_END)"]\n    G -->|"否"| I["逐行比对上一帧"]\n    I --> J["仅输出变化行"]\n```\n\n帧内的面板排版由 `chrome.ts` 的 `renderPanel` 统一负责：圆角边框（`╭─╮`）、标题行、内容行宽度折行（按显示宽度而非字符数）、`minBodyLines` 撑高。上下文徽章（`renderContextBadge`）把令牌利用率渲染成 `▓▓░░` 进度条，配合 provider usage 记账来源标注（`usage`/`usage+est`/`est`），让用户一眼看到上下文压力。\n\nSources: [tty-app.ts](src/tty-app.ts#L746-L852)、[tty-app.ts](src/tty-app.ts#L854-L865)、[screen.ts](src/tui/screen.ts#L53-L85)、[chrome.ts](src/tui/chrome.ts#L171-L212)、[chrome.ts](src/tui/chrome.ts#L214-L247)\n\n## Transcript 条目生命周期与工具聚合\n\n`transcript` 是四条命名的**追加式日志流**：user、assistant、progress、tool。每条目由 `pushTranscriptEntry` 分配单调递增的 `id`。user/assistant 条目一次性写入，而 tool 条目经历了完整的生命周期：\n\n1. **running**：`onToolStart` 推入 `{kind:\'tool\', status:\'running\', body:摘要}`，body 由 `summarizeToolInput` 压缩（如 `edit_file path=src/x.ts`、`patch_file path=... replacements=5`）。\n2. **完成**：`onToolResult` 把条目更新为 `success`/`error`，随即被 `collapseToolEntry` **折叠**——完整输出替换成首行摘要（≤140 字符），因为 tool 输出对用户的价值在"发生了什么"而非"全部输出"。\n3. **兜底**：`finalizeDanglingRunningTools` 在回合结束时扫描仍处于 running 的条目，统一标记为 error 并注明"工具未在回合结束前报告结果"——防止后台进程或生命周期失同步造成永久 spinning。\n\n一个值得注意的优化是**同路径编辑聚合**：`onToolStart` 检测到 `edit_file`/`patch_file`/`modify_file`/`write_file` 且带 `path` 时，会按 `toolName:path` 归并——同一文件的多次编辑共享一个 transcript 条目，实时更新 `Completed: n/m` 进度，全部完成后折叠为一行 `Aggregated edit_file for path`。这让"模型对同一文件连打十几个补丁"的场景从刷屏变成单条目进度条。\n\n渲染侧，`renderTranscriptLines` 先把每条目渲染成带颜色的块，再用 `·` 分隔符连接成逻辑行数组；`renderTranscript` 按 `scrollOffset` 计算可见窗口，窗口底部显示 `scroll offset: n`。用户消息用灰色背景整行填充，assistant 消息追加"Worked for Ns"右对齐分割线（仅最终消息）。这些渲染语义都有对应测试：折行计数、工作耗时、CJK 选区提取等。\n\nSources: [tty-app.ts](src/tty-app.ts#L501-L508)、[tty-app.ts](src/tty-app.ts#L1475-L1597)、[tty-app.ts](src/tty-app.ts#L613-L653)、[tty-app.ts](src/tty-app.ts#L655-L714)、[transcript.ts](src/tui/transcript.ts#L212-L280)\n\n## 交互细节：鼠标、动画与跨平台适配\n\n**鼠标框选与复制**。TUI 启用了 XTerm SGR 鼠标追踪（`?1000h`/`?1002h`/`?1006h`），`screenToAbsoluteLineIndex` 把终端行列换算成 transcript 逻辑行索引，press → drag → release 三段事件维护 `mouseDown` 与 `selection`。释放时 `extractSelectedText` 剥离 ANSI 码、按显示列切片提取纯文本，再 `copyToClipboard` 写入系统剪贴板。跨平台处理在此处体现：Windows 用 `clip` 且 payload 编码为 **UTF-16LE + BOM**（`0xff 0xfe` 前缀），macOS 用 `pbcopy`，Linux 用 `xclip`。选区高亮算法 `highlightRange` 还要在保留 ANSI 颜色的同时插入反显标记，并正确处理半角/全角混排。\n\n**三个节拍器**。主循环内并行运行三个 `setInterval`：`statusAnimationTimer`（1 秒）轮换 "Ready" 的点点动画帧；`welcomeAnimationTimer`（200 毫秒）推进启动时的"啃奶酪"ASCII 动画——提交第一条消息时切换为"逃跑"动画序列；`inputHintTimer`（3 秒）在空输入时轮换两条提示语。动画遵守一个规则：**transcript 滚动偏移非零时暂停动画**，避免遮挡阅读。\n\n**CJK 显示宽度**。`charDisplayWidth` 实现了 East Asian Width 判定（Hangul、CJK 部首、全角标点、Emoji、CJK 扩展区等返回 2），折行、截断、面板 padding、选区列坐标全部基于此函数——这保证了中文用户的消息和代码在窄终端上正确折行、鼠标能精确选中单个全角字符。\n\n**权限提示的可达性**。审批面板（`renderPermissionPrompt`）对编辑类请求做 diff 语法着色（`+`/`-`/`@@` 元信息分色，且对成对的删/增行计算公共前后缀，把**真正变化的词段加粗**）；折叠态最多显示 16 行细节，`Ctrl+O` 展开后可滚动。Esc 语义是"deny once"，反馈模式（deny_with_feedback）下输入框变为反馈输入。审批交互与三层权限系统的关联详见 [三层权限系统：路径、命令与编辑审批](17-san-ceng-quan-xian-xi-tong-lu-jing-ming-ling-yu-bian-ji-shen-pi)。\n\nSources: [tty-app.ts](src/tty-app.ts#L1754-L1773)、[tty-app.ts](src/tty-app.ts#L2110-L2167)、[tty-app.ts](src/tty-app.ts#L352-L379)、[tty-app.ts](src/tty-app.ts#L330-L350)、[chrome.ts](src/tui/chrome.ts#L450-L556)、[screen.ts](src/tui/screen.ts#L9-L16)\n\n## 延伸阅读\n\n本页覆盖了 TUI 的状态机骨架。若想深入某一环：\n\n- 字节流解析的完整细节（SGR 鼠标位掩码、SS3、多行粘贴判定）见 [输入解析器：转义序列、鼠标事件与多行粘贴](26-shu-ru-jie-xi-qi-zhuan-yi-xu-lie-shu-biao-shi-jian-yu-duo-xing-nian-tie)\n- 审批模式的数据来源（PermissionManager 如何生成 `PermissionRequest`）见 [三层权限系统：路径、命令与编辑审批](17-san-ceng-quan-xian-xi-tong-lu-jing-ming-ling-yu-bian-ji-shen-pi)\n- footer 中 `shells N` 计数的后台任务注册表见 [后台 shell 任务：前台工具与后台进程的区分](27-hou-tai-shell-ren-wu-qian-tai-gong-ju-yu-hou-tai-jin-cheng-de-qu-fen)\n- TUI 行为对应的自动化测试（解析器、折行、CJK 选区、欢迎动画、状态行）见 [测试体系：从单元测试到集成回归](28-ce-shi-ti-xi-cong-dan-yuan-ce-shi-dao-ji-cheng-hui-gui)\n- 与 agent 主循环如何对接（`handleInput` 中的回调注入）见 [代理循环：model → tool → model 的多步执行流程](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng)';export{n as default};

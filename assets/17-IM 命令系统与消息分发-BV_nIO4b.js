const n='IM 命令系统是连接**渠道协议层**与**业务执行层**的斜杠命令中枢。本页聚焦三个问题：渠道层如何从普通消息中拦截命令、内建命令如何完成权限门控与分发、插件命令如何被索引与展开——以及未知命令如何优雅回落到 Agent 普通消息管线。理解这条"拦截 → 门控 → 分发 → 回退"链路，是掌握 HappyClaw 多渠道交互模型的关键。\n\n## 命令系统的三层结构\n\n命令系统横跨三个层次，职责自下而上递进：**渠道拦截层**负责把"看起来像命令"的文本从消息流中截出；**内建分发层**负责识别系统内置命令并执行（带 owner 门控）；**插件展开层**负责把插件贡献的命令解析为可执行的 prompt 或系统回复。三层之间以 `null` 作为"未命中"的约定信号逐级回退。\n\n```mermaid\ngraph TB\n    subgraph L1["渠道拦截层（feishu.ts / telegram.ts / ...）"]\n        REGEX["斜杠正则 /^\\\\/(\\\\S+)(.*)$/<br/>先 strip @mention 前缀"]\n        CKPT["飞书持久化 checkpoint<br/>feishu_slash_command 状态机"]\n    end\n\n    subgraph L2["内建分发层（index.ts handleCommand）"]\n        GATE["Owner 门控<br/>OWNER_REQUIRED_IM_COMMANDS"]\n        SWITCH["switch 分发 16+ 命令<br/>/clear /list /bind /sw ..."]\n        NULL["返回 null → 未知命令"]\n    end\n\n    subgraph L3["插件展开层（plugin-command-index + expander-core）"]\n        IDX["每用户命令索引<br/>byShort / byNamespaced / conflicts"]\n        EXP["三态展开<br/>miss / expanded / reply"]\n    end\n\n    MSG["IM 入站消息"] --> AUD["audience 身份边界<br/>先于一切"]\n    AUD --> REGEX\n    REGEX -->|"命中"| CKPT\n    CKPT -->|"onCommand"| GATE\n    GATE --> SWITCH\n    SWITCH -->|"未命中"| NULL\n    NULL -->|"回落普通消息"| MENTION["mention 门控<br/>fail-closed"]\n    MENTION --> EXP\n    EXP -->|"expanded"| AGENT["Agent Runner"]\n    EXP -->|"reply"| REPLY["系统带内回复"]\n    EXP -->|"miss"| AGENT\n```\n\n**渠道拦截层**由各平台连接器实现，用协议相关的正则识别命令，并处理平台特有的文本形态（飞书的 `@bot /cmd` 前缀、Telegram 的 `/cmd@BotUsername` 后缀）；**内建分发层**在 `index.ts` 中以 `handleCommand` 为入口，统一接收各渠道传来的命令字符串；**插件展开层**则是独立于渠道的纯逻辑模块，Web 与 IM 消息管线共用同一套索引与展开实现。\n\nSources: [feishu.ts](src/feishu.ts#L1968-L1992)、[telegram.ts](src/telegram.ts#L711-L725)、[index.ts](src/index.ts#L3550-L3631)、[plugin-expander-core.ts](src/plugin-expander-core.ts#L1-L31)\n\n## 命令分类总览\n\n系统中存在三类斜杠命令，识别与执行路径各不相同：\n\n| 类别 | 来源 | 识别层 | 执行方式 | 例子 |\n|------|------|--------|----------|------|\n| 内建命令 | `index.ts` 的 `handleCommand` switch | 渠道层正则 → 内建分发层 | 同步/异步 handler 返回回复文本 | `/clear` `/bind` `/sw` `/allow` |\n| 插件命令（DMI） | 插件 `commands/{name}.md`，`disable-model-invocation: true` | 插件命令索引 → 展开器 | 展开为 prompt 发给 Agent，或带内系统回复 | `/codex:review` |\n| 插件命令（非 DMI） | 同上，`disable-model-invocation` 缺省 | SDK 原生 `--plugin-dir` | 透传给 Agent Runner，展开器返回 `miss` | 插件默认命令 |\n| 跟随模式命令 | 飞书专属 | 渠道层正则（早于普通命令） | 切换消息为 queue/steer 模式 | `/queue <消息>` `/steer <消息>` |\n\n内建命令清单由 `plugin-command-index.ts` 中的 `BUILTIN_COMMAND_NAMES` 集合固化——这 17 个名称被插件索引保留，任何插件都无法通过短形式覆盖它们。值得注意的细节是：`handleCommand` 的 switch 还包含 `release_owner`（owner 主动释放身份的命令），但该名称不在保留集合内，因此插件理论上可以注册短形式 `/release_owner`——在 IM 中内建 switch 会先命中，Web 中则由插件展开器决定，两条路径存在行为差异。\n\nSources: [plugin-command-index.ts](src/plugin-command-index.ts#L48-L76)、[index.ts](src/index.ts#L3594-L3631)、[index.ts](src/index.ts#L3617-L3618)、[index.ts](src/index.ts#L4121-L4132)、[plugin-expander-core.ts](src/plugin-expander-core.ts#L424-L428)\n\n## 渠道层命令拦截：先于一切副作用\n\n命令拦截发生在**消息管线的最前端**，这是刻意设计：路由解析（`resolveEffectiveChatJid`）在 native-thread 场景下会触发创建会话、挂载工作区等副作用，一条 `/status` 命令不应为尚未确认的会话付出这些代价。飞书与 Telegram 的注释都明确写出这一顺序约束。\n\n| 平台 | 命令正则 | 平台特化处理 |\n|------|----------|--------------|\n| 飞书 | `/^\\/(\\S+)(.*)$/` | 先 strip 开头 `@botname ` 前缀；支持 `/queue` `/steer` 跟随模式解析 |\n| Telegram | `/^\\/(\\S+?)(?:@\\S+)?(?:\\s+(.*))?$/i` | 处理 `/cmd@BotUsername` 后缀；大小写不敏感 |\n\n飞书对命令处理有额外的**持久化 checkpoint 状态机**（`feishu_slash_command`）：命令执行前写入 `executing`、回复前写入 `pending_reply`、发送中写入 `sending_reply`、完成后写入 `reply_acknowledged`。如果进程在命令执行中途崩溃，恢复时发现 `executing`/`sending_reply` 状态会**停止自动重试并要求人工核对**——因为重跑任意命令副作用是不安全的；只有 `pending_reply` 状态允许安全重发回复文本。\n\nSources: [feishu.ts](src/feishu.ts#L1968-L1992)、[feishu.ts](src/feishu.ts#L2055-L2167)、[telegram.ts](src/telegram.ts#L711-L758)\n\n在命令拦截之前，还有一道**audience 身份边界**：发言者白名单（`owner_only`/`everyone`）检查先于命令与 mention 门控执行，被拒绝的发送者消息直接丢弃。这意味着即使是最无害的 `/list` 命令，也受 audience 策略约束——这是"身份边界独立于激活机制"的架构决策。\n\nSources: [feishu.ts](src/feishu.ts#L1950-L1966)、[im-audience-policy.ts](src/im-audience-policy.ts#L20-L32)\n\n## 内建命令分发与 Owner 门控\n\n`handleCommand` 是内建命令的统一入口（`src/index.ts:3550`），签名接收 `chatJid`、命令字符串、发送者 IM 标识与飞书 mention 列表。执行顺序是固定的三段式：\n\n**第一步：DM 自动认领（Auto-claim）**。在 1:1 私聊中发送者身份无歧义，因此首次执行 owner 级命令时自动将发送者认领为 owner，免去 `/owner_mention` 的额外摩擦；群聊**永不自动认领**（第一位发送命令者不一定是 owner）。直聊判定是纯结构化的：各渠道把 DM 性编码进 JID（`qq:c2c:`、`dingtalk:c2c:`、`discord:dm:`、Telegram 正 ID 等），飞书因有独立的 DM owner 学习路径而恒返回 false。\n\n**第二步：Owner 门控**。`OWNER_REQUIRED_IM_COMMANDS` 集合圈定 7 个会变更工作区状态的命令（`clear`、`bind`、`unbind`、`new`、`sw`、`spawn`、`release_owner`），`checkImOwnerCommand` 统一校验"发送者 === 已登记的 owner_im_id"。豁免集合的取舍值得注意：`owner_mention` 是未认领群组的唯一引导路径所以开放；`require_mention` 是刻意共享的设置开关；`allow`/`disallow`/`allowlist` 在各自 handler 内部已校验 owner；只读命令（`list`/`status`/`where`/`recall`）不设门。\n\n**第三步：switch 分发**。按命令名分发到对应 handler，每个 handler 返回回复文本；**未知命令返回 `null`**——这是整个系统的回退信号，渠道层收到 `null` 后不拦截，消息继续流入普通管线。\n\nSources: [index.ts](src/index.ts#L3550-L3592)、[im-command-utils.ts](src/im-command-utils.ts#L270-L313)、[im-command-utils.ts](src/im-command-utils.ts#L315-L345)\n\n内建命令按业务域可归为四组，各自 handler 复用 `im-command-utils.ts` 的纯函数（`formatWorkspaceList`、`resolveLocationInfo`、`resolveBoundChatTarget`、`formatSystemStatus`），保证可单测性：\n\n| 业务域 | 命令 | 核心逻辑 |\n|--------|------|----------|\n| 会话管理 | `/clear` `/recall` | `executeSessionReset` 停止队列进程、清理 session 文件、写入 `context_reset` 分隔消息并推进游标 |\n| 工作区 | `/list` `/ls` `/new` `/bind` `/unbind` `/where` `/status` | 绑定目标解析 + 位置/状态格式化 |\n| 并行任务 | `/sw <任务>` `/spawn` | 创建 `kind: \'spawn\'` 的 SubAgent 与虚拟 JID 会话 |\n| 权限 | `/allow` `/disallow` `/allowlist` `/owner_mention` `/release_owner` | 白名单增删查、owner 认领/释放 |\n\n以 `/clear` 为例可看到命令如何触达执行层：`handleClearCommand` 先用 `resolveBoundChatTarget` 解析真实绑定目标（agent 绑定 → `#agent:` 虚拟 JID；主会话绑定 → 工作区 JID），再调用 `commands.ts` 的 `executeSessionReset`——停止所有相关队列进程（agent 级只停自身，主会话级停整个 folder 的兄弟 JID）、清空 `.claude/` session 文件（保留 `settings.json`）、删除 DB 会话记录、插入 `context_reset` 系统消息并广播、最后推进 `lastAgentTimestamp` 防止旧消息被新会话重放。\n\nSources: [commands.ts](src/commands.ts#L37-L108)、[index.ts](src/index.ts#L3633-L3673)、[index.ts](src/index.ts#L4511-L4560)、[im-command-utils.ts](src/im-command-utils.ts#L64-L94)\n\n## 插件命令索引：命名空间与冲突消解\n\n插件命令是 Claude Code 插件生态的自然延伸：每个启用插件的 `commands/{name}.md` 文件注册两条别名——短形式 `/{name}` 与命名空间形式 `/{plugin}:{name}`。索引构建时逐插件扫描运行时目录（`{DATA_DIR}/plugins/runtime/{userId}/snapshots/...`），解析 YAML frontmatter（`description`、`argument-hint`、`disable-model-invocation`）与命令正文。\n\n**冲突消解是索引的核心契约**，两条规则决定了寻址空间：\n\n1. **内建名优先**：命令名命中 `BUILTIN_COMMAND_NAMES` 时丢弃短形式（内建 handler 在裸 `/status` 上永远获胜），但命名空间形式仍可寻址；\n2. **双重冲突检测**：`byShort`（2+ 插件都提供 `/foo`）与 `byNamespaced`（2+ marketplace 都提供 `/codex:status`）中超过一个条目的 key 进入 `conflicts` 列表。`resolveCommand` 对冲突返回 `conflict`，展开器据此生成"请使用命名空间形式"的引导回复。\n\n索引缓存是**每用户内存 Map + epoch 防丢失失效**：首次 `buildCommandIndex(userId)` 构建后 memoize；插件启用/禁用或 marketplace 删除时由 `routes/plugins.ts` 调用 `invalidateUserCommandIndex`，epoch 递增确保构建中途的失效不会被旧结果覆盖。没有时间 TTL——启用状态是唯一会改变解析结果的变更源。\n\nSources: [plugin-command-index.ts](src/plugin-command-index.ts#L14-L35)、[plugin-command-index.ts](src/plugin-command-index.ts#L207-L277)、[plugin-command-index.ts](src/plugin-command-index.ts#L283-L311)、[plugin-command-index.ts](src/plugin-command-index.ts#L317-L338)、[plugin-command-index.ts](src/plugin-command-index.ts#L358-L382)\n\n## 插件命令展开：三态结果与崩溃安全\n\n展开器（`plugin-expander-core.ts`）是命令系统的执行终端，输出严格的三态结果：\n\n| 状态 | 含义 | 后续动作 |\n|------|------|----------|\n| `miss` | 非插件命令，或命中非 DMI 命令 | 原样透传给 Agent（非 DMI 由 SDK `--plugin-dir` 原生处理） |\n| `expanded` | DMI 命令渲染完成 | 生成的 prompt 替换原消息内容发送给 Agent |\n| `reply` | 冲突提示 / 容器未启动等带内回复 | 作为系统消息存储并广播，不消耗 Agent turn |\n\nDMI（`disable-model-invocation: true`）命令在 CLI 中由人输入后展开、对模型隐藏，SDK 模式下失去了这种展开能力，因此 HappyClaw 需要自建索引与展开器。展开过程处理命令正文中的模板变量：`${CLAUDE_PLUGIN_ROOT}`（插件根路径，Host 与 Docker 路径前缀不同）、`$ARGUMENTS`（经环境变量传递，绝不走 JS 插值）、`$1/$2` 位置参数；独立的 `!`\\`bash\\`\\` 行会**执行并把 stdout 拼接进 prompt**（host 或 docker exec 取决于执行模式），而围栏代码块内的内容原样透传。最终渲染为一段结构化 prompt（命令名、插件标识、参数、frontmatter、正文）交给 Agent。\n\nSources: [plugin-expander-core.ts](src/plugin-expander-core.ts#L390-L428)、[plugin-expander-core.ts](src/plugin-expander-core.ts#L140-L171)、[plugin-expander-core.ts](src/plugin-expander-core.ts#L566-L597)\n\n**崩溃安全**是展开器最精巧的设计。inline `!` bash 是副作用（非纯解析），若崩溃恢复时从原始 DB 行重新展开，inline 命令会**双重执行**。解决方案是 sentinel 机制：展开成功（且所有 inline 成功）后，把渲染出的 prompt 持久化到消息行的 `attachments` 字段（`PLUGIN_EXPANSION_ATTACHMENT_TYPE`）；恢复路径先检查 sentinel，命中则逐字重放已存 prompt，绝不再调展开器。持久化写入发生在游标推进之前，保证"写完才前进"的时序。\n\nSources: [plugin-expander-core.ts](src/plugin-expander-core.ts#L629-L686)、[plugin-expander-core.ts](src/plugin-expander-core.ts#L617-L627)\n\n## 消息分发：IM 与 Web 的统一决策流\n\n插件命令在 IM 管线中的接入点是**冷启动消息处理**而非渠道拦截层——内建 `handleCommand` 不识别插件命令（返回 `null`），消息作为普通消息进入队列后，由 `expandMessagesIfNeeded` 批量处理。全系统共有三处展开接入点，覆盖所有消息来源：\n\n| 接入点 | 位置 | 场景 |\n|--------|------|------|\n| `processGroupMessages` | [index.ts](src/index.ts#L5691-L5708) | IM 群组消息冷启动，回复按 `source_jid` 路由回原渠道 |\n| Agent 会话冷启动 | [index.ts](src/index.ts#L14109-L14124) | 虚拟 JID（`#agent:`）消息恢复，回复路由到 agent 的 `last_im_jid` |\n| `prepareFollowUp` | [index.ts](src/index.ts#L1456-L1477) | 跟随模式队列（queue/steer）消息 |\n\nWeb 路径则采用**热路径/冷启动混合策略**：当存在活跃 Runner 时（`eagerExpandActive`），消息入队前先调用单条展开器，`reply` 短路为带内系统消息、`expanded` 直接改写待发送内容；当空闲时不调用展开器，把展开责任完全交给冷启动的批量路径——因为调用展开器本身会执行 inline 副作用，热路径执行后若 Runner 崩溃，冷启动会从原始 DB 行再次展开导致双重执行。热路径在改写消息的同时也会写 sentinel，把这条竞态窗口压到最小。\n\nSources: [web.ts](src/web.ts#L683-L707)、[web.ts](src/web.ts#L1055-L1157)、[index.ts](src/index.ts#L5251-L5286)\n\n完整的消息分发决策流可以概括为一条逐级回退的链：**audience 身份边界 → 渠道正则拦截（含飞书 checkpoint）→ 内建 switch（owner 门控）→ 插件展开三态 → mention 门控 → Agent 管线**。每一层都用"未命中"信号把控制权交还给下一层，而插件展开层则是唯一同时处理"拦截"与"回退"的层——`miss` 即回退，`reply` 即拦截，`expanded` 即改写。这一设计让渠道差异被收敛在拦截层，业务逻辑收敛在分发层，插件生态收敛在展开层。\n\nSources: [feishu.ts](src/feishu.ts#L2239-L2260)、[feishu-mention-gate.ts](src/feishu-mention-gate.ts#L1-L16)、[index.ts](src/index.ts#L5691-L5708)\n\n## 延伸阅读\n\n命令系统是消息渠道的核心执行面，与以下主题紧密耦合：\n\n- [渠道挂载与会话绑定边界](14-qu-dao-gua-zai-yu-hui-hua-bang-ding-bian-jie)：`/bind` 命令操作的目标模型——工作区挂载与 agent 会话绑定的底层结构\n- [会话队列与并发控制](18-hui-hua-dui-lie-yu-bing-fa-kong-zhi)：`/sw` 并行任务与 `/clear` 停止队列进程的并发语义\n- [Agent 能力治理：Skills、MCP 与 Plugins](8-agent-neng-li-zhi-li-skills-mcp-yu-plugins)：插件命令索引的生态来源——插件的启用/禁用生命周期\n- [渠道可靠性状态机：Inbox、Turn 与 Outbox](16-qu-dao-ke-kao-xing-zhuang-tai-ji-inbox-turn-yu-outbox)：飞书命令 checkpoint 所依赖的 Inbox 租约机制';export{n as default};

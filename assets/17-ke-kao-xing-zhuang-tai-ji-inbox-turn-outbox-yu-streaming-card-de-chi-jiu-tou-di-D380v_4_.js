const e='当一条用户消息穿越"渠道 WebSocket → Agent 执行 → 回复投递"的完整链路时，进程可能在任意一步被 SIGKILL。Miniclaw 用一套基于 SQLite 的持久状态机来回答两个问题：**哪些工作已经被做过**（避免重复执行），以及**哪些副作用的结果不可知**（避免盲目重试造成重复消息）。本页剖析 `channel-reliability-store.ts` 为核心的四张账本——Inbox、Turn、Outbox 与 Streaming Card——的租约 fencing 协议、状态迁移规则与崩溃恢复编排。\n\n## 设计边界：只管持久化与 Fencing\n\n这套机制刻意收窄了模块职责：`channel-reliability-store.ts` 只拥有**持久化与 fencing**，Provider I/O、路由决策和重试策略全部留在渠道/运行时层。注释明确指出，保持这一窄边界使得同一套 inbox/run/outbox 模型可以被飞书、QQ、微信及未来传输渠道复用。整个模块不发起任何网络调用，全部操作是对绑定 SQLite 连接的事务性 SQL。\n\n五张表在 schema v60 引入：`channel_inbox`（入站去重）、`channel_cursors`(游标)、`turn_runs`（执行轮次）、`channel_outbox`（逐条产物投递）与 `streaming_cards`(卡片投影)，各自带有非终止态复合索引以支撑恢复扫描。db.ts 只负责建表并把连接绑定到本进程，schema 的所有权归 store 模块。\n\n```mermaid\nflowchart LR\n    WS[渠道事件<br/>WS / Backfill] --> INBOX[("channel_inbox<br/>入站去重账本")]\n    INBOX -->|claim + lease| TURN[("turn_runs<br/>执行轮次账本")]\n    TURN --> CARD[("streaming_cards<br/>卡片投影")]\n    TURN --> OUTBOX[("channel_outbox<br/>逐条产物投递")]\n    OUTBOX -->|provider ACK| PROVIDER((IM Provider))\n    CARD -->|message_id / card_id| PROVIDER\n    CURSOR[("channel_cursors<br/>单调游标")] -.->|terminal 后配对推进| INBOX\n    REAPER[启动/15s 对账循环] -.->|fence 过期租约| TURN\n    REAPER -.->|reconcile 非终止卡片| CARD\n```\n\nSources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L1-L10), [channel-reliability-store.ts](src/channel-reliability-store.ts#L287-L437), [db.ts](src/db.ts#L2370-L2379)\n\n## 四个实体的状态空间\n\n四个实体共享同一设计语言：非终止态可迁移、终态不可变、每次迁移都以条件 UPDATE（compare-and-set）表达。下表汇总各状态的语义与终态集合：\n\n| 实体 | 状态序列 | 终态 | 关键不变量 |\n|---|---|---|---|\n| **Inbox** | `received → admitted → queued → processing → processed / ignored / failed` | processed, ignored, failed | `UNIQUE(provider, account_id, external_message_id)` 全局去重 |\n| **Turn** | `queued → running ⇄ retry_wait / waiting_user → finalizing → completed / failed / interrupted / cancelled` | completed, failed, interrupted, cancelled | `idempotency_key` UNIQUE；每个 inbox 至多一个 run |\n| **Outbox** | `pending → claimed → uploading → uploaded → sending → delivered`，旁路 `retry_wait / uncertain / failed / cancelled` | delivered, failed, cancelled | `UNIQUE(turn_run_id, ordinal)`；payload hash 参与幂等键 |\n| **Streaming Card** | `creating → streaming → recovering → completed / aborted / failed` | completed, aborted, failed | 每 turn 至多一张活跃卡；revision CAS 写入 |\n\n值得注意的是 Outbox 的 `uncertain` 态：它不是失败，而是"Provider 可能已经接受了这条消息，但回执丢失"的显式标记。这个状态会**冻结整个 Turn**——任何自动重放都被禁止，直到人工裁决。同样，Turn 的 `interrupted` 态配合结构化错误前缀 `[manual_reconciliation] `，把"副作用结果不可知"的情况从自动恢复路径中隔离出来。\n\nSources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L31-L71), [channel-reliability-store.ts](src/channel-reliability-store.ts#L190-L210), [channel-reliability-store.ts](src/channel-reliability-store.ts#L1132-L1150)\n\n## 统一租约协议：lease_token 单调 Fencing\n\n三个队列共用同一 claim 协议。认领是一个事务内的两步操作：先按 `(status 可认领 AND available_at 已到 AND 无租约) OR (processing 且租约已过期)` 选出候选行，再以 `WHERE id = ? AND lease_token = 旧值` 的条件 UPDATE 把 token 加一并写入新租约——两个并发认领者只有一个能改写成功。之后**所有**后续变更（更新载荷、续约、完成、失败）都要求 `status = \'processing\' AND lease_owner = ? AND lease_token = ? AND lease_expires_at > ?` 同时成立。\n\n这套设计的核心是 fencing token 的单向性：过期或被抢走的租约永远无法复活。`renewChannelInboxClaim` 的文档注释直接点明"an expired/stolen claim can never be resurrected"——旧持有者即使还拿着内存里的 claim 对象，它的 token 已经落后，所有写入都会静默返回 false，调用方据此进入 `fenceLost` 处理分支。Turn 侧还有更严格的变体：`interruptChannelTurnRunById` 这类 fence 操作会主动 `lease_token = lease_token + 1`，立即废掉存活中的旧 token，保证停止按钮或对账逻辑能瞬间压制正在执行的轮次。\n\n```mermaid\nsequenceDiagram\n    participant A as 进程 A（旧持有者）\n    participant DB as SQLite\n    participant B as 进程 B（新认领者）\n    B->>DB: BEGIN; SELECT candidate (lease_token=7, 租约已过期)\n    B->>DB: UPDATE SET owner=B, token=8 WHERE token=7\n    Note over DB: A 的后续写入全部失配\n    A->>DB: heartbeat WHERE token=7 AND expires>now\n    DB-->>A: changes=0 → fenceLost\n    B->>DB: complete WHERE token=8 AND expires>now\n    DB-->>B: changes=1 → 成功终态化\n```\n\nSources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L774-L851), [channel-reliability-store.ts](src/channel-reliability-store.ts#L883-L903), [channel-turn-runtime.ts](src/channel-turn-runtime.ts#L318-L334)\n\n## Inbox：入站去重与"游标配对"\n\n飞书是这套模型的参考实现。每条 WS 或 backfill 消息在**触发任何业务逻辑之前**先 `recordChannelInbox` 落库，靠唯一约束天然去重；如果落库本身抛错，代码拒绝降级为易失执行——注释写道"没有 Inbox 唯一性 fence，两个 WS 客户端或一次重连就会启动同一个 Agent turn"。已处于终态的重复事件直接跳过执行。\n\n处理完成后有一个精巧的可恢复配对：`completeChannelInbox` 先行，`advanceChannelCursor` 随后。注释解释了这个窗口的正确性——若进程死在两次写入之间，重复的 WS/backfill 事件会看到终态 Inbox 行，再次触发游标推进完成修复，而无需重跑用户代码。游标本身也是 CAS 式单调推进：SQL 里 `ON CONFLICT ... DO UPDATE ... WHERE excluded.position > position OR (position 相同 AND tie_breaker 更大)`，乱序到达的事件无法让游标回退。对于 IPC 批量投递场景，`delivery-cursor.ts` 还提供了"越带游标不得跨越未覆盖 DB 消息"的守卫函数与延迟提交台账，防止批量回执误伤中间未处理的记录。\n\n失败侧区分两种走向：携带 `retryAt` 的 `failChannelInbox` 把行释放回 `queued` 并推迟 `available_at`，由重连后的 `recoverQueuedInbox` 循环重新认领（上限 500 条）；不带重试时间的失败则终态化为 `failed`。处理期间的心跳定时器周期性续约（飞书租约 5 分钟），续约失败即视为失去 fence 并停止心跳。\n\nSources: [feishu.ts](src/feishu.ts#L1769-L1800), [feishu.ts](src/feishu.ts#L1193-L1233), [channel-reliability-store.ts](src/channel-reliability-store.ts#L484-L537), [delivery-cursor.ts](src/delivery-cursor.ts#L27-L43), [feishu.ts](src/feishu.ts#L221)\n\n## Turn：确定性 ID、重放抑制与四态处置\n\n每个逻辑输入轮次的身份是确定性的：`runId = turn_<sha256(logicalKey) 前 32 hex>`，其中 `logicalKey = channel-turn-v1:provider:accountId:externalMessageId:agentId`。这意味着同一外部消息无论重放多少次，都会命中同一个 `turn_runs` 行——`createChannelTurnRun` 用 `INSERT OR IGNORE` 加冲突校验（幂等键、provider、account、sourceJid 必须一致，否则抛出冲突异常）实现"创建即去重"。\n\n`ChannelTurnRuntime.start()` 在认领之前先做一道**重放抑制 fence**：`interruptChannelTurnRunWithDeliveredEffect` 检查该 run 名下是否已存在 `delivered` 的 Outbox 行或 `completed` 的卡片——若有，说明崩溃发生在"Provider ACK 已持久化"与"Turn 完成"之间，重放 Agent 循环会产生兄弟输出。此检查必须在新执行认领之前运行。综合这些检查，运行时对外暴露四种处置：\n\n| executionDisposition | 触发条件 | 调用方行为 |\n|---|---|---|\n| `execute` | 成功拿到租约 | 正常执行 |\n| `defer` | 排队中但被他人租用 / 未到期 | 稍后由队列再试 |\n| `skip_terminal` | 初始状态已是终态 | 直接读取既有结果 |\n| `manual_reconciliation` | 已交付副作用后被打断 | 发送人工对账通知，不再自动执行 |\n\n默认租约 45s、心跳 12s（不超过租约一半）。完成路径有双重保险：`ChannelTurnRuntime.complete()` 在内存层拒绝任何存在 `uncertain` 兄弟行的完成请求；底层 `completeChannelTurnRun` 的 SQL 再拦一次——只要存在非终态 Outbox 子行，`completed` 迁移直接失败。这保证了"父 Turn 完成时所有子产物都已收敛"这一不变量。\n\nSources: [channel-turn-runtime.ts](src/channel-turn-runtime.ts#L46-L129), [channel-reliability-store.ts](src/channel-reliability-store.ts#L1057-L1123), [channel-reliability-store.ts](src/channel-reliability-store.ts#L1156-L1193), [channel-turn-runtime.ts](src/channel-turn-runtime.ts#L148-L160), [channel-reliability-store.ts](src/channel-reliability-store.ts#L1433-L1482)\n\n## Outbox：分阶段持久投递与 uncertain Fence\n\nOutbox 是整台状态机中最精细的部分。`deliverChannelOutboxItem` 把"一条物理输出"（文本/图片/文件/卡片/mutation）的投递拆成逐段持久化的阶段：\n\n```mermaid\nflowchart TB\n    U[uncertain 兄弟行? 存在则直接短路] --> E["enqueueChannelOutbox<br/>幂等键 = turnRunId:ordinal:kind:sha256(payload)"]\n    E --> C{claimChannelOutboxById<br/>60s 租约}\n    C -->|单阶段模式| S["markSending → provider.send()"]\n    C -->|两阶段模式 image/file| UP["uploading → upload() → uploaded<br/>回执独立持久化"]\n    UP --> S\n    S --> ACK{收到非空 providerMessageId?}\n    ACK -->|是| D["delivered + 回执落库"]\n    ACK -->|普通异常 after sending| UNC["uncertain ⛔ 冻结整个 Turn"]\n    ACK -->|Definitive 明确拒绝| R["retry_wait(带 retryAt) 或 failed"]\n```\n\n错误分类学是这个协议的灵魂。`sending` 阶段之后的普通超时/断连**不能证明** Provider 拒绝了消息，因此一律 fence 为 `uncertain` 而不是重试；只有 `DefinitiveChannelDeliveryError`（Provider 显式拒绝）才允许安全地进入重试或失败。回执校验也遵循同样的保守哲学：上传阶段缺少 upload key 是明确失败（此时必然无可见消息）；发送阶段缺少 message id 则是模糊结果——可见消息可能已经发出。测试专用的 `ChannelDeliveryProcessCrash` 故障注入会原样上抛且不动租约，使启动对账观察到与真实 SIGKILL 相同的持久状态。\n\n幂等身份的设计同样值得注意。飞书能力 mutation 场景中，runner/model 重试会分配全新 requestId，因此语义身份刻意**排除 requestId**，改为路由快照 + kind + payload hash 的组合哈希；ordinal 也从该语义身份派生，保证 SQLite 安全的正整数。对不支持统一回执的旧式连接器 API，代码在操作确认成功后才持久化一个确定性合成 ACK（`miniclaw-synthetic:<digest>`）。启动时的 `reconcileExpiredChannelOutbox` 则补齐崩溃缺口：过期的 pre-send 租约（claimed/uploading/uploaded）安全回到 `retry_wait`，过期的 `sending` 一律转 `uncertain`。\n\nSources: [channel-outbox-delivery.ts](src/channel-outbox-delivery.ts#L244-L404), [channel-outbox-delivery.ts](src/channel-outbox-delivery.ts#L73-L87), [feishu-capability-outbox.ts](src/feishu-capability-outbox.ts#L46-L120), [channel-outbox-runtime-scope.ts](src/channel-outbox-runtime-scope.ts#L131-L177), [channel-reliability-store.ts](src/channel-reliability-store.ts#L1951-L1986)\n\n### uncertain 的人工裁决面\n\n`uncertain` 行会冻结所属 Turn 直到有人裁决，而这个 fence 必须有释放口，否则行就永远不可达。监控路由暴露了一对端点：`GET /api/status/channel-outbox/uncertain` 按 `updated_at` 最旧优先列出待裁决项（载荷被刻意省略以保护用户内容），`POST /api/status/channel-outbox/:id/resolve` 接受 `delivered`（必须附 providerMessageId）或 `failed` 两种裁决，以 revision CAS 提交——CAS 失败时绝不盲目重试，要求调用方重新读取再决策。\n\nSources: [routes/monitor.ts](src/routes/monitor.ts#L374-L475), [channel-reliability-store.ts](src/channel-reliability-store.ts#L1707-L1725)\n\n## Streaming Card：卡片投影的持久化与恢复纪律\n\n流式卡片的难点在于它同时是"Provider 端的活对象"和"本地崩溃恢复的证据"。`streaming_cards` 表为每张卡持久化 provider 身份（`message_id`/`card_id`）、版本号、文本快照与 revision。`StreamingCardController` 通过生命周期钩子把 creating/streaming/completed/aborted/failed 及快照推给宿主，`ChannelTurnRuntime.onCardEvent` 以 revision CAS（最多三次冲突重读重试）将每个事件落库；任何持久化失败都会置起 `durabilityError`，阻止 Turn 完成。\n\n两个关键防御动作体现了 fail-closed 原则。其一是**预留回滚**：`rollbackUnpublishedStreamingCardReservation` 只在 DELETE 语句内同时验证执行租约、run/card 身份、card revision、`status=\'creating\'` 且无 provider 身份、且不存在活跃 Outbox 行——任何可能已触达 Provider 的痕迹都会使操作失败并保留现场供对账。其二是**双状态机解耦**：卡片投影与 Agent 执行是独立的状态机，compact/溢出可以关闭 Provider 卡片而同一逻辑输入仍在运行，只有宿主的确切 `inputTurnCompleted` 加物理投递 ACK 才能终结 Turn。\n\nSources: [feishu-streaming-card.ts](src/feishu-streaming-card.ts#L2146-L2180), [channel-turn-runtime.ts](src/channel-turn-runtime.ts#L386-L479), [channel-reliability-store.ts](src/channel-reliability-store.ts#L2143-L2187)\n\n## 崩溃恢复编排：启动序与 Live 循环\n\n恢复顺序本身就是一个正确性边界。主入口在启动时严格排序：先 `deferInbound()` 暂停入站、连接全部启用账号，然后依次执行卡片对账 → Outbox 对账 → 启动 15s 恢复循环 → 注册渠道就绪触发器 → `resumeDeferredInbound()`，最后才恢复各类缓冲并启动消息循环。注释点明风险：过早启动消息循环会在 Provider 还显示旧卡运行中时，为同一逻辑轮次创建第二张活跃卡。\n\n```mermaid\nflowchart TD\n    A["imManager.deferInbound()"] --> B["并行连接所有启用账号"]\n    B --> C["reconcileChannelReliabilityOnStartup<br/>关闭旧进程遗留的非终止卡片"]\n    C --> D["reconcileChannelOutboxDeliveries<br/>过期 pre-send→retry_wait / expired sending→uncertain"]\n    D --> E["startChannelReliabilityRecoveryLoop (15s)<br/>+ onChannelReady 触发"]\n    E --> F["resumeDeferredInbound()"]\n    F --> G["recoverPendingMessages 等 → startMessageLoop"]\n```\n\n对账 pass 本身遵循几条纪律。**尊重存活租约**：卡片所属 Turn 若仍处于 running/finalizing 且租约未过期，启动与 live 模式都必须让路，死主的行等租约有界过期后再回收。**Boot epoch fence**：live 定时器只能碰启动时间戳之前创建的行——否则一个健康的 30s+ 长回复会被 15s 定时器误杀。**精确 Bot 归属**：卡片对账只允许通过记录中的那个 Bot 账号执行，路由不匹配或 Bot 未授权/未就绪都直接抛错延迟处理，绝不静默地用另一个 Bot 收尾。**缺失身份即人工对账**：中断发生时若连 messageId/cardId 都没来得及持久化，无法安全地在 Provider 端定位卡片，只能置 failed 并 interrupt 所属 Turn。\n\n对账成功的卡片被终态化为 `aborted` 并附带恢复原因快照，同时 interrupt 其 Turn；Provider I/O 失败则释放 recovery 租约延迟到下一轮（`recovering` 刻意保持非终止态，后续 pass 可再次认领）。启动模式额外执行 `interruptChannelTurnRunsWithDeliveredEffects` 批量关闭"ACK 之后崩溃"窗口，以及 `interruptExpiredChannelTurnRuns`——过期执行不会被自动重放，因为副作用不可知。日志层面还有一个工程细节：全零结果的 live pass 以 debug 级输出，因为生产环境 87% 的 info 日志曾被这种空转刷屏。\n\nSources: [index.ts](src/index.ts#L21320-L21368), [channel-reliability-recovery.ts](src/channel-reliability-recovery.ts#L34-L206), [channel-reliability-recovery.ts](src/channel-reliability-recovery.ts#L213-L261), [im-manager.ts](src/im-manager.ts#L997-L1042)\n\n## 运维面：健康扫描与生命周期清理\n\n两块运维支撑补齐闭环。`scanChannelReliabilityNonterminal` 按表枚举所有非终止行，供健康面板观察积压。`cleanupChannelReliability` 分两级回收：先对终态行做载荷脱敏（raw_payload/payload/result/snapshot 置 NULL 但保留幂等回执），再删除超出 Provider 重放窗口的终态审计行——删除带有防御性子查询，存在非终止 Outbox/卡片子行的 Turn 和仍被 Turn 引用的 Inbox 都不会被动到；外键级联确保子表先于父行消失。退役传输的游标则通过显式的 CAS 条件删除。\n\nSources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L2338-L2492)\n\n## 测试证据\n\n契约测试覆盖了上述每个不变量：`channel-reliability-store.test.ts` 用 20 个用例验证四张账本的创建幂等性、跨 Bot 认领隔离、去重、租约续期与抢占、waiting/resume CAS、过期执行 interrupt 等；`channel-outbox-delivery.test.ts` 专门覆盖崩溃窗口与 uncertain fence；schema v60 迁移测试验证 v59 就地升级保留状态、重启幂等并生成迁移备份。E2E 层还有 `interrupt-ipc-message-loss.mjs` 场景模拟中断下的 IPC 消息丢失。\n\n| 测试文件 | 关注点 |\n|---|---|\n| [schema-v60-channel-reliability.test.ts](tests/schema-v60-channel-reliability.test.ts#L32-L66) | v59→v60 升级、状态保留、重启幂等 |\n| [channel-reliability-store.test.ts](tests/channel-reliability-store.test.ts#L40-L1205) | 四账本 CRUD、fencing、CAS、清理 |\n| [channel-outbox-delivery.test.ts](tests/channel-outbox-delivery.test.ts) | 投递阶段持久化、崩溃注入、uncertain |\n| [channel-turn-runtime.test.ts](tests/channel-turn-runtime.test.ts) | Turn 生命周期、重放抑制、卡片预留 |\n\n## 小结与延伸阅读\n\n这台状态机的统一哲学可以浓缩为三句话：**先落库再动手**（Inbox 记录先于执行、Outbox 阶段先于 Provider 调用）；**不确定即冻结**（uncertain 副作用阻断 Turn 完成，interrupted 副作用阻断重放）；**恢复尊重租约**（一切对账都在租约过期后才接管，且只通过记录中的精确身份操作 Provider）。理解这套模型后，建议按以下路径继续深入：\n\n- 渠道适配器如何消费这套模型：[七大渠道适配器：飞书、Telegram、微信、QQ、钉钉、Discord、WhatsApp](15-qi-da-qu-dao-gua-pei-qi-fei-shu-telegram-wei-xin-qq-ding-ding-discord-whatsapp)\n- Turn 之上的串行队列与卡死恢复：[会话串行队列：Runner 生命周期、重试退避与卡死恢复](14-hui-hua-chuan-xing-dui-lie-runner-sheng-ming-zhou-qi-zhong-shi-tui-bi-yu-qia-si-hui-fu)\n- 流式卡片的渲染细节与 StreamEvent 契约：[StreamEvent 流式契约：三端共享类型与同步机制](25-streamevent-liu-shi-qi-yue-san-duan-gong-xiang-lei-xing-yu-tong-bu-ji-zhi)\n- 底层 SQLite 迁移策略：[SQLite 持久化：Schema 版本化、迁移策略与核心表族](9-sqlite-chi-jiu-hua-schema-ban-ben-hua-qian-yi-ce-lue-yu-he-xin-biao-zu)';export{e as default};

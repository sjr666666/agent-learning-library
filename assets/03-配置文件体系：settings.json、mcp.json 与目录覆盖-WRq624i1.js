const n='MiniCode 的配置体系遵循「少即是多」的设计原则：你只需要理解**三个文件**（`settings.json`、`mcp.json`、项目级 `.mcp.json`）和**一套合并规则**，就能掌控模型的连接、认证与外部工具注入。本文面向零基础读者，从"配置文件长什么样"出发，逐步拆解文件位置、覆盖优先级、字段含义与常用操作命令，最后给出一个从安装到运行的最小实践路径。\n\nSources: [config.ts](src/config.ts#L36-L46), [USAGE.md](USAGE.md#L243-L269)\n\n## 配置文件全景：一张目录图看懂所有位置\n\nMiniCode 的配置分为**用户级**（全局，位于 `~/.mini-code/`）与**项目级**（跟随当前工作目录）两个层面。启动时，程序从各文件读取内容并按优先级合并成一份"有效设置"。\n\n```\n~/.mini-code/                        # 用户级配置目录（可用 MINI_CODE_HOME 覆盖）\n├── settings.json                    # 用户设置：模型、环境变量、最大输出、MCP\n├── mcp.json                         # 全局 MCP 服务器（仅含 mcpServers）\n├── mcp-tokens.json                  # 远程 MCP 的 bearer token（minicode mcp login 写入）\n├── mcp-protocol-cache.json          # stdio 协议协商缓存（自动维护）\n├── permissions.json                 # 权限存储（路径/命令白名单）\n└── projects/                        # 按工作目录编码的会话 JSONL 目录\n\n~/.claude/settings.json              # Claude Code 兼容回退（可选）\n<当前项目>/.mcp.json                 # 项目级 MCP 服务器（Claude Code 兼容格式）\n<当前项目>/.mini-code/               # /init 生成的项目脚手架（MINI.md、rules/）\n```\n\n关键点在于：**用户级目录的默认位置是 `~/.mini-code/`，但可通过环境变量 `MINI_CODE_HOME` 整体重定向**——这是"目录覆盖"的第一层含义。安装时指定自定义目录，所有配置、会话、权限数据都会随之迁移到新位置。\n\nSources: [config.ts](src/config.ts#L36-L46), [USAGE.md](USAGE.md#L100-L110)\n\n## 文件职责与优先级：谁覆盖谁\n\n四个配置来源按固定顺序合并，**越靠后优先级越高**。`loadEffectiveSettings` 会并发读取四个文件，然后用 `mergeSettings` 逐级合并：\n\n```mermaid\nflowchart LR\n    A["~/.claude/settings.json<br/>Claude 兼容设置<br/>（优先级最低）"] --> M1\n    B["~/.mini-code/mcp.json<br/>全局 MCP 服务器"] --> M1\n    M1["merge ①"] --> M2\n    C["./.mcp.json<br/>项目级 MCP 服务器"] --> M2\n    M2["merge ②"] --> M3\n    D["~/.mini-code/settings.json<br/>MiniCode 用户设置<br/>（文件级最高）"] --> M3\n    M3["merge ③"] --> E["有效设置 effectiveSettings"]\n    E --> F["运行时配置 loadRuntimeConfig"]\n    G["process.env<br/>环境变量<br/>（运行时最高）"] --> F\n    F --> H["模型适配器 / 工具注册表"]\n```\n\n合并遵循三条精细规则：\n\n1. **顶层字段浅合并**：`model`、`maxOutputTokens` 等直接覆盖；\n2. **`env` 深度合并**：后出现的文件只补充新键，不整体替换；\n3. **`mcpServers` 按服务器名逐个合并**：同名服务器时字段级拼接，且其内部的 `env` 与 `headers` 也做深度合并——这意味着你可以在 `mcp.json` 里定义服务器的命令，再在 `settings.json` 里只补充它的 `headers`，两者会合成一份完整配置。\n\n进入运行时后，`process.env` 再叠加一层：进程环境变量中的 `MINI_CODE_MODEL`、`ANTHROPIC_BASE_URL` 等会**压过**所有文件设置。官方文档给出的完整优先级为：`~/.mini-code/settings.json` > `~/.mini-code/mcp.json` > 项目 `.mcp.json` > Claude 兼容设置 > 进程环境变量。\n\nSources: [config.ts](src/config.ts#L139-L188), [config.ts](src/config.ts#L203-L251), [USAGE.md](USAGE.md#L306-L312)\n\n## settings.json：模型、认证与全局环境\n\n`settings.json` 是 MiniCode 自己的主配置文件，由 `npm run install-local` 交互式安装向导写入。它支持四个顶层字段：\n\n| 字段 | 类型 | 作用 | 示例 |\n|---|---|---|---|\n| `model` | string | 默认模型名（运行时被 `MINI_CODE_MODEL` 或 `ANTHROPIC_MODEL` 覆盖） | `"claude-sonnet-4-20250514"` |\n| `maxOutputTokens` | number | 单次回复的最大输出 token 数 | `8192` |\n| `env` | object | 注入模型适配器与子进程的环境变量（值可为字符串或数字） | `{ "ANTHROPIC_BASE_URL": "..." }` |\n| `mcpServers` | object | 内联声明 MCP 服务器（等价于写入 mcp.json） | `{ "filesystem": {...} }` |\n\n运行时解析逻辑（`loadRuntimeConfig`）会从合并后的设置中提取：`baseUrl` 默认 `https://api.anthropic.com`（可被 `env.ANTHROPIC_BASE_URL` 覆盖）；认证优先读取 `ANTHROPIC_AUTH_TOKEN`，其次 `ANTHROPIC_API_KEY`。**如果最终既没有模型也没有认证信息，程序会直接报错并提示你去 `~/.mini-code/settings.json` 或环境变量中补充**——这是新手最常见的启动失败原因，报错信息本身就是配置指引。\n\n一个最简示例（安装向导的产物）：\n\n```json\n{\n  "model": "claude-sonnet-4-20250514",\n  "env": {\n    "ANTHROPIC_BASE_URL": "https://api.anthropic.com",\n    "ANTHROPIC_AUTH_TOKEN": "your-token",\n    "ANTHROPIC_MODEL": "claude-sonnet-4-20250514"\n  }\n}\n```\n\nSources: [config.ts](src/config.ts#L6-L11), [config.ts](src/config.ts#L203-L251), [install.ts](src/install.ts#L39-L87)\n\n## mcp.json 与 .mcp.json：MCP 服务器的两种作用域\n\nMCP（Model Context Protocol）服务器为 MiniCode 提供外部工具。同一个 `mcpServers` 结构可以写在三个地方，按作用域从小到大：\n\n| 文件 | 作用域 | 何时使用 |\n|---|---|---|\n| `~/.mini-code/mcp.json` | 用户全局 | 所有项目通用的服务器 |\n| `./.mcp.json`（项目根目录） | 项目级 | 只属于当前仓库的服务器（Claude Code 兼容格式） |\n| `~/.mini-code/settings.json` 内嵌 | 用户全局 | 与其它设置放在一起维护 |\n\n每个服务器配置项支持以下字段：\n\n| 字段 | 类型 | 说明 |\n|---|---|---|\n| `command` | string | stdio 启动命令（如 `npx`） |\n| `args` | string[] | 命令参数 |\n| `env` | object | 附加到子进程的环境变量 |\n| `url` | string | 远程 HTTP 端点（配合 `streamable-http`） |\n| `headers` | object | HTTP 请求头，值支持 `$VAR` / `${VAR}` 环境变量插值 |\n| `cwd` | string | 子进程工作目录（相对启动目录解析） |\n| `enabled` | boolean | 设为 `false` 时跳过连接，状态显示 `disabled` |\n| `protocol` | string | `auto`（默认）、`content-length`、`newline-json`、`streamable-http` |\n\n对本地 stdio 服务器，协议默认 `auto`：先尝试标准 MCP 的 `Content-Length` 帧格式，失败后回退到换行分隔 JSON；协商结果缓存在 `mcp-protocol-cache.json` 避免重复探测。对远程服务器，使用 `streamable-http` + `url`，认证令牌通过 `minicode mcp login <name> --token <token>` 单独存入 `mcp-tokens.json`（不会明文出现在配置文件中），请求时自动注入 `Authorization: Bearer <token>`。\n\nSources: [config.ts](src/config.ts#L13-L22), [config.ts](src/config.ts#L85-L137), [mcp.ts](src/mcp.ts#L297-L355), [mcp.ts](src/mcp.ts#L985-L1007)\n\n## 目录覆盖的第二层：项目级配置与 /init 脚手架\n\n除了用 `MINI_CODE_HOME` 重定向用户目录，"目录覆盖"还指**项目级配置覆盖用户级配置**。进入项目目录后，`./.mcp.json` 的项目 MCP 服务器会自动叠加到全局配置之上（合并顺序高于 `~/.mini-code/mcp.json`）。\n\n此外，交互式斜杠命令 `/init` 会在当前项目生成脚手架：\n\n- 创建 `.mini-code/` 目录与 `MINI.md` 指令文件（内容根据检测到的语言/框架自动生成）；\n- 向 `.gitignore` 追加 `# MiniCode local artifacts` 段落，忽略 `.mini-code/settings.local.json` 与 `.mini-code/sessions/`；\n- 生成的 `MINI.md` 中明确约定：**共享默认值放 `~/.mini-code/settings.json`，项目本地覆盖保留给 `.mini-code/settings.local.json`**。\n\n需要特别说明的是：`settings.local.json` 目前是"预留约定"——`/init` 已为其预留了 `.gitignore` 条目和文档位置，但 `loadEffectiveSettings` 尚未读取该文件。若你手动创建它，当前版本不会生效；项目级覆盖现阶段请使用 `.mcp.json`（MCP 服务器）或 MINI.md 指令文件（行为约定）。\n\nSources: [init.ts](src/init.ts#L34), [init.ts](src/init.ts#L159-L219), [init.ts](src/init.ts#L302-L319), [config.ts](src/config.ts#L173-L188)\n\n## 常用操作命令速查\n\n配置不需要总是手改 JSON，MiniCode 提供了管理子命令与斜杠命令两条路径：\n\n| 操作 | 命令 | 写入位置 |\n|---|---|---|\n| 查看 MCP 服务器 | `minicode mcp list` / `minicode mcp list --project` | — |\n| 添加本地服务器 | `minicode mcp add <name> -- <command> [args...]` | 用户级或项目级 mcp.json |\n| 添加远程服务器 | `minicode mcp add <name> --protocol streamable-http --url <endpoint> [--header K=V ...]` | 同上 |\n| 保存远程令牌 | `minicode mcp login <name> --token <bearer-token>` | `~/.mini-code/mcp-tokens.json` |\n| 清除令牌 | `minicode mcp logout <name>` | 同上 |\n| 移除服务器 | `minicode mcp remove <name> [--project]` | 同上（对应 mcp.json） |\n| 查看配置路径 | 交互界面输入 `/config-paths` | — |\n| 查看当前模型与来源 | 交互界面输入 `/status` | — |\n| 持久化切换模型 | 交互界面输入 `/model <模型名>` | `~/.mini-code/settings.json` |\n| 查看 MCP 连接状态 | 交互界面输入 `/mcp` | — |\n\n`minicode mcp add` 同时支持 `--env KEY=VALUE`（可重复）与 `--protocol`，并且会在添加时校验：`--url` 与本地命令不能同时给出；`streamable-http` 协议必须搭配 `--url`。交互界面内的 `/status` 会显示 `sourceSummary`，直接告诉你当前模型来自哪一层配置。\n\nSources: [manage-cli.ts](src/manage-cli.ts#L13-L24), [manage-cli.ts](src/manage-cli.ts#L90-L206), [cli-commands.ts](src/cli-commands.ts#L199-L206), [cli-commands.ts](src/cli-commands.ts#L249-L283)\n\n## 动手实践：从零到能跑通的最小路径\n\n1. **安装并配置**：`npm install` 后运行 `npm run install-local`，按提示输入模型名、`ANTHROPIC_BASE_URL` 与 `ANTHROPIC_AUTH_TOKEN`——向导会把它们合并写入 `~/.mini-code/settings.json`，不会覆盖你已有的其它设置（它先读取现有有效设置再合并更新）。\n2. **验证**：启动 `minicode` 后输入 `/config-paths` 确认路径、`/status` 确认模型与认证来源。\n3. **添加一个项目级 MCP 服务器**：在项目根目录执行 `minicode mcp add filesystem -- npx -y @modelcontextprotocol/server-filesystem .`，然后输入 `/mcp` 查看连接状态。\n4. **目录隔离**：若你希望配置与工作区完全分离（例如便携环境），设置 `export MINI_CODE_HOME=/path/to/custom/dir` 后重新安装，所有文件将写入自定义目录。\n\n如果你希望进一步深入，建议按目录顺序阅读：[交互式界面与斜杠命令速览](4-jiao-hu-shi-jie-mian-yu-xie-gang-ming-ling-su-lan) 了解命令的完整用法，或跳到 [MCP 集成：stdio/HTTP 启动、协议协商与工具注入](22-mcp-ji-cheng-stdio-http-qi-dong-xie-yi-xie-shang-yu-gong-ju-zhu-ru) 深挖 MCP 连接内部机制；想要了解会话与项目目录的关系，可以阅读 [追加式 JSONL 事件日志与断点恢复](19-zhui-jia-shi-jsonl-shi-jian-ri-zhi-yu-duan-dian-hui-fu)。';export{n as default};

const n='IM 渠道层是 HappyClaw 连接外部即时通讯平台（飞书、Telegram、QQ、微信、钉钉、Discord、WhatsApp）的统一入口。本页聚焦两件事：**渠道抽象如何抹平七个协议差异**，以及**连接池如何在"一个用户 ↔ 多个渠道账号"的约束下管理连接生命周期**。后续的会话绑定、可靠性状态机等主题会在对应页面展开。\n\n## 从单连接走向多账号连接池\n\n早期 HappyClaw 的 IM 接入是"每用户每渠道一个连接"的单例模型：用户通过旧版配置接口（`/api/config/user-im/:provider`）填写一组凭据，系统为每个 provider 维护一个连接。这种模型有两个结构性缺陷：**无法在同一渠道下同时挂载多个机器人/账号**（例如一个飞书应用做群聊、另一个做私聊），以及**无法表达"账号"这一独立于用户的生命周期实体**（授权、启用/禁用、扫描二维码等状态无处安放）。\n\n演进的结果是引入了**一等公民的渠道账号（Channel Account）**模型：每个账号在 `channel_accounts` 表中拥有独立记录，携带自己的密钥引用（`secret_ref`）、认证状态（`auth_status`）与传输状态（`transport_status`）。旧的单例配置不再是独立连接通道，而是通过 `ensureLegacyDefaultChannelAccount` / `syncDefaultChannelAccountCredentials` 投影为"默认账号"的兼容层——**启动时先投影旧配置，之后只连接一等公民账号**，这一契约由专门的启动契约测试锁定。\n\nSources: [channel-account-migration.ts](src/channel-account-migration.ts#L21-L98)、[channel-account-startup-contract.test.ts](tests/channel-account-startup-contract.test.ts#L8-L22)、[types.ts](src/types.ts#L238-L267)\n\n## 三层架构总览\n\n整个 IM 渠道层可以拆解为三层，各层职责单一、依赖方向向下：\n\n```mermaid\ngraph TB\n    subgraph L3["账号模型层（持久化）"]\n        CA["channel_accounts 表<br/>secret_ref / auth_status / transport_status"]\n        SEC["AES-256-GCM 加密密钥文件<br/>config/channel-accounts/*.json"]\n        RG["registered_groups 表<br/>channel_account_id 绑定"]\n    end\n\n    subgraph L2["连接池管理层（im-manager.ts）"]\n        POOL["连接池<br/>Map&lt;userId, UserIMConnection&gt;"]\n        SLOT["渠道槽位<br/>channelKey = provider 或 provider + \'\\\\0\' + accountId"]\n        CLAIM["凭据互斥<br/>credentialClaims"]\n        GATE["入站门控 inboundAllowed<br/>出站门控 isOutboundConnectionAllowed"]\n    end\n\n    subgraph L1["渠道抽象层（im-channel.ts）"]\n        IF["IMChannel 统一接口<br/>connect / sendMessage / createStreamingSession"]\n        AD["适配器工厂<br/>createFeishuChannel / createTelegramChannel / ..."]\n        CONN["平台连接器<br/>feishu.ts / telegram.ts / qq.ts / ..."]\n    end\n\n    APP["业务层：turn 运行时 / 命令系统 / 队列"]\n    APP -->|"sendMessage(jid, ...)"| POOL\n    POOL --> SLOT\n    SLOT --> IF\n    AD --> CONN\n    CLAIM --> SLOT\n    GATE --> POOL\n    POOL -->|"getChannelAccount / saveSecret"| CA\n    CA --> SEC\n    RG -.->|"findChannelForJid 路由依据"| POOL\n```\n\n**渠道抽象层**（`im-channel.ts`）定义了一个与协议无关的 `IMChannel` 接口，覆盖连接建立、消息收发、文件/图片发送、流式卡片会话、群组同步、凭据级登出（`logout?`）等能力；每个平台通过适配器工厂将原生连接器包装成该接口。**连接池管理层**（`im-manager.ts`）持有"用户 → 渠道槽位映射"的内存状态，并叠加了账号持久化状态作为双重校验。**账号模型层**将账号元数据、加密密钥与群组绑定关系落库，构成连接池的事实来源。\n\nSources: [im-channel.ts](src/im-channel.ts#L1-L6)、[im-channel.ts](src/im-channel.ts#L170-L219)、[im-manager.ts](src/im-manager.ts#L1-L7)、[im-manager.ts](src/im-manager.ts#L79-L82)\n\n## JID 寻址模型：账号作用域的载体\n\n多账号连接的根基是**寻址能力**——系统必须能从一条消息的标识符中精确推导出"这是哪个渠道、哪个外部会话、属于哪个账号"。所有渠道统一使用 `provider:` 前缀格式的 JID，前缀映射表在 `shared/channel-prefixes.ts` 中定义，**主服务与 agent-runner 共享同一份映射**，保证两侧对消息来源的判定一致。\n\nJID 的完整语法为：\n\n```\nprovider:externalChatId#account:<账号ID>#thread:<话题ID>#root:<消息ID>\n```\n\n| JID 片段 | 含义 | 说明 |\n|---|---|---|\n| `provider:` | 渠道类型 | `feishu:`、`telegram:`、`qq:`、`wechat:`、`dingtalk:`、`discord:`、`whatsapp:` |\n| `externalChatId` | 平台原生会话 ID | 如飞书 `oc_*`、Telegram chat id |\n| `#account:<id>` | 渠道账号作用域 | 由 `scopeChannelJid` 注入，连接池据此选择对应 Bot |\n| `#thread:<tid>` | 平台原生话题 ID | 仅飞书等支持话题映射的渠道使用 |\n| `#root:<mid>` | 根消息 ID | 用于话题/子帖的父子定位 |\n\n`parseChannelAddress` 负责解析，`legacy` 标志标识"无账号片段的历史 JID"——这类 JID 仍然有效，由遗留默认账号（`is_legacy_default = 1`）认领。`toProviderJid` 在调用平台 SDK 前**剥离** `account:` 片段，因为该片段是 HappyClaw 内部概念，平台 API 不认识它；`extractProviderTarget` 则进一步去掉 `provider:` 前缀，得到纯平台 ID。\n\nSources: [channel-prefixes.ts](shared/channel-prefixes.ts#L1-L10)、[channel-address.ts](src/channel-address.ts#L1-L48)、[channel-address.ts](src/channel-address.ts#L54-L75)\n\n## 连接池核心机制\n\n连接池的内存在态是 `Map<userId, UserIMConnection>`，每个 `UserIMConnection` 内部再用 `Map<channelKey, IMChannel>` 承载同一用户下不同渠道、不同账号的连接。\n\n### 槽位键与凭据互斥\n\n`channelKey` 的构造规则决定了池的粒度：有账号时为 `provider + \'\\0\' + accountId`，无账号时退化为纯 `provider`。这意味着**同一用户可以在同一渠道下挂载多个账号连接，各占一个槽位**。\n\n多账号场景引入了一个现实约束：**同一个 Bot 凭据不能被两个账号连接同时占用**（例如两个渠道账号填写了同一个飞书 appId）。`claimCredential` 以 `provider + \'\\0\' + sha256(credentialIdentity)` 为键做全局互斥声明——`credentialIdentity` 对飞书是 appId、对 Telegram 是 botToken。若凭据已被其他账号声明，连接直接抛错；连接失败或断开时通过 `releaseCredentialClaim` 释放声明，避免出现"清理失败但凭据仍被占用"的死锁。\n\n### 双重锁与 sealed 用户\n\n并发安全上，连接池采用**用户级锁 + 渠道槽位锁**两级加锁：`withUserLock` 串行化同一用户的连接操作，`withChannelLock` 串行化同一槽位的操作。此外引入 **sealed（封存）标记**：`disconnectAllUserChannels`（用户禁用/删除流程）完成后将用户标为 sealed，后续任何 `connectChannel` 直接拒绝，防止禁用用户通过竞态重新连上；运维侧通过 `markUserReconnectable` 显式解除封存。代码在 connect 前、等待锁后、`channel.connect` 网络耗时后共做了**三次 sealed 复查**，因为网络 I/O 期间 disconnect-all 可能已经完成。\n\nSources: [im-manager.ts](src/im-manager.ts#L267-L310)、[im-manager.ts](src/im-manager.ts#L588-L599)、[im-manager.ts](src/im-manager.ts#L606-L724)、[im-manager.ts](src/im-manager.ts#L735-L759)\n\n### 双状态模型：认证 vs 传输\n\n每个渠道账号维护两个正交的状态机，这是理解账号生命周期的关键：\n\n| 状态维度 | 类型 | 取值 | 语义 |\n|---|---|---|---|\n| `auth_status` | 认证生命周期 | `draft` / `awaiting_scan` / `authorized` / `revoked` / `error` | 凭据是否已被平台授权（QR 会话渠道特有"待扫码"态） |\n| `transport_status` | 传输连接 | `disconnected` / `connecting` / `reconnecting` / `connected` / `error` | 实时 socket/流是否存活 |\n| `auth_mode` | 认证方式（创建时固化） | `credentials` / `bot_token` / `qr_session` | 飞书/QQ/钉钉用凭据，Telegram/Discord 用 Bot Token，微信/WhatsApp 用二维码会话 |\n\n其中 `auth_mode` 在账号创建时根据 provider 推导且不可变（见 `ensureLegacyDefaultChannelAccount` 中的分支）。微信/WhatsApp 这类 `qr_session` 渠道在连接过程中通过 `onConnectionUpdate` 回调把 QR 码状态（`qr` / `qrDataUrl`）推给前端渲染，扫码授权后持久化登录态到 authDir（WhatsApp 使用 Baileys MultiFileAuthState，目录权限收紧为 0700）。\n\nSources: [types.ts](src/types.ts#L223-L267)、[channel-account-migration.ts](src/channel-account-migration.ts#L56-L73)、[whatsapp.ts](src/whatsapp.ts#L313-L326)、[whatsapp.ts](src/whatsapp.ts#L413-L430)\n\n## 账号生命周期：从创建到启动恢复\n\n### 连接器工厂：reloadChannelAccountById\n\n账号级连接的中枢是 `reloadChannelAccountById(accountId)`——它先断开旧连接，再按"加载密钥 → 解析默认工作区 → 按 provider 分派连接器"的顺序重建连接：\n\n```mermaid\nflowchart TD\n    A["reloadChannelAccountById(accountId)"] --> B{"账号存在？"}\n    B -->|否| END1["返回 false"]\n    B -->|是| C["断开旧连接<br/>disconnectUserChannelAccount"]\n    C --> D{"enabled？"}\n    D -->|否| E["置 transport_status = disconnected"]\n    D -->|是| F["加载加密密钥<br/>loadChannelAccountSecret"]\n    F --> G{"密钥与默认工作区<br/>均可用？"}\n    G -->|否| H["置 error 状态并返回"]\n    G -->|是| I["置 connecting"]\n    I --> J{"provider 分派"}\n    J -->|feishu| K["connectUserFeishu<br/>+ onP2pSender 自动认领 owner"]\n    J -->|telegram / qq| L["connectUserTelegram/QQ<br/>+ isChatAuthorized / onPairAttempt"]\n    J -->|wechat| M["connectUserWeChat<br/>+ 持久化游标防重放"]\n    J -->|whatsapp| N["connectUserWhatsApp<br/>+ QR 状态回调"]\n    J -->|discord / dingtalk| O["connectUserDiscord / DingTalk"]\n    K & L & M & N & O --> P{"连接成功？"}\n    P -->|是| Q["置 connected<br/>触发 channelReadyListeners"]\n    P -->|否| R["清理连接 + 释放凭据声明"]\n```\n\n每个 provider 分支都会注入**账号作用域的回调包** `common`：`scopeIncomingJids` 控制入站 JID 是否打上账号片段（遗留默认账号不打，保证历史无作用域 JID 继续有效）；`onNewChat` 回调在注册群组时调用 `applyChannelAccountRegistrationFallback` 把 `channel_account_id` 回填到群组记录——注意它只在**未显式绑定**时写默认工作区，避免每条入站消息都触发写放大（该函数注释明确提到无条件 `setRegisteredGroup` 每消息约 10 条 SQL）。\n\nSources: [index.ts](src/index.ts#L19136-L19200)、[index.ts](src/index.ts#L19196-L19260)、[index.ts](src/index.ts#L19279-L19320)、[channel-account-routing.ts](src/channel-account-routing.ts#L37-L55)\n\n### 启动恢复的单一路径\n\n进程启动时，账号恢复遵循严格顺序：\n\n1. **投影遗留配置**：遍历所有用户，把旧版 provider 配置迁移为 legacy 默认账号（`ensureLegacyDefaultChannelAccount`），并为所有无账号片段的历史群组回填 `channel_account_id`；\n2. **暂停入站**：调用 `imManager.deferInbound()`，让连接建立期间到达的消息进入延迟队列而非直接处理；\n3. **并行重连**：`Promise.allSettled(listEnabledChannelAccounts().map(reloadChannelAccountById))`——所有启用的一等公民账号并行恢复，**不再有任何遗留直连路径**（启动契约测试断言源码中不存在 `connectUserIMChannels`）；\n4. **恢复可靠性状态**：先协调中断的流式卡片、再恢复 Outbox 投递（不确定投递会被阻止自动重放），最后才恢复 Agent 执行。\n\n这个顺序不是性能优化而是正确性边界：如果消息循环启动过早，同一逻辑轮次可能出现"新进程卡片与旧进程卡片并存"的双活状态。\n\nSources: [index.ts](src/index.ts#L21338-L21370)、[channel-account-startup-contract.test.ts](tests/channel-account-startup-contract.test.ts#L24-L38)\n\n## 入站门控与出站 fail-closed 路由\n\n### 入站：回调包装 + 账号片段注入\n\n连接建立时，`scopeConnectOpts` 会**包装所有入站回调**（onNewChat、onMessage、onCommand、onPairAttempt、onNewChat 等）：每个回调先过 `inboundAllowed()` 门控（入站暂停、用户封存、用户非 active、账号被禁用或不属于该用户 → 拒绝），再对 JID 施加 `normalizeIncomingJid: scope` 注入账号片段。这样连接池内部拿到的始终是**作用域化 JID**，后续路由（群组绑定、turn 注册）天然携带账号信息。\n\n### 出站：群组绑定优先，拒绝跨账号回退\n\n出站发送（`sendMessage` / `sendImage` / `sendFile`）的路径是：`getChannelType(jid)` 判定渠道 → `extractProviderTarget` 剥离内部片段 → `findChannelForJid` 定位具体连接。`findChannelForJid` 的账号解析优先级是：\n\n```\nJID 自带 account 片段  >  群组记录的 channel_account_id  >  legacy 默认账号\n```\n\n解析出账号后还必须通过 `isOutboundConnectionAllowed` 的持久化校验（用户 active + 账号 enabled），**连接池里存在 socket 本身不构成发送授权**——因为 disconnect 可能失败留下"僵尸连接"，必须用数据库状态做最终裁决。同时注释明确：同一工作区内其他 Bot **绝不**作为出站回退，防止串号。\n\n```mermaid\ngraph LR\n    J["jid 消息"] --> T["getChannelType 判渠道"]\n    T --> A{"parseChannelAddress 取<br/>account 片段"}\n    A -->|"有片段"| G["直接用账号"]\n    A -->|"无片段"| R["registered_groups<br/>channel_account_id"]\n    R -->|"无绑定"| L["legacy 默认账号"]\n    G & L --> C{"isOutboundConnectionAllowed<br/>用户 active + 账号 enabled"}\n    C -->|"通过"| S["channelKey 定位 IMChannel"]\n    C -->|"拒绝"| X["fail-closed：无连接可用"]\n```\n\nSources: [im-manager.ts](src/im-manager.ts#L312-L364)、[im-manager.ts](src/im-manager.ts#L766-L789)、[im-manager.ts](src/im-manager.ts#L1043-L1095)\n\n## 密钥管理：凭据永不落库\n\n渠道账号的元数据行只保存 `secret_ref`（形如 `channel-account:<id>`），凭据本体以 **AES-256-GCM 加密**后写入 `DATA_DIR/config/channel-accounts/<id>.json`。加密密钥位于 `config/claude-provider.key`，创建采用**原子 link(2) 发布**：临时文件写入后通过硬链接发布，多个进程竞争时后到者收到 `EEXIST` 转而读取已有密钥，杜绝密钥被覆盖。密钥文件与密文文件权限均收紧为 0600/0700。每次凭据校验（如新增账号时的 `testChannelAccountCredentials`）针对各 provider 调用不同探测端点：飞书用 tenant_access_token 探测、Telegram 调 `getMe`、QQ 调 `getAppAccessToken`，均有超时与代理支持。\n\nSources: [channel-account-secrets.ts](src/channel-account-secrets.ts#L16-L52)、[channel-account-secrets.ts](src/channel-account-secrets.ts#L76-L120)、[channel-account-connectivity.ts](src/channel-account-connectivity.ts#L17-L63)\n\n## 渠道能力矩阵\n\n不同平台协议能力差异巨大，系统通过 `IM_CHANNEL_CAPABILITIES` 声明式描述各渠道能力，前端据此渲染绑定策略选项、后端据此做能力守卫：\n\n| 渠道 | 绑定工作区 | 绑定会话 | 话题映射 | 流式更新 | 文件发送 | 认证方式 |\n|---|---|---|---|---|---|---|\n| 飞书 | ✅ | ✅ | ✅ | ✅ | ✅ | credentials |\n| 钉钉 | ✅ | ✅ | ❌ | ✅ | ✅ | credentials |\n| Telegram | ✅ | ✅ | ✅ | ❌ | ✅ | bot_token |\n| QQ | ✅ | ✅ | ❌ | ✅ | ✅ | credentials |\n| 微信 | ✅ | ✅ | ❌ | ❌ | ❌ | qr_session |\n| Discord | ✅ | ✅ | ❌ | ✅ | ✅ | bot_token |\n| WhatsApp | ✅ | ✅ | ❌ | ❌ | ✅ | qr_session |\n\n渠道无关的会话分类（`resolveChannelConversationKind`）将外部会话归为 `direct` / `group` / `unknown` 三类，作为绑定策略的输入——未知类型**刻意 fail-closed**，例如飞书用同一 `oc_*` 标识符表示私聊与群聊，绝不能仅凭 JID 猜测会话类型，必须以持久化/实时元数据为准。\n\nSources: [im-channel-capabilities.ts](src/im-channel-capabilities.ts#L1-L80)、[channel-conversation-kind.ts](src/channel-conversation-kind.ts#L1-L74)\n\n## 小结\n\nIM 渠道架构的本质是**用统一接口吸收协议差异，用账号作用域支撑多连接，用持久化状态兜底内存态**。三个设计决策值得记住：其一，JID 是贯穿全链路的路由载体，`account:` 片段让"多账号"在寻址层面显式化；其二，连接池的每处授权都以数据库为准（fail-closed），socket 存在不等于发送合法；其三，账号的认证态与传输态分离，使 QR 扫码类渠道能够表达"已授权但未连接"的中间状态。\n\n建议继续阅读：[渠道挂载与会话绑定边界](14-qu-dao-gua-zai-yu-hui-hua-bang-ding-bian-jie)（群组如何绑定到账号与工作区）、[渠道可靠性状态机：Inbox、Turn 与 Outbox](16-qu-dao-ke-kao-xing-zhuang-tai-ji-inbox-turn-yu-outbox)（连接之上的投递保障）、[IM 命令系统与消息分发](17-im-ming-ling-xi-tong-yu-xiao-xi-fen-fa)（入站消息进入命令与 Agent 处理管道后的流向）。';export{n as default};

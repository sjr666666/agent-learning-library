const e='本篇是 HappyClaw 开发者的"命令速查手册"。它回答三个问题：**如何启动开发环境**、**哪些命令负责质量把关**、**CI 会替你验证什么**。阅读前提是已完成 [快速开始：环境要求与一键启动](2-kuai-su-kai-shi-huan-jing-yao-qiu-yu-jian-qi-dong) 中的环境准备（Node.js 20+、npm、GNU Make、可选 Docker）。\n\n## 工具链铁律：只用 Node，不用 Bun\n\n项目的第一条工程约束写在 Makefile 开头：**运行时只使用原生 Node 工具链（npm / npx / tsx / node），不使用 bun**。这不是风格偏好，而是技术硬约束——主服务的 WebSocket 走 `ws` 包 + `@hono/node-server` 的 `server.on(\'upgrade\')` 握手，该模式在 bun 的 HTTP server 下不会触发，会导致所有 WebSocket 握手失败：HTTP/接口正常，但前端实时流式卡片与通知全部失效。理解了这一点，你就明白为什么所有命令都围绕 `npm`、`npx`、`tsx` 组织。\n\n另一个关键变量是默认端口：`PORT` 默认取 `WEB_PORT` 环境变量，否则为 `3000`，并会导出为 `WEB_PORT` 供子进程继承。运行时数据目录默认是 `data/`，Agent 容器镜像默认是 `riba2534/happyclaw-agent:latest`，这些都可以通过环境变量覆盖。\n\nSources: [Makefile](Makefile#L7-L20)\n\n## 开发模式：一键启动前后端\n\n### 顶层架构\n\n开发环境由三个进程组成：后端主服务（Node + tsx 直跑 TypeScript）、Vite 前端开发服务器、以及承载 Agent 执行的 Runner（Host 模式下是主服务内的子进程，Container 模式下是 Docker 容器）。\n\n```mermaid\nflowchart LR\n    subgraph Browser["浏览器"]\n        UI["Vite 前端<br/>localhost:5173"]\n    end\n\n    subgraph Backend["后端主服务 (Node)"]\n        API["Hono API + WebSocket<br/>localhost:3000"]\n        RUNNER["tsx src/index.ts<br/>tsx 直跑 TS，免编译"]\n    end\n\n    subgraph AgentExec["Agent 执行"]\n        HOST["Host Runner<br/>宿主 Node 进程"]\n        CONT["Container Runner<br/>Docker 沙箱"]\n    end\n\n    UI -- "/api 代理<br/>/ws 代理 (ws: true)" --> API\n    API -- "调度与 IPC" --> HOST\n    API -- "容器编排" --> CONT\n```\n\n前端通过 Vite 代理把 `/api` 转发到后端 3000 端口、`/ws` 以 WebSocket 模式转发到同一目标；后端在 3000 端口同时暴露 REST API 与 WebSocket。开发模式下前端地址是 <http://localhost:5173>，API/WebSocket 是 <http://localhost:3000>。\n\nSources: [Makefile](Makefile#L22-L36) · [README.md](README.md#L247-L256) · [web/vite.config.ts](web/vite.config.ts#L20-L39)\n\n### `make dev` 启动流程\n\n`make dev` 不是简单执行一条命令，而是一个带自愈能力的引导流程：先检查三端依赖是否需要重装，再物化内置 Skills、拉取 Agent 镜像、编译 agent-runner，最后才并发启动前后端。\n\n```mermaid\nflowchart TD\n    A["make dev"] --> B{"依赖过期？<br/>package.json / lockfile 比 node_modules 新"}\n    B -- "是" --> C["npm ci 三端安装<br/>（根 / web / agent-runner）"]\n    B -- "否" --> D["_ensure-builtin-skills<br/>校验 builtin-skills catalog"]\n    C --> D\n    D --> E["_ensure-docker-image<br/>docker pull 最新 Agent 镜像"]\n    E --> F["编译 agent-runner<br/>npm --prefix container/agent-runner run build"]\n    F --> G["npm run dev:all<br/>concurrently 并发启动"]\n    G --> H["backend: tsx src/index.ts<br/>frontend: vite dev"]\n```\n\n依赖过期判定非常精细：`node_modules` 不存在、或 `package.json`、`package-lock.json` 比 `node_modules` 新，都会触发重装。前端与 agent-runner 的依赖独立判定，互不影响。启动后由 `concurrently` 以 `backend,frontend` 两个命名进程并发运行，带时间戳日志前缀。\n\n如果你只想启动单端，`make dev-backend` 用 `tsx src/index.ts` 直跑后端（免编译、热重启友好），`make dev-web` 单独启动 Vite。\n\nSources: [Makefile](Makefile#L22-L36) · [package.json](package.json#L10-L24)\n\n## 常用命令速查表\n\n以下是 Makefile 暴露的全部面向开发者的命令。建议先跑一次 `make help`，它会用 `##` 注释自动生成同样的清单。\n\n| 命令 | 作用 | 关键行为 |\n| --- | --- | --- |\n| `make install` | 安装全部依赖并编译 agent-runner | 根/Web/agent-runner 三处 `npm ci`，修复 node-pty spawn-helper 权限，物化内置 Skills |\n| `make dev` | 启动开发环境（前后端） | 自动补依赖、拉镜像、编译 runner，`concurrently` 双进程 |\n| `make dev-backend` | 仅启动后端 | `tsx` 直跑 TS，不编译 |\n| `make dev-web` | 仅启动前端 | Vite dev server（5173） |\n| `make start` | 生产模式前台启动 | 端口占用检查 + 增量编译 + `node dist/index.js` |\n| `make build` | 编译三端 | 先 `sync-types` 再并发构建 |\n| `make typecheck` | 全量类型检查 | 三端 tsc + 共享类型同步校验 + Prompt 引用校验 + 文档链接校验 |\n| `make test` | 运行单元测试 | Vitest 全量运行 |\n| `make format` | 格式化代码 | Prettier 写回 `src/**/*.ts` |\n| `make format-check` | 检查改动文件格式 | 基于 Git diff 的增量检查 |\n| `make sync-types` | 同步 shared/ 类型到三端 | 内容不同才复制，避免无谓触发增量构建 |\n| `make docker-pull` | 拉取 Agent 镜像 | 默认 `riba2534/happyclaw-agent:latest` |\n| `make install-host-tools` | 安装宿主工具 + 刷新内置 Skills | 固定版本 Skills 物化到 `data/builtin-skills/` |\n| `make update-sdk` | 显式升级 Claude Agent SDK/CLI | 写回 lockfile 后需自行验证 |\n| `make ensure-latest-sdk` | 只读检查 SDK 版本 | 不写文件，兼容旧工作流 |\n| `make backup` | 一致性备份 | SQLite 快照 + 目录归档，拒绝链接/硬链接 |\n| `make restore FILE=xxx.tar.gz` | 恢复备份 | 服务必须已停止，覆盖前需确认 |\n| `make clean` | 清理构建产物 | 删除三个 dist 目录 |\n| `make reset-init` | 完全重置为首装状态 | **危险**：删除 data/store/groups |\n| `make logs` | 实时查看日志 | 依赖 `make start > /tmp/happyclaw.log 2>&1 &` 后台运行 |\n| `make stop` | 停止 3000 端口服务 | 按端口 kill |\n| `make status` | 查看运行状态 | 端口、健康检查、日志、Docker 容器一览 |\n| `make help` | 显示帮助 | 自动从 `##` 注释生成 |\n\nSources: [Makefile](Makefile#L216-L331) · [README.md](README.md#L258-L270)\n\n## 生产启动与增量构建机制\n\n`make start` 是"一键生产模式"，与开发模式的关键区别是：它运行**编译后**的 `dist/index.js`（Node 直跑），并且**不会隐式改写依赖图**——SDK 升级必须显式执行 `make update-sdk`。启动前它会检查端口占用（占用则报错并提示先 `make stop`），然后依次执行四个哨兵检查：\n\n```mermaid\nflowchart TD\n    A["make start"] --> B{"端口 3000 被占用？"}\n    B -- "是" --> X["报错退出，提示 make stop"]\n    B -- "否" --> C["_ensure-builtin-skills"]\n    C --> D["_check-sync<br/>shared/ 类型有变更？"]\n    D -- "有" --> E["make sync-types 同步"]\n    D -- "无" --> F["_build-backend-if-stale<br/>src/ 比 dist/index.js 新？"]\n    E --> F\n    F --> G["_build-web-if-stale<br/>web/src 或配置变更？"]\n    G --> H["_build-ar-if-stale<br/>agent-runner 变更？"]\n    H --> I["node dist/index.js"]\n```\n\n增量判定依赖"时间戳哨兵"：后端以 `dist/index.js` 为基准、前端以 `web/dist/index.html` 为基准、agent-runner 以 `dist/.tsbuildinfo` 为基准，任何源文件或配置文件比基准新就重新编译，否则跳过。这套机制让 `make start` 既能保证产物新鲜，又不会每次全量构建。\n\nSources: [Makefile](Makefile#L50-L69) · [Makefile](Makefile#L88-L119)\n\n## 质量检查：typecheck、format 与 test\n\n### 三层 typecheck\n\n`make typecheck` 是质量把关的第一道门，它串行执行五个检查：\n\n1. **`sync-types`**：先同步 shared/ 下的类型副本；\n2. **三端 `tsc --noEmit`**：根目录（后端）、`web/`、`container/agent-runner/` 各自独立类型检查；\n3. **`check-stream-event-sync.sh`**：校验 3 份 StreamEvent 类型副本、2 份 image-detector、2 份 channel-prefixes 与 `shared/` 源完全一致（内容级 diff，不是时间戳）；\n4. **`check-agent-runner-prompts.sh`**：校验源码中引用的 `.md` Prompt 文件真实存在——因为 agent-runner 在模块加载时同步 `readFileSync` 读取 Prompt，缺文件会导致容器启动即崩溃（`ENOENT`），这个检查把失败提前到 typecheck 阶段；\n5. **`docs:check`**：校验仓库内 Markdown 文档的所有本地链接不悬空。\n\nSources: [Makefile](Makefile#L148-L162) · [scripts/check-stream-event-sync.sh](scripts/check-stream-event-sync.sh#L1-L50) · [scripts/check-agent-runner-prompts.sh](scripts/check-agent-runner-prompts.sh#L1-L22)\n\n### 格式检查只针对改动文件\n\n`make format-check` 走 `scripts/check-format-changed.mjs`，它**只检查本次 Git 改动涉及的格式化文件**（基于 `merge-base` 的 diff，包含已暂存、未暂存和未跟踪文件），而不是全仓库扫描。CI 中通过 `FORMAT_BASE_REF` 环境变量指定对比基准（PR 的 base sha 或 push 前的 commit）。`make format` 则是全量 Prettier 写回，`format:changed --write` 只格式化改动文件。\n\nSources: [Makefile](Makefile#L167-L171) · [scripts/check-format-changed.mjs](scripts/check-format-changed.mjs#L1-L50) · [.github/workflows/ci.yml](.github/workflows/ci.yml#L39-L42)\n\n### 测试与自检\n\n`make test` 运行 Vitest 全量单元测试。测试发现范围由 `vitest.config.ts` 控制：它显式排除了 `data/**`——因为 `data/` 存放用户的 Agent 工作区（已被 gitignore），其中可能嵌套自带测试套件的项目，而 Vitest 不尊重 `.gitignore`，必须显式排除。Web 的端到端测试（`web/tests/e2e`）也被排除在根测试之外，由独立的 Playwright 配置运行。\n\n除了单元测试，还有两条特殊的验证命令：\n\n| 命令 | 作用 |\n| --- | --- |\n| `npm run self-test:agent-runner` | Agent Runner 自检（不依赖真实模型，回放历史 incident 验证后台任务汇总判定逻辑） |\n| `npm run test:real-model` | 真实模型连通性冒烟：向已配置的 Provider 发一条固定指令，校验返回完全匹配，只输出布尔证据，绝不打印凭据 |\n| `npm run reset:admin -- <username> <new_password>` | 重置管理员账户（密码至少 8 位），同步清除该用户全部会话 |\n\nSources: [vitest.config.ts](vitest.config.ts#L1-L33) · [package.json](package.json#L11-L21) · [scripts/self-test-agent-runner.ts](scripts/self-test-agent-runner.ts#L1-L30) · [scripts/test-real-model-smoke.ts](scripts/test-real-model-smoke.ts#L1-L40) · [src/reset-admin.ts](src/reset-admin.ts#L1-L20)\n\n## 共享类型同步机制\n\n项目存在**三处 StreamEvent 类型副本**（`src/stream-event.types.ts`、`web/src/stream-event.types.ts`、`container/agent-runner/src/stream-event.types.ts`）和两处 image-detector / channel-prefixes 副本，全部以 `shared/` 为唯一真相源。`make sync-types` 执行 `scripts/sync-stream-event.sh`，**仅在内容不同时才复制**——这是刻意设计：避免无谓的时间戳变化触发冗余的增量构建。而 `check-stream-event-sync.sh` 在 typecheck 阶段用内容级 diff 反向校验副本未漂移，CI 中还会额外用 `git diff --exit-code` 确认同步后没有产生未提交的变更。\n\nSources: [Makefile](Makefile#L185-L188) · [scripts/sync-stream-event.sh](scripts/sync-stream-event.sh#L1-L46) · [.github/workflows/ci.yml](.github/workflows/ci.yml#L44-L48)\n\n## SDK 升级策略：显式更新，禁止隐式漂移\n\nClaude Agent SDK 与 Claude Code CLI 的版本由三处 lockfile 共同锁定（根、agent-runner、以及 agent-runner 内的 CLI 依赖）。`make start` 的生产启动**故意不触碰依赖图**，防止隐式升级引入未验证的运行时变化。升级必须走显式流程：\n\n1. `make ensure-latest-sdk`（只读）查看本地版本与最新版本的差距；\n2. 确认后执行 `make update-sdk`：用 `--save-exact` 把 agent-runner 与主服务的 SDK/CLI 精确钉到最新版本，并重新编译 agent-runner；\n3. 运行 `make typecheck && make test` 验证，通过后再提交 package.json 与 lockfile。\n\n这个"显式升级、验证后提交"的纪律，与 CI 的可复现安装（`npm ci`）相互配合，保证任何一次构建的依赖版本都可追溯。\n\nSources: [Makefile](Makefile#L190-L214) · [Makefile](Makefile#L52-L54)\n\n## 数据维护：备份、恢复与重置\n\n| 命令 | 安全边界 |\n| --- | --- |\n| `make backup` | 先对 SQLite 做一致性快照（`sqlite-snapshot.mjs`），再复制目录；**拒绝包含符号链接、非常规文件或硬链接**的数据，避免产出不可恢复的备份；自动剔除 groups 下的 logs 目录；生成 manifest |\n| `make restore FILE=xxx.tar.gz` | 启动前先做 `assert-port-free` 检查，**服务运行中拒绝覆盖数据库**；目标目录已有数据时必须输入 `y` 确认；恢复后提示缺少 `session-secret.key` 则需重新登录 |\n| `make reset-init` | 直接删除 `data/ store/ groups/`，回到首装状态——**不可恢复，仅适合开发环境** |\n| `make clean` | 仅清理构建产物（三个 dist 目录），不动运行时数据 |\n\n关键原则：**不要在服务运行时直接复制 SQLite 文件**，一致性备份必须走 `make backup`。备份/恢复的完整细节见 [数据目录结构与一致性备份恢复](23-shu-ju-mu-lu-jie-gou-yu-zhi-xing-bei-fen-hui-fu) 与 [部署与运维：环境变量、Docker 镜像与备份恢复](5-bu-shu-yu-yun-wei-huan-jing-bian-liang-docker-jing-xiang-yu-bei-fen-hui-fu)。\n\nSources: [Makefile](Makefile#L232-L322)\n\n## CI 合并门槛：提交前先本地过一遍\n\nCI 流水线（`.github/workflows/ci.yml`）在每个 PR 和 main 分支 push 时执行 7 步验证。**这 7 步就是你的提交门槛**——本地全部通过后再提交，能避免大部分 CI 返工：\n\n| CI 步骤 | 本地等价命令 |\n| --- | --- |\n| 1. 三端 `npm ci` 可复现安装 | `make install` |\n| 2. 改动文件格式检查 | `make format-check` |\n| 3. 共享事件类型一致性检查 | `make sync-types` + `git diff --exit-code` |\n| 4. 三端类型检查 | `make typecheck` |\n| 5. 全量 Vitest 测试 | `make test` |\n| 6. 三端生产构建 | `npm run build:all` |\n| 7. Agent Runner 自检 + Web E2E | `npm run self-test:agent-runner`、`npm --prefix web run test:e2e` |\n\n`npm run self-test` 是本地一键合并检查：先 `build:all`，再 `self-test:agent-runner`，最后全量测试（`--run` 单次运行模式）。CI 使用 Node 24 与 Playwright Chromium，建议本地也用相近版本（项目引擎要求 Node ≥ 20）。\n\nAgent 镜像本身不在 CI 的 validate 任务中构建——`docker-publish.yml` 独立负责：在 main 分支构建 amd64/arm64 双平台镜像、通过真实 CDP HTTP 探测冒烟、Cosign 签名后 promote 到 `:latest`。**用户机器从不执行 `docker build`**，`make dev` 和 `make start` 都只是拉取已发布镜像。\n\nSources: [.github/workflows/ci.yml](.github/workflows/ci.yml#L1-L64) · [.github/workflows/docker-publish.yml](.github/workflows/docker-publish.yml#L1-L73) · [README.md](README.md#L432-L443) · [README.md](README.md#L272-L274)\n\n## 下一步阅读\n\n- 想理解开发模式下三个进程各自的职责边界，先读 [主服务架构与核心模块划分](9-zhu-fu-wu-jia-gou-yu-he-xin-mo-kuai-hua-fen)，再看 [Host 与 Container 双执行模式](7-host-yu-container-shuang-zhi-xing-mo-shi)\n- 首次启动后的配置操作（管理员、Provider、渠道）见 [首次配置向导：管理员、模型提供商与渠道接入](4-shou-ci-pei-zhi-xiang-dao-guan-li-yuan-mo-xing-ti-gong-shang-yu-qu-dao-jie-ru)\n- 想深入测试体系与 CI 工程规范，分别阅读 [测试体系：单元测试、契约测试与真实模型冒烟](29-ce-shi-ti-xi-dan-yuan-ce-shi-qi-yue-ce-shi-yu-zhen-shi-mo-xing-mou-yan) 和 [CI 流水线与工程规范](30-ci-liu-shui-xian-yu-gong-cheng-gui-fan)\n- 生产部署的环境变量与镜像管理见 [部署与运维：环境变量、Docker 镜像与备份恢复](5-bu-shu-yu-yun-wei-huan-jing-bian-liang-docker-jing-xiang-yu-bei-fen-hui-fu)';export{e as default};

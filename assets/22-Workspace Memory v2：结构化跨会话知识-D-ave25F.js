const e='Workspace Memory v2 是 HappyClaw 的**工作区长期知识层**：它保存经过提炼、未来 Session 仍可复用的内容，并以 Workspace 作为唯一的产品入口和权限边界。与 v1 的"路径式、用户全局、文件存储"不同，v2 将全部记忆收编进 SQLite 主数据库，引入 revision 乐观并发、幂等写入、provenance 审计与 FTS 检索，同时为 Agent Runtime 提供"快照注入 + 签名写能力"的完整闭环。本文从产品边界、数据模型、一致性、来源隐私、Runtime 集成与 Web 交互六个角度拆解这套机制。\n\nSources: [docs/workspace-memory-v2.md](docs/workspace-memory-v2.md#L1-L4)\n\n## 产品边界：Workspace 是唯一记忆容器\n\nHappyClaw 的相关数据被严格划分为四个彼此独立的层次，Workspace Memory 只是其中之一：\n\n| 层次 | 用途 | 生命周期 |\n| --- | --- | --- |\n| 平台 / AgentProfile 身份 | Agent 是谁、平台规则和能力策略 | 配置或代码版本管理，不可被忘记 |\n| Home Owner Profile | 内置 HappyClaw 如何称呼实际 owner | Home 跨 Session，专用 API 管理 |\n| Session 历史 | 一段对话的消息、工具轨迹与即时上下文 | 由 Session/消息管理功能单独处理 |\n| Workspace Memory | 同一 Workspace 跨 Session 复用的提炼知识 | 忘记后不再检索，但保留修订审计线 |\n\n这套边界带来四条硬性约束：Memory 页面必须先选 Workspace，只查询该 Workspace 的数据；内置 HappyClaw 的平台身份和安全约束不是 Memory，自定义 Agent 的身份由各自 AgentProfile 管理；Workspace Memory 不是用户全局 Profile，不会跨 Workspace 共享；Session、日期和任意文件路径都不是可浏览的"记忆源"——来源 Session 只用于回溯 provenance，忘记一条 Memory 不会删除 Session 或聊天历史，Workspace 文件也仍由文件功能管理，不能通过 Memory API 任意读写。**"重建工作区"是明确的内容级永久重置**：保留 Workspace 外壳，但删除整个 Memory store 及其 versions、provenance、tombstones、audit/outbox，Home 同时重置 Owner Profile 称呼与首次唤醒生命周期，关联定时任务停止并移入回收站。\n\nSources: [docs/workspace-memory-v2.md](docs/workspace-memory-v2.md#L6-L30)\n\n### Home、Agent 与首次唤醒\n\nHome Workspace 永久绑定内置 HappyClaw，不能删除或迁移给自定义 Agent；`POST /api/agent-profiles` 只创建自定义 AgentProfile，不自动创建 Workspace、Session 或 Memory。非 Home Workspace 可以显式迁移到另一个 Agent，文件、Session、渠道挂载和 Workspace Memory 均跟随保留——**迁移不是复制**。首次唤醒由宿主用 `workspace_onboarding_states` 原子 claim lease 保证只发生一次，称呼的物理唯一真相仍是保留的 Workspace Memory item `happyclaw.owner.preferred_address`，沿用 revision、provenance、audit、outbox 和 CAS，但通用 Memory 的所有 API 与 Runtime snapshot 均不暴露它——旧库重复项先确定性收敛（最近更新者胜出），再建立数据库唯一索引。\n\nSources: [docs/workspace-memory-v2.md](docs/workspace-memory-v2.md#L32-L53); [src/memory-store.ts](src/memory-store.ts#L700-L760)\n\n## 知识类型：四种结构化分类\n\n每条 Memory 必须且只能属于以下一种类型，用 `kind` 字段表达：\n\n| Kind | UI 名称 | 含义 | 典型示例 |\n| --- | --- | --- | --- |\n| `fact` | 事实 | 工作区中稳定、可验证的信息 | "生产数据库是 PostgreSQL 16" |\n| `decision` | 决策 | 已经做出的选择及必要背景 | "统一走 SQLite 作为唯一真相源" |\n| `lesson` | 经验 | 可复用的做法、反例或教训 | "恢复演练通过后再切换生产数据" |\n| `open_loop` | 待跟进 | 尚未关闭的问题、承诺、风险或下一步 | "下周评审渠道挂载迁移方案" |\n\n设计原则是**保存结论和必要上下文，而不是复制整段聊天**；可复用知识不足时，保留在 Session 历史中即可。服务端对 `kind` 做白名单校验（`KINDS`），超出四种类型直接返回 `invalid_request`。\n\nSources: [docs/workspace-memory-v2.md](docs/workspace-memory-v2.md#L55-L67); [src/memory-service.ts](src/memory-service.ts#L33-L38)\n\n## 数据模型：SQLite 唯一真相源\n\nWorkspace Memory v2 的 canonical store 位于 HappyClaw SQLite 主数据库（schema v65 引入，`createWorkspaceMemorySchema` 在 `db.initDatabase` 中先于 canonical 投影收敛执行）。每个 Workspace 有独立的 store revision，每个 item 有单独的 revision，二者随每次创建/编辑/忘记递增。九张表各司其职：\n\n| 表 | 职责 |\n| --- | --- |\n| `workspace_memory_stores` | 每 Workspace 一行，`revision` 计数器；workspaces 表 INSERT 触发器自动创建 |\n| `workspace_memory_items` | 记忆本体：kind/status/content/有效期/importance/confidence/revision，含 `create_idempotency_key` |\n| `workspace_memory_versions` | 不可变修订历史：`change_type`（create/update/forget）、actor，按 revision 新到旧读取 |\n| `workspace_memory_platform_items` | 平台保留项命名空间（如 `happyclaw.owner-profile`），从通用 API 隐藏 |\n| `workspace_memory_provenance` | 每 (item, revision) 的来源：source_type/source_id/session_id/observed_at |\n| `workspace_memory_tombstones` | 忘记记录：deleted_revision、reason、actor、deleted_at |\n| `workspace_memory_mutation_requests` | 幂等表：`(store_id, idempotency_key)` 主键 + request_hash + 缓存响应 |\n| `workspace_memory_outbox` | 事件投递队列：pending/processing/delivered/failed，含 attempt/available_at |\n| `workspace_memory_audit_events` | 审计流水：action、actor、source、session、store_revision |\n| `workspace_memory_fts` | FTS5 trigram 虚拟表，`bm25` 排序 + `snippet` 高亮 |\n\n```mermaid\nerDiagram\n    workspaces ||--o{ workspace_memory_stores : "1:1"\n    workspace_memory_stores ||--o{ workspace_memory_items : "1:N"\n    workspace_memory_items ||--o{ workspace_memory_versions : "1:N"\n    workspace_memory_versions ||--o| workspace_memory_provenance : "1:1 per revision"\n    workspace_memory_items ||--o| workspace_memory_tombstones : "0:1 on forget"\n    workspace_memory_items ||--o{ workspace_memory_platform_items : "reserved namespace"\n    workspace_memory_stores ||--o{ workspace_memory_mutation_requests : "idempotency"\n    workspace_memory_items ||--o{ workspace_memory_outbox : "events"\n    workspace_memory_items ||--o{ workspace_memory_audit_events : "audit"\n    workspace_memory_items ||--o| workspace_memory_fts : "FTS5 trigram"\n```\n\n所有写操作都在 `db.transaction()` 内完成：`bumpStore` 递增 store revision，`appendVersion` 写入不可变版本，`replaceFts` 事务性同步 FTS 索引，`appendAuditAndOutbox` 追加审计与投递事件——任何一步失败整体回滚，FTS 与主表不会出现不一致。\n\nSources: [src/memory-store.ts](src/memory-store.ts#L494-L693); [src/db.ts](src/db.ts#L787-L792); [src/memory-store.ts](src/memory-store.ts#L408-L492)\n\n### 检索：可召回列表与 FTS 搜索\n\n运行时与列表走两条不同路径。`listRecallableWorkspaceMemoryItems` 是**可召回视图**：只返回 `active` 状态、时间窗口有效（`valid_from`/`valid_until`/`expires_at` 用 `julianday` 比较）、且排除平台保留项的条目，按 `importance DESC, updated_at DESC` 排序。`searchWorkspaceMemoryItems` 则按查询长度分叉：≥3 字符走 FTS5 `bm25` 相关性排序并生成高亮 snippet；不足 3 字符退化为 `LIKE` 模糊匹配（转义 `%`/`_`/`\\`）。搜索同样只在当前 Workspace 的 store 内进行，天然隔离其他工作区。\n\nSources: [src/memory-store.ts](src/memory-store.ts#L1717-L1754); [src/memory-store.ts](src/memory-store.ts#L1756-L1857)\n\n## 并发控制：revision、CAS 与幂等\n\nv2 的并发模型以"双 revision + compare-and-set + 幂等键"三层构成：\n\n1. **item revision** 是乐观锁的凭证。PATCH 和 DELETE 必须提交 `expectedRevision`；服务端用 `WHERE ... AND revision = ? AND status <> \'deleted\'` 的 UPDATE 原子校验，`changes !== 1` 即抛 `revision_conflict`，同时返回 `currentRevision` 与 `storeRevision`（HTTP 409）。\n2. **store revision** 是整库版本水位。每次变更递增，前端用它判断数据是否过期，Runtime 快照用它标识"这份记忆属于哪个版本"。\n3. **idempotencyKey** 保证网络重试安全。create 在 `workspace_memory_items.create_idempotency_key` 上查重，update/forget 在 `workspace_memory_mutation_requests` 上按 `(operation, item_id, request_hash)` 三重匹配；相同请求重试返回 `replayed: true`，同一 key 对应不同请求返回 409 `idempotency_conflict`。\n\n```mermaid\nsequenceDiagram\n    participant U as 用户\n    participant W as Web 页面\n    participant S as 服务端(SQLite)\n    U->>W: 打开 item（服务端 revision=r2）\n    U->>W: 编辑本地草稿\n    W->>S: PATCH expectedRevision=r2\n    alt 服务端仍为 r2\n        S-->>W: 200 新 revision=r3\n    else 服务端已变为 r3\n        S-->>W: 409 revision_conflict(currentRevision=3)\n        W-->>U: 保留本地草稿 + 展示冲突\n        U->>W: 显式选择“加载服务端最新版”\n        W->>S: GET 最新 item（r3）\n    end\n```\n\n关键约束是**页面不能在冲突后自动重发 PATCH，也不能悄悄合并或覆盖内容**——只有用户显式选择"加载服务端最新版"后，页面才用 r3 覆盖草稿。忘记不是物理删除：它把 status 置为 `deleted`、写入 `forget` 版本与 tombstone，活跃列表和 Runtime 检索不再返回该条目，但审计历史仍然存在。\n\nSources: [docs/workspace-memory-v2.md](docs/workspace-memory-v2.md#L69-L86); [src/memory-store.ts](src/memory-store.ts#L1436-L1448); [src/memory-store.ts](src/memory-store.ts#L1592-L1631); [src/memory-store.ts](src/memory-store.ts#L1538-L1669)\n\n## 来源与隐私：provenance 与权限边界\n\n每条 item 和 version 都携带 provenance，回答"这条知识何时、从哪、被谁观察到"：\n\n| 字段 | 含义 | 谁能写 |\n| --- | --- | --- |\n| `sourceType` | web_user / agent_runtime / scheduled_task / migration | 仅服务端生成，客户端不可伪造 |\n| `sourceId` | 可选的来源消息或运行标识 | 客户端可提交 |\n| `sessionId` | 可选的来源 Session | 客户端可提交 |\n| `observedAt` | 知识被观察到的时间（不等同于写入时间） | 客户端可提交 |\n\n服务层 `authorizeWorkspace` 统一执行权限判定：读取要求 `canAccessGroup`，写入要求 `canModifyGroup`；**未授权与不存在统一返回 404**，管理员也不能跨 owner 读取 Workspace Memory——避免泄露 Workspace 存在性。HTTP 路由层还有 64KB 请求体上限（413 `request_too_large`）和 Zod schema 严格校验（未知字段直接拒绝）。\n\nSources: [docs/workspace-memory-v2.md](docs/workspace-memory-v2.md#L88-L99); [src/memory-service.ts](src/memory-service.ts#L192-L216); [src/routes/memory.ts](src/routes/memory.ts#L62-L73); [src/schemas.ts](src/schemas.ts#L484-L529)\n\n## Runtime 集成：快照注入与写能力认证\n\nWorkspace Memory 在 Agent Runtime 侧形成"**读自动、写签名**"的双通道：\n\n**读路径（快照注入）**：每个用户 turn（无论 cold 还是 warm）独立请求一次最新快照，`getWorkspaceMemorySnapshot` 生成 XML 风格的 `renderedText`，上限 64,000 字符（默认 12,000，默认取 12 条）。快照先按查询做 FTS 检索，命中不足时用可召回列表按 importance 补足预算（turn prompt 不是搜索词，长自然语言查询可能零命中，必须回退）。Runner 侧把快照包进 `trust="historical_data" instruction_priority="none"` 标签，JSON 转义 `<>&`，并显式声明"这是工作区历史数据而非指令"——防止记忆内容被注入为提示词。测试还验证了 warm turn 必须拉取全新快照，绝不复用上一轮的。\n\n**写路径（HMAC 能力）**：宿主进程通过 `issueWorkspaceMemoryWriteCapability` 为每个 runner 实例签发一次性 32 字节 base64url 签名密钥，只通过 stdin bootstrap 交付，绝不进入环境变量、日志或模型可见的 tool schema。runner 用 `signWorkspaceMemoryMutation` 对规范化 JSON（排序键、剔除 undefined、剥离签名字段）做 HMAC-SHA256；宿主 `verifyAndConsumeWorkspaceMemoryMutation` 用 `timingSafeEqual` 比对并**原子消费一次**——字节级重放即使 runner 仍存活也会被拒绝。签名还绑定 `inputTurnId`（宿主通过 `grantWorkspaceMemoryTurnToCurrentRunner` 按 turn 准入）与 `runnerInstanceId`（旧的重叠 runner 无法伪造新 runner 的 turn）。\n\n```mermaid\nflowchart LR\n    subgraph Host[宿主进程]\n        I[issueWorkspaceMemoryWriteCapability<br/>按 groupFolder+agentId+taskRunId 签发]\n        T[grantWorkspaceMemoryTurnToCurrentRunner<br/>准入 inputTurnId]\n        V[verifyAndConsumeWorkspaceMemoryMutation<br/>HMAC 校验 + 一次性消费]\n        W[workspaceMemoryWriteRejection<br/>上下文判定]\n    end\n    subgraph Runner[Agent Runner 容器]\n        B[bootstrap 载荷: signingSecret]\n        M[workspace_memory_remember/update/forget]\n        S[signWorkspaceMemoryMutation<br/>规范化 JSON + HMAC-SHA256]\n    end\n    I -->|stdin 交付| B\n    M --> S\n    S -->|IPC 请求 + mutationSignature| V\n    T -->|admittedTurnIds| V\n    V --> W\n```\n\n**上下文收紧**：`workspaceMemoryWriteRejection` 在签名之上再做执行上下文判定——Scheduled Run（group/isolated）与 SDK Task Sub-Agent **只能读取**，不能创建、更新或忘记；只有宿主准入的顶层交互式 Main/Runtime Session 且 runtime agent kind 为 `conversation` 时才放行。Sub-Agent 还额外被 runner 内的 `PreToolUse` hook 二次拦截（`createWorkspaceMemoryWriteGuard`），返回"将候选学习交回顶层会话"的拒绝理由。IPC 端还强制用 folder 解析 canonical Web workspace JID，**绝不信任模型提供的 JID**，并把 actor/workspace/sourceType 作为权威字段在服务端最后覆盖，`buildWorkspaceMemoryUpdateInput` 的白名单过滤保证攻击者无法通过 patch 篡改 principal。\n\nSources: [container/agent-runner/src/workspace-memory-context.ts](container/agent-runner/src/workspace-memory-context.ts#L8-L28); [src/memory-service.ts](src/memory-service.ts#L593-L684); [src/workspace-memory-capability.ts](src/workspace-memory-capability.ts#L76-L96); [src/workspace-memory-capability.ts](src/workspace-memory-capability.ts#L122-L188); [src/workspace-memory-ipc.ts](src/workspace-memory-ipc.ts#L42-L80); [src/index.ts](src/index.ts#L11962-L12140); [container/agent-runner/src/workspace-memory-runtime.ts](container/agent-runner/src/workspace-memory-runtime.ts#L15-L38); [container/agent-runner/src/workspace-memory-auth.ts](container/agent-runner/src/workspace-memory-auth.ts#L32-L51)\n\n### Runtime 工具面\n\nRunner 暴露四个 MCP 工具（`mcp__happyclaw__*`）：`workspace_memory_search` 与 `workspace_memory_get` 为只读工具，任何上下文始终可用；`workspace_memory_remember`、`workspace_memory_update`、`workspace_memory_forget` 仅在非定时任务且非 Task 上下文中注册（`memoryWriteEnabled = !ctx.isScheduledTask && !ctx.currentTaskId`）。update 强制 `expected_revision` 并要求冲突时先 search/get 最新版再协调，避免静默覆盖；每条写入都自动携带由 turn ID + canonical key/title/item ID 派生的默认幂等键。\n\nSources: [container/agent-runner/src/mcp-tools.ts](container/agent-runner/src/mcp-tools.ts#L2685-L2815)\n\n## Web 交互与前端体验\n\nMemory 页面（`web/src/pages/MemoryPage.tsx` + `web/src/features/workspace-memory/model.ts`）采用 Workspace-first 流程：从 `/api/workspaces` 选择 Workspace 并在 URL 保存 `?workspace=<workspaceJid>`；加载活跃 item 并总览四种 kind 的数量（`memoryKindCounts`）；搜索只在当前 Workspace 内执行、类别筛选可与搜索组合；详情展示来源 Session/来源 ID、观察时间、item revision、store revision 与修订时间线。`KIND_META` 为四种类型提供了中文 label（事实/决策/经验/待跟进）与说明文案，贯穿筛选与创建表单。\n\n```mermaid\nflowchart TD\n    A[进入 Memory 页] --> B{URL 有 workspace?}\n    B -- 否 --> C[从 /api/workspaces 选择]\n    B -- 是 --> D[加载活跃 item + kind 计数]\n    D --> E{用户动作}\n    E -->|搜索| F[仅当前 Workspace FTS/LIKE]\n    E -->|创建/编辑| G[POST / PATCH expectedRevision]\n    E -->|忘记| H[DELETE expectedRevision]\n    G -->|409| I[保留草稿 + role=alert 冲突提示]\n    I -->|显式操作| J[加载服务端最新版]\n```\n\n交互细节上，空状态必须区分"没有 Workspace""Workspace 尚无 Memory""搜索无结果"三种情形；请求失败在当前区域展示并允许刷新，不能回退到旧文件源；移动端在列表和详情之间切换，返回列表不改变 Workspace；输入控件都有可读 label，冲突和错误使用 `role=alert` 通知辅助技术。切换 Workspace 会清空旧选择和草稿（`handleWorkspaceChange` 同时更新 ref 与 URL 参数），并用 `isCurrentWorkspace(workspaceJid, epoch)` 防御过期异步响应覆盖新选择。\n\nSources: [docs/workspace-memory-v2.md](docs/workspace-memory-v2.md#L111-L136); [web/src/features/workspace-memory/model.ts](web/src/features/workspace-memory/model.ts#L110-L134); [web/src/pages/MemoryPage.tsx](web/src/pages/MemoryPage.tsx#L715-L736)\n\n## 重建、迁移与兼容性\n\n**重建工作区**（`POST /:jid/clear-history`）走 `resetWorkspaceMemoryData`：在单事务内删除该 Workspace 的全部 memory 数据（FTS、audit、outbox、mutation_requests、tombstones、provenance、versions、platform_items、items、stores），再插入一个 revision=0 的新 store——因为 Workspace 行本身保留，直接删 store 会让 Memory API 永久停留在 `store_not_found`。测试验证了重建同时重置 Home Owner Profile 称呼、把首次唤醒生命周期重置为 `pending`，且 SQLite 提交失败时文件、Memory 与任务定义整体回滚。\n\n**旧版迁移**：v1 的路径式全局/日期/文件 Memory 端点（`/api/memory/sources`、`/search`、`/file`、`/global`）已退役并返回 410；旧 `data/memory/` 文件只保留用于备份、离线导出或显式迁移，Web 与 Runtime 均不再读写。v67 迁移还会把更早的 `memory_chunks`/`memory_chunks_fts` 索引先备份到 `migration-backups/` 再删除——该索引曾占生产库约 65% 体积且数月无写入。迁移到 v2 后，SQLite 记录是唯一在线真相源，**不允许双写**。\n\nSources: [src/memory-store.ts](src/memory-store.ts#L829-L902); [tests/routes-groups-rebuild-memory.test.ts](tests/routes-groups-rebuild-memory.test.ts#L104-L140); [docs/workspace-memory-v2.md](docs/workspace-memory-v2.md#L138-L146); [tests/memory-chunks-drop-migration.test.ts](tests/memory-chunks-drop-migration.test.ts#L48-L80)\n\n## 测试保障\n\nWorkspace Memory 的契约被四组测试覆盖：`routes-workspace-memory.test.ts` 验证 CAS 拒绝过期更新并保留胜者版本、forget 生成 tombstone 并从召回中移除、create 幂等但拒绝 key 复用、FTS 事务性更新且按 Workspace 隔离、旧端点 410 退役；`workspace-memory-runtime.test.ts` 验证 HMAC 缺失/伪造/篡改/重放全部失败关闭、旧重叠 runner 无法伪造新 turn、每次 warm turn 拉取全新快照、恶意 snapshot 解析失败时 fail-open 不注入；`schema-v65-workspace-memory.test.ts` 验证每 Workspace 自动建 store 且不导入旧文件；`routes-groups-rebuild-memory.test.ts` 验证重建的原子性与干净状态。这些测试共同构成"读隔离、写签名、删留痕、建可重放"四条不变式的回归防线。\n\nSources: [tests/routes-workspace-memory.test.ts](tests/routes-workspace-memory.test.ts#L284-L460); [tests/workspace-memory-runtime.test.ts](tests/workspace-memory-runtime.test.ts#L320-L562)\n\n## 下一步阅读\n\nWorkspace Memory v2 是"数据与记忆"专题的第一环，建议按以下顺序延伸：\n\n- 记忆的物理载体与备份恢复边界，见 [数据目录结构与一致性备份恢复](23-shu-ju-mu-lu-jie-gou-yu-zhi-xing-bei-fen-hui-fu)\n- Runtime 快照注入与 IPC 通道所依赖的执行模型，见 [Agent Runner 与 IPC 通信协议](10-agent-runner-yu-ipc-tong-xin-xie-yi)\n- Memory 的 Web 入口所属的前端体系，见 [前端架构与页面路由设计](27-qian-duan-jia-gou-yu-ye-mian-lu-you-she-ji)\n- 平台保留项与 owner 身份的关系，见 [RBAC 权限模型与资源隔离](24-rbac-quan-xian-mo-xing-yu-zi-yuan-ge-chi)';export{e as default};

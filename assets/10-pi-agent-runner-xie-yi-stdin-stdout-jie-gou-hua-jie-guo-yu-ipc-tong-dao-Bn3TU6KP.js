const n='Miniclaw 的 Agent 执行体是一个独立 Node 子进程——它可能运行在 Docker 容器内，也可能以 host 进程形态被直接 spawn。无论哪种形态，它都必须回答同一个问题：一个无网络套接字、无共享内存的子进程，如何与主进程完成「接收任务、流式回传、追加指令、外呼渠道」四类交互？本文剖析这套协议的完整设计：stdin 上的一次性 JSON 引导、stdout 上的分帧结构化结果、以及基于文件系统的双向 IPC 通道。理解该协议是读懂执行引擎所有上层行为（队列调度、可靠投递、卡死恢复）的前提。\n\nSources: [index.ts](container/agent-runner/src/index.ts#L1-L15)\n\n## 协议全景：三条通信平面\n\nRunner 与主进程之间共有三个通信平面，各自承担不同性质的流量。第一平面是 **stdin 单向引导**：主进程在 spawn 时把完整的 `ContainerInput` JSON 写入标准输入直至 EOF，这是一次性的冷启动契约。第二平面是 **stdout 分帧输出**：runner 把每个结构化结果包在 `---MINICLAW_OUTPUT_START---` / `---MINICLAW_OUTPUT_END---` 标记对中写出，一次运行可产生多帧。第三平面是 **文件系统 IPC**：双方通过约定目录下的 JSON 文件与哨兵文件交换后续输入和工具调用结果，天然获得断电级持久性。\n\n```mermaid\nflowchart LR\n    subgraph Host["主进程"]\n        GQ["GroupQueue<br/>sendMessage / 哨兵写入"]\n        Parser["agent-output-parser<br/>分帧解析器"]\n        Router["IPC Output Router<br/>messages/ 目录消费者"]\n    end\n    subgraph Boundary["进程边界"]\n        STDIN["stdin 管道<br/>一次性 ContainerInput"]\n        STDOUT["stdout 管道<br/>分帧 ContainerOutput"]\n        STDERR["stderr 管道<br/>诊断日志"]\n    end\n    subgraph FS["共享文件系统挂载<br/>DATA_DIR/ipc/{folder} ↔ /workspace/ipc"]\n        INPUT["input/<br/>跟进消息 + _close/_drain/_interrupt"]\n        MSGS["messages/<br/>MCP 外呼请求"]\n        RESULTS["message-results/<br/>请求响应"]\n        TASKS["tasks/<br/>能力调用请求"]\n    end\n    subgraph Runner["agent-runner 子进程"]\n        Loop["main() 查询循环"]\n        MCP["SDK MCP Server<br/>send_message 等"]\n    end\n    GQ -->|JSON until EOF| STDIN --> Loop\n    Loop -->|"标记帧 × N"| STDOUT --> Parser\n    Loop --> log["[agent-runner] 日志"] --> STDERR\n    GQ -->|原子写 .json / 哨兵| INPUT -->|fs.watch + 轮询| Loop\n    MCP -->|requestId .json| MSGS\n    MCP -->|requestId .json| TASKS\n    MSGS --> Router\n    TASKS --> Router\n    Router -->|result_{requestId}.json| RESULTS -->|轮询读取| MCP\n```\n\n这一设计的动机在于：管道只适合「一次写、持续读」的单向流，而 Agent 会话的本质是长生命周期、多轮次、需要中途注入指令的；文件系统则同时提供了持久性（进程崩溃后消息仍在）与跨容器可见性（Docker bind mount 让宿主目录与 `/workspace/ipc` 共享同一份 inode）。挂载映射由 `container-runner.ts` 在构建 volume 时完成，并为 `messages`、`tasks`、`input`、`agents` 四个子目录预建 0700 权限。\n\nSources: [container-runner.ts](src/container-runner.ts#L1330-L1351)、[Dockerfile](container/Dockerfile#L212)、[index.ts](container/agent-runner/src/index.ts#L155-L174)\n\n## 冷启动引导：stdin 上的 ContainerInput 契约\n\n`main()` 的第一步是把 stdin 读到 EOF 并整体 `JSON.parse` 为 `ContainerInput`。若缺少 `turnId`，runner 会生成 `ipc-{timestamp}-{random}` 形式的兜底 ID，并将其快照进模块级的 `activeOutputInputTurnId`——此后每一帧输出都会携带这个 ID，即使某些状态帧绕过了常规 emit 路径也不会丢失相关性。解析失败时 runner 立即写一帧 `status:\'error\'` 并退出码 1，绝不带病运行。\n\nSources: [index.ts](container/agent-runner/src/index.ts#L3925-L3943)、[index.ts](container/agent-runner/src/index.ts#L797-L807)、[index.ts](container/agent-runner/src/index.ts#L176-L183)\n\n`ContainerInput` 是整个协议中信息密度最高的结构，其关键字段可分为五组：\n\n| 字段组 | 代表字段 | 语义 |\n|---|---|---|\n| 回合标识 | `prompt`、`sessionId`、`turnId`、`queryRunId` | 初始提示、可恢复会话 ID、回合身份、Web 流栅栏所用的精确查询尝试 ID |\n| 渠道身份 | `chatJid`、`currentSourceJid`、`channelContext` | 触发本轮的消息来源 JID 与净化后的能力上下文（provider/bot/chat/sender） |\n| 权限边界 | `isHome`、`isAdminHome`、`workspaceMemoryMutationSigningSecret` | 容器角色标志；仅经 stdin 下发的 HMAC 签名密钥 |\n| 能力清单 | `plugins`、`skillManifest`、`contextAudit` | 插件路径、有效 Skill 集 hash、上下文审计引导 |\n| 任务归属 | `isScheduledTask`、`taskRunId`、`sessionAgentId`、`messageTaskId` | 定时任务标记、隔离命名空间、输出路由归属 |\n\nSources: [types.ts](container/agent-runner/src/types.ts#L194-L287)\n\n值得强调的是这条协议的安全不变量：`workspaceMemoryMutationSigningSecret` 只允许出现在 stdin 引导中，「必须保持 runner 私有，绝不可写入 IPC、文件、环境变量或 prompt」。与之配套的是 `ChannelTurnContext` 的白名单归一化——`normalizeChannelTurnContext` 显式丢弃未知键（包括任何 token/secret 形态的数据），只保留描述触发消息本身的能力字段，从而保证凭据永不跨越 runner 边界进入模型上下文。\n\nSources: [types.ts](container/agent-runner/src/types.ts#L219-L227)、[types.ts](container/agent-runner/src/types.ts#L84-L98)、[types.ts](container/agent-runner/src/types.ts#L12-L18)\n\n## stdout 分帧协议：标记、解析与容错\n\n`writeOutput` 是唯一的出口函数：打印 START 标记、单行 JSON、END 标记。写帧前会把 `activeOutputInputTurnId` 快照合并进每一帧，使直接状态帧（错误、关闭、SIGTERM 会话保存）也能与发起它的用户输入回合关联。\n\nSources: [index.ts](container/agent-runner/src/index.ts#L809-L823)\n\n主进程侧的解析器 `attachStdoutHandler` 对字节流做增量扫描：找到 START 标记后，并不依赖 END 标记定界，而是用**括号深度匹配**（`findJsonObjectEnd`）从 JSON 结构本身推导对象终点。注释明确指出原因：Agent 的回复文本里可能出现字面量标记字符串，按字符串扫描会被欺骗；括号匹配对「同一缓冲区内尾随的第二帧」也保持正确，且单遍 O(n)。解析失败时有专门的重同步逻辑跳过坏帧，避免缓冲区停滞到容量上限；10MB 解析缓冲溢出时会截断到最后一个 START 标记。\n\nSources: [agent-output-parser.ts](src/agent-output-parser.ts#L13-L15)、[agent-output-parser.ts](src/agent-output-parser.ts#L43-L63)、[agent-output-parser.ts](src/agent-output-parser.ts#L128-L200)\n\nstderr 承载诊断日志，且有一套双级别约定：普通日志走 `[agent-runner]` 前缀，主进程以 debug 级消费（生产环境默认不可见）；运维必须看到的信息走 `[agent-runner:warn]` 前缀，被 stderr 处理器提升为 warn 级。此外协议还处理了两类管道边缘情况：stdout/stderr 上的 EPIPE 错误静默退出码 0（收尾写入失败不应拖垮 host 侧查询），SIGTERM 则先补发一帧携带最新 `sessionId` 的 success 再退出，让主进程在强杀前保住会话续接点。\n\nSources: [index.ts](container/agent-runner/src/index.ts#L825-L836)、[agent-output-parser.ts](src/agent-output-parser.ts#L310-L320)、[index.ts](container/agent-runner/src/index.ts#L4730-L4762)\n\n## ContainerOutput 帧：四类状态与相关性元数据\n\n每一帧都是一个自包含的 `ContainerOutput` 对象。`status` 只有四个合法值，但配合元数据字段可以表达远超「成功/失败」的语义：\n\n| status | 含义 | 典型伴随字段 |\n|---|---|---|\n| `success` | 一个健康的最终文本或控制信号 | `result`、`sourceKind`、`inputTurnCompleted`、`ipcReceipts` |\n| `error` | 失败终结（含机器可读前缀如 `context_overflow:`） | `error`、`finalizationReason` |\n| `stream` | 载荷是一条内嵌 StreamEvent | `streamEvent` |\n| `closed` | 因 `_close` 哨兵退出，非静默死亡 | 无 result——用于区分「正常关停」与「吞消息」故障 |\n\nSources: [types.ts](container/agent-runner/src/types.ts#L289-L363)、[index.ts](container/agent-runner/src/index.ts#L4269-L4278)\n\n相关性元数据是协议的骨架。`emit()` 在每次发帧前把当前的 `queryRunId`、`turnId`、`sessionId` 装饰到 StreamEvent 上，使 Web 端能拒绝迟到的 A 尝试输出而不误伤复用相同持久输入 ID 的合法重试 B。`inputTurnId` 被特别注明「在后续 steer 仅排队等待时不得轮换」，它是主进程判定「哪个输入回合拥有当前输出」的权威依据。`queryIdle:true` 则告诉主进程：本次完成的 SDK 输入回合之后没有已接受的 steer 排队——主进程据此而非仅仅一个 result 来释放下一条排队的持久跟进。\n\nSources: [index.ts](container/agent-runner/src/index.ts#L1854-L1878)、[types.ts](container/agent-runner/src/types.ts#L315-L352)、[group-queue.ts](src/group-queue.ts#L1622-L1627)\n\n`sourceKind` 枚举进一步细分了 result 的产生路径（`sdk_final`、`sdk_send_message`、`interrupt_partial`、`truncation_continue` 等），`pendingBgTasks` 计数告诉主进程在后台任务未清零前应把流式卡片保持在「运行中」而非定稿。这些字段共同构成主进程 `wrappedOnOutput` 的决策输入：抑制被取代回合的输出、确认 IPC 投递、登记健康完成的输入回合、标记 runner 空闲。\n\nSources: [types.ts](container/agent-runner/src/types.ts#L329-L352)、[index.ts](src/index.ts#L15236-L15305)\n\n## StreamEvent：嵌入帧内的流式事件载荷\n\n`status:\'stream\'` 帧的 `streamEvent` 字段承载细粒度流式事件，其类型定义的唯一权威源是仓库根部的 `shared/stream-event.ts`——构建步骤把它同步复制到 runner、后端与 Web 三端，注释明确警告「不要编辑副本」。事件类型覆盖文本/思考增量、工具生命周期、任务通知、权限拒绝、记忆召回、压缩边界、用量统计等二十余种。\n\nSources: [shared/stream-event.ts](shared/stream-event.ts#L1-L10)、[shared/stream-event.ts](shared/stream-event.ts#L12-L36)\n\nrunner 侧由 `StreamEventProcessor` 把 Claude Agent SDK 的原始消息流翻译成这些事件，统一经 `emitStreamEvent` 包装成 `{status:\'stream\', result:null, streamEvent}` 帧写出；主进程收到后按事件类型分别处理（工作流快照登记、用量记账、`broadcastStreamEvent` 向 WebSocket 与 IM 渠道广播）。三端共享类型与广播机制的完整展开属于独立主题，此处只需建立「StreamEvent 是 stdout 帧的内嵌载荷」这一定位即可。\n\nSources: [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L217-L218)、[index.ts](src/index.ts#L15341-L15358)\n\n## IPC 输入通道：消息文件、原子发布与三类哨兵\n\n输入通道的目录布局按 runner 身份分层：主会话用 `data/ipc/{folder}/input/`，会话子 Agent 用 `agents/{agentId}/input/`，隔离任务用 `tasks-run/{taskRunId}/input/`。主进程的 `sendMessage` 是唯一合法发布者，其写入流程体现了严格的崩溃安全次序：先写 `{filepath}.tmp`，再 `renameSync` 原子发布，随后在同一同步临界区内登记内存投递账本并刷新活跃时间戳——变更暂停/停止不可能观察到「文件已存在但恢复元数据缺失」的中间态。\n\nSources: [group-queue.ts](src/group-queue.ts#L1518-L1540)、[group-queue.ts](src/group-queue.ts#L1633-L1709)\n\n每条消息文件的载荷包含 `type:\'message\'`、`text`、可选 `images`、`queryRunId`、`sourceJid`、`channelContext`、`taskId` 以及关键的 `receipt`。runner 侧 `drainIpcInput` 按 `readdirSync` 排序枚举、逐个解析并删除，再交给 `orderIpcInputMessages` 恢复持久到达顺序——注释解释了为何不能信任文件名顺序：重新入队的消息可能拿到比查询拆除期间写入的消息更新的文件名，只有当整批都带 receipt 时才以数据库游标为权威排序。\n\nSources: [group-queue.ts](src/group-queue.ts#L1678-L1691)、[ipc-delivery.ts](container/agent-runner/src/ipc-delivery.ts#L11-L20)、[ipc-delivery.ts](container/agent-runner/src/ipc-delivery.ts#L30-L46)、[index.ts](container/agent-runner/src/index.ts#L1394-L1440)\n\n除 `.json` 消息外，输入目录还承载三个哨兵文件，它们构成 runner 生命周期的遥控信号：\n\n| 哨兵 | 写入方语义 | runner 侧行为 | 检查时机 |\n|---|---|---|---|\n| `_close` | 会话结束，立即回收 | `waitForIpcMessage` 中消费并退出循环，补发 `status:\'closed\'` 帧 | 空闲等待与查询中均检查 |\n| `_drain` | 跑完当前查询再退出一问一答语义 | 结果已收到时视为 close 结束流；空闲期同样导致退出 | 查询完成后、空闲等待 |\n| `_interrupt` | 温和中止当前查询（用户点停止） | 调 `query.interrupt()`，取消本回合投递账本、抑制后续输出，容器存活 | 仅查询进行中 |\n\nSources: [index.ts](container/agent-runner/src/index.ts#L1292-L1306)、[index.ts](container/agent-runner/src/index.ts#L1374-L1390)、[group-queue.ts](src/group-queue.ts#L2040-L2070)、[group-queue.ts](src/group-queue.ts#L1788-L1805)\n\n事件驱动检测由 `createIpcWatcher` 提供：`fs.watch` 监听全部事件类型（注释指出 Linux 上原子写入产生 rename，而 macOS virtiofs bind mount 可能只产生 change）、50ms 防抖、外加 5000ms 后备轮询以防 inotify 事件丢失；watcher 出错时自动降级为纯轮询。启动阶段还有一层防御性清理：删除陈旧 `_close`，但对 `_interrupt` 只清除超过 10 秒宽限窗口的旧文件，避免误杀刚写入的中断信号。\n\nSources: [index.ts](container/agent-runner/src/index.ts#L1445-L1508)、[index.ts](container/agent-runner/src/index.ts#L4034-L4045)、[index.ts](container/agent-runner/src/index.ts#L1308-L1331)\n\n## 暖 Runner 主循环与查询中转向\n\nRunner 的常驻形态是一个「查询—等待—再查询」的循环。`main()` 完成 stdin 引导与 MCP 工具装配后进入 `while(true)`：执行 `runQuery` → 处理各类失败路径（会话恢复失败降级新会话、上下文溢出限三次重试、不可恢复转录错误要求重置）→ 若 `_close` 已被消费则补发 closed 帧退出 → 否则 `waitForIpcMessage()` 阻塞等待下一批输入或哨兵。批量到达的多条 IPC 消息会被合并为单个 prompt（文本换行拼接、图片合并、taskId/sourceJid/channelContext 取最新），实现飞书转发+评论这类场景的自然聚合。\n\n```mermaid\nflowchart TD\n    A["readStdin: ContainerInput"] --> B["runQuery(初始 prompt)<br/>SDK 流式执行"]\n    B --> C{"失败分类"}\n    C -->|resume 失败| D["清 session<br/>注入旧转录历史"] --> B\n    C -->|context overflow| E{"重试 < 3 次?"}\n    E -->|是| B\n    E -->|否| F["error 帧 + exit 1"]\n    C -->|正常结束| G{"_close 已消费?"}\n    G -->|是| H["closed 帧 + 退出"]\n    G -->|否| I["waitForIpcMessage()<br/>fs.watch + 5s 轮询"]\n    I -->|".json 消息"| J["聚合批次为新 prompt<br/>更新 turnId/taskId/渠道上下文"]\n    J --> B\n    I -->|"_close / _drain"| H\n```\n\nSources: [index.ts](container/agent-runner/src/index.ts#L4110-L4121)、[index.ts](container/agent-runner/src/index.ts#L4197-L4263)、[index.ts](container/agent-runner/src/index.ts#L4655-L4688)、[index.ts](container/agent-runner/src/index.ts#L1507-L1587)\n\n真正体现协议精妙之处的是**查询中转向**：`runQuery` 内部的 `pollIpcDuringQuery` 在 SDK 流执行期间持续轮询输入目录。一旦 SDK transport 就绪（过早 push 会触发 `ProcessTransport is not ready` 异常），新到达的 IPC 消息不再等查询结束，而是经 `stream.push` 直接注入正在运行的 SDK 流——这就是「steering」。每条注入消息先经 `ipcDeliveryTracker.acceptTurn` 登记，附带各自的 channelContext、Workspace Memory 快照与 Owner Profile 上下文装饰。维护性续查（截断续写、auto-continue）则被 `shouldAcceptIpcMessagesDuringQuery` 明确禁止消费用户消息，防止内部查询偷走属于主循环的输入。\n\nSources: [index.ts](container/agent-runner/src/index.ts#L2063-L2135)、[index.ts](container/agent-runner/src/index.ts#L2152-L2255)\n\n结果后的收尾同样有协议窗口：result 到达后 runner 给主进程 5 秒时间写 `_drain`（POST_RESULT_TIMEOUT_MS），期间主动 `query.interrupt()` 中止卡住的工具调用让 for-await 自然结束、回到 warm 等待——不杀进程。主进程关闭时的优雅退出路径也优先写 `_close` 哨兵而非立即 SIGTERM，注释说明这是为了避免等到 grace 用尽后被强制结束、丢失 conversation archive。\n\nSources: [index.ts](container/agent-runner/src/index.ts#L2049-L2060)、[index.ts](container/agent-runner/src/index.ts#L2136-L2150)、[group-queue.ts](src/group-queue.ts#L3004-L3022)\n\n## IPC 出站通道：send_message 的请求-响应文件握手\n\n出站方向上，runner 内嵌的 SDK MCP Server 工具（`send_message`、`send_image`、Feishu 能力、Agent Builder 操作等）通过统一的「写请求文件 + 轮询结果文件」模式与主进程握手。`writeIpcFile` 以 `wx` flag + 0600 权限写临时文件后 rename，保证原子性与权限收敛；`pollIpcResult` 每 500ms 直接尝试 `readFileSync` 并只吞 ENOENT（注释指出这修复了 TOCTOU），超时后抛错。`send_message` 的请求写入 `messages/`，随后最长等待 120 秒读取 `message-results/send_message_result_{requestId}.json`。\n\nSources: [mcp-tools.ts](container/agent-runner/src/mcp-tools.ts#L62-L86)、[mcp-tools.ts](container/agent-runner/src/mcp-tools.ts#L93-L121)、[mcp-tools.ts](container/agent-runner/src/mcp-tools.ts#L776-L784)\n\n```mermaid\nsequenceDiagram\n    participant Model as SDK 模型\n    participant Tool as send_message MCP 工具\n    participant FS as 共享文件系统\n    participant Host as 主进程 IpcWatcher\n    Model->>Tool: send_message(text, delivery_role=final)\n    Tool->>FS: messages/{ts}-{rand}.json<br/>{type,message,text,requestId,<br/>interactionMode 冻结,inputTurnId}\n    FS->>Host: fs.watch 触发（debounce）\n    Host->>Host: 校验冻结 interactionMode<br/>路由到 Turn Output Coordinator\n    Host->>FS: message-results/<br/>send_message_result_{requestId}.json\n    Host->>FS: unlink 源请求文件\n    FS->>Tool: 轮询读到结果帧\n    Tool-->>Model: 确认语义（staged_final / delivered…）\n```\n\nSources: [index.ts](src/index.ts#L1082-L1135)、[index.ts](src/index.ts#L10236-L10247)、[index.ts](src/index.ts#L10293-L10330)\n\n这个握手里有一个容易忽视的协议细节：`buildSendMessageData` 在**写文件时刻**把 `interactionMode` 冻结进请求（proactive 或 assistant 二选一），注释的理由是「主进程不得在副作用已写下后因 Workspace 模式变化而重新解读它」。主进程校验发现无效冻结模式时直接写失败结果并删除请求文件，模型收到权威错误而不是被静默吞掉。\n\nSources: [mcp-tools.ts](container/agent-runner/src/mcp-tools.ts#L451-L475)、[index.ts](src/index.ts#L10304-L10330)\n\n## 投递回执闭环：从 IPC 文件到数据库游标\n\n输入通道的可靠性核心是 `receipt` 结构：`deliveryId`（主进程签发的持久投递身份）、`chatJid`、`cursor`（数据库游标 timestamp+id+sourceJid）以及可选的 `coveredCursors` 数组——后者表示本次投递一并覆盖的更早游标，且双侧都强制校验「cursor 必须等于 coveredCursors 中的最大值」，不一致即视为非法载荷。`parseIpcReceipt` 在 runner 侧做严格形状验证，任何字段缺失或最大值不匹配都会使 receipt 被丢弃降级为 legacy 消息。\n\nSources: [ipc-delivery.ts](container/agent-runner/src/ipc-delivery.ts#L48-L111)、[group-queue.ts](src/group-queue.ts#L1638-L1661)\n\n闭环的完成发生在 stdout 帧里：`IpcTurnDeliveryTracker` 在 SDK 输入回合健康结束时产出 `ipcReceipts` 数组随帧发出，主进程 `wrappedOnOutput` 据此调用 `queue.acknowledgeIpcDeliveries` 提交数据库游标——至此消息才算「已被消费」，下次重启不会重放。`inputTurnCompleted:false` 的帧（截断、后台任务挂起、中断、错误路径）则不触发提交。若 runner 在确认前崩溃，两条恢复路径接管：`recoverUnconsumedIpc` 检测磁盘上残留的 `.json` 并重新点亮 pendingMessages（对应 issue #240 的竞态）；`recoverUnacknowledgedIpcDeliveries` 则先把仍在盘上的投递文件作废、再把账本中的 receipt 回放给上层，确保「IPC 文件与数据库重放」二选一、绝不双投。\n\nSources: [index.ts](src/index.ts#L15267-L15297)、[group-queue.ts](src/group-queue.ts#L1813-L1859)、[group-queue.ts](src/group-queue.ts#L1861-L1891)\n\n## 小结\n\nPi Agent Runner 协议的本质是用三种传输介质各取所长：stdin 承担低频高密度的引导与密钥下发，stdout 承担高频单向的结构化结果流并以括号匹配分帧抵抗内容干扰，文件系统 IPC 承担需要持久性与跨边界的双向流量（跟进输入、哨兵遥控、MCP 外呼握手）。所有跨回合的相关性都锚定在显式 ID 上——`inputTurnId` 归属输出、`queryRunId` 栅栏 Web 流、`deliveryId` 幂等投递、`requestId` 匹配请求响应——这使得协议在暖进程复用、steer 注入、崩溃恢复等复杂场景下依然可以逐帧审计。掌握这些原语后，执行引擎的上层行为（双执行模式的容量治理、串行队列的生命周期管理、可靠投递状态机）都可以还原为对这些协议原语的编排。\n\n建议继续沿以下路径深入：执行模式的容量与超时治理见 [Host 与 Container 双执行模式：容量、超时与暖 Runner](11-host-yu-container-shuang-zhi-xing-mo-shi-rong-liang-chao-shi-yu-nuan-runner)；Runner 生命周期与卡死恢复见 [会话串行队列：Runner 生命周期、重试退避与卡死恢复](14-hui-hua-chuan-xing-dui-lie-runner-sheng-ming-zhou-qi-zhong-shi-tui-bi-yu-qia-si-hui-fu)；StreamEvent 的三端同步机制见 [StreamEvent 流式契约：三端共享类型与同步机制](25-streamevent-liu-shi-qi-yue-san-duan-gong-xiang-lei-xing-yu-tong-bu-ji-zhi)。';export{n as default};

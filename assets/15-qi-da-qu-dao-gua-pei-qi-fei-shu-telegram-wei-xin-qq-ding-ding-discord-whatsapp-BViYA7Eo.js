const n='Miniclaw 将七个异构 IM 平台收敛为一套统一的渠道接入层。本页聚焦**适配器本身**：统一契约 `IMChannel` 如何定义、每个平台各自的连接协议与认证方式差异、以及各适配器独有的能力（流式卡片、持久化收件箱、重连策略等）。多账号绑定规则与投递状态机分别属于后续两页，此处仅在边界处点到为止。\n\n## 统一契约：IMChannel 接口与工厂适配模式\n\n整个渠道层的核心是一个约 50 行的接口。`IMChannel` 定义了所有平台必须实现的最小集合——`connect`/`disconnect`/`sendMessage`/`setTyping`/`isConnected`——外加一组可选能力方法：`sendFile`、`sendImage`、`syncGroups`、`logout`、`clearAckReaction`、`createStreamingSession`（流式卡片会话）、`reconcileStreamingCard`（中断卡片修复）、`getChatInfo`，以及飞书专属的 `executeFeishuCapability`。可选方法的存在意味着"能力差异被显式建模为接口的可选性"，而不是让七个实现互相迁就一个最大公约数。\n\nSources: [im-channel.ts](src/im-channel.ts#L170-L219)\n\n入站方向同样有一份回调契约：`IMChannelConnectOpts` 约定了适配器在收到消息时必须回调的钩子——从基础的 `onReady`/`onNewChat`/`onCommand`，到配对准入的 `onPairAttempt`/`isChatAuthorized`，再到群聊治理的 `shouldProcessGroupMessage`/`isGroupOwnerMessage`/`isSenderAllowedInGroup`。适配器只负责"把平台事件翻译成这些回调"，路由与权限判断全部上移到编排层，这保证了七个渠道共享同一条业务语义管线。\n\nSources: [im-channel.ts](src/im-channel.ts#L84-L168)\n\n代码组织采用双层工厂：每个平台文件（如 `qq.ts`）导出 `createQQConnection` 这类**连接工厂**，封装平台 SDK 与私有状态；`im-channel.ts` 再提供 `createQQChannel` 等**适配器工厂**，把连接包装成统一的 `IMChannel`。这种隔离使得平台特有的扩展可以安全地附加在适配器外层——例如 Discord 通过 `DiscordChannelExtensions` 额外暴露历史消息与频道/服务器元数据查询，并用类型守卫 `isDiscordChannel` 在运行时识别。\n\nSources: [im-channel.ts](src/im-channel.ts#L1020-L1044)\n\n跨渠道寻址依赖**带前缀的 JID 字符串**。`CHANNEL_PREFIXES` 为七个渠道各定义一个前缀（`feishu:`、`telegram:`、`qq:`……），`getChannelFromJid` 无法匹配时回落为 `\'web\'`——这意味着 Web 前端天然是"第八个渠道"，只是不走适配器。\n\nSources: [channel-prefixes.ts](src/channel-prefixes.ts#L1-L19)\n\n## 架构总览：事件如何穿越适配器层\n\n理解下图需要先明确三个角色：**平台 SDK/协议**（各渠道官方或社区库）、**Connection 工厂**（管理 socket、token、去重等私有状态）、**IMConnectionManager**（按用户维度持有已连接渠道并向上提供统一 API）。适配器层的全部职责就是把左边的异构协议折叠成右边的同构调用：\n\n```mermaid\nflowchart LR\n    subgraph Platforms["IM 平台"]\n        FS["飞书<br/>WSClient WebSocket"]\n        TG["Telegram<br/>getUpdates 长轮询"]\n        WX["微信 iLink<br/>HTTP 长轮询"]\n        QQ["QQ Bot v2<br/>Gateway WebSocket"]\n        DT["钉钉<br/>Stream 模式"]\n        DC["Discord<br/>discord.js Gateway"]\n        WA["WhatsApp<br/>Baileys WebSocket"]\n    end\n\n    subgraph Factories["Connection 工厂层"]\n        F1["createFeishuConnection"]\n        F2["createTelegramConnection"]\n        F3["createWeChatConnection"]\n        F4["createQQConnection"]\n        F5["createDingTalkConnection"]\n        F6["createDiscordConnection"]\n        F7["createWhatsAppConnection"]\n    end\n\n    subgraph Unified["统一适配器层 im-channel.ts"]\n        IF["IMChannel 接口<br/>+ create*Channel 包装"]\n    end\n\n    MGR["IMConnectionManager<br/>per-user 渠道注册表"]\n\n    FS --> F1 --> IF\n    TG --> F2 --> IF\n    WX --> F3 --> IF\n    QQ --> F4 --> IF\n    DT --> F5 --> IF\n    DC --> F6 --> IF\n    WA --> F7 --> IF\n    IF --> MGR\n```\n\nSources: [im-channel.ts](src/im-channel.ts#L1-L58), [im-manager.ts](src/im-manager.ts#L79-L82)\n\n`IMConnectionManager` 以 `Map<userId, UserIMConnection>` 持有每个用户名下的渠道实例，键名为 `channelType[:accountId]` 以支持同一平台多个账号并存。通用入口 `connectChannel` 内部做了三重防护：用户级锁 + 渠道级锁串行化连接操作、"sealed 用户"三次校验（禁用/删除用户后拒绝任何新连接，并在网络耗时的窗口期反复复查）、以及凭据身份认领防止同一个 Bot 被两个用户同时占用。连接失败时的清理路径也考虑到了半初始化状态——若 disconnect 抛错，失败渠道仍会留在注册表中等待重试，避免定时器与监听器泄漏。\n\nSources: [src/im-manager.ts](src/im-manager.ts#L606-L724)\n\n启动期的接线发生在 `index.ts`：按账号的 `provider` 字段分发到对应的 `connectUser*` 方法，注入该渠道专属配置（如微信的 `getUpdatesBuf` 游标、钉钉/Discord 的 `streamingMode`）。这段分发逻辑是观察"每个渠道需要哪些凭据"的最佳位置。\n\nSources: [index.ts](src/index.ts#L19235-L19274)\n\n## 七大渠道横向对比\n\n下表汇总了每个适配器的核心事实，均来自各文件头注释与常量定义。注意"接收机制"一列的差异直接决定了断线重连的实现复杂度——轮询类渠道（Telegram、微信）天然自带重试语义，而长连接类渠道（其余五个）都必须自建心跳与重连状态机。\n\n| 渠道 | 协议 / SDK | 接收机制 | 认证方式 | 流式输出 | 文本分片上限 |\n|------|-----------|---------|---------|---------|------------|\n| 飞书 | @larksuiteoapi/node-sdk | `lark.WSClient` WebSocket 长连接 | appId + appSecret | ✅ 三级降级卡片 | 流式预算 30K 字符 + 多卡滚动 |\n| Telegram | grammy `Bot` | HTTP 长轮询 `getUpdates` | botToken（支持代理） | ❌（typing 动作） | grammy 默认限制 |\n| 微信 | iLink Bot API（HTTP） | `getupdates` 长轮询 + 持久游标 | botToken + ilinkBotId | ❌ | 2000 字符 |\n| QQ | 官方 Bot API v2 | 原生 WebSocket Gateway（op 0–11） | OAuth Token 自动刷新 | ✅ 仅 C2C 私聊 | 5000 字符 |\n| 钉钉 | dingtalk-stream SDK | Stream 模式 WebSocket（DWClient） | clientId + clientSecret | ✅ AI Card | 4000 字符 |\n| Discord | discord.js `Client` | Gateway WebSocket（5 个 Intent） | botToken | ⚠️ message.edit（默认关闭） | 2000 字符 |\n| WhatsApp | baileys 7.0.0-rc13 | WebSocket 直连 Meta（逆向协议） | QR 扫码 + 本地 auth state | ❌ | 4096 字符 |\n\nSources: [feishu-streaming-card.ts](src/feishu-streaming-card.ts#L1-L12), [telegram.ts](src/telegram.ts#L1-L10), [wechat.ts](src/wechat.ts#L1-L13), [qq.ts](src/qq.ts#L255-L263), [dingtalk.ts](src/dingtalk.ts#L1-L11), [discord.ts](src/discord.ts#L777-L786), [whatsapp.ts](src/whatsapp.ts#L51-L55)\n\nJID 格式同样值得单独一张表——它是贯穿数据库、路由与投递的统一地址语言，且部分渠道在一个前缀内还嵌套了二级类型：\n\n| 渠道 | JID 格式 | 说明 |\n|------|---------|------|\n| 飞书 | `feishu:{chatId}` | chatId 为 `oc_`（群）或 open_id（私聊），thread 通过 route target 表达 |\n| Telegram | `telegram:{chatId}#thread:{id}` | Forum 话题作为 base chat 的片段，仅用于回复路由 |\n| QQ | `qq:c2c:{openid}` / `qq:group:{openid}` | 二级类型决定能否使用流式接口 |\n| 微信 | `wechat:{userId}` | 扁平结构 |\n| 钉钉 | `dingtalk:c2c:{staffId}` / `dingtalk:group:{conversationId}` | 同 QQ 的二级类型设计 |\n| Discord | `discord:{channelId}` / `discord:dm:{userId}` | 频道级而非服务器级注册 |\n| WhatsApp | `whatsapp:{remoteJid}` | 直接沿用 Baileys 的 JID |\n\nSources: [telegram.ts](src/telegram.ts#L93-L101), [qq.ts](src/qq.ts#L355-L365), [discord.ts](src/discord.ts#L183-L192), [dingtalk.ts](src/dingtalk.ts#L281-L292)\n\n## 飞书：能力最重的参考实现\n\n飞书适配器以 3844 行代码成为七个渠道中最庞大的一员，其复杂度主要来自四项独有能力。第一是**持久化收件箱围栏**：入站消息先经 `recordChannelInbox` 写入 SQLite 并利用唯一性约束去重，写入失败时直接抛错拒绝执行——注释明确说明这是为了防止"两个 WS 客户端或一次重连对同一条消息启动两次 Agent turn"。只有成功落库的消息才会被 lease 认领后进入处理流程。\n\nSources: [feishu.ts](src/feishu.ts#L1761-L1810)\n\n第二是 **WebSocket 自愈机制**。适配器通过反射 SDK 私有属性探测真实连接状态，每 15 秒执行一次健康检查；连续多次判定离线且距上次重连超过最小间隔时，重建 `WSClient` 并在恢复后自动补跑队列中的 Inbox 项与历史 backfill。这套逻辑与 QQ 的重连策略形成对比：飞书选择"探测 + 整体重建"，因为 SDK 不暴露细粒度的断线原因。\n\nSources: [feishu.ts](src/feishu.ts#L3087-L3175), [feishu.ts](src/feishu.ts#L3372-L3384)\n\n第三是**三级降级的流式卡片**：Level 0 使用原生 `streaming_mode` 打字机效果（70ms/字符）；API 失败时降级到 Level 1 CardKit 全量 JSON 替换（≥1000ms 间隔）；再降级到 Level 2 传统 `message.create + patch`。配合代码块安全的文本切分与超长输出的多卡滚动，这是四个支持流式的渠道中工程化程度最高的实现。\n\nSources: [feishu-streaming-card.ts](src/feishu-streaming-card.ts#L1-L12)\n\n第四是 **Capability Broker**：`executeFeishuCapability` 允许 Agent Runner 以无凭据方式执行十种飞书操作（读群信息、成员列表、发卡片、加/删表情、编辑/撤回消息、透传 API 请求等）。凭据始终留在主进程侧，Runner 只传递操作名与参数——这是安全边界（容器内不持有 appSecret）在渠道层的具体体现。\n\nSources: [feishu-capability.ts](src/feishu-capability.ts#L7-L18)\n\n此外值得一提的还有群聊 mention 门控：它被刻意抽取为纯函数并锁定 **fail-closed** 语义——当 bot 的 open_id 未知时默认拒绝处理，而不是历史上曾导致 `require_mention` 全群静默失效的 fail-open。这个决策由单测固化，防止未来重构回退。\n\nSources: [feishu-mention-gate.ts](src/feishu-mention-gate.ts#L1-L14)\n\n## Telegram 与微信：轮询双雄\n\nTelegram 适配器基于 grammy 框架，通过 `getUpdates` 长轮询接收更新。它有两个特色设计：其一，**typing 租约机制**——由于 Telegram 的 typing 指示器只能维持数秒，适配器用"每租约一条记录 + 定时脉冲（4 秒）+ 60 分钟强制过期回收"的组合，保证任何一个租约持有者崩溃都不会把指示器永久钉死，也不会让 A turn 误删 B turn 的指示器。\n\nSources: [im-channel.ts](src/im-channel.ts#L420-L455), [im-channel.ts](src/im-channel.ts#L594-L629)\n\n其二是对 Forum 话题的原生感知：`buildTelegramRouteJid` 把 `message_thread_id` 编码为 base JID 上的 `#thread:` 片段——base chat 仍然是注册与绑定的对象，话题只是回复路由的修饰，这与飞书的 thread 处理思路一致但表达形式不同。\n\nSources: [telegram.ts](src/telegram.ts#L104-L124)\n\n微信适配器走的是 iLink Bot API（`ilinkai.weixin.qq.com`），这是一个非官方公开文档的 HTTP 协议。它的关键设计是**游标即重放边界**：每次 `getupdates` 返回的 `getUpdatesBuf` 游标会在下一轮轮询前持久化，重启时若存在持久游标则放弃 `ignoreMessagesBefore` 截断，确保崩溃期间的消息不会被丢弃。媒体处理也别具一格——CDN 图片下载后需经 AES-128-ECB 解密，上传则要携带模拟 iLink 客户端的身份头。\n\nSources: [wechat.ts](src/wechat.ts#L1110-L1150), [wechat-crypto.ts](src/wechat-crypto.ts#L20-L36), [index.ts](src/index.ts#L19288-L19294)\n\n代理处理上，微信是唯一支持"按账号绕过系统代理"的渠道：`createWeChatHttpDispatcher` 为每个账号构建独立的 undici Dispatcher，直连模式下仍尊重 Clash TUN 等操作系统级路由。\n\nSources: [wechat-http.ts](src/wechat-http.ts#L36-L40)\n\n## QQ 与钉钉：官方长连接的两条路线\n\nQQ 适配器完全手写官方 Bot API v2 协议：OAuth token 自动刷新、WebSocket Gateway 握手（op 10 HELLO → op 2 IDENTIFY → op 1 心跳）、以及模块化的重连策略。重连辅助函数被抽到独立文件以便单测——瞬态网络错误（DNS 失败、连接重置等）不消耗重连预算，退避间隔按 `[1s, 2s, 5s, 10s, 30s, 60s]` 阶梯递增，服务端下发的 close code 还会被分类为"正常关闭 / 刷新 token / 限速"等不同动作。另一个安全细节是网关 URL 校验：SDK 返回的 WebSocket 地址必须是 `wss://*.qq.com`，否则直接抛错拒绝连接。\n\nSources: [qq.ts](src/qq.ts#L383-L417), [qq-reconnect.ts](src/qq-reconnect.ts#L40-L47), [qq.ts](src/qq.ts#L367-L379)\n\nQQ 的流式输出有严格的适用边界：`stream_messages` 接口**仅 C2C 私聊可用**，且要求附带最近一条入站消息的 `msg_id` 作为被动回复引用——群聊目标或不满足前置条件的场景会直接返回 `undefined` 跳过流式，回落到普通发送。\n\nSources: [im-channel.ts](src/im-channel.ts#L729-L761), [qq-streaming-card.ts](src/qq-streaming-card.ts#L1-L14)\n\n钉钉适配器则把重连职责完全委托给 `dingtalk-stream` SDK（注释记载了一段踩坑史：旧版自建监控误读了 SDK 从未置位的 `registered` 属性，导致工作连接每 15 秒被破坏性重连一次，最终删除监控、信任 SDK 自愈）。接入时还有一个针对性 workaround：临时禁用 axios 全局代理以防 PAC 配置干扰 SDK 内部请求，DWClient 创建完成后立即恢复原值，避免影响同样基于 axios 的飞书 SDK。\n\nSources: [dingtalk.ts](src/dingtalk.ts#L1996-L2058)\n\n钉钉流式输出走 AI Card 六步生命周期：创建卡实例 → 投递 → 切 INPUTING → 按 500ms 节流推送内容流 → 终帧 isFinalize → 切 FINISHED。控制器实现了与飞书相同的公共 API，因此上游的 `feedStreamEventToCard()` 无需修改即可驱动它——这正是四种流式实现能够统一驱动的关键约定。\n\nSources: [dingtalk-streaming-card.ts](src/dingtalk-streaming-card.ts#L1-L32)\n\n## Discord 与 WhatsApp：两种"非典型"接入\n\nDiscord 是唯一默认**关闭**流式输出的渠道（`streamingMode` 默认 `\'off\'`，需显式开启 `\'edit\'`）：开启后通过 `channel.send()` 创建占位消息，再以 500ms 节流的 `message.edit()` 推进内容，超 2000 字符时收尾当前消息并续发新消息，且跨越分割点保持代码围栏状态。它的另一特色是在 `ClientReady` 时向 Discord 注册六个原生 Application Commands（`/clear`、`/list`、`/status`、`/recall`、`/pair`、`/require_mention`），斜杠命令被翻译成与文本命令相同的格式复用同一条命令管线；群组 DM 则因平台限制被显式排除。\n\nSources: [discord-streaming-edit.ts](src/discord-streaming-edit.ts#L1-L16), [im-channel.ts](src/im-channel.ts#L1046-L1050), [discord.ts](src/discord.ts#L799-L832), [discord.ts](src/discord.ts#L56-L58)\n\nWhatsApp 是唯一的**个人号**方案：基于社区逆向的 Baileys 库直连 Meta 的 WebSocket 协议。文件头注释坦诚记录了风险权衡——封号率随 Meta 风控收紧上升，商用应选官方 Cloud API。登录采用 QR 扫码流程，连接状态机为五态（`connecting`/`qr`/`connected`/`disconnected`/`logged_out`），QR 字符串会被渲染成 PNG data URL 供前端 `<img>` 直接展示；登录态经 `useMultiFileAuthState` 持久化到 `authDir`，被 Meta 主动断开（非登出）时延迟 3 秒自动重连，用户主动登出则清除本地凭据。\n\nSources: [whatsapp.ts](src/whatsapp.ts#L1-L19), [whatsapp.ts](src/whatsapp.ts#L59-L87)\n\n## 共性入站管线与流式统一驱动\n\n尽管七个适配器的传输层各异，入站消息在进入编排层之前都经过同一条防御性管线。下图中的每个环节都有独立的模块与测试，适配器只需按序调用：\n\n```mermaid\nflowchart TD\n    A["平台事件到达"] --> B{"LRU 去重缓存<br/>1000 条 / 30 分钟 TTL"}\n    B -- 重复 --> X["丢弃"]\n    B -- 新消息 --> C["normalizeIncomingJid<br/>账号作用域归一化"]\n    C --> D["evaluateChannelAdmission<br/>allow / paired / pair_rejected / deny"]\n    D -- 未配对 --> E["onPairAttempt 配对码验证"]\n    D -- deny --> X\n    E --> F["resolveEffectiveChatJid<br/>解析 workspace / conversation agent 目标"]\n    F --> G["ProcessingLock 会话级互斥"]\n    G --> H["附件下载 im-downloader<br/>图片 MIME 检测"]\n    H --> I["notifyNewImMessage<br/>通知 Web 端与 Agent 编排"]\n```\n\n其中准入决策的四态枚举是渠道与业务层的正式契约——配对建立的是"外部聊天 → 默认 workspace"的所有权关系，而 workspace/session 的细分路由必须等到准入之后才允许查询。\n\nSources: [channel-admission.ts](src/channel-admission.ts#L11-L27), [qq.ts](src/qq.ts#L408-L410)\n\n出站方向，四种流式实现（飞书卡片、钉钉 AI Card、Discord 消息编辑、QQ stream_messages）共同实现了一组同构的公共 API，由主流程的单一入口 `feedStreamEventToCard()` 统一驱动。新增渠道若要支持流式，只需照此约定实现控制器即可接入现有管线，无需触碰事件分发逻辑：\n\n| 实现 | 载体 | 更新节流 | 降级路径 |\n|------|------|---------|---------|\n| FeishuStreamingCardController | CardKit 卡片 + streaming_mode | 文本 300ms / 辅助 800ms | streaming → CardKit v1 → legacy patch |\n| DingTalkStreamingCardController | AI Card streaming 接口 | 500ms（保守 2 QPS） | 失败回落静态消息 |\n| DiscordStreamingEditController | message.edit 编辑 | 500ms | 超 2000 字符拆分为多条 |\n| QQStreamingController | C2C stream_messages | input_state 状态推进 | 失败回落 sendQQMessage |\n\nSources: [index.ts](src/index.ts#L595-L595), [feishu-streaming-card.ts](src/feishu-streaming-card.ts#L1-L12), [dingtalk-streaming-card.ts](src/dingtalk-streaming-card.ts#L11-L32), [discord-streaming-edit.ts](src/discord-streaming-edit.ts#L5-L16), [qq-streaming-card.ts](src/qq-streaming-card.ts#L5-L14)\n\n最后补充可选能力的分布差异，这在排障时很有用——例如"为什么 QQ 群聊没有打字指示器"的答案就写在工厂方法的空实现里（"QQ Bot API v2 does not support typing indicators"）：\n\n| 可选能力 | 飞书 | Telegram | 微信 | QQ | 钉钉 | Discord | WhatsApp |\n|---------|------|----------|------|-----|------|---------|----------|\n| sendImage / sendFile | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |\n| setTyping 有效实现 | ❌（改用精确 ack reaction） | ✅ 租约脉冲 | ✅ 租约 | ❌ 平台不支持 | ❌ SDK 不支持 | ✅ 9 秒脉冲 | ✅ |\n| createStreamingSession | ✅ | ❌ | ❌ | ✅ 仅 C2C | ✅ | ⚠️ edit 模式开启时 | ❌ |\n| logout 凭据撤销 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ 清除 authDir |\n| syncGroups | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |\n| 平台专属扩展 | executeFeishuCapability / reconcileStreamingCard | Forum thread 路由 | 游标持久化 / CDN 解密 | msg_seq 管理 | 回复消息解析 | 历史/频道/服务器查询 | QR 状态机 |\n\nSources: [im-channel.ts](src/im-channel.ts#L332-L344), [im-channel.ts](src/im-channel.ts#L721-L761), [im-manager.ts](src/im-manager.ts#L112-L123), [whatsapp.ts](src/whatsapp.ts#L127-L146)\n\n## 小结与下一步\n\n七大适配器的共同点是"薄翻译层"哲学：平台 SDK 的差异被压缩在 Connection 工厂内部，对外一律呈现 `IMChannel` 接口与标准回调；差异点则以可选方法和渠道专属扩展的形式诚实暴露，而非强行抹平。理解了这一层，就可以继续深入两条主线——想了解**一个账号如何在多群多私聊间划分归属与挂载边界**，请阅读[多账号管理与绑定边界：群聊挂载、私聊 Session 与渠道身份归属](16-duo-zhang-hao-guan-li-yu-bang-ding-bian-jie-qun-liao-gua-zai-si-liao-session-yu-qu-dao-shen-fen-gui-shu)；想了解**消息从 Inbox 到 Outbox 的可靠投递与流式卡片恢复**，请阅读[可靠性状态机：Inbox、Turn、Outbox 与 Streaming Card 的持久投递](17-ke-kao-xing-zhuang-ti-ji-inbox-turn-outbox-yu-streaming-card-de-chi-jiu-tou-di)。若需要回顾消息进入适配器之前的整体编排，可回到[服务启动与消息编排主流程剖析](7-fu-wu-qi-dong-yu-xiao-xi-bian-pai-zhu-liu-cheng-pou-xi)。';export{n as default};

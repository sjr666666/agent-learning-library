const t='## 引言：精确计量是压缩决策的地基\n\nMiniCode 的每一次压缩决策——自动压缩、确定性裁剪、上下文折叠、微压缩——都建立在同一个前提上：**准确知道"当前会话已经消耗了多少输入令牌"**。这个计量问题存在一对天然的矛盾：供应商 API 返回的 `usage` 字段是真实计费口径，但它是一次**快照**，只在每次模型调用结束时给出，无法覆盖调用之后新追加的消息；本地字符估算可以随时对任意消息序列计算，但它只是近似值，与供应商的 tokenizer 之间存在系统性偏差。\n\n本页的记账系统正是为了解决这对矛盾而设计：**以最后一次可信的供应商用量为锚点（boundary），其后的消息用本地估算补齐；当会话中不存在任何可用用量时，整体退化为纯估算**。这个"用量优先、估算兜底"的策略，让压缩阈值既能信任真实计费数据，又不会因缺失快照而失灵。下游各压缩策略消费的 `ContextStats` 结构、以及 TUI 状态栏中的 `ctx` 徽标，都源于这一层计量结果。\n\nSources: [token-estimator.ts](src/utils/token-estimator.ts#L9-L32)、[agent-loop.ts](src/agent-loop.ts#L174-L219)\n\n## 一、三层记账来源模型\n\n`TokenAccountingSource` 用三个枚举值描述一次记账结果的精确程度，这是整个系统的核心抽象：\n\n```mermaid\nflowchart TD\n    A[扫描消息序列<br/>从尾部向前] --> B{找到带有效<br/>providerUsage 的消息?}\n    B -->|是| C{边界之后<br/>还有消息?}\n    C -->|否| D[provider_usage<br/>totalTokens = 用量总和<br/>isExact = true]\n    C -->|是| E[provider_usage_plus_estimate<br/>totalTokens = 用量 + 尾部估算<br/>isExact = false]\n    B -->|否| F{存在被标记<br/>usageStale 的消息?}\n    F -->|是| G[estimate_only<br/>stale = true, 附带原因]\n    F -->|否| H[estimate_only<br/>reason = no provider usage available]\n    D & E & G & H --> I[ContextStats<br/>utilization / warningLevel]\n```\n\n| 来源 | 触发条件 | totalTokens 构成 | isExact | 精确度语义 |\n|---|---|---|---|---|\n| `provider_usage` | 最后一条带用量消息恰是会话末尾 | 仅用供应商 `totalTokens` | `true` | 与计费口径完全一致 |\n| `provider_usage_plus_estimate` | 用量边界之后还有新消息 | 边界用量 + 尾部逐条估算 | `false` | 历史部分精确，新增部分近似 |\n| `estimate_only` | 无任何可用用量（或全部过期） | 全量本地估算 | `false` | 全程近似，且携带失效原因 |\n\n`TokenAccountingResult` 还额外携带 `usageBoundary`（边界消息的索引与 ID）与 `stale`/`reason` 字段。边界 ID 对 `assistant_tool_call` 消息取 `toolUseId`，使外部消费者能精确定位"用量覆盖到哪一条消息为止"。\n\nSources: [token-estimator.ts](src/utils/token-estimator.ts#L4-L21)、[token-estimator.ts](src/utils/token-estimator.ts#L120-L156)\n\n## 二、供应商用量的采集与归一化\n\n### 2.1 归一化：把供应商字段折叠成统一口径\n\n不同供应商的 usage 字段结构各异。Anthropic 适配器把 `input_tokens`、`cache_creation_input_tokens`、`cache_read_input_tokens` 三者和为 `inputTokens`，与 `output_tokens` 相加得到 `totalTokens`，并标注 `source: \'anthropic\'`。值得注意的是缓存读写令牌被**计入输入侧**——这是正确的：缓存命中的 token 虽然折扣计费，但依然占据上下文窗口。若总和为 0（例如流式响应缺失 usage），则返回 `undefined`，让记账系统自然落入估算分支。\n\nSources: [anthropic-adapter.ts](src/anthropic-adapter.ts#L193-L208)、[anthropic-adapter.ts](src/anthropic-adapter.ts#L29-L34)、[types.ts](src/types.ts#L1-L6)\n\n### 2.2 附加规则：用量只挂在三类消息上\n\n代理循环中的 `withProviderUsage` 是一个纯函数包装器：只有当消息角色是 `assistant`、`assistant_progress` 或 `assistant_tool_call` 时，才把 `AgentStep.usage` 写入消息的 `providerUsage` 元数据。这里有三个值得注意的细节：\n\n- **多工具调用只给最后一条**：当一次响应包含多个 tool_call 时，`next.usage` 代表的是"发出这些调用时的完整输入上下文"，因此只附加到最后一个 `assistant_tool_call` 消息上，避免同一份用量被重复累计；\n- **带工具调用的 assistant 文本不挂用量**：当 `next.calls.length > 0` 时，assistant 文本消息的用量被置为 `undefined`，因为同一轮里 `assistant_tool_call` 消息已承载用量；\n- **元数据不回流**：适配器向供应商序列化消息时只映射角色与内容，`providerUsage` 纯属内部记账，永远不会随下一次请求发送出去。\n\nSources: [agent-loop.ts](src/agent-loop.ts#L35-L48)、[agent-loop.ts](src/agent-loop.ts#L326-L367)、[agent-loop.ts](src/agent-loop.ts#L416-L428)、[anthropic-adapter.ts](src/anthropic-adapter.ts#L412-L436)\n\n### 2.3 端到端验证\n\n`provider-usage-ingestion.test.ts` 用桩适配器验证了完整链路：模型返回带 `usage` 的 `AgentStep` → `runAgentTurn` 把用量写入最后一条 assistant 消息 → `tokenCountWithEstimation` 识别出 `source = \'provider_usage\'` 且 `totalTokens = 50`。\n\nSources: [provider-usage-ingestion.test.ts](test/provider-usage-ingestion.test.ts#L8-L42)\n\n## 三、用量边界与尾部估算算法\n\n`tokenCountWithEstimation` 是整个记账系统的核心算法。它从消息序列**尾部向前**扫描，寻找最后一条满足条件的消息——角色为 assistant 系列、携带 `providerUsage`、且未被标记为 `usageStale`。找到后即停止，因为越靠后的边界越贴近当前真实上下文：\n\n```mermaid\nflowchart LR\n    subgraph 消息序列\n        M1[system] --- M2[user] --- M3[assistant<br/>usage=125] --- M4[assistant_tool_call<br/>usage=800] --- M5[tool_result] --- M6[user]\n    end\n    style M4 fill:#d4edda\n    style M5 fill:#fff3cd\n    style M6 fill:#fff3cd\n```\n\n上图中 M4 是最后一个有效用量边界。M5、M6（边界之后的尾部）逐条用估算器补算，`usageBoundary.messageIndex = 3`。由于 M4 是 `assistant_tool_call`，`usageBoundary.messageId` 取其 `toolUseId`，方便消费者把边界对应回具体的工具调用。\n\n算法的三个分支与测试一一对应：边界恰为末尾时得到 `provider_usage`（精确）；边界之后有尾巴时得到 `provider_usage_plus_estimate`；扫描全程无有效用量时，再检查是否有 `usageStale` 消息——若有，说明历史用量因压缩而失效，`estimate_only` 结果会携带 `stale: true` 与具体原因。\n\nSources: [token-estimator.ts](src/utils/token-estimator.ts#L92-L118)、[token-estimator.ts](src/utils/token-estimator.ts#L125-L156)、[token-estimator.test.ts](test/token-estimator.test.ts#L139-L233)\n\n## 四、角色敏感的字符→令牌估算器\n\n本地估算采用"角色敏感"的字符密度模型：不同角色承载的信息密度不同，因此每字符对应的令牌数不同。`CHARS_PER_TOKEN` 定义了九种角色的换算比率：\n\n| 角色 | 每令牌字符数 | 取值依据 |\n|---|---|---|\n| `tool_result` | 2.0 | 密度最高——工具输出（文件内容、命令回显）高度压缩 |\n| `assistant_tool_call` | 2.5 | JSON 化的工具输入参数密集 |\n| `user` / `assistant_thinking` | 3.0 | 用户指令与思考块 |\n| `system` / `assistant` / `assistant_progress` / `context_summary` / `snip_boundary` | 3.5 | 自然语言为主，密度较低 |\n\n`messageContentLength` 按角色取长度：文本类消息直接取 `content.length`；`assistant_thinking` 取思考块 JSON 序列化长度；`assistant_tool_call` 取 `input` 字段的 JSON 序列化长度。估算值 `Math.ceil(length / ratio)` 保证至少为 1。测试特意验证了**相同长度的内容，`tool_result` 估算值大于 assistant**——这反映了大工具结果占用上下文更猛的事实，也是微压缩与结果落盘机制的前提。\n\n另一个关键细节是 `CLEAR_MARKER`（`\'[Output cleared for context space]\'`）：微压缩把旧工具结果替换成这个占位串后，它按 `tool_result` 角色估算仅约 21 个令牌，使被清空的工具结果在记账上几乎"零成本"。\n\nSources: [token-estimator.ts](src/utils/token-estimator.ts#L34-L90)、[token-estimator.test.ts](test/token-estimator.test.ts#L25-L41)、[microcompact.ts](src/compact/microcompact.ts#L34-L47)\n\n## 五、陈旧标记：压缩后用量快照的失效\n\n供应商用量是"某一时刻完整消息序列"的快照。一旦消息序列被重写——例如压缩把中间消息折叠成 `context_summary`、裁剪插入 `snip_boundary`——旧快照就**不再对应**当前序列，直接使用会造成严重误判。`markProviderUsageStale` 正是为此设计的失效开关：它只对携带 `providerUsage` 的 assistant 系列消息追加 `usageStale: true` 和原因字符串。\n\n三个压缩策略在重写消息后都会对**保留的消息**统一打上陈旧标记：\n\n| 调用点 | 失效原因 | 作用时机 |\n|---|---|---|\n| `compact.ts` | `conversation was compacted after this provider usage was recorded` | 保留消息进入 `messagesToKeep` |\n| `snipCompact.ts` | `conversation was snip-compacted after this provider usage was recorded` | 裁剪后的全部剩余消息 |\n| `context-collapse.ts` | `CONTEXT_COLLAPSE_STALE_REASON` | 被折叠区间的相邻消息 |\n\n失效后，`tokenCountWithEstimation` 跳过这些消息的用量，最终落入 `estimate_only` 分支并附带 `stale: true`。测试验证了这一点：压缩场景下即使残留旧用量（1050 令牌），结果也不会采用它，而是全量估算且 `reason` 包含 `compacted`。\n\nSources: [token-estimator.ts](src/utils/token-estimator.ts#L158-L175)、[compact.ts](src/compact/compact.ts#L141-L148)、[snipCompact.ts](src/compact/snipCompact.ts#L395-L400)、[context-collapse.ts](src/compact/context-collapse.ts#L228-L245)、[token-estimator.test.ts](test/token-estimator.test.ts#L204-L232)\n\n## 六、模型上下文窗口解析\n\n估算出的令牌数必须与"这个模型到底能装多少输入"对比才有意义。`getModelContextWindow` 维护了一张按模型家族匹配的规则表，输出三要素：`contextWindow`（总窗口）、`outputReserve`（为输出预留的令牌）、`effectiveInput`（实际可用输入 = 两者之差）。\n\n规则匹配采用**小写化 + 子串包含 + 顺序优先**：遍历规则数组，命中任一 pattern 即返回。这带来了两个行为特征——大小写不敏感（`CLAUDE-OPUS-4-6` 与 `claude-opus-4-6` 等价）、部分匹配即可（`anthropic/claude-3-5-sonnet-latest` 命中 3.5 Sonnet）。规则顺序有讲究：更具体的型号（如 `gpt-5-codex`）排在同族通用名之前，避免被通用 pattern 抢先吞掉。未知模型回落到 128k/8k 的保守默认值。\n\n| 模型家族 | contextWindow | outputReserve | effectiveInput |\n|---|---|---|---|\n| Claude 4.6/4.1/4 / 3.x Sonnet | 200,000 | 16,000 / 8,192 / 4,096 | 窗口减预留 |\n| GPT-5 / GPT-4o / GPT-4 | 128,000 | 16,000 / 16,384 / 8,192 | 窗口减预留 |\n| GPT-4.1 系列 | 1,047,576 | 16,000 | ≈ 1,031,576 |\n| Gemini 2.5 系列 | 1,048,576 | 16,000 | ≈ 1,032,576 |\n| DeepSeek Chat / Reasoner | 128,000 | 4,000 / 16,000 | 124,000 / 112,000 |\n| MiniMax-M3 / M2.7 | 1,000,000 / 204,800 | 16,000 | 984,000 / 188,800 |\n\n与窗口解析配套的还有 `utils/context.ts` 中的 `resolveMaxOutputTokens`：它按模型查上限，并把用户配置钳制在 `upperLimit` 之内——这保证输出预留不会因用户误配而被击穿。二者共同构成"输入预算"的完整边界。\n\nSources: [model-context.ts](src/utils/model-context.ts#L1-L17)、[model-context.ts](src/utils/model-context.ts#L19-L149)、[context.ts](src/utils/context.ts#L91-L116)、[model-context.test.ts](test/model-context.test.ts#L5-L96)\n\n## 七、ContextStats：利用率与四级告警\n\n`computeContextStats` 把记账结果与窗口信息合成最终消费结构。核心公式：\n\n```\nutilization = min(1, accounting.totalTokens / window.effectiveInput)\n```\n\n利用率**封顶为 1**，避免极端超限时出现 >100% 的干扰数值。基于利用率划分四级告警，阈值与 `compact/constants.ts` 中的压缩触发阈值完全对齐：\n\n| 告警级别 | 利用率区间 | 颜色 | 对应压缩策略 |\n|---|---|---|---|\n| `normal` | < 50% | 绿 | 无 |\n| `warning` | ≥ 50% | 黄 | 微压缩（0.50） |\n| `critical` | ≥ 85% | 红 | 自动压缩（0.85） |\n| `blocked` | ≥ 95% | 亮红 | 发送被阻断（0.95） |\n\nTUI 顶栏的 `ctx` 徽标把这份统计渲染成一行：百分比 + 10 格进度条（`█`/`░`）+ 来源标签。来源标签有三种：`usage`（纯供应商用量）、`usage+est`（用量加估算）、`est`（纯估算）——用户一眼即可分辨当前数字的可信度。\n\nSources: [token-estimator.ts](src/utils/token-estimator.ts#L177-L206)、[constants.ts](src/compact/constants.ts#L1-L5)、[chrome.ts](src/tui/chrome.ts#L214-L247)、[context-badge.test.ts](test/context-badge.test.ts#L50-L63)\n\n## 八、记账结果的下游消费\n\n`ContextStats` 是代理循环中所有压缩决策的公共输入。循环每步开头都调用 `computeContextStats`，随后依次执行 snip 裁剪、微压缩、上下文折叠，每个策略都复用同一份计量结果；步骤结束后通过 `onContextStats` 回调把最新统计推给 TUI 渲染：\n\n```mermaid\nflowchart LR\n    A[computeContextStats] --> B[snipCompact<br/>阈值 0.70]\n    A --> C[microcompact<br/>阈值 0.50]\n    A --> D[context-collapse<br/>阈值 0.75]\n    A --> E[autoCompact<br/>阈值 0.85]\n    A --> F[ctx 徽标渲染]\n    B & C & D & E & F --> G[onContextStats → TUI]\n```\n\n| 消费者 | 使用的字段 | 阈值/触发条件 | 所属页面 |\n|---|---|---|---|\n| 自动压缩 `shouldAutoCompact` | `utilization`, `accounting.source` | ≥ 0.85 | [自动压缩：触发阈值与失败熔断](12-zi-dong-ya-suo-hong-fa-yu-zhi-yu-shi-bai-rong-duan) |\n| 确定性裁剪 `snipCompactConversation` | `totalTokens`, `effectiveInput` | ≥ 0.70 | [确定性裁剪压缩：保护编辑与错误轮次的安全删除](13-que-ding-xing-cai-jian-ya-suo-bao-hu-bian-ji-yu-cuo-wu-lun-ci-de-an-quan-shan-chu) |\n| 上下文折叠 `applyContextCollapseIfNeeded` | `utilization`（投影视图） | ≥ 0.75 | [上下文折叠投影：可摘要区间识别与替换](14-shang-xia-wen-zhe-die-tou-ying-ke-zhai-yao-qu-jian-shi-bie-yu-ti-huan) |\n| 微压缩 `microcompact` | `utilization` | ≥ 0.50 | [微压缩：旧工具结果的占位清理](15-wei-ya-suo-jiu-gong-ju-jie-guo-de-zhan-wei-qing-li) |\n| 手动压缩 `compactConversation` | `tokenCountWithEstimation` 前后对比 | 用户触发 | [会话管理入门](6-hui-hua-guan-li-ru-men-bao-cun-hui-fu-zhong-ming-ming-yu-fen-cha) |\n\n值得注意的是**折叠策略在投影视图上重新记账**（`computeContextStats(currentProjected, model)`）：因为折叠尚未实际改写消息，它必须基于"假设折叠后的序列"估算，才能判断目标利用率是否达成。这体现了记账层"可预测、可回放"的设计——任何压缩策略都能在真实改写前先用同一套计量验证收益。\n\nSources: [agent-loop.ts](src/agent-loop.ts#L179-L219)、[auto-compact.ts](src/compact/auto-compact.ts#L33-L43)、[snipCompact.ts](src/compact/snipCompact.ts#L402-L419)、[context-collapse.ts](src/compact/context-collapse.ts#L612-L627)、[microcompact.ts](src/compact/microcompact.ts#L6-L13)\n\n## 总结\n\n"供应商用量优先"的记账体系可以概括为一条主线：**精确的用量快照打底，近似的估算兜底，失效的标记剔除**。供应商返回的真实 usage 在每次调用后锚定在消息序列的尾部，之后新增的消息用角色敏感的字符密度模型补算；一旦压缩重写了序列，旧快照立即失效并让位给全量估算。模型窗口规则把原始令牌数折算成可比的利用率，驱动四级告警与 TUI 徽标，也驱动下游每一种压缩策略的触发与验证。这条"采集 → 锚定 → 补算 → 失效 → 折算"的流水线，让 MiniCode 在不引入重型 tokenizer 依赖的前提下，尽可能贴近供应商的真实计费口径。\n\nSources: [token-estimator.ts](src/utils/token-estimator.ts#L125-L156)、[model-context.ts](src/utils/model-context.ts#L132-L149)、[agent-loop.ts](src/agent-loop.ts#L174-L219)\n\n---\n\n**延伸阅读**：本页聚焦计量层本身。用量边界如何触发压缩见 [自动压缩：触发阈值与失败熔断](12-zi-dong-ya-suo-hong-fa-yu-zhi-yu-shi-bai-rong-duan)；供应商用量从何而来见 [Anthropic 适配器：重试退避、限流与思考块保留](10-anthropic-gua-pei-qi-zhong-shi-tui-bi-xian-liu-yu-si-kao-kuai-bao-liu)；`CLEAR_MARKER` 占位与工具结果计账的完整故事见 [大工具结果落盘：预览替换与稳定记账](16-da-gong-ju-jie-guo-luo-pan-yu-lan-ti-huan-yu-wen-ding-ji-zhang)。';export{t as default};

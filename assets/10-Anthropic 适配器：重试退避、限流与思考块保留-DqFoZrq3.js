const t='`AnthropicModelAdapter` 是 MiniCode 与 Anthropic 兼容 Messages API 之间的唯一协议通道。它实现 `ModelAdapter` 接口，承担三项核心职责：把内部消息模型翻译成 Anthropic 载荷、在 HTTP 层实现重试退避与限流容错、以及在工具调用往返中完整保留模型的思考块（thinking block）。本文逐一剖析这三条主线，并说明它们如何与代理循环协作。\n\n## 适配器在代理循环中的位置\n\n整个执行链路以 `ModelAdapter` 接口为抽象边界：代理循环只调用 `next(messages)` 并消费返回的 `AgentStep`，完全不感知底层 HTTP 协议细节（[src/types.ts](src/types.ts#L88-L90)）。`AnthropicModelAdapter` 在启动时被构造，依赖注入工具注册表与运行时配置加载器（[src/index.ts](src/index.ts#L81-L84)）：\n\n```mermaid\nflowchart LR\n    A[runAgentTurn<br/>代理循环] -->|"next(messages)"| B[ModelAdapter 接口]\n    B --> C[AnthropicModelAdapter]\n    C -->|"POST /v1/messages"| D[Anthropic 兼容 API]\n    C --> E[ToolRegistry<br/>工具 schema 注入]\n    C --> F[loadRuntimeConfig<br/>baseUrl / token / model]\n    B --> G[MockModelAdapter<br/>离线演示模式]\n```\n\n适配器的输入是内部 `ChatMessage[]`，输出是 `AgentStep`（`assistant` 或 `tool_calls` 两种形态），中间经过"序列化 → 网络请求 → 反序列化"三个阶段。设计上的关键约束是：**适配器不做业务决策**——是否重试空响应、是否继续工具循环，都属于代理循环的职责；适配器只负责"协议翻译"与"网络韧性"。\n\n## 请求构建：从内部消息到 Anthropic 载荷\n\n序列化入口是 `toAnthropicMessages()`，它把内部九种消息角色映射为 Anthropic 的 `system` / `user` / `assistant` 三种消息，每种角色对应一种或多种内容块（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L234-L302)）：\n\n| 内部角色 | Anthropic 映射 | 说明 |\n|---|---|---|\n| `system` | 独立 `system` 参数 | 多个 system 消息以 `\\n\\n` 拼接 |\n| `user` | `user` 消息 + text 块 | 直接透传文本 |\n| `assistant_thinking` | `assistant` 消息 + 原始思考块 | 见"思考块保留"一节 |\n| `assistant` / `assistant_progress` | `assistant` 消息 + text 块 | progress 包裹 `<progress>…</progress>` 标记 |\n| `assistant_tool_call` | `assistant` 消息 + `tool_use` 块 | id/name/input 一一对应 |\n| `tool_result` | `user` 消息 + `tool_result` 块 | 携带 `is_error` 标志 |\n| `context_summary` | `user` 消息 + 带前缀的文本 | 前缀 `[Context Summary from earlier conversation]` |\n| `snip_boundary` | `user` 消息 + 占位文本 | 使用 `buildAnthropicSnipBoundaryText()` 的固定文案（[src/compact/snipCompact.ts](src/compact/snipCompact.ts#L365-L372)） |\n\n角色映射本身不复杂，真正值得注意的机制是 `pushAnthropicMessage()` 的**同角色合并**：如果新块的 role 与当前最后一条消息相同，则直接追加到该消息的 `content` 数组，否则新建消息（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L220-L232)）。这个看似简单的合并策略是整个思考块保留机制的地基——它保证了相邻的 thinking 块与 tool_use 块落在同一条 assistant 消息里，而多个并行 tool_result 也能合并进同一条 user 消息。\n\n请求的 URL、鉴权与 `max_tokens` 在 `next()` 中组装（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L310-L340)）。baseUrl 默认 `https://api.anthropic.com`，可被 `ANTHROPIC_BASE_URL` 覆盖（[src/config.ts](src/config.ts#L215-L218)）；鉴权优先 `Authorization: Bearer`，其次 `x-api-key`；`max_tokens` 由 `resolveMaxOutputTokens()` 依据模型名查表决定，规则表覆盖 Claude、GPT、Gemini、DeepSeek 各系列，并以上限值钳制用户配置（[src/utils/context.ts](src/utils/context.ts#L16-L116)）。\n\n## 重试与退避策略\n\n网络层重试由三组常量与三个纯函数控制（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L14-L78)）：\n\n| 常量 | 值 | 含义 |\n|---|---|---|\n| `DEFAULT_MAX_RETRIES` | 4 | 默认最大重试次数（总尝试 5 次） |\n| `BASE_RETRY_DELAY_MS` | 500 | 首次重试基础延迟 |\n| `MAX_RETRY_DELAY_MS` | 8 000 | 退避延迟上限 |\n\n重试判定规则 `shouldRetryStatus()` 只对两类状态码放行：**429（限流）** 与 **5xx（服务端错误）**；4xx 客户端错误（如 400、401、403）一律不重试，避免把无效请求重复轰炸（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L50-L52)）。最大重试次数可通过环境变量 `MINI_CODE_MAX_RETRIES` 覆盖，非有限数字或负数时回退到默认值 4（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L42-L48)）。\n\n退避延迟由 `getRetryDelayMs()` 计算（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L68-L78)）：\n\n```mermaid\nsequenceDiagram\n    participant L as 代理循环\n    participant A as AnthropicModelAdapter\n    participant H as fetch /v1/messages\n    L->>A: next(messages)\n    loop attempt = 0..maxRetries\n        A->>H: POST 请求体\n        alt response.ok\n            H-->>A: 200 载荷\n        else 429 或 5xx 且未耗尽重试\n            H-->>A: 状态码 + retry-after?\n            Note over A: retry-after 存在 → 精确等待<br/>否则 → 指数退避 + 抖动\n            A->>A: sleep(延迟)\n        else 其他 4xx 或已耗尽\n            H-->>A: 错误状态码\n            Note over A: 直接抛出错误\n        end\n    end\n    A-->>L: AgentStep 或 Error\n```\n\n无 `retry-after` 时采用**带抖动的指数退避**：`base = min(500 × 2^(attempt-1), 8000)`，抖动为 `base` 的 0~25% 随机增量。第 1 次重试约 500~625ms，第 2 次约 1000~1250ms，依此类推直到 8000ms 封顶。抖动的作用是避免多个客户端在同一次限流后同时苏醒形成"惊群效应"。由于每次重试都重新执行 `fetch`（请求体已在循环外序列化，内容不变），整个重试过程对调用方完全透明，只是整体延迟可能累加数秒。\n\n## 限流：429 与 Retry-After\n\n429 状态码的专用处理是 `parseRetryAfterMs()`，它需要兼容两种 `Retry-After` 响应头格式（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L54-L66)）：\n\n1. **delta-seconds**：形如 `Retry-After: 3`，直接乘以 1000 转为毫秒；\n2. **HTTP-date**：形如 `Retry-After: Wed, 21 Oct 2025 07:28:00 GMT`，用 `Date.parse` 解析后减去当前时间。\n\n两种格式都无法解析时返回 `null`，退化为指数退避。**优先级的取舍**是：只要 `retry-after` 可解析，就完全覆盖指数退避公式（`getRetryDelayMs` 第一分支直接返回 `retryAfterMs`），因为服务端对限流窗口的估算通常比客户端公式更准确（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L68-L71)）。\n\n需要留意的是，`readJsonBody()` 与 `extractErrorMessage()` 保证了错误响应的可读性：即使响应体不是合法 JSON，也会把原始文本回退为错误消息；多层嵌套的 `{ error: { message } }`、`{ error: string }`、`{ message }` 结构都会被提取，最终兜底为 `Model request failed: {status}`（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L80-L131)）。这意味着用户看到的不是晦涩的 HTTP 状态码，而是模型提供方返回的真实错误文本。\n\n## 思考块保留：往返闭环\n\n思考块保留是 MiniCode 与 Claude 系模型协作的核心能力。Anthropic 的 Messages API 要求：当 `assistant` 消息包含 `thinking` 块时，它必须位于该消息的**开头**，且后续同一消息内的 `tool_use` 才能被正确校验。MiniCode 通过三层机制保证这一点。\n\n**第一层：内部存储**。`ProviderThinkingBlock` 只约束 `type` 为 `\'thinking\' | \'redacted_thinking\'`，其余字段透传（签名 `signature`、思考内容 `thinking` 等原样保留）（[src/types.ts](src/types.ts#L14-L17)）。代理循环在每次拿到 `AgentStep` 后，通过 `appendThinkingBlocks()` 把思考块追加为独立的 `assistant_thinking` 消息（[src/agent-loop.ts](src/agent-loop.ts#L163-L172)），该角色在会话持久化中对应 `thinking` 事件类型（[src/session.ts](src/session.ts#L61-L74)）。\n\n**第二层：序列化**。`toAnthropicMessages()` 遇到 `assistant_thinking` 时，逐块以 `assistant` 角色推送；借助 `pushAnthropicMessage` 的同角色合并，如果前一条消息是 `assistant_tool_call`（上一轮的 tool_use），思考块会被合并进**同一条** assistant 消息，且由于遍历顺序在 tool_use 之前，天然满足"thinking 在 tool_use 之前"的协议要求（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L253-L258)）。\n\n**第三层：反序列化与恢复**。响应解析时 `isThinkingBlock()` 同时识别 `thinking` 与 `redacted_thinking`（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L149-L151)），收集进 `thinkingBlocks` 随 `AgentStep` 返回，代理循环再将其写回消息历史（[src/agent-loop.ts](src/agent-loop.ts#L316-L330)）。由此构成闭环：\n\n```mermaid\nflowchart LR\n    A[响应含 thinking 块] -->|isThinkingBlock 捕获| B[AgentStep.thinkingBlocks]\n    B -->|appendThinkingBlocks| C[assistant_thinking 消息]\n    C -->|pushAnthropicMessage 合并| D[同一 assistant 消息<br/>thinking + tool_use]\n    D -->|下一次请求| A\n```\n\n测试文件用三个用例锁定了这一闭环（[test/anthropic-thinking-roundtrip.test.ts](test/anthropic-thinking-roundtrip.test.ts#L16-L258)）：\n\n| 用例 | 验证点 |\n|---|---|\n| 工具结果后继续 | 第二次请求的 assistant 消息块序严格为 `[\'thinking\', \'tool_use\']`，思考内容与签名原样保留 |\n| 并行工具调用 | 两个 tool_use 与一个 thinking 合并进**同一条** assistant 消息；两个 tool_result 合并进同一条 user 消息 |\n| 最终回答保留 | 不涉及工具轮次时，最终 `[\'thinking\', \'text\']` 也会在下一轮用户提示时原样回传 |\n\n除此之外，代理循环还处理一种特殊恢复场景：当响应**为空**且 `stop_reason` 为 `pause_turn` 或 `max_tokens`、同时诊断信息中出现 `thinking` 块时，判定为"思考阶段被截断"，自动追加续写提示并重试（最多 3 次），提示语明确要求"继续下一步具体操作，不要重复之前的计划"（[src/agent-loop.ts](src/agent-loop.ts#L92-L110) 与 [src/agent-loop.ts](src/agent-loop.ts#L264-L290)）。这一机制让 `max_tokens` 截断不再导致整轮对话失败，而是把思考块当作可恢复的中间状态。\n\n## 响应解析与诊断\n\n响应体的内容块按四种方式分流（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L375-L404)）：text 块汇入 `textParts` 等待后续标记解析；`tool_use` 块转换为 `ToolCall`（id、toolName、input）；thinking 块进入 `thinkingBlocks`；**未知类型块既不丢弃也不报错**，而是记入 `ignoredBlockTypes` 集合——这保证了未来 Anthropic 新增块类型时适配器不会崩溃，只会把未知类型透传给诊断层。\n\n文本标记解析由 `parseAssistantText()` 完成（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L153-L187)）：识别 `<final>` / `[FINAL]` / `<progress>` / `[PROGRESS]` 四种前缀，剥离后返回内容与 `kind`（`final` 或 `progress`）。这是模型与代理循环之间约定俗成的"收尾信令"——只有带 `final` 标记的文本才被当作最终回答，否则在多轮工具循环中可能被误判为任务完成。\n\n最终返回的 `AgentStep` 附带 `StepDiagnostics`：`stopReason`、`blockTypes`、`ignoredBlockTypes` 三项（[src/types.ts](src/types.ts#L63-L67)）。当模型连续返回空响应时，代理循环把这些诊断信息格式化后写入用户可见的兜底消息（如 `stop_reason=…; blocks=thinking,tool_use; ignored=…`），让用户能理解"模型为什么没输出"（[src/agent-loop.ts](src/agent-loop.ts#L70-L90)）。\n\n## 用量归一化\n\nAnthropic 的 usage 结构包含 `input_tokens`、`output_tokens`、`cache_creation_input_tokens`、`cache_read_input_tokens` 四个字段。`normalizeAnthropicUsage()` 将前两者与两类缓存令牌合并为统一的 `ProviderUsage`（input/output/total + `source: \'anthropic\'`），且当总令牌数 ≤ 0 时返回 `undefined`，避免零用量污染下游的上下文记账（[src/anthropic-adapter.ts](src/anthropic-adapter.ts#L193-L208)）。这一归一化是"供应商用量优先的上下文记账"（见下节交叉引用）的数据入口——后续的令牌估算与自动压缩都依赖这份统一的用量结构。\n\n## 边界与权衡\n\n把观察到的实现细节放在一起，可以总结出适配器的设计取舍：\n\n| 设计决策 | 收益 | 代价 |\n|---|---|---|\n| 同角色消息合并 | 满足 Anthropic 协议对 thinking/tool_use 顺序与 tool_result 分组的严格要求 | 连续 user 文本会被拼进同一消息，依赖 API 对多 text 块的兼容性 |\n| 未知块类型静默忽略 | 对 API 演进具备前向兼容性 | 新类型块的语义可能丢失，仅靠诊断信息可追溯 |\n| `retry-after` 优先于退避公式 | 尊重服务端限流窗口，减少无效重试 | 服务端给出的等待时间可能偏保守 |\n| 每次重试原样重发同一请求体 | 实现简单，幂等性由上游 API 语义保证 | 不适用于非幂等端点（当前仅用于只读生成请求） |\n\n一个值得注意的克制点：适配器**不缓存、不重放**工具执行结果。重试只发生在 HTTP 层，一旦请求成功返回 `tool_calls`，代理循环就会执行工具并携带结果进入下一轮——思考块此时已通过 `assistant_thinking` 消息回到请求中，形成下一轮请求的一部分。\n\n## 延伸阅读\n\n- [代理循环：model → tool → model 的多步执行流程](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng) — 适配器的调用方，空响应重试与思考截断恢复的决策逻辑所在\n- [统一工具协议：注册、Schema 校验与集中执行](8-tong-gong-ju-xie-yi-zhu-ce-schema-xiao-yan-yu-ji-zhong-zhi-xing) — `ToolRegistry` 如何为适配器提供工具 schema\n- [结构化消息模型：角色类型与工具调用编排](9-jie-gou-hua-xiao-xi-mo-xing-jiao-se-lei-xing-yu-gong-ju-diao-yong-bian-pai) — 本文依赖的 `ChatMessage` 九种角色定义\n- [供应商用量优先的上下文记账与令牌估算](11-gong-ying-shang-yong-liang-you-xian-de-shang-xia-wen-ji-zhang-yu-ling-pai-gu-suan) — 归一化用量的下游消费方';export{t as default};

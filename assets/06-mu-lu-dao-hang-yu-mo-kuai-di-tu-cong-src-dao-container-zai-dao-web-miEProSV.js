const e='初次打开 Miniclaw 仓库的开发者，往往会在根目录的 `src/` 下看到 168 个 TypeScript 文件而感到迷失。本页是整个仓库的"地图页"：我们将自顶向下拆解四个独立软件包（后端、容器 Runner、Web 前端、Electron 壳）与一个共享类型层的目录组织方式，解释每个目录的存在理由、命名规律以及它们之间的构建期协作机制。掌握这张地图后，你在后续章节遇到任何文件路径都能立刻定位它的职责归属。\n\n## 一、仓库全景：四个包 + 一个共享层\n\nMiniclaw 不是单体仓库里的单个应用，而是**四个各自拥有独立 `package.json` 的包**加上一层共享类型源码。根目录本身就是一个包——后端服务，名为 `miniclaw`，入口是编译产物 `dist/index.js`（由 `src/index.ts` 编译而来），开发时用 `tsx` 直接运行 TS 源码；`npm run dev:all` 会用 concurrently 同时拉起后端和前端两个开发进程，而 `npm run build:all` 则按 backend、web、agent-runner 的分组顺序依次构建三个包 [package.json](package.json#L6-L24)。\n\n另外三个包分别是：容器内的 Agent 执行器 `miniclaw-agent-runner`（位于 `container/agent-runner/`）、React 前端 `miniclaw-web`（位于 `web/`）、以及桌面壳 `miniclaw-desktop`（位于 `electron/`）。它们互不共享 `node_modules`，依赖各自安装、各自锁定版本——例如后端和 Runner 都依赖 `@earendil-works/pi-coding-agent@0.84.2`，但版本声明写在各自的清单里 [container/agent-runner/package.json](container/agent-runner/package.json#L1-L18)、[web/package.json](web/package.json#L13-L42)。\n\n```mermaid\nflowchart TB\n    subgraph repo["Miniclaw 仓库"]\n        subgraph shared["shared/ 共享类型层（唯一真源）"]\n            S["stream-event.ts<br/>image-detector.ts<br/>channel-prefixes.ts"]\n        end\n        subgraph backend["根目录 = 后端包 miniclaw"]\n            SRC["src/ 168 个模块<br/>routes/ · feishu-cards/<br/>im-safety/ · middleware/"]\n        end\n        subgraph runnerpkg["container/agent-runner 包"]\n            AR["src/runtime/pi/<br/>prompts/ · tests/"]\n        end\n        subgraph webpkg["web 包 miniclaw-web"]\n            WEB["pages/ · stores/<br/>components/ · hooks/"]\n        end\n        subgraph elec["electron 包 miniclaw-desktop"]\n            E["main/ · preload/<br/>各仅 1 个源文件"]\n        end\n    end\n    S -- "make sync-types 构建期复制" --> SRC\n    S -- "复制" --> WEB\n    S -- "复制" --> AR\n    SRC -- "stdin/stdout + IPC 驱动" --> AR\n    WEB -- "HTTP + WebSocket" --> SRC\n    E -- "加载 Web UI + 桥接后端" --> WEB\n```\n\n下表是根目录一级条目的完整地图，可作为你日常导航的速查卡：\n\n| 目录/文件 | 所属包 | 职责 | 关键入口 |\n|---|---|---|---|\n| `src/` | 后端 miniclaw | 认证、API、队列、调度、七渠道接入、ACL、SQLite 持久化 | `src/index.ts` |\n| `container/` | 容器镜像 + Runner 包 | Dockerfile 组装、非 root 入口脚本、Agent 执行器 | `agent-runner/src/pi-index.ts` |\n| `web/` | 前端 miniclaw-web | React 管理界面与聊天工作台 | `src/main.tsx` |\n| `electron/` | 桌面壳 miniclaw-desktop | 受限窗口与 Preload IPC 桥 | `src/main/index.ts` |\n| `shared/` | 三端共用 | StreamEvent 等类型的唯一真源 | `stream-event.ts` |\n| `tests/` | 后端测试 | 与 `src/` 同构命名的 329 个测试文件 | vitest 运行 |\n| `scripts/` | 工程工具链 | 类型同步、文档检查、备份恢复、冒烟测试 | 各 `.mjs/.sh/.ts` |\n| `config/` | 后端消费 | 渠道默认组、挂载白名单等静态配置 JSON | 启动时读取 |\n\nSources: [package.json](package.json#L6-L24), [README.md](README.md#L127-L152), [container/agent-runner/package.json](container/agent-runner/package.json#L1-L18), [web/package.json](web/package.json#L13-L42)\n\n## 二、src/：后端的扁平模块地图\n\n进入 `src/` 后你会发现一个鲜明的风格选择：**几乎全部业务代码平铺在一层**，只有四个子目录——`routes/`（HTTP 路由族，20 个文件）、`feishu-cards/`（飞书卡片构造器）、`im-safety/`（消息处理锁与卡死检测）、`middleware/`（认证中间件）。项目没有按"层"或按"领域"建深层目录树，而是靠**文件名前缀**表达领域归属，这是阅读本目录最重要的心智模型。\n\n前缀即领域。看到文件名就能猜到它属于哪个子系统：\n\n| 文件名前缀 | 领域 | 典型例子 |\n|---|---|---|\n| `channel-*` | 渠道账号、投递、可靠性的横切逻辑 | `channel-outbox-delivery.ts`、`channel-turn-runtime.ts` |\n| `feishu/dingtalk/discord/qq/wechat/whatsapp/telegram*` | 七大 IM 渠道适配器 | `feishu-streaming-card.ts`、`wechat-onboarding.ts` |\n| `im-*` | 跨渠道共用的 IM 抽象 | `im-manager.ts`、`im-context-isolation.ts` |\n| `plugin-*` | 插件展开与物化管线 | `plugin-expander-core.ts`、`plugin-materializer.ts` |\n| `task-*` | 定时任务调度与运行历史 | `task-scheduler.ts`、`task-run-history.ts` |\n| `workspace-*` | 工作区边界与记忆能力 | `workspace-memory-service` 族、`workspace-folder.ts` |\n| `group-*` / `provider-*` / `mcp-*` / `ipc-*` / `turn-*` | 群队列、模型供应商池、MCP 清单、IPC 投递、回合输出协调 | 见目录列表 |\n\n启动链条只有两跳：进程从 `src/index.ts` 进入，第一行就强制先加载环境变量，随后导入回复投递、回合输出协调、渠道卡片收尾等核心模块开始编排 [index.ts](src/index.ts#L1-L47)。而对外 HTTP 能力集中在 `src/web.ts`：它在第 48–70 行集中导入全部路由模块，然后在第 267 行起逐一挂载到 `/api/*` 前缀下，例如 `/api/auth`、`/api/groups`、`/api/workspaces`、`/api/mcp-servers` 等约 19 个路由族 [web.ts](src/web.ts#L265-L283)。值得注意的是，同一个后端进程还负责把前端构建产物 `web/dist` 以静态资源方式伺服，并对非文件路径做 SPA 回退到 `index.html` [web.ts](src/web.ts#L1311-L1349)——这解释了为什么生产部署只需要跑一个 Node 进程。\n\nSources: [index.ts](src/index.ts#L1-L47), [web.ts](src/web.ts#L48-L70), [web.ts](src/web.ts#L265-L283), [web.ts](src/web.ts#L1311-L1349)\n\n## 三、web/：前端的分层目录\n\n与后端的"扁平 + 前缀"不同，前端 `web/src/` 采用的是经典的**按角色分层**结构，每一层的职责单一且方向清晰：`pages/` 是路由级页面（19 个），`stores/` 是 Zustand 全局状态（17 个 store，通常一个领域一个），`components/` 按 UI 域再分 15 个子目录（如 `chat/`、`settings/`、`layout/`、`ui/`），`hooks/` 收纳可复用的 React Hook，`api/` 封装 HTTP 客户端与 WebSocket 连接，`lib/` 与 `utils/` 放纯函数工具，`features/` 目前只承载 workspace-memory 这一较重的功能域 [web/src 目录](web/src/App.tsx#L1-L18)。\n\n技术栈可以从依赖清单直接读出：React 19 + Vite 6 构建，react-router-dom v7 做路由，Zustand 5 做状态管理，Radix UI 与 Tailwind CSS 4 做组件体系，另有 xterm 终端、recharts 图表、mermaid 图渲染等专项库 [web/package.json](web/package.json#L13-L42)。路由组织在 `App.tsx` 中完成：登录、注册、初始化向导等轻页面直接静态导入，而 `ChatPage`、`TasksPage`、`SettingsPage` 等重页面通过 `lazy()` 动态导入实现代码分割，聊天页还有条件预加载逻辑以优化首跳体验 [App.tsx](web/src/App.tsx#L9-L50)。\n\n给新手的一条定位经验：**改页面找 `pages/`，改全局数据流找 `stores/`，改某个面板的外观找 `components/` 对应子目录**。三层之间通过 props 和 store hook 连接，不存在页面反向定义状态的结构。\n\nSources: [App.tsx](web/src/App.tsx#L9-L50), [web/package.json](web/package.json#L13-L42)\n\n## 四、container/：镜像组装与 Agent Runner 子包\n\n`container/` 目录承担双重身份：它既是 **Docker 镜像的组装现场**，又包含一个完整的 npm 包。镜像层面，`Dockerfile` 以 `/app` 为工作目录，先只复制 `agent-runner` 的依赖清单以利用层缓存，再整体复制 Runner 源码并在镜像内完成 TypeScript 编译，最后把 `entrypoint.sh` 和四个 `session-permissions-*` 权限脚本放入 `/app`；镜像内刻意**不写 `USER node` 指令**，而是把权限降级交给入口脚本在运行时处理，容器启动后的实际工作目录是 `/workspace/group` [Dockerfile](container/Dockerfile#L119-L215)、[Dockerfile](container/Dockerfile#L255-L258)。\n\nRunner 本身（`container/agent-runner/`）是一个名为 `miniclaw-agent-runner` 的独立包，入口为 `dist/pi-index.js`，依赖 Pi Runtime 及其 SubAgents 扩展和浏览器工具 [package.json](container/agent-runner/package.json#L1-L18)。其源码中最值得关注的是 `src/runtime/pi/` 子目录——六个 `pi-*` 文件（provider、runner、runtime、session、subagents、tools）构成了 Agent 在容器内的执行内核 [runtime/pi](container/agent-runner/src/runtime/index.ts)。同级还有一个 `prompts/` 目录存放 13 个 Markdown 提示词模板，其中 `channels/` 子目录为七大 IM 渠道各备一份渠道专属提示词 [prompts/channels](container/agent-runner/prompts/security-rules.md)。\n\n目录里还有两个容易被误读的位置：仓库中的 `container/skills/` 在工作区里是空的，真正的技能在运行期以只读方式挂载到容器内的 `/workspace/effective-skills`，由入口脚本遍历准备 [entrypoint.sh](container/entrypoint.sh#L57-L70)；而散落在 `container/` 根部的几个 `.mjs`/`.sh` 文件则是会话级权限挂载的配套脚本，随镜像一起部署。\n\nSources: [Dockerfile](container/Dockerfile#L119-L215), [Dockerfile](container/Dockerfile#L255-L258), [entrypoint.sh](container/entrypoint.sh#L57-L70), [runtime/index.ts](container/agent-runner/src/runtime/index.ts)\n\n## 五、electron/：只有两个源文件的最小桌面壳\n\nElectron 包是四个包中最精简的一个：`package.json` 只是一份 10 行的元信息清单，真正的源码仅有两个文件——`src/main/index.ts`（主进程）与 `src/preload/index.ts`（预加载桥接脚本）[electron/package.json](electron/package.json#L1-L10)。它的构建也不走常规 tsc 流程，而是由根目录的两个 esbuild 命令分别打包出 `dist/main.cjs` 和 `dist/preload.cjs`，并显式将 `electron` 模块标记为 external；打包发布则由 electron-builder 基于 `electron-builder.yml` 完成 [package.json](package.json#L25-L31)。\n\n这种"壳极薄"的设计是有意为之：桌面包不承载任何业务逻辑，它只负责开一个受限窗口加载 Web UI 并暴露最小化的 IPC 桥，所有功能仍由后端与前端的既有代码提供。因此当你需要排查桌面端问题时，只需聚焦这两个文件即可。\n\nSources: [electron/package.json](electron/package.json#L1-L10), [package.json](package.json#L25-L31)\n\n## 六、shared/ 与三端同步机制：本仓库最重要的约定\n\n`shared/` 只有三个文件，却是理解本仓库工程化的关键。以 `stream-event.ts` 为例，文件头注释明确写着它是**唯一真源**，构建步骤会把它复制到 `container/agent-runner/src/stream-event.types.ts`、`src/stream-event.types.ts` 和 `web/src/stream-event.types.ts` 三处拷贝，并警告"不要直接编辑拷贝" [stream-event.ts](shared/stream-event.ts#L1-L10)。之所以要复制而非跨包 import，正是因为三个包的依赖与构建彼此独立，无法共享同一份 `node_modules`。\n\n同步的不止这一个文件。Makefile 中的同步目标列出了完整的映射关系：`shared/stream-event.ts` 复制到三端，`shared/image-detector.ts` 复制到后端与 Runner，`shared/channel-prefixes.ts` 同样复制到后端与 Runner，共产生 7 份拷贝 [Makefile](Makefile#L75-L80)。这套机制由三层保障兜底：`make build` 把 `sync-types` 设为前置依赖，构建前自动检测并同步 [Makefile](Makefile#L40-L41)；全量类型检查会额外运行 `check-stream-event-sync.sh` 与提示词检查脚本 [Makefile](Makefile#L150-L152)；该检查脚本用逐文件 `diff` 比对真源与每份拷贝，任何一处漂移都会以非零码失败 [check-stream-event-sync.sh](scripts/check-stream-event-sync.sh#L1-L20)。我们实测三份 `stream-event.types.ts` 均为 396 行且内容一致，验证了机制的有效性。\n\n```mermaid\nflowchart LR\n    A["编辑<br/>shared/*.ts"] --> B{"make build"}\n    B --> C["sync-types<br/>检测 shared/ 是否比拷贝新"]\n    C --> D["复制到 7 个目标位置：<br/>3 × stream-event.types.ts<br/>2 × image-detector.ts<br/>2 × channel-prefixes.ts"]\n    D --> E["并行编译三包<br/>build:all"]\n    F["typecheck"] --> G["diff 校验全部拷贝<br/>不一致即报错"]\n```\n\n对初学者的操作纪律只有一条：**永远修改 `shared/` 下的原件，然后跑一次 `make build` 或 `make sync-types`，绝不手改任何 `*-types.ts` 拷贝**。这一约定将在 [StreamEvent 流式契约：三端共享类型与同步机制](25-streamevent-liu-shi-qi-yue-san-duan-gong-xiang-lei-xing-yu-tong-bu-ji-zhi) 页面中进一步展开。\n\nSources: [stream-event.ts](shared/stream-event.ts#L1-L10), [Makefile](Makefile#L75-L80), [Makefile](Makefile#L40-L41), [Makefile](Makefile#L150-L152), [check-stream-event-sync.sh](scripts/check-stream-event-sync.sh#L1-L20)\n\n## 七、tests/ 与 scripts/：与源码同构的工程配套\n\n测试布局遵循"**每个包自带测试**"的原则。后端测试集中在根目录 `tests/`，多达 329 个文件，命名与被测源文件一一对应——例如 `plugin-expander-core.test.ts` 对应 `src/plugin-expander-core.ts`、`schema-v65-workspace-memory.test.ts` 对应数据库迁移，这让"从报错测试名反查被测模块"成为可能；前端有自己的 `web/tests/`（含 Playwright E2E），Runner 有自己的 `container/agent-runner/tests/` [vitest.config.ts](vitest.config.ts#L1-L21)。根目录的 vitest 配置里有一个对新手很友好的细节：它把 `@` 别名指向 `web/src` 并重定向 React 相关解析，使部分 DOM 测试能直接复用前端模块，同时显式排除 `data/`（用户的 Agent 工作区）避免误扫嵌套项目的测试套件。\n\n`scripts/` 则是纯工具链，不含业务逻辑，可按用途分为四类：**同步与校验**（`sync-stream-event.sh`、`check-stream-event-sync.sh`、`check-agent-runner-prompts.sh`、`check-docs.mjs`）、**备份恢复**（`backup-manifest.mjs`、`restore-backup.mjs`、`sqlite-snapshot.mjs`）、**冒烟与自检**（`self-test-agent-runner.ts`、`smoke-agent-image.sh`、`test-real-model-smoke.ts`）以及**内置技能物化**（`install-host-tools.sh`、`builtin-skill-catalog.mjs`）。日常开发中你主要通过 Makefile 间接使用它们，无需记忆每个脚本的名字。\n\nSources: [vitest.config.ts](vitest.config.ts#L1-L21), [check-stream-event-sync.sh](scripts/check-stream-event-sync.sh#L1-L20)\n\n## 八、推荐阅读路径\n\n有了这张地图，建议按"先主干后枝叶"的顺序深入：先沿启动链条读 `src/index.ts` 与 `src/web.ts` 的路由挂载，理解后端骨架，对应 [服务启动与消息编排主流程剖析](7-fu-wu-qi-dong-yu-xiao-xi-bian-pai-zhu-liu-cheng-pou-xi) 与 [Hono Web 服务：路由族、Cookie 认证与 WebSocket](8-hono-web-fu-wu-lu-you-zu-cookie-ren-zheng-yu-websocket)；再进入 `container/agent-runner/src/runtime/pi/` 理解执行引擎，对应 [Pi Agent Runner 协议：stdin/stdout 结构化结果与 IPC 通道](10-pi-agent-runner-xie-yi-stdin-stdout-jie-gou-hua-jie-guo-yu-ipc-tong-dao)；最后回到 `web/src/stores/` 与 `web/src/api/` 理解前端如何消费后端事件，对应 [Web 前端架构：React 19、Zustand 状态管理与页面路由](24-web-qian-du-jiagou-react-19-zustand-zhuang-tai-guan-li-yu-ye-mian-lu-you)。\n\n若你想回顾本页所处的上下文，可先读 [整体架构：Backend、Pi Runner、Workspace 与客户端的边界划分](4-zheng-ti-jia-gou-backend-pi-runner-workspace-yu-ke-hu-duan-de-bian-jie-hua-fen) 建立"为什么分四个包"的全局观，再结合 [Agent-first 产品模型：Agent Profile、Workspace 与 Session 层级](5-agent-first-chan-pin-mo-xing-agent-profile-workspace-yu-session-ceng-ji) 将目录结构与产品概念一一对应。';export{e as default};

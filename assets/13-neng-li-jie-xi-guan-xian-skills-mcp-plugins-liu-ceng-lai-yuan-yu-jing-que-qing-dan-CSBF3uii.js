const e='Miniclaw 的每一个 Agent 在每一轮对话中能看到哪些 Skill、哪些 MCP 工具、哪些插件，都不是"环境里碰巧有什么就用什么"，而是由一条**确定性的能力解析管线**在每次运行前重新计算出来的。本文拆解这条管线的三个核心子系统：Skills 的六层来源解析器、MCP 的多层合并与清单哈希、Plugins 的导入—快照—物化三段式流水线，并说明它们如何在 Host 与 Container 两种执行模式下落成磁盘投影，以及如何通过审计事件向前端暴露"精确清单"。\n\n## 一、为什么需要一条能力解析管线\n\n一个 Miniclaw 工作区的执行环境同时叠加了多种能力来源：平台内置工具技能、管理员的宿主机 `~/.claude` 配置、仓库自带的项目级 Skill、按用户管理的 Skill 目录、工作区私有 Skill，以及 Claude Code 插件携带的命令与 MCP 服务。如果这些来源各自为政，同名冲突、禁用残留、跨运行泄漏就会成为常态。因此 Miniclaw 把"这个 Agent 这一轮到底能用什么"收敛为一次纯函数式计算：输入是各来源根目录与 AgentProfile 策略，输出是一份带 SHA-256 哈希的**有效清单**（Effective Manifest），再由清单驱动磁盘投影与 SDK 参数。\n\n```mermaid\nflowchart LR\n    subgraph 来源层\n        A[Skills 六层来源] --> R1[resolveEffectiveSkills]\n        B[MCP 多路来源] --> R2[buildEffectiveMcpManifest]\n        C[Plugins v2 配置] --> R3[loadUserPlugins]\n    end\n    R1 --> M1["EffectiveSkillManifest&lt;br/&gt;hash + selected"]\n    R2 --> M2["EffectiveMcpManifest&lt;br/&gt;hash + serverIds"]\n    R3 --> P["SdkPluginConfig[]&lt;br/&gt;--plugin-dir"]\n    M1 --> T1[会话目录调和 / 只读挂载]\n    M2 --> T2["settings.json mcpServers 替换写入"]\n    P --> T3["SDK options.plugins"]\n    T1 --> AU[ClaudeContextAudit 流事件]\n    T2 --> AU\n    T3 --> AU\n```\n\n上图的前提是理解三个角色分工：**解析器**（`effective-skill-resolver` / `effective-mcp-manifest`）只做计算不落盘；**投影层**（`claude-context-resolver.syncHostClaudeContext` 与 `container-runner` 的挂载构建）把清单变成 Host 符号链接或 Container 只读挂载；**审计层**把清单哈希随 StreamEvent 发给前端，实现"所见即所用"的可解释性。\n\nSources: [effective-skill-resolver.ts](src/effective-skill-resolver.ts#L121-L133)，[effective-mcp-manifest.ts](src/effective-mcp-manifest.ts#L80-L95)\n\n## 二、Skills：六层来源与统一优先级模型\n\n`EffectiveSkillSource` 枚举精确定义了六个来源：`builtin`、`host`、`project`、`managed`、`workspace`、`plugin`。解析器要求调用方**按优先级从低到高**传入各层的根目录，最终形成如下固定次序：\n\n| 优先级 | source | 磁盘根目录 | 归属与管理 |\n|---|---|---|---|\n| 1（最低） | `builtin` | `data/builtin-skills/` | 平台内置（feishu-cli 固定版本技能） |\n| 2 | `host` | `<externalClaudeDir>/skills/` | 管理员宿主机 `~/.claude/skills`，需 Agent 策略显式开启 |\n| 3 | `project` | `container/skills/` | Miniclaw 仓库自带项目级技能 |\n| 4 | `managed` | `data/skills/{ownerId}/` | 用户通过 Web 安装管理的技能 |\n| 5 | `workspace` | `data/groups/{folder}/.claude/skills/` | 工作区私有的 `.claude/skills` |\n| 6（最高） | `plugin` | 插件快照目录下的 `skills/` | 随插件启用而注入 |\n\n这一组装发生在 `buildClaudeContextPlan` 中：内置、项目、工作区三层无条件参与，host 层仅在策略未禁用时插入，managed 层仅在有归属用户且允许挂载时插入，plugin 层由 `pluginSkillLayers()` 把每个启用插件的 `skills/` 子目录转换为带 `idPrefix` 的最高优先级层。\n\nSources: [effective-skill-resolver.ts](src/effective-skill-resolver.ts#L7-L13)，[claude-context-resolver.ts](src/claude-context-resolver.ts#L266-L300)，[effective-skill-resolver.ts](src/effective-skill-resolver.ts#L58-L66)\n\n这套模型的两个关键语义值得单独强调。其一是**禁用定义不做"墓碑"**：被禁用（`SKILL.md` 改名为 `SKILL.md.disabled`）的定义在评估优先级之前就被移出竞争，绝不会挡住更低层级中同名的启用版本——例如用户在工作区禁用了一个托管 Skill，项目层的同名版本仍然生效。其二是 **managed 与 host 两层接受独立的 AgentProfile 策略过滤**：`runtime_policy.skills` 以 `inherit`/`custom`/`disabled` 三种模式筛选托管用户技能，`host` 技能同理；而内置、项目、工作区三层不受此过滤影响，始终按来源优先级裁决。未通过过滤的候选会被标记 `profile_filtered` 而非直接消失。\n\nSources: [effective-skill-resolver.ts](src/effective-skill-resolver.ts#L145-L176)，[skill-utils.ts](src/skill-utils.ts#L167-L181)，[claude-context-resolver.ts](src/claude-context-resolver.ts#L222-L237)\n\n## 三、EffectiveSkillManifest：候选、裁决与内容哈希\n\n解析器的输出不是简单的文件列表，而是一份结构完整的清单。它包含四个正交视图：\n\n| 字段 | 含义 |\n|---|---|\n| `candidates[]` | 所有扫描到的候选，含来源、路径、启用态、优先级、排除原因 |\n| `selected[]` | 每个胜出 id 的最终条目，记录 `overrides`（被它遮蔽的低层来源） |\n| `conflicts[]` | 存在同名的 id 列表，供前端告警 |\n| `missingManagedSkillIds` / `missingHostSkillIds` | custom 模式下引用了但磁盘上不存在的 id |\n\n裁决规则很简洁：对每个 id，收集所有启用且未被策略过滤的候选，按优先级排序取最高者；其余候选标记为 `shadowed` 并记入胜者的 `overrides`。这意味着前端可以完整回答"为什么这个 Skill 生效/不生效"——每个排除都有 `disabled` / `profile_filtered` / `shadowed` 三选一的明确原因。\n\nSources: [effective-skill-resolver.ts](src/effective-skill-resolver.ts#L27-L56)，[effective-skill-resolver.ts](src/effective-skill-resolver.ts#L186-L235)\n\n清单的**内容哈希**是整条管线的锚点。`hashSkillDirectory` 不只哈希 `SKILL.md` 元数据，而是递归遍历整个 Skill 目录——普通文件读入字节流、符号链接记录目标、目录记录路径标记，并忽略 `.DS_Store`、`.git`、`node_modules` 等非载荷条目。最终清单哈希把策略、选中项及其 `overrides`、全部排除项一起纳入 JSON 后做 SHA-256。只要任何一层任何一个字节变化，哈希即变，下游据此判断是否需要重建投影。\n\nSources: [effective-skill-resolver.ts](src/effective-skill-resolver.ts#L68-L119)，[effective-skill-resolver.ts](src/effective-skill-resolver.ts#L236-L258)\n\n还有一个容易被忽略的细节：`custom` 模式下的引用完整性是双向校验的。除了运行时校验（见第七节），解析器还会反向报告 `missing*SkillIds`，让配置错误在预览阶段就可见，而不是等到运行时报错。\n\nSources: [effective-skill-resolver.ts](src/effective-skill-resolver.ts#L212-L235)\n\n## 四、会话目录调和：从清单到磁盘投影\n\n清单必须落到磁盘才能被 Claude Code CLI 看到。`reconcileSessionSkills` 负责把会话目录下的 `skills/` 重建成与清单严格一致的状态，其设计目标是**防残留、可回滚**：\n\n```mermaid\nflowchart TD\n    S0[读取现有 session/.claude/skills] --> S1{存在真实目录或文件?}\n    S1 -- 是 --> Q[整体移入 orphaned-skills/时间戳/ 隔离区]\n    S1 -- 否 --> ST\n    Q --> ST[创建 .skills-staging-* 暂存目录]\n    ST --> LK["为每个 selected 非 plugin Skill<br/>symlink 到源路径"]\n    LK --> SW[原子 rename 交换 staging 与 skills]\n    SW -- 失败 --> RB[删除 staging 并回滚 previous]\n    SW -- 成功 --> RM[清理 previous]\n```\n\n流程图中有两点需要预先说明。第一，隔离而非删除：上一个 Agent 留下的真实文件目录（可能是它在运行中自己创建的 Skill）不会被销毁，而是移入时间戳命名的隔离区并在审计告警中列出。第二，`plugin` 来源的 Skill 在符号链接阶段被显式跳过——插件技能由 SDK 的 `options.plugins` 加载机制接管，它们只以 `插件名:技能id` 的限定名贡献给清单和 SDK 选择器，不参与目录投影。\n\nSources: [effective-skill-resolver.ts](src/effective-skill-resolver.ts#L278-L340)，[claude-context-resolver.ts](src/claude-context-resolver.ts#L463-L482)\n\n## 五、MCP：宿主机、工作区、系统、用户与插件的多路合并\n\nMCP 侧没有统一的六层枚举，而是两条互补的合并链。第一条链处理 **Claude 原生上下文**：`loadClaudeContextMcpServers` 按"越局部越优先"的 Claude 设置次序展开——宿主机 `settings.json` 与同级 `.claude.json`、工作区 `.mcp.json`、工作区 `.claude/settings.json`、`.claude/settings.local.json` 依次覆盖。第二条链是 **Miniclaw 托管的附加层**：`mergeMcpServerLayers` 把托管服务器作为最终叠加层合并进去——注释明确指出这是加法关系，启用原生上下文不会替换掉 Miniclaw 显式授予的服务器。\n\n| 来源 | 定义文件 | 可见性控制 |\n|---|---|---|\n| host | `~/.claude/settings.json`、`~/.claude.json` | 仅 `host_claude` 上下文策略开启时 |\n| workspace | `{workspace}/.mcp.json` 及两份 `.claude/settings*.json` | 绑定工作区后生效 |\n| system | `data/mcp-servers/system/servers.json` | `memberAccess: shared` 才对成员可见，默认 fail-closed 为 `admin_only` |\n| user | `data/mcp-servers/{userId}/servers.json` | 该用户全部工作区共享 |\n| plugin | 插件内 `.mcp.json` 与 `mcp-servers/*.json` | 仅 MCP 策略为 `inherit` 时发现，id 带 `plugin:` 前缀命名空间 |\n\nSources: [mcp-context.ts](src/mcp-context.ts#L57-L89)，[mcp-context.ts](src/mcp-context.ts#L83-L89)，[agent-capability-preview.ts](src/agent-capability-preview.ts#L222-L260)\n\n托管层内部还有一层 scope 结构。`loadManagedMcpLayers` 返回 `{ system, user, restrictedSystemIds }`：system scope 来自 `system` 用户的共享服务器集合，且除非调用方已验证当前主体是活跃管理员并显式传入 `allowAdminOnlySystemMcp`，否则受限 id 一律剔除——这是一个防御性设计，旧数据或缺省的 system 定义会被强制改写为 `admin_only` 并触发持久化迁移，永远不会因为 command/url 内容"看起来安全"就对成员放开。AgentProfile 的 MCP 策略通过 `parseManagedMcpReference` 解析 `system:`/`user:` 前缀引用（裸 id 兼容为 user scope），`custom` 模式下引用了不存在服务器的会在运行时以 `agent_profile_unavailable` 前缀快速失败，而不是静默降级重试。\n\nSources: [mcp-utils.ts](src/mcp-utils.ts#L41-L56)，[mcp-utils.ts](src/mcp-utils.ts#L330-L341)，[mcp-utils.ts](src/mcp-utils.ts#L433-L474)，[container-runner.ts](src/container-runner.ts#L674-L697)\n\n插件的 MCP 贡献走独立通道：`loadPluginMcpDefinitions` 镜像 Claude 插件的发现逻辑，读取每个插件根目录的 `.mcp.json` 和 `mcp-servers/` 下所有 JSON 文件，并把结果放进 `plugin:<qualifier>:<serverId>` 命名空间，天然避免与普通名称冲突。最终的 `buildEffectiveMcpManifest` 对整个运行时映射做**键排序 + 递归去 undefined 的规范化**后再哈希，输出只含 `hash` 和排序后的 `serverIds`——刻意不包含 command、args 或 env，保证清单可以安全地进入日志与审计事件而不泄露密钥。\n\nSources: [effective-mcp-manifest.ts](src/effective-mcp-manifest.ts#L16-L65)，[effective-mcp-manifest.ts](src/effective-mcp-manifest.ts#L80-L95)\n\n运行时的合成入口是 `resolveRuntimeMcpServers`（上下文层 + 托管层）与 `buildRuntimeMcpManifest`（再加上条件性的插件服务器），其结果通过 `ensureSettingsJson` 以 **replace 模式**写进会话 `settings.json` 的 `mcpServers` 字段——每次运行都全量替换，确保上一轮被禁用或取消勾选的服务器不会残留在会话设置里。\n\nSources: [container-runner.ts](src/container-runner.ts#L699-L735)，[container-runner.ts](src/container-runner.ts#L305-L313)\n\n## 六、Plugins：市场导入 → 目录快照 → 运行时物化的三段式\n\n插件是三类能力中最"重"的，因为它可能同时携带 commands、agents、skills、hooks 和 MCP 服务器。Miniclaw 用一条三段式流水线管理它，核心不变量是**目录快照不可变、运行时树 inode 隔离**。\n\n```mermaid\nflowchart LR\n    MP["宿主市场目录<br/>~/.claude/plugins/marketplaces/*"] -->|"importer 扫描<br/>content-hash 快照"| CAT["data/plugins/catalog/<br/>versions/{sha256}/ 不可变"]\n    CAT --> EN["plugins.json v2<br/>{enabled: {fullId: {snapshot,...}}}"]\n    EN -->|"materializeUserRuntime<br/>隔离 inode 复制"| RT["data/plugins/runtime/{userId}/<br/>snapshots/{snapshot}/{mp}/{plugin}"]\n    RT --> LP["loadUserPlugins<br/>校验 plugin.json 存在"]\n    LP --> SDK["SDK options.plugins<br/>→ claude CLI --plugin-dir"]\n```\n\n先解释图中三个关键转换。**导入段**：`plugin-importer` 扫描物理市场子目录与 `known_marketplaces.json` 登记的就地目录的并集，每个插件按内容哈希（排除 `.git/` 等）写入 `versions/{hash}/` 快照——相同哈希的重复导入是 no-op，索引更新在同一进程级互斥锁下经 tmp+rename 原子完成。**物化段**：`materializeUserRuntime` 把用户启用的每个快照复制到其运行时树，采用隔离 inode 策略（APFS/btrfs 上近乎零成本的 reflink，否则回退字节拷贝），使宿主模式下 `bypassPermissions` 的 Agent 写操作无法污染共享目录；刻意不做同步 GC，因为删除旧快照会与仍在读取它的活容器竞态。**加载段**：`loadUserPlugins` 逐条校验 v2 引用的名字段落合法性及 `.claude-plugin/plugin.json` 是否真实存在于磁盘，缺失者跳过并告警——过期配置永远不会向 SDK 注入悬空路径。\n\nSources: [plugin-importer.ts](src/plugin-importer.ts#L1-L36)，[plugin-materializer.ts](src/plugin-materializer.ts#L152-L284)，[plugin-utils.ts](src/plugin-utils.ts#L177-L238)\n\n加载结果的路径形态取决于运行时：Docker 模式返回 `/workspace/plugins/snapshots/{snapshot}/{mp}/{plugin}`（整个运行时根被挂载到 `/workspace/plugins`），Host 模式返回绝对 DATA_DIR 路径。宿主模式在启动前还会调用 `prepareHostPlugins` 做 best-effort 物化，避免"v2 配置存在但运行时树尚未构建导致 Agent 以零插件静默启动"。值得注意的是 MCP 精确选择与插件加载是解耦的：当 AgentProfile 的 MCP 策略为 `custom`/`disabled` 时，Runner 让 SDK 跳过插件 MCP 发现（`skipPluginMcpDiscovery`），但插件的 commands/agents/skills/hooks 仍然完整可用。\n\nSources: [plugin-utils.ts](src/plugin-utils.ts#L42-L43)，[container-runner.ts](src/container-runner.ts#L946-L973)，[runtime-mcp-policy.ts](container/agent-runner/src/runtime-mcp-policy.ts#L17-L34)\n\n## 七、双执行模式的运行时投影与可观测审计\n\n同一份清单在两种执行模式下有不同的落地方式，这也是理解本页与前文《Host 与 Container 双执行模式》的衔接点：\n\n| 维度 | Container 模式 | Host 模式 |\n|---|---|---|\n| Skill 投影 | 每个 selected Skill 单独只读挂载到 `/workspace/effective-skills/{id}`；entrypoint 先清空 `/home/node/.claude/skills` 再逐一链接 | `reconcileSessionSkills` 直接在会话目录物化符号链接 |\n| MCP 投影 | `settings.json` 的 `mcpServers` 全量替换写入，随会话目录挂载进容器 | 同样的 replace 写入，落在宿主会话目录 |\n| 插件投影 | 整个用户运行时根挂载到 `/workspace/plugins`，input.plugins 使用容器内路径 | `prepareHostPlugins` 物化后直接使用宿主绝对路径 |\n| 托管 Skill 定制集 | 按 AgentProfile 版本号发布不可变运行时根 `agent-profile-runtime/{owner}/{profile}/v{n}/skills` | 复用同一机制，通过 `userSkillsDirOverride` 注入 |\n\nSources: [container-runner.ts](src/container-runner.ts#L1233-L1250)，[entrypoint.sh](container/entrypoint.sh#L55-L70)，[session-generated-paths.mjs](container/session-generated-paths.mjs#L100-L120)，[container-runner.ts](src/container-runner.ts#L644-L671)\n\n容器内的 entrypoint 脚本值得展开一句：它先用 `--reset-skills` 通过文件描述符边界检查彻底清空 skills 目录，再遍历 `/workspace/effective-skills/` 中每个含 `SKILL.md` 的挂载项建立链接。这构成双重防线——即使某个早前容器把真实 Skill 目录写进了会话卷，重启后的重建也会让它无处藏身。\n\nSources: [entrypoint.sh](container/entrypoint.sh#L56-L70)，[session-generated-paths.mjs](container/session-generated-paths.mjs#L100-L118)\n\n可观测性方面，Host 后端在构造 `dockerInput` 时会把 `skillManifest.hash` + `selectedSkillIds` 以及 `contextAudit`（含 MCP 的 `manifestHash` + `serverIds`）一并经 stdin 传给 Runner；Runner 将其并入 `ClaudeContextAudit` 流事件发往前端，其中容器模式下 Skill 各来源的 `runtimePath` 显示为 `/workspace/effective-skills`，插件来源显示为 `options.plugins`。Runner 侧还保留了向后兼容：旧版 Host 未传 `skillManifest` 时 SDK 收到 `\'all\'`，新版则收到精确 id 数组——`skills: selectedSkillIds` 成为 SDK 的唯一事实来源。\n\nSources: [container-runner.ts](src/container-runner.ts#L1841-L1884)，[index.ts](container/agent-runner/src/index.ts#L383-L391)，[index.ts](container/agent-runner/src/index.ts#L2743-L2747)，[stream-event.types.ts](src/stream-event.types.ts#L237-L268)\n\n面向人的可解释性则由 `buildAgentCapabilityPreview` 提供：它用与运行时完全相同的解析函数生成预览条目、冲突列表与一组中文说明（例如"同名 Skill 按内置 → 宿主机 → Miniclaw 项目 → 系统附加 → 工作区项目 → 插件的顺序解析"），并且故意以管理员视角加载完整系统 MCP 目录，以便向成员解释"某服务器为何不可用"而不是让它凭空消失。\n\nSources: [agent-capability-preview.ts](src/agent-capability-preview.ts#L152-L199)，[agent-capability-preview.ts](src/agent-capability-preview.ts#L289-L315)\n\n## 八、能力变更的并发安全：锁与运行时静默\n\n最后一块拼图是：当用户在 Web 界面修改 Skill/MCP/插件时，正在运行的 Agent 怎么办？Miniclaw 的答案是**提交前静默、失败即封锁**。`withCapabilityScopeLocks` 以词法排序获取 `system` 与 `user:{id}` 两把进程内串行锁，避免跨 scope 操作死锁；`mutateCapabilityAroundRuntimeQuiesce` 则先计算哪些工作区的活跃 Runner 会观察到这次变更（依据 AgentProfile 策略模式与引用的 id 精确匹配），暂停这些队列、提交变更、删除受影响的 SDK 会话缓存后再恢复。若提交在原子 rename 之后失败——文件系统状态已部分可见——则安装运行时安全门，让相关组保持阻塞直到清理完成，宁可拒绝服务也不让旧凭据或旧 Skill 快照进入下一轮。\n\nSources: [capability-lock.ts](src/capability-lock.ts#L41-L60)，[capability-runtime-mutation.ts](src/capability-runtime-mutation.ts#L41-L66)，[capability-runtime-mutation.ts](src/capability-runtime-mutation.ts#L107-L168)\n\n内置 Skill 层也有同样的一致性纪律：`make dev/up` 启动前会用 `builtin-skill-catalog.mjs validate` 校验 `data/builtin-skills` 的 `.catalog.json` 标记（含 feishu-cli 固定版本、来源 tarball 的 SHA-256 与全量 payloadHash），缺失或不匹配时经 staging 目录 + rename 原子换新。这与 Skill 目录哈希、清单哈希共同构成了贯穿六层来源的同一个主题：**一切能力都以可验证的内容指纹为准**。\n\nSources: [Makefile](Makefile#L80-L85)，[builtin-skill-catalog.mjs](scripts/builtin-skill-catalog.mjs#L59-L74)，[install-host-tools.sh](scripts/install-host-tools.sh#L71-L99)\n\n---\n\n至此，能力解析管线的全貌已经清晰：六层 Skill 来源经统一优先级模型收敛为带内容哈希的清单，五路 MCP 来源按稳定的覆盖次序合成为不含密钥的服务器清单，插件经不可变快照与隔离物化进入 SDK。若想继续深入，建议阅读 [四段 Prompt 体系：IDENTITY、SOUL、AGENTS、TOOLS 的组装与上下文预算](12-si-duan-prompt-ti-xi-identity-soul-agents-tools-de-zu-zhuang-yu-shang-xia-wen-yu-suan) 了解这些能力如何进一步进入模型上下文，以及 [ACL 权限矩阵：层次化授权与无 admin 旁路的资源隔离](19-acl-quan-xian-ju-zhen-ceng-ci-hua-shou-quan-yu-wu-admin-pang-lu-de-zi-yuan-ge-chi) 理解系统级 MCP 的成员可见性如何嵌套在更大的授权体系中。';export{e as default};

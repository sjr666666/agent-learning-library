const t='MiniCode 的测试体系遵循与项目本体一致的**零依赖哲学**：不引入 Jest、Vitest 或任何第三方断言库，而是完全建立在 Node.js 内置的 `node:test` 测试运行器与 `node:assert/strict` 之上。整个 `test/` 目录包含 **24 个测试文件、222 个测试用例、约 4475 行代码**，按被测模块的复杂度和 I/O 距离自然形成从纯函数单元测试到多子系统集成回归的分层结构。理解这套体系，等于同时理解了 MiniCode 的架构骨架——测试文件的组织方式精确映射了 `src/` 的模块边界。\n\nSources: [test/run-tests.mjs](test/run-tests.mjs#L1-L33)、[package.json](package.json#L13-L13)\n\n## 运行架构：Node 原生测试运行器\n\n测试入口是 `package.json` 中的 `npm test`，它调用一个极简的编排脚本 `test/run-tests.mjs`。这个脚本只有 33 行，职责清晰：读取 `test/` 目录下所有 `*.test.ts` 文件、按字母序排序、然后以子进程方式启动 Node.js 内置测试运行器，并透传退出码与信号。TypeScript 的加载由 `tsx` 模块加载器（`--import tsx`）完成，因此测试文件可以直接以 ESM 语法导入 `src/` 下的 `.ts` 源码，无需预编译步骤。\n\n```mermaid\nflowchart LR\n    A[npm test] --> B[test/run-tests.mjs]\n    B --> C{扫描 test/*.test.ts}\n    C --> D[字母序排序]\n    D --> E[spawn node --import tsx --test]\n    E --> F[24 个测试文件并行执行]\n    F --> G[exit code 透传]\n    G --> H{全部通过?}\n    H -->|是| I[退出码 0]\n    H -->|否| J[退出码非 0]\n```\n\n这个设计选择有两个关键收益。其一，**零第三方测试依赖**——`package.json` 的 `devDependencies` 中只有 `typescript`、`tsx`、`eslint` 等工具链，没有任何测试框架，这保证了 `npm install` 的体积和供应链风险都保持在最低水平。其二，**并行执行**——Node 原生测试运行器会将多个测试文件分发到独立进程，222 个用例可以在数秒内跑完，保持了开发循环的即时反馈。若 `test/` 下没有测试文件，脚本会输出明确错误并以退出码 1 终止。\n\nSources: [test/run-tests.mjs](test/run-tests.mjs#L1-L33)、[package.json](package.json#L13-L13)\n\n## 分层测试全景：五层金字塔\n\n将 24 个测试文件按"距真实 I/O 的距离"划分，可以清晰看到一座经典的五层测试金字塔。**纯单元层**直接调用无副作用函数（模型上下文窗口映射、令牌估算、斜杠命令解析）；**行为算法层**通过手写 Mock 的 `ModelAdapter` 驱动压缩、裁剪、折叠等上下文管理算法，验证的是逻辑不变量；**文件集成层**在临时目录中执行真实的文件读写，覆盖会话持久化、记忆文件发现、脚手架生成；**协议集成层**通过猴子补丁 `globalThis.fetch` 模拟 Anthropic API，驱动完整的 agent 循环；**TUI 渲染层**通过操纵 `process.stdout.columns` 模拟不同终端宽度，验证渲染输出。\n\n```mermaid\nflowchart TB\n    subgraph L1[纯单元测试 · 无 I/O]\n        A1[model-context 13 例]\n        A2[token-estimator 16 例]\n        A3[input-parser / local-tool-shortcuts]\n        A4[status-line / context-badge / cli-commands]\n    end\n    subgraph L2[行为算法测试 · Mock ModelAdapter]\n        B1[compact 19 例]\n        B2[auto-compact 8 例]\n        B3[microcompact 5 例]\n        B4[snip-compact 16 例]\n        B5[context-collapse 19 例]\n    end\n    subgraph L3[文件集成测试 · 真实临时目录]\n        C1[session 35 例]\n        C2[memory 26 例]\n        C3[init 12 例]\n        C4[tool-result-storage 10 例]\n    end\n    subgraph L4[协议集成测试 · mock fetch]\n        D1[anthropic-thinking-roundtrip 3 例]\n        D2[provider-usage-ingestion 1 例]\n    end\n    subgraph L5[TUI 渲染测试 · mock 终端宽度]\n        E1[input / transcript-wrapping]\n        E2[transcript-cjk-selection]\n        E3[welcome-animation / mouse-release]\n        E4[windows-clipboard-encoding]\n    end\n    L1 --> L2 --> L3 --> L4 --> L5\n```\n\n各层测试的典型代表与验证重点如下表所示：\n\n| 层级 | 代表文件 | 用例数 | 验证重点 |\n|---|---|---|---|\n| 纯单元 | [model-context.test.ts](test/model-context.test.ts#L5-L96) | 13 | 模型名匹配大小写不敏感、`effectiveInput = contextWindow - outputReserve` 属性恒等式 |\n| 纯单元 | [token-estimator.test.ts](test/token-estimator.test.ts#L11-L69) | 16 | 不同角色消息的令牌密度差异（tool_result 密度高于 assistant） |\n| 行为算法 | [compact.test.ts](test/compact.test.ts#L41-L166) | 19 | 压缩后保留系统消息与最近轮次、无工具孤儿的配对不变量 |\n| 行为算法 | [auto-compact.test.ts](test/auto-compact.test.ts#L58-L90) | 8 | 连续失败三次后熔断禁用、成功后重置计数 |\n| 行为算法 | [microcompact.test.ts](test/microcompact.test.ts#L34-L62) | 5 | 仅清理 `COMPACTABLE_TOOLS` 名单内工具、不改变消息总数 |\n| 文件集成 | [session.test.ts](test/session.test.ts#L107-L199) | 35 | JSONL 事件信封格式、增量追加、`parentUuid` 链 |\n| 文件集成 | [memory.test.ts](test/memory.test.ts#L133-L164) | 26 | 自 cwd 向上遍历、内容相同去重、全局优先于项目 |\n| 协议集成 | [anthropic-thinking-roundtrip.test.ts](test/anthropic-thinking-roundtrip.test.ts#L16-L104) | 3 | thinking 块在工具调用往返后原样保留 |\n| TUI 渲染 | [input.test.ts](test/input.test.ts#L25-L58) | 4 | 空输入提示语循环、窄终端截断 |\n| TUI 渲染 | [transcript-cjk-selection.test.ts](test/transcript-cjk-selection.test.ts#L30-L47) | 2 | 全角 CJK 字符按显示列高亮与选取 |\n\n这个分层的价值在于**测试速度与覆盖深度的权衡**：下层纯函数测试毫秒级完成，能覆盖大量边界条件；上层集成测试虽然更慢、更重，但验证了跨模块协同时的真实行为。当压缩算法变更时，只需运行 `compact`、`snip-compact`、`context-collapse` 三个文件即可获得快速反馈，而 `session.test.ts` 中跨 snip→save→load→compact→save→load 的链条测试则负责兜底整个持久化管线。\n\nSources: [test/compact.test.ts](test/compact.test.ts#L41-L166)、[test/auto-compact.test.ts](test/auto-compact.test.ts#L58-L90)、[test/session.test.ts](test/session.test.ts#L402-L494)\n\n## Mock 策略与隔离手法：手写而非依赖\n\nMiniCode 没有引入 mock 库，而是用四种手写模式覆盖了从接口替身到全局环境模拟的全部需求。最基础的是 **ModelAdapter 替身**——`createMockModelAdapter(response)` 返回一个固定响应的适配器，用于测试压缩逻辑；更复杂的场景使用**序列适配器**（按调用次序依次吐出响应或抛出错误）和**失败适配器**（无条件抛错），专门用于熔断与降级路径的测试。\n\n第二种模式是 **fetch 猴子补丁**。`anthropic-thinking-roundtrip.test.ts` 将 `globalThis.fetch` 替换为按调用次数返回不同响应的模拟实现（第一次返回 `tool_use` 停止原因，第二次返回 `end_turn`），在 `afterEach` 中恢复原始 fetch。这使测试能穿透 `AnthropicModelAdapter` 直达 HTTP 请求体，断言第二个请求中 assistant 消息的 content 块顺序严格为 `[\'thinking\', \'tool_use\']`——这是 Anthropic API 对思考块签名的硬性要求。\n\n第三种是 **临时目录隔离**。所有涉及文件写入的测试都用 `os.tmpdir()` + `mkdtempSync` 创建独立目录，并在 `afterEach` 或 `finally` 中递归删除；同时，`session.test.ts` 通过 `MINI_CODE_HOME` 环境变量指向的配置目录（`src/config.ts` 中 `MINI_CODE_DIR` 的取值逻辑）将真实用户目录与测试数据彻底隔离。第四种是 **终端宽度模拟**——`Object.defineProperty(process.stdout, \'columns\', ...)` 临时改写标准输出宽度，并在 `try/finally` 中还原，用于验证换行与截断逻辑。\n\n| Mock 手法 | 使用场景 | 代表性文件 |\n|---|---|---|\n| `createMockModelAdapter` 固定响应 | 压缩/裁剪算法的正常路径 | [compact.test.ts](test/compact.test.ts#L8-L15) |\n| 序列/失败适配器 | 熔断、降级、异常处理 | [auto-compact.test.ts](test/auto-compact.test.ts#L68-L90) |\n| `globalThis.fetch` 猴子补丁 | Anthropic API 协议往返 | [anthropic-thinking-roundtrip.test.ts](test/anthropic-thinking-roundtrip.test.ts#L21-L48) |\n| `os.tmpdir()` 临时目录 | 会话、记忆、脚手架文件 I/O | [memory.test.ts](test/memory.test.ts#L14-L47) |\n| `Object.defineProperty` 终端宽度 | TUI 换行、截断、CJK 对齐 | [input.test.ts](test/input.test.ts#L9-L23) |\n| `resetAutoCompactState()` 状态复位 | 模块级全局状态的前置清理 | [auto-compact.test.ts](test/auto-compact.test.ts#L30-L33) |\n\nSources: [anthropic-thinking-roundtrip.test.ts](test/anthropic-thinking-roundtrip.test.ts#L10-L48)、[memory.test.ts](test/memory.test.ts#L14-L47)、[src/config.ts](src/config.ts#L36-L44)\n\n## 不变量测试：压缩与持久化的守护神\n\nMiniCode 的测试体系最值得学习的一点，是它对**结构性不变量**的显式验证。压缩、裁剪、折叠这类破坏性操作最容易引入两类缺陷：破坏了工具调用配对（`assistant_tool_call` 与 `tool_result` 必须成对出现），或破坏了消息链的因果完整性。为此，多个测试文件重复实现了 `assertNoToolOrphans` 辅助函数——它收集所有工具调用的 `toolUseId` 集合与所有工具结果的 `toolUseId` 集合，断言两者互为子集。这个不变量在 `compact.test.ts`、`snip-compact.test.ts`、`session.test.ts` 中反复出现，因为它直接决定压缩后的消息是否还能通过 Anthropic API 的校验。\n\n另一个典型是**边界值与幂等性测试**。`tool-result-storage.test.ts` 精确验证了 50_000 字符的单条落盘边界（恰好 50_000 不落盘，50_001 必须落盘）与 200_000 字符的批次预算边界；`init.test.ts` 验证 `initializeRepo` 二次执行时所有产物状态为 `skipped`，确保脚手架生成是幂等的。这些测试以"恰好踩在边界上"的输入为断言锚点，比模糊的范围断言更能捕捉 off-by-one 错误。\n\n最复杂的集成回归测试在 `session.test.ts` 第 402 行：它构造一个 24 轮长对话 + 尾部工具调用，依次执行 snip 裁剪 → 追加边界事件 → 保存 → 重新加载 → compact 压缩 → 追加边界 → 再保存 → 再加载，每一步都断言被删除的消息 ID 不会复活、工具调用与结果始终配对、当前任务消息保持可见。这单条测试横跨了上下文管理（snip/compact）、持久化（JSONL 追加）、事件边界（snip_boundary/compact_boundary）三个子系统，是名副其实的"集成回归"。\n\nSources: [session.test.ts](test/session.test.ts#L71-L93)、[session.test.ts](test/session.test.ts#L402-L494)、[tool-result-storage.test.ts](test/tool-result-storage.test.ts#L61-L70)、[init.test.ts](test/init.test.ts#L110-L121)\n\n## 质量门禁：check → lint → test 三件套\n\nMiniCode 的开发循环由三个独立的 npm script 构成质量门禁。`npm run check` 执行 `tsc --noEmit` 做全量类型检查（注意 `tsconfig.json` 的 `rootDir` 只覆盖 `src/`，测试文件的类型由 `tsx` 在运行时隐式校验）；`npm run lint` 执行 ESLint，其配置对 `test/**/*.ts` 有专门的覆盖规则——测试文件关闭 `@typescript-eslint/no-unused-vars`，因为测试中频繁使用 `_` 前缀占位参数和 `as any` 类型断言来构造 fixture；`npm test` 则运行上述 222 个用例。\n\n```mermaid\nflowchart LR\n    A[npm run check] --> B[tsc --noEmit]\n    C[npm run lint] --> D[eslint src test]\n    E[npm test] --> F[node test/run-tests.mjs]\n    B --> G{全部通过}\n    D --> G\n    F --> G\n    G -->|是| H[可提交]\n    G -->|否| I[修复后重跑]\n```\n\n值得注意的细节是 ESLint 配置的**分层策略**：第 33 行起的基础配置同时作用于 `src` 与 `test`，但第 57 行起的覆盖规则专门为测试文件放松约束。这体现了测试代码与生产代码的定位差异——测试追求表达力与覆盖度，允许一定程度的类型宽松；生产代码则保持严格。类型检查、静态检查、行为验证三道门禁各司其职，共同保证了重构时的安全性。\n\nSources: [package.json](package.json#L11-L14)、[eslint.config.js](eslint.config.js#L32-L61)、[tsconfig.json](tsconfig.json#L2-L14)\n\n## 从测试体系反推架构：模块边界即测试边界\n\n观察 `test/` 与 `src/` 的对应关系，可以反推 MiniCode 的架构原则：**每个可独立测试的模块，都是精心维护的边界**。`src/compact/` 下的五个压缩策略（auto-compact、compact、snipCompact、microcompact、context-collapse）各自拥有独立测试文件，因为它们通过 `ModelAdapter` 接口与外部世界解耦，可以在纯内存中验证；`src/utils/` 下的纯工具函数（token-estimator、model-context、tool-result-storage）测试最轻量，反映了它们被设计为无状态、可预测的纯函数。\n\n测试密度本身也是代码健康度的信号：`session.test.ts`（35 例）是测试最多的文件，因为它承载了 JSONL 事件日志、压缩边界、分叉、过期清理等最复杂的持久化语义；而 `cli-commands.test.ts` 只有 1 例，因为其逻辑简单且依赖方少。如果某个模块的测试用例数持续膨胀，往往意味着它承担了过多的职责，是提取新模块的信号。对于想要参与贡献的开发者，从"给测试密度低的模块补测试"或"为新增压缩策略写行为测试"入手，是理解架构最快的方式。\n\nSources: [test/session.test.ts](test/session.test.ts#L310-L357)、[test/session.test.ts](test/session.test.ts#L742-L762)、[test/cli-commands.test.ts](test/cli-commands.test.ts#L6-L26)\n\n---\n\n**延伸阅读**：本页是工程实践专题的第一篇。测试体系验证了[代理循环：model → tool → model 的多步执行流程](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng)与[自动压缩：触发阈值与失败熔断](12-zi-dong-ya-suo-hong-fa-yu-zhi-yu-shi-bai-rong-duan)的实现细节，建议对照阅读；`session.test.ts` 中丰富的持久化用例对应[追加式 JSONL 事件日志与断点恢复](19-zhui-jia-shi-jsonl-shi-jian-ri-zhi-yu-duan-dian-hui-fu)；理解测试中的 fetch 模拟后，可继续阅读[Anthropic 适配器：重试退避、限流与思考块保留](10-anthropic-gua-pei-qi-zhong-shi-tui-bi-xian-liu-yu-si-kao-kuai-bao-liu)。下一站可进入[多语言版本家族：TypeScript/Rust/Python/Go/Java 对照](29-duo-yu-yan-ban-ben-jia-zu-typescript-rust-python-go-java-dui-zhao)，了解其他语言实现如何移植这套测试思想。';export{t as default};

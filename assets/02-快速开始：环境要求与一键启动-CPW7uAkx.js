const e='本文是 HappyClaw 维基的**第二步**，面向初次接触该项目的初学者。读完本页，你将知道：运行 HappyClaw 需要准备哪些环境、如何用一条命令完成启动、启动后如何验证服务正常，以及最常见的启动故障怎么排查。本页只覆盖"跑起来"的最小路径——**环境要求与一键启动**；开发模式的完整命令、首次配置向导的细节和部署运维专题，分别由后续页面承接。\n\nSources: [README.md](README.md#L215-L256)\n\n## 环境要求\n\nHappyClaw 的运行时依赖非常收敛：**主服务是单一 Node.js 进程**，Docker 只用于隔离智能体执行，不承载主服务本身。因此在开始之前，你只需要确认四项环境条件：\n\n| 项目 | 最低要求 | 推荐配置 | 说明 |\n| --- | --- | --- | --- |\n| **操作系统** | macOS 或 Linux | macOS / Ubuntu 24.04 | Windows 用户请使用 WSL2 |\n| **Node.js** | 20 或更高 | 24（与 CI 同款） | 项目 `engines` 声明 `node >= 20` |\n| **包管理器 / 构建工具** | npm + GNU Make | npm 10+ | 项目所有命令通过 `make` 聚合 |\n| **Docker** | 可选 | Docker Desktop / OrbStack / Docker Engine | 仅使用管理员 Host 工作区时可不安 |\n\nSources: [README.md](README.md#L217-L226)、[package.json](package.json#L64-L66)\n\n**Node.js 版本为什么是 20？** 三个层面的原因：其一，`package.json` 的 `engines` 字段声明了 `node >= 20`，这是硬性门槛；其二，启动时加载 `.env` 依赖 `process.loadEnvFile()`，该 API 自 Node 20.12 起才稳定可用，因此实际建议使用 20.12+；其三，CI 流水线使用 Node 24 完成三端安装、类型检查与测试，用同款版本可以最大程度复现官方验证环境。另外需要注意一个工程决策：**本项目只使用原生 Node 工具链（npm / npx / tsx / node），不使用 bun**——主服务的 WebSocket 依赖 `ws` 包 + `@hono/node-server` 的 `server.on(\'upgrade\')` 握手，bun 的 HTTP server 不触发该事件，会导致所有 WebSocket 握手失败，前端实时流式输出全部失效。\n\nSources: [package.json](package.json#L64-L66)、[src/load-env.ts](src/load-env.ts#L14-L16)、[.github/workflows/ci.yml](.github/workflows/ci.yml#L9-L18)、[Makefile](Makefile#L8-L15)\n\n**Claude Code CLI 不需要单独安装**。项目依赖中已经内置了与 Claude Agent SDK 配套、版本锁定的 Claude Code 运行时（`@anthropic-ai/claude-agent-sdk` 以精确版本固定），你无需手动 `npm install -g @anthropic-ai/claude-code`。**Docker 是可选的**：普通成员和 Container 工作区必须依赖它，但如果你只打算以管理员身份使用宿主机（Host）工作区，完全可以不安装 Docker——`make start` 启动时会检测 `docker` 命令是否可用，不可用时自动跳过镜像拉取，不影响主服务运行。\n\nSources: [README.md](README.md#L222-L226)、[package.json](package.json#L36-L38)、[Makefile](Makefile#L63-L69)、[src/config.ts](src/config.ts#L95-L107)\n\n## 一键启动：make start\n\n启动 HappyClaw 只需要三条命令：\n\n```bash\ngit clone https://github.com/riba2534/happyclaw.git\ncd happyclaw\nmake start\n```\n\n`make start` 是**生产模式的一键启动**，以前台阻塞方式运行。它的设计目标是"首次零配置跑通"，内部是一个带缓存的准备流水线：依赖缺失或过期时自动安装、内置 Skills 自动物化、Docker 可用时自动拉取智能体镜像、三个子项目按需增量编译，最后才启动主服务进程。\n\n```mermaid\nflowchart TD\n    A["git clone 拉取仓库"] --> B["cd happyclaw"]\n    B --> C["make start"]\n    C --> D{"依赖缺失或过期?"}\n    D -- 是 --> E["自动安装三端依赖<br/>npm ci × 3"]\n    D -- 否 --> F["物化内置 Skills<br/>builtin-skill-catalog"]\n    E --> F\n    F --> G{"docker 命令可用?"}\n    G -- 是 --> H["docker pull 智能体镜像<br/>riba2534/happyclaw-agent:latest"]\n    G -- 否 --> I["跳过 · 仅 Host 模式可用"]\n    H --> J["增量编译<br/>后端 dist / Web dist / Agent Runner"]\n    I --> J\n    J --> K["node dist/index.js<br/>前台阻塞运行"]\n    K --> L["浏览器打开 http://localhost:3000"]\n```\n\nSources: [README.md](README.md#L228-L243)、[Makefile](Makefile#L52-L69)\n\n首次启动时，`make start` 会自动完成以下四类准备工作：\n\n| 阶段 | 动作 | 触发条件 |\n| --- | --- | --- |\n| **依赖安装** | 主服务、Web、Agent Runner 三端 `npm ci` | `node_modules` 缺失或 `package.json` / lockfile 更新 |\n| **内置 Skills 物化** | 从 feishu-cli 固定版本解压并校验 Skill 清单到 `data/builtin-skills/` | catalog 校验失败（首次启动必然触发） |\n| **镜像拉取** | `docker pull riba2534/happyclaw-agent:latest` | `docker` 命令可用时；不可用则跳过 |\n| **增量编译** | 按需编译后端、Web 前端、Agent Runner | 对应 `dist` 缺失或源码比产物新 |\n\n其中编译环节是**按需（stale）检测**的：`_check-sync` 检查 `shared/` 类型源是否漂移并同步；`_build-backend-if-stale`、`_build-web-if-stale`、`_build-ar-if-stale` 三个内部目标分别比较源码与产物的时间戳，没有变更就直接跳过。因此二次启动通常秒级完成。注意 `make start` 是前台进程，`Ctrl+C` 即停止；需要后台运行时可自行重定向日志并配合 `make status` / `make stop` 管理（详见 [开发模式与常用命令](3-kai-fa-mo-shi-yu-chang-yong-ming-ling)）。\n\nSources: [Makefile](Makefile#L52-L69)、[Makefile](Makefile#L78-L98)、[Makefile](Makefile#L124-L138)、[scripts/install-host-tools.sh](scripts/install-host-tools.sh#L45-L80)\n\n关于 Docker 镜像还需要澄清一个容易误解的点：**智能体镜像只由 GitHub Actions 构建发布，本地从不执行 `docker build`**。`main` 分支每次推送都会在原生 x64 与 ARM64 Runner 上并行构建 `riba2534/happyclaw-agent`（amd64/arm64，不使用 QEMU 模拟），两边都通过真实 Chromium CDP 冒烟探测后才会合并并提升 `latest` 标签。你本机只需要拉取。如果镜像拉取失败或想提前准备，可以单独执行 `make docker-pull` 或 `docker pull riba2534/happyclaw-agent:latest`。\n\nSources: [README.md](README.md#L273-L285)、[Makefile](Makefile#L180-L185)、[.github/workflows/docker-publish.yml](.github/workflows/docker-publish.yml#L14-L60)\n\n## 首次访问与验证\n\n启动完成后打开 <http://localhost:3000>。这个端口由 `WEB_PORT` 环境变量控制（默认 `3000`），同时承载 **Web 静态资源、REST API 与 WebSocket**——生产模式下所有流量走单一端口，不需要额外配置跨域。\n\nSources: [README.md](README.md#L238)、[src/config.ts](src/config.ts#L43-L47)、[Makefile](Makefile#L10-L13)\n\n首次访问时，系统会引导你完成初始化向导：\n\n1. **创建第一个管理员账户**——只有系统中不存在任何用户时，`POST /api/auth/setup` 才允许执行；向导同时创建管理员专属的 Home Workspace（`web:main`，宿主机模式）并自动登录。\n2. **配置模型提供商**——选择 Anthropic 官方账号（OAuth 一键登录）或第三方 Claude 兼容端点。\n3. **可选接入消息渠道**——飞书、Telegram、QQ、钉钉、微信、Discord、WhatsApp。\n4. **进入工作台**——创建智能体、工作区和会话，开始第一次对话。\n\nSources: [README.md](README.md#L239-L243)、[src/routes/auth.ts](src/routes/auth.ts#L139-L205)\n\n这三步向导的完整细节（Provider 类型选择、渠道凭据填写、注册策略等）属于 [首次配置向导：管理员、模型提供商与渠道接入](4-shou-ci-pei-zhi-xiang-dao-guan-li-yuan-mo-xing-ti-gong-shang-yu-qu-dao-jie-ru) 的范畴，本页不展开。\n\n想确认服务是否健康，除了看浏览器界面，还有两个轻量手段。其一是**健康检查端点**：`curl http://localhost:3000/api/health` 无需认证即可访问，会返回数据库连通性、消息队列可用性和进程运行时间三项检查结果。其二是 `make status`：它会一次性汇报端口占用、健康状态、日志文件位置（`/tmp/happyclaw.log`，仅后台模式有）和 Docker 容器列表。\n\nSources: [src/routes/monitor.ts](src/routes/monitor.ts#L198-L240)、[Makefile](Makefile#L124-L138)\n\n首次启动后，运行数据默认落在仓库根目录的 `data/` 下（SQLite 主数据库、加密配置、工作区目录、会话、IPC、Skills 等），**不要提交到 Git**。会话签名密钥会在首次启动时自动生成并持久化到 `data/config/session-secret.key`（权限 `0600`），后续重启复用同一密钥，登录态不会失效。数据目录的完整结构与备份恢复见 [数据目录结构与一致性备份恢复](23-shu-ju-mu-lu-jie-gou-yu-zhi-xing-bei-fen-hui-fu)。\n\nSources: [src/config.ts](src/config.ts#L20-L23)、[src/config.ts](src/config.ts#L57-L90)、[README.md](README.md#L324-L343)\n\n## 开发模式速览\n\n如果你打算修改代码并即时预览效果，使用 `make dev` 替代 `make start`。两者共享同一套自动准备逻辑（依赖安装、镜像拉取、内置 Skills），区别在于运行形态：\n\n| 维度 | `make start`（生产） | `make dev`（开发） |\n| --- | --- | --- |\n| **入口命令** | `node dist/index.js` | `tsx src/index.ts`（后端直跑 TS）+ Vite 热更新 |\n| **前端** | 编译后的静态资源，随主服务 3000 端口提供 | Vite Dev Server 运行在 **5173** 端口，热更新 |\n| **API / WebSocket** | 3000 | 3000（Vite 将 `/api` 与 `/ws` 代理到后端） |\n| **浏览器入口** | <http://localhost:3000> | <http://localhost:5173> |\n| **适用场景** | 部署、验收、长期运行 | 前后端联调、改代码即时生效 |\n\n```mermaid\nflowchart LR\n    subgraph P["生产模式 · make start"]\n        B1["localhost:3000<br/>Web 静态资源 + REST + WebSocket"]\n    end\n    subgraph D["开发模式 · make dev"]\n        F["localhost:5173<br/>Vite 前端（热更新）"]\n        B2["localhost:3000<br/>后端 API + WebSocket"]\n    end\n    F -->|"/api 代理"| B2\n    F -->|"/ws 代理"| B2\n```\n\nSources: [Makefile](Makefile#L24-L30)、[package.json](package.json#L16-L35)、[web/vite.config.ts](web/vite.config.ts#L20-L38)、[README.md](README.md#L247-L256)\n\n开发模式涉及的命令体系（`make typecheck`、`make test`、`make format` 等）会在 [开发模式与常用命令](3-kai-fa-mo-shi-yu-chang-yong-ming-ling) 中系统介绍，本页不再展开。\n\n## 常见问题排查\n\n| 症状 | 原因与处理 |\n| --- | --- |\n| **"端口 3000 已被占用"** | `make start` 启动前会用 `lsof` 检查端口，命中即拒绝启动并提示。执行 `make stop` 停掉旧进程，或改用 `WEB_PORT=8080 make start` 换端口 |\n| **Node 版本过低 / `loadEnvFile` 报错** | `engines` 要求 Node ≥ 20，`.env` 加载依赖 20.12+。用 `node -v` 检查，升级后重试 |\n| **Docker 未安装但想用普通成员工作区** | 主服务可正常启动（自动跳过拉取），但普通成员的 Container 工作区无法执行。安装 Docker Desktop / OrbStack 后重启 `make start` |\n| **镜像拉取慢或失败（网络受限）** | 手动重试 `make docker-pull` 或 `docker pull riba2534/happyclaw-agent:latest`；镜像由 CI 构建发布，本地不执行 build |\n| **服务器端请求被 Anthropic 403（大陆网络）** | 主服务读取 `HTTP_PROXY` / `HTTPS_PROXY` 环境变量设置全局代理（`NO_PROXY` 豁免 localhost），代理串会脱敏后打印启动日志。注意它不读 `ALL_PROXY`，也不支持 SOCKS |\n| **用 bun 启动后前端实时输出失效** | 项目明确不使用 bun：WebSocket upgrade 握手在 bun 下不触发。始终用 `node` 运行（`make start` / `make dev` 已强制） |\n| **想彻底回到首装状态** | 在确认数据无需保留后执行 `make reset-init`（清空 `data/`、`store/`、`groups/`），然后重新 `make start` |\n\nSources: [Makefile](Makefile#L52-L69)、[Makefile](Makefile#L221-L231)、[src/load-env.ts](src/load-env.ts#L18-L45)、[Makefile](Makefile#L8-L15)、[Makefile](Makefile#L240-L244)\n\n大部分配置（Provider 密钥、渠道凭据、注册策略、执行容量）都可以在 Web 设置界面完成，**不需要维护庞大的环境变量清单**。少数可选的启动级变量（`WEB_PORT`、`CONTAINER_IMAGE`、`ADMIN_HOST_ONLY_MODE`、`TRUST_PROXY`、`TZ` 等）及其默认值，属于 [部署与运维：环境变量、Docker 镜像与备份恢复](5-bu-shu-yu-yun-wei-huan-jing-bian-liang-docker-jing-xiang-yu-bei-fen-hui-fu) 的详细范畴。\n\nSources: [README.md](README.md#L306-L323)\n\n## 下一步阅读\n\n你已经完成了 HappyClaw 的首次启动。按照目录结构，建议的阅读顺序是：\n\n- [首次配置向导：管理员、模型提供商与渠道接入](4-shou-ci-pei-zhi-xiang-dao-guan-li-yuan-mo-xing-ti-gong-shang-yu-qu-dao-jie-ru)——启动之后的第一件事：完成管理员、模型 Provider 与渠道配置，让 HappyClaw 真正开始工作\n- [开发模式与常用命令](3-kai-fa-mo-shi-yu-chang-yong-ming-ling)——深入 `make dev`、类型检查、测试与格式化命令体系\n- [部署与运维：环境变量、Docker 镜像与备份恢复](5-bu-shu-yu-yun-wei-huan-jing-bian-liang-docker-jing-xiang-yu-bei-fen-hui-fu)——公网部署、反向代理、环境变量全集与 `make backup` / `make restore`\n- 如果还没有阅读起点页，建议先回到 [项目概览：自托管多用户 Claude Code 智能体工作台](1-xiang-mu-gai-lan-zi-tuo-guan-duo-yong-hu-claude-code-zhi-neng-ti-gong-zuo-tai) 建立整体认知';export{e as default};

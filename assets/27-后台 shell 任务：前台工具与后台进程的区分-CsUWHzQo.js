const n='MiniCode 的 `run_command` 工具面对一个朴素但关键的工程问题：当模型需要启动 `npm run dev`、`pytest --watch` 这类长期运行的服务时，如果像普通工具一样等待它退出，整个代理循环就会被一个"永远 running"的工具调用卡死。本文分析 MiniCode 如何用一个**尾部 `&` 操作符**作为分界，把命令划分为前台同步执行与后台分离进程两条路径，并追踪后台任务的注册、状态探活、TUI 呈现与模型回传的完整数据流。这一设计直接借鉴了 Claude Code 中"前台工具执行 vs 后台 shell 任务"的区分方式（Sources: [ARCHITECTURE.md](ARCHITECTURE.md#L86)）。\n\n## 核心概念：一个 `&` 决定执行模式\n\n`run_command` 的输入在进入执行前会经过一条**分类流水线**，最终落在三条执行路径之一。整个决策链由三个纯函数驱动：\n\n1. **结构化与单字符串归一化**：若模型提供了 `args` 数组则直接采用；否则把单字符串命令（如 `"git status"`）按引号、转义与空白拆分为 `[command, ...args]`（Sources: [run-command.ts](src/tools/run-command.ts#L57-L131)）。\n2. **shell 片段判定**：仅当命令是单字符串、且包含 `| & ; < > ( ) $ \\`` 等元字符时，才判定为需要 shell 解释的片段，交给 `bash -lc` 执行（Sources: [run-command.ts](src/tools/run-command.ts#L133-L139)）。\n3. **后台判定**：在 shell 片段的前提下，若去掉首尾空白后**以 `&` 结尾且不是 `&&`**，则判定为显式后台命令（Sources: [run-command.ts](src/tools/run-command.ts#L141-L148)）。\n\n```mermaid\nflowchart TD\n    A[run_command 输入] --> B{提供 args 数组?}\n    B -- 是 --> C[结构化调用 command + args]\n    B -- 否 --> D[splitCommandLine 拆分单字符串]\n    C --> E{是否白名单命令?}\n    D --> F{包含 shell 元字符?}\n    F -- 否 --> E\n    F -- 是 --> G{以 & 结尾且非 &&?}\n    G -- 否 --> H[bash -lc 前台执行]\n    G -- 是 --> I[bash -lc 后台 spawn, detached + unref]\n    E -- 是 --> J[execFileAsync 前台执行, 等待退出]\n    E -- 否 --> K[权限确认: 未知命令强制提示]\n    K --> J\n    I --> L[注册后台任务, 立即返回 TASK/PID]\n```\n\n三条路径的执行语义差异是理解整个系统的钥匙：**前台路径用 `execFileAsync` 等待进程退出并收集全部输出**（`maxBuffer` 上限 1 MB，stdout/stderr 合并返回）；**后台路径用 `spawn` 启动一个脱离的 `bash -lc` 子进程，`detached: true` + `unref()` 让父进程无需等待，`stdio: \'ignore\'` 丢弃全部输出**，随后立即注册任务并返回（Sources: [run-command.ts](src/tools/run-command.ts#L211-L242)）。\n\n一个值得注意的细节：命令允许列表（只读命令 `pwd/ls/find/rg/grep/cat/head/tail/wc/sed/echo/df/du/free/uname/uptime/whoami`，开发命令 `git/npm/node/python3/pytest/bash/sh/bun`）**只约束前台结构化路径**；shell 片段一律走 `bash -lc`，权限系统对 `bash` 本身做确认，未知命令则强制弹出提示（Sources: [run-command.ts](src/tools/run-command.ts#L12-L49)）。\n\n## 后台任务注册表：内存 Map 与信号探活\n\n后台任务的核心状态存放在 `src/background-tasks.ts` 的一个**模块级内存 `Map<string, BackgroundTaskRecord>`** 中，键为任务 ID。任务 ID 的生成规则为 `shell_<base36 时间戳>_<6 位随机串>`，例如 `shell_m5k3a1_ab12cd`（Sources: [background-tasks.ts](src/background-tasks.ts#L5-L13)）。\n\n每条记录的数据结构由 `BackgroundTaskResult` 定义，包含 `taskId`、`type: \'local_bash\'`、`command`、`pid`、`status` 三态枚举与 `startedAt` 时间戳，并额外携带 `cwd` 供诊断（Sources: [tool.ts](src/tool.ts#L11-L18)、[background-tasks.ts](src/background-tasks.ts#L5-L7)）。\n\n```mermaid\nflowchart LR\n    subgraph 注册表[内存 Map]\n        R1[shell_xxx: running]\n        R2[shell_yyy: completed]\n    end\n    S[run_command 后台分支] -->|registerBackgroundShellTask| R1\n    R1 -->|read 时 process.kill pid, 0| P{探活}\n    P -->|无异常| R1\n    P -->|ESRCH 进程不存在| C[标记 completed]\n    P -->|其他错误 EINVAL/EPERM| F[标记 failed]\n    C --> TUI[renderToolPanel / renderFooterBar]\n    F --> TUI\n```\n\n状态迁移是**惰性（lazy）的**：注册表本身不运行任何定时器，只在 `listBackgroundTasks()` 或 `getBackgroundTask()` 被调用时，对每个 `running` 记录执行一次 `process.kill(pid, 0)` **信号零探活**。`process.kill` 的返回语义被映射为三态：不抛错 → 进程存活，维持 `running`；抛 `ESRCH`（无此进程）→ 标记 `completed`；抛其他错误（如 `EINVAL`/`EPERM`）→ 标记 `failed`。值得注意的是，状态迁移**会写回 Map**——一旦探活发现进程消失，后续读取将稳定返回终态，而非每次重新探测（Sources: [background-tasks.ts](src/background-tasks.ts#L15-L41)）。\n\n## 状态机的语义与边界\n\n`process.kill(pid, 0)` 是 POSIX 系统上经典的"进程存在性探测"惯用法——发送 0 号信号不会真正影响目标进程，只触发内核的权限与存在性检查。其语义映射如下：\n\n| 探活结果 | 含义 | 迁移目标 |\n|---|---|---|\n| 无异常返回 | 进程仍存活 | 维持 `running` |\n| `ESRCH` | 进程已不存在 | `completed` |\n| 其他错误（`EINVAL` 等） | 非法 PID 或权限不足 | `failed` |\n\n这里有两个必须意识到的**边界**。其一，探活对象是 `spawn` 出的那个 `bash` 包装进程本身，而非模型真正关心的命令进程树——如果 `bash -lc "npm run dev"` 中的 `npm` 又派生了子进程，只要 bash 包装进程退出（无论命令是否真的完成），任务都会被标记为 `completed`。其二，`stdio: \'ignore\'` 意味着后台任务**不捕获任何输出**，模型拿不到任务日志；若需要日志，模型必须主动把输出重定向到文件（例如 `npm run dev > /tmp/dev.log 2>&1 &`），这恰好会被 shell 片段判定捕获并原样交给 `bash -lc`（Sources: [run-command.ts](src/tools/run-command.ts#L141-L152)、[run-command.ts](src/tools/run-command.ts#L211-L231)）。\n\n此外，注册表是**纯内存态**：任务记录不写入会话 JSONL、不参与断点恢复。会话重启后，后台任务会与 TUI 一起消失，但进程本身因 `detached` 而继续存活（Sources: [background-tasks.ts](src/background-tasks.ts#L9)、[session.ts](src/session.ts)）。\n\n## 与 TUI 的联动：1 秒心跳驱动的可视呈现\n\n后台任务在 TUI 中有两处呈现，其数据源是同一帧渲染中对注册表的一次全量读取。`renderScreen` 每帧调用 `listBackgroundTasks()` 获取最新状态，并把结果同时传给 activity 面板与底部状态栏（Sources: [tty-app.ts](src/tty-app.ts#L746-L747)、[tty-app.ts](src/tty-app.ts#L766)）。\n\n```mermaid\nsequenceDiagram\n    participant T as statusAnimationTimer\n    participant R as renderScreen\n    participant B as background-tasks.ts\n    participant C as chrome.ts\n    loop 每 1000ms\n        T->>R: scheduleRender()\n        R->>B: listBackgroundTasks()\n        B->>B: process.kill(pid, 0) 探活每个 running 记录\n        B-->>R: 刷新后的任务列表\n        R->>C: renderToolPanel / renderFooterBar\n    end\n```\n\n刷新的节拍来自 TUI 主循环中的 `statusAnimationTimer`——一个 1000ms 间隔的 `setInterval`，每次触发都调用 `scheduleRender()` 重绘（Sources: [tty-app.ts](src/tty-app.ts#L1759-L1762)）。因此后台任务的状态轮询粒度约为一秒，且**只在 TUI 存活期间有效**。\n\n呈现规则是"只看 running"：activity 面板在存在运行中任务时显示青色 `background:` 标签，单任务展示命令截断（48 字符），多任务只显示计数 `N shells running`；底部状态栏则在右侧追加 `shells N` 徽标（Sources: [chrome.ts](src/tui/chrome.ts#L348-L355)、[chrome.ts](src/tui/chrome.ts#L389-L393)）。任务终态不会在面板中反向通报——面板只负责"正在运行"的可见性。\n\n## 与模型的交互：TASK/PID 回传与轮询协议\n\n后台分支的返回值是一个**结构化工具结果**：`ToolResult` 携带 `backgroundTask` 字段，同时 `output` 字符串向模型呈现人类可读的启动确认：\n\n```\nBackground command started.\nTASK: shell_m5k3a1_ab12cd\nPID: 4231\n```\n\n（Sources: [run-command.ts](src/tools/run-command.ts#L226-L230)、[tool.ts](src/tool.ts#L20-L25)）\n\n该 `output` 经代理循环的 `onToolResult` 回调与 `replaceLargeToolResult` 处理，作为 `tool_result` 消息进入模型上下文（Sources: [agent-loop.ts](src/agent-loop.ts#L391-L399)）。MiniCode **没有**为后台任务提供专用的状态查询工具或斜杠命令——模型获知任务是否完成的唯一途径，是拿着 `PID` 再发起一次前台 `run_command`（例如 `ps -p 4231`）。这是一种有意为之的轻量协议：后台任务的结果不进入上下文记账，模型需要主动轮询并自行解读。\n\n对照之下，前台路径的结果则是"完整同步"的：`execFileAsync` 等待命令退出，把 stdout/stderr 合并为单个字符串返回（上限 1 MB），并在 TUI transcript 中按 1800 字符 / 36 行截断预览（Sources: [run-command.ts](src/tools/run-command.ts#L233-L242)、[transcript.ts](src/tui/transcript.ts#L135-L151)）。\n\n## 前台与后台：行为对比\n\n| 维度 | 前台执行 | 后台 shell 任务 |\n|---|---|---|\n| 触发条件 | 白名单命令 / shell 片段（不以 `&` 结尾） | shell 片段以 `&` 结尾（非 `&&`） |\n| 进程模型 | `execFileAsync`（结构化）或 `bash -lc`（片段） | `spawn` + `detached: true` + `unref()` |\n| 输出捕获 | 收集 stdout/stderr，1 MB 上限 | `stdio: \'ignore\'`，完全丢弃 |\n| 等待语义 | 阻塞等待命令退出 | 立即返回，不等待 |\n| 状态反馈 | 输出即结果 | 返回 `TASK` ID + `PID`，状态需信号探活 |\n| 权限检查 | `ensureCommand`，未知命令强制提示 | 同为 `bash -lc` 路径的 `ensureCommand` |\n| TUI 呈现 | transcript 工具条目（ok/err） | activity 面板 `background:` 标签 + 底部 `shells N` |\n| 上下文记账 | 完整结果进入模型上下文 | 仅启动确认字符串进入上下文 |\n\n## 设计取舍与学习要点\n\n从架构视角，这套后台任务机制的价值在于**用极小的代码量解决了代理循环的活锁问题**。官方文档将其定位为"轻量 shell task"，并明确这是对 Claude Code 前台/后台区分思路的轻量复刻（Sources: [USAGE_ZH.md](USAGE_ZH.md#L72)、[ARCHITECTURE.md](ARCHITECTURE.md#L51)）。其取舍清晰可辨：\n\n- **内存注册表**：不持久化意味着实现零 I/O 开销，但会话恢复后无法追溯任务；适合"短命进程跟踪"场景。\n- **信号探活**：`process.kill(pid, 0)` 是跨平台性最弱的探活方式之一（Windows 上语义不同），但足够满足终端编码助手的 Unix 主场。\n- **无输出捕获**：把日志责任转移给模型（重定向到文件），换来的是 spawn 的零管道管理成本。\n- **惰性刷新**：状态迁移只在读取时发生，避免了后台定时器与 TUI 生命周期耦合。\n\n## 延伸阅读\n\n- [统一工具协议：注册、Schema 校验与集中执行](8-tong-gong-ju-xie-yi-zhu-ce-schema-xiao-yan-yu-ji-zhong-zhi-xing) — `ToolResult` 的 `backgroundTask` 字段如何融入统一工具执行管线\n- [代理循环：model → tool → model 的多步执行流程](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng) — 后台任务的 TASK/PID 回传如何参与多步执行\n- [三层权限系统：路径、命令与编辑审批](17-san-ceng-quan-xian-xi-tong-lu-jing-ming-ling-yu-bian-ji-shen-pi) — `ensureCommand` 对后台 `bash -lc` 的审批路径\n- [TUI 架构：状态机驱动的终端界面](25-tui-jia-gou-zhuang-tai-ji-qu-dong-de-zhong-duan-jie-mian) — `statusAnimationTimer` 驱动的 1 秒渲染心跳\n- [大工具结果落盘：预览替换与稳定记账](16-da-gong-ju-jie-guo-luo-pan-yu-lan-ti-huan-yu-wen-ding-ji-zhang) — 前台长输出的上下文保护机制，与后台"零输出进上下文"形成对照';export{n as default};

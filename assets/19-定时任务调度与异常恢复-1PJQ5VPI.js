const e='定时任务子系统负责将「用户定义的调度计划」转化为「可执行的运行记录」，并在进程崩溃、重启、租约过期、投递失败等异常场景下维持语义一致性。其核心是两代实现并存的架构：**V1 遗留路径**（进程内 `runningTaskIds` + `scheduled_tasks` 表上的简单租约）与 **V2 持久化路径**（`task_runs` 表上的 durable occurrence + 强 fencing）。当前主调度循环完全运行在 V2 之上，V1 仅保留给手动触发与兼容路径。整个设计围绕三个不变量展开：**定义与运行解耦**、**租约决定接管权而非结算权**、**已开始执行的任务至多执行一次（at-most-once）**。\n\n## 三层模型：定义、发生、运行\n\n调度系统的数据模型把「任务是什么」与「某一次具体触发」彻底分离。`scheduled_tasks` 表保存任务定义（prompt、cron/interval/once 计划、执行模式、投递路由），`task_runs` 表则记录每次物化出来的 occurrence（`occurrence_key = taskId:scheduledFor` 唯一），携带**不可变的定义快照**——一个已排队或重试中的运行绝不会静默切换到更新后的任务定义（[types.ts](src/types.ts#L578-L592)）。\n\n```mermaid\nflowchart LR\n    subgraph DEF["scheduled_tasks（任务定义）"]\n        T1["status: active<br/>next_run 游标"]\n    end\n    subgraph OCC["task_runs（occurrence）"]\n        R1["queued → running → success/failed"]\n        R2["retry_wait → running"]\n        R3["missed（重叠/错失）"]\n    end\n    subgraph EXEC["执行路径"]\n        E1["isolated 容器/宿主"]\n        E2["group 工作区队列注入"]\n        E3["script 子进程"]\n    end\n\n    T1 -->|"materializeDueOccurrences<br/>原子推进游标"| OCC\n    R1 -->|"claimNextTaskRun 租约"| EXEC\n    R2 -->|"退避后重新认领"| EXEC\n```\n\nV2 与 V1 在故障语义上有本质差异：V1 用 `running_until`/`runner_id` 两个字段标记运行中状态，进程重启后这些租约必须被无条件清除（[db.ts](src/db.ts#L4899-L4910)）；V2 的租约存在于 `task_runs` 行内，**启动时绝不主动清除**，而是靠租约过期自然恢复——因为一个仍在磁盘上的 V2 租约可能属于一个尚存活的 worker，清除会造成双写（[task-scheduler.ts](src/task-scheduler.ts#L3290-L3299)）。`task_runs` 表上的部分唯一索引 `idx_task_runs_one_nonterminal` 从数据库层面保证同一任务同一时刻最多只有一个活跃 occurrence（queued/running/retry_wait）（[db.ts](src/db.ts#L647-L652)）。\n\nSources: [types.ts](src/types.ts#L507-L544), [db.ts](src/db.ts#L574-L656)\n\n## 调度循环：单进程泵模型\n\n调度器不是常驻的 cron daemon，而是一个**事件驱动的泵**：`pumpTaskScheduler` 每次被唤醒后依次执行固定阶段的流水线，结束时根据数据库中的最近唤醒时间重新武装定时器（[task-scheduler.ts](src/task-scheduler.ts#L3204-L3236)）。\n\n```mermaid\nflowchart TD\n    A["armScheduler 定时唤醒<br/>(精确到最近 next_run / lease 过期 / 通知重试)"] --> B{"queue 是否在关停?"}\n    B -->|是| Z["清空定时器<br/>不再物化/认领"]\n    B -->|否| C["failExpiredStartedTaskRuns<br/>崩溃后的已启动运行 → failed"]\n    C --> D["finalizeExpiredTaskRunNotificationAttempts<br/>崩溃的通知尝试 → 终局 fencing"]\n    D --> E["materializeDueOccurrences<br/>到期定义 → task_runs occurrence"]\n    E --> F["pumpTaskNotificationRetries<br/>最多 8 个通知租约"]\n    F --> G["claimNextTaskRun × 32<br/>认领并执行"]\n    G --> H["每 24h 清理 30 天前的日志"]\n    H --> I["armSchedulerFromStore<br/>计算下一次精确唤醒"]\n    I --> A\n```\n\n唤醒模型分三层（[task-scheduler.ts](src/task-scheduler.ts#L2209-L2223)）：**精确唤醒**取 `getNextScheduledTaskWakeAt()`（最早 next_run）与 `getNextTaskRunWakeAt()`（最早 queued/retry_wait available_at、running 租约过期、通知重试可用时间）的较小值；**即时唤醒**由 `notifyTaskSchedulerChanged()` 在任务创建/修改后触发（delay=0）；**安全网**是 60 秒的 `SCHEDULER_RECONCILE_MS` 上限，兜底时钟跳跃与遗漏的进程内唤醒。定时器 `unref()` 化，不阻塞进程退出。\n\n关键常量（[task-scheduler.ts](src/task-scheduler.ts#L2127-L2133)）：\n\n| 常量 | 值 | 语义 |\n|---|---|---|\n| `TASK_RUN_LEASE_MS` | 60,000 | V2 运行租约时长，心跳每 1/3 续租 |\n| `MAX_CLAIMS_PER_PUMP` | 32 | 单轮泵最多认领的运行数 |\n| `MAX_SAFE_PRESTART_ATTEMPTS` | 5 | 执行前安全重试上限（指数退避） |\n| `MAX_TASK_NOTIFICATION_ATTEMPTS` | 5 | 通知投递最大尝试次数 |\n| `SCHEDULER_RECONCILE_MS` | 60,000 | 调度对账安全网间隔 |\n| `CLEANUP_INTERVAL_MS` | 24h | 日志清理周期（保留 30 天） |\n\nSources: [task-scheduler.ts](src/task-scheduler.ts#L2187-L2228), [db.ts](src/db.ts#L7513-L7594), [config.ts](src/config.ts#L9)\n\n## Occurrence 物化：从定义到可执行记录\n\n`materializeDueOccurrences` 每轮泵扫描 `next_run <= now` 的 active 任务（上限 100 个），为每个到期定义调用 `materializeTaskOccurrence`。后者是一个**原子事务**：先校验任务仍 active 且 `next_run` 与传入值一致（CAS 语义），再插入 occurrence 行并同步推进定义游标；任何一步失败都整体回滚（[db.ts](src/db.ts#L5220-L5286)）。测试明确锁定：物化与游标推进必须是原子的，否则重复物化会制造虚假的并发运行（[task-runs-v2.test.ts](tests/task-runs-v2.test.ts#L262-L288)）。\n\n物化时有三个分支决定 occurrence 的命运：\n\n- **重叠保护**：若同一任务已有活跃 occurrence（queued/running/retry_wait），新 occurrence 直接落为 `missed`，原因记录为 `Skipped: previous occurrence … is still active`——调度器宁可跳过一次触发，也不允许同一任务并发执行。\n- **错失补偿（backfill）**：`shouldSkipBackfill` 判断 recurring 任务的到期时间早于调度进程启动时间，则记录为 `trigger_type=\'backfill\'` 的 `missed`，只推进游标不补跑；**一次性任务**不受此限制，重启后仍会执行（[task-scheduler.ts](src/task-scheduler.ts#L615-L622)）。\n- **非法计划**：cron 解析失败或 interval 小于 `MIN_INTERVAL_MS=60s` 时，物化一个带 `missedReason` 的 occurrence 并将任务置为 `paused`——绝不静默完成。`validateCronMinimumInterval` 对六段式 cron 的要求是秒字段必须解析为单一值，防止亚分钟频率的失控循环（[task-scheduler.ts](src/task-scheduler.ts#L629-L634)）。\n\n`computeNextRun` 以当前 `next_run` 为锚点推进周期：interval 用 `anchor + ceil(elapsed/ms)*ms` 对齐到周期边界，并防御损坏的 `next_run`（NaN 时回退到 now）；`finalizeRecurringRun` 是所有结算路径的唯一出口——**recurring 任务算不出 next_run 就 pause，绝不 silent-complete**（[task-scheduler.ts](src/task-scheduler.ts#L646-L779)）。\n\nSources: [task-scheduler.ts](src/task-scheduler.ts#L2926-L2962), [db.ts](src/db.ts#L4877-L4888), [task-restart-recovery.test.ts](tests/task-restart-recovery.test.ts#L73-L112)\n\n## 租约、认领与 fencing：崩溃安全的核心\n\nV2 的并发控制完全建立在 `task_runs` 行的租约字段上：`lease_owner`、`lease_token`（单调递增的 fencing token）、`lease_expires_at`。`claimNextTaskRun` 在单个 SQLite 事务内完成「选候选 + 原子更新」，候选条件覆盖两类情况：**可执行**（queued/retry_wait 且 available_at 到期）与**执行前崩溃遗留**（running 但 `started_at IS NULL` 且租约已过期）——后者可以安全重新认领，因为还没有产生任何外部副作用（[db.ts](src/db.ts#L5344-L5382)）。\n\n认领与执行之间的边界由 `markTaskRunExecutionStarted` 标记：一旦 `started_at` 写入，运行就进入 **at-most-once 保护区**——崩溃后只会被 `failExpiredStartedTaskRuns` 标记为 `failed`（错误信息为 "Process stopped after execution began; not retried to avoid duplicate side effects"），绝不会重放，因为重放可能重复发送消息或执行脚本（[db.ts](src/db.ts#L5385-L5458)）。\n\n**租约的语义是"决定谁可以接管"，而不是"决定谁可以结算"**。这是整套 fencing 设计的核心决策：`renew`/`release`/`complete` 只以 `lease_owner + lease_token` 为栅栏，不检查 `lease_expires_at`。原因在于：一个租约已过期但尚未被他人认领的 worker，其运行可能已经完成并向用户发出了输出；若此时拒绝它提交结果，运行会永远停在 `running` 状态，随后被误判为 failed，造成一次成功执行被静默丢失。而所有接管路径（claim、cancel、fail-expired）都会递增 `lease_token`，因此一旦新 owner 认领，旧 worker 的所有写入立即被 token 不匹配拒绝——不存在双写窗口（[db.ts](src/db.ts#L5460-L5495)）。\n\n```mermaid\nsequenceDiagram\n    participant W1 as Worker A\n    participant DB as task_runs\n    participant W2 as Worker B\n\n    W1->>DB: claimNextTaskRun<br/>(token=1, lease=60s)\n    Note over W1,DB: 执行开始 started_at 写入\n    W1->>DB: renewTaskRunLease (心跳每 20s)\n    Note over DB: 租约过期但未被接管\n    W1->>DB: completeTaskRun(token=1)<br/>→ 仍可提交成功结果\n    Note over DB: 场景二：租约过期且已被接管\n    W2->>DB: claimNextTaskRun<br/>(token 递增为 2)\n    W1->>DB: completeTaskRun(token=1)<br/>→ 被 fencing 拒绝\n    W2->>DB: completeTaskRun(token=2)<br/>→ 正常结算\n```\n\n运行期间的心跳由 `startTaskRunHeartbeat` 驱动：每 `LEASE/3`（至少 1 秒）续租一次，续租失败（被接管或 DB 异常）立即触发 `onLeaseLost` 回调——script 运行会被 `AbortController` 中止，group/isolated 运行则放弃结算权（[task-scheduler.ts](src/task-scheduler.ts#L2266-L2298)）。attempt 计数同时承担重试预算：`releaseTaskRunForRetry` 支持 `countsAsAttempt:false` 归还预算，用于「进程关停」「工作区迁移中」这类**非运行自身过错**的释放，否则 5 次普通重启就会耗尽一个从未执行过的 occurrence 的重试额度（[db.ts](src/db.ts#L5497-L5526)）。测试逐一锁定了过期可结算、接管后 fencing、attempt 归还这三条契约（[task-lease-settlement.test.ts](tests/task-lease-settlement.test.ts#L60-L179)）。\n\nSources: [db.ts](src/db.ts#L5344-L5405), [task-scheduler.ts](src/task-scheduler.ts#L2238-L2264), [task-lease-settlement.test.ts](tests/task-lease-settlement.test.ts#L60-L179)\n\n## 异常恢复分层\n\n恢复策略按「异常发生的时间点」分层设计，每层针对不同的语义风险：\n\n| 异常场景 | 检测时机 | 恢复动作 | 语义保证 |\n|---|---|---|---|\n| 进程启动（V1 遗留运行日志残留） | `startSchedulerLoop` | `cleanupStaleRunningLogs` 将 running 日志标记为 error「Process crashed before completion」 | 遗留 UI 不再显示永久 running |\n| 进程启动（V1 遗留租约） | `startSchedulerLoop` | 无条件清除 `running_until`/`runner_id` | 崩溃前任务重新可认领 |\n| 进程启动（V2 租约） | 每轮泵 | **不主动清除**，靠 `claimNextTaskRun` 的过期分支接管 | 尚存活 worker 不被误伤 |\n| 进程启动（亚分钟 interval） | `materializeDueOccurrences` | 物化 missed + 任务置 paused | 阻止毫秒级失控循环 |\n| 执行前崩溃（`started_at IS NULL`） | 每轮泵 | 租约过期后重新认领执行 | 无副作用，可安全重放 |\n| 执行中崩溃（`started_at` 已写） | 每轮泵 | `failExpiredStartedTaskRuns` → failed | at-most-once，不重放 |\n| 工作区迁移进行中 | `executeClaimedTaskRun` | `scheduleSafePrestartRetry` 1 秒后重试（不耗 attempt） | 不跨迁移边界启动进程 |\n| 任务被删除/目标组消失 | `executeClaimedTaskRun` | cancelled / failed（终局） | 严格投递绑定，不回退到其他组 |\n| 通知投递崩溃 | 每轮泵 | 终局 fencing，不重放（见下节） | 避免重复消息 |\n\n启动恢复的排序是刻意设计的：进程内保留集（`runningTaskIds` 等）先清空（重启后不再权威），V2 租约保持不动，V1 遗留日志标记失败，然后再进入正常泵循环（[task-scheduler.ts](src/task-scheduler.ts#L3281-L3305)）。启动测试锁定了完整行为：遗留 running 日志被关闭、V2 租约原样保留、遗留快间隔任务被 pause（[task-scheduler-startup.test.ts](tests/task-scheduler-startup.test.ts#L33-L109)）。关停侧则由 `stopSchedulerLoop` 提供契约：先停新物化，再以有界轮次（8 轮 / 10 秒上限）排空 `detachedSchedulerWork`（script 运行与通知重试这类队列不可见的异步工作），最后置空依赖引用（[task-scheduler.ts](src/task-scheduler.ts#L3238-L3279)）。关停测试验证了 queue 关停标志一旦置位，泵不再物化也不再重武装定时器（[task-scheduler-shutdown.test.ts](tests/task-scheduler-shutdown.test.ts#L39-L76)）。\n\nSources: [task-scheduler.ts](src/task-scheduler.ts#L3281-L3345), [db.ts](src/db.ts#L7641-L7668), [task-scheduler-startup.test.ts](tests/task-scheduler-startup.test.ts#L33-L109)\n\n## 执行分发与结果结算\n\n`executeClaimedTaskRun` 认领后先做四道执行前校验（任务已删 → cancelled；投递目标未注册 → failed；工作区迁移 → 安全重试；script 运行时策略违规 → 任务 pause + failed），然后按 `execution_type`/`context_mode` 分三条路径执行（[task-scheduler.ts](src/task-scheduler.ts#L2637-L2770)）：\n\n- **isolated**：为运行创建虚拟 task 会话与独立 IPC 命名空间，通过 container-runner 执行，结束后以原子 terminal hand-off 移交结算权；\n- **group**：通过 `storeGroupPromptAndDeliverRun` 把提示词以稳定 messageId 原子注入源工作区队列，并同时将 durable run 推入 delivered 状态——提示词一旦落库，运行就跨过了其持久化边界（[task-scheduler.ts](src/task-scheduler.ts#L2026-L2123)）；\n- **script**：以 `AbortController` 包裹子进程，注册 `activeDurableExecutions` stopper 供取消与租约丢失时中止（[task-scheduler.ts](src/task-scheduler.ts#L2711-L2770)）。\n\n投递目标采用**严格绑定**：`resolveTargetGroupJid` 只接受 `delivery_route_jid` 或 `chat_jid` 且与任务 `group_folder` 匹配的已注册组，解析失败即终局失败——绝不降级到同文件夹的任意组（历史版本曾静默漂移，现已被 CLAUDE.md 6.4 与测试禁止）（[task-scheduler.ts](src/task-scheduler.ts#L127-L153)）。任务输出到 IM 的路由决策由 `resolveTaskRoutingDecision` 完成：有合法 IM `chat_jid` 走 direct 模式，否则广播到 owner 的已连接渠道并按 `notify_channels` 过滤（[task-routing.ts](src/task-routing.ts#L101-L139)）。\n\n结算统一走 `finishDurableRunFromLegacyLog`：以 legacy 执行日志的最新结果为权威（`queued` → delivered、`error` → failed、否则 success），调用 `completeTaskRun` 原子写入结果、时长与通知状态（[task-scheduler.ts](src/task-scheduler.ts#L2583-L2635)）。运行期间的通知追踪器 `trackTaskRunNotifications` 会把失败回执**立即持久化**（生成感知的收据合并保证崩溃后不被成功覆盖），成功回执则延迟到运行终结时合并（[task-scheduler.ts](src/task-scheduler.ts#L2357-L2395)）。\n\nSources: [task-scheduler.ts](src/task-scheduler.ts#L2637-L2770), [task-routing.ts](src/task-routing.ts#L101-L139), [types.ts](src/types.ts#L595-L627)\n\n## 通知投递的持久化重试\n\n任务结果的 IM 通知不是尽力而为的旁路，而是与运行行绑定的**持久化重试状态机**。`notification_payload`（结构化投递载荷，含 workspace-relative 路径而非绝对路径）、`notification_attempt`、`notification_lease_owner/token/expires_at` 都随 `task_runs` 行持久化。认领查询覆盖三种情况：失败/部分失败且到期可重试、通知租约过期、以及被取消的 group 运行仍需要投递 workspace 取消结果（[db.ts](src/db.ts#L6970-L7140)）。\n\n每次重试由 `processClaimedTaskRunNotification` 执行，带独立心跳与租约；`deliverPersistedNotificationPayload` 按载荷类型分发（send_message / workspace_result / store_result_and_notify），关键设计是**重放的安全化**：workspace 投影重试时 `skipStore: true`（消息 ID 稳定，Web 侧结果不重复写），`send_message` 重试只做物理 IM 投递，group 运行的 `finalizeDeliveredGroupTaskRun` 只接受一次终局（[task-scheduler.ts](src/task-scheduler.ts#L3005-L3139)）。失败项被 `retryPayloadForReceipt` 收敛为只含失败 payload 的新 batch，成功项绝不重投。\n\n尝试上限为 `MAX_TASK_NOTIFICATION_ATTEMPTS = 5`。超过上限或崩溃后的最后一次尝试（结果不可知）由 `finalizeExpiredTaskRunNotificationAttempts` 做**终局 fencing**：不重放（重复风险）也不空转（busy-loop），而是把尝试耗尽/崩溃的载荷钉死为 failed（[db.ts](src/db.ts#L7142-L7175)）。测试覆盖了「崩溃前失败回执存活」「重试成功不重跑工作」「粗粒度成功更新不能隐藏 durable 重试工作」等边界（[task-runs-v2.test.ts](tests/task-runs-v2.test.ts#L476-L530)）。\n\nSources: [db.ts](src/db.ts#L6970-L7175), [task-scheduler.ts](src/task-scheduler.ts#L3141-L3202), [task-notification.ts](src/task-notification.ts#L65-L143)\n\n## 人工触发、取消与历史合并\n\n除定时触发外，调度器还提供三类运维入口（[task-scheduler.ts](src/task-scheduler.ts#L3348-L3493)）：\n\n- **`triggerTaskNow`**：手动运行走 `createTaskRun` 的幂等键去重（24 小时过期轮换），已存在活跃运行或解析中任务会返回明确错误；创建后同步泵一次，让调用方立刻观察到 queued/running 状态。任务定义的执行指纹（`buildTaskExecutionFingerprint`）用于内容级幂等，防止重复创建相同 agent 任务（script 任务刻意排除，因为同名脚本可能做不同的事）（[task-definition-fingerprint.ts](src/task-definition-fingerprint.ts#L23-L64)）。\n- **`cancelTaskRunNow`**：先做 DB 侧取消（fencing 掉后续结算），再调用进程侧 stopper 中止活跃 worker；对已 delivered 的 group 运行则通过 workspace_result 载荷投递取消意图。`waitForTaskRunsToStop` 等待 `activeDurableExecutions` 清空——DB 取消完成不代表脚本进程已停止写工作区，工作区重建必须等待进程侧退出（[task-scheduler.ts](src/task-scheduler.ts#L3387-L3493)）。\n- **历史查询**：`getMergedTaskRunHistory` 把 V2 durable 运行与 V1 legacy 日志合并去重（5 秒时间窗内模糊匹配同一执行的镜像），保证升级后历史不丢失也不重复（[task-run-history.ts](src/task-run-history.ts#L81-L131)）。\n\nSources: [task-scheduler.ts](src/task-scheduler.ts#L3348-L3493), [task-definition-fingerprint.ts](src/task-definition-fingerprint.ts#L23-L64), [task-run-history.ts](src/task-run-history.ts#L81-L131)\n\n## 下一步阅读\n\n- 调度的执行载体：任务的 isolated/group 运行最终进入 [会话队列与并发控制](18-hui-hua-dui-lie-yu-bing-fa-kong-zhi)，script 与 group 路径依赖队列的投递与 ACK 语义\n- 执行失败与模型切换：运行中 Provider 故障的降级策略见 [Provider 池与负载均衡策略](20-provider-chi-yu-fu-zai-jun-heng-ce-lue)\n- Runner 进程自身的卡死检测与恢复（`stuck-runner-recovery`、`runner-liveness`）见 [Runner 生命周期、卡死检测与故障恢复](21-runner-sheng-ming-zhou-qi-qia-si-jian-ce-yu-gu-zhang-hui-fu)，与本文的租约/心跳机制互补\n- 调度依赖的数据库结构与迁移机制见 [SQLite Schema 与数据库迁移机制](12-sqlite-schema-yu-shu-ju-ku-qian-yi-ji-zhi)';export{e as default};

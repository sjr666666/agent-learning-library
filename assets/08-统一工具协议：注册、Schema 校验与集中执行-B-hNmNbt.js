const n='MiniCode 的能力边界完全由一套统一的工具协议划定：无论是内置的文件操作、命令执行，还是通过 MCP 注入的外部工具，最终都以同一种 `ToolDefinition` 形态注册进 `ToolRegistry`，经由同一个 `execute()` 方法完成"查找 → Schema 校验 → 执行"的调度。本文深入剖析这套协议的三层设计——**双 Schema 声明、集中注册、集中执行**——并说明它如何与权限系统、代理循环和本地快捷指令协同。\n\n## 工具协议在整体架构中的定位\n\n代理循环是 `model → tool → model` 的多步执行流程，而工具协议正是这个循环中"tool"一侧的标准化接口。模型只负责根据工具描述生成调用意图（工具名 + 参数对象），真正的落地执行——参数校验、权限检查、错误归一化——全部收敛在 `ToolRegistry.execute()` 这一个入口。\n\n```mermaid\ngraph LR\n    subgraph "模型侧"\n        M[模型 / Anthropic Adapter]\n    end\n    subgraph "工具协议层"\n        R[ToolRegistry]\n        D[ToolDefinition 集合]\n        S[Zod Schema 校验]\n    end\n    subgraph "执行侧"\n        T1[内置工具 12 个]\n        T2[MCP 工具 mcp__server__tool]\n        P[权限系统 PermissionManager]\n    end\n\n    M -->|"tools 数组<br/>(name/description/input_schema)"| R\n    R -->|"list() 枚举"| D\n    R -->|"execute() 调度"| S\n    S -->|"通过"| T1\n    S -->|"通过"| T2\n    T1 --> P\n    T2 --> P\n```\n\n模型侧通过 `AnthropicModelAdapter` 将注册表中的工具序列化为 Anthropic Messages API 的 `tools` 数组（`name`、`description`、`input_schema`），每次请求都会携带完整清单；执行侧则完全复用同一份注册表。这意味着**注册表是模型可见能力与运行时实际能力的单一事实来源**——不存在"模型以为能用、运行时却没有"的割裂状态。\nSources: [anthropic-adapter.ts](src/anthropic-adapter.ts#L330-L340)\n\n## 核心契约：ToolDefinition 的五要素\n\n所有工具（内置与 MCP）都必须满足 `ToolDefinition<TInput>` 接口，它由五个字段构成：\n\n| 字段 | 类型 | 职责 | 消费方 |\n|------|------|------|--------|\n| `name` | `string` | 唯一标识，模型调用时使用的工具名 | 模型、注册表查找 |\n| `description` | `string` | 人类可读的功能说明，指导模型选择工具 | 模型（作为 prompt 上下文） |\n| `inputSchema` | `Record<string, unknown>` | JSON Schema 格式的参数声明 | 模型 API（`input_schema` 字段） |\n| `schema` | `z.ZodType<TInput>` | Zod 校验器，运行时参数强校验 | `ToolRegistry.execute()` |\n| `run(input, context)` | 异步函数 | 实际执行逻辑，返回 `ToolResult` | `ToolRegistry.execute()` |\n\n`ToolContext` 是每个工具执行时的环境参数：`cwd` 提供工作目录基准，可选的 `permissions` 挂载权限管理器，使工具内部能够发起路径、命令与编辑审批。\nSources: [tool.ts](src/tool.ts#L6-L22)\n\n## 双 Schema 设计：JSON Schema 与 Zod 的分工\n\nMiniCode 为每个工具声明**两套平行的 Schema**，分别服务于"模型理解"与"运行时校验"两个截然不同的阶段：\n\n- **`inputSchema`（JSON Schema）**：面向模型。在每次 API 请求中被序列化为 `input_schema`，帮助模型理解参数结构、类型与约束（含属性级 `description`）。它只做"告知"，不做"拦截"。\n- **`schema`（Zod）**：面向运行时。在工具真正执行前对模型传来的参数做强校验，是防御非法输入的真正闸门。\n\n以 `read_file` 为例，两套 Schema 描述了相同的参数形状，但 Zod 版本额外编码了运行时的数值边界（`offset` 非负整数、`limit` 上限 20000），这些约束在模型侧只是软提示：\nSources: [read-file.ts](src/tools/read-file.ts#L9-L31)\n\n校验规则从简单到复杂可分为四个层级：\n\n| 层级 | 规则类型 | 示例 | 出处 |\n|------|---------|------|------|\n| 基本类型 | 必填、最小长度 | `path: z.string().min(1)` | [edit-file.ts](src/tools/edit-file.ts#L27-L32) |\n| 数值范围 | 整数、上下界 | `limit: z.number().int().min(1).max(20000)` | [read-file.ts](src/tools/read-file.ts#L28-L32) |\n| 结构化数组 | 元素级约束 + 集合约束 | `replacements: z.array(...).min(1)` | [patch-file.ts](src/tools/patch-file.ts#L40-L49) |\n| 交叉字段 | 跨字段互斥校验 | `allowed_domains` 与 `blocked_domains` 不能同时出现 | [web-search.ts](src/tools/web-search.ts#L34-L53) |\n\n第四层是 Zod 相对 JSON Schema 的优势所在——`web_search` 通过 `.superRefine()` 实现跨字段的业务规则校验：当调用方同时传入 `allowed_domains` 与 `blocked_domains` 时，会在 `allowed_domains` 字段上附加自定义错误，这种"字段间矛盾"的校验在纯 JSON Schema 声明中难以优雅表达。\nSources: [web-search.ts](src/tools/web-search.ts#L41-L53)\n\n值得注意的是，**两套 Schema 是手写平行的，而非由单一来源推导**。这意味着新增工具时开发者需要维护两份声明，稍有疏忽就会产生"模型看到的约束"与"运行时强制的约束"之间的偏差——这是当前实现的已知取舍，换来的好处是 `inputSchema` 可以携带模型友好的描述性注释（如 `web_fetch` 中 `max_chars` 的默认值说明），而 Zod 层保持纯粹的类型逻辑。\nSources: [web-fetch.ts](src/tools/web-fetch.ts#L18-L28)\n\n## 集中注册：内置工具与 MCP 工具注入\n\n工具的注册入口是 `createDefaultToolRegistry()`，它一次性注册 12 个内置工具，并将工作区发现的技能（`discoverSkills`）与 MCP 服务器摘要作为元数据一并挂载：\nSources: [tools/index.ts](src/tools/index.ts#L42-L66)\n\n```mermaid\ngraph TD\n    A[createDefaultToolRegistry] --> B[12 个内置工具]\n    A --> C[discoverSkills 技能元数据]\n    A --> D[MCP 服务器摘要<br/>status=connecting]\n    E[hydrateMcpTools 异步注入] --> F[createMcpBackedTools<br/>启动 stdio/HTTP 服务器]\n    F --> G[包装为 mcp__server__tool]\n    G --> H[addTools 按名去重追加]\n    F --> I[setMcpServers 更新连接状态]\n    F --> J[addDisposer 注册清理函数]\n    H --> K[ToolRegistry]\n    I --> K\n    J --> K\n```\n\nMCP 工具的注入采用**异步水合（hydration）**模式：`hydrateMcpTools` 在启动时异步执行，即使部分 MCP 服务器连接失败也不会阻塞主流程（调用方通过 `.catch(() => {})` 吞掉启动期错误）。注入过程涉及三个动作：\n\n1. **`addTools()`** —— 将 MCP 工具包装为 `ToolDefinition` 后追加进注册表。追加前会以工具名为键去重，重名工具被静默跳过，避免覆盖内置工具。\n2. **`setMcpServers()`** —— 用连接后的真实状态（`connected`/`error`/工具数量）替换启动前的占位摘要。\n3. **`addDisposer()`** —— 注册服务器关闭函数，随注册表统一 `dispose()`，保证进程退出时子进程被正确清理。\nSources: [tools/index.ts](src/tools/index.ts#L68-L81) [tool.ts](src/tool.ts#L76-L89)\n\nMCP 工具的名称统一采用 `mcp__{server}__{tool}` 三段式命名，经 `sanitizeToolSegment` 清洗为合法的 `[a-z0-9_-]` 字符集。这种命名约定让模型从工具名即可推断来源服务器，也让注册表内不会与内置工具产生命名冲突。\nSources: [mcp.ts](src/mcp.ts#L1061-L1077)\n\n### MCP 工具的 Schema 例外\n\nMCP 工具是统一协议中唯一的"降级"案例：其 `schema` 固定为 `z.unknown()`，意味着**运行时校验被完全跳过**，参数直接透传给远端服务器。原因在于 MCP 描述符只提供 JSON Schema（`inputSchema`），没有对应的 Zod 校验器，而将远端 schema 动态编译为 Zod 在当前架构下并不划算。因此 MCP 工具的校验责任整体下沉到远端服务器——本地只负责将参数原样转发。\nSources: [mcp.ts](src/mcp.ts#L1065-L1076) [mcp.ts](src/mcp.ts#L120-L131)\n\n## 集中执行：ToolRegistry.execute 的三段式调度\n\n`execute()` 是整个工具协议的心脏，它将所有工具的执行路径收敛为固定三段，任何工具都不例外：\n\n```mermaid\nflowchart TD\n    A[execute toolName + input] --> B{find 查找注册表}\n    B -->|未找到| C[返回 ok:false<br/>Unknown tool 错误]\n    B -->|找到| D{schema.safeParse<br/>Zod 校验}\n    D -->|校验失败| E[返回 ok:false<br/>parsed.error.message]\n    D -->|校验通过| F[try / catch 执行 run]\n    F -->|正常返回| G[透传 ToolResult]\n    F -->|抛异常| H[捕获并返回 ok:false<br/>异常消息字符串化]\n```\n\n第一段**查找**：以工具名在注册表中线性查找，未命中直接返回 `{ ok: false, output: \'Unknown tool: ...\' }`。第二段**校验**：`tool.schema.safeParse(input)` 不抛异常，而是返回带 `.success` 标记的结果；失败时把 Zod 的错误消息整体回传。第三段**执行**：调用 `run(parsed.data, context)`，注意传入的是**校验通过后的数据**（Zod 会剥离多余字段、应用默认值），并用 `try/catch` 包裹，任何运行时异常都被捕获并归一化为 `ok: false` 的 `ToolResult`。\nSources: [tool.ts](src/tool.ts#L95-L107)\n\n### 错误归一化：永不向上抛异常\n\n这套设计最核心的哲学是**错误即返回值**：工具不存在、参数非法、执行抛异常、业务逻辑失败（如文本未找到），四种截然不同的失败形态最终都以"`ok: false` + 人类可读的字符串"返回给调用方。这样做的直接收益是代理循环无需 `try/catch` 包裹每一次工具调用——它只需检查 `result.ok` 即可决定是否计数错误、如何回传给模型。模型拿到的是格式一致的错误文本，可以基于此调整策略重试。\nSources: [agent-loop.ts](src/agent-loop.ts#L380-L391)\n\n## 执行上下文与权限集成\n\n`ToolContext` 是工具协议与权限系统之间的桥梁。每个工具的 `run` 都收到同一个上下文，权限检查发生在工具**内部**而非 `execute()` 统一层，这样设计是因为不同工具需要不同粒度的权限语义：\n\n- **路径类工具**（`read_file`/`write_file`/`edit_file` 等）通过 `resolveToolPath()` 解析目标路径。当没有权限管理器时，执行工作区逃逸检查（拒绝 `..` 越界与绝对路径）；有权限管理器时，则调用 `permissions.ensurePathAccess(resolved, intent)` 按读/写/列目录/搜索四种意图发起审批。\n  Sources: [workspace.ts](src/workspace.ts#L4-L28)\n\n- **写文件类工具**（`write_file`/`modify_file`/`edit_file`/`patch_file`）在写入前调用 `applyReviewedFileChange()`，该函数统一生成 unified diff 并调用 `permissions.ensureEdit()` 请求编辑审批，审批通过后才真正落盘——写前审阅由此成为所有编辑工具共用的强制通道。\n  Sources: [file-review.ts](src/file-review.ts#L52-L71)\n\n- **命令工具**（`run_command`）按命令白名单分流：只读命令（`ls`/`cat`/`rg` 等）直接执行；开发命令（`git`/`npm`/`node` 等）触发 `ensureCommand` 审批；白名单外的未知命令则强制携带 `forcePromptReason` 发起审批。\n  Sources: [run-command.ts](src/tools/run-command.ts#L8-L26) [run-command.ts](src/tools/run-command.ts#L189-L198)\n\n```mermaid\ngraph LR\n    subgraph "内置工具执行链"\n        R[run 执行] --> RP{路径类?}\n        RP -->|是| RW[resolveToolPath]\n        RW -->|无权限管理器| WE[工作区逃逸检查]\n        RW -->|有权限管理器| PA[ensurePathAccess]\n        RP -->|写文件类| ED[applyReviewedFileChange<br/>统一 diff + ensureEdit]\n        RP -->|命令类| CM[ensureCommand<br/>白名单分流]\n    end\n```\n\n## 工具结果协议\n\n`ToolResult` 是工具执行的统一出口，除基础的 `ok` 与 `output` 外，还定义了两种扩展语义：\n\n| 字段 | 语义 | 使用场景 | 消费方行为 |\n|------|------|---------|-----------|\n| `ok` | 成功/失败标志 | 全部工具 | 代理循环据此计数 `toolErrorCount`、标注 `isError` |\n| `output` | 人类可读的结果文本 | 全部工具 | 直接回传给模型作为 `tool_result` 消息内容 |\n| `awaitUser` | 是否暂停回合等待用户 | `ask_user` | 代理循环发现后立即停止本回合，将问题呈现给用户 |\n| `backgroundTask` | 是否已转入后台 | `run_command`（`&` 后缀） | 附带任务 ID/PID，由后台任务系统跟踪 |\n\n`ask_user` 是 `awaitUser` 语义的典型实现：它的 `run` 不执行任何副作用，仅返回规范化的问题文本并置位 `awaitUser: true`。代理循环在批量执行完本轮所有工具后，检测到 `awaitUser` 便提前结束回合——这是模型"询问澄清问题"这一交互模式的协议级支撑。\nSources: [ask-user.ts](src/tools/ask-user.ts#L10-L31) [agent-loop.ts](src/agent-loop.ts#L427-L435)\n\n后台命令是另一个协议级扩展：当 `run_command` 收到以 `&` 结尾的 shell 片段时，用 `detached: true` 的 `spawn` 启动子进程并 `unref()` 使其脱离前台，返回的 `ToolResult` 携带 `backgroundTask`（任务 ID、PID、初始状态）。该结果同时会被 `registerBackgroundShellTask` 登记，供 TUI 后台任务面板追踪。\nSources: [run-command.ts](src/tools/run-command.ts#L207-L226)\n\n## 两个执行入口：共享同一个 execute\n\n`ToolRegistry.execute()` 的签名是 `(toolName, input, context)`，这个中立的形态使得**模型驱动**与**用户驱动**两条执行路径可以完全复用同一套调度逻辑：\n\n```mermaid\ngraph LR\n    subgraph "模型驱动（代理循环）"\n        M[模型返回 tool_use 块] --> MC[assistant_tool_call 消息]\n        MC --> EX[tools.execute]\n    end\n    subgraph "用户驱动（本地快捷指令）"\n        U[用户输入 /ls /read /cmd...] --> P[parseLocalToolShortcut]\n        P --> SC[映射为 toolName + input]\n        SC --> EX\n    end\n    EX --> TR[ToolResult]\n    TR -->|模型路径| TMSG[tool_result 消息<br/>进入对话历史]\n    TR -->|快捷路径| UI[TUI 折叠展示<br/>成功/错误状态]\n```\n\n**代理循环路径**：模型生成的 `ToolCall[]` 被逐条执行，每条调用的 `id`、`toolName`、`input` 被编码为 `assistant_tool_call` 消息，执行结果编码为 `tool_result` 消息，成对追加进对话历史，供模型在下一轮推理中引用。大结果还会经过 `replaceLargeToolResult` 落盘替换为预览。\nSources: [agent-loop.ts](src/agent-loop.ts#L380-L399) [types.ts](src/types.ts#L64-L67)\n\n**本地快捷指令路径**：`parseLocalToolShortcut` 将 `/ls`、`/read`、`/cmd` 等斜杠指令解析为与模型调用完全相同的 `{ toolName, input }` 结构，随后同样调用 `tools.execute()`。TUI 根据返回的 `ok` 状态渲染成功/错误徽标，并将输出折叠进转录面板。\nSources: [tty-app.ts](src/tty-app.ts#L918-L948) [local-tool-shortcuts.ts](src/local-tool-shortcuts.ts#L17-L34)\n\n两条路径共享同一个注册表与执行器，意味着：本地快捷指令能调用 MCP 工具、能触发同样的权限审批、能享受同样的错误归一化——**协议的统一性直接带来了交互渠道的统一性**。\n\n## 设计总结与延伸阅读\n\n统一工具协议的核心价值可以用一句话概括：**声明（双 Schema）与执行（集中调度）分离，错误（返回值而非异常）与成功统一**。它为 MiniCode 提供了三个关键能力：模型可发现的能力清单（注册表即事实源）、可信的参数防线（Zod 闸门）、可扩展的集成点（任何工具只需实现五字段契约即可接入）。\n\n| 设计维度 | 选择 | 权衡 |\n|---------|------|------|\n| 双 Schema | JSON Schema（对模型）+ Zod（对运行时） | 声明冗余，但各取所需 |\n| 错误处理 | 归一化为 `ok:false` 返回值 | 调用方零异常处理，但丢失堆栈 |\n| 权限位置 | 工具内部各自发起 | 粒度精确，但需各工具自觉调用 |\n| MCP 校验 | `z.unknown()` 跳过本地校验 | 灵活透传，校验责任下沉远端 |\n| 注册时机 | 同步内置 + 异步水合 | 启动不被 MCP 阻塞，但工具列表会动态变化 |\n\n下一步可以沿着两条线索深入：一是工具调用如何被编码进结构化消息模型、`assistant_tool_call` 与 `tool_result` 如何成对编排，见 [结构化消息模型：角色类型与工具调用编排](9-jie-gou-hua-xiao-xi-mo-xing-jiao-se-lei-xing-yu-gong-ju-diao-yong-bian-pai)；二是权限系统本身的三层审批决策（路径/命令/编辑）如何与本文提到的 `ensurePathAccess`/`ensureCommand`/`ensureEdit` 一一对应，见 [三层权限系统：路径、命令与编辑审批](17-san-ceng-quan-xian-xi-tong-lu-jing-ming-ling-yu-bian-ji-shen-pi)。若想了解工具结果如何被落盘替换，可参考 [大工具结果落盘：预览替换与稳定记账](16-da-gong-ju-jie-guo-luo-pan-yu-lan-ti-huan-yu-wen-ding-ji-zhang)。';export{n as default};

const r='Provider 池是 HappyClaw 在多模型提供商场景下的**流量分配与故障隔离中枢**。它的核心价值在于：把"多套 Claude Code 运行环境"组织成一个可治理的候选集合，在会话发起时按策略挑选 Provider，在运行中持续追踪每个 Provider 的健康状态，并在 Provider 失效时决定"换一家重放"还是"向用户终结失败"。本页将沿着**配置模型 → 选择算法 → 会话粘性 → 健康状态机 → 故障分级处置**的链路展开，解释这三个关键设计：为什么会话要粘住 Provider、为什么故障要分"模型级"与"Provider 级"两级处理、以及 Agent 钉住模型配置后池如何退居二线。\n\nSources: [provider-pool.ts](src/provider-pool.ts#L1-L8)、[provider-failure.ts](src/provider-failure.ts#L1-L2)\n\n## 配置模型：Provider 是一套完整运行环境\n\n在 HappyClaw 中，**Provider（模型配置）不是"一个 API 端点"这么简单，而是一整套可以独立启动 Claude Code 的运行环境**。磁盘上的 V5 配置（`claude-provider.json`）中，每个 Provider 携带以下字段：\n\n| 字段 | 说明 |\n|---|---|\n| `id` / `name` | 唯一标识与展示名，`id` 为随机 hex |\n| `type` | `official`（官方订阅）或 `third_party`（第三方网关） |\n| `enabled` | 是否参与全局自动池；首个创建的 Provider 自动启用 |\n| `weight` | 加权轮询权重，钳制在 1–100，默认 1 |\n| `anthropicBaseUrl` | 第三方端点地址（官方为空） |\n| `anthropicModel` | 默认模型名，可带 `[1m]` 百万上下文后缀 |\n| `secrets` | **独立加密**的凭据：API Key / Auth Token / OAuth 凭据 |\n| `customEnv` | 追加的自定义环境变量（过滤保留的 Claude 保留键） |\n\n这套设计的关键是"**每项都是一套完整 Provider 运行环境**"——不同的 OAuth 账号、不同的网关、不同的模型各自占一个 Provider，彼此密钥隔离。系统限制最多 20 个 Provider；当没有显式指定默认 Provider 时，第一个启用的 Provider 自动成为默认。\n\n池的均衡参数独立于 Provider 存储，构成 `BalancingConfig`：策略（`round-robin` / `weighted-round-robin` / `failover`）、不健康阈值（默认连续失败 3 次）、自动恢复间隔（默认 300 秒）。该配置与 V3/V4 旧格式在首次读取时**惰性迁移**，旧 `provider-pool.json` 的成员权重与策略也会被吸收进新格式。\n\nSources: [runtime-config.ts](src/runtime-config.ts#L422-L432)、[runtime-config.ts](src/runtime-config.ts#L435-L463)、[runtime-config.ts](src/runtime-config.ts#L504)、[runtime-config.ts](src/runtime-config.ts#L1277-L1290)、[runtime-config.ts](src/runtime-config.ts#L1292-L1370)、[runtime-config.ts](src/runtime-config.ts#L1466-L1498)\n\n## ProviderPool：纯内存的池与三种选择算法\n\n`ProviderPool` 是一个**纯内存单例**——它不持有任何持久化状态，成员列表与均衡参数由 `refreshFromConfig()` 从 runtime-config V4 注入，健康状态（连续错误数、隔离时间、活跃会话数）只存在于进程内。每次 `selectProvider()` 调用前都会先执行 `refreshRecoveryState()`，把超过恢复间隔的 Provider 自动放回候选集，确保"池是否耗尽"的判定与下一次选择永远一致。\n\n选择算法先过滤出 `enabled && healthy` 的候选，再按策略挑选：\n\n```mermaid\nflowchart TD\n    A[selectProvider] --> B[refreshRecoveryState<br/>超恢复间隔自动转健康]\n    B --> C[过滤候选<br/>enabled && healthy]\n    C --> D{候选数 = 0?}\n    D -->|是| E[best-effort 兜底<br/>返回第一个 enabled 成员并告警]\n    D -->|否| F{策略}\n    F -->|round-robin| G[轮询游标<br/>idx = roundRobinIndex % N]\n    F -->|weighted-round-robin| H[累计权重区间<br/>target = index % totalWeight]\n    F -->|failover| I[恒选第一个候选<br/>即最高优先级健康者]\n```\n\n三种策略的行为差异可以用一张表概括：\n\n| 策略 | 分配逻辑 | 适用场景 |\n|---|---|---|\n| `round-robin` | 游标在候选间循环，每次 +1 | 同质化的多个账号，追求均匀消耗配额 |\n| `weighted-round-robin` | 在总权重上取模，按累计权重区间落点 | 账号配额/速率不同，按比例分流 |\n| `failover` | 恒取候选列表第一个 | 有明确主备关系，主健康时绝不使用备 |\n\n加权轮询的权重在候选过滤后重新钳制计算，避免配置了 `enabled=false` 的 Provider 残留权重干扰分配。值得注意的是**候选顺序就是故障转移的优先级**——候选列表保持配置中的成员顺序，因此"故障转移"策略实际上是把第一个健康者作为主用。\n\nSources: [provider-pool.ts](src/provider-pool.ts#L30-L43)、[provider-pool.ts](src/provider-pool.ts#L59-L77)、[provider-pool.ts](src/provider-pool.ts#L87-L158)、[provider-pool.ts](src/provider-pool.ts#L213-L232)、[provider-pool.ts](src/provider-pool.ts#L267)\n\n## 选择管线：池不是唯一入口\n\n`trySelectPoolProvider()` 是会话发起时唯一的 Provider 决策入口（Container 与 Host 双执行模式共用），但它内部是一条**带优先级的管线**，池的负载均衡只是其中一环：\n\n```mermaid\nflowchart TB\n    A[会话发起] --> B{Agent 钉住 modelConfigId<br/>或存在默认 Provider?}\n    B -->|是| C[权威直选<br/>绕过池，enabled 不约束]\n    B -->|否| D{工作区有环境变量级<br/>Provider 覆盖?}\n    D -->|是| E[返回 null<br/>用工作区自己的凭据]\n    D -->|否| F{会话已有粘性绑定<br/>且仍启用?}\n    F -->|是, 健康| G[复用绑定 Provider]\n    F -->|是, 不健康| H[落入池选择<br/>大概率换一家]\n    F -->|否| I{启用的 Provider 数}\n    I -->|1| J[直选唯一 Provider]\n    I -->|>1| K[池 selectProvider<br/>按均衡策略]\n```\n\n第一优先级是**Agent/默认模型配置绑定**：一旦某个 Agent 配置了 `model_config_id`，或系统设置了默认 Provider，该选择就是权威的，`enabled` 开关只控制"全局自动池"的成员资格，不约束 Agent 的显式绑定。这是 V5 配置引入 `defaultProviderId` 后的语义变化——池从"唯一选择器"降级为"未钉住时的兜底分配器"。\n\n第二优先级是**工作区环境变量覆盖**：如果群组（工作区）级别配置了 `anthropicApiKey` / `anthropicAuthToken` / `anthropicBaseUrl` 中的任意一项，则整个池被跳过，会话直接使用工作区自有凭据——这保证了"凭据归属"的边界不被池的全局选择破坏。\n\nSources: [container-runner.ts](src/container-runner.ts#L827-L957)、[container-runner.ts](src/container-runner.ts#L865-L871)、[runtime-config.ts](src/runtime-config.ts#L1399-L1410)\n\n## 会话粘性绑定：为什么不能随意换 Provider\n\n池要解决的不仅是"分流量"，还有一个更微妙的问题：**Claude Code 的 thinking block 签名与生成它的账号绑定**。如果会话 A 在 Provider X（某个 OAuth 账号）下产生了 thinking block，恢复会话时被池路由到 Provider Y（另一个账号/API Key），Claude Code 会返回 `Invalid signature in thinking block` 400 错误。\n\n为此系统在 `sessions` 表上维护 `provider_id` 绑定列，键为 `group_folder + agent_id`。每次成功选择后都会调用 `setSessionProviderId()` 刷新绑定；恢复会话时，若绑定仍启用且健康，则直接复用（sticky 路径），不再进入负载均衡。\n\n粘性绑定与池选择之间有一个精心设计的边界（`willClearSessionOnProviderSwitch`）：\n\n| 场景 | 行为 |\n|---|---|\n| 绑定 Provider 被删除/禁用 | 视为无绑定，池重新选择，新选择者接管 |\n| 只有一个启用 Provider | 天然粘性，无需绑定判断 |\n| 多个 Provider，绑定仍健康 | 复用绑定，不均衡 |\n| 多个 Provider，绑定不健康 | 落入池选择，优先换到健康者；此时需要清理旧 SDK 会话 |\n\n当 Provider 真的切换时，宿主会**先 `deleteSession()` 清掉旧 SDK 会话，再重新写入新 Provider 的绑定**——这个"删了再绑"的顺序是关键不变式：`deleteSession` 会连带删除整个 sessions 行（包括刚写入的 provider_id 绑定），所以必须重绑，否则下一回合又会重新均衡，导致绑定丢失。该不变式被 `provider-switch-session.test.ts` 以 DB 级测试钉死。\n\nSources: [db.ts](src/db.ts#L7857-L7898)、[container-runner.ts](src/container-runner.ts#L770-L813)、[container-runner.ts](src/container-runner.ts#L880-L920)、[provider-switch-session.test.ts](tests/provider-switch-session.test.ts#L36-L70)\n\n## 健康状态机：成功即复位，失败按级隔离\n\n池的健康状态是一个五字段状态机：`healthy`、`consecutiveErrors`、`lastErrorAt`、`lastSuccessAt`、`unhealthySince`，外加 `activeSessionCount` 会话计数。上报入口有三个：\n\n| 上报时机 | 调用 | 效果 |\n|---|---|---|\n| 容器流式输出标记 `providerFailure` | `reportFailure(id, true)` | **立即隔离**：错误数直接抬到阈值以上 |\n| 运行成功 / `closed` | `reportSuccess(id)` | 错误数归零，若处于隔离态则立即恢复 |\n| 运行错误且错误信息匹配 API 错误 | `reportFailure(id)` | 普通计数 +1，达阈值才隔离 |\n\n设计上最值得注意的一点是**"即时隔离"参数**：一旦 agent-runner 在流式输出中明确报告 Provider 失败（如账号额度用尽），宿主机调用 `reportFailure(id, true)` 把错误数直接推到阈值以上，使其**立刻**从候选集消失——而不是等下一次选择时才发现。更关键的是，这个隔离发生在等待 IM/卡片投影之前（注释明确要求"先隔离再投影"），避免并发会话在这段时间内继续选中刚失败的 Provider。\n\n恢复是**纯时间驱动**的：隔离中的 Provider 经过 `recoveryIntervalMs`（默认 5 分钟）后，在下一次 `selectProvider` 或故障处置判定时自动转回健康。这意味着额度类故障（如"resets 2:10am"）在恢复间隔过后会被重新试探——这是一个务实的折中：状态是内存态的，重启即全部复位，时间恢复让长期运行的系统不至于永久卡死在某个 Provider 上。\n\nSources: [provider-pool.ts](src/provider-pool.ts#L162-L192)、[provider-pool.ts](src/provider-pool.ts#L196-L204)、[provider-pool.ts](src/provider-pool.ts#L213-L232)、[container-runner.ts](src/container-runner.ts#L2040-L2046)、[container-runner.ts](src/container-runner.ts#L2132-L2147)\n\n## 故障分级：模型级回退 vs Provider 级隔离\n\nProvider 故障不是铁板一块。HappyClaw 把故障**按作用域分两级**处理，这是整个容错体系最核心的设计：\n\n```mermaid\nflowchart TB\n    subgraph 容器内["agent-runner（容器内）"]\n        ERR["SDK 失败信号"] --> CLS{作用域分类}\n        CLS -->|"account: 账号级<br/>额度/认证/overloaded"| PF["provider_failure 信号<br/>+ 隔离整个 Provider"]\n        CLS -->|"model: 模型级<br/>Opus/Sonnet/Fable 5 限额"| MF{配置了 fallbackModel?}\n        MF -->|是| SW["切换到 fallback 模型<br/>本回合重试，后续回合保持"]\n        MF -->|否| SR["surface_result<br/>结果可见，不隔离账号"]\n    end\n\n    subgraph 宿主["container-runner（宿主）"]\n        PFS["收到 providerFailure"] --> DP{还有别的<br/>健康 Provider?}\n        DP -->|是| R["retryElsewhere<br/>保留输入，failover 重放"]\n        DP -->|否| T["terminal<br/>向用户展示稳定失败通知"]\n    end\n```\n\n**模型级回退**发生在容器内的 agent-runner：`classifyProviderRateLimitType` 把 SDK 结构化拒绝（`rate_limit_event`）分为 `model`（`seven_day_opus` / `seven_day_sonnet` / `seven_day_overage_included`）与 `account`（`five_hour` / `seven_day` / `overage` 等）两类；对旧版 SDK 没有结构化事件的情况，用文本横幅正则兜底分类。模型级限额的处置是**透明切换模型**：`ProviderFallbackModelState` 记录主模型与回退模型，一旦激活回退，同进程内后续所有回合都留在回退档，避免每个回合都付一次失败调用。文本分类极其严格——`"You\'ve reached your storage limit"` 这类非 Provider 横幅绝不会触发隔离。\n\n**Provider 级隔离**发生在宿主机：当容器输出 `providerFailure`，宿主调用 `applyProviderFailureDisposition()` 刷新池状态后，用 `resolveProviderFailureDisposition()` 判断"除失败者外是否还有健康 Provider"。若有，则 `retryElsewhere: true`，输入被保留用于 failover 重放，流式卡片收起为"模型服务切换中，正在重试"；若池已耗尽，则 `terminal: true`，向用户投影稳定的中文通知（`PROVIDER_FAILURE_USER_NOTICE`），并在定时任务场景中由 `task-scheduler` 把该通知落为任务的持久错误。\n\nSources: [provider-fallback.ts](container/agent-runner/src/provider-fallback.ts#L30-L46)、[provider-fallback.ts](container/agent-runner/src/provider-fallback.ts#L64-L113)、[provider-fallback.ts](container/agent-runner/src/provider-fallback.ts#L127-L145)、[provider-fallback.ts](container/agent-runner/src/provider-fallback.ts#L178-L212)、[provider-failure.ts](src/provider-failure.ts#L22-L36)、[container-runner.ts](src/container-runner.ts#L480-L506)、[index.ts](src/index.ts#L7689-L7727)、[task-scheduler.ts](src/task-scheduler.ts#L1312-L1316)\n\n## failover 重放与定时任务的特殊规则\n\n"换一家重放"不是无条件的。宿主侧 `runAgentWithModelFallback()` 对重试次数与重放条件做了精细控制：\n\n| 条件 | 行为 |\n|---|---|\n| Agent 钉住 `modelConfigId` 或存在默认 Provider | `maxAttempts = 1`——**不跨 Provider 重试**，因为一个 Agent 只归属一套完整模型配置，换网关会破坏契约 |\n| 未钉住（旧安装） | `maxAttempts = 启用的 Provider 数`，逐个尝试 |\n| 定时任务在**输入已完成后**才遇到 Provider 失败 | 抑制重放并标记成功——避免副作用重复执行 |\n| 定时任务在输入完成前失败 | 同一 prompt 在下一个 Provider 重试 |\n\n重放的上游是消息循环：当 `providerFailureTerminal !== true` 时，`index.ts` 设置 `providerFailoverPending` 并**不消费输入游标**，让同一份持久化输入留在队列中被下一个选择周期重新拾取；只有终端失败才投影用户通知。这套"控制面信号不伪装成用户可见输出"的纪律，与渠道可靠性状态机（Inbox/Turn/Outbox）的语义一致——Provider 失败是控制面重试信号，用户只看到最终结果。\n\nSources: [container-runner.ts](src/container-runner.ts#L3135-L3231)、[index.ts](src/index.ts#L7696-L7727)、[index.ts](src/index.ts#L8633-L8636)、[provider-scheduled-fallback.test.ts](tests/provider-scheduled-fallback.test.ts#L41-L79)\n\n## 配置与运维：管理 API 与前端界面\n\nProvider 池的配置面在 `src/routes/config.ts` 中以 `/claude/*` 前缀暴露，全部经过 `systemConfigMiddleware` 权限门控：\n\n| 端点 | 作用 |\n|---|---|\n| `GET /claude/providers` | 列出全部 Provider + 健康状态 + 均衡配置 |\n| `POST /claude/providers` / `PATCH /claude/providers/:id` | 创建 / 更新非密钥字段 |\n| `PUT /claude/providers/:id/secrets` | 更新独立加密的密钥 |\n| `DELETE /claude/providers/:id` | 删除（保留无关会话绑定） |\n| `POST /claude/providers/:id/toggle` | 启用 / 停用 |\n| `POST /claude/providers/:id/reset-health` | 手动复位健康状态（`resetHealth()`） |\n| `GET /claude/providers/health` | 全量健康快照 |\n| `PUT /claude/balancing` | 更新策略、阈值、恢复间隔（`BalancingConfigSchema` 校验） |\n| `POST /claude/apply` | 把配置应用到所有容器（带变更锁，失败返回 503） |\n\n前端设置页有两个关键组件：`ProviderList` 用**健康指示灯**（绿=健康 / 红=有连续错误 / 灰=未启用）与权重徽标直观呈现池状态，`BalancingSettings` 提供策略下拉（轮询 / 加权轮询 / 故障转移）与两个高级参数（不健康阈值 1–20、恢复间隔 30–3600 秒）的可视化配置。前端表单对参数做了与后端一致的钳制（权重 1–100、阈值 1–20、恢复间隔 30–3600s），防止非法值进入配置。\n\nSources: [config.ts](src/routes/config.ts#L854-L880)、[config.ts](src/routes/config.ts#L1304)、[config.ts](src/routes/config.ts#L1357-L1369)、[config.ts](src/routes/config.ts#L1401-L1428)、[config.ts](src/routes/config.ts#L1430-L1452)、[BalancingSettings.tsx](web/src/components/settings/BalancingSettings.tsx#L24-L123)、[ProviderList.tsx](web/src/components/settings/ProviderList.tsx#L35-L58)\n\n## 双执行模式的一致性\n\nProvider 池的选择与健康上报在 Container 与 Host 两种执行模式下**共用同一套代码**：`trySelectPoolProvider` 同时服务两种模式；Host 模式在 `runHostAgent` 中走完全相同的选择管线，并在切换 Provider 时执行同样的"`deleteSession` + 重绑"序列。两处的健康上报也对称——流式输出中的 `providerFailure` 与 `providerFailureRetrying` 都会触发 `reportFailure(id, true)`，成功路径触发 `reportSuccess`，最后统一 `releaseSession` 归还会话计数。这种对称性保证了负载均衡策略不因执行模式而异，也意味着针对池的测试可以同时覆盖两种运行路径。\n\nSources: [container-runner.ts](src/container-runner.ts#L2518-L2553)、[container-runner.ts](src/container-runner.ts#L2903-L2924)、[container-runner.ts](src/container-runner.ts#L3063-L3075)\n\n## 小结\n\nProvider 池的本质是一个**带健康感知的候选选择器**，但 HappyClaw 在它之上叠加了三层语义，使其成为模型治理的中枢：**粘性**（会话与 Provider 绑定，保护 thinking block 签名）、**分级**（模型级限额透明回退，Provider 级故障隔离重放）、**钉住优先**（Agent/默认配置权威直选，池只服务未钉住的流量）。理解这三层，就能解释池的所有看似"不均衡"的行为——均衡永远让位于正确性与契约。\n\n建议继续阅读：[会话队列与并发控制](18-hui-hua-dui-lie-yu-bing-fa-kong-zhi)（Provider 失败重放与队列游标的关系）、[Runner 生命周期、卡死检测与故障恢复](21-runner-sheng-ming-zhou-qi-qia-si-jian-ce-yu-gu-zhang-hui-fu)（Provider 失败后的 Runner 处置）、[首次配置向导：管理员、模型提供商与渠道接入](4-shou-ci-pei-zhi-xiang-dao-guan-li-yuan-mo-xing-ti-gong-shang-yu-qu-dao-jie-ru)（Provider 的落地配置路径）。';export{r as default};

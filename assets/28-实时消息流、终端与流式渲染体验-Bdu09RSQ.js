const e='本页深入剖析 HappyClaw 的实时体验层：从 Agent Runner 产生的 `StreamEvent` 流式事件如何穿越 IPC 与 WebSocket 到达浏览器，到流式 Markdown 渲染、思考/工具过程的可视化，再到基于 xterm.js 与 node-pty 的容器终端。这些机制共同构成了用户与智能体交互时"所见即所得"的即时反馈链路。\n\n## StreamEvent：单一事实来源与类型同步\n\n流式体验的根基是一套**跨三端共享的规范化事件类型** `StreamEvent`。它定义于 `shared/stream-event.ts`，是唯一的事实来源；构建时由脚本同步到三个消费方：主服务（`src/stream-event.types.ts`）、Agent Runner（`container/agent-runner/src/stream-event.types.ts`）与 Web 前端（`web/src/stream-event.types.ts`），三处副本逐字节一致，禁止手工编辑。\n\n事件类型覆盖了智能体执行过程中的全部可观察行为：`text_delta` 与 `thinking_delta` 承载增量文本；`tool_use_start` / `tool_use_end` / `tool_progress` 描述工具生命周期；`task_start` / `task_progress` / `task_notification` 描述 SDK Task 与子 Agent；另有 `hook_*`、`memory_recall`、`compact_boundary`、`permission_denied`、`todo_update`、`usage`、`status`、`init` 等语义。事件还携带 `agentScope`（区分主 Agent / Task / 子 Agent / 系统）、`displayLevel`（控制 UI 优先级：primary 内联展示、detail 进 trace 面板、debug 仅开发者可见）以及 `turnId` / `queryRunId` 等关联字段。\nSources: [stream-event.ts](shared/stream-event.ts#L1-L25) [stream-event.ts](shared/stream-event.ts#L305-L397)\n\n同步由两个脚本保证：`sync-stream-event.sh` 在内容不一致时才覆盖拷贝，避免触发多余的增量构建；`check-stream-event-sync.sh` 在 CI 的 `typecheck` 阶段校验所有副本与 `shared/` 完全一致，任何偏离都会让流水线失败并提示执行 `make sync-types`。`Makefile` 中 `build` 目标以 `sync-types` 为前置依赖，确保编译产物总是基于最新类型。\nSources: [sync-stream-event.sh](scripts/sync-stream-event.sh#L1-L46) [check-stream-event-sync.sh](scripts/check-stream-event-sync.sh#L1-L50) [Makefile](Makefile#L40) [Makefile](Makefile#L187-L188)\n\n## 端到端流式事件管线\n\n流式事件的完整旅程横跨三个进程：容器内的 Agent Runner 消费 Claude Code SDK 的原始流，主服务负责关联与分发，浏览器负责增量渲染。下面用一张架构图呈现这条管线：\n\n```mermaid\nflowchart LR\n    subgraph Container["容器 / 宿主进程 (Agent Runner)"]\n        SDK[Claude Code SDK 流] --> SP[StreamEventProcessor]\n        SP -->|status:\'stream\' + streamEvent| IPC[IPC 输出通道]\n    end\n\n    subgraph Host["主服务 (Bun)"]\n        IPC --> RUN[runAgent / 调度器]\n        RUN --> FENCE[RunStreamFence 围栏]\n        FENCE -->|accepted| BROADCAST[broadcastStreamEvent]\n        BROADCAST --> SNAP[updateStreamingSnapshot]\n        BROADCAST --> WS1[(WebSocket 客户端)]\n        SNAP --> SNAPSTORE[(streamingSnapshots 内存快照)]\n    end\n\n    subgraph Web["浏览器 (React + Zustand)"]\n        WS1 -->|stream_event| STORE[chat store / handleStreamEvent]\n        STORE --> RAF[rAF 批处理 text_delta]\n        RAF --> STREAMING[streaming 状态]\n        STREAMING --> UI[StreamingDisplay 流式卡片]\n        STREAMING --> SCROLL[MessageList rAF 自动滚动]\n    end\n```\n\n管线起点是 Runner 内的 `StreamEventProcessor`。它接收 SDK 原始 `stream_event`，把 `text_delta` / `thinking_delta` 按**缓冲键**（主对话或某个 `parentToolUseId` 对应的子 Agent）聚合，以 100ms 定时或 200 字符阈值触发批量冲刷，避免高频帧压垮前端与 IM 卡片投影；`message_stop` 到达时强制冲刷短缓冲，确保消息语义边界正确闭合。工具调用、Skill、Task、子 Agent 消息则被转换为结构化的 `tool_use_*`、`task_*` 事件。冲刷产物以 `{ status: \'stream\', streamEvent: ... }` 形式经 IPC 输出通道送回主服务。\nSources: [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L55-L80) [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L340-L425) [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L436-L470)\n\n主服务收到事件后进入 `broadcastStreamEvent`：先做安全过滤（`context_audit` 属运维诊断数据，绝不进入用户 WebSocket，只落盘审计与告警），再经 `RunStreamFence` 判定事件归属的查询尝试，只有当前活跃 run 的事件才被接受，迟到的旧 run 回调被丢弃，防止 A→B 切换后旧事件污染新投影。通过围栏的事件经 `safeBroadcast` 推送给有权限的 WS 客户端，并同步写入 `streamingSnapshots` 快照（部分文本、思考文本、活跃工具、最近事件、任务状态、todos 等，均设长度上限），供断线重连恢复。\nSources: [web.ts](src/web.ts#L2901-L2966) [web.ts](src/web.ts#L2424-L2447) [web.ts](src/web.ts#L2662-L2877) [run-stream-fence.ts](src/run-stream-fence.ts#L1-L84)\n\n## WebSocket 实时通道与断线重连\n\n前端与主服务之间是一条应用级 WebSocket（`/ws`），由 `web/src/api/ws.ts` 中的 `WsManager` 单例管理。连接在 `AppLayout` 挂载时建立，使所有页面（不仅是聊天页）都具备实时能力。`WsManager` 采用**发布-订阅**模型：`on(type, handler)` 按消息类型注册监听并返回退订函数，`send(object)` 发送 JSON 消息；断线后以 1s 起步、30s 封顶的指数退避自动重连，并监听 `online` / `offline` 事件——网络恢复立即重连、离线时暂停重试避免空转。服务端关闭码 1008/4001（认证失败）会直接跳转登录页而非重连。\nSources: [ws.ts](web/src/api/ws.ts#L1-L126) [AppLayout.tsx](web/src/components/layout/AppLayout.tsx#L40-L50)\n\nWS 消息分为几大类：**流式事件**（`stream_event`，含 `chatJid` / `agentId` / `runId`）、**生命周期事件**（`run_started` / `run_finished` / `runner_state`，以精确 `runId` 界定一次查询尝试的起止）、**消息落库**（`new_message`，消除轮询延迟造成的消息"丢失"）、**快照恢复**（`active_run_snapshot` 与 `stream_snapshot`，见下文）、**终端协议**（`terminal_start` / `terminal_input` / `terminal_resize` / `terminal_stop` / `terminal_output` 等）以及各类状态广播。订阅分散在 `AppLayout`（全局生命周期）与 `ChatView`（当前会话的流式事件与快照）两层，保证切换页面后子 Agent 状态仍能更新。\nSources: [AppLayout.tsx](web/src/components/layout/AppLayout.tsx#L64-L100) [ChatView.tsx](web/src/components/chat/ChatView.tsx#L490-L555)\n\n## 前端流式渲染体验\n\n### 流式状态机与增量应用\n\n前端把一次执行的流式状态收敛到 Zustand 的 `streaming[chatJid]`（主会话）与 `agentStreaming[agentId]`（会话 Agent / 子 Agent）中，字段包括 `partialText`、`thinkingText`、`activeTools`、`recentEvents`、`traceEvents`、`taskStates`、`todos`、`systemStatus`、`activeHook`、`isThinking` 等。`handleStreamEvent` 是唯一入口：`text_delta` / `thinking_delta` 先进入**按 run 键控的 rAF 批处理队列**，同一动画帧内合并多次增量再一次性写入 store，避免高频事件触发过量 React 渲染；`tool_use_start` 记录工具开始时间并追加活动工具列表，`tool_use_end` 按 `toolUseId` 精确移除并生成"✓ 工具名 (耗时)"条目——缺失 ID 的异常 end 事件被忽略，防止误清全部活跃工具。\nSources: [chat.ts](web/src/stores/chat.ts#L2503-L2560) [chat.ts](web/src/stores/chat.ts#L1332-L1531)\n\n### StreamingDisplay：流式卡片的三层信息架构\n\n`StreamingDisplay` 组件把流式状态渲染为一张卡片，信息按"系统状态 → 思考 → 工具/任务 → 正文"自顶向下组织。**系统状态**行显示 `requesting`（正在处理…）/ `compacting`（正在整理上下文…）等瞬时状态；**思考块**（Reasoning）以琥珀色可折叠面板呈现 `thinkingText`，思考开始/结束时记录并展示耗时（"已思考 Xs"），并在思考→正文的转变瞬间自动折叠，消除流式卡片与落库消息之间的布局跳变；**工具活动**用 `ToolActivityCard` 渲染，展示工具名、耗时与参数摘要（Bash 显示命令、Read/Write 显示路径），嵌套工具缩进呈现层级；**Todo 进度**、**SDK Task 运行时块**与**动态 Workflow 卡片**则对应 `todo_update` 与 `task_*` 事件。权限拒绝（`permission_denied`）以红色醒目呈现，而非埋入 trace 面板。\nSources: [StreamingDisplay.tsx](web/src/components/chat/StreamingDisplay.tsx#L520-L719) [StreamingDisplay.tsx](web/src/components/chat/StreamingDisplay.tsx#L900-L970) [ToolActivityCard.tsx](web/src/components/chat/ToolActivityCard.tsx#L1-L80)\n\n### 交互模式对正文可见性的约束\n\n`shouldShowStreamingPartialText` 决定是否展示未落库的 `partialText`：assistant 模式下流式正文实时可见；proactive（主动回复）模式下正文只允许通过原生消息通道落库，流式卡片仅保留生命周期信号与"正在处理…"的占位提示，避免把未提交的回复渲染成正式消息。被中断的流式内容（`interrupt_partial`）会先冻结保留已输出部分，等 `new_message` 到达后再完成最终清理与呈现归一化。\nSources: [interaction-mode.ts](web/src/lib/interaction-mode.ts#L19-L23) [message-presentation.ts](web/src/lib/message-presentation.ts#L1-L45)\n\n### 流式 Markdown 渲染与自动滚动\n\n流式渲染的性能关键在 `MarkdownRenderer` 的 `streaming` 模式：跳过 KaTeX 数学渲染与 `rehypeSanitize` 安全插件（保留 GFM 与代码高亮），换取更快的增量渲染；同时通过廉价子串检测（`$$`、```` ``` ````）决定是否加载重型插件，首屏历史消息全量渲染也因此大幅提速。`MessageList` 的自动滚动采用**订阅 store 而非 React 渲染**的策略：直接监听 `partialText` / `thinkingText` 变化，用 `requestAnimationFrame` 合并到每帧一次 `scrollTo`，替代旧版 100ms 轮询；用户向上翻阅时置 `autoScroll=false` 停止跟随，且会为进行中的平滑滚动让路（600ms 锁定期），避免滚动被流式更新打断。\nSources: [MarkdownRenderer.tsx](web/src/components/chat/MarkdownRenderer.tsx#L254-L310) [MessageList.tsx](web/src/components/chat/MessageList.tsx#L410-L470)\n\n### 等待态与卡死自愈\n\n当 run 已开始但尚无任何流式数据时，卡片降级为"正在准备回复…"的加载态。`StreamingDisplay` 内部维护 10s 心跳定时器：若 `isWaiting` 且 `activeRuns` 中已无对应 run（权威生命周期已消失）、又长期无流式活动，则判定为孤儿等待态并自动清理，防止"永远转圈"的僵尸 UI。\nSources: [StreamingDisplay.tsx](web/src/components/chat/StreamingDisplay.tsx#L840-L900) [chat.ts](web/src/stores/chat.ts#L541-L554)\n\n## 终端：PTY 与兼容模式\n\n### 双模式终端架构\n\n容器终端采用"PTY 优先、管道兜底"的双模式设计。主服务中的 `TerminalManager` 按 `groupJid` 维护会话映射，启动时优先走 PTY 模式：由于 Bun 无法原生加载 node-pty 的原生插件，`terminal-manager.ts` 以子进程方式派生 `node pty-worker.cjs`，由 pty-worker 在 Node 运行时内完成 `node-pty` 的 spawn 与数据转发，主服务与 worker 之间用 **JSON Lines 协议**通信（stdin 收 `write` / `resize` / `kill`，stdout 发 `data` / `exit`）。PTY 模式提供完整终端语义：`TERM=xterm-256color`、行编辑、光标控制、全屏程序。\n\n```mermaid\nsequenceDiagram\n    participant Browser as 浏览器 (xterm.js)\n    participant WS as 主服务 WebSocket\n    participant TM as TerminalManager\n    participant PW as pty-worker (Node)\n    participant Docker as docker exec 容器\n\n    Browser->>WS: terminal_start {chatJid, cols, rows}\n    WS->>WS: 鉴权 / 容器状态检查 / 占用接管\n    WS->>TM: terminalManager.start()\n    TM->>PW: spawn node pty-worker.cjs\n    PW->>Docker: pty.spawn(\'docker\', [\'exec\',\'-it\',...])\n    WS-->>Browser: terminal_started\n    Browser->>WS: terminal_input {data}\n    WS->>TM: write()\n    TM->>PW: {type:\'write\', data}\n    PW->>Docker: p.write(data)\n    Docker-->>PW: pty output\n    PW-->>WS: {type:\'data\', data}\n    WS-->>Browser: terminal_output {data}\n    Browser->>Browser: terminal.write(data)\n```\n\n若 pty-worker 在 2 秒健康窗口内崩溃或 spawn 失败（如 node-pty 与 Node 版本不兼容），`TerminalManager` 判定 PTY 不可用并**永久降级为 pipe 模式**：直接 `docker exec -i` 派生 shell，stdin/stdout 管道直通。管道模式没有 PTY 行规程，`write()` 在本地维护行缓冲，自行处理退格（`\\b \\b` 回显擦除）、Ctrl-U 清行、Ctrl-W 删词、Ctrl-C 丢弃当前行、Ctrl-D 空行发 EOF，仅把编辑完成的整行在回车时送入 shell，并在启动时向用户提示"[terminal compatibility mode: no PTY available]"。\nSources: [terminal-manager.ts](src/terminal-manager.ts#L1-L79) [terminal-manager.ts](src/terminal-manager.ts#L130-L200) [terminal-manager.ts](src/terminal-manager.ts#L200-L345) [pty-worker.cjs](src/pty-worker.cjs#L1-L79)\n\n### 服务端终端协议与所有权\n\nWebSocket 消息处理器为终端定义了严格的**所有权模型**：`wsTerminals`（ws → groupJid）与 `terminalOwners`（groupJid → ws）双向映射确保同一时刻只有一个客户端持有某工作区的终端；新的 `terminal_start` 会先停掉同一 ws 的旧终端，若目标已被其他连接占用，则通知旧 owner"终端被其他连接接管"并释放。`terminal_input` / `terminal_resize` 均校验发起者是否为当前 owner，防止越权写入。启动前还依次检查：group 存在、调用者有访问权限、执行模式非 host（宿主机模式不支持终端）、容器活跃——容器未就绪时调用 `ensureTerminalContainerStarted` 触发"终端预热"排队任务并返回"工作区启动中"提示，前端收到该错误后 5 秒自动重连。\nSources: [web.ts](src/web.ts#L1856-L1997) [web.ts](src/web.ts#L2015-L2090) [index.ts](src/index.ts#L9439-L9456)\n\n### 前端 TerminalPanel：xterm.js 集成\n\n浏览器端 `TerminalPanel` 基于 `@xterm/xterm` 6.x 构建，配 `FitAddon`（自适应尺寸）与 `WebLinksAddon`（链接可点），采用 Tokyo Night 风格暗色主题，scrollback 5000 行。为控制首屏体积，`ChatView` 通过 `React.lazy` 将约 488KB 的 xterm.js 拆出聊天主包，仅在终端面板首次挂载时才加载。关键工程细节包括：\n\n- **尺寸同步**：FitAddon 在 CSS 高度过渡（200ms）结束后再 fit，避免动画期间算出 0×0；`ResizeObserver` 以 150ms 防抖监听尺寸变化，fit 后经 WS 发送 `terminal_resize`。\n- **IME 组合处理**：监听 `compositionstart` / `compositionend`，中文输入法组合期间拦截 `onData` 直通发送，防止重复输入泄漏。\n- **断线自愈**：订阅 `connected` / `disconnected`，WS 断开时提示并等待重连；`terminal_stopped` 非用户关闭时 3 秒自动重连，工作区启动中错误 5 秒重试，状态栏以绿/黄/灰圆点区分已连接/连接中/已断开。\n- **生命周期**：切换工作区或组件卸载时发送 `terminal_stop` 并释放会话；宿主模式自动强制关闭终端入口。\nSources: [TerminalPanel.tsx](web/src/components/chat/TerminalPanel.tsx#L1-L200) [TerminalPanel.tsx](web/src/components/chat/TerminalPanel.tsx#L200-L306) [ChatView.tsx](web/src/components/chat/ChatView.tsx#L53-L56) [ChatView.tsx](web/src/components/chat/ChatView.tsx#L720-L746) [ChatView.tsx](web/src/components/chat/ChatView.tsx#L1254-L1290)\n\n## 断线恢复：快照与 run 生命周期围栏\n\n实时体验的可靠性支柱是"**精确查询尝试（exact query attempt）**"模型。服务端在每次查询启动时广播 `run_started`（携带 `runId`），结束时广播 `run_finished`；前端 `run-lifecycle.ts` 维护 `activeRuns` 映射，只有 `runId` 精确匹配的流式事件才被应用，重连快照被视为权威投影——若新快照的 `runId` 取代了旧尝试，前端立即丢弃旧尝试的本地流式状态，防止 A 的迟到数据污染 B 的界面。\n\nWebSocket 重连时，服务端按顺序推送两批恢复数据：先是 `active_run_snapshot`（所有活跃 run 与排队会话，无精确 runId 的排队消息单独以 `queuedChatJids` 传递，避免重载后出现"空闲输入框"诱导重复发送），随后是每个活跃会话的 `stream_snapshot`（含截断后的部分文本、思考文本、活跃工具、最近事件、trace、任务状态、todos、系统状态与 `turnId`）。前端据此重建流式卡片与等待态，30 分钟内的快照才会被推送，过期快照与孤儿子 Agent 快照会被清除。服务端另有 `snapshotTombstones` 墓碑机制：run 结束后迟到的流事件不得重建已清理的快照，下一轮 run 启动时墓碑才被移除。\nSources: [run-lifecycle.ts](web/src/stores/run-lifecycle.ts#L1-L108) [web.ts](src/web.ts#L1510-L1610) [web.ts](src/web.ts#L3100-L3220)\n\n## 相关阅读\n\n- 流式事件类型体系的全量字段定义见 [StreamEvent 流式事件体系与类型同步](11-streamevent-liu-shi-shi-jian-ti-xi-yu-lei-xing-tong-bu)\n- Runner 侧事件生产与 IPC 回传细节见 [Agent Runner 与 IPC 通信协议](10-agent-runner-yu-ipc-tong-xin-xie-yi)\n- 会话排队与查询尝试（query attempt）的并发控制见 [会话队列与并发控制](18-hui-hua-dui-lie-yu-bing-fa-kong-zhi)\n- 前端整体架构与页面路由见 [前端架构与页面路由设计](27-qian-duan-jia-gou-yu-ye-mian-lu-you-she-ji)\n- 断线恢复与消息可靠性状态机见 [渠道可靠性状态机：Inbox、Turn 与 Outbox](16-qu-dao-ke-kao-xing-zhuang-tai-ji-inbox-turn-yu-outbox)';export{e as default};

const e='StreamEvent 是 HappyClaw 中贯穿「容器内 Agent Runner → 宿主机主服务 → 浏览器前端」三端的统一流式事件契约。本文聚焦两个层面：**事件模型本身**（24 种事件类型、40+ 字段的语义设计）与**类型同步机制**（如何保证三个独立 TypeScript 工程共享同一份类型定义且永不漂移）。理解本文后，你将能够判断一条事件从 Claude SDK 原始帧到前端渲染经历了哪些转换，以及为什么某些事件永远不会出现在用户界面上。\n\n## 设计动机：三层进程间的可序列化事件契约\n\nHappyClaw 的运行时天然跨进程：Claude Agent SDK 在 `container/agent-runner` 中执行（Docker 容器或宿主进程），主服务在 `src/` 中负责消息路由与 WebSocket 广播，浏览器在 `web/` 中渲染。SDK 原生流事件（`message_start`、`content_block_delta`、`system/task_started` 等）是 SDK 内部形态——字段命名蛇形、包含 SDK 私有结构、且不同 SDK 版本可能变化——不能直接跨 IPC 或 WebSocket 暴露。\n\nStreamEvent 因此被设计为**与 SDK 解耦的可序列化 DTO**：字段全部使用 camelCase、扁平的 `Record<string, unknown>` 可安全 JSON 序列化、且每个字段都有明确的跨端消费语义。`StreamEvent` 接口同时定义了 `agentScope`（哪个运行时角色产生的事件）、`displayLevel`（UI 优先级）、`turnId`/`queryRunId`/`sessionId`/`messageUuid`（关联键）等横切字段，使单条事件可以独立于传输层被消费端理解。核心类型定义见 [shared/stream-event.ts](shared/stream-event.ts#L297-L397)。\n\n```mermaid\nflowchart LR\n    subgraph Container["container/agent-runner（Docker 或宿主进程）"]\n        SDK["Claude Agent SDK 原始流事件"]\n        P["StreamEventProcessor<br/>语义转换 + 缓冲 + 合成"]\n    end\n    subgraph Host["src/（主服务）"]\n        CO["ContainerOutput.streamEvent"]\n        H["流处理管线<br/>计价归一化 / Answer 归约 / 围栏过滤"]\n        WS["WebSocket broadcastStreamEvent"]\n    end\n    subgraph Web["web/（浏览器）"]\n        STORE["chat store<br/>applyStreamEvent"]\n        UI["MessageBubble / WorkflowRunCard / 追踪面板"]\n    end\n    SDK --> P --> CO --> H --> WS --> STORE --> UI\n```\n\nSources: [shared/stream-event.ts](shared/stream-event.ts#L1-L46) · [container/agent-runner/src/stream-processor.ts](container/agent-runner/src/stream-processor.ts#L1-L120) · [container/agent-runner/src/types.ts](container/agent-runner/src/types.ts#L289-L340)\n\n## 单一事实来源与四副本同步机制\n\n这是本页最核心的工程机制。`shared/stream-event.ts` 被声明为 **canonical source of truth**，构建时被复制到三个子项目的同名目标文件中：\n\n| 目标文件 | 消费方 | 用途 |\n|---|---|---|\n| `container/agent-runner/src/stream-event.types.ts` | Agent Runner | SDK 事件 → StreamEvent 转换的产出类型 |\n| `src/stream-event.types.ts` | 主服务 | 消费 ContainerOutput、转发 WebSocket 的入参类型 |\n| `web/src/stream-event.types.ts` | 前端 | chat store 状态归约与渲染组件类型 |\n\ncanonical 文件头部注释明确写着「DO NOT edit the copies directly」——三个副本是编译产物，任何直接编辑都会在 CI 中被检出。见 [shared/stream-event.ts](shared/stream-event.ts#L1-L10)。\n\n同步链路由两个脚本 + Makefile 钩子 + CI 双保险构成：\n\n**同步脚本** `scripts/sync-stream-event.sh`：将 canonical 文件复制到三个目标，且仅在内容不同时复制（`cmp -s` 判断），避免无谓的时间戳变化触发增量构建。同一脚本还同步 `image-detector.ts`（两个目标）与 `channel-prefixes.ts`（两个目标），证明这是一套通用的共享类型治理模式，StreamEvent 只是其中最大的一个。见 [scripts/sync-stream-event.sh](scripts/sync-stream-event.sh#L1-L46)。\n\n**校验脚本** `scripts/check-stream-event-sync.sh`：逐一 `diff -q` 比对 canonical 与所有副本，任一缺失或漂移即返回非零退出码并提示 `make sync-types`。见 [scripts/check-stream-event-sync.sh](scripts/check-stream-event-sync.sh#L1-L50)。\n\n**Makefile 三层钩子**：`build` 目标首行依赖 `sync-types`（构建前强制同步）；`typecheck` 目标在四个子工程 `tsc --noEmit` 之后运行校验脚本（类型检查后验证同步）；生产启动路径另有 `_check-sync` 内部目标，通过比较 `shared/` 与副本的 mtime 增量检测漂移并在启动前自动同步。见 [Makefile](Makefile#L40)、[Makefile](Makefile#L75-L79)、[Makefile](Makefile#L150-L151)、[Makefile](Makefile#L187-L188)。\n\n**CI 双保险**：`Verify generated type sources` 步骤先执行 `make sync-types`，再对四个文件运行 `git diff --exit-code`——若同步后仍有 diff，说明有人直接改了副本而未改 canonical，CI 直接失败；随后 prettier 检查格式一致性。这比单纯依赖「复制即同步」更严格：它把「canonical 未被编辑」本身变成可验证事实。见 [.github/workflows/ci.yml](.github/workflows/ci.yml#L46-L49)。\n\n```mermaid\nflowchart TD\n    A["编辑 shared/stream-event.ts<br/>（唯一合法入口）"] --> B{"make build / make sync-types<br/>或启动时 _check-sync"}\n    B --> C["sync-stream-event.sh<br/>cmp -s 仅复制差异内容"]\n    C --> D["3 个类型副本"]\n    D --> E["make typecheck<br/>check-stream-event-sync.sh diff 校验"]\n    D --> F["CI: make sync-types +<br/>git diff --exit-code + prettier"]\n    E --> G["编译通过"]\n    F --> G\n```\n\nSources: [shared/stream-event.ts](shared/stream-event.ts#L1-L10) · [scripts/sync-stream-event.sh](scripts/sync-stream-event.sh#L1-L46) · [Makefile](Makefile#L150-L151) · [.github/workflows/ci.yml](.github/workflows/ci.yml#L46-L49)\n\n## 事件类型全景：24 种事件按功能域归类\n\n`StreamEventType` 联合类型定义了 24 种事件，按功能域可划分为六个族系：\n\n| 功能域 | eventType | 典型消费者 |\n|---|---|---|\n| **文本流** | `text_delta`、`thinking_delta` | 前端 partialText / thinkingText 累积、飞书卡片正文 |\n| **工具执行** | `tool_use_start`、`tool_use_end`、`tool_progress`、`tool_result` | 前端 activeTools 列表、追踪面板、卡片工具指示器 |\n| **Hook 生命周期** | `hook_started`、`hook_progress`、`hook_response` | 前端 hook 状态指示器、卡片 |\n| **SDK Task / 子 Agent** | `task_start`、`task_progress`、`task_updated`、`task_notification` | 前端 StreamingTaskRuntimeState、飞书卡片任务块 |\n| **系统与生命周期** | `status`、`init`、`raw_sdk_event`、`compact_boundary`、`notification`、`prompt_suggestion`、`permission_denied`、`memory_recall`、`todo_update`、`usage`、`context_audit` | 前端 recentEvents 时间线、服务端日志、计价账本 |\n| **工作流投影** | `task_*` 内嵌的 `workflowRun` 字段 | 前端 WorkflowRunCard 一等公民渲染 |\n\n类型定义见 [shared/stream-event.ts](shared/stream-event.ts#L12-L36)。值得注意的是事件类型是可扩展的字符串联合而非枚举——这使 SDK 新增事件形态时可以向前兼容（未知类型落入 `raw_sdk_event` 兜底），同时前端 `applyStreamEvent` 的 switch 对未列出的类型静默跳过，不会因类型爆炸而崩溃。见 [web/src/stores/chat.ts](web/src/stores/chat.ts#L1329-L1341)。\n\n两个横切字段定义了事件的「身份与优先级」：\n\n- **`agentScope`**：`main`（主 Agent）、`task`（SDK Task/子任务）、`subagent`（嵌套子 Agent 消息）、`system`（系统合成信号）。前端用其区分主对话流与 Task 运行时状态。\n- **`displayLevel`**：`primary`（内联展示）、`detail`（追踪面板）、`debug`（开发者追踪）。`raw_sdk_event` 默认 `debug`，仅当 SDK 错误类消息（如 `mirror_error`）提升为 `primary` 才会进入用户可见时间线。见 [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L1295-L1300)。\n\nSources: [shared/stream-event.ts](shared/stream-event.ts#L12-L46) · [web/src/stores/chat.ts](web/src/stores/chat.ts#L1329-L1341) · [container/agent-runner/src/stream-processor.ts](container/agent-runner/src/stream-processor.ts#L1295-L1300)\n\n## 端到端数据流：从 SDK 原始帧到前端渲染\n\n事件在每个环节经历不同的转换，理解各环节职责是排障的前提：\n\n**① 语义转换（Agent Runner）**：`StreamEventProcessor.processStreamEvent()` 接收 SDK 的 `stream_event` 帧，识别 `message_start/stop`、`content_block_start/delta` 等结构，将原始帧映射为带语义的 StreamEvent。`message_start/stop` 被转换为 `raw_sdk_event`（rawType `stream_event/message_start`）——这两个边界帧是宿主机 Answer 归约器判定「一段完整回复结束」的语义提交点。见 [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L439-L500)。\n\n**② 缓冲与合成（Agent Runner）**：文本增量并非逐帧透传。`text_delta` 进入按 `parentToolUseId` 键控的缓冲区（顶层用 `__main__` 键），每 100ms 或累计 200 字符刷出一次；`message_stop` 到达时强制刷出残留缓冲，避免短文本被计时器误判为新消息。见 [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L58-L67)。\n\n**③ 包装进 ContainerOutput（Agent Runner）**：所有事件经 `emitStreamEvent()` 统一包装为 `{ status: \'stream\', result: null, streamEvent }` 的 `ContainerOutput` 输出。`ContainerOutput` 还携带 `inputTurnId`（不可变用户输入轮次标识）、`turnId`（展示层轮次标识）、`sessionId` 等关联字段。见 [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L217-L219) 与 [container/agent-runner/src/types.ts](container/agent-runner/src/types.ts#L289-L340)。\n\n**④ 宿主处理（主服务）**：主服务在 `runAgent` 回调中处理每条流式输出，依次执行：steering 顶替抑制 → Workflow 运行快照登记 → 断流触顶信号收口 → usage 计价归一化（与账本一致的 Kaboo 计价入口，保证流式金额与最终统计一致）→ Answer 归约 → 广播决策 → 飞书卡片更新。见 [src/index.ts](src/index.ts#L7174-L7273)。\n\n**⑤ WebSocket 广播（主服务）**：`broadcastStreamEvent()` 执行两个关键过滤：`context_audit` 事件**永远留在服务端**（仅记录运行上下文快照到内存，供 `stream_snapshot` 重连恢复）；其余事件经 `streamRunFence` 围栏判定后以 `WsMessageOut` 的 `stream_event` 类型推送。见 [src/web.ts](src/web.ts#L2901-L2960)。\n\n**⑥ 前端归约（浏览器）**：chat store 的 `handleStreamEvent` 接收后先跳过 `context_audit`、校验 run 作用域，再对 `text_delta`/`thinking_delta` 做 **requestAnimationFrame 批量**（同一帧内的多个 delta 合并为一次状态更新，避免高频 setState 卡顿），其余事件进入 `applyStreamEvent` 状态归约。见 [web/src/stores/chat.ts](web/src/stores/chat.ts#L2503-L2560)。\n\n```mermaid\nsequenceDiagram\n    participant SDK as Claude SDK\n    participant P as StreamEventProcessor\n    participant CO as ContainerOutput\n    participant H as 主服务 runAgent 回调\n    participant WS as broadcastStreamEvent\n    participant S as chat store\n    SDK->>P: stream_event / content_block_delta\n    P->>P: 文本缓冲 (100ms / 200字符)\n    P->>CO: { status:\'stream\', streamEvent }\n    CO->>H: onOutput 回调\n    H->>H: 顶替抑制 / 计价归一化 / Answer 归约\n    H->>WS: context_audit? → 留服务端\n    WS->>S: WsMessageOut stream_event\n    S->>S: rAF 批量 delta / applyStreamEvent\n```\n\nSources: [container/agent-runner/src/stream-processor.ts](container/agent-runner/src/stream-processor.ts#L439-L500) · [src/index.ts](src/index.ts#L7174-L7273) · [src/web.ts](src/web.ts#L2901-L2960) · [web/src/stores/chat.ts](web/src/stores/chat.ts#L2503-L2560)\n\n## 核心事件语义详解\n\n### 文本与思考流：双轨累积与心跳合成\n\n`text_delta` 与 `thinking_delta` 是仅有的两个携带增量文本的事件。前端对顶层事件（无 `parentToolUseId`）累积到 `partialText`/`thinkingText`（分别截断至 16000/8000 字符），对带 `parentToolUseId` 的增量则路由到对应 Task 的运行时状态，避免子 Agent 文本污染主对话。见 [web/src/stores/chat.ts](web/src/stores/chat.ts#L1332-L1370) 与 [web/src/stores/chat.ts](web/src/stores/chat.ts#L649-L652)。\n\nSDK 的 `thinking_tokens` 系统消息是高频近似计数器，一次推理密集型回合可能到达数千帧。Processor 将其**合成为低频语义心跳**：最多每 2 秒发一条 `status` 事件（`statusText: \'正在深入分析…\'`），且标记 `isSynthetic: true`——这条事件不是 SDK 语义的直接产物，而是本地合成的存活信号，原始 token 计数**永不透传**（避免暴露思维链细节）。测试 `coalesces thinking_tokens into a low-frequency semantic heartbeat` 验证了 2 秒窗口内只产生 2 条心跳且 JSON 中不含 `estimated_tokens`。见 [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L1300-L1326) 与 [tests/stream-processor.test.ts](tests/stream-processor.test.ts#L57-L100)。\n\n### 工具事件：start/end 配对与结果提取\n\n`tool_use_start`/`tool_use_end` 由 `content_block_start` 的 `tool_use` 块与 `tool_result` 块推导。`tool_result` 事件携带 `toolResult` 字段——工具执行结果文本经截断与净化后透传，使前端追踪面板与 Claude Code 原生展示对齐；失败的工具调用以 `⚠️` 前缀标记。主 Agent 与子 Agent 路径分别提取（`processMainToolResults` 处理主路径，子路径在消息转换时内联处理）。见 [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L1545-L1625)。\n\n### Task 事件：SDK 任务生命周期到前端运行时状态\n\nSDK 的 `system/task_started`、`task_progress`、`task_updated`、`task_notification` 被映射为四个 Task 事件。前端维护 `StreamingTaskRuntimeState` 记录（标题、状态机 `running/completed/error/backgrounded`、thinkingTail/textTail、activeTools），并用 `taskIdFromEvent` 从 `parentToolUseId`/`taskId`/`toolUseId` 推导关联键。SDK 终态集合（`completed/failed/killed`）在三端各有等价映射——漏判的代价是 `pendingSdkTasks` 泄漏导致关流被永久推迟。见 [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L1030-L1080) 与 [web/src/stores/chat.ts](web/src/stores/chat.ts#L1163-L1180)。\n\n### usage 事件：幂等计价的流式通道\n\n`usage` 事件在查询完成时发出，携带 `eventId`（稳定逻辑运行 ID，由 SDK result UUID 派生 `sdk-result:<uuid>`）、`batchIndex`/`batchCount`（每条消息账本批次定位）、完整 token 四维与 `costUSD`。主服务先经 `writeUsageRecords` 走与账本相同的计价入口归一化 `costUSD`，再合并批次后广播。`eventId` 使计费幂等去重，且与最终消息关联。见 [container/agent-runner/src/index.ts](container/agent-runner/src/index.ts#L1980-L2034) 与 [src/index.ts](src/index.ts#L7218-L7260)。\n\n### workflowRun 快照：动态工作流的一等公民投影\n\nSDK Task 若为 Claude Code 动态 Workflow，事件额外携带 `workflowRun` 字段——一个可持久化的 `WorkflowRunSnapshot`（阶段列表 + Agent 快照列表 + 汇总统计）。快照有三个来源：`workflowRunFromToolInput`（从 `Task` 工具输入的 script 解析计划，运行前即可投影）、`workflowRunFromTaskProgress`（累积进度合并）、`workflowRunFromOutputFile`（输出文件回读）。主服务维护 `activeWorkflowRuns`/`completedWorkflowRuns`，前端以 WorkflowRunCard 一等公民渲染。见 [container/agent-runner/src/workflow-run.ts](container/agent-runner/src/workflow-run.ts#L209-L260) 与 [src/index.ts](src/index.ts#L7174-L7195)。\n\nSources: [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L1300-L1326) · [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L1545-L1625) · [container/agent-runner/src/index.ts](container/agent-runner/src/index.ts#L1980-L2034) · [container/agent-runner/src/workflow-run.ts](container/agent-runner/src/workflow-run.ts#L209-L260) · [tests/stream-processor.test.ts](tests/stream-processor.test.ts#L57-L100)\n\n## 关联、防乱序与生命周期治理\n\n流式场景的经典难题是**迟到的旧事件污染新状态**。StreamEvent 体系通过四层关联键 + 围栏解决：\n\n| 关联键 | 粒度 | 用途 |\n|---|---|---|\n| `turnId` | 一次用户轮次 | 关联同一轮次的所有事件 |\n| `queryRunId` | 一次 GroupQueue 查询尝试 | 精确区分重试/顶替 |\n| `sessionId` | SDK 会话 | 关联 warm runner 的会话身份 |\n| `messageUuid` | 单条 SDK 消息 | Answer 归约与幂等记账 |\n\n主服务侧的 `RunStreamFence` 维护每个运行时 JID 的 active run 映射与 turn 归属表：`observeExact` 用于 runner 携带 `queryRunId` 的精确事件（合法重试可复用同一 durable turnId 但带新 runId，旧尝试的迟到事件保留旧 runId 并被拒绝）；`observe` 用于遗留路径——未携带 turnId 的事件在精确 run 活跃期间直接丢弃，宁可丢不错标。见 [src/run-stream-fence.ts](src/run-stream-fence.ts#L1-L60) 与 [src/web.ts](src/web.ts#L2918-L2940)。\n\n顶替场景（steering transition）下，主服务在进入流处理管线前先经 `steeringTransitions.shouldSuppressOutput` 判定——被顶替轮次的输出整体抑制。前端侧 `shouldApplyRunScopedPayload` 再做一层 run 作用域校验，双端夹击确保状态一致。见 [src/index.ts](src/index.ts#L7115-L7140) 与 [web/src/stores/chat.ts](web/src/stores/chat.ts#L2512-L2516)。\n\nAnswer 归约器（`TurnOutputCoordinator`）是防乱序的语义层：它监听 `raw_sdk_event/message_start|stop` 边界帧，将 `text_delta` 累积进当前消息，若消息内含顶层工具调用则判定该段文本为叙述而非最终答案（`narrationDiscarded`）——这保证「模型先说话再调工具」的轮次不会把临时叙述误发为用户可见回复。见 [src/turn-output-coordinator.ts](src/turn-output-coordinator.ts#L95-L170)。\n\nSources: [src/run-stream-fence.ts](src/run-stream-fence.ts#L1-L60) · [src/web.ts](src/web.ts#L2918-L2940) · [src/index.ts](src/index.ts#L7115-L7140) · [src/turn-output-coordinator.ts](src/turn-output-coordinator.ts#L95-L170)\n\n## 可见性治理：什么事件不该出现在用户面前\n\nStreamEvent 体系对「用户能看到什么」做了三层治理，这是理解事件广播边界的关键：\n\n**① context_audit 服务端隔离**：`context_audit` 携带宿主机路径、提示词接线、框架术语等运行时诊断信息。`broadcastStreamEvent` 收到后仅记录 `runContextSnapshot`（供重连 `stream_snapshot` 恢复）并记录告警日志，**永不推送 WebSocket**；前端同样跳过该类型。两端各有一道独立防线。见 [src/web.ts](src/web.ts#L2902-L2917) 与 [web/src/stores/chat.ts](web/src/stores/chat.ts#L2507-L2510)。\n\n**② Proactive 模式广播收窄**：`shouldBroadcastSdkStreamEvent` 在 assistant 交互模式下放行全部事件；在 proactive/persona 模式下仅放行 `status` 事件中的 `requesting`/`idle`/`interrupted` 三个生命周期边界——模型思考、工具、usage、工作流均被视为私有模型循环状态，只暴露 spinner 启停所需的最小信号。见 [src/workspace-interaction-runtime.ts](src/workspace-interaction-runtime.ts#L146-L157)。\n\n**③ displayLevel 驱动的前端过滤**：`pushTrace` 将 `text_delta`、`thinking_delta`、`usage`、`init`、`context_audit` 及内部生命周期状态排除出追踪时间线（它们各自有专门的状态通道）；`raw_sdk_event` 仅在 `displayLevel === \'primary\'` 时进入 recentEvents。追踪时间线裁剪至最近 200 条。见 [web/src/stores/chat.ts](web/src/stores/chat.ts#L1129-L1160) 与 [web/src/stores/chat.ts](web/src/stores/chat.ts#L652)。\n\n```mermaid\nflowchart TD\n    E["SDK 原始事件"] --> P["StreamEventProcessor"]\n    P -->|"context_audit"| S1["服务端快照 + 日志（不广播）"]\n    P -->|"其他事件"| H["主服务管线"]\n    H -->|"Proactive 模式"| F{"status ∈<br/>requesting/idle/interrupted?"}\n    F -->|否| DROP["丢弃"]\n    F -->|是| WS["WebSocket"]\n    H -->|"assistant 模式"| WS\n    WS --> S["chat store"]\n    S -->|"displayLevel=debug"| TRACE["仅开发者追踪"]\n    S -->|"displayLevel=primary/detail"| UI["用户可见时间线/卡片"]\n```\n\nSources: [src/web.ts](src/web.ts#L2902-L2917) · [src/workspace-interaction-runtime.ts](src/workspace-interaction-runtime.ts#L146-L157) · [web/src/stores/chat.ts](web/src/stores/chat.ts#L1129-L1160)\n\n## 质量保障：同步校验与契约测试\n\nStreamEvent 体系的质量保障分两条线：\n\n**类型同步线**：`make typecheck` 在四个子工程类型检查后运行 `check-stream-event-sync.sh`；CI 的 `Verify generated type sources` 用 `git diff --exit-code` 把「副本未漂移」固化为不可绕过的门禁。任何直接编辑副本的行为都会在三处（本地 typecheck、CI diff、prettier）被拦截。见 [Makefile](Makefile#L150-L151) 与 [.github/workflows/ci.yml](.github/workflows/ci.yml#L46-L49)。\n\n**事件语义线**：`tests/stream-processor.test.ts` 从 `StreamEventProcessor` 的产出断言契约——`message_start/stop` 边界的 raw_sdk_event 与文本增量顺序、thinking 心跳节流、detached bash 不阻塞输入等。这些测试直接构造 SDK 帧、断言转换后的 StreamEvent 结构，是事件语义的活文档。见 [tests/stream-processor.test.ts](tests/stream-processor.test.ts#L14-L100)。\n\n## 延伸阅读\n\n- 事件如何在主服务中与群聊队列、IPC 交付协同：[Agent Runner 与 IPC 通信协议](10-agent-runner-yu-ipc-tong-xin-xie-yi)\n- 流式事件在前端渲染层的落点：[实时消息流、终端与流式渲染体验](28-shi-shi-xiao-xi-liu-zhong-duan-yu-liu-shi-xuan-ran-ti-yan)\n- 事件消费方之一：渠道可靠性状态机如何保证送达：[渠道可靠性状态机：Inbox、Turn 与 Outbox](16-qu-dao-ke-kao-xing-zhuang-tai-ji-inbox-turn-yu-outbox)\n- StreamEvent 同步校验所在的工程规范体系：[CI 流水线与工程规范](30-ci-liu-shui-xian-yu-gong-cheng-gui-fan)';export{e as default};

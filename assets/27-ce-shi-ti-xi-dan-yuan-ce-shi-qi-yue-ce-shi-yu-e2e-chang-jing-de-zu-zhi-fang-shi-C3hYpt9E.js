const t='Miniclaw 的测试体系围绕一个核心决策展开：**用一个根级 Vitest 实例统一发现后端、前端组件与 Agent Runner 三端的全部测试**，再通过命名后缀（`*-contract`、`*.integration`、`*-dom`）划分语义层级，用临时目录 + `vi.mock` 保证零环境污染，最后把"跨进程、跨包、跨仓库工件"的不变量交给契约测试与同步防线守护。本文剖析这套体系的组织方式、隔离模式与 CI 编排。\n\n## 测试拓扑：单实例 Vitest 统管三端\n\n仓库只在根部放置一份 `vitest.config.ts`，且**没有设置自定义 `include` 规则**，因此 Vitest 走默认发现规则扫描全仓的 `*.test.ts`/`*.test.tsx`——这同时覆盖了根目录 `tests/` 的 300 余个文件和 `container/agent-runner/tests/` 下的 Runner 测试。排除列表只保留四类目录：`node_modules`、`dist`、`data/`（用户的 Agent 运行时工作区，其中可能嵌套带自己测试套件的第三方项目，Vitest 不遵守 `.gitignore` 所以必须显式排除）、以及属于 Playwright 管辖的 `web/tests/e2e/**`。\n\n这份配置还有一个关键设计：将 `@` 别名指向 `./web/src`，并把 `react`、`react-dom`、`react-router-dom` 解析到 `web/node_modules` 下。这意味着**根目录的测试可以直接以 happy-dom 渲染真实的 React 组件**，无需为前端单独维护一套 Vitest 工程——三个 `.test.tsx` DOM 测试正是依赖这一别名才能在根套件中运行。\n\nSources: [vitest.config.ts](vitest.config.ts#L4-L31), [tests/agent-profiles-page-dom.test.tsx](tests/agent-profiles-page-dom.test.tsx#L1-L11)\n\n入口命令分为两层：日常开发用 `make test`（等价于 `npx vitest run`）；完整自检用 `npm run self-test`，它串联了"编译三端产物 → 执行 Agent Runner 自检脚本 → 全量跑 Vitest"三个阶段，保证测试运行在最新构建之上。\n\nSources: [Makefile](Makefile#L164-L165), [package.json](package.json#L11-L19)\n\n## 命名约定即分类法\n\n在没有独立目录分层的前提下，`tests/` 目录依靠**命名后缀与前缀家族**承担了分类职能。以下是从文件系统实测的分布：\n\n| 命名模式 | 数量 | 语义层级 | 代表文件 |\n| --- | --- | --- | --- |\n| `*.test.ts`（无特殊后缀） | ~280 | 单元/宿主集成 | `group-queue-reentrancy.test.ts` |\n| `*-contract.test.ts` | 26 | 契约测试 | `electron-shell-contract.test.ts` |\n| `*.integration.test.ts` | 2 | SDK/CLI 集成 | `agent-runner-sdk-subagent-contract.integration.test.ts` |\n| `*-dom.test.tsx` | 3 | 浏览器环境 DOM 测试 | `agent-profiles-page-dom.test.tsx` |\n| `frontend-*` 前缀 | 22 | 前端产品契约 | `frontend-product-terminology.test.ts` |\n| `routes-*` 前缀 | 24 | HTTP API 层测试 | `routes-tasks-contract.test.ts` |\n| `schema-v*` 前缀 | 12 | 数据库迁移回归 | `schema-v65-workspace-memory.test.ts` |\n\nDOM 测试通过文件首行的 `// @vitest-environment happy-dom` 注释指令切换单文件测试环境——这是 Vitest 的 per-file environment 机制，使这三个组件渲染测试能与数千个 Node 环境用例共存于同一次 `vitest run` 中，而 `happy-dom` 作为 devDependency 安装在根 `package.json`。\n\nSources: [tests/agent-profiles-page-dom.test.tsx](tests/agent-profiles-page-dom.test.tsx#L1-L2), [package.json](package.json#L60-L75)\n\n```mermaid\ngraph TD\n    subgraph Root["根级 Vitest 单实例（npm test）"]\n        A["单元/宿主集成<br/>~280 个 *.test.ts"]\n        B["契约测试<br/>26 个 *-contract.test.ts"]\n        C["SDK 集成<br/>2 个 *.integration.test.ts"]\n        D["DOM 测试<br/>3 个 *-dom.test.tsx<br/>(happy-dom)"]\n        E["Runner 测试<br/>container/agent-runner/tests/"]\n    end\n    F["Playwright E2E<br/>web/tests/e2e/ (独立进程)"]\n    G["进程级 E2E 脚本<br/>tests/e2e/*.mjs (手动触发)"]\n    H["冒烟/自检脚本<br/>scripts/* (构建后运行)"]\n\n    D -. "@ 别名 → web/src" .-> WEB["web/src React 组件"]\n    E -. 相对导入 .-> AR["container/agent-runner/src"]\n    A -. vi.mock(config) + 临时 SQLite .-> SRC["src/ 后端模块"]\n```\n\n## 隔离模式：临时目录 + 模块 mock + 动态导入\n\n数据库相关测试（schema 迁移、事务、路由 ACL 等）共享一套高度一致的隔离配方，实测有 123 个测试文件使用 `fs.mkdtempSync(os.tmpdir())` 创建一次性目录。其完整范式分三步：先建临时目录并写入真实 SQLite 文件；再用 `vi.mock(\'../src/config.js\')` 把 `STORE_DIR`/`GROUPS_DIR`/`DATA_DIR` 重定向到该目录；最后**在 mock 声明之后用 `await import()` 动态导入被测模块**——因为 ES Module 的静态绑定在模块首次求值时固化，动态导入确保模块内部读到的路径是 mock 后的值。\n\n```typescript\n// 1. 临时目录承载真实 better-sqlite3 文件\nconst tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), \'db-tx-test-\'));\n// 2. 重定向路径常量\nvi.mock(\'../src/config.js\', async () => ({\n  STORE_DIR: tmpStoreDir, GROUPS_DIR: tmpGroupsDir,\n}));\n// 3. mock 之后才动态导入，绑定生效\nconst { initDatabase } = await import(\'../src/db.js\');\n```\n\nSources: [tests/db-transactions.test.ts](tests/db-transactions.test.ts#L10-L26), [tests/schema-v65-workspace-memory.test.ts](tests/schema-v65-workspace-memory.test.ts#L9-L22)\n\n这一范式的两个变体值得注意：一是路由测试常用 `vi.mock(importOriginal)` 做**部分 mock**——保留真实 `config.js` 导出其余成员，仅覆写目录常量；二是验证类断言往往绕开被测模块，另开一条 `readonly` 的 better-sqlite3 探针连接直接读库文件，避免依赖内部未导出的 `db` 绑定。这种"写走被测代码、读走独立连接"的结构让断言建立在持久化事实而非内存状态上。\n\nSources: [tests/routes-tasks-contract.test.ts](tests/routes-tasks-contract.test.ts#L17-L23), [tests/db-transactions.test.ts](tests/db-transactions.test.ts#L29-L37)\n\n## 契约测试：以源码文本为规格的边界守护\n\n`*-contract.test.ts` 家族守护的是**类型系统无法表达的跨边界不变量**。其实现手法非常一致：用 `readFileSync` 把源码、配置或 CI 工作流当作字符串读入，然后对文本做正包含/排除断言。这类测试本质上是"可执行的架构决策记录"——一旦有人改动破坏约定，测试会在 PR 阶段给出精确到关键词的失败信息。\n\n| 契约测试 | 守护对象 | 断言的关键不变量 |\n| --- | --- | --- |\n| [electron-shell-contract](tests/electron-shell-contract.test.ts#L15-L21) | Electron 主/preload 源码 | `contextIsolation: true`、preload 不含 `fs`/`child_process` |\n| [makefile-runtime-contract](tests/makefile-runtime-contract.test.ts#L8-L31) | Makefile | 生产启动唯一入口是 `node dist/index.js`，禁止 pm2 回潮 |\n| [docker-publish-workflow-contract](tests/docker-publish-workflow-contract.test.ts#L9-L35) | GitHub Actions 工作流 | 双架构构建先行冒烟、digest 校验后才晋升 `latest` |\n| [frontend-product-terminology](tests/frontend-product-terminology.test.ts#L8-L44) | Web 页面源码 | 产品术语统一为"智能体"，同时保留 Pi Runtime 技术名词 |\n\n值得注意的是最后一类：`frontend-*` 家族中有相当一部分并不测试行为，而是**治理产品文案与交互术语**（例如要求导航栏使用"智能体"而非"Agent"）。这类测试把 UX 决策也纳入了机器校验范围，是"契约"概念在本项目中的延伸用法。\n\nSources: [tests/electron-shell-contract.test.ts](tests/electron-shell-contract.test.ts#L4-L13), [tests/frontend-product-terminology.test.ts](tests/frontend-product-terminology.test.ts#L7-L44)\n\n## 三端共享类型的同步防线\n\n除测试文件外，还有一组"元测试"性质的 shell 校验嵌入 `make typecheck` 流程。[check-stream-event-sync.sh](scripts/check-stream-event-sync.sh#L6-L28) 用 `diff` 逐一比对 `shared/` 下的规范类型文件与其在三端（`src/`、`web/src/`、`container/agent-runner/src/`）的副本，任何漂移都会以非零码失败。CI 中另有一道更严格的闸门：执行 `make sync-types` 后用 `git diff --exit-code` 验证生成物没有产生未提交变更。这两道防线共同保证 StreamEvent 三端契约的一致性，细节见 [StreamEvent 流式契约：三端共享类型与同步机制](25-streamevent-liu-shi-qi-yue-san-duan-gong-xiang-lei-xing-yu-tong-bu-ji-zhi)。\n\nSources: [scripts/check-stream-event-sync.sh](scripts/check-stream-event-sync.sh#L6-L28), [.github/workflows/ci.yml](.github/workflows/ci.yml#L44-L51), [Makefile](Makefile#L150-L155)\n\n## E2E 场景的三种形态\n\n项目的"E2E"并非单一框架，而是按被验证边界的不同分化为三种形态：\n\n| 形态 | 位置 | 运行方式 | 验证边界 |\n| --- | --- | --- | --- |\n| 进程级消息链路 | `tests/e2e/*.mjs` | 手动对运行中服务执行 | REST 登录 → WebSocket 注入 → Interrupt → 回复完整性 |\n| 移动端浏览器交互 | `web/tests/e2e/` | CI 自动执行 Playwright | 真实 Chromium 渲染层（Pixel 7 设备模拟） |\n| Agent 容器镜像 | `scripts/smoke-agent-image.sh` | 发布工作流内执行 | Docker 镜像可启动、架构匹配、无挂载探测 |\n\n进程级 E2E 的代表是针对 Issue #421 的中断丢消息场景：脚本以普通用户凭据登录 REST API，携带 Cookie 建立 WebSocket 连接，发送长任务消息 A，在其处理期间注入消息 B 并调用 interrupt 接口，最终断言消息 B 仍能收到回复。它不依赖任何测试框架，就是一个带退出码的原生 Node 脚本，通过 `MINICLAW_URL`/`TIMEOUT_MS` 等环境变量适配目标环境。\n\nSources: [tests/e2e/interrupt-ipc-message-loss.mjs](tests/e2e/interrupt-ipc-message-loss.mjs#L1-L13), [tests/e2e/interrupt-ipc-message-loss.mjs](tests/e2e/interrupt-ipc-message-loss.mjs#L96-L142)\n\n前端 Playwright 配置则体现了"harness 页面 + 路由拦截"的轻量策略：`webServer` 启动 Vite 开发服务器并直接指向一个专用的 `mobile-chat-harness.html` 静态页面，spec 内通过 `page.route()` 拦截 `/api/**` 请求返回受控响应（甚至可以延迟 PDF 响应以测试加载态），从而在不拉起后端的情况下测试真实浏览器中的交互行为。CI 中该项目固定为 `chromium-mobile`（Pixel 7 设备描述符），失败重试一次并保留 trace/截图/视频证据。\n\nSources: [web/playwright.config.ts](web/playwright.config.ts#L4-L38), [web/tests/e2e/mobile-chat.spec.ts](web/tests/e2e/mobile-chat.spec.ts#L26-L52)\n\n## Vitest 之外：自检与冒烟脚本的设计取舍\n\n有两类验证刻意放在 Vitest 体系之外。其一是 `npm run self-test:agent-runner`：它导入 Runner 的真实产物代码，用**事故回放**方式驱动 `shouldForceBackgroundTaskSummary` 决策函数——构造"后台任务等待回复看似完结实则未完成"的历史场景序列，断言强制续写逻辑会被触发。这类需要真实构建产物的验证若放进 Vitest 反而要解决打包时序问题，独立脚本更简单可靠。\n\nSources: [scripts/self-test-agent-runner.ts](scripts/self-test-agent-runner.ts#L1-L10), [scripts/self-test-agent-runner.ts](scripts/self-test-agent-runner.ts#L55-L74)\n\n其二是真实模型连通性冒烟 `npm run test:real-model`：向已配置的 Provider 发送固定标记问答，逐字比对响应。它的安全设计很讲究——输出 JSON 只含布尔值与耗时字段，注释明确声明"绝不打印 Provider 值、端点、模型或凭据"，异常分支也只输出一行不含上游片段的错误信息。这使该脚本可以在有真实密钥的环境中安全复现。\n\nSources: [scripts/test-real-model-smoke.ts](scripts/test-real-model-smoke.ts#L6-L14), [scripts/test-real-model-smoke.ts](scripts/test-real-model-smoke.ts#L25-L47)\n\n这些分层策略在 [docs/PROMPT-SKILL-RUNTIME-TEST-PLAN.md](docs/PROMPT-SKILL-RUNTIME-TEST-PLAN.md#L3-L17) 中被正式归纳为五层表格：纯函数 → 宿主集成 → SDK/CLI 集成 → 前端契约/构建 → 真实 Provider 冒烟，每层都附有对应的可复现命令清单。\n\n## CI 流水线：从格式到自检的完整闸门序列\n\n`.github/workflows/ci.yml` 在 ubuntu-24.04 / Node 24 上以 20 分钟超时上限编排了七道顺序闸门。顺序本身传递着设计意图：\n\n```mermaid\nflowchart LR\n    A[format:check<br/>变更文件格式] --> B[sync-types 验证<br/>git diff --exit-code]\n    B --> C[make typecheck<br/>三端 tsc + 同步脚本]\n    C --> D[vitest run<br/>全量单测/契约/DOM]\n    D --> E[build:all<br/>backend+web+runner]\n    E --> F[Playwright<br/>移动端 E2E]\n    F --> G[self-test:agent-runner<br/>构建产物回放]\n\n    style B fill:#e0f2fe\n    style D fill:#dcfce7\n    style G fill:#fef9c3\n```\n\n几个关键点：安装阶段会为三个独立的 lockfile 分别 `npm ci` 并预装 Playwright Chromium；"Verify generated type sources" 步骤（上图标蓝）位于类型检查之前，保证同步防线先于一切编译验证；Vitest 全量套件（标绿）在构建之后运行但并不依赖构建产物——它直接消费 TS 源码；而 Runner 自检（标黄）被刻意排在最后，因为它验证的是刚构建完成的 dist 产物。发布侧的镜像冒烟则由独立的 docker-publish 工作流承接，且其结构本身也被契约测试锁定。\n\nSources: [.github/workflows/ci.yml](.github/workflows/ci.yml#L1-L63), [tests/docker-publish-workflow-contract.test.ts](tests/docker-publish-workflow-contract.test.ts#L9-L35)\n\n## 小结\n\n这套测试体系的组织逻辑可以压缩为三条原则：**单一运行器、命名分类法、契约前置**。单实例 Vitest 加别名解析消除了多工程测试栈的维护成本；命名后缀让 328 个文件、约 2500 个用例的分类一目了然；而把架构决策（Electron 安全边界、Makefile 启动模型、发布流程、产品术语）编码为可执行契约，使得最容易被口头约定侵蚀的部分获得了与代码同等的回归保护。理解数据库迁移测试的组织方式可继续阅读 [SQLite 持久化：Schema 版本化、迁移策略与核心表族](9-sqlite-chi-jiu-hua-schema-ban-ben-hua-qian-yi-ce-lue-yu-he-xin-biao-zu)，日常开发命令速查见 [开发工作流与常用命令速查](3-kai-fa-gong-zuo-liu-yu-chang-yong-ming-ling-su-cha)，Electron Shell 安全契约的背景见 [Electron 受限 Shell：contextIsolation 与 Preload IPC 桥接](26-electron-shou-xian-shell-contextisolation-yu-preload-ipc-qiao-jie)。';export{t as default};

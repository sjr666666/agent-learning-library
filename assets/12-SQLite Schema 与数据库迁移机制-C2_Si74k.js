const e='HappyClaw 的持久化层是一个**单文件 SQLite 数据库**（`data/db/messages.db`），由 14,497 行的 `src/db.ts` 统一承载。与常见 ORM 迁移框架（按目录收集 `ALTER TABLE` 脚本）不同，本项目采用"**声明式建表 + 版本门控迁移 + 启动期对账**"的组合策略：所有迁移逻辑内联在 `initDatabase()` 启动管线中，通过 `router_state` 表中的 `schema_version` 键追踪演进，当前头部版本为 **69**。本文从驱动兼容层、表族归属、迁移模式、安全门禁与测试资产五个维度拆解这套机制。\n\n## 单文件存储与驱动兼容层\n\n数据库文件位于 `STORE_DIR/messages.db`，其中 `STORE_DIR = DATA_DIR/db`（`DATA_DIR` 默认 `data/`）。所有核心业务表共用一个连接，由 `src/db.ts` 中的模块级 `db` 单例持有，并通过 `isDatabaseInitialized()` 暴露初始化状态。\n\n**驱动抽象**是理解整个持久化层的前提：`src/sqlite-compat.ts` 在 Bun 与 Node.js 之间做二选一——Bun 运行时动态 `import(\'bun:sqlite\')`，Node.js 则加载 `better-sqlite3`。两者 API 几乎一致（`prepare/run/get/all/exec/transaction`），唯一差异是 Bun 缺少 `pragma()` 方法，因此所有 PRAGMA 操作统一走 `db.exec()`。这意味着 `src/db.ts` 以及所有模块化存储（`channel-reliability-store`、`memory-store`、`owner-profile-store`）中的 SQL 都能在两种运行时下原样执行。\n\nSources: [config.ts](src/config.ts#L23-L25)、[sqlite-compat.ts](src/sqlite-compat.ts#L1-L23)、[db.ts](src/db.ts#L100-L110)\n\n**运行时参数**在 `initDatabase()` 打开连接后立即配置：`busy_timeout = 5000` 缓解多进程争用；随后切换 WAL 日志模式、`synchronous = NORMAL` 与 `temp_store = MEMORY`。代码注释记录了一个关键实测数据：WAL + NORMAL 组合使同步写路径提速约 21 倍——提交不再每次 fsync WAL，由 checkpoint 负责落盘，最坏情况下仅丢失最近瞬间的已提交工作，不会损坏文件。注意这些 PRAGMA 的启用**严格排在预迁移备份门禁之后**（见下文），避免备份阶段产生任何写放大。\n\nSources: [db.ts](src/db.ts#L468-L492)\n\n## Schema 组织：核心表族与模块化表族\n\n数据库结构并非单块巨石，而是遵循"**核心表由 db.ts 声明，领域表由所属模块声明**"的归属原则。`src/db.ts` 内联 41 条 `CREATE TABLE` 语句，按启动顺序分为四组：\n\n- **基础会话表**：`chats`、`messages`（复合主键 `(id, chat_jid)` + 指向 chats 的外键）、`scheduled_tasks`、`task_run_logs`、`task_runs`（含租约与通知双状态机字段）\n- **状态投影表**：`router_state`（key-value，承载 schema_version）、`sessions`、`registered_groups`、`channel_accounts`、`im_context_bindings`、`channel_mounts`、`workspaces`、`workspace_runtime_sessions`、`agent_channel_mounts`\n- **认证与计费表**：`users`、`invite_codes`、`user_sessions`、`auth_audit_log`、`user_pinned_groups`、`agents`、`agent_profiles`、`agent_profile_prompt_versions`、`redeem_code_usage`、`billing_audit_log`、`daily_usage`、`user_quotas`\n- **用量明细表**：`usage_records`、`usage_events`、`usage_daily_summary`\n\n模块化表族由各存储模块导出 `createXxxSchema(db)` 工厂，在 `initDatabase()` 的特定时间点被调用——`channel-reliability-store.ts` 拥有 5 张可靠性状态表（`channel_inbox`、`channel_cursors`、`turn_runs`、`channel_outbox`、`streaming_cards`），`memory-store.ts` 拥有 9 张记忆表 + 1 个 FTS5 虚拟表 + 1 个触发器，`owner-profile-store.ts` 拥有 1 张引导状态表。这些模块通过 `bindXxxDatabase(db)` 获得共享连接引用，持久化与围栏逻辑完全内聚在模块内部。\n\n```mermaid\nflowchart TB\n    subgraph File["data/db/messages.db（单文件）"]\n        direction TB\n        Core["核心表族 — src/db.ts<br/>41 张 CREATE TABLE<br/>基础会话 / 状态投影 / 认证计费 / 用量明细"]\n        Rel["渠道可靠性表族 — channel-reliability-store.ts<br/>channel_inbox · turn_runs · channel_outbox · streaming_cards · channel_cursors"]\n        Mem["记忆表族 — memory-store.ts<br/>workspace_memory_* ×9 · FTS5 · 触发器"]\n        Owner["引导表族 — owner-profile-store.ts<br/>workspace_onboarding_states"]\n    end\n\n    Init["initDatabase() 启动管线"]\n    Init -->|"CREATE IF NOT EXISTS + ensureColumn"| Core\n    Init -->|"createChannelReliabilitySchema + bind"| Rel\n    Init -->|"createWorkspaceMemorySchema + bind"| Mem\n    Init -->|"createOwnerProfileSchema + bind"| Owner\n```\n\n模块化表族的引入本身就是版本化演进的结果：v59→v60 引入渠道可靠性表族、v64→v65 引入 Workspace Memory v2、v65→v66 引入 Owner Profile 表族。每张模块表通过 `FOREIGN KEY` 与核心表（如 `workspaces(jid)`）建立引用，在单一连接内共享事务边界——这正是单文件架构的优势：跨表族的原子性不需要分布式协调。\n\nSources: [db.ts](src/db.ts#L540-L657)、[db.ts](src/db.ts#L659-L785)、[db.ts](src/db.ts#L794-L860)、[channel-reliability-store.ts](src/channel-reliability-store.ts#L287-L437)、[memory-store.ts](src/memory-store.ts#L494-L660)\n\n## 迁移执行流程：initDatabase 启动管线\n\n版本号存放在 `router_state.schema_version` 而非 SQLite 的 `PRAGMA user_version`——原因在于 `router_state` 是现成的 key-value 状态表，且迁移过程需要读取/回写多个运行时状态键。`CURRENT_SCHEMA_VERSION = 69` 作为导出常量，注释明确说明导出动机：迁移测试需要断言"旧库到达头部"而不重复硬编码数字，避免每次版本号提升引发十几处测试文件的机械改动。\n\n`initDatabase()` 是全部迁移逻辑的唯一入口，其执行顺序本身就是一种架构声明：\n\n```mermaid\nflowchart TB\n    A["打开 messages.db<br/>PRAGMA busy_timeout = 5000"] --> B["读取 rawSchemaVersionBeforeInit"]\n    B --> C{"enforcePreMigrationBackup"}\n    C -->|"版本 > 69：拒绝降级"| X["抛错中止启动"]\n    C -->|"版本 39–68：VACUUM INTO 预迁移备份"| D\n    C -->|"备份失败：关闭连接并中止"| X\n    D["WAL + synchronous=NORMAL<br/>temp_store=MEMORY"]\n    D --> E["reportKnownForeignKeyOrphans 预检"]\n    E --> F["PRAGMA foreign_keys=ON<br/>messages→chats 孤儿自动级联清除"]\n    F --> G["CREATE TABLE IF NOT EXISTS 核心表族"]\n    G --> H["createXxxSchema 模块化表族<br/>memory / owner-profile / channel-reliability"]\n    H --> I["ensureColumn 轻量列迁移（约 70 处）"]\n    I --> J["版本门控数据迁移 v13→v69"]\n    J --> K["assertSchema 结构不变量校验"]\n    K --> L["created_by / is_home 等回填 + 投影对账"]\n    L --> M["INSERT OR REPLACE schema_version = 69"]\n```\n\n管线中**版本读取时机**存在两个来源：`rawSchemaVersionBeforeInit` 在备份门禁前捕获（用于 v66 与 v69 的 JSON 语义迁移），而各处版本门控迁移独立调用 `getRouterStateInternal(\'schema_version\')` 重新读取（此时仍是旧值，因为头部版本仅在管线末尾写入）。这套"先备份 → 再建表 → 再迁移 → 最后落版本号"的顺序保证了：任何一步失败，`schema_version` 停留在旧值，下次启动会重新执行整个管线，而预迁移备份的存在让重复执行在语义上安全。\n\nSources: [db.ts](src/db.ts#L101-L106)、[db.ts](src/db.ts#L295-L305)、[db.ts](src/db.ts#L2484-L2486)\n\n## 迁移模式的四类实现\n\n所有迁移可归纳为四种模式，选择标准取决于变更的形态：\n\n| 模式 | 触发条件 | 幂等性 | 典型示例 |\n|---|---|---|---|\n| `ensureColumn` 加列 | `PRAGMA table_info` 检测缺失 | 天然幂等（存在即跳过） | `messages.token_usage`、`scheduled_tasks.delivery_route_jid`、`users.permissions` |\n| 版本门控数据迁移 | `schema_version < N` 读旧值 | 事务包裹 + `INSERT OR IGNORE` / `UPDATE` 语义 | v24 计费初始化、v28 用量历史回填、v51 事件账本、v39 用户名小写化 |\n| 表重建 | 主键 / 唯一约束 / CHECK 无法用 ALTER 表达 | 门控在版本比较上，重建过程本身一次完成 | v16→v17 sessions 复合主键、v61→v62 proactive 模式 CHECK 约束、`registered_groups.folder` 去 UNIQUE |\n| 废弃表删除 | 版本门控 + `tableExists` 探测 | `DROP TABLE IF EXISTS` | v45→v46 删除 `group_members`、v66→v67 删除 `memory_chunks` + FTS 影子表 |\n\n**`ensureColumn` 是最轻量的一类**：`hasColumn()` 通过 `PRAGMA table_info` 检查目标列，缺失时执行 `ALTER TABLE ... ADD COLUMN`。它在 `initDatabase()` 中集中出现约 70 处，是新增业务字段的默认路径。关键约束是 SQLite 的 `ADD COLUMN` 只能追加**可空或带默认值**的列——这正是所有新列都携带 `NOT NULL DEFAULT ...` 或允许 NULL 的原因。注释中的顺序要求值得注意：`ensureColumn` 必须发生在 `assertSchema` 之前，否则旧库会在结构校验处被拒绝。\n\n**版本门控数据迁移**处理存量数据的语义转换，统一模式是"读旧版本号 → `db.transaction()` 包裹 → 逐行或批量 UPDATE/INSERT"。v39 用户名小写化展示了这类迁移的谨慎程度：只对 `username != lower(username)` 的行操作，按 `created_at ASC` 排序让早创建账号优先，遇到冲突（既有 `admin` 又有 `Admin`）时**拒绝变更该行**并记录日志，留给操作员手工清理——迁移代码从不替用户做二义性决策。v51 则展示了两种幂等策略的组合：门控迁移为历史用量行生成 `legacy:{id}` 事件号，另有一段**无条件启动修复**（补写缺失的 `usage_date`），专门兜底"曾短暂运行过早版 v51 构建"的安装。\n\nSources: [db.ts](src/db.ts#L256-L272)、[db.ts](src/db.ts#L2184-L2237)、[db.ts](src/db.ts#L2305-L2373)\n\n**表重建**用于 ALTER 无法表达的约束变更。v61→v62 的 proactive 模式迁移是最完整的样例：新建 `workspace_agent_profiles_v62` 表并附加 `CHECK (interaction_mode IN (\'assistant\',\'proactive\'))`，将旧 `persona`/`proactive` 值归一化为 `proactive`，再 `DROP` 旧表、`RENAME` 新表、重建索引，整个过程包裹在事务中。`registered_groups` 的 UNIQUE(folder) 移除同样采用此模式，修复了一个真实 bug：旧约束导致 `INSERT OR REPLACE` 静默删除同 folder 的冲突行，使 `web:main` 与飞书群互斥。\n\n**废弃表删除**是 v67 迁移的亮点：旧版 `memory_chunks` 及其 FTS5 影子表占生产库体积的 65% 且数月零写入，被 Workspace Memory v2 全面取代后，迁移在版本门控内 `DROP TABLE IF EXISTS` 两表。`enforcePreMigrationBackup` 对无版本标记（`schema_version` 缺失）的库特判：只要探测到 `memory_chunks` 存在就按 v0 执行备份——因为"未版本化 + 遗留索引"本身就是一次真实升级，不能静默放行。\n\nSources: [db.ts](src/db.ts#L2255-L2298)、[db.ts](src/db.ts#L1450-L1487)、[db.ts](src/db.ts#L2464-L2482)、[db.ts](src/db.ts#L440-L452)\n\n## 安全门禁：预迁移备份、降级拒绝与结构校验\n\n迁移机制最鲜明的特征是**把"不可逆变更"变成"有保险的变更"**。`createPreMigrationBackup()` 在任何 schema 或数据写入之前执行：通过 `VACUUM INTO` 生成自包含快照（WAL 已提交页完整包含），文件名编码迁移区间 `messages-v{from}-to-v{to}-{timestamp}-{pid}.db`；随后用独立连接执行 `PRAGMA quick_check` 验证备份完整性，并 `chmod 0600` 收紧权限。**备份失败直接抛错中止启动**——迁移永远不会在无保险的情况下执行。备份目录可用 `HAPPYCLAW_MIGRATION_BACKUP_DIR` 覆盖，默认落在数据库同级的 `migration-backups/`。\n\n```mermaid\nflowchart LR\n    A["版本门禁判定"] -->|"未版本化 + 遗留 memory_chunks"| B["按 v0 备份"]\n    A -->|"39 ≤ 版本 < 69"| C["VACUUM INTO 快照"]\n    A -->|"版本 > 69"| D["拒绝降级，抛错"]\n    C --> E["quick_check 验证 + chmod 0600"]\n    E -->|"失败"| F["清理残留 + 抛错中止"]\n    E -->|"成功"| G["继续迁移管线"]\n```\n\n**降级拒绝**与**结构校验**构成第二、三道防线：`schema_version > 69` 时拒绝启动（防止用旧二进制打开新库）；`assertSchema()` 在迁移管线末尾检查每个关键表的必需列集合与禁用列集合，不匹配时抛出带明确指引的错误——"删除 messages.db 并重启"。`db-upgrade-safety.test.ts` 完整验证了这套门禁：预置 v39 旧库（含将被删除的 `group_members` 与需保留的审计孤儿行）→ 启动迁移 → 断言备份文件存在且内容为旧版本 → 断言主库删除 `group_members` 但保留审计行 → 人为制造备份目录不可写 → 断言启动被拒且主库**分毫未动** → 修复后重试 → 断言不产生重复备份。\n\n**外键强制**是另一道独特的安全机制。SQLite 默认 `foreign_keys = OFF`，因此所有 FK 声明在未显式开启时都是静默空操作。`initDatabase()` 在迁移后执行 `PRAGMA foreign_keys = ON` 并运行 `foreign_key_check`：`messages → chats` 孤儿（中断的聊天删除级联残留，生产环境实测 6 条）会被自动清除以保持强制开启；其余违规类（如故意保留的计费审计行）则记录警告并回退到 `OFF`，避免开启后拒绝下一次写入。启动前另有 `reportKnownForeignKeyOrphans()` 预检四张用户关联表，仅告警不处理——审计证据保留给运维决策。`foreign-key-orphan-repair.test.ts` 用"删 chats 留 messages"模拟历史残缺级联，验证重启后孤儿清除且 `foreign_key_check` 归零。\n\nSources: [db.ts](src/db.ts#L379-L438)、[db.ts](src/db.ts#L274-L293)、[db.ts](src/db.ts#L493-L539)、[db.ts](src/db.ts#L314-L373)、[db-upgrade-safety.test.ts](tests/db-upgrade-safety.test.ts#L41-L163)、[foreign-key-orphan-repair.test.ts](tests/foreign-key-orphan-repair.test.ts#L27-L60)\n\n## 数据迁移的语义层级：从 JSON 归一化到身份版本\n\n除结构变更外，迁移还需处理**持久化 JSON 的语义演进**。`agent_profiles.runtime_policy` 是典型载体——它以 TEXT 存储序列化 JSON，迁移代码需要解析、变换、再序列化。v68→v69 的 effort 策略迁移刻意**不提升 Agent 身份版本**：`normalizeAgentEffort` 只注入 `reasoning.effort` 字段并保留全部既有字段，继承默认值不改变任何 Provider/SDK 行为，因此无需让会话失效。相反，`removeLegacyAgentToolPolicies()` 移除已退役的工具边界时，若 `identity_hash` 发生变化则**递增 `version` 并更新 `updated_at`**——因为旧限制下创建的会话不能在无限制的新身份下续跑。这一对比揭示了迁移语义的一条铁律：**改变运行时行为的迁移必须使旧会话失效，纯默认值归一化的迁移则必须保持身份稳定**。\n\nSources: [db.ts](src/db.ts#L1996-L2005)、[db.ts](src/db.ts#L8115-L8159)、[db.ts](src/db.ts#L8166-L8215)\n\n## 迁移测试体系：每个版本一桌"旧库 → 头部"\n\n迁移质量由一套**按版本命名的契约测试**保障，模式高度统一：在临时目录用 `better-sqlite3` 手工构造旧版本 schema + 种子数据 → mock 配置与日志 → `initDatabase()` 执行真实迁移 → 断言版本号到达头部、关键数据保真 → **二次启动断言幂等**（不重建已废弃表、不重复迁移）。`schema-v69-agent-effort.test.ts` 即按此模式构造 v68 库：预置旧版 `runtime_policy`（含 skills/mcp 字段）与 warm session，验证迁移后 `effort: \'inherit\'` 注入、`identity_hash` 与 `version` 保持原值、会话身份投影可解析。`schema-v46-migration.test.ts` 更进一步：迁移后人为注入"投影幽灵"工作区，二次启动必须清理幽灵而不触碰已保留的运行时状态。\n\n```mermaid\nflowchart TB\n    subgraph Test["schema-vXX-migration.test.ts 契约测试"]\n        A["mkdtemp 临时目录"] --> B["手工构造 vN 旧 schema<br/>+ 种子数据 + schema_version=N"]\n        B --> C["mock config/logger"]\n        C --> D["initDatabase() 真实迁移"]\n        D --> E["断言到达 CURRENT_SCHEMA_VERSION"]\n        E --> F["断言数据保真（身份/会话/审计）"]\n        F --> G["二次启动断言幂等"]\n    end\n```\n\n`db-upgrade-safety.test.ts` 中有一段"**头部版本钉死**"测试值得单独强调：它用字面量 `expect(db.CURRENT_SCHEMA_VERSION).toBe(69)` 锚定当前版本。设计意图是防止版本提升沦为"改个常量"——因为其余迁移测试断言的是 `CURRENT_SCHEMA_VERSION` 本身，各自构成同义反复；唯一会在版本移动时失败的正是这个字面量，它强制提交者在同一 commit 中落地匹配的迁移代码与迁移测试。此外 `db-transactions.test.ts` 验证 `incrementUsageBoth` 的月度/日度原子更新，`memory-chunks-drop-migration.test.ts` 验证未版本化遗留索引的备份前置行为——测试资产与迁移门禁一一对应。\n\nSources: [schema-v69-agent-effort.test.ts](tests/schema-v69-agent-effort.test.ts#L62-L160)、[schema-v46-migration.test.ts](tests/schema-v46-migration.test.ts#L26-L120)、[db-upgrade-safety.test.ts](tests/db-upgrade-safety.test.ts#L166-L178)、[db-transactions.test.ts](tests/db-transactions.test.ts#L54-L100)、[memory-chunks-drop-migration.test.ts](tests/memory-chunks-drop-migration.test.ts#L48-L60)\n\n## 版本演进速览：v13 至 v69\n\n版本号演进本身就是一部产品架构史。按时间线摘录关键里程碑：\n\n| 版本区间 | 变更内容 | 迁移模式 |\n|---|---|---|\n| v13 | `web:main` 标记 `is_home` | 无条件 UPDATE |\n| v16→v17 | `sessions` 改复合主键支持每 Agent 会话 | 表重建 |\n| v19→v20 | `messages.token_usage` 引入用量追踪 | ensureColumn |\n| v23→v28 | 计费体系（套餐/余额/订阅）与用量历史回填 | 门控事务 + JSON 展开回填 |\n| v37→v39 | 每用户 `require_mention`；用户名小写化 + NOCASE 唯一 | ensureColumn + 冲突感知 UPDATE |\n| v44→v46 | `workspace_sessions` 改名投影表；删除共享成员表 | 表重建 + DROP |\n| v47→v48 | Agent 提示词拆分为 IDENTITY/SOUL/AGENTS/TOOLS 四段 | 门控事务 + 身份哈希重算 |\n| v48→v50 | 渠道账号一等公民；认证态与传输态分离 | ensureColumn + 状态回填 |\n| v50→v51 | 用量事件账本（`usage_events` + `event_id` 幂等键） | 门控事务 + 无条件修复 |\n| v56→v58 | 头像缓存；飞书 audience 策略拆分 | ensureColumn + 一次性语义迁移 |\n| v59→v60 | 渠道可靠性表族（Inbox/Turn/Outbox） | 模块化 schema 引入 |\n| v60→v62 | interaction_mode → proactive 模式语义归一 | ensureColumn + CHECK 表重建 |\n| v62→v63 | 任务投递路由 `delivery_route_jid` 回填 | ensureColumn + 条件 UPDATE |\n| v63→v64 | 清理飞书内嵌引用链污染 | 逐行门控事务（保留唯一副本） |\n| v64→v66 | Workspace Memory v2 与 Owner Profile 表族 | 模块化 schema 引入 |\n| v66→v67 | 删除占库 65% 的遗留 memory_chunks | 门控 DROP |\n| v67→v69 | Agent 模型配置绑定；reasoning effort 归一化 | ensureColumn + JSON 归一化 |\n\nv63→v64 是语义迁移的教科书案例：早期飞书回复增强把引用祖先链嵌入 `messages.content`，污染 Web 历史并在会话恢复时二次复制。迁移逐行检查——仅当消息的直接父消息在同一逻辑会话中**安全存在**时才剥离引用内容，引用上下文是唯一副本的行保持原样。这类"数据有歧义时宁可不迁"的原则贯穿整个演进史。\n\nSources: [db.ts](src/db.ts#L1636-L1671)、[db.ts](src/db.ts#L1705-L1754)、[db.ts](src/db.ts#L1835-L1924)、[db.ts](src/db.ts#L2099-L2150)、[db.ts](src/db.ts#L2381-L2462)、[db.ts](src/db.ts#L1283-L1294)\n\n## 运维联动与延伸阅读\n\n迁移机制与备份恢复体系共享同一套一致性理念：`scripts/sqlite-snapshot.mjs` 利用 better-sqlite3 的在线备份 API 生成事务一致快照（WAL 已提交页包含在内，且无需停进程），并在目标文件上执行 `quick_check` 验证与 `chmod 0600`——与预迁移备份的验证步骤完全同构。运维层面需记住三个要点：**迁移备份不可删除**（`migration-backups/` 是唯一回滚依据）、**版本只进不退**（降级被硬拒绝）、**备份失败等于拒绝启动**（宁可停机不可无保险迁移）。\n\n本文属于"系统架构"深度专题。建议按目录顺序继续阅读：[渠道可靠性状态机：Inbox、Turn 与 Outbox](16-qu-dao-ke-kao-xing-zhuang-tai-ji-inbox-turn-yu-outbox)（深入本页引入的可靠性表族）、[Workspace Memory v2：结构化跨会话知识](22-workspace-memory-v2-jie-gou-hua-kua-hui-hua-zhi-shi)（记忆表族的行为语义）、[数据目录结构与一致性备份恢复](23-shu-ju-mu-lu-jie-gou-yu-zhi-xing-bei-fen-hui-fu)（快照与恢复的操作面），以及[测试体系：单元测试、契约测试与真实模型冒烟](29-ce-shi-ti-xi-dan-yuan-ce-shi-qi-yue-ce-shi-yu-zhen-shi-mo-xing-mou-yan)（迁移契约测试在整个测试金字塔中的位置）。';export{e as default};

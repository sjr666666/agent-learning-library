const s='MiniCode 的会话持久化建立在 **追加式 JSONL 事件日志**之上：每一行是一个不可变的 JSON 事件，只允许在文件尾部追加，从不改写历史。这个设计让"保存"退化为一次 `appendFile`，让"恢复"变成一次确定性的回放——崩溃不会损坏已有日志，压缩与裁剪也无需重写文件。本页深入 `src/session.ts` 及其在 TTY 应用中的集成点，讲解事件信封的结构、幂等追加的去重机制、`parentUuid` 链、以压缩边界为断点的上下文重建，以及恢复会话时的完整装配流程。\n\n读者应已了解斜杠命令的日常用法（[会话管理入门：保存、恢复、重命名与分叉](6-hui-hua-guan-li-ru-men-bao-cun-hui-fu-zhong-ming-ming-yu-fen-cha)）；分叉、压缩边界写入与过期清理的细节由下一页 [会话分叉、压缩边界与过期清理](20-hui-hua-fen-cha-ya-suo-bian-jie-yu-guo-qi-qing-li) 承担。\n\n## 事件日志而非状态快照：持久化的第一性原理\n\nMiniCode 的运行时状态模型刻意保持简单：**对话消息在回合期间驻留内存，回合成功结束后追加到会话日志**。这意味磁盘上不存在"当前会话状态"的单一快照文件，而是保留了从会话诞生至今的完整事件流：\n\n```mermaid\nflowchart LR\n    subgraph 内存运行时\n        A[消息数组<br/>当前回合]\n    end\n    subgraph 磁盘日志\n        B["<sessionId>.jsonl<br/>逐行追加事件"]\n    end\n    A -- "回合结束 saveSession" --> B\n    B -- "loadSession 断点回放" --> A\n    B -- "loadTranscript 全量重建" --> C[可见转录]\n```\n\n追加式日志的收益是结构性的：一是**崩溃安全**——追加写入天然原子，已落盘的行永不被修改，中途断电至多丢失最后一批未落盘消息；二是**历史可重放**——即使上下文经过压缩、裁剪、折叠，原始事件流仍保留在文件中，转录界面（transcript）可以从头重建，而模型上下文则只加载最近的有效段。这正是"断点恢复"的根基：日志是唯一的真相来源，内存只是它的投影。\n\nSources: [ARCHITECTURE.md](ARCHITECTURE.md#L65-L75)、[session.ts](src/session.ts#L218-L250)\n\n## 事件信封：统一的日志行结构\n\nJSONL 的每一行都是一个 **事件信封（event envelope）**。`SessionEvent` 定义了所有事件共享的骨架字段，外加按事件类型出现的可选元数据载荷：\n\n| 字段 | 类型 | 说明 |\n|---|---|---|\n| `type` | `EventType` | 事件类型，见下方枚举 |\n| `message` | `ChatMessage` | 原始消息体（仅消息类事件携带） |\n| `uuid` | `string` | 事件唯一 ID，也是消息的持久化 ID |\n| `timestamp` | `string` | ISO 8601 时间戳 |\n| `sessionId` | `string` | 所属会话（8 位十六进制） |\n| `cwd` | `string` | 事件产生时的工作目录 |\n| `parentUuid` | `string \\| null` | 物理前驱事件 ID（链式结构） |\n| `logicalParentUuid` | `string \\| null` | 逻辑前驱事件 ID（元事件专用） |\n| `subtype` / `title` / `compactMetadata` / `snipMetadata` / `contextCollapseSpan` | 可选 | 类型化载荷 |\n\n事件类型由 `roleToType` 从消息角色单向映射而来，再加四类不携带 `message` 的元事件：\n\n| 事件类型 | 来源 | 用途 |\n|---|---|---|\n| `system` / `user` / `assistant` | 对应消息角色 | 常规对话内容 |\n| `thinking` | `assistant_thinking` | 思考块 |\n| `progress` | `assistant_progress` | 进度流 |\n| `tool_call` / `tool_result` | 对应消息角色 | 工具调用与结果 |\n| `summary` | `context_summary` | 压缩摘要消息 |\n| `snip_boundary` | `snip_boundary` 角色 | 裁剪边界（携带 `snipMetadata`） |\n| `compact_boundary` | 追加写入 | 压缩边界（携带 `compactMetadata`） |\n| `context_collapse` | 追加写入 | 折叠跨度（携带 `contextCollapseSpan`） |\n| `rename` | 追加写入 | 重命名（携带 `title`） |\n\n写入时 `wrapEvent` 把 `ChatMessage` 原样嵌入信封的 `message` 字段，再补齐审计字段；若消息还没有 `id`，`ensureMessageId` 现场生成 UUID 并回写，保证"消息 ID == 事件 UUID"这一恒等式。读取时 `unwrapMessage` 反向操作，把 `event.uuid` 覆盖回消息的 `id`——这正是幂等恢复的钥匙。\n\nSources: [session.ts](src/session.ts#L22-L47)、[session.ts](src/session.ts#L61-L104)、[session.ts](src/session.ts#L106-L122)\n\n## 存储布局：按项目隔离的会话文件\n\n会话文件位于用户级配置目录下，按工作目录隔离。目录名由工作目录路径编码而来：`/`、`\\`、`:` 全部替换为 `-`，再剥离前导 `-`：\n\n```\n~/.mini-code/                       ← MINI_CODE_DIR（可用 MINI_CODE_HOME 覆盖）\n└── projects/                       ← MINI_CODE_PROJECTS_DIR\n    └── Users-me-demo/              ← 编码后的工作目录\n        └── a1b2c3d4.jsonl          ← 会话 ID + .jsonl\n```\n\n三个关键常量都定义在 `config.ts`：`MINI_CODE_DIR` 默认 `~/.mini-code`，`MINI_CODE_PROJECTS_DIR` 为其下 `projects`，另有独立的 `MINI_CODE_HISTORY_PATH`（`history.jsonl`）记录跨会话的命令历史，与会话日志是两套数据。会话 ID 在每次启动时生成：`crypto.randomUUID().slice(0, 8)`，即 8 位随机十六进制。\n\n`listSessions` 负责目录扫描：读取每个 `.jsonl` 的行数与 `mtime`，并用 `extractTitleFromEvents` 提取标题——优先取最后一个 `rename` 事件，否则回退到第一条非空 `user` 消息，超过 60 字符截断加 `...`。结果按 `mtime` 倒序，会话选择器正是消费这个列表。\n\nSources: [config.ts](src/config.ts#L36-L44)、[session.ts](src/session.ts#L49-L59)、[session.ts](src/session.ts#L169-L188)、[session.ts](src/session.ts#L468-L502)、[index.ts](src/index.ts#L109)\n\n## 幂等追加：UUID 去重与已保存计数\n\n`saveSession` 是写入路径的核心。它先读取文件内全部既有事件的 UUID 集合，再对消息数组做三重过滤，只追加真正的新消息：\n\n```mermaid\nflowchart TD\n    A[saveSession 收到完整消息数组] --> B[读取文件已有 UUID 集合]\n    B --> C[跳过 system 消息 slice 1]\n    C --> D{消息有 id?}\n    D -- 是, 且已在文件中 --> E[跳过<br/>避免重复行]\n    D -- 是, 且不在文件中 --> F[保留<br/>UUID 是权威判据]\n    D -- 否 --> G{索引 >= alreadySavedCount?}\n    G -- 是 --> F\n    G -- 否 --> E\n    F --> H[读取文件最后一个事件的 uuid]\n    H --> I[wrapEvent 生成新 uuid<br/>parentUuid 链式衔接]\n    I --> J[appendFile 一次追加全部新行]\n```\n\n过滤逻辑同时依赖两种判据：**UUID 去重**优先——凡消息自带 `id`，以文件中是否已存在该 ID 为准；`alreadySavedCount` 作为兜底——仅当消息没有 `id` 时，才用"已落盘消息数"做索引比较。`messages.slice(1)` 跳过 `system` 提示（它由权限、技能、MCP 状态动态生成，不属于会话内容）。若没有新消息，函数直接返回，连一次文件写入都不会发生。\n\n双判据的价值在断点恢复场景充分显现：恢复后加载的历史消息都携带 ID（来自事件 UUID），下一次保存时 ID 去重自动跳过它们；而新产生的消息在 `wrapEvent` 时获得新 UUID，`parentUuid` 则从文件最后一个事件继续衔接。测试用连续两次保存验证了"文件行数恰好等于消息数、无重复行"这一不变量。\n\nSources: [session.ts](src/session.ts#L202-L250)、[session.test.ts](test/session.test.ts#L211-L233)、[session.test.ts](test/session.test.ts#L581-L608)\n\n## parentUuid 链：物理顺序与逻辑断点\n\n普通消息事件在文件内构成一条**物理单链表**：每个事件的 `parentUuid` 指向上一事件的 `uuid`，首事件的 `parentUuid` 为 `null`。追加批次通过 `readLastEventUuid` 读取文件末尾事件的 UUID 作为链头，因此即使分多次追加，链条也连续不断。\n\n压缩、裁剪、折叠这三类元事件则采用**双父设计**——`parentUuid` 置 `null`、`logicalParentUuid` 指向文件末尾最后一个事件：\n\n| 事件 | parentUuid | logicalParentUuid | 语义 |\n|---|---|---|---|\n| 普通消息 | 上一个事件 uuid | 无 | 物理链，直接接续 |\n| `compact_boundary` | `null` | 最后一个事件 uuid | 逻辑接续、物理断链：边界后的摘要消息 `parentUuid` 指向边界 |\n| `snip_boundary` | `null` | 最后一个事件 uuid | 同上，代表一段被裁剪的历史 |\n| `context_collapse` | `null` | 最后一个事件 uuid | 同上，记录折叠跨度 |\n\n为什么要"物理断链"？因为压缩边界之后，文件里同时存在"压缩前历史"与"压缩后摘要"，二者是替代关系而非父子关系。若边界也走普通链，回放时无法区分"应该继续读取"与"历史已被取代"。`parentUuid = null` 把边界声明为树的新根，而 `logicalParentUuid` 保留了它出现位置的信息——转录重建时可以据此把边界安插在正确的叙述位置。\n\nSources: [session.ts](src/session.ts#L241-L248)、[session.ts](src/session.ts#L252-L313)、[session.ts](src/session.ts#L328-L366)、[session.test.ts](test/session.test.ts#L556-L633)\n\n## 断点恢复：从最后压缩边界重建上下文\n\n`loadSession` 是断点恢复的读取端。它没有从头回放整个文件，而是**从最后一个 `compact_boundary` 的下一条事件开始**构建消息数组：\n\n```mermaid\nflowchart TD\n    A[读取 JSONL 全部行] --> B[从末尾向前扫描<br/>找最后一个 compact_boundary]\n    B --> C{存在边界?}\n    C -- 否 --> D[startLine = 0<br/>加载全部事件]\n    C -- 是 --> E[startLine = 边界索引 + 1]\n    D & E --> F[截取活动事件段]\n    F --> G[reconstructSnippedEvents<br/>还原裁剪占位]\n    G --> H[unwrapMessage<br/>uuid → message.id]\n    H --> I[返回消息数组]\n```\n\n这个设计有双重含义。其一，**压缩边界就是检查点**：压缩把旧上下文浓缩为一条摘要消息（`appendCompactBoundary` 写入的 `user` 类型 summary 事件），恢复时只把摘要之后的消息交给模型，历史不会回灌上下文窗口。其二，文件依然保留压缩前的全部事件，转录重建可以读它们。测试验证了这一不对称：压缩前有 4 条消息 + 边界 + 摘要 + 2 条新消息时，`loadSession` 只返回摘要加 2 条新消息共 3 条。\n\n`loadContextCollapseState` 采用同一套边界定位逻辑，但只收集 `status === \'committed\'` 的 `context_collapse` 跨度——折叠状态随会话一起持久化，恢复时原样还原，保证模型视角的投影摘要与压缩前一致。\n\nSources: [session.ts](src/session.ts#L368-L438)、[session.test.ts](test/session.test.ts#L334-L357)、[ARCHITECTURE.md](ARCHITECTURE.md#L69-L71)\n\n## 被裁剪历史的占位还原\n\n`reconstructSnippedEvents` 解决一个棘手的回放问题：`snip_boundary` 事件记录了被移除消息的 ID 列表（`removedMessageIds`），但移除本身是**元数据**，不是文件行的删除。加载时必须把被裁剪的消息从事件流中剔除，并在原位置插入边界占位符：\n\n1. 收集所有携带非空 `removedMessageIds` 的 `snip_boundary` 事件，建立 `removedId → snip 事件列表` 的映射；\n2. 遍历事件流，跳过 `snip_boundary` 自身；\n3. 若某事件的 `uuid` 命中映射，则在**该事件原本的位置**插入对应的 snip 边界（按 `snip.uuid` 去重，同一边界只插一次），并跳过被移除的事件本身；\n4. 未命中映射的事件原样保留。\n\n这样恢复出的消息序列中，被裁剪的段落收缩为一个 `snip_boundary` 占位消息，而工具调用与结果的配对关系（`toolUseId`）在裁剪后依然完整——`assertNoToolOrphans` 测试专门锁定了这一点。转录重建 `loadTranscript` 同样先过这一层，所以界面上的"裁剪提示"和模型看到的边界占位来自同一条事件。\n\nSources: [session.ts](src/session.ts#L124-L167)、[session.test.ts](test/session.test.ts#L359-L399)、[session.test.ts](test/session.test.ts#L402-L494)\n\n## 恢复会话：resumeSession 的完整装配\n\n恢复不只是"读出消息"——`resumeSession` 要在一个函数里完成会话身份切换、模型上下文重建、界面转录重建与折叠状态还原四件事：\n\n| 装配步骤 | 实现 | 目的 |\n|---|---|---|\n| 切换 `sessionId` | `args.sessionId = sessionId` | 后续所有追加写入落到正确文件 |\n| 重建模型上下文 | 保留新生成的 system 提示 + `loaded` 消息 | 从检查点继续对话 |\n| 重建可见转录 | `loadTranscript` 全量回放；失败则回退遍历消息 | 界面恢复完整历史观感 |\n| 还原折叠状态 | `loadContextCollapseState` + `createContextCollapseState` 兜底 | 投影摘要不丢 |\n| 校准已保存计数 | `args.alreadySavedCount = loaded.length` | 配合 UUID 去重，后续追加不重不漏 |\n\n最后在转录中追加一条 `Session <id> resumed (<n> messages loaded).` 的提示。入口有三条路径，全部汇入同一个 `resumeSession`：交互式 `/resume`（含选择器）、`/resume <id>` 直接指定，以及启动参数 `minicode --resume [id]`（`--fork <id>` 则先分叉再恢复新会话）。启动时还会顺带执行 30 天过期清理并提示"Found N saved session(s)"。\n\n退出时终端打印 `Session <id> saved. To resume: minicode --resume <id>`——这句提示就是"断点"的门牌号：它指向一个可被 `loadSession` 精确重建的文件路径。\n\nSources: [tty-app.ts](src/tty-app.ts#L955-L1009)、[tty-app.ts](src/tty-app.ts#L1223-L1279)、[tty-app.ts](src/tty-app.ts#L1723-L1750)、[tty-app.ts](src/tty-app.ts#L1788)、[index.ts](src/index.ts#L32-L54)、[index.ts](src/index.ts#L107-L134)\n\n## 转录重建：从事件流还原可见界面\n\n`loadTranscript` 把完整事件流（含压缩边界之前的段落）投影为界面可渲染的转录条目。它与 `loadSession` 的差异是本质性的：**模型上下文只要检查点之后，界面转录要全部历史**。\n\n| 事件类型 | 转录条目 | 呈现 |\n|---|---|---|\n| `user` | `{ kind: \'user\' }` | 用户消息原文 |\n| `assistant` | `{ kind: \'assistant\' }` | 助手回复原文 |\n| `progress` | `{ kind: \'progress\' }` | 进度流文本 |\n| `tool_call` | `{ kind: \'tool\', status: \'success\' }` | 工具名 + 序列化输入 |\n| `summary` | `{ kind: \'assistant\' }` | `[Context summary: N messages compressed]` |\n| `compact_boundary` | `{ kind: \'assistant\' }` | `[Context compacted: X → Y tokens]` |\n| `snip_boundary` | `{ kind: \'assistant\' }` | `[Snipped earlier context: removed N messages, freed ~T tokens]` |\n\n这个投影层让"压缩/裁剪"在界面上一目了然：用户看到的不再是消息凭空消失，而是一条带 token 数字的说明条目。`PersistedTranscriptEntry` 的联合类型（user / assistant / progress / tool）定义了投影的契约，`resumeSession` 优先使用它填充转录，失败才回退到遍历消息的简化分支。\n\nSources: [session.ts](src/session.ts#L642-L708)、[session.test.ts](test/session.test.ts#L496-L519)\n\n## 与周边系统的协同\n\n`session.ts` 是持久化中枢，被多条链路消费。命令历史 `history.jsonl` 独立于会话日志，只做去重追加（上限 500 条）与展示：\n\n```mermaid\nflowchart LR\n    subgraph 写入端\n        A[agent-loop 回合结束] --> B[saveSession]\n        C[auto-compact 回调] --> D[appendCompactBoundary]\n        E[snip 回调] --> F[appendSnipBoundary]\n        G[collapse 回调] --> H[appendContextCollapseSpan]\n    end\n    B & D & F & H --> I[sessionId.jsonl]\n    I --> J[loadSession 断点恢复]\n    I --> K[loadContextCollapseState]\n    I --> L[loadTranscript 转录重建]\n    I --> M[listSessions 选择器]\n    N[用户输入] --> O[history.jsonl 命令历史]\n```\n\n一条值得注意的分工：**压缩、裁剪、折叠的写端都只追加元事件，不触碰历史行**。自动压缩回调（`onAutoCompact`）写 `compact_boundary` 后更新 `alreadySavedCount`；snip 回调写 `snip_boundary` 后把计数归零，迫使后续保存走 UUID 去重；折叠回调逐跨度追加 `context_collapse`。所有读端都从这份不可变日志派生自己的视图——这正是事件溯源式架构的一致性来源。\n\n下一步可以顺着两条线继续深入：一是这些边界事件如何支撑分叉与过期清理（[会话分叉、压缩边界与过期清理](20-hui-hua-fen-cha-ya-suo-bian-jie-yu-guo-qi-qing-li)）；二是压缩边界本身是如何被自动压缩与裁剪流程产生的（[自动压缩：触发阈值与失败熔断](12-zi-dong-ya-suo-hong-fa-yu-zhi-yu-shi-bai-rong-duan)、[微压缩：旧工具结果的占位清理](15-wei-ya-suo-jiu-gong-ju-jie-guo-de-zhan-wei-qing-li)）。';export{s as default};

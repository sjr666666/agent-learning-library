const t='MiniCode 的"工具"体系有**两个入口**：一是供**模型（agent）**在推理过程中调用的内置工具（built-in tools），二是供**你**在输入框直接敲击的本地快捷指令（local tool shortcuts，如 `/ls`、`/read`）。二者共享同一套工具注册表与执行管线——快捷指令本质上就是"绕过模型、直接执行内置工具"的语法糖。本页将带你认识这套双入口体系：12 个内置工具各自能做什么、模型如何调用它们、8 个快捷指令的书写语法，以及它们背后统一的权限与审批边界。\n\n## 两大入口：模型的工具与你的快捷指令\n\n在架构上，MiniCode 将"工具能力"抽象为一份**统一注册表**（`ToolRegistry`），而模型与用户只是两种不同的调用方：\n\n- **模型入口**：agent 循环把注册表中的工具 schema 序列化后发给模型，模型返回工具调用请求，由注册表统一校验并执行，形成 `model → tool → model` 闭环。\n- **用户入口**：你在输入框输入 `/ls src`、`/read package.json` 这类快捷指令，MiniCode 先在本地解析成对应的工具调用，直接执行并把结果显示在 transcript 中，**不消耗模型 token、也不经过模型推理**。\n\n输入分发逻辑位于 `tty-app.ts`：先检查 `/tools` 特殊命令，再尝试本地命令（`/help`、`/status` 等），接着尝试解析工具快捷指令，最后才落入普通对话交给模型。这意味着快捷指令的响应是即时的——哪怕离线、没有模型连接也能用。\n\nSources: [tty-app.ts](src/tty-app.ts#L1323-L1369)、[local-tool-shortcuts.ts](src/local-tool-shortcuts.ts#L17-L134)\n\n```mermaid\nflowchart TD\n    A[用户在输入框输入] --> B{以 / 开头?}\n    B -- 否 --> C[普通对话<br/>进入 agent 回合]\n    B -- 是 --> D{本地命令?<br/>/help /status /mcp ...}\n    D -- 命中 --> E[直接输出命令结果<br/>不调用模型]\n    D -- 未命中 --> F{工具快捷指令?<br/>/ls /read /write ...}\n    F -- 命中 --> G[executeToolShortcut<br/>→ ToolRegistry.execute]\n    F -- 未命中 --> H[提示未识别命令]\n    C --> I[Anthropic adapter<br/>发送工具 schema 给模型]\n    I --> J[模型返回 tool_use]\n    J --> K[ToolRegistry 校验并执行]\n    K --> C\n```\n\n## 内置工具总览\n\nMiniCode 在启动时通过 `createDefaultToolRegistry` 注册 **12 个内置工具**。每个工具都是一个 `ToolDefinition`：声明名称、描述、JSON Schema（`inputSchema`）和运行时 Zod Schema（`schema`），其中 `inputSchema` 用于发给模型，`schema` 用于执行前的参数校验。\n\nSources: [tools/index.ts](src/tools/index.ts#L42-L66)、[tool.ts](src/tool.ts#L27-L33)\n\n| 工具名 | 描述 | 类别 |\n|---|---|---|\n| `list_files` | 列出工作区内某目录下的文件与子目录（最多 200 条） | 文件系统 |\n| `grep_files` | 用 ripgrep 按正则搜索文件内容 | 文件系统 |\n| `read_file` | 按 UTF-8 读取文本文件，支持 `offset`/`limit` 分块读取大文件 | 文件系统 |\n| `write_file` | 写入/覆盖一个文件（写入前先审阅 diff） | 文件写入 |\n| `modify_file` | 用审阅后的完整内容替换文件 | 文件写入 |\n| `edit_file` | 按精确文本替换修改文件（支持 `replaceAll`） | 文件写入 |\n| `patch_file` | 一次操作对同一文件应用多组精确文本替换 | 文件写入 |\n| `run_command` | 从白名单中执行常见开发命令，支持 shell 片段与后台任务 | 命令执行 |\n| `web_search` | 通过 DuckDuckGo 搜索公开网页 | Web |\n| `web_fetch` | 抓取网页并提取可读文本内容 | Web |\n| `ask_user` | 向用户提出澄清问题并暂停当前回合等待回复 | 交互 |\n| `load_skill` | 加载指定 `SKILL.md` 的完整内容供模型遵循 | 扩展 |\n\n此外，当有 MCP 服务器连接且发布了 resources/prompts 时，`mcp.ts` 还会按需注入 `list_mcp_resources`、`read_mcp_resource`、`list_mcp_prompts`、`get_mcp_prompt` 四个条件工具；这属于 [MCP 集成](22-mcp-ji-cheng-stdio-http-qi-dong-xie-yi-xie-shang-yu-gong-ju-zhu-ru) 的范畴，本页不再展开。\n\nSources: [tools/index.ts](src/tools/index.ts#L49-L65)、[mcp.ts](src/mcp.ts#L1110-L1180)\n\n## 工具如何被模型调用\n\n工具注册表 `ToolRegistry` 承担了三个职责：**存储**工具列表、**查找**工具、**校验并执行**。模型侧的关键路径是：`AnthropicModelAdapter` 在每次请求时把 `tools.list()` 映射为 Anthropic Messages API 所需的 `tools` 数组（`name` + `description` + `input_schema`），随请求体一起发送。\n\n模型返回 `tool_use` 块后，注册表的 `execute()` 会先对参数做 `schema.safeParse` 校验，校验失败直接返回错误信息而不执行；校验通过才调用 `tool.run(input, context)`。`context` 携带当前工作目录 `cwd` 和权限管理器 `permissions`，工具内部由此完成路径与命令的权限检查。\n\nSources: [anthropic-adapter.ts](src/anthropic-adapter.ts#L330-L340)、[tool.ts](src/tool.ts#L95-L124)\n\n```mermaid\nsequenceDiagram\n    participant M as 模型\n    participant A as AnthropicAdapter\n    participant R as ToolRegistry\n    participant T as 具体工具\n    M->>A: 返回 tool_use(name, input)\n    A->>R: execute(toolName, input)\n    R->>R: schema.safeParse 校验参数\n    alt 校验失败\n        R-->>A: 返回错误信息\n    else 校验通过\n        R->>T: run(input, { cwd, permissions })\n        T->>T: resolveToolPath / ensureCommand\n        T-->>R: ToolResult { ok, output }\n        R-->>A: 结果回传给模型\n    end\n```\n\n## 本地快捷指令速查\n\n`local-tool-shortcuts.ts` 定义了 **8 个快捷指令**，它们与内置工具一一对应，但不覆盖全部工具——Web 搜索、澄清提问等没有快捷形式。所有快捷指令使用 `::` 作为字段分隔符，注意：\n\n- `/write`、`/modify`、`/cmd` 只在第一个 `::` 处切分，**其后的内容（文件内容、命令文本）可以包含 `::`**；\n- `/edit`、`/patch`、`/grep` 使用 `split(\'::\')` 切分所有分隔符，因此 search/replace 文本**不能包含 `::`**；\n- 快捷指令要求精确的前缀匹配：`/ls` 和 `/ls src` 合法，而 `/lsfoo` 会解析失败，避免把相邻的普通文本误当成路径。\n\nSources: [local-tool-shortcuts.ts](src/local-tool-shortcuts.ts#L17-L134)、[local-tool-shortcuts.test.ts](test/local-tool-shortcuts.test.ts#L5-L26)\n\n| 快捷指令 | 语法 | 对应工具 | 示例 |\n|---|---|---|---|\n| `/ls` | `/ls [path]` | `list_files` | `/ls src/tools` |\n| `/grep` | `/grep <pattern>::[path]` | `grep_files` | `/grep TODO::src` |\n| `/read` | `/read <path>` | `read_file` | `/read package.json` |\n| `/write` | `/write <path>::<content>` | `write_file` | `/write notes.txt::hello` |\n| `/modify` | `/modify <path>::<content>` | `modify_file` | `/modify app.ts::export const x = 1` |\n| `/edit` | `/edit <path>::<search>::<replace>` | `edit_file` | `/edit a.ts::foo::bar` |\n| `/patch` | `/patch <path>::<s1>::<r1>::<s2>::<r2>...` | `patch_file` | `/patch a.ts::v1::v2::v3::v4` |\n| `/cmd` | `/cmd [cwd::]<command> [args...]` | `run_command` | `/cmd git status` 或 `/cmd src::npm test` |\n\n解析器对每个指令做了防错处理：`/read` 缺路径、`/write`/`/modify`/`/edit` 缺路径或分隔符、`/patch` 操作数不足或非偶数对、`/cmd` 空命令时都会返回 `null`，从而进入"未识别命令"分支，而不是带着残缺参数执行。\n\nSources: [local-tool-shortcuts.ts](src/local-tool-shortcuts.ts#L39-L133)\n\n## 文件类工具对比：write / modify / edit / patch\n\n四个文件写入类工具共享同一条安全链路：`resolveToolPath(context, path, \'write\')` 解析路径并触发写权限检查，随后 `applyReviewedFileChange` 生成 unified diff 供你审阅批准后才落盘。它们的差别在于**修改粒度**：\n\n| 工具 | 输入 | 典型场景 | 说明 |\n|---|---|---|---|\n| `write_file` | 完整内容 | 新建文件、整文件重写 | 无"搜索是否命中"的校验 |\n| `modify_file` | 完整内容 | 替换文件并展示可审阅 diff | 与 write_file 执行逻辑相同，语义上强调"修改" |\n| `edit_file` | 精确文本 search→replace | 单点替换 | search 未命中时直接报错，不执行；`replaceAll` 可全量替换 |\n| `patch_file` | 多组 search→replace | 一次改多处 | 按序应用，任一 search 未命中即整体中止并报告是第几组失败 |\n\n`edit_file` 与 `patch_file` 都基于"精确字符串包含"匹配（`original.includes(search)`），因此要求 search 与文件内容**逐字符一致**——这与正则或模糊匹配不同，适用于结构化、可预期的代码修改。`patch_file` 执行成功后还会汇报实际应用了几组替换及各自的模式（`replaceOnce`/`replaceAll`）。\n\nSources: [write-file.ts](src/tools/write-file.ts#L11-L30)、[modify-file.ts](src/tools/modify-file.ts#L11-L30)、[edit-file.ts](src/tools/edit-file.ts#L14-L50)、[patch-file.ts](src/tools/patch-file.ts#L18-L84)\n\n## 命令执行与权限边界\n\n`run_command` 是唯一会脱离 MiniCode 自身进程、调用外部程序的工具，因此安全设计最重。它内置了两份白名单：**只读命令**（`pwd`、`ls`、`find`、`rg`、`grep`、`cat`、`head`、`tail`、`wc`、`sed`、`echo`、`df`、`du`、`free`、`uname`、`uptime`、`whoami`）和**开发命令**（`git`、`npm`、`node`、`python3`、`pytest`、`bash`、`sh`、`bun`）。\n\n执行时有三条路径：\n\n- **白名单内命令**：直接以 `execFile` 执行（参数分离，不经过 shell）；\n- **含管道/重定向/变量展开的片段**：`looksLikeShellSnippet` 检测到 `|&;<>()$`` 等字符时，自动改用 `bash -lc` 执行完整片段；\n- **未知命令**：触发 `ensureCommand` 的强制审批弹窗，说明"不在内置只读/开发集合中"。\n\n另外，以 `&` 结尾的命令片段会被识别为**后台任务**：以 detached 方式 spawn，返回 `TASK` id 与 `PID`，并在 transcript 中以轻量 shell task 呈现，而不是阻塞等待。无论哪条路径，命令执行前都会经过 `permissions.ensureCommand` 或 `ensurePathAccess` 审批。更完整的权限层级见 [三层权限系统](17-san-ceng-quan-xian-xi-tong-lu-jing-ming-ling-yu-bian-ji-shen-pi)。\n\nSources: [run-command.ts](src/tools/run-command.ts#L12-L49)、[run-command.ts](src/tools/run-command.ts#L175-L244)、[workspace.ts](src/workspace.ts#L4-L28)\n\n## 快捷指令的执行路径\n\n当你敲下一条快捷指令时，`tty-app.ts` 会走完整的"解析 → 执行 → 呈现"链路。`executeToolShortcut` 负责把快捷指令包装成一次普通工具调用：先在 transcript 中创建 `tool` 条目并显示摘要输入，然后调用 `tools.execute(toolName, input, { cwd, permissions })`，最后把成功或失败的输出回填到该条目，并自动折叠为摘要以降低噪音。\n\nSources: [tty-app.ts](src/tty-app.ts#L903-L953)、[tty-app.ts](src/tty-app.ts#L1347-L1357)\n\n```mermaid\nflowchart LR\n    A[/ls src] --> B[parseLocalToolShortcut]\n    B --> C[{ toolName: list_files,<br/>input: { path: src } }]\n    C --> D[executeToolShortcut]\n    D --> E[ToolRegistry.execute]\n    E --> F{list_files schema 校验}\n    F -- 通过 --> G[resolveToolPath<br/>path=src, intent=list]\n    G --> H[readdir 并格式化输出]\n    H --> I[transcript 显示结果<br/>并折叠为摘要]\n    F -- 失败 --> J[显示校验错误]\n```\n\n## 查看与发现工具\n\n你随时可以在会话中输入 `/tools` 查看当前注册的全部工具及其描述（含 MCP 注入的工具），输入 `/help` 查看所有斜杠命令的用法清单。快捷指令同时登记在 `SLASH_COMMANDS` 注册表中，因此也会出现在 `/` 补全和 `findMatchingSlashCommands` 的模糊匹配建议里——敲错命令时，MiniCode 会提示你可能想输入的命令。\n\nSources: [tty-app.ts](src/tty-app.ts#L1323-L1332)、[cli-commands.ts](src/cli-commands.ts#L19-L155)、[tty-app.ts](src/tty-app.ts#L1359-L1369)\n\n## 下一步阅读\n\n工具只是 agent 的能力集，真正把它们串起来的是执行引擎与消息编排。建议按以下路径继续：\n\n- [代理循环：model → tool → model 的多步执行流程](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng)：理解模型如何连续发起多步工具调用；\n- [统一工具协议：注册、Schema 校验与集中执行](8-tong-gong-ju-xie-yi-zhu-ce-schema-xiao-yan-yu-ji-zhong-zhi-xing)：深入 `ToolRegistry` 与工具定义协议；\n- [三层权限系统：路径、命令与编辑审批](17-san-ceng-quan-xian-xi-tong-lu-jing-ming-ling-yu-bian-ji-shen-pi)：了解本页多次提及的审批边界如何落地；\n- [写前审阅流程与统一 diff 生成](18-xie-qian-shen-yue-liu-cheng-yu-tong-diff-sheng-cheng)：`write/modify/edit/patch` 共用的审阅链路；\n- [交互式界面与斜杠命令速览](4-jiao-hu-shi-jie-mian-yu-xie-gang-ming-ling-su-lan)：系统性地浏览全部斜杠命令（含会话、压缩类命令）。';export{t as default};

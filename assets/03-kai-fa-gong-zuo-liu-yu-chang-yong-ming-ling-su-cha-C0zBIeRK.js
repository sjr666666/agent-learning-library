const e='本页是面向初次参与 Miniclaw 开发的工程师的一站式命令手册。上一页 [快速开始：从克隆到一键启动](2-kuai-su-kai-shi-cong-ke-long-dao-jian-qi-dong) 已经带你把服务跑了起来；本页则回答一个更长期的问题——在日常"改代码 → 验证 → 提交"的循环中，每个环节应该敲什么命令、这些命令背后发生了什么、哪些坑是前人已经替你踩过的。读完本页，你应当能独立完成一次符合项目规范的开发迭代。\n\n## 命令体系的分层结构\n\nMiniclaw 的所有工程命令都围绕一个事实组织：仓库里同时存在三个独立的 Node 项目，它们各有自己的 `package.json` 和锁文件。为了不让开发者记住三套命令，仓库用根目录的 `Makefile` 作为统一编排层——`make` 目标负责"按正确顺序、以正确的联动关系"调用底层的 npm 脚本。\n\n```mermaid\ngraph TD\n    YOU["你敲的命令"] --> MK["make 目标<br/>Makefile 统一编排层"]\n    YOU --> NP["npm run 脚本<br/>根目录 package.json"]\n    MK -- "委托执行" --> NP\n    NP --> BE["后端主服务<br/>src/ 编译到 dist/"]\n    NP --> FE["Web 前端<br/>web/src/ 编译到 web/dist/"]\n    NP --> RUN["Agent Runner<br/>container/agent-runner/"]\n    SHARED["shared/stream-event.ts<br/>跨端共享类型真相源"] -. "make sync-types 同步副本" .-> BE\n    SHARED -.-> FE\n    SHARED -.-> RUN\n```\n\n三个项目的分工如下表所示。注意它们的依赖都必须通过已提交的 lockfile 安装（即 `npm ci` 而非 `npm install`），这是保证团队间依赖一致的关键约定：\n\n| 项目位置 | 角色 | 关键脚本 | 安装方式 |\n| --- | --- | --- | --- |\n| `./`（根目录） | 后端主服务：消息编排、路由、调度、SQLite | `dev`、`build`、`test`、`format` | `npm ci` |\n| `web/` | React 19 + Vite 前端 | `dev`、`build`、`test:e2e` | `npm ci` |\n| `container/agent-runner/` | 容器内/宿主机通用的 Pi Agent Runner | `build`、`start` | `npm ci` |\n\nSources: [Makefile](Makefile#L219-L228)、[package.json](package.json#L7-L32)、[web/package.json](web/package.json#L6-L11)、[container/agent-runner/package.json](container/agent-runner/package.json#L7-L10)\n\n还有一个必须一开始就刻进脑子的约束：**本项目只用原生 Node.js 工具链（npm / npx / tsx / node），不要使用 Bun**。这不是风格偏好，而是硬性技术限制——主服务的 WebSocket 依赖 `ws` 包配合 `@hono/node-server` 的 `server.on(\'upgrade\')` 握手模式，该模式在 Bun 的 HTTP server 下不触发，会导致 WS 全部握手失败：HTTP 接口看似正常，但前端实时流式卡片和通知全部静默失效，非常难以排查。此外项目要求 Node 版本不低于 20。\n\nSources: [Makefile](Makefile#L7-L16)、[package.json](package.json#L76-L78)\n\n## 第一步：安装依赖（每次克隆后执行一次）\n\n`make install` 是唯一推荐的依赖安装入口，它按顺序完成五件事：对根目录、`container/agent-runner/`、`web/` 分别执行 `npm ci`；修复 macOS 上 `node-pty` 预构建二进制 `spawn-helper` 可能缺失的可执行权限（否则 PTY 终端功能会失败）；编译 agent-runner；物化固定版本的内置 Skills 到 `data/builtin-skills/`；最后刷新各 `node_modules` 的目录时间戳，让后续 `make start` 能正确判断依赖是否需要重装。如果你手动绕过这个目标直接在各子目录 `npm install`，很容易漏掉其中某一步。\n\n```bash\nmake install\n```\n\nSources: [Makefile](Makefile#L216-L228)、[CLAUDE.md](CLAUDE.md#L313-L315)\n\n## 第二步：日常开发循环\n\n日常开发的主体循环如下图所示：改动源码后，若涉及 `shared/` 下的共享类型则先同步副本，然后依次通过类型检查、单元测试、格式化三道门禁，最后提交并交给 CI 复验。每一环的具体命令在下文逐一展开。\n\n```mermaid\nflowchart LR\n    A["① 修改代码"] --> B{"动了 shared/ 类型?"}\n    B -- "是" --> C["make sync-types"]\n    B -- "否" --> D["② make typecheck"]\n    C --> D\n    D --> E["③ make test"]\n    E --> F["④ npm run format:changed"]\n    F --> G["⑤ git diff --check"]\n    G --> H["提交 PR"]\n    H --> I["CI 自动复验同样步骤"]\n```\n\n启动开发环境有三种粒度可选。最常用的是 `make dev`，它会先检测依赖是否缺失或过期（比对 `package.json` 与各 `node_modules` 的时间戳）并自动补装，接着确保内置 Skills 和 Docker Agent 镜像就绪、预编译 agent-runner，最后用 `concurrently` 同时拉起后端和前端，日志带蓝绿双色时间戳前缀，方便区分两个进程的输出。如果你只改某一端，可以用单端目标减少噪音和启动开销：\n\n| 启动方式 | 启动内容 | 底层命令 | 适用场景 |\n| --- | --- | --- | --- |\n| `make dev` | 后端 + 前端并行 | `npm run dev:all`（concurrently） | 大多数联调场景 |\n| `make dev-backend` | 仅后端 | `npx tsx src/index.ts` | 只改后端，前端已有产物 |\n| `make dev-web` | 仅前端 | `cd web && vite --host 0.0.0.0` | 只改前端界面 |\n| `make dev PORT=3100` | 同 `make dev`，换端口 | 通过 `WEB_PORT` 传递给两端 | 默认 3000 端口被占用时 |\n\n后端开发模式直接用 `tsx` 运行 TypeScript 源码，无需预先编译；但 agent-runner 是例外——即使 `make dev` 也会先编译它，因为运行中的容器/宿主 Runner 加载的是编译产物。\n\nSources: [Makefile](Makefile#L24-L36)、[package.json](package.json#L21-L22)\n\n## 构建、启动与停止\n\n`make build` 在编译前会自动执行 `sync-types` 同步共享类型，然后用一条 `concurrently` 命令并行编译三个项目，结束时写入 `.build-sentinel` 哨兵文件标记构建新鲜度。生产启动 `make start` 则更"聪明"：它先检查端口占用（占用则报错并提示 `make stop`，绝不悄悄杀进程），再依次执行内置 Skills 物化、Docker 镜像拉取、类型一致性检查，以及三个 `_build-*-if-stale` 增量检测——只有当对应源码目录中存在比上次构建产物更新的 `.ts` 文件时才会重新编译，跳过无变更的部分。最终以 `node dist/index.js` 前台阻塞运行编译产物。\n\n| 命令 | 行为 |\n| --- | --- |\n| `make build` | 同步类型 + 并行编译三项目 |\n| `make build-backend` / `make build-web` | 只编译后端 / 前端 |\n| `make start` | 生产启动：增量检测、按需重编、前台运行 |\n| `make stop` | 杀掉监听 `$(PORT)` 的进程（默认 3000） |\n| `make status` | 显示后端健康状态、日志文件与 Docker 容器概况 |\n| `make logs` | `tail -f /tmp/miniclaw.log`（需先用 `make start > /tmp/miniclaw.log 2>&1 &` 后台启动） |\n| `make clean` | 删除 `dist`、`web/dist`、agent-runner 的 `dist` 及哨兵文件 |\n| `make help` | 列出全部 make 目标及注释 |\n\n停止服务时有一个安全红线：只能用 `lsof -ti:PORT -sTCP:LISTEN | xargs kill` 这类精确匹配监听进程的方式，绝不要使用宽泛的端口 kill 命令——后者可能误杀连接方进程或 Docker 网络代理。\n\nSources: [Makefile](Makefile#L40-L48)、[Makefile](Makefile#L52-L69)、[Makefile](Makefile#L121-L146)、[Makefile](Makefile#L230-L238)、[CLAUDE.md](CLAUDE.md#L320-L322)\n\n## 共享类型同步：最容易踩的坑\n\nMiniclaw 的流式事件契约定义在 `shared/stream-event.ts` 这一个文件里，但它会被复制成三份副本分别供后端、前端和 Runner 引用（连同 `image-detector.ts` 和 `channel-prefixes.ts` 一起）。同步脚本 `sync-stream-event.sh` 采用"内容有差异才覆盖"的策略，避免无谓的时间戳变化触发冗余增量构建。规则很简单：**凡是修改了 `shared/` 下任何文件，立刻运行 `make sync-types`**。如果忘记这一步，`make typecheck` 内嵌的 `check-stream-event-sync.sh` 会替你拦下来，而 CI 中还有一道更严格的校验——同步后用 `git diff --exit-code` 断言副本与真相源完全一致。这套机制的完整设计背景将在 [StreamEvent 流式契约：三端共享类型与同步机制](25-streamevent-liu-shi-qi-yue-san-duan-gong-xiang-lei-xing-yu-tong-bu-ji-zhi) 中展开。\n\nSources: [scripts/sync-stream-event.sh](scripts/sync-stream-event.sh#L19-L46)、[Makefile](Makefile#L73-L78)、[Makefile](Makefile#L187-L188)、[.github/workflows/ci.yml](.github/workflows/ci.yml#L44-L48)\n\n## 质量门禁：提交前必须全绿\n\n`make typecheck` 不只是三次 TypeScript 检查。它首先同步类型副本，然后对根项目、`web/`、`container/agent-runner/` 各跑一遍 `tsc --noEmit`，最后追加三项专项校验：StreamEvent 副本一致性、Runner Prompt 文件完整性（`check-agent-runner-prompts.sh`），以及全仓 Markdown 链接有效性（`docs:check`，防止文档里的相对路径链接失效）。也就是说，一个 make 目标覆盖了四类容易腐化的资产。\n\n格式化工具是 Prettier，配置极简（仅强制单引号），但项目为它做了一层性能优化——`format:check` 和 `format:changed` 都基于 `check-format-changed.mjs`，只处理相对于基准分支（优先 `FORMAT_BASE_REF` 环境变量，其次 `origin/main` 的 merge-base，兜底 `HEAD^`）新增或修改的文件，避免全仓扫描：\n\n| 命令 | 读/写 | 作用范围 | 使用时机 |\n| --- | --- | --- | --- |\n| `make format` | 写入 | 仅 `src/**/*.ts`（Prettier 直跑） | 想全量重排后端代码 |\n| `npm run format:check` | 只读 | 相对基准分支的变更文件 | CI 门禁、自查 |\n| `npm run format:changed` | 写入 | 相对基准分支的变更文件 | **提交前的标准动作** |\n\nSources: [Makefile](Makefile#L150-L171)、[.prettierrc](.prettierrc#L1-L3)、[scripts/check-format-changed.mjs](scripts/check-format-changed.mjs#L41-L60)、[CLAUDE.md](CLAUDE.md#L301-L318)\n\n官方约定的提交前门槛是以下六条命令，全部通过后再 `git push`。这是最低要求，不是建议：\n\n```bash\nnpm run format:changed   # 格式化本次改动\nnpm run docs:check       # 校验文档链接\nmake typecheck           # 四合一静态检查\nmake test                # 全量单元测试\nmake build               # 确保三端都能编译通过\ngit diff --check         # 检查空白错误\n```\n\nSources: [CLAUDE.md](CLAUDE.md#L336-L345)\n\n## 测试速查：单元、E2E、自检与真模型冒烟\n\n测试分四个层级，各自入口和注意事项如下表。初学者最常见的疑问是"`npm test` 怎么一直不退出"——因为根目录的 `test` 脚本是 Vitest 的监听模式，适合边写边测；而 `make test` 和 CI 都显式加了 `--run` 参数表示单次执行完退出。\n\n| 层级 | 命令 | 运行器 | 要点 |\n| --- | --- | --- | --- |\n| 单元/契约测试 | `make test`（或 `npx vitest run`） | Vitest | 约 400 个测试文件位于 `tests/`；排除 `data/**`、`.claude/**`、`web/tests/e2e/**` |\n| 前端 E2E | `cd web && npm run test:e2e` | Playwright | 仅 Chromium 移动端视口（Pixel 7 设备模拟），自动拉起 Vite 开发服务器于 `127.0.0.1:5173` |\n| Runner 自检 | `npm run self-test:agent-runner` | 自研脚本 | 直接驱动 Runner 进程验证 stdin/stdout 协议 |\n| 全链路自检 | `npm run self-test` | 组合命令 | `build:all` + Runner 自检 + 全量测试 |\n| 真实模型冒烟 | `npm run test:real-model` | tsx 脚本 | ⚠️ 产生真实 API 请求和费用，勿将凭据或未脱敏回复写入日志 |\n\n关于 Vitest 配置有两点值得知道：其一，根级 Vitest 同时承担了前端组件测试的角色——配置里把 `@` 别名指向 `web/src`，并把 `react`、`react-dom` 等解析到 `web/node_modules` 下的真实包，所以 `tests/` 目录里的 `.tsx` DOM 测试可以直接运行；其二，`data/**` 被显式排除是因为该目录存放用户的 Agent 工作区，里面可能嵌套着带自己测试套件的项目，而 Vitest 并不遵守 `.gitignore`。完整的测试分层策略详见 [测试体系：单元测试、契约测试与 E2E 场景的组织方式](27-ce-shi-ti-xi-dan-yuan-ce-shi-qi-yue-ce-shi-yu-e2e-chang-jing-de-zu-zhi-fang-shi)。\n\nSources: [vitest.config.ts](vitest.config.ts#L4-L31)、[web/playwright.config.ts](web/playwright.config.ts#L5-L37)、[package.json](package.json#L11-L12)、[CLAUDE.md](CLAUDE.md#L347-L348)\n\n## CI 会替你检查什么\n\n推送之后，GitHub Actions 会按以下顺序完整复验你的改动。对照表的价值在于让你确信：**本地把提交前门槛跑绿，CI 就不会有意外**——两者执行的几乎是同一组命令：\n\n| CI 步骤 | 本地等价命令 |\n| --- | --- |\n| 三项目 `npm ci` + 安装 Playwright Chromium | `make install`（本地多一步 chmod 与 Skills 物化） |\n| 变更文件格式检查（注入 `FORMAT_BASE_REF`） | `npm run format:check` |\n| 同步类型后断言无 diff + Prettier 校验副本 | `make sync-types && git diff --exit-code` |\n| 全量类型检查 | `make typecheck` |\n| 单元测试 | `npm test -- --run` |\n| 三端编译 | `npm run build:all` |\n| 移动端浏览器交互测试 | `npm --prefix web run test:e2e` |\n| Runner 协议自检 | `npm run self-test:agent-runner` |\n\nSources: [.github/workflows/ci.yml](.github/workflows/ci.yml#L29-L63)\n\n## Pi Runtime 升级：只走显式通道\n\nAgent Runner 所依赖的 Pi Runtime（`@earendil-works/pi-coding-agent`）与 SubAgents 扩展版本被锁定在 agent-runner 的 `package.json` 和 lockfile 中。`make start` 被刻意设计为**不会隐式升级任何依赖**，升级必须显式执行 `make update-pi-runtime`：它会查询 npm 上的最新版本，以精确版本号写入依赖并重新编译 runner，随后提示你运行 `make typecheck && npm test -- --run` 验证，验证通过后把 `package.json` 与 lockfile 一起提交。只想看看差多远不动手，可用只读的 `make ensure-latest-pi-runtime` 对比本地与远端版本号；`make update-sdk` 和 `make ensure-latest-sdk` 是旧命名留下的兼容别名，行为完全等同前者。\n\nSources: [Makefile](Makefile#L52-L54)、[Makefile](Makefile#L192-L212)\n\n## 常见修改任务 → 入口文件与配套命令\n\n当你接到一个具体需求时，下表帮你快速定位该动哪些文件、以及改完后必须补跑的命令。这张表的深层含义是：Miniclaw 中多数横切修改都要求"多侧同时改"，例如新增 HTTP API 必须同步更新 `docs/API.md` 和权限矩阵，否则 `docs:check` 与评审都会卡住：\n\n| 任务 | 主要入口 | 改完必跑 |\n| --- | --- | --- |\n| 新增 Web 设置 | `src/runtime-config.ts`、`src/schemas.ts`、`web/src/components/settings/` | `make typecheck` |\n| 新增 HTTP API | 对应 `src/routes/*.ts`，同步 `docs/API.md` 与 ACL | `npm run docs:check` |\n| 新增 MCP 工具 | `container/agent-runner/src/mcp-tools.ts` 与 `src/index.ts` 的 IPC | `npm run self-test:agent-runner` |\n| 新增渠道 | 渠道工厂、`src/im-manager.ts`、`src/channel-prefixes.ts`、账号 Schema/UI | `make typecheck && make test` |\n| 新增 StreamEvent | `shared/stream-event.ts` | `make sync-types` |\n| 修改数据库 | `src/db.ts` 新迁移 + 升级 `CURRENT_SCHEMA_VERSION` + 补迁移测试 | `make test` |\n| 修改 Agent 能力 | Resolver、Capability Preview、Runner 三侧同时验证 | `make typecheck && make test` |\n\n另外提醒：修改 `container/Dockerfile` 或 `entrypoint.sh` 后需要重建容器镜像才能生效；修改 `container/agent-runner/prompts/` 下的运行时 Prompt 则要确保 `scripts/check-agent-runner-prompts.sh` 通过（已包含在 `make typecheck` 中）。各模块职责的详细地图参见 [目录导航与模块地图：从 src 到 container 再到 web](6-mu-lu-dao-hang-yu-mo-kuai-di-tu-cong-src-dao-container-zai-dao-web)。\n\nSources: [CLAUDE.md](CLAUDE.md#L324-L334)、[CLAUDE.md](CLAUDE.md#L158-L160)、[CLAUDE.md](CLAUDE.md#L317-L320)\n\n## 高频故障排查\n\n初学者阶段最常遇到的五类问题及标准解法如下。多数问题的根源可以归结为"没走统一入口"：\n\n| 症状 | 原因 | 解法 |\n| --- | --- | --- |\n| `make start` 报"端口已被占用" | 上次进程未退出 | `make stop`，或换端口 `make start PORT=3100` |\n| 前端收不到流式回复，HTTP 正常 | 误用了 Bun 运行 | 改用 `make dev` / `make start`（Node 模式） |\n| `typecheck` 报共享类型不一致 | 改了 `shared/` 忘记同步 | `make sync-types` 后重跑 |\n| PTY 终端无法创建 | `node-pty` 的 spawn-helper 缺执行权限 | 重跑 `make install` |\n| Vitest 收集到奇怪的外部测试 | `data/` 下嵌套了用户项目 | 属预期，配置已排除，无需处理 |\n\nSources: [Makefile](Makefile#L56-L60)、[Makefile](Makefile#L7-L11)、[Makefile](Makefile#L220-L222)、[vitest.config.ts](vitest.config.ts#L23-L31)\n\n## 备份与重置（速览）\n\n三条与数据生命周期相关的命令在此一并收录：`make backup` 会先用 SQLite 一致性快照接口导出数据库、拒绝包含硬链接或不安全特殊文件的数据、打包为带时间戳的 `miniclaw-backup-{date}.tar.gz`；`make restore` 从备份恢复（多个备份文件时须用 `FILE=xxx.tar.gz` 显式指定，且检测到服务在运行时会拒绝覆盖数据库）；`make reset-init` 则删除 `data/`、`store/`、`groups/` 将系统完全重置为首装状态，属不可逆操作。这三者的原理、数据目录布局细节与日常运维实践，请阅读 [备份恢复与日常运维：数据目录布局与 make 工作流](29-bei-fen-hui-fu-yu-ri-chang-yun-wei-shu-ju-mu-lu-bu-ju-yu-make-gong-zuo-liu)；环境变量如何与 Web 设置、代码默认值叠加生效，见 [配置优先级与环境变量：Web 设置、环境变量与代码默认值](28-pei-zhi-you-xian-ji-yu-huan-jing-bian-liang-web-she-zhi-huan-jing-bian-liang-yu-dai-ma-mo-ren-zhi)。\n\nSources: [Makefile](Makefile#L236-L238)、[Makefile](Makefile#L242-L287)、[Makefile](Makefile#L289-L325)\n\n## 下一步阅读\n\n至此你已经掌握了在本仓库高效迭代的全部操作面。接下来建议按以下路径深入原理：先通过 [整体架构：Backend、Pi Runner、Workspace 与客户端的边界划分](4-zheng-ti-jia-gou-backend-pi-runner-workspace-yu-ke-hu-duan-de-bian-jie-hua-fen) 建立"命令背后的组件全景"，再用 [服务启动与消息编排主流程剖析](7-fu-wu-qi-dong-yu-xiao-xi-bian-pai-zhu-liu-cheng-pou-xi) 理解 `make dev` 拉起的那个进程内部到底在做什么；当你需要修改流式输出行为时，回头精读 [StreamEvent 流式契约：三端共享类型与同步机制](25-streamevent-liu-shi-qi-yue-san-duan-gong-xiang-lei-xing-yu-tong-bu-ji-zhi)，你会对本页反复强调的 `sync-types` 流程有豁然开朗之感。';export{e as default};

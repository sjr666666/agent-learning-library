const n='`/init` 是 MiniCode 的"项目登记"命令：在**当前工作目录**一次性创建 `.mini-code/` 目录、向 `.gitignore` 追加本地产物条目、并自动检测仓库技术栈后生成一份 `MINI.md` 项目指南。它的核心设计目标是**幂等**——重复执行不会破坏已有文件，只会跳过或补充缺失项。这份文档带你走完它的完整生命周期：命令如何接线、检测逻辑如何识别语言与框架、生成了什么、以及生成的文件后续如何被 MiniCode 消费。\n\n## 从斜杠命令到三个产物\n\n`/init` 在斜杠命令注册表中被声明为"Create `.mini-code/`, `.gitignore` entries, and MINI.md in the current project (idempotent)"，随后在本地命令分发器中与 `tryHandleLocalCommand` 挂钩。无论你是在 TUI 交互界面输入，还是在非交互模式（stdin 管道）中使用，最终都会调用同一个入口函数 `initializeRepo(cwd)`，并把执行结果渲染成一段可读的报告。\n\n```mermaid\nflowchart TD\n    A["用户输入 /init"] --> B["tryHandleLocalCommand(input, { cwd })"]\n    B --> C["initializeRepo(cwd)"]\n    C --> D["ensureDir: 创建 .mini-code/"]\n    C --> E["ensureGitignoreEntries: 更新 .gitignore"]\n    C --> F["detectRepo 检测技术栈"]\n    F --> G["renderInitMiniMd 生成 MINI.md 内容"]\n    C --> H["writeFileIfMissing: 写入 MINI.md"]\n    C --> I["renderInitReport 输出执行报告"]\n    I --> J["created / updated / skipped (already exists)"]\n```\n\nSources: [cli-commands.ts](src/cli-commands.ts#L146-L149), [cli-commands.ts](src/cli-commands.ts#L260-L263), [tty-app.ts](src/tty-app.ts#L1334-L1345), [init.ts](src/init.ts#L302-L325)\n\n三个产物的职责各不相同，形成一套"项目侧基础设施"：\n\n| 产物 | 生成方式 | 作用 | 默认状态 |\n|---|---|---|---|\n| `.mini-code/` | `ensureDir` | 项目级配置与会话数据的存放目录 | 已存在则 `skipped` |\n| `.gitignore` 条目 | `ensureGitignoreEntries` | 让 `.mini-code/settings.local.json` 和 `.mini-code/sessions/` 不进版本库 | 追加或新建 |\n| `MINI.md` | `detectRepo` + `renderInitMiniMd` | 给 MiniCode 的项目指南（语言、验证命令、工作约定） | 已存在则**绝不覆盖** |\n\nSources: [init.ts](src/init.ts#L221-L242), [init.ts](src/init.ts#L268-L300), [init.ts](src/init.ts#L244-L266)\n\n## 仓库自动检测：从文件标记推断技术栈\n\n"仓库自动检测"指的是 `detectRepo(cwd)` 函数：它不依赖任何配置文件或用户声明，而是通过**探查目录中的标记文件**（marker files）与 `package.json` 的依赖关键字，一次性判定 11 个布尔特征。检测结果随后被三个渲染函数消费：`detectedLanguages`（语言清单）、`detectedFrameworks`（框架清单）、`verificationLines`（验证命令建议）。\n\n```mermaid\nflowchart LR\n    A["detectRepo(cwd)"] --> B["读取 package.json"]\n    A --> C["existsSync 检查标记文件"]\n    B --> D["next / react / vite / @nestjs / typescript"]\n    C --> E["Cargo.toml / pyproject.toml / tsconfig.json / src/ / tests/"]\n    D --> F["detectedLanguages + detectedFrameworks"]\n    E --> F\n    F --> G["MINI.md 的 Detected stack 小节"]\n```\n\n具体判定规则如下（全部基于文件存在性与关键字匹配）：\n\n| 特征 | 判定依据 | 典型触发 |\n|---|---|---|\n| Rust 工作区 | 存在 `rust/Cargo.toml` | 多语言仓库的 Rust 子目录 |\n| Rust 根项目 | 存在 `Cargo.toml` | 纯 Rust 仓库 |\n| Python | 存在 `pyproject.toml` / `requirements.txt` / `setup.py` | 任选其一 |\n| TypeScript | 存在 `tsconfig.json`，或 `package.json` 含 `typescript` | 两者任一 |\n| Next.js / React / Vite / NestJS | `package.json` 依赖关键字 `"next"` / `"react"` / `"vite"` / `@nestjs` | 精确子串匹配 |\n| `src/` / `tests/` 目录 | `src` 与 `tests` 目录存在 | 目录结构探测 |\n\nSources: [init.ts](src/init.ts#L36-L63), [init.ts](src/init.ts#L65-L81), [init.ts](src/init.ts#L83-L114)\n\n一个值得注意的细节是 TypeScript 的判定存在**优先级**：若检测到 `typescript` 则标记为 "TypeScript"；否则只要存在 `package.json`，就降级为 "JavaScript/Node.js"。这样即使是纯 JS 项目也不会被误报为"无语言"。检测结果完全基于静态文件探查，因此对空目录项目会诚实地输出 "No specific language markers were detected yet" 的占位提示，而不是强行编造技术栈。\n\nSources: [init.ts](src/init.ts#L65-L72), [init.test.ts](test/init.test.ts#L19-L30)\n\n## MINI.md 模板：六段式项目指南\n\n`renderInitMiniMd` 把检测结果组装成一个结构化的 Markdown 模板。它不是随便生成的样板文件，而是按"检测 → 验证 → 结构 → 约定"的顺序递进，最终形成 MiniCode 在项目中工作的行为契约：\n\n| 小节 | 内容来源 | 回答的问题 |\n|---|---|---|\n| `# MINI.md` 头部 | 固定文案 | 这份文件是给谁看的 |\n| `## Detected stack` | `detectedLanguages` / `detectedFrameworks` | 仓库用什么语言和框架 |\n| `## Verification` | `verificationLines` | 改代码前应该跑什么命令 |\n| `## Repository shape` | `repositoryShapeLines` | `src/`、`tests/`、`rust/` 各是什么角色 |\n| `## Framework notes` | `frameworkNotes` | 框架特有的约定（路由、构建、模块边界） |\n| `## Working agreement` | 固定三条约定 | MiniCode 与人类协作者如何相处 |\n\nSources: [init.ts](src/init.ts#L159-L219)\n\nVerification 小节是自动检测最有价值的产出：Rust 项目会得到 `cargo fmt` / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo test --workspace` 的完整命令链，Python 项目会根据是否检测到 `pyproject.toml` 给出不同的检查建议，JS/TS 项目则统一指向 `npm test`、`npm run lint`、`npm run build`。三条固定的工作约定（Working agreement）则沉淀了项目治理原则：\n\n1. 保持小步可审查的变更，让生成的引导文件与实际仓库工作流对齐；\n2. 共享默认值放在 `~/.mini-code/settings.json`，项目本地覆盖只放在 `.mini-code/settings.local.json`；\n3. **绝不自动覆盖已有 `MINI.md`**——仓库工作流变化时应由人工有意更新。\n\nSources: [init.ts](src/init.ts#L83-L114), [init.ts](src/init.ts#L209-L216), [init.ts](src/init.ts#L134-L157)\n\n## 幂等性：同一仓库跑一百次也安全\n\n`/init` 的注册描述里特意标注了 `(idempotent)`，这是它区别于一次性脚手架工具的关键。三个产物分别由三个"存在性优先"的写入助手负责，每个助手都把"已存在"当作正常路径而非错误：\n\n- **`ensureDir`**：目录已存在且确实是目录 → `skipped`；不存在 → `mkdir` 后返回 `created`；对并发创建导致的 `EEXIST` 竞态也做了兜底。\n- **`writeFileIfMissing`**：使用 `writeFile` 的 `wx` 独占标志写入，目标已存在 → `skipped`，绝不截断覆盖；父目录缺失时先 `mkdir` 再重试。\n- **`ensureGitignoreEntries`**：先读取现有 `.gitignore`，逐行比对后再追加缺失条目，重复执行不会产生重复行。\n\n```mermaid\nflowchart TD\n    A["再次执行 /init"] --> B{".mini-code/ 已存在?"}\n    B -- 是 --> C["skipped"]\n    B -- 否 --> D["created"]\n    A --> E{"MINI.md 已存在?"}\n    E -- 是 --> F["skipped — 保留用户自定义内容"]\n    E -- 否 --> G["created"]\n    A --> H{".gitignore 已含条目?"}\n    H -- 是 --> I["skipped"]\n    H -- 否 --> J["updated — 仅追加缺失行"]\n```\n\n这套幂等语义被测试显式锁定：`initializeRepo` 连续执行两次时所有产物必须全部 `skipped`；预先写入自定义 `MINI.md` 后执行 `/init`，文件内容必须原样保留；预先写入带 `.mini-code/sessions/` 的 `.gitignore` 再执行，条目不得重复。测试还验证了 `.gitignore` 不存在时的首次创建路径（首行是 `# MiniCode local artifacts` 注释头）。\n\nSources: [init.test.ts](test/init.test.ts#L110-L121), [init.test.ts](test/init.test.ts#L123-L133), [init.test.ts](test/init.test.ts#L135-L160), [init.ts](src/init.ts#L33-L34)\n\n## 执行报告与后续消费\n\n`/init` 执行完毕后，`renderInitReport` 输出一个对齐格式的报告：第一行 `Init`，第二行列出项目根目录，随后是三个产物的状态标签（`created` / `updated` / `skipped (already exists)`），最后以 `Next step: Review and tailor the generated guidance` 收尾，提示用户生成的模板只是起点、需要人工审阅调优。\n\nSources: [init.ts](src/init.ts#L327-L343), [init.test.ts](test/init.test.ts#L163-L182)\n\n`MINI.md` 并非一次性产物——它在**每次会话启动时**都会被分层记忆系统重新读取。`discoverInstructionFiles` 在从根目录到当前目录的每一级祖先目录中，按固定优先级扫描 `MINI.md`、`MINI.local.md`、`.mini-code/MINI.md`、`CLAUDE.md` 等候选文件（`MINI.md` 排在候选列表首位），并支持 `@include` 引用与内容哈希去重。这正是 `/init` 生成的文件能持续影响 MiniCode 行为的原因：它写入的验证命令、仓库结构说明与工作约定，会随系统提示词注入每次会话。\n\nSources: [memory.ts](src/memory.ts#L21-L28), [memory.ts](src/memory.ts#L130-L145), [memory.ts](src/memory.ts#L234-L253)\n\n## 进一步阅读\n\n- 想了解 `MINI.md` 如何被遍历、`@include` 如何展开：见 [分层记忆：指令文件遍历、@include 与去重](23-fen-ceng-ji-yi-zhi-ling-wen-jian-bian-li-atinclude-yu-qu-zhong)\n- 想了解 `.gitignore` 中排除的 `.mini-code/settings.local.json` 与全局 `~/.mini-code/settings.json` 的关系：见 [配置文件体系：settings.json、mcp.json 与目录覆盖](3-pei-zhi-wen-jian-ti-xi-settings-json-mcp-json-yu-mu-lu-fu-gai)\n- 想了解 `.mini-code/sessions/` 为何需要排除在版本库之外：见 [会话管理入门：保存、恢复、重命名与分叉](6-hui-hua-guan-li-ru-men-bao-cun-hui-fu-zhong-ming-ming-yu-fen-cha)\n- 想了解斜杠命令体系的整体布局：见 [交互式界面与斜杠命令速览](4-jiao-hu-shi-jie-mian-yu-xie-gang-ming-ling-su-lan)\n- 想了解 `init.test.ts` 在测试体系中的位置：见 [测试体系：从单元测试到集成回归](28-ce-shi-ti-xi-cong-dan-yuan-ce-shi-dao-ji-cheng-hui-gui)';export{n as default};

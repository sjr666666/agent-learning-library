const e='HappyClaw 的测试体系遵循"由快到慢、由隔离到真实"的分层原则：绝大多数验证落在毫秒级的 Vitest 单元测试上，通过**源码级契约**与**类型同步契约**在编译前锁定跨模块边界，再由少量**真实 SDK 运行时契约**验证与锁定版本 Claude Agent SDK 的集成行为；最昂贵的两类冒烟测试——真实模型连通性与容器镜像运行时——被排除在常规 CI 之外，作为手动验证与发布门禁独立执行。\n\n## 测试分层全景\n\n`docs/PROMPT-SKILL-RUNTIME-TEST-PLAN.md` 明确将测试分为五层，从纯函数到真实 Provider 冒烟，每层有明确的"真实对象"与"通过标准"：\n\n| 层级 | 真实对象 | 代表用例 | 通过标准 |\n| --- | --- | --- | --- |\n| 纯函数 | PromptPlan、ContextBudget、TurnOutcome、Skill/MCP Resolver | 同名 Skill、MCP 配置变化、超预算、关闭未回复 | 输出状态、哈希与游标决策精确匹配 |\n| 宿主集成 | Session Skill 对账、Container mounts、API preview | ghost Skill、disabled collision、Plugin MCP | 隔离 ghost；预览与实际 manifest 一致 |\n| SDK/CLI 集成 | 锁定版本 Claude Agent SDK 与 Claude CLI | main → Task Subagent → main | 子请求只收到自有 prompt、短运行契约与显式 Skill |\n| 前端契约/构建 | Agent 页面、Effective Context | 点击"新建"、选择工作区 | 创建入口在右侧主区；真实上下文与总预算可见 |\n| 真实 Provider 冒烟 | 当前已配置的 Provider | 固定标记问答 | 模型完成请求且逐字返回固定标记 |\n\nSources: [PROMPT-SKILL-RUNTIME-TEST-PLAN.md](docs/PROMPT-SKILL-RUNTIME-TEST-PLAN.md#L5-L13)\n\n从执行频率与成本看，这套体系呈金字塔结构——底部是零成本、全量运行的单元测试，顶部是需要真实 Provider 请求（可能计费）与 Docker 镜像的冒烟测试：\n\n```mermaid\nflowchart TB\n    subgraph CI[CI 自动执行 · 每次 PR / push]\n        A[纯函数单元测试<br/>Vitest · 数百个用例文件]\n        B[契约测试<br/>源码文本断言 · 类型同步 · SDK 运行时]\n        C[前端契约<br/>DOM 源码断言 · Playwright 移动端 E2E]\n    end\n    subgraph RELEASE[发布 / 手动门禁]\n        D[真实模型冒烟<br/>npm run test:real-model]\n        E[容器镜像冒烟<br/>scripts/smoke-agent-image.sh]\n    end\n    A --> B --> C\n    C -. 非 CI 自动 · 手动触发 .-> D\n    C -. 仅 main 分支发布 .-> E\n```\n\nSources: [ci.yml](.github/workflows/ci.yml#L53-L64)、[docker-publish.yml](.github/workflows/docker-publish.yml#L1-L45)\n\n## 单元测试层：Vitest 配置与目录约定\n\n根目录统一使用 **Vitest 4** 作为测试运行器，配置集中在 `vitest.config.ts`。该配置有两处关键设计：\n\n1. **跨包别名**：将 `@` 指向 `web/src`，并将 react 系列解析到 `web/node_modules`，使根目录的 Vitest 可以同时驱动后端 TS 测试与前端 React 组件测试（如 `web/tests/MarkdownRenderer.test.tsx`）。\n2. **显式排除**：注释明确说明 *vitest 不尊重 .gitignore*，因此必须排除 `data/**`（用户 agent 工作区，可能嵌套自带测试套件的项目）、`.claude/**` 与 `web/tests/e2e/**`（后者交给 Playwright 独立运行）。\n\nSources: [vitest.config.ts](vitest.config.ts#L1-L33)\n\n测试文件规模与命名约定（根 `tests/` 目录）：\n\n| 类别 | 数量 | 说明 |\n| --- | --- | --- |\n| `*.test.ts` | 323 | 后端纯函数、服务逻辑、契约、路由测试 |\n| `*.test.tsx` | 3 | React DOM 相关测试 |\n| 文件名含 `contract` | 26 | 契约测试（见下一节） |\n| 文件名含 `integration` | 2 | 真实 SDK/CLI 集成测试 |\n| `container/agent-runner/tests/` | 1 | 交叉引入 agent-runner 源码的单元测试 |\n| `web/tests/` | 1 + e2e | MarkdownRenderer 组件测试 + Playwright 套件 |\n\n典型纯函数测试（`turn-outcome.test.ts`）直接导入 `src/turn-outcome.js`，对 `resolveTurnOutcome` 的状态机输入穷举断言——例如"runner 关闭但输入回合未健康完成 → 可重试、游标保持"，这是测试计划中"输出状态、哈希与游标决策精确匹配"标准的直接落地。`container/agent-runner/tests/background-task-drain.test.ts` 则跨越包边界，通过相对路径引入 agent-runner 的 `background-task-drain.js`，验证后台任务排空追踪器的状态机与事件到达顺序无关性。\n\nSources: [turn-outcome.test.ts](tests/turn-outcome.test.ts#L8-L60)、[background-task-drain.test.ts](container/agent-runner/tests/background-task-drain.test.ts#L1-L50)\n\n## 契约测试层：四种形态\n\n"契约测试"在此仓库不是单一技术，而是**四种互补的形态**，覆盖从源码文本到真实 SDK 运行时的不同边界：\n\n```mermaid\nflowchart LR\n    subgraph SRC[源码级契约]\n        A1[Makefile 运行时契约]\n        A2[REST 路由绑定契约]\n        A3[前端 DOM 源码契约]\n    end\n    subgraph TYPE[类型同步契约]\n        B1[shared/ → 三端副本 diff]\n    end\n    subgraph RUNTIME[运行时契约]\n        C1[fake Anthropic SSE + 真实 SDK/CLI]\n    end\n    subgraph SCRIPT[工程脚本契约]\n        D1[prompts 引用存在性]\n        D2[文档链接完整性]\n    end\n    SRC -. 编译前 .-> RUNTIME\n    TYPE -. CI 门禁 .-> RUNTIME\n    SCRIPT -. typecheck 阶段 .-> RUNTIME\n```\n\n### 形态一：源码文本断言（读取文件验证约定）\n\n这类测试不执行被测代码，而是**读取源码文本并断言关键约定存在**，防止重构时悄然偏离既定架构。`tests/makefile-runtime-contract.test.ts` 读取 Makefile 并断言：生产启动路径必须是 `make start` → `node dist/index.js`、禁止引入 pm2、备份必须走 `sqlite-snapshot.mjs` 与带端口守卫的 restore。`tests/channel-binding-rest-contract.test.ts` 则同时读取多个路由源文件与前端组件，断言它们共享相同的 mount 构建器、错误文案与绑定目标分类逻辑——本质是"多实现必须保持同步"的跨文件契约。\n\nSources: [makefile-runtime-contract.test.ts](tests/makefile-runtime-contract.test.ts#L1-L32)、[channel-binding-rest-contract.test.ts](tests/channel-binding-rest-contract.test.ts#L1-L60)\n\n### 形态二：类型同步契约（shared/ 单源真值）\n\nStreamEvent、image-detector、channel-prefixes 三类类型定义以 `shared/` 为唯一真值，通过 `make sync-types` 生成到主服务、agent-runner 与 web 三端的副本。CI 中"Verify generated type sources"步骤先执行同步再 `git diff --exit-code`，任何手工改动副本而未更新 shared 的提交都会失败。`scripts/check-stream-event-sync.sh` 在 typecheck 阶段以 `diff -q` 再次校验，双保险确保运行在三端的类型永不分叉。\n\nSources: [ci.yml](.github/workflows/ci.yml#L44-L48)、[check-stream-event-sync.sh](scripts/check-stream-event-sync.sh#L1-L50)\n\n### 形态三：真实 SDK/CLI 运行时契约（integration）\n\n`tests/agent-runner-sdk-subagent-contract.integration.test.ts` 是体系中最重的一个用例：它在临时目录中构造项目，用**锁定版本的 Claude Agent SDK + 真实 claude 可执行文件**发起查询，同时起一个本地的 **fake Anthropic SSE 服务器**捕获所有上游请求。测试验证的是代理（delegation）运行时契约：main 请求携带 `MAIN_ONLY_MARKER`，子 Agent 请求只包含 HappyClaw 委托契约与显式 Skill 内容，**不得泄漏 main marker**；通过 SSE 事件流模拟 Task 工具调用，断言子请求的 system prompt 与消息序列符合 `withHappyClawSubagentContract` 的注入逻辑。\n\nSources: [agent-runner-sdk-subagent-contract.integration.test.ts](tests/agent-runner-sdk-subagent-contract.integration.test.ts#L1-L80)、[agent-runner-sdk-subagent-contract.integration.test.ts](tests/agent-runner-sdk-subagent-contract.integration.test.ts#L145-L279)\n\n### 形态四：工程脚本契约（把"启动才爆"的故障提前到 typecheck）\n\n`scripts/check-agent-runner-prompts.sh` 专门针对一类生产事故：agent-runner 在模块加载时 `readFileSync` 读取 prompts 下的 `.md` 文件，文件缺失会导致**容器启动时才 ENOENT 崩溃**。该脚本用四种正则模式捕获所有 `loadPrompt(\'foo.md\')` 调用形态（含 prettier 重排后的多行形态），并设置"防漏网"守卫——任何未被模式捕获的 `.md` 字面量都会使构建失败，而不是静默缩小检查范围。`scripts/check-docs.mjs` 则校验仓库内所有 Markdown 链接的目标文件真实存在。两者都挂在 `make typecheck` 目标下，与三个子项目的 `tsc --noEmit` 一起构成静态门禁。\n\nSources: [check-agent-runner-prompts.sh](scripts/check-agent-runner-prompts.sh#L1-L30)、[check-agent-runner-prompts.sh](scripts/check-agent-runner-prompts.sh#L100-L158)\n\n## 前端测试：DOM 源码契约与 Playwright 移动端 E2E\n\n前端质量保障分两条路径：\n\n**路径一（Vitest 内）**：`tests/frontend-*.test.ts` 读取 `web/src/pages/*.tsx` 源码文本做**结构断言**——例如 `frontend-agent-create-workspace.test.ts` 断言"新建"入口必须落在右侧主工作区而非侧边栏、保留 `aria-labelledby` 与 `focus-visible:ring-2` 等可访问性约定。这类测试不渲染 DOM，但通过精确的源码片段提取（`slice` + 关键词定位）锁定交互模式的回归。真正渲染组件的用例（如 `web/tests/MarkdownRenderer.test.tsx`）使用 `react-dom/server` 的静态渲染验证图片路径解码逻辑。\n\n**路径二（Playwright）**：`web/tests/e2e/mobile-chat.spec.ts` 针对 Pixel 7 移动端视口，通过自定义 harness 页面（`mobile-chat-harness.html` + tsx 驱动）在纯前端层模拟消息上下文，验证滚动边界提示、嵌套模态层内的 Markdown 预览焦点保持、PDF 预览与图片灯箱交互。Playwright 配置使用系统 Chromium 二进制（CI 中安装 `--with-deps chromium`），并对 `web/tests/e2e/**` 与 Vitest 做了目录隔离。\n\nSources: [frontend-agent-create-workspace.test.ts](tests/frontend-agent-create-workspace.test.ts#L1-L67)、[MarkdownRenderer.test.tsx](web/tests/MarkdownRenderer.test.tsx#L1-L40)、[playwright.config.ts](web/playwright.config.ts#L1-L39)\n\n## 真实模型冒烟：固定标记问答\n\n真实模型冒烟由 `npm run test:real-model` 触发（package.json 第 20 行），对应脚本 `scripts/test-real-model-smoke.ts`。它**不进入 CI 自动执行**，原因在测试计划中写明：会产生一次真实 Provider 请求，可能计费。\n\n工作流程：\n\n1. 从 `runtime-config` 读取当前 Provider 配置，通过 `sdkQuery` 发送一条指令：要求模型**只回复固定标记 `HAPPYCLAW_REAL_SMOKE_OK_20260721`**，不加标点、解释或代码块，超时 90 秒；\n2. 输出一条**结构化证据 JSON**，仅含五个布尔/数值字段：`providerConfigured`（凭据是否存在）、`baseUrlPresent`、`modelPresent`、`completed`、`exactMatch`、`responseLength` 与 `elapsedMs`；\n3. 判定标准是 `exactMatch`——模型必须**逐字**返回标记（长度 32），否则进程以退出码 1 失败。\n\n安全设计是此脚本的核心约束：**绝不打印** Provider 值、Endpoint、模型名、凭据或意外回复正文——注释明确说明 Provider 错误可能包含端点或上游响应片段，因此 catch 分支只写一行 `real-model smoke failed`。测试计划记录 2026-07-21 的两次执行：均完成且逐字匹配，回复长度 32，耗时约 9.1 秒与 7.5 秒，全程未泄漏任何敏感值。\n\nSources: [package.json](package.json#L19-L20)、[test-real-model-smoke.ts](scripts/test-real-model-smoke.ts#L6-L49)、[PROMPT-SKILL-RUNTIME-TEST-PLAN.md](docs/PROMPT-SKILL-RUNTIME-TEST-PLAN.md#L73-L81)\n\n## 容器镜像冒烟：CDP 探针与发布门禁\n\n与模型冒烟并列的是**镜像级冒烟** `scripts/smoke-agent-image.sh`，它作为 `docker-publish.yml` 的核心门禁运行：镜像按 digest 构建（不带用户可见标签），启动容器后轮询探测内部 Chromium 的 CDP HTTP 端点（`http://127.0.0.1:9222/json/version`），要求返回的 JSON 含非空 Browser 与 `ws://` 开头的 `webSocketDebuggerUrl`。脚本还校验：\n\n- **架构匹配**：传入期望架构（amd64/arm64），`docker image inspect` 不符即失败；\n- **身份模式 fail-closed**：根据调用者 uid 显式传入 `HAPPYCLAW_HOST_IDENTITY_MODE=host-root` 或 `direct` + uid/gid——与生产容器运行时的身份契约一致，未设置时 entrypoint 必须拒绝启动；\n- **临时文件系统**：`.claude`、`ipc`、`group`、`extra` 全部挂载为 tmpfs。\n\n只有两个原生架构 runner（amd64 + arm64）**都通过 CDP 探针**后，`promote` 任务才合并 multi-platform manifest、用 cosign 签名并以 `:latest` 发布——先验后发，任何平台失败都不会污染用户可见标签。\n\nSources: [smoke-agent-image.sh](scripts/smoke-agent-image.sh#L1-L105)、[docker-publish.yml](.github/workflows/docker-publish.yml#L46-L99)\n\n## CI 流水线中的测试整合\n\n常规 CI（`.github/workflows/ci.yml`）按依赖顺序串联六道验证，形成"格式化 → 类型 → 测试 → 构建 → 浏览器 → 运行时"的渐进门禁：\n\n```mermaid\nflowchart LR\n    A[format:check<br/>变更文件 prettier] --> B[Verify generated types<br/>sync-types + git diff]\n    B --> C[Typecheck<br/>三子项目 tsc + 同步脚本 + docs:check]\n    C --> D[Test<br/>vitest run]\n    D --> E[Build<br/>build:all]\n    E --> F[Mobile E2E<br/>Playwright Chromium]\n    F --> G[Agent runner self-test<br/>tsx 事故重放]\n```\n\n其中"Agent runner self-test"（`scripts/self-test-agent-runner.ts`）是一个**零依赖的事故重放脚本**：模拟"后台 Agent 仍运行时模型给出看似最终的等待回复"这一真实事故序列，验证 `shouldForceBackgroundTaskSummary` 能在两次尝试内强制续写、且续写 prompt 包含禁止再次发送进度更新的指令。它不依赖 SDK 与网络，在 CI 中以毫秒级成本锁住一个高频生产故障模式。此外还有一份**后端手工 E2E 脚本** `tests/e2e/interrupt-ipc-message-loss.mjs`（针对 #421：中断不得丢失 IPC 注入消息），它需要运行中的服务与真实账号，通过 WebSocket 模拟"发送 A → 处理中发送 B → 中断 → 验证 B 被回复"的完整链路，属于按需执行的全栈验证。\n\nSources: [ci.yml](.github/workflows/ci.yml#L39-L64)、[self-test-agent-runner.ts](scripts/self-test-agent-runner.ts#L1-L75)、[interrupt-ipc-message-loss.mjs](tests/e2e/interrupt-ipc-message-loss.mjs#L1-L40)\n\n## 选择测试层级的决策框架\n\n| 验证目标 | 推荐层级 | 原因 |\n| --- | --- | --- |\n| 纯函数状态机（游标、预算、解析） | Vitest 单元测试 | 毫秒级、可穷举、零外部依赖 |\n| 跨文件/跨端约定一致性 | 源码文本契约 | 编译前锁定，重构即时报错 |\n| 三端共享类型 | 类型同步契约 + CI diff | 单一真值源，杜绝手工副本漂移 |\n| SDK/CLI 集成行为 | 运行时 integration 测试 | 锁定版本的真实 SDK + fake 上游，兼顾真实性与确定性 |\n| 前端交互体验 | Playwright 移动端 E2E | 真实浏览器视口与滚动/焦点行为 |\n| Provider 连通性 | 真实模型冒烟（手动） | 验证端到端链路，但可能计费，不进 CI |\n| 发布镜像可用性 | 镜像 CDP 冒烟（发布门禁） | 先验后发，digest 门禁 + 双架构 + 签名 |\n\n**延伸阅读**：理解测试所守护的运行时行为，可继续阅读 [CI 流水线与工程规范](30-ci-liu-shui-xian-yu-gong-cheng-gui-fan)、[Agent Runner 与 IPC 通信协议](10-agent-runner-yu-ipc-tong-xin-xie-yi)（契约测试的核心被测对象）、[StreamEvent 流式事件体系与类型同步](11-streamevent-liu-shi-shi-jian-ti-xi-yu-lei-xing-tong-bu)（类型同步契约的源头）；若关心冒烟测试覆盖的运行时故障模式，参见 [Runner 生命周期、卡死检测与故障恢复](21-runner-sheng-ming-zhou-qi-qia-si-jian-ce-yu-gu-zhang-hui-fu)。';export{e as default};

const n='Miniclaw 允许同一用户在同一渠道提供商下接入多个 Bot（例如两个飞书应用、多个 Telegram Bot），并把任意一个外部聊天（群聊或私聊）挂载到某个 Workspace 或某个具体 Session 上。本页聚焦三条主线：**多渠道账号（Channel Account）如何被寻址与隔离**、**群聊/私聊到 Workspace/Session 的绑定边界如何强制执行**、以及**外部聊天最终归属于哪个 Miniclaw 用户与 Bot 身份如何判定与迁移**。所有结论均来自对路由、挂载服务与准入契约源码的直接考证。\n\n## 三层模型：账号、挂载与绑定目标\n\n理解多账号体系的第一性原理是把"谁在收消息"、"消息进哪个聊天上下文"、"聊天上下文落到哪个执行对象"三个问题分开。第一层是 `channel_accounts` 表中的 **ChannelAccount**：它持有 `owner_user_id`、`provider`、加密密钥引用（`secret_ref`）、授权生命周期（`auth_status`）与传输生命周期（`transport_status`），并以 `UNIQUE(owner_user_id, provider, name)` 约束和"每用户每提供商仅一个默认账号"的部分唯一索引保证多账号下的确定性 [Sources: [db.ts](src/db.ts#L680-L704)]。第二层是 `registered_groups` 表中每条外部聊天记录（**RegisteredGroup**），其上的 `channel_account_id` 字段声明该外部聊天归属于哪个账号，而 `target_agent_id`（Session 绑定）与 `target_main_jid`（Workspace 绑定）两个字段互斥地描述挂载目标 [Sources: [types.ts](src/types.ts#L173-L184)]。第三层是归一化的 `channel_mounts` 投影表，以 `channel_jid` 为主键，冗余存储 `workspace_jid`、`session_id`、`routing_mode` 等查询友好字段 [Sources: [types.ts](src/types.ts#L121-L136)、[db.ts](src/db.ts#L720-L735)]。\n\n```mermaid\nflowchart LR\n    subgraph L1["第 1 层：渠道身份"]\n        A1["ChannelAccount bot-a"]\n        A2["ChannelAccount bot-b"]\n    end\n    subgraph L2["第 2 层：外部聊天记录 RegisteredGroup"]\n        G1["feishu:group-1#account:bot-a<br/>target_main_jid = web:ws1"]\n        G2["feishu:p2p-9#account:bot-b<br/>target_agent_id = sess-42"]\n    end\n    subgraph L3["第 3 层：执行目标"]\n        W["Workspace web:ws1<br/>(含主会话)"]\n        S["会话 Session sess-42<br/>挂在某 Workspace 下"]\n    end\n    A1 -- "拥有(ownership)" --> G1\n    A2 -- "拥有(ownership)" --> G2\n    G1 -- "群聊→workspace 挂载" --> W\n    G2 -- "私聊→session 绑定" --> S\n```\n\n这一分层的意义在于：账号解决"**凭证与连接**"，聊天记录解决"**路由与绑定**"，二者通过账号作用域 JID 关联而非靠运行时猜测。后续小节将逐层展开这条链路。\n\n## 账号作用域 JID：多账号共存的寻址基础\n\n当同一提供商有多个 Bot 时，仅凭平台原生 ID 无法区分消息属于哪个账号——飞书的 `oc_*` 群 ID 在不同自建应用间可能重复出现。Miniclaw 的解法是在 JID 中引入 **`account:` 片段**：`parseChannelAddress` 把形如 `feishu:chat-a#account:bot-a#root:r1#thread:t1` 的地址解析为 provider、externalChatId、channelAccountId、threadId/rootMessageId 四个正交维度；没有 `account:` 片段的地址被标记为 `legacy: true`，即历史单账号时代的遗留格式 [Sources: [channel-address.ts](src/channel-address.ts#L27-L52)]。\n\n围绕这个解析器有一组对称的纯函数构成完整的地址代数：`scopeChannelJid` 为 JID 补上或替换账号片段（保留 provider 原生的 thread/root 片段）；`toProviderJid` 在调用提供商 SDK 前剥掉 Miniclaw 私有的 `account:` 片段；`channelConversationJid` 则截取到"账号 + 外部聊天 ID"粒度、丢弃 thread/root，作为跨线程稳定的会话标识 [Sources: [channel-address.ts](src/channel-address.ts#L54-L105)]。`isJidForChannelAccount` 提供"这个 JID 是否属于指定账号"的谓词判断，是各处归属校验的基础工具 [Sources: [channel-address.ts](src/channel-address.ts#L99-L115)]。\n\n作用域注入发生在连接层的唯一入口：`IMManager.connectChannel` 接受 `accountId` 并派生出 `${channelType}\\0${accountId}` 形式的通道键，使同用户同类型的多账号可以并存连接 [Sources: [im-manager.ts](src/im-manager.ts#L267-L269)]。随后 `scopeConnectOpts` 用闭包包装所有回调——`normalizeIncomingJid`、`onNewChat`、`onMessage` 等——在事件进入业务层之前统一调用 `scopeChannelJid` 加上账号片段；同时每个回调都先过 `inboundAllowed()` 门控，实时校验用户未被封禁、账号仍启用且归属正确，不满足则静默拒收 [Sources: [im-manager.ts](src/im-manager.ts#L312-L346)]。这保证了**下游永远看到带账号作用域的 JID**，多账号区分不依赖任何调用方自觉。\n\n唯一的例外是遗留兼容：`reloadChannelAccountById` 在重连账号时以 `scopeIncomingJids: !account.is_legacy_default` 决定是否加作用域——历史遗留默认账号保持无片段 JID，使既有绑定与历史记录原样存活 [Sources: [index.ts](src/index.ts#L19178-L19188)]。\n\n## 群聊挂载：Workspace 绑定与 thread_map 路由模式\n\n"挂载"指把一个外部聊天持久绑定到一个执行目标。挂载服务提供三个纯函数构造更新：`buildWorkspaceMountUpdate` 写入 `target_main_jid` 并按传入模式设置 `binding_mode`（`thread_map` 或 `single_context`）；`buildSessionMountUpdate` 写入 `target_agent_id` 且清空 `target_main_jid`；`buildUnmountUpdate` 清空两者。三者都保留激活/受众等策略字段，且 session 更新与 workspace 更新天然互斥 [Sources: [channel-mount-service.ts](src/channel-mount-service.ts#L469-L515)]。\n\n| 构造函数 | 写入字段 | 清除字段 | 典型用途 |\n|---|---|---|---|\n| `buildWorkspaceMountUpdate` | `target_main_jid`、`binding_mode` | `target_agent_id` | 群聊挂到 Workspace |\n| `buildSessionMountUpdate` | `target_agent_id` | `target_main_jid` | 私聊绑到固定 Session |\n| `buildUnmountUpdate` | （无） | `target_*_id/main_jid` | 软解绑 |\n\n所有写入必须经 `commitChannelMountUpdate` 提交：它在单个数据库事务里同时更新遗留路由列与 `channel_mounts` 归一化镜像，并同步内存路由缓存、清理发送失败计数，避免双写漂移 [Sources: [channel-mount-service.ts](src/channel-mount-service.ts#L535-L545)]。\n\n**路由模式的判定**来自 `isNativeContextContainer`：飞书话题群（`chat_mode === \'topic\'` 或消息形态为 `thread`）、Telegram Forum 等容器会在持久化元数据与实时元数据合并后被判为"原生上下文容器"，从而选择 `thread_map` 模式——一个容器内的每个话题映射为独立 Session；普通群则走 `single_session` 固定到 Workspace 主会话 [Sources: [channel-mount-service.ts](src/channel-mount-service.ts#L125-L161)]。当一个新检测到的原生容器首次收到消息前，`upgradeNativeContextChannelMount` 会把它升级为 thread_map 绑定；若它已被固定绑定到某个单一 Session（`bound_to_session`）或目标 Workspace 已消失，升级失败并返回冲突——注释明确说明"绑定到固定会话的原生容器是非法状态"，此时消息被丢弃（fail-closed）而不是错误路由 [Sources: [channel-mount-service.ts](src/channel-mount-service.ts#L300-L334)、[index.ts](src/index.ts#L18551-L18570)]。\n\n运行时解析由 `resolveChannelMountTarget` 完成：session 型挂载先校验 Session 存在再回溯其宿主 Workspace（不一致时报告 `workspaceMismatch` 并以 Session 宿主为准）；workspace 型直接查表。任何一环缺失返回 `stale`，调用方据此拒绝路由而非降级猜测 [Sources: [channel-mount-service.ts](src/channel-mount-service.ts#L412-L467)]。`resolveEffectiveChatJid` 是消息主路径上的消费者，其优先级依次为：mount 镜像 → `target_agent_id` 显式绑定 → thread_map 原生线程 → `target_main_jid` 主会话绑定；全部落空则返回 null，消息不入队 [Sources: [index.ts](src/index.ts#L18635-L18745)]。\n\n## 绑定策略门控：群聊绑 Workspace、私聊绑 Session\n\n绑定方向不是约定而是硬性策略。`resolveChannelConversationKind` 对七种提供商做中立分类：QQ 以 `c2c:`/`group:` 前缀、钉钉/Discord 以 `c2c:`/`dm:` 前缀、WhatsApp 以 `@s.whatsapp.net`/`@g.us` 后缀、Telegram 以 ID 正负号区分私聊与群聊；微信当前连接器仅支持 P2P。关键设计是**未知值刻意 fail-closed**——尤其飞书对 P2P 和群聊使用同样的不透明 `oc_*` 标识符，因此仅凭 JID 绝不允许猜测，必须依赖持久化或实时的 chat_mode 元数据 [Sources: [channel-conversation-kind.ts](src/channel-conversation-kind.ts#L12-L73)]。\n\n分类结果经由 `conversationBindingPolicyError` 变成 API 层的 400 错误："Workspace bindings only accept group chats" / "Session bindings only accept direct chats"。REST 绑定端点在两处消费它：session 绑定分支和 workspace 绑定分支各自先取实时聊天信息、再对 await 之后重新读取的最新行计算分类——因为一次实时网络调用会让出事件循环，期间并发请求可能改变绑定或话题能力，用旧快照计算会把已变成话题容器的聊天错绑成固定单会话 [Sources: [channel-conversation-kind.ts](src/channel-conversation-kind.ts#L76-L91)、[routes/config.ts](src/routes/config.ts#L4425-L4448)、[routes/config.ts](src/routes/config.ts#L4535-L4557)]。\n\n账号一致性同样被强制：`hasConsistentChannelAccount` 要求 JID 中编码的账号片段与记录中存储的 `channel_account_id` 完全一致；唯一豁免是"无片段 + 记录指向当前用户的遗留默认账号"的组合，否则返回 400 [Sources: [routes/config.ts](src/routes/config.ts#L1696-L1714)]。此外，原生上下文容器禁止 session 绑定（只能绑 Workspace）[Sources: [routes/config.ts](src/routes/config.ts#L4425-L4433)]，冲突时若不带 `force=true` 返回 409 "IM group is already bound elsewhere" [Sources: [routes/config.ts](src/routes/config.ts#L4442-L4448)]。\n\n```mermaid\nflowchart TD\n    M["入站消息 source_jid"] --> ADM{"evaluateChannelAdmission"}\n    ADM -- "/pair 命令" --> PAIR{"verifyPairingCode<br/>+ 匹配账号归属"}\n    PAIR -- 成功 --> REG["注册聊天 + 回退默认工作区<br/>+ claimOwner(trusted_direct)"]\n    PAIR -- 失败 --> DROP1["pair_rejected：命令被消费，不进 Agent"]\n    ADM -- 未授权 --> DROP2["deny"]\n    ADM -- 已授权 --> RT["resolveEffectiveChatJid"]\n    RT -- "mount.session_id" --> S1["虚拟 JID ws#agent:sess"]\n    RT -- "thread_map + 原生线程" --> TH["resolveOrCreateNativeThreadAgent<br/>按 thread 建/复用 Session"]\n    RT -- "target_main_jid" --> WS["Workspace 主会话"]\n    RT -- 无绑定/stale --> NULL["null：不入队"]\n```\n\n这张图的准入半部分源自统一的准入契约：`evaluateChannelAdmission` 先拦截 `/pair <code>` 命令（成功返回 `paired`，失败返回 `pair_rejected`，两种情况下配对码都不会到达 Agent），再对未授权聊天返回 `deny`；没有配置授权回调的老式连接器保留旧的开放行为 [Sources: [channel-admission.ts](src/channel-admission.ts#L103-L132)]。\n\n## 私聊准入与配对：/pair 如何建立归属\n\n私聊没有"群管理员拉机器人进群"这样的显式安装动作，因此 Miniclaw 用一次性配对码建立归属。用户在 Web 端为某账号生成配对码（支持 telegram、qq、wechat、dingtalk、discord、whatsapp 六种提供商；飞书走独立的 openId 自动认领），然后在私聊里发送 `/pair <code>` [Sources: [routes/channel-accounts.ts](src/routes/channel-accounts.ts#L1024-L1060)]。\n\n配对回调 `buildOnPairAttempt` 执行五重校验：码有效、码的用户匹配当前连接用户、码内账号与连接账号一致（除非显式允许遗留无作用域码）、账号存在且归属正确、以及 `matchesChannelPairTarget` 确认目标聊天的账号片段/既有归属与本次配对兼容 [Sources: [index.ts](src/index.ts#L18243-L18273)、[channel-admission.ts](src/channel-admission.ts#L59-L72)]。全部通过后做三件事：以用户主工作区为文件夹自动注册聊天；若聊天尚无显式绑定则写入回退目标（账号默认工作区或主工作区）；最后从结构性私聊 JID 中恢复 provider 原生 owner id 并 `claimOwner(...,\'trusted_direct\')`——配对码认证的是对话本身，因此足以确立强来源的归属证明 [Sources: [index.ts](src/index.ts#L18274-L18305)、[agent-builder-turn-auth.ts](src/agent-builder-turn-auth.ts#L105-L135)]。\n\n授权判定侧的 `matchesChannelAccountAuthorization` 是归属边界的浓缩表达：群组必须属于该用户；期望特定账号时，记录的 `channel_account_id` 必须等于该账号，且 JID 要么编码了相同账号、要么显式允许遗留无片段格式（仅限遗留默认账号）；反向地，无账号期望的兼容连接器绝不接受带账号片段的 JID，防止历史行被任意一等 Bot 冒名 [Sources: [channel-admission.ts](src/channel-admission.ts#L33-L57)、[index.ts](src/index.ts#L18180-L18220)]。契约测试将"Telegram/QQ 旧路由把带作用域聊天严格限制在遗留默认账号"固化为源码级断言 [Sources: [legacy-paired-chat-isolation-contract.test.ts](tests/legacy-paired-chat-isolation-contract.test.ts#L6-L16)]。\n\n## 渠道身份归属：粘性 Owner、所有权学习与凭证转移\n\n一旦聊天进入某个逻辑会话，**传输所有权就是粘性的**。`resolveStickyChannelOwner` 的语义是：已有 IM 所有者时，只有"同一会话粒度对话（provider + 外部聊天 + 账号，不含 thread/root）内的新消息"才能移动回复锚点——第一条入站消息恰好带 `root_id` 不能让会话永远锁死在那个话题里；但换对话、换账号、换提供商都无法夺走所有权。Web 注入可以往 IM 所属会话里写内容，却永远不能改变传输归属 [Sources: [channel-session-owner.ts](src/channel-session-owner.ts#L4-L36)]。持久化侧配套 `setSessionChannelOwnerOnce`，用 `INSERT OR IGNORE` 保证"先到先得"，后来的 Web 或兄弟 IM 输入无法偷走所有权 [Sources: [db.ts](src/db.ts#L7764-L7782)]。消息主流程在批量处理积压消息时用它决定回复去向，并额外保证"混合了 Web 来源的批次绝不向 IM 广播"（issue #99 的回归防护）[Sources: [index.ts](src/index.ts#L5650-L5687)]。上述语义均有针对性测试锁定，包括"不同聊天或同提供商下另一账号不能夺取所有权" [Sources: [channel-session-owner.test.ts](tests/channel-session-owner.test.ts#L9-L36)]。\n\n**聊天级的归属学习**发生在自动注册路径 `buildOnNewChat`。同名聊天已存在时分四种情形：属主相同则仅同步名称；已有显式绑定则绝不覆盖；`created_by` 缺失则回填属主但保留既有文件夹（迁移遗留数据）；否则进入最关键的判别——**凭证转移 vs 多 Bot 共存**。判据是前任属主在同类型渠道上是否仍有活跃连接：仍有（比如他的 Telegram 还在线，只是飞书应用被转给了别人）则视为多 Bot 场景，跳过改属；否则视为凭证转移，聊天被改属到新用户的主目录文件夹，并且信任被主动隔离——`owner_claim_source` 置为 `transfer_reset`、白名单清空、受众收紧为 owner_only，因为"信任属于前一任 Miniclaw 用户的渠道绑定，而不是仅仅属于 provider 聊天 ID" [Sources: [index.ts](src/index.ts#L17756-L17871)]。\n\n`owner_claim_source` 这个溯源字段是特权工作流（如 Agent Builder）的信任根基，五个取值构成明确的信任阶梯：\n\n| 取值 | 含义 | 信任强度 |\n|---|---|---|\n| `configured` | 用户在配置中显式指定 | 强 |\n| `trusted_direct` | 配对码认证的私聊 | 强 |\n| `explicit` | 用户显式认领命令 | 强 |\n| `auto_feishu` | 飞书 openId 自动匹配 | 弱（自动） |\n| `transfer_reset` | 凭证转移后隔离占位 | 不可信 |\n\n弱来源与转移隔离都不能授权 Builder；而且普通消息只能把"无主"的已准入私聊升级为 `trusted_direct`，任何已有属主或已有来源的记录都不允许借此洗白 [Sources: [types.ts](src/types.ts#L195-L201)、[agent-builder-turn-auth.ts](src/agent-builder-turn-auth.ts#L78-L100)]。飞书有一条专属的认领路径：首个合法私聊发送者永久认领该 Bot 的 ownerOpenId 并写回密钥存储，后来者无法替换 [Sources: [index.ts](src/index.ts#L19212-L19231)]。\n\n## 默认工作区回退与账号生命周期边界\n\n多账号体系需要一个确定性的兜底问题答案："这个账号的消息在没有任何显式绑定时应该去哪？" `resolveChannelAccountFallbackWorkspace` 给出两级回退：优先使用账号自己的 `default_workspace_jid`，但前提是该 Workspace 存在且 `created_by` 就是账号属主——注释特别警告"已废弃的 Agent 默认值绝不能选中该 Agent 任意的第一个工作区"；否则回退到用户的主工作区（home）[Sources: [channel-account-routing.ts](src/channel-account-routing.ts#L3-L28)]。\n\n回退的写入时机经过精确控制。`applyChannelAccountRegistrationFallback` 只在"聊天尚无显式绑定"时补写账号归属与回退目标，且输入对象无需变更时原样返回——因为每条入站消息都会流经此路径，无条件写库意味着每条消息约 10 条语句的纯粹写放大 [Sources: [channel-account-routing.ts](src/channel-account-routing.ts#L30-L56)、[index.ts](src/index.ts#L19163-L19177)]。与之呼应的是解绑语义：`/unbind` 不是清空路由，而是恢复到账号默认工作区——`restoreDefaultChannelMount` 先解析后提交，恢复失败时聊天保持原绑定，绝不会短暂处于"无绑定黑洞"；健康修复路径复用同一机制，恢复失败则保留孤儿绑定等待下次修复 [Sources: [index.ts](src/index.ts#L2007-L2037)、[channel-unbind-default-contract.test.ts](tests/channel-unbind-default-contract.test.ts#L6-L20)]。Workspace 删除场景同理：`resolveDefaultChannelMountForWorkspaceDeletion` 允许把所有受影响渠道的重定向准备好后，与删除操作放进同一个数据库事务，并把正在消失的 Workspace 排除出回退候选 [Sources: [channel-mount-service.ts](src/channel-mount-service.ts#L275-L298)]。\n\n从单账号到多账号的迁移由 `ensureLegacyDefaultChannelAccount` 完成：它把历史凭据包装成一个标记 `is_legacy_default` 的账号，并回填所有"无账号归属的无片段 JID"聊天记录指向自己，使旧绑定与历史完整存活；旧配置路由 `/api/config/user-im/:provider` 经 `syncDefaultChannelAccountCredentials` 映射到新模型 [Sources: [channel-account-migration.ts](src/channel-account-migration.ts#L21-L98)]。生命周期出口同样设防：DELETE 账号时若有任何绑定（`countChannelAccountBindings > 0`）返回 409 要求先解绑；遗留默认账号永久不可删除，只能停用或清除授权，因为它承载着无片段历史 JID 的归属 [Sources: [routes/channel-accounts.ts](src/routes/channel-accounts.ts#L1229-L1266)]。每个账号的凭据以 AES-256-GCM 加密存于独立文件、引用计数式管理，创建失败时同步回收密文，避免留下不可解密的孤儿密钥 [Sources: [channel-account-secrets.ts](src/channel-account-secrets.ts#L60-L93)、[channel-account-migration.ts](src/channel-account-migration.ts#L51-L58)]。\n\n## 设计不变量速查\n\n| # | 不变量 | 强制点 |\n|---|---|---|\n| 1 | 下游看到的 IM JID 一律带账号作用域（遗留账号除外） | `scopeConnectOpts` 回调包装 |\n| 2 | 群聊只能绑 Workspace，私聊只能绑 Session，未知 fail-closed | `conversationBindingPolicyError` |\n| 3 | `target_agent_id` 与 `target_main_jid` 互斥，写入必经 `commitChannelMountUpdate` 双写事务 | mount 构造函数族 |\n| 4 | 原生话题容器禁止固定单 Session 绑定，升级冲突即丢消息 | `upgradeNativeContextChannelMount` |\n| 5 | 会话传输归属粘滞：跨对话/账号/提供商不可夺，Web 不可夺 | `resolveStickyChannelOwner` + persist-once |\n| 6 | 弱来源归属不得授权特权工作流；凭证转移即隔离 | `owner_claim_source` 溯源检查 |\n| 7 | 解绑 = 恢复账号默认，而非清空路由；恢复失败保持原状 | `restoreDefaultChannelMount` |\n| 8 | 有绑定的账号不可删；遗留默认账号不可删 | DELETE 路由守卫 |\n\n掌握这三层模型与八条不变量后，你可以继续深入相邻主题：消息进入会话后的串行执行见 [会话串行队列：Runner 生命周期、重试退避与卡死恢复](14-hui-hua-chuan-xing-dui-lie-runner-sheng-ming-zhou-qi-zhong-shi-tui-bi-yu-qia-si-hui-fu)，投递侧的持久化状态机见 [可靠性状态机：Inbox、Turn、Outbox 与 Streaming Card 的持久投递](17-ke-kao-xing-zhuang-tai-ji-inbox-turn-outbox-yu-streaming-card-de-chi-jiu-tou-di)，而 `owner_im_id`/`audience_mode` 在命令系统中的门控应用见 [IM 命令系统：只读、变更与 Owner 门控命令](18-im-ming-ling-xi-tong-zhi-du-bian-geng-yu-owner-men-kong-ming-ling)。';export{n as default};

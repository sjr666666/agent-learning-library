const n='Miniclaw 允许 LLM Agent 在 Docker 容器内执行任意代码，这使容器边界成为整个系统最关键的安全防线。本页从三条正交的防御轴剖析这套安全体系：**身份边界**（容器内进程永远以非 root 的 node 用户运行，且通过主机身份探测决定挂载文件的所有权策略）、**文件系统边界**（主机目录挂载必须同时通过部署侧白名单与运行前严格校验）、**密钥边界**（凭据以 AES-256-GCM 加密落盘，通过一次性 env 文件注入，且从不进入环境变量或日志）。所有机制共享同一个设计原则——**fail-closed**：任何无法验证的状态都拒绝启动或拒绝放行，而不是猜测一个宽松的默认值。\n\n## 安全边界总览：三层防线如何协同\n\n理解这套体系的前提是分清三个边界各自拦截的威胁。身份边界解决"容器内进程是谁"的问题——Agent 进程以 node 用户运行，即使被完全攻陷也无法篡改镜像内的系统工具；文件系统边界解决"容器能看到主机的哪些目录"的问题——白名单是部署方拥有的策略文件，而非用户可配置项；密钥边界解决"凭据在何处存在、以什么形态暴露"的问题。三者由同一次 `docker run` 串联起来：\n\n```mermaid\nflowchart LR\n    subgraph Host["主机侧（宿主权限）"]\n        CR["container-runner.ts<br/>buildVolumeMounts + buildContainerArgs"]\n        MS["mount-security.ts<br/>白名单校验"]\n        SEC["channel-account-secrets.ts<br/>AES-256-GCM 密文"]\n    end\n    subgraph Container["容器内"]\n        EP["entrypoint.sh（root 引导）"]\n        ID["session-permissions.sh<br/>身份模式判定"]\n        W["permission watcher<br/>O_NOFOLLOW 描述符遍历"]\n        NODE["node 用户<br/>runuser/setpriv 降权"]\n    end\n    CR -->|"探测 docker info SecurityOptions"| ID\n    CR -->|"validateMount 双重校验"| MS\n    EP --> ID --> W --> NODE\n    SEC -.->|"0600 env 文件<br/>只读挂载 /workspace/env-dir"| NODE\n```\n\n主机侧负责决策（谁可以挂载什么、密钥如何加密），容器侧负责执行（降权、修复挂载所有权、以最小权限启动浏览器与 Runner）。两侧之间唯一的信任传递物是 stdin 中的 JSON 输入与只读的 env 文件，其余一切都要重新验证。\n\nSources: [mount-security.ts](src/mount-security.ts#L1-L8), [entrypoint.sh](container/entrypoint.sh#L1-L10), [Dockerfile](container/Dockerfile#L256-L261)\n\n## 非 root 执行模型：root 引导与 node 运行的特权降级链\n\n镜像基于 `node:24-slim` 构建，但**刻意不设置 `USER node`**——Dockerfile 尾部的注释明确写着 "Do NOT add USER node here — entrypoint handles the privilege drop"。原因是 bind mount 的所有权修复需要 root 权限：entrypoint 以 root 启动，完成卷权限归一化后，再通过 `setpriv --reuid=node --regid=node --init-groups` 启动受管的 Chromium，最终用 `runuser -u node -- node /tmp/dist/pi-index.js` 执行真正的 Agent Runner。root 只存在于两个时刻之间的引导窗口，业务代码从未以 root 身份运行过一条指令。\n\n这个窗口本身也被压缩到最小攻击面。入口脚本第一行可执行代码就是 `umask 0077`——默认创建的任何文件对 group/other 完全不可见；随后加载的身份助手脚本强调 "This root-owned helper accepts no runtime-configurable path"，它从镜像内固定路径 `/app/session-permissions.sh` 加载，不接受任何来自运行时环境的路径覆盖，防止宿主通过注入环境变量劫持 root 阶段的执行流。\n\n镜像构建期还固化了一层不可变资产保护：编译完成后，prompt 资产被显式 `chown root:root` 并统一压到 `0555`/`0444` 权限，注释说明这是为了让 prompt 资产不依赖 CI 或本地构建时的 checkout umask——即使 Agent 在容器内获得任意代码执行，也无法改写驱动其行为的身份提示词。\n\n| 阶段 | 执行者 | 关键动作 | 权限特征 |\n|---|---|---|---|\n| 镜像构建 | root | 固化 prompt 资产为 `0444/0555` | 运行期不可变 |\n| entrypoint 引导 | root | umask 0077、身份判定、挂载归一化 | 仅限固定路径助手 |\n| Chromium 启动 | node | `setpriv --reuid=node` 显式降权 | CDP 仅绑定 127.0.0.1 |\n| Runner 执行 | node | `runuser -u node` 最终降权 | umask 0007（rootless） |\n\nSources: [Dockerfile](container/Dockerfile#L202-L219), [Dockerfile](container/Dockerfile#L256-L261), [entrypoint.sh](container/entrypoint.sh#L1-L10), [entrypoint.sh](container/entrypoint.sh#L125-L136), [entrypoint.sh](container/entrypoint.sh#L158-L163)\n\n## 主机身份探测：五种模式与 fail-closed 语义\n\nbind mount 把宿主的 uid/gid 原样带入容器，但不同 Docker 部署形态会重映射这些数字。如果容器内 node 用户的 uid 与挂载目录的所有者不一致，Agent 写文件会产生 root 拥有的"幽灵文件"，宿主后端反而读不了。Miniclaw 的解法是每次启动都探测守护进程的安全属性，将部署归类为五种身份模式之一：\n\n| 模式 | 判定条件 | 容器内处理 |\n|---|---|---|\n| `virtualized` | macOS / Windows 宿主 | 保持 node 属主语义（Docker Desktop 虚拟化 bind 所有权），绝不授予 group/other 权限 |\n| `rootless` | SecurityOptions 含 rootless | 容器 uid 0 映射到守护进程 uid，先验证 bind 根的所有权再使用 |\n| `userns` | SecurityOptions 含 userns | **拒绝启动**：rootful userns-remap 无安全的身份桥 |\n| `host-root` | Linux 且宿主 uid 为 0 | 容器内仍保持非 root，仅归一化显式授权的可写挂载 |\n| `direct` | Linux 且 uid 为已验证的正整数 | 将容器内 node 账号的 uid/gid 改写为主机数值 |\n\n判定逻辑的关键在于**未知即拒绝**：安全选项探测失败返回 null 时归入 `unknown`，身份助手直接 fatal 退出（"refusing to start"），而不是退回某种宽松行为。探测结果也**不做缓存**——注释解释 Docker context 和守护进程安全选项可能在服务运行期间变化，复用早前的 `direct` 结果可能绕过 rootless/userns 的 fail-closed 处理，因此每次 `docker run` 前都重新探测。\n\n`direct` 模式下的 uid 改写同样设防：目标 uid 必须是通过 `miniclaw_valid_nonroot_id` 校验的非零正整数；若主机 uid 与镜像内已有账号冲突则拒绝启动；gid 冲突时选择保留原 gid 而非强行授予一个影子组。rootless 模式还有一道桥接验证——逐一 `stat` 五个固定 bind 根（`.claude`、`.feishu-cli`、`group`、`ipc`、`extra`），发现任何既不是 0 也不是遗留 node uid 的属主就中止启动。\n\nSources: [container-runner.ts](src/container-runner.ts#L1539-L1575), [container-runner.ts](src/container-runner.ts#L1598-L1613), [session-permissions.sh](container/session-permissions.sh#L51-L141), [session-permissions.sh](container/session-permissions.sh#L225-L240)\n\n## 挂载白名单：部署侧策略与运行前双重校验\n\n主机目录挂载是唯一能把宿主任意目录暴露给 Agent 的功能，因此它的授权模型被设计成**配置 ≠ 授权**：工作区创建时校验一次，紧接 `docker run` 之前再校验一次，持久化的配置永远不会被当作持久的授权凭证。策略文件 `config/mount-allowlist.json` 由部署方持有，包含三个字段——`allowedRoots`（允许挂载的主机根及其读写许可）、`blockedPatterns`（路径组件黑名单）、`nonMainReadOnly`（非主工作区强制只读）。\n\n校验管线的每一层都是独立的否决点：\n\n```mermaid\nflowchart TD\n    A["parseContainerConfig<br/>不受信 JSON 形状校验"] --> B["路径规范化<br/>normalizeAdditionalMountContainerPath"]\n    B --> C["绝对路径 + 存在性 + 目录类型"]\n    C --> D{"hardDeniedHostPath<br/>保护路径硬否决"}\n    D -->|命中| X1["拒绝"]\n    D -->|未命中| E{"blockedPatterns<br/>组件级黑名单"}\n    E -->|命中| X2["拒绝"]\n    E -->|未命中| F{"findAllowedRoot<br/>最长匹配允许根"}\n    F -->|无匹配| X3["拒绝"]\n    F -->|匹配| G{"nonMainReadOnly +<br/>allowReadWrite 双重检查"}\n    G -->|通过| OK["放行并记录 effectiveReadonly"]\n```\n\n黑名单采取"默认叠加"而非"默认替换"语义：用户配置的 `blockedPatterns` 会与代码内置的 `DEFAULT_BLOCKED_PATTERNS` 合集去重后生效。内置清单覆盖 `.ssh`、`.gnupg`、`.aws`、`.kube`、`.docker`、`credentials`、`.env`、`id_rsa` 等 18 类敏感组件——这意味着策略文件的作者**没有能力**移除基础防护，只能追加。策略加载同样是 fail-closed：文件缺失、JSON 损坏或形状非法都会让 `loadMountAllowlist()` 返回 null 并记日志 "host directory mounts are blocked"；同时基于 `(dev, ino, mtime, size, sha256)` 签名的热重载让收紧后的策略无需重启即可生效。\n\n硬否决层独立于白名单之上：`/proc`、`/sys`、`/dev`、`DATA_DIR`、项目 `.env`/`.git`、以及宿主家目录下的全部凭据目录构成保护清单，任何与之重叠的真实路径一律拒绝；此外正则 `(docker|podman|containerd|cri-dockerd)*.sock` 会在路径组件级别拦截容器运行时套接字——把 `/var/run/docker.sock` 挂进容器等于交出宿主 root，这条规则确保即使白名单误配也无法触达。白名单根的选择采用**最长前缀优先**，防止一个宽泛的可写根仅仅因为排在前面就覆盖嵌套的只读根。\n\n批量校验接口 `validateAdditionalMountsStrict` 是原子性的：任一条目非法就整体抛出 `AdditionalMountValidationError`，调用方绝不能部分应用输出；条目间还会做容器路径重叠检测。最后还有一层与 ACL 联动的活体特权门控——只要工作区配置了额外挂载，就在挂载构建时实时查库确认 owner 当前仍是 active admin，注释明确写着 "Host execution is a live privilege, not a property inherited forever from a workspace row"。\n\nSources: [mount-allowlist.json](config/mount-allowlist.json#L1-L12), [mount-security.ts](src/mount-security.ts#L23-L45), [mount-security.ts](src/mount-security.ts#L120-L175), [mount-security.ts](src/mount-security.ts#L247-L287), [mount-security.ts](src/mount-security.ts#L232-L245), [mount-security.ts](src/mount-security.ts#L523-L562), [mount-security.ts](src/mount-security.ts#L593-L671), [host-execution-policy.ts](src/host-execution-policy.ts#L13-L26), [container-runner.ts](src/container-runner.ts#L1091-L1116)\n\n## 容器路径规范化与符号链接防护\n\n主机侧路径有真实文件系统的 realpath 兜底，容器侧路径则完全是用户输入的字符串，需要独立的语法防线。`normalizeAdditionalMountContainerPath` 要求挂载目标是相对后缀（禁止绝对路径、前后斜杠、反斜杠与冒号——后者会破坏 Docker `-v` 语法），逐组件拒绝空段、`.`、`..` 以及保留组件 `.npm-global`（npm 全局目录的持久化位置，不允许被额外挂载遮蔽），最终所有合法路径都被收敛到固定根 `/workspace/extra/<suffix>` 之下。\n\n文件系统层面的对抗则围绕 symlink TOCTOU 展开。容器内的权限 watcher 和生成目录管理器从不按名字打开不可信路径，而是先用 `fstatSync` 记录固定根的 `(device, mnt_id)` 边界，再通过 `/proc/self/fd/<parent>/<name>` 模拟 openat 语义逐层下钻，打开标志始终携带 `O_NOFOLLOW`——最后一个组件若是符号链接直接失败。每打开一级目录就用 `assertBoundary` 复核设备号与挂载点 id，跨越嵌套挂载边界立即抛错。这意味着即使 Agent 在挂载卷里预埋了一条指向宿主的符号链接，watcher 的遍历也会在该链接处终止而非跟随。\n\nSources: [mount-security.ts](src/mount-security.ts#L303-L348), [mount-security.ts](src/mount-security.ts#L47-L50), [session-generated-paths.mjs](container/session-generated-paths.mjs#L19-L54), [session-permissions-watcher.mjs](container/session-permissions-watcher.mjs#L180-L190), [session-permissions-watcher.mjs](container/session-permissions-watcher.mjs#L56-L109)\n\n## 密钥加密边界：AES-256-GCM、env 文件隔离与 stdin-only 秘密\n\n密钥边界的核心问题是：凭据在静态存储、传输注入、运行时三个阶段各以什么形态存在。Miniclaw 给出的答案是三种不同的载体，每种都选择了当时威胁模型下最小的暴露面。\n\n**静态存储：加密落盘 + 原子发布。** 渠道账号凭据保存在 `data/config/channel-accounts/` 下，以 AES-256-GCM 加密（随机 12 字节 IV，认证标签随文件存储，解密前强制 `setAuthTag` 校验完整性）。主密钥是 32 字节随机数，存于 0700 目录内的 0600 文件中；首次生成采用 `writeFileSync(flag: \'wx\')` 写临时文件后 `link(2)` 原子发布——注释解释了为何不用 rename：link 是 no-replace 操作，不会覆盖另一个 Miniclaw 进程竞速创建的密钥，EEXIST 时读取既有文件即可。密钥引用本身也经过 `^channel-account:[a-zA-Z0-9_-]+$` 白名单校验后才拼接文件路径，杜绝引用字段携带 `../` 逃逸密钥目录。\n\n**注入通道：一次性 env 文件而非 `-e` 变量。** 容器启动所需的 provider 凭据写入 per-runtime 隔离的 env 文件（mode 0600），以只读方式挂载到 `/workspace/env-dir`。选择文件而非 `docker run -e` 的理由写在源码注释里："keeps credentials out of process listings"——环境变量会出现在 `/proc/<pid>/environ` 和各种进程列表工具中，文件不会。env 目录路径由 `getContainerRuntimeEnvDir` 生成，工作区名、agent id、task run id、渠道账号 id 四个维度都必须通过 `[A-Za-z0-9_-]{1,128}` 段校验才能参与拼路径，保证两个 Bot 即使路由到同一工作区也不会互相覆盖凭据；任务结束后 `cleanupContainerTaskRuntimeEnvDirs` 会跨所有 Bot 身份清理快照，不留陈旧凭据。\n\n容器内侧的对应防御是变量遮蔽：entrypoint 在 source 宿主生成的 env 时，把每一个 root 控制变量（`MINICLAW_HOST_IDENTITY_MODE`、`MINICLAW_HOST_UID` 等）声明为 local 局部变量再 source，注释直言这是为了 shadow "stale values persisted before the host-side denylist"——即使宿主数据库曾被注入恶意 env 值，它们也无法影响 root 阶段已完成的安全判定。\n\n**运行时秘密：stdin-only 的 HMAC 签名密钥。** Workspace Memory v2 的变更签名密钥走的是第三条路：它只出现在传给容器的 stdin JSON 引导载荷中，`ContainerInput` 的文档字符串用加粗级别的措辞约束——"Never expose this in environment variables, files, logs, prompts, or IPC"。三类载体互为补充：GCM 加密保护落盘数据，env 文件保护进程间注入，stdin 保护单次运行期的高价值秘密。\n\n**MCP 运行时秘密切面。** MCP server 定义可能携带 args、URL query、header 或 env 形态的 token。`ensureSettingsJson` 以 `replaceMcpServers: true` 全量重建每个工作区的 settings.json，注释说明这样被取消选择的 managed MCP 无法从上一次运行残留下来。成员工作区的解析结果是"只接收显式共享的系统 MCP"——契约测试断言成员的 settings 中不存在 `adminSecret`（admin-only 系统 server）、`privateOther`（其他租户私有 server）、`legacyArgs`/`legacyUrl`（未声明访问级别的遗留定义会被迁移为 admin_only），并对原始文件做全文断言：其中不含任何一个敏感 token 字符串。\n\nSources: [channel-account-secrets.ts](src/channel-account-secrets.ts#L16-L52), [channel-account-secrets.ts](src/channel-account-secrets.ts#L54-L68), [channel-account-secrets.ts](src/channel-account-secrets.ts#L76-L121), [container-runner.ts](src/container-runner.ts#L1346-L1404), [container-runner.ts](src/container-runner.ts#L366-L371), [container-runner.ts](src/container-runner.ts#L997-L1077), [session-permissions.sh](container/session-permissions.sh#L189-L215), [mcp-runtime-secret-boundary.test.ts](tests/mcp-runtime-secret-boundary.test.ts#L172-L181), [mcp-runtime-secret-boundary.test.ts](tests/mcp-runtime-secret-boundary.test.ts#L268-L338)\n\n## 纵深防御的测试锚点\n\n上述每条边界都有契约测试钉死行为，防止后续重构悄悄放宽语义。这些测试本身就是理解安全不变量的最佳说明书：\n\n| 威胁场景 | 测试锚点 | 断言的不变量 |\n|---|---|---|\n| 白名单损坏导致权限放大 | mount-security-strict：rejects an invalid allowlist | 非法策略被拒绝而非放宽访问 |\n| 读写请求静默降级 | rejects read-write instead of silently downgrading | 不允许的读写请求直接失败 |\n| 部分校验留下半生效状态 | is all-or-nothing when one of several mounts is invalid | 批量校验原子性 |\n| 目录删除/换链后复用旧授权 | revalidation fails after deletion / escaping symlink | docker run 前强制复验 |\n| 宿主伪装 rootless 绕过映射 | does not hide a rootless daemon behind a host-root client uid | 身份探测不被客户端 uid 干扰 |\n| 探测缓存导致模式漂移 | re-probes daemon security options for every launch | 每次启动重新探测 |\n| 伪造迁移标记保留危险权限 | fake legacy migration marker cannot preserve unsafe credentials | 迁移每次启动重扫、忽略标记 |\n| 陈旧 MCP 凭据残留 | managed MCP runtime secret boundary | settings.json 全量重建且全文无泄漏 token |\n| env 快照跨 Bot 串写 | isolates generated env paths by Bot identity and runtime | 四维路径互异 + 注入段校验 |\n\nSources: [mount-security-strict.test.ts](tests/mount-security-strict.test.ts#L87-L260), [container-session-permissions.test.ts](tests/container-session-permissions.test.ts#L57-L155), [container-session-permissions.test.ts](tests/container-session-permissions.test.ts#L224-L284), [mcp-runtime-secret-boundary.test.ts](tests/mcp-runtime-secret-boundary.test.ts#L223-L255)\n\n## 小结与延伸阅读\n\nMiniclaw 的容器安全不是单一开关，而是身份、文件系统、密钥三条轴上的 fail-closed 决策叠加：root 只存在于引导窗口且助手脚本不可劫持；身份模式未知即拒启；白名单默认黑名单不可削弱、保护路径硬否决、docker run 前必复验；密钥在存储、注入、运行时三个阶段分别选用 GCM 密文、0600 env 文件与 stdin 载荷，并以全量重建的 settings.json 保证 MCP 凭据不残留。若要理解这些能力在多租户场景下"谁能给哪个工作区配挂载"的上层授权逻辑，请继续阅读 [ACL 权限矩阵：层次化授权与无 admin 旁路的资源隔离](19-acl-quan-xian-ju-zhen-ceng-ci-hua-shou-quan-yu-wu-admin-pang-lu-de-zi-yuan-ge-chi)；若想了解容器与 Host 两种执行模式本身的容量与生命周期差异，请参见 [Host 与 Container 双执行模式：容量、超时与暖 Runner](11-host-yu-container-shuang-zhi-xing-mo-shi-rong-liang-chao-shi-yu-nuan-runner)。';export{n as default};

const e='Miniclaw 把所有有价值的运行时状态集中在一个 `data/` 目录里，并围绕它构建了一条以"宁可拒绝、不可损坏"为原则的备份/恢复管线。本页是这个运维主题的核心参考：先拆解 `data/` 目录的持久层与瞬态层边界，再逐步剖析 `make backup` 与 `make restore` 的防御性设计，最后汇总日常运维所需的全部 make 目标。理解本页的前提是已经了解服务的启动方式（见 [快速开始：从克隆到一键启动](2-kuai-su-kai-shi-cong-ke-long-dao-jian-qi-dong)）和 SQLite 持久化层的基本形态（见 [SQLite 持久化：Schema 版本化、迁移策略与核心表族](9-sqlite-chi-jiu-hua-schema-ban-ben-hua-qian-yi-ce-lue-yu-he-xin-biao-zu)）。\n\nSources: [Makefile](Makefile#L240-L288), [CLAUDE.md](CLAUDE.md#L218-L242)\n\n## 数据目录布局：持久层与瞬态层的边界\n\n所有运行时路径都从一个常量推导出来：`DATA_DIR` 固定为项目根下的 `data`，其下派生出数据库目录 `STORE_DIR = data/db` 和工作区根 `GROUPS_DIR = data/groups`。这个约定意味着"备份数据"等价于"备份一个目录"，是整条备份管线的物理基础。\n\nSources: [src/config.ts](src/config.ts#L33-L36)\n\n仓库文档给出了权威的目录清单。结合备份脚本中的组件定义，可以把这些子目录划分为三类：**持久层**（丢失即数据损失，必须进备份）、**瞬态层**（进程间通信与缓冲，重启即可再生）、**可再生层**（由持久数据派生，运行时会按需重建）。其中 `data/config/session-secret.key` 值得单独强调——它是 Web 登录 Cookie 的签名密钥，首次启动时生成并以 `0o600` 权限落盘；一旦丢失，所有用户的登录态都会失效。\n\nSources: [CLAUDE.md](CLAUDE.md#L218-L239), [scripts/backup-manifest.mjs](scripts/backup-manifest.mjs#L6-L18), [src/config.ts](src/config.ts#L70-L102)\n\n```mermaid\nflowchart LR\n    ROOT["data/"]\n    ROOT --> D1["db/<br/>messages.db 主库"]\n    ROOT --> D2["config/<br/>session-secret.key 等"]\n    ROOT --> D3["groups/·sessions/<br/>Workspace 与会话"]\n    ROOT --> D4["skills·mcp-servers·plugins<br/>memory·avatars·extra·builtin-skills"]\n    ROOT --> T1["ipc/·env/<br/>streaming-buffer/"]\n    ROOT --> R1["agent-profile-runtime/<br/>Skill 链接缓存"]\n```\n\n各目录的内容、生命周期与备份归属汇总如下：\n\n| 目录 | 内容 | 生命周期 | 进入备份 |\n| --- | --- | --- | --- |\n| `data/db/` | `messages.db` 主数据库（WAL 模式） | 持久 | ✅ 以在线快照形式 |\n| `data/config/` | Session 密钥、渠道账号密文等敏感配置 | 持久 | ✅ 整目录复制 |\n| `data/groups/{folder}/` | Workspace 文件树（`CLAUDE.md`、产出物等） | 持久 | ✅ |\n| `data/sessions/{folder}[/agents/{id}]/.claude/` | 会话上下文、conversation 记录 | 持久 | ✅（剔除可再生的内部链接） |\n| `data/skills/{userId}/` | 用户安装的 Skills | 持久 | ✅ |\n| `data/mcp-servers/{userId}/` | MCP Server 配置 | 持久 | ✅ |\n| `data/plugins/` | Plugin 目录、用户清单与 runtime 材料 | 持久 | ✅ |\n| `data/memory/{folder}/` | Workspace 记忆文件 | 持久 | ✅ |\n| `data/avatars/` | Agent 头像 | 持久 | ✅ |\n| `data/extra/{folder}/` | 附加挂载材料 | 持久 | ✅ |\n| `data/builtin-skills/` | 固定版本内置 Skills 的物化产物 | 持久（可重物化） | ✅ |\n| `data/ipc/{folder}/` | Runner IPC 输入/输出管道文件 | 瞬态 | ❌ |\n| `data/env/{folder}/` | 容器环境变量注入目录 | 瞬态 | ❌ |\n| `data/streaming-buffer/` | 流式卡片输出缓冲 | 瞬态 | ❌ |\n| `data/agent-profile-runtime/` | Profile 选定 Skills 的符号链接视图 | 可再生 | ❌（不在清单中） |\n\nSources: [scripts/backup-manifest.mjs](scripts/backup-manifest.mjs#L33-L46), [CLAUDE.md](CLAUDE.md#L218-L239)\n\n两个边界细节值得说明。其一，瞬态集合不是拍脑袋定的——`ipc`、`env`、`streaming-buffer`、`logs` 被显式写入备份清单的 `excludedTransientComponents` 字段，作为格式契约的一部分随备份一起归档，供恢复端理解语义。其二，`agent-profile-runtime` 是运行时按需物化的目录：当某个 Agent Profile 以精确清单模式启用 Skills 时，系统会在该目录下建立指向 `data/skills/{ownerId}` 中对应 Skill 的符号链接，并以 profile 版本号隔离；由于源文件都在持久层里，这层链接缓存无需备份。\n\nSources: [scripts/backup-manifest.mjs](scripts/backup-manifest.mjs#L40-L41), [src/container-runner.ts](src/container-runner.ts#L609-L665)\n\n## 备份管线：make backup 的七步流水线\n\n`make backup` 不是简单的 `tar data/`，而是一条带多重前置校验的流水线。它的核心设计目标是：**产出的每一个归档都必须保证可以成功恢复**——任何可能导致"打包成功但恢复必败"的状态（硬链接、逃逸符号链接、特殊文件）都会在打包前被拦截并直接失败。备份默认落在当前目录，可通过 `BACKUP_DIR=` 重定向；被备份的数据源通过 `RUNTIME_DATA_DIR=` 指定（默认 `data`），这两个变量正是自动化测试能在临时目录中驱动完整管线的关键。\n\nSources: [Makefile](Makefile#L17-L20), [Makefile](Makefile#L242-L287)\n\n```mermaid\nflowchart TD\n    A["make backup"] --> B["预检：RUNTIME_DATA_DIR 内<br/>是否存在硬链接文件"]\n    B -- 有 --> X1["❌ 拒绝备份"]\n    B -- 无 --> C["mktemp 暂存目录<br/>trap 保证异常时清理"]\n    C --> D["sqlite-snapshot.mjs<br/>在线快照 messages.db"]\n    D --> E["cp -a 复制 10 个持久组件目录"]\n    E --> F["prepare-backup-tree.mjs<br/>处置符号链接 / 检查特殊文件"]\n    F --> G{"暂存树复查：<br/>残留符号链接或特殊文件？"}\n    G -- 有 --> X2["❌ 拒绝备份"]\n    G -- 无 --> H{"仍有 nlink&gt;1 的文件？"}\n    H -- 有 --> X2\n    H -- 无 --> I["裁剪 groups/*/logs"]\n    I --> J["生成 backup-manifest.json（v2）"]\n    J --> K["tar -czf 写入临时文件"]\n    K --> L["原子 mv + chmod 600"]\n```\n\nSources: [Makefile](Makefile#L256-L287)\n\n### 第一步：在线 SQLite 快照而非文件复制\n\n数据库不能直接 `cp`：Miniclaw 的 SQLite 运行在 WAL 模式下，已提交的事务可能还停留在 `-wal` 文件里，直接复制主库文件会丢掉这部分数据。备份脚本改用 better-sqlite3 的在线备份 API——它从 SQLite 引擎自身读取事务一致的快照页，因此**无需停止运行中的 Miniclaw 进程**就能把 WAL 中已提交的行一并带入副本。快照完成后还会对副本执行 `quick_check` 完整性校验，任何一步失败都会连同 `-wal`/`-shm` 边车文件一起清理干净，绝不留下半成品。测试用例专门构造了"`wal_autocheckpoint = 0` 强制让第二行只存在于 WAL 中"的场景，验证恢复后的数据库确实包含两行数据。\n\nSources: [scripts/sqlite-snapshot.mjs](scripts/sqlite-snapshot.mjs#L35-L59), [tests/backup-restore-safety.test.ts](tests/backup-restore-safety.test.ts#L702-L732)\n\n### 第二步：符号链接与硬链接的分级处置\n\n这是整条管线中最精妙的部分。运行时数据里天然存在符号链接（会话目录里的 `.claude` 链接、Workspace 里的 `AGENTS.md → CLAUDE.md`），而 tar 对符号链接和硬链接的序列化方式会让归档变得不可靠——硬链接会被存成指向"首个见到的兄弟条目"的 link 型条目，而恢复端恰恰无条件拒绝 link 型条目，于是"打包成功、恢复必败"。`prepare-backup-tree.mjs` 因此把链接分为三级处理：\n\nSources: [scripts/prepare-backup-tree.mjs](scripts/prepare-backup-tree.mjs#L82-L92), [tests/backup-restore-safety.test.ts](tests/backup-restore-safety.test.ts#L126-L131)\n\n| 链接/文件类型 | 备份时处置 | 恢复时行为 |\n| --- | --- | --- |\n| `sessions/**/.claude` 内的链接 | 直接删除（运行时可重建），计数提示 | 不恢复 |\n| `groups/` 下指向数据树外部的链接 | 删除并计数提示（无法安全迁移） | 不恢复 |\n| 指向 `data` 树内部的相对链接 | 从暂存树摘除，记录进 `backup-symlinks.json` | 多重校验后原样重建 |\n| 绝对路径或逃逸出 `data` 的链接 | **抛错，终止备份** | — |\n| 特殊文件（FIFO、设备等） | **抛错，终止备份** | — |\n| 硬链接文件（nlink > 1） | **两道防线均拒绝**（预检 + 暂存树复查） | — |\n\nSources: [scripts/prepare-backup-tree.mjs](scripts/prepare-backup-tree.mjs#L33-L93), [Makefile](Makefile#L251-L277)\n\n分级背后的逻辑很清晰：能安全重建的链接删掉即可；能安全迁移的链接先摘下来记账、恢复时再挂回去；而无法判断安全性的链接一律宁可让备份失败，也不产出一个"看起来成功"的坏归档。摘下来的链接元数据写入 `backup-symlinks.json`（formatVersion 1，权限 0o600），成为归档的一部分。\n\nSources: [scripts/prepare-backup-tree.mjs](scripts/prepare-backup-tree.mjs#L96-L116)\n\n### 第三步：清单契约与原子打包\n\n流水线最后由 `backup-manifest.mjs` 生成 `backup-manifest.json`：它记录了 11 个受管组件的全集（`managedComponents`）、本次实际存在的子集（`presentComponents`）以及显式排除的瞬态项。这份清单是恢复端的"权威事实来源"——v2 格式的备份会按清单逐组件替换目标目录，即使某个组件在备份时不存在也会被当作"应删除"处理，从而保证恢复后的状态与备份时刻严格一致。最终 tar 打包先写到 `.tmp-` 临时文件再原子 `mv`，避免中断留下截断的归档，产物权限收紧为 `0o600`。\n\nSources: [scripts/backup-manifest.mjs](scripts/backup-manifest.mjs#L26-L46), [Makefile](Makefile#L282-L287)\n\n## 恢复管线：make restore 的纵深防御\n\n恢复比备份更危险——它要覆盖现有数据。`make restore` 因此构建了从 shell 层到 Node 层的多道防线：定位备份、确认服务离线、验证归档、互斥锁、暂存解压、完整性校验、逐组件换装带回滚。任何一道防线失败，现有数据都保持原状。\n\nSources: [Makefile](Makefile#L289-L325), [scripts/restore-backup.mjs](scripts/restore-backup.mjs#L456-L520)\n\n```mermaid\nflowchart TD\n    A["make restore"] --> B{"FILE= 指定，或目录内<br/>恰好一个 miniclaw-backup-*.tar.gz"}\n    B -- "多个/零个" --> X1["❌ 报错并列出候选"]\n    B -- 唯一 --> C["assert-port-free：<br/>TCP 探测 PORT，占用即拒绝"]\n    C --> D{"目标数据目录非空？"}\n    D -- 是 --> E["交互确认 y/N"]\n    E -- 非 y --> X2["取消"]\n    E -- y --> F["二次端口探测（收窄竞窗）+ 获取互斥锁"]\n    F --> G["tar -tzf / -tvzf 校验条目类型与路径"]\n    G --> H["解压到 .miniclaw-restore-* 暂存目录"]\n    H --> I["复核查验解压树 + 解析 manifest v2"]\n    I --> J["加固 config/sessions/db 权限"]\n    J --> K["校验 messages.db integrity_check<br/>并清除遗留 wal/shm"]\n    K --> L["按 backup-symlinks.json 重建内部链接"]\n    L --> M["逐组件 rename 换装，失败自动回滚"]\n    M --> N["清扫历史孤儿暂存目录"]\n    N --> O["释放锁 + 输出后续步骤提示"]\n```\n\nSources: [Makefile](Makefile#L289-L325), [scripts/restore-backup.mjs](scripts/restore-backup.mjs#L456-L520)\n\n### 前置防线：离线确认与归档校验\n\n第一道防线是**服务必须离线**。Makefile 先调用 `restore-backup.mjs assert-port-free` 做 TCP 探测（同时尝试 IPv4 与 IPv6，500ms 超时），端口被占即拒绝覆盖数据库；真正开始文件变更前还会再次探测，收窄"确认之后、动手之前服务又起来了"的竞态窗口。若目标 `data/` 已有内容，还需人工键入 `y` 确认覆盖。归档本身的校验同样严格：先读普通列表再读 verbose 列表，逐条目核对类型首字符——只接受常规文件（`-`）和目录（`d`），符号链接、设备、FIFO 一律拒绝；路径层面拒绝绝对路径、`..` 段以及一切不在 `data/` 前缀之内的条目，使恶意归档无法把写操作引向暂存目录之外。\n\nSources: [Makefile](Makefile#L307-L316), [scripts/restore-backup.mjs](scripts/restore-backup.mjs#L42-L66), [scripts/restore-backup.mjs](scripts/restore-backup.mjs#L87-L122)\n\n### 互斥锁：fail-closed 的并发保护\n\n两个并发恢复指向同一父目录时会互相删除对方的回滚数据，因此恢复全程持有一把 PID 戳记的排他锁文件 `.miniclaw-restore.lock`（`wx` 标志创建）。关键决策在于**陈旧锁永不自动回收**：PID 存活检测只是一个时间点快照，可移植的 Node 文件 API 又提供不了"比较后删除"的原子操作，自动接管无法做到无竞态。于是锁存在且持有者疑似死亡时，恢复同样失败退出，要求运维者人工核实后手动删除锁文件——破坏性操作宁可 fail closed。配套测试覆盖了"第二个恢复必须失败""暂存目录创建失败也要放锁""两个进程观察到同一把陈旧锁时双双失败"等边角场景。\n\nSources: [scripts/restore-backup.mjs](scripts/restore-backup.mjs#L400-L444), [tests/backup-restore-safety.test.ts](tests/backup-restore-safety.test.ts#L374-L444), [tests/backup-restore-safety.test.ts](tests/backup-restore-safety.test.ts#L513-L583)\n\n### 核心事务：暂存、校验与带回滚的换装\n\n通过前置防线的归档被解压到父目录下随机命名的 `.miniclaw-restore-*` 暂存目录（使用 `--no-same-owner --no-same-permissions` 剥离归属信息），随后依次执行：解压树复查（不允许出现任何符号链接或特殊文件）、manifest 解析（v2 为权威清单；旧格式备份按"非权威的部分备份"兼容处理，只替换实际存在的组件）、敏感目录权限加固（`config`/`sessions`/`db` 收紧到目录 `700`、文件 `600`）、数据库强校验（必须存在 `messages.db`，删除遗留 `-wal`/`-shm` 边车后跑 `integrity_check`，校验本身可能再生的空边车也一并清除）。备份时记录的内部相对符号链接在此阶段经多重校验后重建：路径不得包含 `\\` 或 `..` 段、首段必须是受管组件、不得嵌套于另一链接之下、目标解析后不得逃逸出数据目录、不得与归档条目冲突，且总数上限 10 万条。\n\nSources: [scripts/restore-backup.mjs](scripts/restore-backup.mjs#L124-L171), [scripts/restore-backup.mjs](scripts/restore-backup.mjs#L173-L250), [scripts/restore-backup.mjs](scripts/restore-backup.mjs#L492-L507)\n\n真正的落地动作是 `replaceComponents` 的逐组件换装：每个受管组件先把现有目录 `rename` 到暂存的 `rollback/` 里，再把新目录 `rename` 进位；中途任何一步失败，已完成的组件按逆序回滚到原有状态。这套设计的容错边界甚至考虑到了 SIGKILL/OOM 这类极端情况——历史上被强杀的恢复可能遗留下孤儿暂存目录，其中的 `rollback/` 子树可能是某次好数据的唯一幸存副本，因此孤儿清扫被刻意安排在**本次恢复完全验证成功之后**才执行，避免"本次归档其实是坏的、却提前销毁了上一次的可恢复数据"这种复合损失。\n\nSources: [scripts/restore-backup.mjs](scripts/restore-backup.mjs#L303-L347), [scripts/restore-backup.mjs](scripts/restore-backup.mjs#L349-L387)\n\n### 恢复之后的三个动作\n\n恢复完成不代表可以立刻开机。Makefile 会检查 `data/config/session-secret.key` 是否随备份而来，缺失则警告用户登录 Cookie 全部失效需重新登录（此时下次启动会自动生成新密钥）；随后提示拉取 Agent 容器镜像（`docker pull helsome/miniclaw-agent:latest`）并用 `make start` 启动服务。这三步是迁移场景的标准收尾。\n\nSources: [Makefile](Makefile#L317-L325)\n\n## 安全不变量与测试契约\n\n上述所有防御性设计都有对应的契约测试钉死，位于 `tests/backup-restore-safety.test.ts`。这些测试直接驱动真实的 `make backup` / `node scripts/restore-backup.mjs` 进程，在临时目录中构造对抗性输入，属于典型的"安全不变量回归网"：\n\n| 安全不变量 | 验证场景 | 测试位置 |\n| --- | --- | --- |\n| 可再生链接被剔除，本体保留 | 会话 `.claude` 内链接不入档，`conversation.json` 完好 | [L38-L100](tests/backup-restore-safety.test.ts#L38-L100) |\n| 外部符号链接 → 拒绝备份 | `skills/external → /tmp` 导致 make 失败且零产物 | [L102-L123](tests/backup-restore-safety.test.ts#L102-L123) |\n| 硬链接文件 → 拒绝备份 | `link()` 出的 nlink=2 文件被两道防线拦截 | [L125-L155](tests/backup-restore-safety.test.ts#L125-L155) |\n| 归档条目类型白名单 | 含符号链接的伪造归档在解压前被拒 | [L156-L182](tests/backup-restore-safety.test.ts#L156-L182) |\n| 链接元数据不可逃逸 | 伪造 `backup-symlinks.json` 试图越界被拒 | [L184-L220](tests/backup-restore-safety.test.ts#L184-L220) |\n| 大归档可用性 | 条目列表超 1 MiB 的真实规模归档仍可恢复 | [L222-L256](tests/backup-restore-safety.test.ts#L222-L256) |\n| 孤儿暂存目录的清扫时机 | 上次被杀的暂存目录只在本次成功后清扫 | [L258-L372](tests/backup-restore-safety.test.ts#L258-L372) |\n| 并发互斥与锁生命周期 | 并发恢复被拒、早失败放锁、陈旧锁 fail-closed | [L374-L669](tests/backup-restore-safety.test.ts#L374-L669) |\n| WAL 数据完整性 + 离线强制 | 仅存在于 WAL 的行出现在恢复库；端口占用时拒绝恢复 | [L670-L818](tests/backup-restore-safety.test.ts#L670-L818) |\n\nSources: [tests/backup-restore-safety.test.ts](tests/backup-restore-safety.test.ts#L37-L818)\n\n最后一个测试尤其值得展开：它在源库上关闭自动 checkpoint、提交一行到主文件、再提交一行只留在 WAL，先用"直接复制主库文件只能读到一行"证明问题真实存在，再走完整的 `make backup` → `make restore` 流程断言恢复库读出两行。同一测试还顺带验证了四个恢复终态：敏感文件权限确为 `0o600`、Workspace 内部相对链接（`AGENTS.md → CLAUDE.md`）被原样重建而外部链接消失、权威 manifest 使目标处多余的 `extra/stale.txt` 被清除、以及恢复库不带任何 `-wal`/`-shm` 残留。\n\nSources: [tests/backup-restore-safety.test.ts](tests/backup-restore-safety.test.ts#L670-L818)\n\n## 日常运维命令速查\n\n除了备份/恢复，Makefile 还承载着日常运维的全部入口。有一条贯穿性的约束需要先说明：本项目**只用原生 Node 工具链**（npm/npx/tsx/node），明确不使用 bun——因为主服务的 WebSocket 依赖 `ws` 包配合 `@hono/node-server` 的 `server.on(\'upgrade\')` 握手，这一模式在 bun 的 HTTP server 下不触发，会导致前端实时流式卡片全数失效。这条注释写在 Makefile 变量区，属于运维者最容易踩的隐式坑位。\n\nSources: [Makefile](Makefile#L7-L20)\n\n| 类别 | 命令 | 行为要点 |\n| --- | --- | --- |\n| 开发运行 | `make dev` | 首次自动安装依赖、物化内置 Skills、拉取镜像，前后端一起起 |\n| | `make dev-backend` / `make dev-web` | tsx 直跑 TS 后端 / Vite 前端 |\n| 生产启动 | `make start` | 端口占用预检 → 依赖变更检测 → 四路增量编译 → `node dist/index.js` 前台阻塞 |\n| 进程控制 | `make stop` | 按 `PORT` 用 lsof 找到并 kill 监听进程 |\n| | `make status` | 进程存活 + `/api/health` 探活 + 日志文件 + Docker 容器一览 |\n| | `make logs` | tail `/tmp/miniclaw.log`（需以后台模式自行启动） |\n| 构建产物 | `make build` / `clean` | 三端编译（先同步共享类型）/ 清理 dist 与 sentinel |\n| 质量门禁 | `make typecheck` | 三端 tsc + 共享类型同步检查 + Prompt 同步检查 + 文档检查 |\n| | `make test` / `format-check` | Vitest 单测 / Prettier 检查 |\n| 运行时升级 | `make update-pi-runtime` | 显式升级 Pi Runtime 与 SubAgents 到最新版并重编译 runner |\n| | `make ensure-latest-pi-runtime` | 只读比对本地与 npm 最新版本号 |\n| 环境管理 | `make install` | 三项目 `npm ci` + 修复 spawn-helper 权限 + 物化内置 Skills |\n| | `make reset-init` | ⚠️ 删除 `data store groups`，完全回到首装状态 |\n| 备份恢复 | `make backup` / `make restore FILE=…` | 见前文两条管线 |\n\nSources: [Makefile](Makefile#L22-L48), [Makefile](Makefile#L121-L146), [Makefile](Makefile#L150-L171), [Makefile](Makefile#L192-L238)\n\n### make start 的增量编译检测\n\n生产启动不会盲目重编译。它先用 lsof 做端口占用预检（占用则打印占用进程并退出），再检测依赖图是否过期（任一 `package.json`/lockfile 比 `node_modules` 新就重装），然后并行驱动四路陈旧性检查：`_check-sync` 比较 `shared/` 与三端同步副本的 mtime 决定是否同步共享类型；`_build-web-if-stale`、`_build-backend-if-stale`、`_build-ar-if-stale` 分别以前端 `dist/index.html`、后端 `dist/index.js`、runner 的 `.tsbuildinfo` 为基准做目录级 mtime 扫描，只有真正变化的一端才重新编译。这套机制让"改了一行前端就重启"的场景不必等待后端重编。\n\nSources: [Makefile](Makefile#L52-L69), [Makefile](Makefile#L73-L119)\n\n### 运行时升级与重置的纪律\n\nPi Runtime 的升级被刻意做成显式动作：`make start` 的注释明确写着"生产启动不得隐式改写依赖图"，升级必须手动执行 `make update-pi-runtime`，验证通过后再提交 `package.json` 与 lockfile——这与 SQLite 迁移"必须具备备份、前向升级和拒绝降级保护"的纪律一脉相承。另一端，`make reset-init` 是唯一的全量销毁入口，直接移除 `data store groups` 三个目录；执行前务必确认已有可用备份，因为它绕过了恢复管线所有的保护机制。\n\nSources: [Makefile](Makefile#L52-L54), [Makefile](Makefile#L192-L200), [Makefile](Makefile#L236-L238), [CLAUDE.md](CLAUDE.md#L241-L242)\n\n## 典型运维场景手册\n\n把前面的机制组合起来，可以覆盖四类高频场景。**例行备份**建议利用变量重定向把产物送离工作目录：`make backup BACKUP_DIR=/var/backups/miniclaw`，产物自带秒级时间戳与 `0o600` 权限，可直接接入 crontab。**跨机迁移**的标准序列是：源机 `make backup` → 拷贝 `miniclaw-backup-*.tar.gz` 到新机 → 新机 `make install` 装依赖 → `make restore FILE=<归档>` → `docker pull` 拉镜像 → `make start`；由于备份内含在线快照的数据库与全部持久组件，新机会获得与源机一致的用户、绑定关系与会话状态。**疑似数据库损坏**的处理路径是先 `make stop` 再 `make restore` 恢复最近一次快照——恢复管线的 `integrity_check` 会拒绝把损坏的库换装进去，而带回滚的组件换装保证失败时不破坏现场。\n\nSources: [Makefile](Makefile#L242-L247), [Makefile](Makefile#L317-L325), [scripts/restore-backup.mjs](scripts/restore-backup.mjs#L159-L171), [scripts/restore-backup.mjs](scripts/restore-backup.mjs#L303-L347)\n\n第四个场景是**服务无响应排查**：`make status` 一次性给出进程存活、健康接口返回、日志尾部三行与 Docker 容器状态四类信号，足以区分"进程死了""进程活着但卡死""Docker 侧异常"三种故障域；若需要持续观察，用 `make start > /tmp/miniclaw.log 2>&1 &` 后台启动后再 `make logs` 跟踪。注意日志走 pino 输出到 stdout，`logs` 目标只是对这个约定文件的 tail 封装。\n\nSources: [Makefile](Makefile#L127-L146)\n\n## 小结与延伸阅读\n\n本页的两条主线——`data/` 目录的三层生命周期划分，以及围绕它建立的备份/恢复管线——共同构成 Miniclaw 的数据安全模型：持久层以在线快照和清单契约进入归档，瞬态与可再生层被明确排除，而管线的每一步都以"拒绝优于损坏"为取向，并由 13 个契约测试钉死行为。日常运维的全部入口收敛在 Makefile 中，增量编译、显式升级与 fail-closed 的恢复锁共同降低了人为失误的空间。\n\n若想继续深入，推荐以下阅读路径：\n\n- 想理解被备份的 `messages.db` 内部结构（Schema 版本化、迁移与核心表族），请阅读 [SQLite 持久化：Schema 版本化、迁移策略与核心表族](9-sqlite-chi-jiu-hua-schema-ban-ben-hua-qian-yi-ce-lue-yu-he-xin-biao-zu)；\n- 想了解本页提到的契约测试在整个测试体系中的位置，请阅读 [测试体系：单元测试、契约测试与 E2E 场景的组织方式](27-ce-shi-ti-xi-dan-yuan-ce-shi-qi-yue-ce-shi-yu-e2e-chang-jing-de-zu-zhi-fang-shi)；\n- 想弄清环境变量（如 `WEB_PORT`、`CONTAINER_IMAGE`）与代码默认值的叠加规则，请阅读 [配置优先级与环境变量：Web 设置、环境变量与代码默认值](28-pei-zhi-you-xian-ji-yu-huan-jing-bian-liang-web-she-zhi-huan-jing-bian-liang-yu-dai-ma-mo-ren-zhi)；\n- 若尚未跑通第一次 `make dev`，请回到 [快速开始：从克隆到一键启动](2-kuai-su-kai-shi-cong-ke-long-dao-jian-qi-dong)。';export{e as default};

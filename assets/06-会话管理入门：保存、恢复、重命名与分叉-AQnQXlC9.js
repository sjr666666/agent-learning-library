const s='MiniCode 把一次完整的对话称为一个**会话（session）**。每次启动程序都会分配一个新的 8 位会话 ID，此后你与模型的每一轮交互都会被自动记录。本页面向初学者，介绍会话是如何保存的、保存在哪里，以及如何通过 `/resume`、`/rename`、`/new`、`/fork` 四个核心命令管理你的会话。\n\n## 会话的存储：追加式 JSONL 事件日志\n\n理解会话管理，先要理解数据落在哪里。所有会话都保存在用户级配置目录 `~/.mini-code/`（可用环境变量 `MINI_CODE_HOME` 覆盖）下，按工作目录隔离：\n\n| 路径组成 | 说明 | 示例 |\n|---|---|---|\n| `~/.mini-code/` | MiniCode 全局目录，含 settings.json、permissions.json 等 | `~/.mini-code/` |\n| `projects/` | 所有会话的根目录 | `~/.mini-code/projects/` |\n| `<编码后目录>/` | 工作目录路径中的 `/`、`\\`、`:` 统一替换为 `-`，并去掉前导 `-` | `/Users/me/demo` → `Users-me-demo` |\n| `<会话ID>.jsonl` | 单个会话文件，ID 为 8 位随机十六进制 | `a1b2c3d4.jsonl` |\n\nSources: [config.ts](src/config.ts#L36-L44), [session.ts](src/session.ts#L49-L59)\n\n会话文件是**追加式 JSONL**：每一行是一条独立的 JSON 事件，涵盖 `system`、`user`、`assistant`、`tool_call`、`compact_boundary`、`rename` 等类型。每条事件都携带 `uuid`、`timestamp`、`sessionId`、`cwd` 和 `parentUuid`，其中 `parentUuid` 把文件内的事件串成一条链，即使追加写入中断，也能从最后一条事件继续衔接。\n\nSources: [session.ts](src/session.ts#L33-L47), [session.ts](src/session.ts#L82-L104)\n\n## 自动保存：每轮对话结束即落盘\n\n你不需要手动保存。每次交互回合结束后，TTY 应用会立即调用 `saveSession` 把当前消息写入对应会话文件：\n\n```mermaid\nflowchart LR\n    A[用户输入消息] --> B[代理循环执行\\nmodel → tool → model]\n    B --> C[saveSession\\n追加写入 JSONL]\n    C --> D[更新 alreadySavedCount]\n    D --> E[等待下一轮输入]\n```\n\nSources: [tty-app.ts](src/tty-app.ts#L1599-L1602)\n\n这套写入机制有三个关键设计：\n\n1. **幂等追加**：写入前先读取文件里已有的全部事件 UUID，只追加未出现过的新消息，避免重复行。\n2. **增量记账**：`alreadySavedCount` 记录已落盘的消息数，配合 UUID 去重，即使消息被压缩、裁剪后重排，也不会写重。\n3. **排除系统提示**：`messages.slice(1)` 跳过 `system` 角色的系统提示（它由启动时的权限、技能、MCP 状态动态生成，不属会话内容）。\n\nSources: [session.ts](src/session.ts#L218-L250), [session.ts](src/session.ts#L228-L238)\n\n当你退出程序时，终端会打印当前会话 ID 和恢复命令，例如 `Session a1b2c3d4 saved. To resume: minicode --resume a1b2c3d4`——这就是下次回来的"钥匙"。\n\nSources: [tty-app.ts](src/tty-app.ts#L1788)\n\n## 恢复会话：三种方式进入旧对话\n\n### 方式一：会话选择器 `/resume`\n\n在交互界面输入 `/resume`（不带参数），会弹出一个会话选择面板，列出当前项目下的所有会话。每一行显示会话 ID、标题、消息数和最后更新时间，按更新时间倒序排列：\n\n```mermaid\nflowchart TD\n    A[输入 /resume] --> B{有已保存会话?}\n    B -- 否 --> C[提示 No saved sessions]\n    B -- 是 --> D[渲染会话选择器]\n    D --> E{按键}\n    E -- ↑/↓ --> F[移动高亮行]\n    E -- Enter --> G[恢复选中会话]\n    E -- d 两次 --> H[删除该会话]\n    E -- Tab --> I[切换"全部项目"视图]\n    E -- Esc --> J[取消，返回输入]\n```\n\nSources: [tty-app.ts](src/tty-app.ts#L1223-L1264), [tty-app.ts](src/tty-app.ts#L783-L818)\n\n选择器还支持跨项目浏览：按 `Tab` 切换到"全部项目"视图，展示其他项目目录的会话数量与最近更新时间。但切换项目需要退出后 `cd` 到目标目录再启动，因此这个视图主要用于查看而非直接跳转。\n\nSources: [tty-app.ts](src/tty-app.ts#L1980-L2008)\n\n### 方式二：直接指定 ID `/resume <id>`\n\n如果你记得会话 ID，可以直接 `resume a1b2c3d4` 跳过选择器。找不到时会提示 `Session <id> not found.`。\n\nSources: [tty-app.ts](src/tty-app.ts#L1268-L1278)\n\n### 方式三：启动时恢复 `minicode --resume`\n\n`--resume` 是命令行参数，在启动前就决定要恢复哪个会话：\n\n| 启动命令 | 行为 |\n|---|---|\n| `minicode --resume` | 启动后立即弹出会话选择器 |\n| `minicode --resume <id>` | 直接恢复指定会话 |\n| （不带参数） | 新建会话；若当前项目已有会话，会提示 `Found N saved session(s). Type /resume to continue one.` |\n\nSources: [index.ts](src/index.ts#L32-L43), [tty-app.ts](src/tty-app.ts#L1723-L1750)\n\n### 恢复时加载了什么\n\n`resumeSession` 内部做了四件事：切换 `sessionId`、重建模型消息列表、重建终端 transcript 展示、复位上下文折叠状态。其中消息加载有一个重要规则：`loadSession` 只加载**最后一个压缩边界（compact_boundary）之后**的事件——压缩边界之前的上下文已被摘要替代，恢复时不会再回灌原始消息，从而保证恢复后的会话与压缩后的上下文一致。\n\nSources: [tty-app.ts](src/tty-app.ts#L955-L1009), [session.ts](src/session.ts#L368-L403)\n\n## 重命名会话：`/rename <名称>`\n\n会话默认标题取自**第一条用户消息**（超过 60 字符时截断并追加 `...`）。但你可以用 `/rename <名称>` 自定义标题。它的实现是在 JSONL 文件末尾追加一条 `rename` 事件，而不是修改历史记录——这正是追加式日志的好处：所有操作都可追溯。\n\n标题的优先级规则是：**最后一次 rename 事件的值优先于首条用户消息**。如果你把会话重命名为 `重构登录模块`，之后在选择器里看到的标题就是这个名字。\n\nSources: [session.ts](src/session.ts#L169-L188), [session.ts](src/session.ts#L504-L526), [tty-app.ts](src/tty-app.ts#L1206-L1221)\n\n## 开始新会话：`/new`\n\n`/new` 会生成一个新的 8 位会话 ID、清空当前消息与 transcript，并重新构建系统提示。注意它**不会删除旧会话文件**——旧的 `.jsonl` 依然保存在磁盘上，随时可以 `/resume` 找回。这相当于"换一张白纸，但旧稿还在抽屉里"。\n\nSources: [tty-app.ts](src/tty-app.ts#L1281-L1294), [tty-app.ts](src/tty-app.ts#L1282)\n\n## 分叉会话：`/fork` 与 `minicode --fork <id>`\n\n分叉是会话管理中最强大的能力：它把当前会话的完整历史**复制**成一个新的独立会话，此后你在新会话中的任何操作都不会影响原会话。典型场景是"实验性探索"——你对一个方案不确定，分叉后大胆尝试，不满意就回到原会话。\n\n```mermaid\nflowchart TD\n    A[输入 /fork] --> B[loadSession 读取当前会话]\n    B --> C[生成新 8 位会话 ID]\n    C --> D[saveSession 写入完整副本]\n    D --> E[自动命名 baseTitle_forkN\\nN 取已有分叉最大编号 + 1]\n    E --> F[切换 sessionId 到新会话]\n    F --> G[后续保存只写新文件\\n原会话保持不变]\n```\n\nSources: [session.ts](src/session.ts#L528-L554), [tty-app.ts](src/tty-app.ts#L1296-L1314)\n\n分叉的独立性有测试保证：在分叉会话中追加消息后，原会话的消息列表不受影响。另外，分叉文件中的 `parentUuid` 链会完整重建，新文件首条消息事件是链的根节点。\n\nSources: [session.test.ts](test/session.test.ts#L635-L655), [session.test.ts](test/session.test.ts#L657-L679), [session.test.ts](test/session.test.ts#L681-L699)\n\n在启动时也可以分叉：`minicode --fork <id>` 会先分叉指定会话，然后直接进入这个新副本。\n\nSources: [index.ts](src/index.ts#L45-L54), [index.ts](src/index.ts#L112-L120)\n\n## 删除与过期清理\n\n### 手动删除\n\n在会话选择器中，对目标行连续按两次 `d` 键即可删除（第一次按键进入确认状态，显示 `[DELETE? Press d again to confirm]`，第二次真正执行）。删除通过 `clearSession` 完成：移除对应 `.jsonl` 文件；如果项目目录因此变空，目录本身也会被清除。\n\nSources: [tty-app.ts](src/tty-app.ts#L2045-L2070), [session.ts](src/session.ts#L440-L459)\n\n### 自动过期清理\n\n每次**不带恢复参数**启动时，MiniCode 会执行 `cleanupExpiredSessions`，删除最后修改时间超过 30 天的会话文件。清理完成后会在界面提示 `Cleaned up N expired session(s) (>30 days old).`。\n\nSources: [tty-app.ts](src/tty-app.ts#L1736-L1742), [session.ts](src/session.ts#L556-L594)\n\n## 命令与参数速查表\n\n| 操作 | 命令 | 作用 |\n|---|---|---|\n| 保存 | （自动） | 每轮对话结束自动追加写入 JSONL |\n| 恢复（选择器） | `/resume` | 弹出当前项目会话选择器 |\n| 恢复（指定 ID） | `/resume <id>` | 直接恢复指定会话 |\n| 恢复（启动时） | `minicode --resume [<id>]` | 启动即进入选择器或指定会话 |\n| 重命名 | `/rename <名称>` | 追加 rename 事件，覆盖默认标题 |\n| 新建 | `/new` | 生成新会话 ID，旧会话文件保留 |\n| 分叉 | `/fork` | 复制当前会话为独立副本并进入 |\n| 分叉（启动时） | `minicode --fork <id>` | 分叉指定会话并直接进入 |\n| 删除 | 选择器中按 `d` 两次 | 删除会话文件，空目录一并清理 |\n| 过期清理 | （自动） | 启动时清理超过 30 天的会话 |\n\n## 下一步阅读\n\n- 会话的底层存储格式（事件类型、`parentUuid` 链、compact boundary）在 [追加式 JSONL 事件日志与断点恢复](19-zhui-jia-shi-jsonl-shi-jian-ri-zhi-yu-duan-dian-hui-fu) 中有深入剖析。\n- 分叉与压缩边界、过期清理的完整机制见 [会话分叉、压缩边界与过期清理](20-hui-hua-fen-cha-ya-suo-bian-jie-yu-guo-qi-qing-li)。\n- 恢复时只加载压缩边界之后的消息，其背后的自动压缩触发逻辑见 [自动压缩：触发阈值与失败熔断](12-zi-dong-ya-suo-hong-fa-yu-zhi-yu-shi-bai-rong-duan)。\n- 还不熟悉交互界面？先阅读 [交互式界面与斜杠命令速览](4-jiao-hu-shi-jie-mian-yu-xie-gang-ming-ling-su-lan)，了解这些命令如何输入与自动补全。';export{s as default};

const e='**范围说明**：本文聚焦 MiniCode 的"先审阅、后写入"机制——四个写文件工具如何在落盘之前统一生成 diff、请求用户审批，以及审批决策如何反馈回模型。权限体系中的路径审批（`ensurePathAccess`）与命令审批（`ensureCommand`）不在本文展开，详见 [三层权限系统：路径、命令与编辑审批](17-san-ceng-quan-xian-xi-tong-lu-jing-ming-ling-yu-bian-ji-shen-pi)。\n\n## 统一写入口：一切修改都经过同一个审阅管道\n\nMiniCode 的写前审阅建立在**单点汇聚**的架构原则上。模型可调用的四个文件修改工具——`write_file`、`modify_file`、`edit_file`、`patch_file`——虽然语义各不相同，但最终都汇入同一个函数 `applyReviewedFileChange`。这意味着无论模型用哪种方式修改文件，diff 生成与人工审批都只发生一次，且路径完全一致。\n\n| 工具 | 修改粒度 | 入口职责 |\n|---|---|---|\n| `write_file` | 整文件覆盖 | 直接透传目标内容 |\n| `modify_file` | 整文件覆盖 | 与 `write_file` 相同，描述明确强调"先审阅" |\n| `edit_file` | 单次精确文本替换（可选 replaceAll） | 先校验 search 存在，再替换 |\n| `patch_file` | 单文件多次精确替换 | 逐个替换，任一失败即中止 |\n\n四个工具都先通过 `resolveToolPath(context, input.path, \'write\')` 解析并校验路径，随后调用 `applyReviewedFileChange(context, input.path, target, nextContent)`。`modify_file` 的描述直接点明了设计意图："Replace a file with reviewed content so the user can approve the diff first"（先用待审阅的内容替换文件，让用户先批准 diff）。Sources: [edit-file.ts](src/tools/edit-file.ts#L33-L49)、[write-file.ts](src/tools/write-file.ts#L26-L30)、[patch-file.ts](src/tools/patch-file.ts#L50-L83)、[modify-file.ts](src/tools/modify-file.ts#L26-L30)\n\n`applyReviewedFileChange` 是审阅管道的核心，其执行顺序严格遵循"先审后写"：\n\n```mermaid\nflowchart TD\n    A[工具 run 计算 nextContent] --> B{loadExistingFile 读取旧内容}\n    B --> C{旧内容 == 新内容?}\n    C -- 是 --> D[返回 No changes needed]\n    C -- 否 --> E[buildUnifiedDiff 生成统一 diff]\n    E --> F[ensureEdit 请求审批]\n    F -- 拒绝 --> G[抛出 Edit denied 异常]\n    F -- 批准 --> H[mkdir + writeFile 落盘]\n    H --> I[返回 Applied reviewed changes]\n```\n\n其中 `loadExistingFile` 对不存在的文件返回空字符串（而不是报错），因此新建文件的 diff 会呈现为整段新增，审阅者能清晰看到将要创建的内容。Sources: [file-review.ts](src/file-review.ts#L34-L70)\n\n## 统一 diff 的生成：基于 createTwoFilesPatch 的精简输出\n\ndiff 由 `buildUnifiedDiff` 生成，其基于 `diff` 包的 `createTwoFilesPatch`，但做了两项针对终端审阅场景的定制：\n\n1. **3 行上下文**：`{ context: 3 }` 在保持可读性的同时控制输出体积；\n2. **剥离头部分隔线**：`createTwoFilesPatch` 输出的首行是 `===` 开头的分隔线，TUI 审批弹窗空间有限，代码会检测并丢弃该行，使 diff 直接以 `--- a/...` / `+++ b/...` 头部开始。\n\n文件路径以 `a/${filePath}` 与 `b/${filePath}` 双前缀标注，与标准 `git diff` 的命名约定一致，便于审阅者对照工作区状态。Sources: [file-review.ts](src/file-review.ts#L7-L32)、[package.json](package.json#L17)\n\n## 审批门禁 ensureEdit：七选一的决策状态机\n\n`ensureEdit(targetPath, diffPreview)` 是审阅决策的核心。其逻辑分两阶段：**快速路径**（凭已有信任/拒绝记录直接放行或拦截）与**交互审批**（弹出 UI 等待用户决策）。\n\n快速路径按顺序检查四类集合——任一命中即短路返回或抛错：\n\n- **拦截**：`sessionDeniedEdits`（本次会话拒绝过的文件）、`deniedEditPatterns`（持久化"永远拒绝"列表）\n- **放行**：`sessionAllowedEdits`、`turnAllowedEdits`（本轮已批准）、`turnAllowAllEdits`（本轮全放行开关）、`allowedEditPatterns`（持久化"永远允许"列表）\n\n未命中快速路径时，若当前运行环境没有审批处理器（如非 TTY 的基础模式），`ensureEdit` 直接抛错，提示用户以 TTY 模式启动以进行审阅——这保证了审阅流程在无 UI 环境下不会被静默跳过。Sources: [permissions.ts](src/permissions.ts#L409-L434)\n\n交互审批提供七个选项，其作用域与持久化行为差异显著：\n\n| 按键 | 决策 | 作用域 | 持久化 | 后续行为 |\n|---|---|---|---|---|\n| `1` | apply once | 会话级 | 否 | 加入 `sessionAllowedEdits`，本次会话内该文件不再提示 |\n| `2` | allow this file in this turn | 回合级 | 否 | 加入 `turnAllowedEdits`，回合结束即清空 |\n| `3` | allow all edits in this turn | 回合级（全文件） | 否 | 置位 `turnAllowAllEdits`，本轮所有文件免审 |\n| `4` | always allow this file | 全局 | 是 | 写入 `allowedEditPatterns` → `permissions.json` |\n| `5` | reject once | 会话级 | 否 | 加入 `sessionDeniedEdits` |\n| `6` | reject and send guidance | 一次性 | 否 | 携带用户反馈抛错回传模型 |\n| `7` | always reject this file | 全局 | 是 | 写入 `deniedEditPatterns` → `permissions.json` |\n\n值得注意的语义细节：**"apply once"实际是会话级放行**——按 `1` 后该文件在本次会话中不再触发审批，而按 `2` 的"本轮放行"才是严格的最小授权。持久化选项（`4`、`7`）通过 `persist()` 将模式写入 `$MINI_CODE_DIR/permissions.json`，重启后依然生效。Sources: [permissions.ts](src/permissions.ts#L436-L496)\n\n回合边界由 `beginTurn()` / `endTurn()` 管理：每次调用 `runAgentTurn` 之前清空 `turnAllowedEdits` 并复位 `turnAllowAllEdits`，回合结束后再次清理。TTY 模式与基础模式（`index.ts`）都在各自的消息循环中成对调用，确保"本轮放行"的授权不会泄漏到下一轮。Sources: [permissions.ts](src/permissions.ts#L212-L220)、[tty-app.ts](src/tty-app.ts#L1388-L1615)、[index.ts](src/index.ts#L240-L263)\n\n## TUI 审批界面：语法高亮、词级强调与反馈输入\n\n审批弹窗由 `renderPermissionPrompt` 渲染，标题为黄色加粗的 `Approval Required`，正文包含请求摘要（`mini-code wants to apply a file modification`）与 diff 详情。针对 `edit` 类型的请求，diff 会经过一层专门的着色管线 `colorizeEditPermissionDetails`：\n\n```mermaid\nflowchart LR\n    A[diff 原始文本] --> B[classifyDiffLine 逐行分类]\n    B --> C[meta: --- / +++ / @@ 头部]\n    B --> D[add: + 行 / remove: - 行]\n    B --> E[context: 其他行]\n    C --> F[青色加粗]\n    D --> G[配对相邻增减行<br/>computeChangedRange 计算公共前后缀]\n    G --> H[applyWordEmphasis<br/>将变更词段加粗]\n    E --> I[暗色]\n```\n\n这一管线的精妙之处在于**词级差异强调**：`computeChangedRange` 通过剔除公共前缀与公共后缀，定位删除行与新增行之间真正变化的子串，再由 `applyWordEmphasis` 在行级颜色（增行亮绿、删行亮红）之上叠加加粗。审阅者可以一眼识别出"哪几个词被改动了"，而无需逐字符比对整行。Sources: [chrome.ts](src/tui/chrome.ts#L559-L728)\n\n弹窗的交互设计同样围绕大 diff 的可读性展开：\n\n- **折叠/展开**：默认折叠只显示有限行数，超出部分提示 `Ctrl+O expand full diff`；展开后支持滚轮、PgUp/PgDn、Alt+Up/Alt+Down 滚动；\n- **快速选择**：数字键 `1`-`7` 直接触发对应决策，方向键移动光标、Enter 确认、Esc 默认拒绝一次；\n- **反馈模式**：选择 `6`（deny_with_feedback）后弹窗切换为 `Reject With Guidance` 输入模式，用户可键入文字指导，Enter 提交、Esc 返回。\n\nSources: [chrome.ts](src/tui/chrome.ts#L498-L557)、[tty-app.ts](src/tty-app.ts#L1866-L1950)\n\n审批请求的挂载点在 `createPermissionPromptHandler`：它构造一个 `PendingApproval`（携带请求体、resolve 回调与 UI 状态：是否展开、滚动偏移、选中项、反馈模式），置入 `state.pendingApproval` 后等待用户输入。整个工具执行线程在 Promise 上挂起，直到用户在 UI 上做出决策。Sources: [tty-app.ts](src/tty-app.ts#L1626-L1644)\n\n## 拒绝反馈如何回到模型：异常→工具结果→下一轮迭代\n\n审阅决策并非终点——**拒绝本身是模型迭代的信号**。整个回传链路由异常驱动：\n\n1. `ensureEdit` 对拒绝决策抛出 `Error`；若用户提供了反馈（决策 `6`），错误消息包含 `User guidance: ...` 原文；\n2. `ToolRegistry.execute` 的 try/catch 将异常转换为 `{ ok: false, output: error.message }`；\n3. `runAgentTurn` 将该结果作为 `tool_result` 消息（`isError: true`）追加回对话，并递增 `toolErrorCount`；\n4. 模型在下一轮看到 `Edit denied: <path>` 及用户指导，可据此调整方案重试。\n\n```mermaid\nsequenceDiagram\n    participant Model as 模型\n    participant Tool as ToolRegistry.execute\n    participant Review as applyReviewedFileChange\n    participant Perm as PermissionManager.ensureEdit\n    participant User as 用户\n\n    Model->>Tool: 调用 write_file / edit_file ...\n    Tool->>Review: run 计算 nextContent\n    Review->>Perm: ensureEdit(path, diff)\n    Perm->>User: 弹窗展示 diff + 7 选项\n    User-->>Perm: deny_with_feedback("不要修改配置文件")\n    Perm-->>Review: 抛出 "Edit denied: ... User guidance: ..."\n    Review-->>Tool: 异常冒泡\n    Tool-->>Model: tool_result { isError: true, output: 错误+指导 }\n    Model->>Model: 依据指导调整方案，重试\n```\n\n注意 `deny_with_feedback` 的一个细节：当用户提供了指导时，该文件**不会**被加入 `sessionDeniedEdits`——这意味着模型修正方案后重试同一文件，仍会再次触发审批，而不是被静默拦截。只有当用户没有填写指导时，才退化为会话级拒绝。Sources: [permissions.ts](src/permissions.ts#L477-L486)、[tool.ts](src/tool.ts#L95-L124)、[agent-loop.ts](src/agent-loop.ts#L380-L406)\n\n## 与权限体系的关系：先路径、后编辑的两级门禁\n\n写文件操作实际经过两级审批，本文只深入了第二级：\n\n1. **路径级**（`resolveToolPath` → `ensurePathAccess`）：校验目标是否在工作区内，工作区外的路径需要目录级审批；\n2. **编辑级**（`applyReviewedFileChange` → `ensureEdit`）：路径通过后，内容变更本身还需要 diff 审阅。\n\n两级门禁的审批请求类型分别为 `kind: \'path\'` 与 `kind: \'edit\'`，作用域、选项与持久化机制各不相同。另外，`getSummary()` 会将 `allowedEditPatterns` 中的"trusted edit targets"注入系统提示词的 Permission context，让模型在调用工具前就知道哪些文件已被用户信任、不会触发审批弹窗。Sources: [workspace.ts](src/workspace.ts#L4-L28)、[permissions.ts](src/permissions.ts#L222-L248)、[prompt.ts](src/prompt.ts#L33-L35)\n\n## 设计权衡小结\n\n| 设计决策 | 权衡 |\n|---|---|\n| 统一入口 `applyReviewedFileChange` | 四个工具共享同一审阅管道，行为一致、无遗漏路径；代价是 `write_file` 与 `modify_file` 语义高度重叠 |\n| diff 在审批前生成 | 用户看到的即是将要落盘的内容，审阅有据可依；代价是每次修改都要承担一次 diff 计算 |\n| `allow_once` 实为会话级 | 减少重复打扰；但标签与语义存在偏差，用户可能误以为只授权一次 |\n| 非 TTY 模式强制拒绝 | 保证审阅流程不被绕过；代价是无 UI 环境下无法完成任何需审批的写操作 |\n| 拒绝即异常回传 | 复用工具错误通道，模型天然能感知并迭代；代价是反馈混入错误消息，模型需自行解析 `User guidance` |\n\n本文属于"安全边界"篇章。后续可继续阅读 [三层权限系统：路径、命令与编辑审批](17-san-ceng-quan-xian-xi-tong-lu-jing-ming-ling-yu-bian-ji-shen-pi) 了解路径级门禁的完整决策树，或通过 [代理循环：model → tool → model 的多步执行流程](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng) 理解工具结果如何驱动模型的迭代循环。';export{e as default};

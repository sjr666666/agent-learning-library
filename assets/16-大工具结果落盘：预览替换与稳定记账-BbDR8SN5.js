const t='终端编码代理的典型场景里，`run_command`、`read_file` 这类工具一次可能返回数万甚至数十万字符的输出。如果原样塞进上下文，轻则挤占模型注意力，重则直接触发压缩甚至逼近上下文窗口上限。MiniCode 的解法是**在工具结果进入消息历史之前就做一次"物理搬移"**：把超大的完整输出写到本地磁盘，只在可见上下文里留下一个带文件路径的短预览，并保证这一过程在记账（token 统计、批次预算、重复处理）层面是**确定且稳定**的。\n\n本页聚焦 `src/utils/tool-result-storage.ts` 这个独立模块：它定义了两道防线——单条结果的 50K 字符落盘阈值，与一批结果的 200K 字符可见预算；并借助 `ContentReplacementState` 让替换结果在同一进程内可字节级重放，让上下文记账不因消息被反复处理而产生漂移。\n\nSources: [tool-result-storage.ts](src/utils/tool-result-storage.ts#L7-L13)、[ARCHITECTURE.md](ARCHITECTURE.md#L55)\n\n## 两道防线：单条阈值与批次预算\n\n工具结果的处理发生在代理循环里，且是**先逐条、后整体**的两阶段流水线。工具执行完毕拿到原始输出后，先调用 `replaceLargeToolResult` 做单条归一化与阈值判断；等这一轮所有工具都执行完，再调用 `applyToolResultBudget` 对整批结果做总额预算控制：\n\n```mermaid\nflowchart TD\n    A[工具执行完成<br/>原始 output] --> B[replaceLargeToolResult<br/>逐条处理]\n    B --> B1{内容为空?}\n    B1 -->|是| B2["(toolName completed with no output)"]\n    B1 -->|否| B3{已是 persisted-output?}\n    B3 -->|是| B4[原样保留<br/>写入 state.replacements]\n    B3 -->|否| B5{超过 50K 字符?}\n    B5 -->|否| B6[原样返回]\n    B5 -->|是| B7[写入磁盘 tool-results 目录<br/>生成预览替换消息]\n    B2 & B4 & B6 & B7 --> C[本批所有 tool_result 消息]\n    C --> D[applyToolResultBudget<br/>批次可见预算 200K]\n    D --> D1{可见总量 > 200K?}\n    D1 -->|是| D2[按 大小降序 + toolUseId 升序<br/>贪心持久化最大的新鲜结果]\n    D1 -->|否| D3[全部保留]\n    D2 --> D4{替换后仍超预算?}\n    D4 -->|是| D2\n    D4 -->|否| D3\n    D2 & D3 --> E[带预览的 tool_result 消息<br/>进入消息历史]\n```\n\n代理循环在 `src/agent-loop.ts` 中把这两步串起来：每个 `call` 执行完立即做单条替换，收集到 `executedToolResults` 后再统一跑批次预算，最后用 `toolResultById` 映射把（可能被替换过的）结果写回消息数组。两条防线分工明确——50K 阈值解决"单条过大"，200K 预算解决"条条不大但合起来超标"（例如 5 条各 45K 的结果合计 225K）。\n\nSources: [agent-loop.ts](src/agent-loop.ts#L393-L411)、[agent-loop.ts](src/agent-loop.ts#L412-L437)\n\n## 落盘路径与预览生成\n\n落盘位置是 MiniCode 本地数据目录下的 `tool-results` 子目录，再按进程级随机 `sessionId` 分一层，最终文件名由 `toolUseId` 清洗而来：\n\n```\n~/.mini-code/tool-results/<进程随机UUID>/<toolUseId清洗后>.txt\n```\n\n`MINI_CODE_DIR` 默认指向 `~/.mini-code`（可用 `MINI_CODE_HOME` 覆盖）。路径安全做了**双保险**：`sanitizePathSegment` 先把非法字符替换为 `_`（空结果退回随机 UUID）；`getToolResultPath` 再用 `path.resolve` + `path.relative` 校验解析后的路径确实落在目标目录内，否则同样退回随机文件名。测试用 `../..\\evil/name` 这样的恶意 `toolUseId` 验证，落盘结果始终被约束在 `tool-results` 根目录之下。\n\nSources: [config.ts](src/config.ts#L36-L38)、[tool-result-storage.ts](src/utils/tool-result-storage.ts#L41-L60)、[tool-result-storage.test.ts](test/tool-result-storage.test.ts#L193-L205)\n\n预览消息由 `buildPersistedToolResultMessage` 拼装，格式固定为带 `persisted-output` 标签的文本块。`generatePreview` 负责截断：内容不超过 2000 字符则完整保留；超过时先截前 2000 字符，若后半段存在换行符（位置大于 1000）则退到最近的换行处切断，避免在行中截断。`formatChars` 把字节数人性化（`50K chars`、`1.0M chars`）。磁盘写入使用 `flag: \'wx\'` 独占模式并容忍 `EEXIST`——同一 `toolUseId` 重复落盘不会覆盖已有文件，预览生成是纯函数，相同内容必然得到相同预览，这是记账稳定性的物理基础。\n\nSources: [tool-result-storage.ts](src/utils/tool-result-storage.ts#L66-L87)、[tool-result-storage.ts](src/utils/tool-result-storage.ts#L94-L140)\n\n## 批次预算：贪心替换与稳定决胜\n\n`applyToolResultBudget` 的核心是"可见大小记账"。它先遍历整批结果，把每条的**可见贡献**累加进 `visibleSize`：已在 `state.replacements` 中登记过的，记替换后长度（即预览长度）；只见过未替换的，记当前内容长度；空结果与已持久化输出分别标记后跳过。真正可能被替换的是 `freshCandidates`——本轮新出现且未处理过的结果。\n\n随后对新鲜候选按 **大小降序、`toolUseId` 升序**排序，从最大的开始贪心落盘，每替换一条就把 `visibleSize` 减去原始大小、加上预览长度，直到总量不超过 200K 或候选耗尽。排序的稳定性是关键设计：同尺寸结果谁先被替换不取决于数组遍历顺序，而是 `toolUseId` 的字典序，保证同一批输入无论经过多少次处理都得到**完全相同**的替换决策。被替换的记录以 `newlyReplaced` 数组返回（`kind: \'tool-result\'` + `toolUseId` + `replacement`），为上层 UI 或日志预留了通知通道。\n\nSources: [tool-result-storage.ts](src/utils/tool-result-storage.ts#L198-L292)\n\n对应测试精确锁定了这些行为：\n\n| 测试场景 | 断言 | 来源 |\n|---|---|---|\n| 单条 50K 边界 | 恰好 50_000 不落盘，50_001 落盘 | [test L61-L70](test/tool-result-storage.test.ts#L61-L70) |\n| 批次 200K 边界 | 两条各 100K 不替换，101K + 100K 替换 1 条 | [test L89-L107](test/tool-result-storage.test.ts#L89-L107) |\n| 最大优先 | 三条 100K 只替换两条最大的，可见总量 ≤ 200K | [test L109-L126](test/tool-result-storage.test.ts#L109-L126) |\n| 稳定决胜 | 三个同尺寸结果按 `toolUseId` 排序选取 | [test L128-L138](test/tool-result-storage.test.ts#L128-L138) |\n\nSources: [tool-result-storage.test.ts](test/tool-result-storage.test.ts#L89-L138)\n\n## 稳定记账：ContentReplacementState 的确定性重放\n\n"稳定记账"由 `ContentReplacementState` 承载，它只有两个字段：`seenIds`（已处理过的 `toolUseId` 集合）与 `replacements`（`toolUseId` → 替换文本的映射）。其核心价值在于**幂等重放**：\n\n- `replaceLargeToolResult` 开头先查 `state.replacements`，命中则直接返回先前生成的替换文本，**不再落盘、不再重新生成预览**；\n- `applyToolResultBudget` 同样优先读取 `previousReplacement`，把它计入可见大小；\n- 已被持久化输出标记（以 `<persisted-output>` 开头）的内容会原样进入 `replacements`，之后永不二次落盘。\n\n这意味着消息即使被反复送入管线（同一回合的多次处理、上下文压缩后重建、会话恢复后的历史重放），同一 `toolUseId` 永远映射到**字节级一致**的替换文本。测试用两次连续调用验证了这一点：第二次调用的 `newlyReplaced` 为空，替换内容与第一次完全相同。\n\nSources: [tool-result-storage.ts](src/utils/tool-result-storage.ts#L15-L39)、[tool-result-storage.ts](src/utils/tool-result-storage.ts#L142-L196)、[tool-result-storage.test.ts](test/tool-result-storage.test.ts#L140-L191)\n\n这种稳定性直接传导到上下文记账：令牌估算器对 `tool_result` 角色按 `content.length / 2.0` 计算（每字符 2.0 token 的倒数）。替换后消息的 content 是短预览而非原始输出，因此估算值反映的是**可见内容**而非磁盘上的完整内容；又因为替换文本确定，估算结果不会因消息被多次处理而波动。这也与"供应商用量优先"的记账体系兼容——`tool_result` 消息不携带 provider usage，始终走本地估算，而预览替换让这条路径的估算值保持小而稳定。\n\nSources: [token-estimator.ts](src/utils/token-estimator.ts#L34-L44)、[token-estimator.ts](src/utils/token-estimator.ts#L67-L82)\n\n## 边界条件：空输出、非字符串与已持久化内容\n\n`normalizeToolResultContent` 统一了内容归一化规则：`null`/`undefined` 归一为空串，字符串原样保留，其他类型 `String()` 强转。空输出的判定用 `content.trim().length === 0`，因此 `\'0\'`、`\'false\'`、`\'[]\'` 这类"falsy 但不为空"的字符串不会被误判，测试逐一验证过。空结果会被替换为 `(run_command completed with no output)`（工具名动态注入），既保留"这个工具确实跑过"的结构信息，又不浪费上下文。\n\nSources: [tool-result-storage.ts](src/utils/tool-result-storage.ts#L89-L92)、[tool-result-storage.test.ts](test/tool-result-storage.test.ts#L72-L87)\n\n值得注意的模块边界：`tool-result-storage` 处理的是**执行当下**的大结果，而微压缩（microcompact）处理的是**历史中**的旧工具结果占位清理，两者按 `COMPACTABLE_TOOLS` 集合和保留条数协作，互不重叠。落盘后的完整内容以文件形式存于 `~/.mini-code/tool-results/`，会话 JSONL 日志中记录的则是带路径的预览消息——因此日志体积与上下文体积同时被压缩，恢复会话时路径依然有效。`compact.ts` 按 API 轮次分组时把 `tool_result` 与前面的 `assistant_tool_call` 归为一组，保证压缩边界不会拆散"调用-结果"对。\n\nSources: [microcompact.ts](src/compact/microcompact.ts#L15-L28)、[session.ts](src/session.ts#L22)、[compact.ts](src/compact/compact.ts#L23-L35)\n\n## 总结与阅读路径\n\n`tool-result-storage` 用极小的模块面积实现了三层保障：**空间**上把大输出物理搬离上下文（50K 单条阈值 + 200K 批次预算 + 2K 预览）；**安全**上以路径清洗、`wx` 独占写、`persisted-output` 标签识别保证落盘幂等；**账目**上以 `ContentReplacementState` 的确定性重放让同一结果的可见内容与 token 估算永远一致。这三层共同构成了"预览替换"与"稳定记账"的完整闭环。\n\n建议按目录顺序继续深入：本页是上下文管理链路的收尾一环，往前可对照 [供应商用量优先的上下文记账与令牌估算](11-gong-ying-shang-yong-liang-you-xian-de-shang-xia-wen-ji-zhang-yu-ling-pai-gu-suan)（理解替换后的消息如何参与 provider usage 记账）、[微压缩：旧工具结果的占位清理](15-wei-ya-suo-jiu-gong-ju-jie-guo-de-zhan-wei-qing-li)（了解执行当下之外的另一条清理路径）；往后可进入 [三层权限系统：路径、命令与编辑审批](17-san-ceng-quan-xian-xi-tong-lu-jing-ming-ling-yu-bian-ji-shen-pi)，看工具执行前的安全边界如何与本页的执行后处理衔接。';export{t as default};

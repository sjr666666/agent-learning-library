const e='HappyClaw 的 Web 前端位于 `web/` 目录，是一个独立的 React 19 + TypeScript 工程。它通过 REST API 与 WebSocket 双通道与主服务通信：REST 承载命令型操作（登录、拉取消息、管理工作区），WebSocket 承载实时事件（流式输出、Runner 状态、渠道绑定）。本文聚焦前端的**架构骨架**：路由体系如何组织、应用外壳如何响应式适配、状态如何分层治理、以及认证与性能边界如何落地。流式渲染体验（StreamingDisplay、终端面板等）的细节见 [实时消息流、终端与流式渲染体验](28-shi-shi-xiao-xi-liu-zhong-duan-yu-liu-shi-xuan-ran-ti-yan)。\n\nSources: [package.json](web/package.json#L13-L42)、[vite.config.ts](web/vite.config.ts#L20-L48)\n\n## 技术栈与目录布局\n\n前端工程建立在四层技术基座之上，每一层都对应明确的职责：\n\n| 层 | 选型 | 职责 |\n|---|---|---|\n| 渲染框架 | React 19 + react-dom 19 | 组件树与并发特性（StrictMode） |\n| 路由 | react-router-dom 7 | 声明式路由树、懒加载、URL 参数驱动 |\n| 状态管理 | Zustand 5 | 15 个独立 store，细粒度 selector 订阅 |\n| 构建 | Vite 6 + Tailwind CSS 4 | 代码分割、`@` 别名、开发代理 |\n\n`web/src/` 内部按功能域划分六个目录：`pages/` 存放 19 个页面级组件；`components/` 按业务域（chat、settings、billing、monitor…）与通用层（common、ui）组织；`stores/` 集中全部 Zustand store；`api/` 封装 REST 客户端与 WebSocket 单例；`hooks/` 与 `utils/` 承载可复用逻辑（主题、路由恢复、消息快照缓存等）；`lib/` 存放纯函数领域逻辑（交互模式规范化、消息时间线）。\n\n开发模式下 Vite 将 `/api` 与 `/ws` 代理到 `127.0.0.1:3000`，生产模式则由主服务（Hono）直接托管 `web/dist` 产物——前端不独立部署，这是"单进程多面"架构的自然延伸。\n\nSources: [vite.config.ts](web/vite.config.ts#L23-L39)、[web.ts](src/web.ts#L1338-L1368)\n\n## 路由设计：声明式路由树与懒加载策略\n\n路由的单一事实来源是 `web/src/App.tsx` 中的 `createRoutesFromElements`。整棵路由树可分为三类：**公开路由**、**受保护路由（带布局）** 与**重定向路由**。\n\n```mermaid\nflowchart TB\n    subgraph Public["公开路由（无守卫）"]\n        A1["/login 登录"]\n        A2["/register 注册"]\n        A3["/setup 首次初始化"]\n        A4["/setup/providers 提供商（AuthGuard）"]\n        A5["/setup/channels 渠道（AuthGuard）"]\n    end\n\n    subgraph Guarded["AuthGuard + AppLayout（受保护）"]\n        B1["/chat/:groupFolder? 工作台"]\n        B2["/agent-profiles 智能体"]\n        B3["/tasks 任务"]\n        B4["/capabilities/:section? 能力库"]\n        B5["/settings 设置"]\n        B6["/usage 用量"]\n        B7["/billing 账单"]\n        B8["/memory Workspace Memory"]\n        B9["/monitor 运行状态（权限）"]\n        B10["/users 用户管理（权限）"]\n    end\n\n    subgraph Redirect["重定向"]\n        C1["/groups → /chat"]\n        C2["/skills → /capabilities/skills"]\n        C3["/mcp-servers → /capabilities/mcp"]\n        C4["/plugins → /capabilities/plugins"]\n        C5["/ 与 * → /chat"]\n    end\n\n    A4 --> Guarded\n    A5 --> Guarded\n    B4 --> D1["skills 子页"]\n    B4 --> D2["mcp 子页"]\n    B4 --> D3["plugins 子页"]\n```\n\n**懒加载是路由设计的核心约束**。除登录、注册、设置向导与 `AuthGuard`/`AppLayout` 外，其余全部页面均通过 `React.lazy` 分包。注释中记录了一个关键教训：CapabilitiesPage 静态引入 SkillsPage → MarkdownRenderer → KaTeX + highlight.js 的整条链曾被提升进入入口 chunk（约 1.74MB 原始、517KB gzip），导致即使访问 `/login` 也要下载它。因此除 ChatPage 外，Memory、Users、Monitor、Capabilities 四个原本同步的页面也被改造为懒加载。\n\nChatPage 的分包策略最为精细：它不再依赖静态 HTML 的 modulepreload，而是在入口执行时调用 `shouldPreloadChatRoute(pathname, hash, APP_BASE)` 判断当前路由是否属于 `/`、`/chat` 或 `/chat/*`——只有命中才立即 `loadChatPage()`。该策略有专门的单元测试矩阵覆盖（含 hash 路由、子路径、`/chatty` 这类前缀相似但不应命中的路径）。\n\nSources: [App.tsx](web/src/App.tsx#L21-L75)、[App.tsx](web/src/App.tsx#L105-L264)、[chat-route-preload.ts](web/src/utils/chat-route-preload.ts#L1-L20)、[chat-route-preload.test.ts](web/src/utils/chat-route-preload.test.ts#L1-L27)\n\n### Hash 与 Browser 路由的双轨切换\n\n路由模式并非写死：`shouldUseHashRouter()` 仅在**iOS 独立模式（PWA standalone）**下返回 true，其余环境一律使用 `createBrowserRouter`。`main.tsx` 在入口立即把该决策写入 `window.__HAPPYCLAW_HASH_ROUTER__`，供 `utils/url.ts` 的 `replaceInApp` 在登录跳转、认证失效重定向时选择正确的 URL 形态（hash 模式下保留当前 pathname、用 `#` 承载含 APP_BASE 的完整路由）。\n\n配套的 `APP_BASE` 归一化逻辑（`normalizeBasePath`）保证任意 `VITE_BASE_PATH`（如子路径部署 `/happyclaw/`）下，`withBasePath`、`stripBasePath` 都能正确换算——REST 请求、WebSocket URL、SPA 跳转共用同一套基准路径语义。\n\nSources: [url.ts](web/src/utils/url.ts#L1-L55)、[main.tsx](web/src/main.tsx#L8-L17)\n\n### 旧路由重定向与设置页信息架构收敛\n\n路由重定向不仅是兼容旧链接，更承载着**信息架构收敛**的意图。设置页（`SettingsPage`）曾吞纳 groups、agent-profiles、memory、skills、mcp-servers、plugins、usage 等产品资源页，现通过 `LEGACY_TAB_ROUTES` 表将 `?tab=xxx` 参数映射回独立路由（如 `skills → /capabilities/skills`），在服务端契约测试中该表结构被显式断言。能力库则收敛为 `/capabilities/:section?` 单一路由下的三个子页（skills/mcp/plugins），同时保留 `/skills`、`/mcp-servers`、`/plugins` 三个 301 式重定向入口。\n\nSources: [SettingsPage.tsx](web/src/pages/SettingsPage.tsx#L28-L71)、[CapabilitiesPage.tsx](web/src/pages/CapabilitiesPage.tsx#L8-L19)、[tests/frontend-settings-information-architecture.test.ts](tests/frontend-settings-information-architecture.test.ts#L20-L60)\n\n## 应用外壳：响应式布局与全局事件接线\n\n`AppLayout` 是全部受保护路由的共享外壳，其核心设计是**"桌面/移动双导航 + 全局 WebSocket 监听"**：\n\n```mermaid\nflowchart TB\n    subgraph Shell["AppLayout（h-screen flex）"]\n        S1["UnifiedSidebar<br/>仅桌面 lg:block 条件挂载"]\n        S2["ConnectionBanner"]\n        S3["main（ErrorBoundary + Outlet）"]\n        S4["BottomTabBar<br/>仅移动端，聊天详情隐藏"]\n    end\n\n    subgraph Global["全局 WS 监听（AppLayout useEffect）"]\n        G1["billing_update → 计费 store"]\n        G2["runner_state / run_started / run_finished → groups + chat store"]\n        G3["active_run_snapshot → 权威运行态恢复"]\n        G4["group_created → 刷新侧边栏与任务"]\n        G5["agent_status → 子 Agent 状态"]\n    end\n\n    S1 --> Shell\n    S4 --> Shell\n    Shell --> Global\n```\n\n几个值得注意的架构决策：\n\n- **桌面侧边栏条件挂载而非 CSS 隐藏**。`{isDesktop && <UnifiedSidebar/>}` 确保移动端不渲染整棵侧边栏树（每个工作区一个 DropdownMenu/Tooltip），避免无谓的数据加载与内存开销。侧边栏仅在聊天路由可展开，其余路由强制折叠；`Cmd/Ctrl+B` 切换。\n- **全局 WS 连接在 AppLayout 建立**（`wsManager.connect()`），而非 ChatView——确保计费、监控等非聊天页面也能接收实时推送。\n- **移动端隐藏底部 Tab 栏**的判定 `hideMobileTabBar = /^\\/chat\\/.+/.test(pathname)` 使聊天详情页获得全屏沉浸空间；Tab 栏本身根据滚动方向压缩（`useScrollDirection`）。\n- **导航项单一数据源** `nav-items.ts`：`filterNavItems(billingEnabled)` 统一控制桌面侧边栏与移动 Tab 栏，计费入口在 `billingEnabled=false` 时同时从两处消失。\n- **document.title 实时反映未读回复数**，聚合 `unreadReplies` 并在移动端隐藏页时递增。\n\nSources: [AppLayout.tsx](web/src/components/layout/AppLayout.tsx#L16-L43)、[AppLayout.tsx](web/src/components/layout/AppLayout.tsx#L63-L105)、[AppLayout.tsx](web/src/components/layout/AppLayout.tsx#L154-L186)、[nav-items.ts](web/src/components/layout/nav-items.ts#L19-L31)、[BottomTabBar.tsx](web/src/components/layout/BottomTabBar.tsx#L8-L17)\n\n## API 层：REST 客户端与 WebSocket 管理器\n\n### REST：统一超时、认证跳转与上传估算\n\n`api/client.ts` 的 `apiFetch` 是所有 HTTP 请求的唯一通道，统一处理三类边界：\n\n1. **超时控制**：默认 8 秒（FormData 上传 120 秒），超时抛 `{status:408}`；\n2. **401/403 语义跳转**：401 跳登录（避免已在登录页时死循环），403 且 `code === \'PASSWORD_CHANGE_REQUIRED\'` 强制跳 `/settings`；\n3. **上传超时按字节推算**：`computeUploadTimeoutMs` 以 20KB/s 的保守下限估算，最少 120 秒、最多 10 分钟，防止大文件在慢网络被固定超时误杀。\n\n`api` 对象进一步封装 get/post/put/patch/delete/uploadFiles，其中 `uploadFiles` 故意不设 `Content-Type`，让浏览器自动附加 multipart boundary。\n\n### WebSocket：单例管理器与指数退避\n\n`api/ws.ts` 的 `WsManager` 是全局唯一实例，维护 `handlers: Map<type, Set<handler>>` 的订阅注册表，任何组件/页面可 `wsManager.on(type, fn)` 订阅、返回退订函数。连接治理包含三层机制：\n\n- **指数退避重连**：1 秒起步、30 秒封顶，`online` 事件立即重连并重置退避，`offline` 取消待定重连；\n- **认证失效关闭码**：1008（Policy Violation）与 4001（自定义认证错误）不重连，直接 `replaceInApp(\'/login\')`；\n- **事件类型分派**：`onmessage` 解析 JSON 后按 `data.type` 广播，与后端 StreamEvent 类型体系对齐。\n\nSources: [client.ts](web/src/api/client.ts#L11-L71)、[client.ts](web/src/api/client.ts#L77-L125)、[ws.ts](web/src/api/ws.ts#L5-L99)、[ws.ts](web/src/api/ws.ts#L102-L121)\n\n## 状态管理：Zustand store 全景与 chat store 的 run 栅栏\n\n前端状态由 15 个 Zustand store 承载，按领域划分：\n\n| Store | 职责 | 关键状态 |\n|---|---|---|\n| `auth` | 会话、用户、权限、外观配置 | authenticated、user、setupStatus、appearance |\n| `chat` | **核心对话状态机**（4,400+ 行） | messages、streaming、waiting、agents、followUps、drafts、unreadReplies |\n| `groups` | 工作区元数据与 Runner 状态 | groups、runnerStates、adminHostOnlyMode |\n| `billing` | 计费开关与账单 | billingEnabled、plans、transactions |\n| 其余 | tasks、skills、plugins、mcp-servers、usage、monitor、users、files、channel-accounts、container-env、agent-profiles | 各领域 CRUD 状态 |\n\n**chat store 是整个前端的复杂度中心**，其设计遵循三条原则：\n\n1. **窄 selector 订阅**：ChatView 与 UnifiedSidebar 均显式注释"整 store 订阅会让页面跟着流式输出每帧重渲染"，因此全部使用 `useChatStore((s) => s.xxx)` 粒度选择；\n2. **请求去重**：`loadMessagesInFlight` Map 以 `jid\\0before` 为键合并首屏并发请求（ChatPage 路由解析与 ChatView 挂载各发一次）；消息按时间戳合并、分页游标基于首条消息时间戳；\n3. **run 栅栏（run fencing）**：这是流式正确性的基石。`run-lifecycle.ts` 提供纯函数族——`applyRunStarted`/`applyRunFinished` 维护 `activeRuns` Map（`chatJid → {runId, phase}`），`shouldApplyRunScopedPayload` 规定**只有 runId 精确匹配的流式载荷才能写入状态**。`active_run_snapshot` 在每次 WS 连接时先于 stream snapshot 到达，作为权威真相：被替换或消失的 run 的本地投影必须先被清除，`shouldDiscardStreamForAuthoritativeRun` 负责判定。\n\nSources: [chat.ts](web/src/stores/chat.ts#L400-L521)、[chat.ts](web/src/stores/chat.ts#L1714-L1764)、[run-lifecycle.ts](web/src/stores/run-lifecycle.ts#L20-L69)、[run-lifecycle.ts](web/src/stores/run-lifecycle.ts#L99-L107)\n\n### 流式状态的三重恢复路径\n\n前端需要应对页面刷新与 WS 断线重连两种中断，chat store 提供了三条恢复路径，各自拥有明确的权威性边界：\n\n| 场景 | 机制 | 权威来源 |\n|---|---|---|\n| 页面刷新/冷启动 | `restoreActiveState()` 拉取 `/api/status`，对 active 群组先 `refreshMessages` 再推断 waiting | 后端 `/api/status` |\n| WS 断线重连 | `handleActiveRunSnapshot` 以 `active_run_snapshot` 重建 activeRuns → `handleStreamSnapshot` 用后端推送的 `stream_snapshot` 恢复 StreamingDisplay | 后端推送 |\n| 消息即时到达 | `handleWsNewMessage` 通过 `new_message` 事件立即追加本地消息，消除轮询延迟 | WebSocket 推送 |\n\n`handleStreamSnapshot` 将快照重建为完整的 `StreamingState`（partialText、thinkingText、activeTools、recentEvents、taskStates、todos…），并同样经过 `shouldApplyRunScopedPayload` 栅栏——没有 runId 身份的旧快照不会被误应用。`restoreActiveState` 则有一条关键注释：**"绝不从消息形状推断 waiting"**——warm 进程与 retry/backoff 没有精确的 terminal 事件，只有 `hasExactQueryAttempt`（`queryInFlight && queryId`）才能恢复等待态，否则会出现没有任何 `run_finished` 能关闭的永久 spinner。\n\nSources: [chat.ts](web/src/stores/chat.ts#L4002-L4100)、[chat.ts](web/src/stores/chat.ts#L4103-L4151)、[chat.ts](web/src/stores/chat.ts#L4251-L4260)、[run-lifecycle.ts](web/src/stores/run-lifecycle.ts#L16-L18)\n\n## 认证与权限门禁\n\n认证决策集中在 `AuthGuard` 组件，它包裹所有受保护路由（含嵌套在 AppLayout 之外的 setup/providers 与 setup/channels），并按优先级顺序裁决：\n\n```mermaid\nflowchart TD\n    A["进入受保护路由"] --> B{"checking？"}\n    B -- "是，超过 12s" --> B1["超时卡片：刷新 / 去登录"]\n    B -- "否" --> C{"initialized === false？"}\n    C -- "是" --> C1["→ /setup"]\n    C -- "否" --> D{"未认证？"}\n    D -- "是" --> D1["→ /login（带 from 状态）"]\n    D -- "否" --> E{"must_change_password？"}\n    E -- "是" --> E1["→ /settings"]\n    E -- "否" --> F{"admin 且 needsSetup？"}\n    F -- "是" --> F1["→ /setup/providers"]\n    F -- "否" --> G{"权限不足？"}\n    G -- "是" --> G1["→ /chat"]\n    G -- "否" --> H["渲染子路由"]\n```\n\n两个关键的性能与健壮性设计：\n\n- **认证预热（auth prewarm）**：`index.html` 在 HTML 解析阶段就发出 `fetch(\'%BASE_URL%api/auth/me\')` 存入 `window.__authPrewarm`，`stores/auth.ts` 的 `checkAuth` 一次性消费该 Promise，省掉"入口 JS 执行完才发认证请求"的一整跳串行 RTT；任何异常静默回落到常规重试。\n- **单次检查守卫**：`checkedRef` 确保 `checkAuth` 只触发一次（StrictMode 双执行下尤为重要）；12 秒超时展示可操作的错误卡片而非无限 LogoLoading。\n\n权限门禁支持三种形态：`requireAdmin`（角色级）、`requiredPermission`（单权限，如 `/monitor` 的 `manage_system_config`）、`requiredAnyPermissions`（任一权限，如 `/users` 的 manage_users/manage_invites/view_audit_log）。设置页内的 tab 级过滤则复用同一套权限判定，按用户角色动态裁剪"系统配置""管理后台"分区。\n\nSources: [AuthGuard.tsx](web/src/components/auth/AuthGuard.tsx#L26-L107)、[index.html](web/index.html#L34-L44)、[auth.ts](web/src/stores/auth.ts#L179-L200)、[SettingsNav.tsx](web/src/components/settings/SettingsNav.tsx#L120-L135)\n\n## 性能与缓存策略\n\n前端性能优化贯穿"构建期 → 运行时 → 服务端"三层：\n\n| 层 | 策略 | 实现 |\n|---|---|---|\n| 构建期 | 路由级代码分割 | 除 4 个公开页与外壳外全部 `React.lazy`；ChatPage 按路由策略预加载 |\n| 构建期 | 重型依赖延迟加载 | xterm.js（~488KB）在终端面板挂载时才 `lazy` 引入 |\n| 运行时 | 窄 selector 订阅 | 避免流式输出导致的整页重渲染 |\n| 运行时 | rAF 批处理 | `text_delta`/`thinking_delta` 在 `requestAnimationFrame` 内累积合并，主会话与子 Agent 分别成批 flush |\n| 运行时 | 条件挂载 | 移动端不渲染桌面侧边栏子树 |\n| 服务端 | 分级缓存头 | `/assets/*`、`/fonts/*` 不可变缓存；`/icons/*` 30 天；SPA shell、manifest、sw.js 禁止缓存 |\n| 服务端 | 条件请求兜底 | 自定义 ETag 中间件，命中 304 空体 |\n\n**主题系统的无闪烁启动**也属于性能架构的一部分：`index.html` 内联脚本在 React 挂载前同步读取 localStorage（`happyclaw-theme`、`happyclaw-color-scheme`、`happyclaw-font-style`）并给 `<html>` 加类，`useTheme` 钩子通过 `useSyncExternalStore` 响应式同步——既避免首屏闪白，也避免主题闪烁。PWA 方面，Service Worker 已整体退役：`sw.js` 变为自毁式清理脚本（`skipWaiting` + `unregister`），manifest 保留供 iOS standalone 使用，旧安装的缓存由 `cleanupLegacyPwaArtifacts` 在入口异步清扫。会话级缓存则迁移到 IndexedDB：`messageSnapshotCache` 为每个会话子 Agent 持久化最近 100 条消息（用户级数据，登录/登出时按用户清空）。\n\nSources: [web.ts](src/web.ts#L1260-L1297)、[web.ts](src/web.ts#L1300-L1368)、[chat.ts](web/src/stores/chat.ts#L2516-L2549)、[ChatView.tsx](web/src/components/chat/ChatView.tsx#L53-L57)、[index.html](web/index.html#L49-L65)、[messageSnapshotCache.ts](web/src/utils/messageSnapshotCache.ts#L83-L110)、[main.tsx](web/src/main.tsx#L19-L23)\n\n## 总结与阅读建议\n\n前端架构的核心矛盾是**实时性与性能的平衡**：Zustand 提供细粒度的状态订阅，run 栅栏保证流式事件在断线重连后依然正确，路由级代码分割把首屏体积控制在登录页即可用的范围。三层数据流（REST 命令、WS 推送、IndexedDB 快照）各有明确权威边界，这使前端即使面对 4,400 行的 chat store 也能保持可维护性。\n\n建议的继续阅读路径：\n\n- 前端实时渲染的细节（消息气泡、流式卡片、终端、Markdown 渲染）见 [实时消息流、终端与流式渲染体验](28-shi-shi-xiao-xi-liu-zhong-duan-yu-liu-shi-xuan-ran-ti-yan)；\n- 前端消费的 StreamEvent 类型体系与类型同步机制见 [StreamEvent 流式事件体系与类型同步](11-streamevent-liu-shi-shi-jian-ti-xi-yu-lei-xing-tong-bu)；\n- 路由背后的服务端 API 面（Hono 路由挂载、WebSocket 三类通道）见 [主服务架构与核心模块划分](9-zhu-fu-wu-jia-gou-yu-he-xin-mo-kuai-hua-fen)；\n- 前端权限门禁所依据的 RBAC 模型见 [RBAC 权限模型与资源隔离](24-rbac-quan-xian-mo-xing-yu-zi-yuan-ge-chi)。';export{e as default};

const n="MiniCode 将 MCP（Model Context Protocol）视为第二类扩展机制：与「技能系统」注入的是本地工作流说明不同，MCP 注入的是**可执行的远端能力**——工具（tools）、资源（resources）与提示模板（prompts）。本页剖析这条集成链路的四个核心环节：**配置加载与作用域合并 → 双传输客户端（stdio 子进程 / streamable HTTP）→ 协议协商与初始化握手 → 工具注入与状态观测**。整条链路被设计为\"启动非阻塞、单服务器失败隔离\"，因此即使某个 MCP server 崩溃，MiniCode 主流程也不会被拖垮。\n\n## 配置模型：四源合并与字段语义\n\nMCP server 的配置由 `McpServerConfig` 类型承载，它同时覆盖本地子进程与远程 HTTP 两种形态：\n\n| 字段 | 类型 | 语义 |\n|---|---|---|\n| `command` / `args` | `string` / `string[]` | stdio 形态：要 spawn 的可执行文件与参数 |\n| `url` | `string` | HTTP 形态：streamable-http 端点；与 `command` 二选一 |\n| `env` | `Record<string, string \\| number>` | 追加到子进程环境变量（继承 `process.env`） |\n| `headers` | `Record<string, string \\| number>` | HTTP 请求头，值支持 `$VAR` 环境变量插值 |\n| `cwd` | `string` | 子进程工作目录，相对启动目录解析 |\n| `enabled` | `boolean` | 设为 `false` 时跳过连接，状态标记为 `disabled` |\n| `protocol` | `'auto' \\| 'content-length' \\| 'newline-json' \\| 'streamable-http'` | 强制指定协议或交给协商 |\n\nSources: [config.ts](src/config.ts#L13-L22)\n\n配置的来源并非单一文件，而是按优先级**深度合并**四层：`~/.claude/settings.json`（兼容 Claude Code）→ `~/.mini-code/mcp.json`（全局）→ 项目根目录 `.mcp.json`（项目级）→ `~/.mini-code/settings.json`（最高优先级）。`mergeSettings` 对 `env` 与 `headers` 做逐键合并，因此低优先级来源的单个环境变量可以被高优先级来源覆盖，而其余变量保留：\n\n```mermaid\nflowchart LR\n    A[~/.claude/settings.json] --> M[mergeSettings]\n    B[~/.mini-code/mcp.json] --> M\n    C[.mcp.json 项目级] --> M\n    D[~/.mini-code/settings.json] --> M\n    M --> E[RuntimeConfig.mcpServers]\n    E --> F[createMcpBackedTools]\n```\n\nSources: [config.ts](src/config.ts#L139-L188)\n\n值得注意的是 `loadEffectiveSettings` 使用 `Promise.all` 并行读取四个文件，任何文件缺失（`ENOENT`）都会被吞掉并返回空对象，保证\"没有配置文件也能启动\"。\n\nSources: [config.ts](src/config.ts#L72-L83)\n\n## 客户端抽象：stdio 子进程与 streamable HTTP 的统一门面\n\n两种传输形态被收敛到同一个接口 `McpClientLike` 之后，上层调用方（工具包装、资源枚举、prompt 拉取）完全不感知底层是子进程还是 HTTP 请求。该接口定义了九项能力：`start` / `getProtocol` / `getServerName` / `listTools` / `listResources` / `readResource` / `listPrompts` / `getPrompt` / `callTool` / `close`。\n\nSources: [mcp.ts](src/mcp.ts#L329-L340)\n\n**stdio 客户端（`StdioMcpClient`）** 通过 `node:child_process.spawn` 拉起子进程，将宿主环境变量与配置 `env` 合并后传入；stderr 只保留最近 8 行非空输出，用于错误报告——这既控制了内存，又能在进程异常退出时给出可诊断的上下文。spawn 本身是异步竞态敏感的：代码用 `once('spawn')` / `once('error')` 配对等待，确保 ENOENT 等启动失败能被及时捕获并格式化。\n\nSources: [mcp.ts](src/mcp.ts#L489-L580)\n\n**HTTP 客户端（`StreamableHttpMcpClient`）** 基于 `fetch` 实现，向配置的 `url` 发 POST，请求头固定声明 `accept: application/json, text/event-stream` 以对齐 MCP streamable HTTP 的内容协商约定；`headers` 配置先经过 `interpolateEnv` 做 `$VAR` / `${VAR}` 插值，再叠加从令牌文件加载的 `Authorization: Bearer`。每个请求受 `AbortController` 超时保护，超时错误会被包装为带方法名的可读信息。\n\nSources: [mcp.ts](src/mcp.ts#L912-L973)\n\n## stdio 协议协商：framing 探测、超时重试与持久化缓存\n\nstdio 传输的最大兼容性挑战在于 JSON-RPC 消息的**分帧格式**并不统一：官方 MCP 标准采用 `Content-Length` 头 + 正文的 framing，而部分实现（尤其以换行为分隔的轻量服务器）只接受每行一个 JSON。MiniCode 的解决方案是一套三级协商策略：\n\n1. **强制优先**：配置 `protocol: 'content-length'` 或 `'newline-json'` 时，候选列表只有该协议，不做协商；\n2. **缓存优先**：首次连接成功后，实际协商结果按 `serverName::endpoint` 为键写入 `~/.mini-code/mcp-protocol-cache.json`；后续启动以缓存值为首选（若缓存为 `newline-json`，候选顺序反转为 `['newline-json', 'content-length']`）；\n3. **默认探测**：无任何提示时，默认先试 `content-length`，失败再回退 `newline-json`。\n\n```mermaid\nflowchart TD\n    A[getProtocolCandidates] --> B{config.protocol 强制?}\n    B -->|content-length| C[['content-length']]\n    B -->|newline-json| D[['newline-json']]\n    B -->|auto/未设置| E{preferredProtocol 缓存?}\n    E -->|newline-json| F[['newline-json','content-length']]\n    E -->|无| G[['content-length','newline-json']]\n    C --> H[尝试 initialize]\n    D --> H\n    F --> H\n    G --> H\n    H --> I{1200ms 探测超时?}\n    I -->|是| J[同协议 10s 完整重试]\n    I -->|否| K[连接成功]\n    J --> K\n```\n\nSources: [mcp.ts](src/mcp.ts#L406-L442) [mcp.ts](src/mcp.ts#L476-L487)\n\n这里的探测时序设计非常精细：首次尝试使用 **1200ms 的短超时**（`MCP_INITIALIZE_PROBE_TIMEOUT_MS`），因为冷启动阶段\"沉默\"通常意味着 framing 不匹配；但短超时也可能误伤正常启动较慢的服务器，因此**对超时错误会先关闭进程、用完整 10 秒超时重试同一协议一次**，仍失败才切换到下一个候选。这样既快速识别错误协议，又避免了冷启动误判。最终协商结果写回缓存时通过 `protocolCacheDirty` 标记批量持久化，且写入失败被静默忽略——缓存只是性能优化，不是正确性依赖。\n\nSources: [mcp.ts](src/mcp.ts#L63-L69) [mcp.ts](src/mcp.ts#L1051-L1108)\n\n**HTTP 形态不参与协商**：协议选择在 `createMcpBackedTools` 中一次性决定——配置 `protocol: 'streamable-http'` 或存在 `url` 即走 HTTP 客户端，否则走 stdio。\n\nSources: [mcp.ts](src/mcp.ts#L1009-L1030)\n\n## 初始化握手与 JSON-RPC 生命周期\n\n连接建立后，客户端以 `protocolVersion: '2024-11-05'`、`clientInfo: mini-code 0.1.0` 发起 `initialize` 请求，成功后发送 `notifications/initialized` 通知，完成 MCP 规范要求的握手序列。\n\nSources: [mcp.ts](src/mcp.ts#L455-L474) [mcp.ts](src/mcp.ts#L797-L817)\n\n消息收发层采用经典 JSON-RPC 2.0 模式，围绕一个 `pending: Map<number, PendingRequest>` 展开：\n\n- **发送**：`request` 分配自增 `id`，注册 `{resolve, reject, timeout}` 后写帧；`notify` 不携带 `id`，发送即完成。stdio 按当前协议写 `Content-Length` 头 + 正文，或单行 JSON + `\\n`；HTTP 则 POST 到端点。\n- **接收**：stdio 侧用增量缓冲区解析。`content-length` 协议查找 `\\r\\n\\r\\n` 分隔符、解析 `Content-Length` 头并等待完整 body；`newline-json` 协议按行切分。HTTP 侧直接解析响应 JSON，空响应返回空对象。\n- **超时**：每个请求默认 5 秒超时（`initialize` 用 10 秒），超时后从 `pending` 移除并 reject，错误信息附带最近 stderr 便于诊断。进程 `exit` / `error` 时，所有挂起请求统一 reject。\n\nSources: [mcp.ts](src/mcp.ts#L658-L698) [mcp.ts](src/mcp.ts#L700-L785)\n\n## 工具注入：命名改写、schema 归一化与条件 helper\n\n`createMcpBackedTools` 是注入管线的核心：对每个启用的 server 依次执行 `start()` → `listTools()` → 将每个工具描述符包装成统一的 `ToolDefinition`。包装过程包含三个关键变换：\n\n1. **命名改写**：`sanitizeToolSegment` 将 server 名与工具名统一小写、非字母数字字符替换为下划线，最终拼成 `mcp__<server>__<tool>`。该前缀约定同时出现在系统提示词中（\"names prefixed like mcp__server__tool\"），让模型能识别工具来源。\n2. **Schema 归一化**：`normalizeInputSchema` 对缺失或非法 schema 回退为 `{ type: 'object', additionalProperties: true }`，避免空 schema 导致 Anthropic API 拒绝工具定义。\n3. **闭包委托**：每个包装工具的 `run` 闭包捕获对应的 `client` 与原始工具名，调用 `client.callTool` 后将 MCP 的 `content` / `structuredContent` / `isError` 响应结构格式化为扁平文本——`isError` 映射为 `ok: false`，使错误能沿 MiniCode 统一的 `ToolResult` 通道返回。\n\nSources: [mcp.ts](src/mcp.ts#L111-L131) [mcp.ts](src/mcp.ts#L1061-L1077) [mcp.ts](src/mcp.ts#L149-L183)\n\n**resources 与 prompts 采用\"有条件注入\"策略**：只有某个 server 实际发布了资源或提示时，才会注册对应的四个 helper 工具（`list_mcp_resources` / `read_mcp_resource` / `list_mcp_prompts` / `get_mcp_prompt`），避免在没有相应能力时污染工具列表。检测通过 `Promise.allSettled` 并行完成，单个 server 的 `listResources` 失败不影响 `listPrompts` 结果。这些 helper 工具的 `run` 内部通过 `clientsByServer` 按名查找客户端，未知 server 名返回明确的错误提示。\n\nSources: [mcp.ts](src/mcp.ts#L1032-L1048) [mcp.ts](src/mcp.ts#L1110-L1180) [mcp.ts](src/mcp.ts#L1182-L1268)\n\n注入的最后一步发生在 `hydrateMcpTools`：`ToolRegistry.addTools` 按名称去重追加（避免重名工具重复注册）、`setMcpServers` 写入连接状态元数据、`addDisposer` 注册关闭回调。`dispose` 会 `Promise.all` 关闭所有存活客户端，确保进程退出时子进程被 kill。\n\nSources: [tools/index.ts](src/tools/index.ts#L68-L80) [tool.ts](src/tool.ts#L76-L89)\n\n```mermaid\nflowchart LR\n    A[mcpServers 配置] --> B[createMcpBackedTools]\n    B --> C[StdioMcpClient / StreamableHttpMcpClient]\n    C --> D[listTools]\n    D --> E[包装为 ToolDefinition<br/>mcp__server__tool]\n    E --> F[ToolRegistry.addTools]\n    C --> G[listResources / listPrompts]\n    G --> H[条件注册 helper 工具]\n    F --> I[Anthropic 适配器 tools 映射]\n    H --> I\n    I --> J[模型可见]\n```\n\nSources: [anthropic-adapter.ts](src/anthropic-adapter.ts#L330-L340)\n\n## 状态观测与系统提示注入\n\nMCP 连接状态贯穿三层展示：**启动横幅**、**`/mcp` 斜杠命令**与**系统提示词**。`McpServerSummary` 携带 `connecting / connected / error / disabled` 四种状态及工具/资源/提示计数，`summarizeMcpServers` 将其聚合为总数与各状态计数，供横幅与 TUI 状态行消费。\n\nSources: [mcp-status.ts](src/mcp-status.ts#L11-L35) [tty-app.ts](src/tty-app.ts#L242-L252)\n\n`/mcp` 命令逐行渲染每个 server 的 `status`、`tools`、`resources`、`prompts`、`protocol` 与错误信息；无配置时提示三个候选配置文件位置。`/status` 则汇总运行时视角的 MCP 配置数量。\n\nSources: [cli-commands.ts](src/cli-commands.ts#L226-L247) [cli-commands.ts](src/cli-commands.ts#L249-L258)\n\n系统提示词是模型感知 MCP 的主通道：`buildSystemPrompt` 先列出所有已配置 server（含状态与计数），再对\"至少有一个已连接 server\"的情形追加能力提示——明确告知模型 MCP 工具已在工具列表中以 `mcp__server__tool` 前缀暴露、可用的资源/提示 helper 工具等。由于工具列表本身由 Anthropic 适配器直接映射，模型实际调用并不依赖提示词，提示词只是引导发现路径。\n\nSources: [prompt.ts](src/prompt.ts#L48-L90)\n\n## 认证与令牌管理\n\n远程 MCP 的认证刻意保持轻量：`minicode mcp login <name> --token <bearer-token>` 将令牌写入 `~/.mini-code/mcp-tokens.json`（内存中以 `Map` 缓存），HTTP 客户端启动时读取并附加 `Authorization: Bearer` 头。`headers` 配置中的值支持 `$VAR` 环境变量插值，因此也可以把令牌放在环境变量中而不落盘。当服务器返回 `WWW-Authenticate` 挑战时，客户端会提取其中的 `resource_metadata` 与 `authorization_uri` 提示，帮助用户理解 OAuth 流程的下一步——但**完整 OAuth 回调 + refresh 状态机被有意排除**，以符合 MiniCode 的轻量架构目标。\n\nSources: [mcp.ts](src/mcp.ts#L314-L327) [mcp.ts](src/mcp.ts#L342-L355) [config.ts](src/config.ts#L42-L43) [manage-cli.ts](src/manage-cli.ts#L169-L200)\n\n## 韧性设计：启动非阻塞与失败隔离\n\nMCP 集成贯穿始终的工程原则是\"**扩展能力不应成为主流程的脆弱点**\"，体现在四个层面：\n\n| 场景 | 处理方式 |\n|---|---|\n| 配置/令牌文件缺失 | `ENOENT` 吞掉，返回空对象 |\n| 单个 server 启动失败 | 捕获后记为 `error` 状态并继续处理下一个 server |\n| 整体注入失败 | `hydrateMcpTools` 外层 `.catch(() => {})`，保证 CLI 正常进入交互 |\n| 子进程中途退出 | 统一 reject 所有挂起请求，附带 stderr 摘要 |\n| 协议缓存写失败 | 静默忽略，下次重新协商 |\n\nSources: [mcp.ts](src/mcp.ts#L1088-L1101) [mcp.ts](src/mcp.ts#L73-L102) [index.ts](src/index.ts#L72-L78) [config.ts](src/config.ts#L58-L62)\n\n这种\"尽力而为\"的设计让 MCP 从\"可选增强\"而非\"硬依赖\"的角度融入系统——用户即使配置了不可达的 server，也能正常使用全部内置工具。\n\n## 小结与延伸阅读\n\n回顾全链路：**四源配置合并 → 双传输客户端（stdio 子进程 / streamable HTTP）→ stdio framing 协商（1200ms 探测 + 10s 重试 + 持久化缓存）→ MCP 初始化握手 → 工具/资源/提示注入 → 状态观测与提示词引导**。这条链路与系统其他模块的接口点包括：`ToolRegistry` 作为工具的唯一注册中心（见 [统一工具协议](8-tong-gong-ju-xie-yi-zhu-ce-schema-xiao-yan-yu-ji-zhong-zhi-xing)）、`ToolResult` 作为统一的执行结果通道（见 [代理循环](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng)）、系统提示词注入机制（见 [结构化消息模型](9-jie-gou-hua-xiao-xi-mo-xing-jiao-se-lei-xing-yu-gong-ju-diao-yong-bian-pai)）。若想了解 MCP 配置文件的写入方式与 `/mcp` 管理命令的完整用法，可参考 [配置文件体系](3-pei-zhi-wen-jian-ti-xi-settings-json-mcp-json-yu-mu-lu-fu-gai)；若关注 MCP server 与技能系统的并列关系，可对照 [技能系统](21-ji-neng-xi-tong-skill-md-de-duo-ji-fa-xian-yu-jia-zai)。";export{n as default};

const e='Miniclaw 中每条会话（Workspace）在同一时刻只允许一个 Agent Runner 进程或容器在执行，这一不变量由 `GroupQueue` 单独守护。本文剖析该队列的完整生命周期模型：入队调度如何保证串行化、失败后如何以指数退避安全重试、暖 Runner 如何用两套时钟追踪活性，以及当 Runner 真正"卡死"时，系统如何通过候选收集、CPU 探针与身份重验证完成无副作用恢复。理解本页需要先掌握 [Host 与 Container 双执行模式](11-host-yu-container-shuang-zhi-xing-mo-shi-rong-liang-chao-shi-yu-nuan-runner) 中的执行模式概念。\n\nSources: [group-queue.ts](src/group-queue.ts#L197-L200)\n\n## 串行化核心：GroupState 与序列化键\n\n`GroupQueue` 内部维护一张 `Map<string, GroupState>`，键为 chat JID，值是该会话的全部运行时状态。`GroupState` 并非简单的布尔开关，而是一组相互配合的字段：`active` 标记是否有 Runner 在运行，`runnerGeneration` 是每次 `runForGroup`/`runTask` 启动时递增的单调代数编号，`queryInFlight`/`queryId` 追踪当前逻辑查询，`lastActivityAt` 记录最近一次可观测输出时间，而 `ipcOwedSinceAt` 则是专门为 IPC 注入建立的"债务时钟"。这些字段共同构成后续卡死检测与重试验证的身份指纹。\n\nSources: [group-queue.ts](src/group-queue.ts#L119-L193)\n\n串行化的边界不是 JID 本身，而是**序列化键**。宿主通过 `setSerializationKeyResolver` 把每个 JID 解析到其 Workspace folder——这意味着共享同一 folder 的兄弟 JID（例如 Web 会话与绑定该 folder 的飞书会话）会被视为同一条串行车道；而 `#agent:{id}`、`#task:{id}` 虚拟 JID 则各自拥有独立车道。`findActiveRunnerFor` 遍历所有活跃状态，只要任一活跃 Runner 的序列化键相同，新请求就必须排队。\n\nSources: [group-queue.ts](src/group-queue.ts#L859-L871), [index.ts](src/index.ts#L20645-L20665)\n\n```mermaid\nflowchart LR\n    subgraph 序列化键解析\n        A["feishu:xxx<br/>telegram:xxx"] -->|"folder 共享"| K["folder-A"]\n        B["web:xxx"] -->|"folder 共享"| K\n        C["folder-A#agent:1"] -->|"独立车道"| K2["folder-A#agent:1"]\n        D["folder-A#task:9"] -->|"独立车道"| K3["folder-A#task:9"]\n    end\n    K --> Q["同一时刻仅一个 Runner"]\n```\n\n## 入队决策：四道门控汇入同一状态机\n\n消息与任务分别经 `enqueueMessageCheck` 和 `enqueueTask` 进入队列，两者经过完全相同的四道门控。第一道是**终态变更丢弃**：若某次策略变更已标记该序列化族为"终态废弃"，入队直接静默丢弃。第二道是**变更暂停**：workspace 删除等破坏性操作通过 `pauseGroupsForMutation` 获取暂停令牌，此期间工作被挂起到 `pendingMessages`/`pendingTasks` 而非丢失。第三道是**活跃 Runner 检查**：注意这里调用的是 `findActiveRunnerFor`——即使请求者自己的 `GroupState` 不活跃，只要序列化兄弟有活跃 Runner，也必须排队，同时还会向那个真正的活跃 Runner 写入 `_drain` 哨兵文件，让它尽早退出而非等满空闲超时。\n\nSources: [group-queue.ts](src/group-queue.ts#L1331-L1389), [group-queue.ts](src/group-queue.ts#L1391-L1487), [group-queue.ts](src/group-queue.ts#L908-L938)\n\n第四道门控是**容量检查**，且 Host 与 Container 两种模式在此分道扬镳。容器模式保留两层显式限额：全局 `maxConcurrentContainers` 上限与按用户计费的并发上限；Host 模式则完全不做应用层限流——注释明确指出这是刻意设计：Claude Agent SDK 的天然进程模型下，序列化键已是唯一准入边界，进程级槽位计数会让空闲但存活的暖会话把无关的飞书话题阻塞长达一个 IDLE_TIMEOUT。通过全部门控后立即启动；否则加入 `waitingGroups` 等待后续 `drainWaiting` 补位调度。\n\nSources: [group-queue.ts](src/group-queue.ts#L873-L895), [group-queue.ts](src/group-queue.ts#L2856-L2911)\n\n| 门控 | 触发条件 | 行为 |\n|---|---|---|\n| 终态丢弃 | 序列化族已被标记 terminal discard | 直接丢弃（任务触发 `onDropped` 回调） |\n| 变更暂停 | 存在未释放的 MutationPauseToken | 挂起为 pending，等待 resume 后 drain |\n| 活跃 Runner | 自身或任意序列化兄弟活跃 | 排队 + 向活跃 Runner 写 `_drain` 哨兵 |\n| 容量不足 | 容器模式超上限；Host 模式永不触发 | 排队等待 `drainWaiting` 补位 |\n\nSources: [group-queue.ts](src/group-queue.ts#L1331-L1389)\n\n## Runner 生命周期：finally 块承载的退出契约\n\n`runForGroup` 与 `runTask` 是两条几乎对称的生命周期主线。启动阶段做五件事：递增 `runnerGeneration`、记录 `runnerRuntime`（host/container）、生成新的 `queryId`（每次 idle→running 转换都换新 UUID）、清空上一轮遗留的债务时钟与重试快照、同步递增全局计数器。随后进入 try 块调用 `processMessagesFn`（消息车道）或 `task.fn()`（任务车道）——这个函数返回 `false` 即表示"本轮失败，光标未推进"，是整个重试机制的触发源。\n\nSources: [group-queue.ts](src/group-queue.ts#L2312-L2358), [group-queue.ts](src/group-queue.ts#L2527-L2568)\n\n真正的精妙之处在 finally 块。它按固定顺序收尾：先清理 IPC 哨兵文件并回收未被确认的 IPC 投递（防止消息因崩溃丢失）；然后如果本次运行中曾有消息被 IPC 注入且用户并未刚点击 Stop，就重新置位 `pendingMessages`——因为注入方已经推进了 DB 游标，若 Runner 崩溃，只有强制重读 DB 才能找回这些消息；接着广播 `runner_exit` 或 `stopped` 终态事件、递减计数器、通知 `onContainerExit` 回调；最后视情况衔接 `drainGroup` 启动下一轮，并唤醒 `stopGroup` 正在等待的 `teardownWaiters`。这个握手设计让停止方无需猜测 Docker kill 是否完成，而是精确等到 finally 执行完毕。\n\nSources: [group-queue.ts](src/group-queue.ts#L2419-L2524), [group-queue.ts](src/group-queue.ts#L411-L439)\n\n```mermaid\nstateDiagram-v2\n    [*] --> Running : enqueue 通过四道门控\n    Running --> Running : IPC 注入 follow-up（不重启）\n    Running --> Draining : processMessages 返回 true 且有 pending\n    Running --> Backoff : 返回 false 且 canScheduleRetry\n    Running --> Idle : 成功且无 pending / 被 Stop\n    Backoff --> Running : retryTimer 到期重新入队\n    Backoff --> Dropped : 超过 MAX_RETRIES=5\n    Draining --> Running : drainGroup 立即续跑\n    Idle --> [*] : 虚拟 JID GC / 保留真实 JID 状态\n```\n\n两个防御性细节值得注意。其一是入口处的重入保护：`runForGroup` 开头检查 `state.active`，若已活跃则直接忽略并依赖活跃 Runner 自己的 finally→drainGroup 收尾，杜绝同一 `GroupState` 上出现第二个并发 Runner（那会导致重复回复与孤儿容器）。其二是**新消息对退避定时器的取代语义**：新用户消息可以合法地在前一次失败的退避到期前启动替换运行，此时 `cancelRetryTimer` 只取消定时器但**保留 `retryCount`**，使一连串 closed/no-reply 结果仍能累计到 `MAX_RETRIES` 触发最终失败。\n\nSources: [group-queue.ts](src/group-queue.ts#L2322-L2344), [group-queue.ts](src/group-queue.ts#L2332-L2337)\n\n## 失败重试：指数退避与三重抑制\n\n当 `processMessagesFn` 返回 `false` 或抛异常，且 `canScheduleRetry` 放行（即不在 shutdown、未被 Stop、不在重启中），`scheduleRetry` 启动指数退避：延迟为 `BASE_RETRY_MS * 2^(retryCount-1)`，即 5s → 10s → 20s → 40s → 80s，最多 5 次。定时器到期后并不直接启动 Runner，而是走标准入队路径（`enqueueMessageCheck` 或 `enqueueTask`），重新经历四道门控——这让重试天然尊重容量限制与变更暂停。\n\nSources: [group-queue.ts](src/group-queue.ts#L115-L117), [group-queue.ts](src/group-queue.ts#L2679-L2771)\n\n退避机制有三个精心设计的边界。第一，**上下文溢出跳过**：`markContextOverflow` 标记的会话（Agent 内部已自行重试 3 次）不再叠加队列级退避，直接归零放弃。第二，**`clearRetryTimer` 与 `cancelRetryTimer` 的语义差异**：前者清定时器并把 `retryCount` 归零（用于成功后清理），后者只取消定时器而保留退避进度——`interruptQuery` 特意使用后者，否则用户一次中断就会让 N 次失败积累的退避进度归零，陷入对同一失败请求的死循环重试。第三，**drainGroup 的互斥护栏**：`drainGroup` 在处理 pendingMessages 前检查 `!state.retryTimer`，因为失败路径上 `scheduleRetry` 与 finally 的 `drainGroup` 都会触发，没有这道护栏就会出现"退避定时器稍后启一个、drain 立刻又启一个"的双重处理。\n\nSources: [group-queue.ts](src/group-queue.ts#L2675-L2677), [group-queue.ts](src/group-queue.ts#L765-L804), [group-queue.ts](src/group-queue.ts#L2813-L2820)\n\n重试期间的用户体验也被显式管理：`processGroupMessages` 在错误出口处调用 `queue.willRetryAfterFailure` 判断——仍会重试的中间轮次**不**向用户广播 `agent_error`（避免每轮一条错误消息加一张中断卡的"消息洪流"），改为补发一个 `status:idle` 流事件让 Web 端停止转圈；只有最终失败才由 `onMaxRetriesExceeded` 回调统一投影：回调使用 `messageRetrySnapshot` 中最后一次失败尝试读取的**精确消息批次**标记 Task Run 失败，确保退避期间新到的提示词不会被误伤。此外 OOM（exit code 137）有独立的恢复通道：连续 2 次 OOM 自动清除会话文件打破死亡循环。\n\nSources: [index.ts](src/index.ts#L9290-L9317), [index.ts](src/index.ts#L20666-L20700), [index.ts](src/index.ts#L9240-L9288), [tests/group-queue-close-retry.test.ts](tests/group-queue-close-retry.test.ts#L36-L60)\n\n| 参数/机制 | 值/行为 | 出处 |\n|---|---|---|\n| 最大重试次数 | `MAX_RETRIES = 5` | [group-queue.ts](src/group-queue.ts#L115-L117) |\n| 退避基数 | `BASE_RETRY_MS = 5000`，指数 `2^(n-1)` | [group-queue.ts](src/group-queue.ts#L2736) |\n| 重试抑制 | shuttingDown / stopRequested / restarting 任一为真 | [group-queue.ts](src/group-queue.ts#L2675-L2677) |\n| 上下文溢出 | 跳过退避，retryCount 归零 | [group-queue.ts](src/group-queue.ts#L2704-L2714) |\n| 用户中断 | 取消定时器但保留退避进度 | [group-queue.ts](src/group-queue.ts#L2053-L2055) |\n\n## 暖 Runner 的双时钟：活动时钟与 IPC 债务时钟\n\n暖 Runner 存活期间，新消息不走冷启动而是经 `sendMessage` 以 IPC 文件注入正在运行的进程。注入成功后有两个关键副作用：`hasIpcInjectedMessages` 置位（供 finally 判断是否需要安全复查），以及刷新 `lastActivityAt` 为注入时刻。后者修复了 issue #618 的缺陷——若沿用"上次输出时间"，一条注入进长时间空闲暖 Runner 的 follow-up 会让卡死检测瞬间误判。与此同时 `ipcOwedSinceAt ??= injectedAt` 建立了**IPC 债务时钟**：它与活动时钟的关键区别在于，普通 Runner 输出（`markRunnerActivity`）只会刷新活动时钟，永远不会刷新债务时钟；只有 `markRunnerQueryIdle` 报告当前查询真正完成时，债务才会清零。\n\nSources: [group-queue.ts](src/group-queue.ts#L1549-L1700), [group-queue.ts](src/group-queue.ts#L1014-L1025), [group-queue.ts](src/group-queue.ts#L1093-L1112)\n\n这套双时钟体系之外还存在第三重看护：`container-runner` 中的外层 watchdog。它的超时值由 `resolveRunnerLivenessTimeouts` 计算：`idleCloseMs = min(executionTimeout, idleTimeout)` 作为暖 Runner 的优雅回收窗口，而 `watchdogMs = max(executionTimeout, idleCloseMs) + 15s` 严格晚于优雅回收。函数注释记录了一次真实事故教训：历史上两者同为 30 分钟，且 stdout watchdog 在宿主投影输出**之前**被重置、idle timer 在其后重置，导致 watchdog 总是赢得竞争、对健康会话误报超时。现在 watchdog 在每次 `onOutput` 投影完成后才重置，保证顺序恒定。\n\nSources: [runner-liveness.ts](src/runner-liveness.ts#L10-L34), [container-runner.ts](src/container-runner.ts#L1888-L1923), [container-runner.ts](src/container-runner.ts#L2022-L2025)\n\n## 卡死恢复：候选收集 → CPU 探针 → 身份重验证 → 决策\n\n即便有三重时钟看护，Runner 仍可能在持有欠债工作的情况下彻底僵死（例如 SDK 查询挂起且不再产出任何 stdout）。主消息循环每轮递增计数器，每 15 个轮询周期（`POLL_INTERVAL = 2000ms` × 15 ≈ 30 秒）执行一次 `recoverStuckPendingGroups`。整条管线分为四个阶段：\n\nSources: [index.ts](src/index.ts#L1233-L1239), [config.ts](src/config.ts#L18-L19), [index.ts](src/index.ts#L17383-L17462)\n\n**阶段一：候选快照。** `snapshotStuckRecoveryCandidate` 用一组严格前置条件过滤：必须活跃、不是定时任务 Runner、要么有 pendingMessages 要么存在未偿清的 IPC 债务（`ipcOwedSinceAt` 非空且 `queryInFlight`）、尚未分配子代理、不在重启中，且空闲时长已达 3 分钟阈值或债务已达绝对上限。满足者被打包成含 `runnerGeneration`、`queryId`、`runnerPid`、`ipcOwedSinceAt` 的完整身份指纹。**阶段二：CPU 探针。** 仅对 Host 模式执行——`probeHostRunnerCpu` 读取 `ps` 进程表并沿 PID 树遍历后代进程，任何后代 CPU 占用超过 0.5% 即判定 `active`。容器模式被刻意排除：容器内进程属于 Docker/containerd，宿主的 `docker` CLI 进程树根本观测不到它们。\n\nSources: [group-queue.ts](src/group-queue.ts#L1165-L1205), [stuck-runner-recovery.ts](src/stuck-runner-recovery.ts#L56-L134)\n\n**阶段三：TOCTOU 重验证。** CPU 探针是异步的，探针期间旧查询可能已完成、新查询可能已启动。`revalidateStuckRecoveryCandidate` 重新做一次快照，逐项比对六个身份字段（generation、queryId、runtime、pid、reason、ipcOwedSinceAt），任何一项漂移即判定候选过期并跳过——这从机制上保证了"前代的陈旧恢复决策永远不会杀死后继的健康查询"。**阶段四：决策。** `decideStuckRunnerRecovery` 应用一套保守但有硬上界的规则：\n\nSources: [group-queue.ts](src/group-queue.ts#L1233-L1259), [stuck-runner-recovery.ts](src/stuck-runner-recovery.ts#L142-L172)\n\n| 条件（依序判断） | 动作 | 原因码 | 设计意图 |\n|---|---|---|---|\n| IPC 债务 ≥ 10 分钟 | restart | `absolute_ipc_ceiling` | 债务时钟不受普通输出刷新，硬上限优先级最高 |\n| 空闲 ≥ 10 分钟 | restart | `idle_ceiling` | 宽限信号只是缓刑，不是无限否决权 |\n| runtime = container | defer | `container_grace` | 宿主无法观测容器内 CPU，保守保留 |\n| CPU 探针 = active | defer | `cpu_active` | 有真实计算在发生 |\n| CPU 探针 = unknown（ps 失败） | defer | `cpu_unknown` | 故障时宁可缓刑 |\n| 其余（host 且 CPU inactive） | restart | `idle` | 确认僵死 |\n\n决策为 restart 时，宿主调用 `restartGroup`：置位 `restarting` 标志（抑制失败重试与新 drain 竞争）、预先取消旧退避定时器、给 `_close` 哨兵 2 秒优雅退出窗口，然后 `docker stop`（15 秒上限）/ SIGTERM，等待 finally 交接最长 20 秒，超时则 `docker kill` 强杀，最后以 `enqueueMessageCheck` 触发全新一代 Runner 从 DB 重读未消费消息。整个过程不丢消息——因为游标只在健康完成时提交，被杀 Runner 欠下的工作自然回到待处理集合。\n\nSources: [index.ts](src/index.ts#L17393-L17462), [group-queue.ts](src/group-queue.ts#L2216-L2310)\n\n```mermaid\nflowchart TD\n    A[主循环每 ~30s 触发] --> B[getStuckPendingGroups<br/>快照候选]\n    B --> C{runtime = host?}\n    C -->|是| D[probeHostRunnerCpu<br/>遍历 ps 进程树]\n    C -->|否| E[CPU = unavailable]\n    D --> F[revalidateStuckRecoveryCandidate<br/>六字段身份比对]\n    E --> F\n    F -->|指纹漂移| G[跳过：候选已过期]\n    F -->|一致| H[decideStuckRunnerRecovery]\n    H -->|defer| I[记录日志，下轮再看]\n    H -->|restart| J[restartGroup<br/>停旧 → 强杀兜底 → 冷启动新一代]\n```\n\n## 停止、中断与重启：三种终止的边界划分\n\n`stopGroup`、`interruptQuery`、`restartGroup` 构成三种语义不同的终止原语。`stopGroup` 是最重的：先同步置位 `stopRequested` 并清空排队工作，再走优雅路径（docker stop 或 SIGTERM，10 秒等待 finally 握手），超时升级为强杀；其精髓在于 `recentlyStoppedFolders` 时间窗——用户刚点 Stop 后，finally 中被 IPC 注入标志重新武装的 `pendingMessages` 会与 Stop 的清除动作竞速，导致 Agent "自杀式复活"，因此 finally 显式检查该窗口并跳过重新入队。`interruptQuery` 则是最轻的：只写一个 `_interrupt` 哨兵文件由 Runner 自行中止当前查询，进程与队列状态原封不动。\n\nSources: [group-queue.ts](src/group-queue.ts#L2082-L2142), [group-queue.ts](src/group-queue.ts#L2444-L2463), [group-queue.ts](src/group-queue.ts#L2046-L2075)\n\n| 操作 | 进程影响 | 队列状态 | 典型场景 |\n|---|---|---|---|\n| `interruptQuery` | 写 `_interrupt` 哨兵，Runner 自行中止 | 定时器取消但退避进度保留 | 用户点"停止生成" |\n| `stopGroup`（默认） | docker stop → 10s → 强杀 | 清空 pending、取消重试、记录 recentlyStopped | 用户停止会话 / 变更前停机 |\n| `stopGroup(force)` | 直接 docker kill / SIGKILL | 同上 | 超时兜底 |\n| `restartGroup` | 优雅停 → 最长 20s 等 → 强杀 → 冷启动 | `restarting` 标志抑制一切并行调度 | 卡死恢复 / 凭据刷新 |\n\nSources: [group-queue.ts](src/group-queue.ts#L2082-L2211), [group-queue.ts](src/group-queue.ts#L2216-L2310)\n\n值得补充的是变更场景的特殊通道：`stopGroup({ preserveQueuedWork: true })` 要求必须持有活跃的变更暂停令牌，它会将当前任务"泊车"回 `pendingTasks` 队首而非丢弃，使 workspace 配置变更能在停掉 Runner 的同时无损保留排队工作。这些机制与 [可靠性状态机：Inbox、Turn、Outbox 与 Streaming Card 的持久投递](17-ke-kao-xing-zhuang-tai-ji-inbox-turn-outbox-yu-streaming-card-de-chi-jiu-tou-di) 中的游标提交协议协同：队列层负责"谁在什么时候跑"，可靠性层负责"跑过的东西绝不重复也不丢失"。\n\nSources: [group-queue.ts](src/group-queue.ts#L2082-L2102), [group-queue.ts](src/group-queue.ts#L806-L826)\n\n## 小结\n\n会话串行队列的本质是把"单 Workspace 单 Runner"这一物理约束编码为一台可观测、可恢复的状态机：四道入队门控保证串行化不被绕过；指数退避配合三重抑制条件让失败重试既坚韧又不失控；双时钟体系区分"沉默但在工作"与"沉默且欠债"；而候选快照、CPU 探针与身份重验证的三段式卡死恢复，则在"绝不误杀健康 Runner"与"绝不无限纵容僵死 Runner"之间取得了有硬上界的平衡。若要继续深入，建议沿两条线索展开：向上追溯 Runner 内部的 [Pi Agent Runner 协议](10-pi-agent-runner-xie-yi-stdin-stdout-jie-gou-hua-jie-guo-yu-ipc-tong-dao)，向下探究投递侧的[可靠性状态机](17-ke-kao-xing-zhuang-tai-ji-inbox-turn-outbox-yu-streaming-card-de-chi-jiu-tou-di)。';export{e as default};

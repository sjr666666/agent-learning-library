const n='MiniCode 的定位不是一个「缩小版的 Claude Code」，而是一个**刻意保留核心架构思想的小体量参考实现**。如果你想知道 Claude Code 这样产品级的终端编码助手内部有哪些关键设计决策，MiniCode 是观察这些模式最清晰的切片：它的代码量足够小，可以逐行读完；它又足够「像」，保留了 agent loop、工具协议、权限边界、上下文压缩等真正塑造系统形态的结构性选择。\n\n本文以官方文档 [CLAUDE_CODE_PATTERNS_ZH.md](CLAUDE_CODE_PATTERNS_ZH.md) 梳理的 14 个设计模式为线索，逐一对照 MiniCode 的源码实现，说明「Claude Code 为什么要这样做」以及「MiniCode 里对应的代码在哪里」。这是面向初学者的讲解文档，阅读前不需要熟悉任何具体框架。\n\nSources: [README.zh-CN.md](README.zh-CN.md#L28)、[CLAUDE_CODE_PATTERNS_ZH.md](CLAUDE_CODE_PATTERNS_ZH.md#L1-L4)、[ARCHITECTURE.md](ARCHITECTURE.md#L74-L85)\n\n## 一、全景：14 个设计模式如何映射到 MiniCode\n\nClaude Code 的设计模式可以归入五个维度：**核心执行**、**安全边界**、**上下文管理**、**扩展机制**、**持久化与交互**。MiniCode 在每个维度上都有对应实现，而且实现与实现之间相互咬合——例如「Provider usage 作为上下文事实」直接决定「自动压缩」的触发时机，两者又共享同一份 accounting 结果。\n\n```mermaid\nflowchart LR\n    subgraph Core["核心执行"]\n        A1["① Agent Loop<br/>回合主循环"] --> A2["② 结构化消息<br/>ChatMessage 联合类型"]\n        A2 --> A3["③ 工具即协议<br/>ToolDefinition + ToolRegistry"]\n        A4["④ Progress 与 Final 分离"]\n    end\n\n    subgraph Safety["安全边界"]\n        S1["⑤ 权限属于执行路径<br/>permissions.ts"]\n        S2["⑥ 写前审阅<br/>file-review.ts"]\n    end\n\n    subgraph Context["上下文管理"]\n        C1["⑨ Provider usage 记账<br/>model-context.ts"]\n        C2["⑧ 自动压缩<br/>compact/*"]\n        C3["⑪ 大工具结果落盘<br/>tool-result-storage.ts"]\n    end\n\n    subgraph Extension["扩展机制"]\n        E1["⑥ MCP 动态接入<br/>mcp.ts"]\n        E2["⑦ Skills 工作流扩展<br/>skills.ts"]\n    end\n\n    subgraph Persist["持久化与交互"]\n        P1["⑩ JSONL 会话事件<br/>session.ts"]\n        P2["⑫ TUI 状态机视图<br/>tui/*"]\n        P3["⑬ 前后台任务分离<br/>background-tasks.ts"]\n    end\n\n    A1 --> Safety\n    A1 --> Context\n    A1 --> Extension\n    Core --> Persist\n```\n\n下表是 14 个模式的速览对照，后续章节会展开每个模式的「Claude Code 设计意图」与「MiniCode 对应实现」：\n\n| # | Claude Code 设计模式 | 设计意图 | MiniCode 核心文件 |\n|---|---|---|---|\n| 1 | Agent Loop | 一切围绕 model→tool→model 主循环 | `src/agent-loop.ts` |\n| 2 | 结构化消息模型 | 会话不是字符串列表，而是有角色的状态流 | `src/types.ts` |\n| 3 | 工具即协议 | 注册、校验、执行、返回格式统一 | `src/tool.ts`、`src/tools/index.ts` |\n| 4 | Progress 与 Final 分离 | 「还在干活」不是「最终回答」 | `src/types.ts`、`src/tui/transcript.ts` |\n| 5 | 权限属于执行路径 | 审批内嵌于正常控制流 | `src/permissions.ts` |\n| 6 | MCP 动态能力注入 | 外部 server 动态挂载能力 | `src/mcp.ts` |\n| 7 | Skills 轻量工作流扩展 | 按需加载 SKILL.md 工作流 | `src/skills.ts` |\n| 8 | 自动上下文压缩 | 旧上下文转摘要而非删除 | `src/compact/*` |\n| 9 | Provider usage 为上下文事实 | 记账贴近真实预算 | `src/utils/model-context.ts` |\n| 10 | 会话事件、恢复与分叉 | 可持久化、可回溯的 session model | `src/session.ts` |\n| 11 | 大工具结果离开 prompt | 「可访问」≠「全量塞进 prompt」 | `src/utils/tool-result-storage.ts` |\n| 12 | TUI 作为状态机视图 | 界面展示内部状态而非纯文本 | `src/tui/*` |\n| 13 | 前台工具与后台任务分离 | 长命令建模为独立 task | `src/background-tasks.ts` |\n| 14 | 借鉴与轻量化的边界 | 保留结构思想而非完整产品体量 | 贯穿全部实现 |\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L3-L191)、[ARCHITECTURE.md](ARCHITECTURE.md#L4-L20)\n\n## 二、核心执行：loop-first 的主循环\n\n### 1. Agent Loop —— 一切围绕同一条主控制流\n\nClaude Code 的主体是一个持续推进的 agent loop：接收输入 → 组织上下文 → 请求模型 → 判断是否调用工具 → 执行工具 → 把结果回传给模型 → 满足条件才结束回合。**工具调用不是旁路，而是主循环的一个阶段**。\n\nMiniCode 的主循环在 `src/agent-loop.ts`。它接收用户消息，调用 `ModelAdapter.next()` 获取模型的下一步（assistant 文本或 tool_calls），然后逐个执行工具、把 `assistant_tool_call` 与 `tool_result` 消息追加回消息列表，再进入下一轮模型请求：\n\n```mermaid\nflowchart TD\n    A[用户输入] --> B[组装 ChatMessage 列表]\n    B --> C[调用 ModelAdapter.next]\n    C --> D{有 tool_calls?}\n    D -- 否 --> E[返回最终消息，回合结束]\n    D -- 是 --> F[逐个执行工具<br/>tools.execute]\n    F --> G[替换大工具结果为预览]\n    G --> H[追加 tool_call + tool_result 消息]\n    H --> C\n```\n\n关键代码在 `src/agent-loop.ts#L360-L440`：循环执行 `args.tools.execute(call.toolName, call.input, { cwd, permissions })`，把结果经 `replaceLargeToolResult` 处理后重新喂给模型。注意 **permissions 是作为执行上下文的一部分传入的**——这正是模式 5「权限属于执行路径」在代码层面的落点。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L3-L20)、[src/agent-loop.ts](src/agent-loop.ts#L360-L440)\n\n### 2. 结构化消息模型 —— 会话是有角色的状态流\n\nClaude Code 不会把会话当作纯文本聊天记录，而是区分用户输入、最终回答、中间进度、工具调用、工具结果、压缩边界等不同状态。这样 loop 判定、TUI 渲染和上下文压缩才能建立在这些状态之上。\n\nMiniCode 的对应物是 `src/types.ts` 中的 `ChatMessage` 联合类型，它定义了 9 种消息角色：\n\n| 角色 | 含义 | 关键字段 |\n|---|---|---|\n| `system` / `user` | 系统提示与用户输入 | `content` |\n| `assistant_thinking` | 模型的思考块 | `blocks` |\n| `assistant` | 最终回答 | `content` + `providerUsage` |\n| `assistant_progress` | 中间进度文本 | `content` + `providerUsage` |\n| `assistant_tool_call` | 工具调用声明 | `toolUseId` / `toolName` / `input` |\n| `tool_result` | 工具执行结果 | `isError` |\n| `context_summary` | 压缩后的摘要边界 | `compressedCount` / `timestamp` |\n| `snip_boundary` | 确定性裁剪边界 | `removedMessageIds` / `tokensFreed` |\n\n这个联合类型是整个系统的「数据契约」：`agent-loop.ts` 消费它来推进回合，TUI 消费它来决定如何渲染，`compact/*` 消费它来决定哪些消息可以压缩。**一种状态一种角色**，是理解 MiniCode 一切后续机制（压缩、记账、恢复）的钥匙。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L21-L37)、[src/types.ts](src/types.ts#L20-L52)\n\n### 3. 工具即协议 —— 统一注册、校验、执行与返回\n\nClaude Code 把工具调用定义为一套协议：模型声明意图 → 系统校验输入 → 权限参与判断 → 执行并返回标准化结果 → 结果进入下一轮推理。**本地工具与远端工具共用同一协议**，是这套设计最关键的收益。\n\nMiniCode 用 `ToolDefinition<TInput>` 把协议显式化为一个类型：\n\n```typescript\nexport type ToolDefinition<TInput> = {\n  name: string\n  description: string\n  inputSchema: Record<string, unknown>\n  schema: z.ZodType<TInput>        // 声明式校验\n  run(input: TInput, context: ToolContext): Promise<ToolResult>\n}\n```\n\n- **注册**：`ToolRegistry` 维护工具表，`addTools()` 可动态追加（MCP 工具就是这样挂载进来的），见 `src/tool.ts#L42-L80`；\n- **校验**：每个工具自带 Zod `schema`，执行前统一校验输入；\n- **统一返回**：所有工具都返回 `ToolResult = { ok, output, backgroundTask?, awaitUser? }`，格式一致；\n- **集中执行**：`agent-loop.ts` 只通过 `tools.execute()` 这一个入口调用工具，见 `src/agent-loop.ts#L385`。\n\n`src/tools/index.ts#L50-L70` 展示了内置工具的全量注册：12 个本地工具（`read_file`、`edit_file`、`run_command`、`web_search`……）与 skills、MCP 元数据一起构成默认注册表。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L38-L53)、[src/tool.ts](src/tool.ts#L17-L80)、[src/tools/index.ts](src/tools/index.ts#L50-L70)\n\n### 4. Progress 与 Final 分离 —— 「还在干活」不等于「回答完了」\n\nClaude Code 的重要判断：**模型输出的过程性文本不是最终答案**。如果系统把一段「正在读取文件…」的自然语言当成回合结束信号，整个 loop 就会提前中断。\n\nMiniCode 用两个机制落实这一分离：\n\n1. **消息角色层面**：`assistant_progress` 与 `assistant` 是不同的角色（`src/types.ts#L27-L28`），进度文本永远不会以最终 assistant 消息的身份进入会话历史；\n2. **回合判定层面**：`agent-loop.ts` 中的 `shouldTreatAssistantAsProgress()` 显式区分 `kind: \'final\'` 与 `kind: \'progress\'`，只有不带 tool_calls、且不是 progress 的 assistant 文本才会触发回合结束（`src/agent-loop.ts#L42-L57`、`src/agent-loop.ts#L365-L368`）。\n\nTUI 侧同样分离：`src/tui/transcript.ts#L175-L176` 把 progress 条目渲染为黄色 `progress` 前缀，与普通回答视觉上截然不同。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L54-L63)、[src/types.ts](src/types.ts#L26-L38)、[src/tui/transcript.ts](src/tui/transcript.ts#L175-L176)\n\n## 三、安全边界：权限属于执行路径本身\n\n### 5. 三层权限系统 —— 审批是正常控制流的一部分\n\nClaude Code 的设计立场是：**权限不是挂在执行外面的检查器，而是执行路径本身的一环**。命令执行、文件修改等高风险操作必须经过统一的审批边界。\n\nMiniCode 的 `src/permissions.ts` 实现了三类审批（`PermissionRequest.kind`）：\n\n| kind | 审批对象 | 典型场景 |\n|---|---|---|\n| `path` | 路径读写访问 | 读取/写入工作目录之外的文件 |\n| `command` | shell 命令执行 | `run_command` 触发的危险命令 |\n| `edit` | 文件编辑 | 写入前展示 diff 供审阅 |\n\n审批决策有 7 种（`allow_once`、`allow_always`、`allow_turn`、`deny_with_feedback` 等），其中 `deny_with_feedback` 会把用户的拒绝理由回传给模型，让模型在下一次推理中修正行为——**拒绝也参与执行流**，而不是简单中断。\n\n在 agent loop 里，权限管理器作为 `ToolContext` 的一部分传入每个工具执行（`src/agent-loop.ts#L385`），这印证了「权限内嵌于执行路径」的架构选择。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L64-L73)、[src/permissions.ts](src/permissions.ts#L1-L70)、[src/agent-loop.ts](src/agent-loop.ts#L385)\n\n### 6. 写前审阅 —— 统一 diff 生成与编辑审批\n\n与权限系统配套的是**文件修改前必须展示 diff**。Claude Code 把「用户可审查的文件修改流」作为产品核心安全体验；MiniCode 在 `src/file-review.ts` 中实现了同一个模式的最小版本。\n\n`applyReviewedFileChange()` 的流程非常清晰：\n\n1. 读取目标文件当前内容（不存在则视为空串，支持新建）；\n2. 用 `createTwoFilesPatch` 生成统一 diff（`src/file-review.ts#L14-L31`）；\n3. 调用 `permissions.ensureEdit(targetPath, diff)` 把 diff 交给用户审批（`src/file-review.ts#L61`）；\n4. 审批通过后才写入文件。\n\n也就是说，**用户在 diff 上点「同意」的那一刻，才是文件真正被修改的时刻**。diff 生成、审批、落盘三个步骤线性排列，任何一步都不省略。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L64-L73)、[src/file-review.ts](src/file-review.ts#L38-L71)\n\n## 四、上下文管理：把预算当作事实\n\n### 7. Provider usage 作为上下文事实来源 —— 记账优先于猜测\n\nClaude Code 的设计原则：**如果 provider 返回 usage metadata，那就是上下文大小的最接近事实**；本地 token 估算只应作为 fallback 或用于 provider 边界之后追加的消息。\n\nMiniCode 把这条原则实现为三层记账来源，并在 TUI 徽章上直接标注来源：\n\n| 来源标识 | 含义 | 何时使用 |\n|---|---|---|\n| `usage` | 纯 provider 用量 | provider 返回且无新消息追加 |\n| `usage+est` | provider 用量 + 本地估算尾巴 | provider 边界后又追加了新消息 |\n| `est` | 纯本地估算 | provider 不返回 usage 或压缩后 usage 失效 |\n\n账目来源显示在 TUI 的 context badge 上（`src/tui/chrome.ts#L236-L244`）。`src/agent-loop.ts#L18-L32` 中的 `withProviderUsage()` 把 provider 返回的 usage 附着在 assistant 响应边界上，作为记账锚点。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L108-L119)、[src/tui/chrome.ts](src/tui/chrome.ts#L236-L244)、[src/agent-loop.ts](src/agent-loop.ts#L18-L32)\n\n### 8. 自动压缩 —— 触发阈值、失败熔断与互补策略\n\nClaude Code 对长上下文的处理不是「删掉旧消息」，而是**把旧上下文转化为可继续工作的摘要，同时保留新上下文的高保真度**，且触发必须贴近真实模型预算。\n\nMiniCode 的 `src/compact/auto-compact.ts` 把触发逻辑建立在模式 7 的 accounting 结果之上：`shouldAutoCompact()` 计算 `stats.utilization >= THRESHOLDS.AUTOCOMPACT_UTILIZATION`（0.85，见 `src/compact/constants.ts#L1-L6`）才触发压缩。连续失败 3 次（`MAX_AUTOCOMPACT_FAILURES`）会自动熔断，避免压缩反复失败拖垮会话。\n\n压缩策略是互补的两种：\n\n| 策略 | 机制 | 保护目标 |\n|---|---|---|\n| 裁剪压缩（snip compact） | 确定性移除中段历史，替换为 `snip_boundary` | 文件编辑轮次与出错轮次不被误删 |\n| 上下文折叠（context collapse） | 识别可摘要区间，替换为 `context_summary` | 保留对话意图的连续投影 |\n\n两种策略各自有独立的利用率阈值（`SNIP_COMPACT_THRESHOLD = 0.70`、`CONTEXT_COLLAPSE_UTILIZATION = 0.75`），并在 `agent-loop.ts` 的同一压缩编排点按需触发。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L98-L107)、[src/compact/auto-compact.ts](src/compact/auto-compact.ts#L1-L83)、[src/compact/constants.ts](src/compact/constants.ts#L1-L33)\n\n### 9. 大工具结果离开 prompt —— 「可访问」≠「全量塞进 prompt」\n\n工具输出往往远大于其中的有效信号。Claude Code 的关键设计是区分**「系统仍可访问完整数据」**与**「完整数据直接塞进 prompt」**。\n\nMiniCode 用 `src/utils/tool-result-storage.ts` 实现同一个模式：\n\n1. 单结果超过 `DEFAULT_MAX_RESULT_SIZE_CHARS`（50,000 字符）或整批超过 `MAX_TOOL_RESULTS_PER_BATCH_CHARS`（200,000 字符）时触发落盘（`src/utils/tool-result-storage.ts#L17-L21`）；\n2. 完整输出写入 `~/.mini-code/tool-results/` 目录，文件路径包含 toolUseId；\n3. 模型可见内容被替换为 `PERSISTED_OUTPUT_TAG` 包裹的 2,000 字符预览 + 完整输出路径；\n4. 在 agent loop 中，落盘发生在工具结果进入消息列表之前（`src/agent-loop.ts#L398-L413`），所以**下一轮模型请求看到的永远是瘦身后的版本**。\n\n完整数据仍然可被检查（预览里有路径），但不会挤占对话历史、最近编辑和任务意图这些更重要的上下文。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L132-L143)、[src/utils/tool-result-storage.ts](src/utils/tool-result-storage.ts#L1-L70)、[src/agent-loop.ts](src/agent-loop.ts#L398-L413)\n\n## 五、扩展机制：动态能力注入\n\n### 10. MCP —— 外部 server 动态暴露能力\n\nMCP 的核心思想是：**外部 server 可以动态地把能力暴露进当前 agent 会话**，承担能力发现、能力挂载与统一接入的角色。\n\nMiniCode 的 `src/mcp.ts` 实现了一条完整的最小 MCP 链路：\n\n```mermaid\nflowchart LR\n    A[读取 mcp.json 配置] --> B[spawn 子进程<br/>stdio 启动]\n    B --> C[initialize 握手<br/>协议协商]\n    C --> D[tools/list 发现远端工具]\n    D --> E[包装为本地 ToolDefinition]\n    E --> F[ToolRegistry.addTools 挂载]\n```\n\n对应代码路径：\n\n- **启动与协议协商**：`src/mcp.ts#L38-L45` 定义三种协议（`content-length`、`newline-json`、`streamable-http`），并通过 `MCP_PROTOCOL_CACHE_PATH` 缓存协商结果；\n- **工具注入**：`src/tools/index.ts#L70-L81` 的 `hydrateMcpTools()` 把远端工具包装成本地 `ToolDefinition` 后调用 `ToolRegistry.addTools()` 注入；\n- **统一抽象**：本地工具与 MCP 工具最终共用同一执行入口（模式 3 的协议），模型侧无感知差异。\n\n此外，MCP server 暴露的 resources 与 prompts 也通过统一的 helper tools 暴露给模型，扩展了「工具」之外的两种能力类型。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L74-L83)、[src/mcp.ts](src/mcp.ts#L38-L80)、[src/tools/index.ts](src/tools/index.ts#L70-L81)\n\n### 11. Skills —— 轻量工作流扩展而非重型插件\n\nClaude Code 的 skills 更像**轻量工作流扩展**：任务说明、领域约束、可复用的工作模式，按需加载，直接参与模型执行。\n\nMiniCode 的 `src/skills.ts` 采用同样的轻量思路。技能发现是多级目录扫描（`getSkillRoots()`）：\n\n| 来源 | 目录 | source 标记 |\n|---|---|---|\n| 项目级 | `<cwd>/.mini-code/skills` | `project` |\n| 用户级 | `~/.mini-code/skills` | `user` |\n| 兼容项目级 | `<cwd>/.claude/skills` | `compat_project` |\n| 兼容用户级 | `~/.claude/skills` | `compat_user` |\n\n每个技能是一个 `SKILL.md` 文件，其首个非标题段落被提取为描述（`src/skills.ts#L20-L36`），供模型发现。加载后的技能内容作为 prompt 和任务执行的一部分进入模型上下文——**扩展直接参与执行过程，而非旁挂插件**。内置的 `load_skill` 工具让模型可以按需加载技能（`src/tools/index.ts#L62`）。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L84-L97)、[src/skills.ts](src/skills.ts#L20-L60)\n\n## 六、持久化与交互：状态可见性与任务模型\n\n### 12. 会话事件、恢复与分叉 —— 可持久化、可回溯的 session model\n\n长时间运行的 coding agent 不能只靠内存里的 chat buffer。Claude Code 需要**跨进程退出可恢复、可理解演进过程**的持久化 session model。\n\nMiniCode 的 `src/session.ts` 实现了追加式 JSONL 事件日志：\n\n| 设计点 | 实现 |\n|---|---|\n| 存储位置 | 按工作目录哈希分目录：`~/.mini-code/projects/<cwd-hash>/` |\n| 事件格式 | 每行一个 JSON 事件，含 `sessionId`、`timestamp`、`cwd`、`parentUuid` |\n| 事件类型 | `system` / `user` / `assistant` / `progress` / `tool_call` / `tool_result` / `summary` / `compact_boundary` / `snip_boundary` 等 |\n| 恢复策略 | 从最近的 compact boundary 之后加载消息，完整 transcript 仍可从全量日志重建 |\n| 会话操作 | 恢复、重命名、新开会话、分叉（fork）为独立分支、过期清理 |\n\n`roleToType()`（`src/session.ts#L48-L60`）把 `ChatMessage` 角色一一映射为事件类型，保证**持久化事件与内存消息模型同构**。压缩边界作为事件写入日志，意味着压缩不会破坏会话的可重建性——这是模式 8 与模式 12 咬合的关键点。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L120-L131)、[src/session.ts](src/session.ts#L1-L80)\n\n### 13. TUI 作为状态机的可视化层\n\nClaude Code 的终端界面不是单纯输出文本，而是**在展示内部状态机**：工具运行中/成功/失败、进度 vs 最终回答、审批等待 vs 正常执行、压缩后的摘要输出。\n\nMiniCode 的 TUI 模块（`src/tui/`）是同一方向的轻量实现，其渲染内容直接对应内部状态：\n\n| 内部状态 | TUI 呈现 |\n|---|---|\n| 工具运行中 | 工具面板（`renderToolPanel`） |\n| 进度消息 | `progress` 前缀的黄色条目（`src/tui/transcript.ts#L175-L176`） |\n| 审批等待 | 权限提示面板（`renderPermissionPrompt`） |\n| 上下文账目 | context badge 显示 `usage` / `usage+est` / `est`（`src/tui/chrome.ts#L236-L244`） |\n| 压缩摘要 | 折叠后的工具结果摘要 |\n\n模块结构也反映状态机思维：`chrome.ts`（边框/徽章/面板）、`transcript.ts`（消息渲染）、`screen.ts`（屏幕帧管理）、`input.ts`（输入区）各司其职，全部通过 `src/tui/index.ts` 统一导出。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L144-L158)、[src/tui/index.ts](src/tui/index.ts#L1-L26)、[src/tui/transcript.ts](src/tui/transcript.ts#L175-L176)\n\n### 14. 前台工具执行与后台 Shell Task 分离\n\nClaude Code 不会把所有命令都当作同一种同步工具调用。**长驻命令应建模为独立 task**，而不是伪装成一个迟迟不返回的普通工具调用。\n\nMiniCode 以最小形式实现了这一分离：`src/background-tasks.ts` 是一个极简的进程注册表，记录 `taskId` / `command` / `pid` / `status`（`running | completed | failed`）。`run_command` 工具检测到 `&` 后台操作符时（`src/tools/run-command.ts#L189`），不再走同步执行路径，而是：\n\n1. 剥离后台操作符启动子进程（`src/tools/run-command.ts#L195`）；\n2. 调用 `registerBackgroundShellTask()` 注册任务（`src/tools/run-command.ts#L220-L229`）；\n3. 返回 `ToolResult` 时携带 `backgroundTask` 字段（`src/tool.ts#L14-L16`），让 agent loop 和 TUI 知道这是一个仍在运行的后台任务；\n4. `refreshRecord()` 用 `process.kill(pid, 0)` 探测进程存活状态并更新状态机（`src/background-tasks.ts#L17-L41`）。\n\n这不是 Claude Code 完整任务系统的复刻，但「前后台分离」的架构方向已经确立。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L159-L168)、[src/background-tasks.ts](src/background-tasks.ts#L1-L60)、[src/tools/run-command.ts](src/tools/run-command.ts#L189-L229)\n\n## 七、借鉴的边界：小体量参考实现而非完整复刻\n\n理解「MiniCode 从 Claude Code 学什么」同样重要的一半是**「MiniCode 刻意不学什么」**。Claude Code 是完整产品级系统，其设计建立在更大的状态管理、上下文处理和交互体系之上。MiniCode 保留的是**塑造系统形态最强烈的结构性想法**：\n\n- loop-first 的主结构\n- 结构化消息（而非字符串列表）\n- 统一工具协议（本地工具与 MCP 工具同构）\n- 审批嵌入执行路径\n- MCP 动态能力接入\n- skills 工作流扩展\n- usage-aware 上下文记账与自动压缩\n- 持久化会话、恢复与分叉\n- 大工具结果移出 prompt context\n- 状态化 TUI\n- 前台工具与后台 shell task 分离\n\n而 `ARCHITECTURE.md` 明确列出「计划中 / 尚未构建」的清单：完整的渲染栈、IDE 双向通信、子代理编排、LSP、技能市场、遥测系统等——**这些正是产品级工程化部分，而非核心架构思想**。这种「取结构、舍体量」的取舍，本身就是值得学习的设计决策。\n\nSources: [CLAUDE_CODE_PATTERNS.md](CLAUDE_CODE_PATTERNS.md#L169-L191)、[ARCHITECTURE.md](ARCHITECTURE.md#L4-L20)、[ARCHITECTURE.md](ARCHITECTURE.md#L22-L33)\n\n## 八、学习路径建议\n\n如果你是从零开始的初学者，建议按以下顺序阅读本 wiki 的相关页面，把本文的模式线索落到具体实现上：\n\n1. **先建立主循环心智**：[代理循环：model → tool → model 的多步执行流程](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng) 与 [统一工具协议：注册、Schema 校验与集中执行](8-tong-gong-ju-xie-yi-zhu-ce-schema-xiao-yan-yu-ji-zhong-zhi-xing)、[结构化消息模型：角色类型与工具调用编排](9-jie-gou-hua-xiao-xi-mo-xing-jiao-se-lei-xing-yu-gong-ju-diao-yong-bian-pai)，对应本文第二、三节；\n2. **再理解上下文管理**：[供应商用量优先的上下文记账与令牌估算](11-gong-ying-shang-yong-liang-you-xian-de-shang-xia-wen-ji-zhang-yu-ling-pai-gu-suan)、[自动压缩：触发阈值与失败熔断](12-zi-dong-ya-suo-hong-fa-yu-zhi-yu-shi-bai-rong-duan)、[大工具结果落盘：预览替换与稳定记账](16-da-gong-ju-jie-guo-luo-pan-yu-lan-ti-huan-yu-wen-ding-ji-zhang)，对应本文第四节；\n3. **再看安全边界**：[三层权限系统：路径、命令与编辑审批](17-san-ceng-quan-xian-xi-tong-lu-jing-ming-ling-yu-bian-ji-shen-pi) 与 [写前审阅流程与统一 diff 生成](18-xie-qian-shen-yue-liu-cheng-yu-tong-diff-sheng-cheng)，对应本文第三节；\n4. **随后看扩展与持久化**：[技能系统：SKILL.md 的多级发现与加载](21-ji-neng-xi-tong-skill-md-de-duo-ji-fa-xian-yu-jia-zai)、[MCP 集成：stdio/HTTP 启动、协议协商与工具注入](22-mcp-ji-cheng-stdio-http-qi-dong-xie-yi-xie-shang-yu-gong-ju-zhu-ru)、[追加式 JSONL 事件日志与断点恢复](19-zhui-jia-shi-jsonl-shi-jian-ri-zhi-yu-duan-dian-hui-fu)，对应本文第五、六节；\n5. **最后用测试体系收尾**：[测试体系：从单元测试到集成回归](28-ce-shi-ti-xi-cong-dan-yuan-ce-shi-dao-ji-cheng-hui-gui) 展示这些模式如何被验证，[多语言版本家族：TypeScript/Rust/Python/Go/Java 对照](29-duo-yu-yan-ban-ben-jia-zu-typescript-rust-python-go-java-dui-zhao) 展示同一套模式如何跨语言复现。\n\n如果你更想直接上手体验，可以从 [快速开始：安装、启动与离线演示模式](2-kuai-su-kai-shi-an-zhuang-qi-dong-yu-chi-xian-yan-shi-mo-shi) 开始，用 `mock-model` 离线跑通第一条 agent loop。';export{n as default};

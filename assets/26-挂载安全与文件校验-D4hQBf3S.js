const t='挂载安全与文件校验是 HappyClaw 在"认证与安全"层面的最后一层防线：**RBAC 决定谁能操作，密钥加密决定凭据如何保存，而本页所讨论的机制决定 Agent 的代码执行能触达宿主机文件系统的哪些边界、以及流入/流出工作区的文件如何被净化**。整个系统围绕一个核心矛盾展开——Container 模式需要把宿主机目录 bind-mount 进 Docker 容器供 Agent 读写，但任何挂载点一旦建立，容器内进程就与宿主机共享同一 inode；因此设计上采取"宿主机侧白名单 + 容器内侧权限归一化"的双边界模型，两层各自独立 fail-closed，任何一层失守都不会直接导致容器获得宿主机的完整访问权。\n\n```mermaid\nflowchart LR\n    subgraph Host[宿主机侧边界]\n        A[config/mount-allowlist.json<br/>部署方白名单] --> B[loadMountAllowlist<br/>签名热重载]\n        B --> C[validateMount<br/>多层路径检查]\n        C --> D[buildVolumeMounts<br/>docker run 前复核]\n    end\n\n    subgraph Container[容器内侧边界]\n        E[entrypoint.sh<br/>umask 0077] --> F[身份探测<br/>direct/rootless/userns/...]\n        F --> G[身份归一化<br/>改写 node uid/gid]\n        G --> H[权限 watcher<br/>descriptor-based 归一化]\n    end\n\n    D -->|"-v host:container:ro"| E\n    C --> I[浏览 API<br/>routes/browse.ts]\n    I --> C\n\n    style A fill:#1f2937,color:#fff\n    style E fill:#1f2937,color:#fff\n```\n\n## 部署方白名单：信任根的设计\n\n所有宿主目录挂载的合法性都源于一个由部署方持有的配置文件 `config/mount-allowlist.json`。默认模板只放行 `~`（整个家目录，可读写）——这是一个宽松的起点，但真正决定策略的是该文件的形状约束与加载语义。`validateAllowlistShape` 严格校验 `allowedRoots`（每项必须含非空 `path` 字符串与布尔 `allowReadWrite`）、`blockedPatterns`（仅字符串数组）与 `nonMainReadOnly`（布尔），任何形状非法都会导致整个白名单不可用，而不是部分放行；`blockedPatterns` 还会与内置的 `DEFAULT_BLOCKED_PATTERNS`（`.ssh`、`.gnupg`、`.aws`、`.kube`、`.docker`、`.claude`、`credentials`、`.env`、`id_rsa`、`private_key` 等 17 项敏感模式）合并去重，部署方只能追加、不能删除这些内置项。\n\nSources: [mount-security.ts](src/mount-security.ts#L23-L45), [mount-security.ts](src/mount-security.ts#L67-L118), [mount-allowlist.json](config/mount-allowlist.json#L1-L12)\n\n加载过程带签名缓存：`loadMountAllowlist` 以 `dev:ino:mtimeMs:size:sha256(content)` 五元组作为签名，文件内容变化时自动重载，无需重启服务。关键语义是 **fail-closed**——白名单缺失、损坏或解析失败时返回 `null`，所有宿主目录挂载被整体阻止，同时记录错误日志；管理员修正文件后同一进程内即可恢复。这种"签名比较 + 惰性重载"的设计使热收紧成为可能：测试 `mount-security-strict.test.ts` 验证了在进程不重启的前提下，将白名单从放行 A 目录改为放行 B 目录后，下一次校验立即拒绝 A 目录。\n\nSources: [mount-security.ts](src/mount-security.ts#L125-L175), [mount-security-strict.test.ts](tests/mount-security-strict.test.ts#L220-L237)\n\n## 双重验证：持久化配置不是长期授权\n\n`mount-security.ts` 的模块注释明确写出一条安全原则："Every configured mount is validated both when the workspace is created and again immediately before `docker run`, so persisted configuration is never treated as a durable authorization grant."（每个配置的挂载在创建工作区时和 `docker run` 之前各验证一次，持久化配置永不被视为长期授权凭证）。\n\n第一道验证发生在创建/更新工作区的 HTTP 路由：`routes/groups.ts` 在接触任何提交的路径之前先做授权检查——仅 active 管理员可提交挂载，且仅 container 执行模式有效（member 请求挂载会得到 403，host 模式得到 400），随后调用 `validateAdditionalMountsStrict` 做全量校验，校验失败按冲突与否返回 400/409。\n\nSources: [routes/groups.ts](src/routes/groups.ts#L620-L677)\n\n第二道验证发生在每次容器启动前：`buildVolumeMounts` 先重新解析持久化的 `containerConfig`，若其中含 `additionalMounts`，则**每次**从数据库读取当前 owner 记录并调用 `canExecuteOnHost`——该函数要求 owner 此刻必须是 `role === \'admin\' && status === \'active\'` 且不在权限撤销集合中，任何一次启动都会重新判定。这意味着管理员被降级、禁用或主动发起权限撤销后，即使数据库中仍保留着旧的挂载配置，下一次 `docker run` 也会直接抛错拒绝启动，而非静默忽略或继续挂载。测试 `container-runner-host-mount.test.ts` 专门覆盖了"owner 降级后旧持久化挂载立即失效"与"owner 禁用后立即失效"两个场景。\n\nSources: [container-runner.ts](src/container-runner.ts#L1104-L1129), [host-execution-policy.ts](src/host-execution-policy.ts#L18-L26), [container-runner-host-mount.test.ts](tests/container-runner-host-mount.test.ts#L173-L196)\n\n同样的防御在 IPC 通道上复现：`registerGroup` 可被 agent 的 IPC 调用触达，因此它对 `containerConfig` 执行与 HTTP 路由完全相同的结构校验、权限校验与 `validateAdditionalMountsStrict` 校验，并将规范化后的 `persisted` 形式写回——IPC 路径与 HTTP 路径共享同一套安全函数，不存在旁路。\n\nSources: [index.ts](src/index.ts#L5128-L5167)\n\n## 挂载验证管线：从字符串到 Docker volume 的七层检查\n\n`validateMount` 是单条挂载的验证核心，按序执行七层检查，任何一层失败即整体拒绝：\n\n| 层级 | 检查内容 | 拒绝示例 |\n|---|---|---|\n| 1. 策略存在 | 白名单已加载且 `allowedRoots` 非空 | 白名单文件缺失/损坏 |\n| 2. 容器路径 | 相对后缀、≤512 字符、无控制字符/反斜杠/冒号、无空组件/`.`/`..`/保留组件（`.npm-global`） | `containerPath: \'/absolute\'`、`\'../escape\'` |\n| 3. 宿主路径 | 必须绝对路径、无控制字符与冒号 | `hostPath: \'relative/path\'` |\n| 4. 真实路径 | `realpathSync` 解析（防 symlink 逃逸），路径必须存在 | 目录被替换为指向外部的 symlink |\n| 5. 目录性 | `statSync` 确认是目录 | 挂载单个文件 |\n| 6. 硬拒绝 | 与 `/proc`、`/sys`、`/dev`、`/run`、`/var/lib/docker`、`DATA_DIR`、家目录敏感子目录重叠；路径含容器 socket 组件（`docker.sock` 等） | 白名单放行 `/` 也无法挂载 `/proc` |\n| 7. 白名单匹配 | `matchesBlockedPattern` 逐段匹配（大小写不敏感 FS 上 lowercase 比较）→ `findAllowedRoot` 选择**最具体**（最长 realpath）的允许根 → 读写权限复核 | 路径含 `.aws` 段；宽泛可写根不覆盖嵌套只读根 |\n\nSources: [mount-security.ts](src/mount-security.ts#L177-L212), [mount-security.ts](src/mount-security.ts#L214-L245), [mount-security.ts](src/mount-security.ts#L247-L287), [mount-security.ts](src/mount-security.ts#L458-L571)\n\n两个值得注意的设计点：其一，`findAllowedRoot` 按 realpath 长度取最长匹配，解决"宽泛可写根与嵌套只读根并存时谁生效"的歧义——例如白名单同时声明 `~/projects`（可读写）与 `~/projects/secret`（只读），对 `~/projects/secret/x` 的请求会命中更具体的只读根；其二，`nonMainReadOnly` 策略使非主工作区（`is_home === false`）的读写请求整体拒绝，即便该根目录声明了 `allowReadWrite: true`——主工作区与成员工作区在文件系统能力上被显式分层。\n\nSources: [mount-security.ts](src/mount-security.ts#L550-L570), [mount-security-strict.test.ts](tests/mount-security-strict.test.ts#L154-L184)\n\n`validateAdditionalMountsStrict` 在单条验证之上叠加**原子性**：任一挂载无效则抛出 `AdditionalMountValidationError`（含全部 issues），调用方不得部分应用输出；同时检测 containerPath 的重复与相互嵌套（`a/b` 与 `a/b/c` 冲突），冲突场景返回 409 `HOST_MOUNT_TARGET_CONFLICT`。成功时返回双重形式——运行时用 `{realHostPath, /workspace/extra/{suffix}, readonly}` 直接构造 Docker volume，持久化用相对后缀的 `persisted` 形式写库，确保存储与执行使用同一套规范化路径。\n\nSources: [mount-security.ts](src/mount-security.ts#L597-L671), [mount-security-strict.test.ts](tests/mount-security-strict.test.ts#L203-L218)\n\n最终挂载装配在 `buildVolumeMounts` 中完成：持久化挂载以子目录形式叠加在 `data/extra/{folder}` 之上（`/workspace/extra` 为每群组独立的持久化额外目录），只读挂载以 `-v host:container:ro` 生成。主工作区（admin home）额外获得整个项目根目录的读写挂载（`/workspace/project`），这一特权仅限 admin home 场景。\n\nSources: [container-runner.ts](src/container-runner.ts#L1137-L1158), [container-runner.ts](src/container-runner.ts#L1512-L1531), [container-runner.ts](src/container-runner.ts#L1657-L1664)\n\n## 容器内侧：身份归一化与 descriptor-based 权限 watcher\n\n宿主机侧的路径校验只保证"挂载了什么"，不保证"容器内进程以什么身份访问"。`resolveContainerHostIdentity` 在每次启动时重新探测 Docker 安全上下文（`docker info --format {{json .SecurityOptions}}`，不缓存结果），将环境归类为五种模式：\n\n| 模式 | 判定条件 | 处理策略 |\n|---|---|---|\n| `direct` | Linux rootful 且无 userns、uid 非零 | 宿主 uid/gid 透传：entrypoint 重写 `/etc/passwd` 中 node 用户的 uid/gid 为宿主值 |\n| `rootless` | securityOptions 含 rootless | 容器 uid 0 映射到 daemon uid；通过宿主创建的 bind root 验证桥接 |\n| `host-root` | uid 为 0 | 保持 node 身份，仅归一化显式授权的可写挂载 |\n| `virtualized` | macOS/Windows | 保留 Docker Desktop 的 node-owned bind 语义，但永不给 group/other 权限 |\n| `userns` / `unknown` | userns-remap 或探测失败 | **拒绝启动**——rootful userns-remap 没有安全的宿主/会话身份桥 |\n\nSources: [container-runner.ts](src/container-runner.ts#L1561-L1597), [container-runner.ts](src/container-runner.ts#L1599-L1635)\n\n身份归一化的执行体在 `container/session-permissions.sh` 与 `session-permissions-watcher.mjs`。`direct` 模式下先校验宿主 uid/gid 为正整数且不与镜像内已有账户/组冲突，然后通过临时文件 + `awk` 原子改写 `/etc/passwd`（`mktemp` → `chmod 0644` → `mv -f`），任何一步失败即拒绝启动；`userns` 与未知模式直接 `happyclaw_permission_fatal` 退出。entrypoint 以 `umask 0077` 开头，确保新创建文件默认 owner-only。\n\nSources: [session-permissions.sh](container/session-permissions.sh#L29-L49), [session-permissions.sh](container/session-permissions.sh#L51-L141), [entrypoint.sh](container/entrypoint.sh#L4-L17)\n\n容器内权限 watcher 的独到之处在于 **descriptor-based 操作**：所有 chown/chmod 不通过路径字符串，而是经由 `/proc/self/fd/{parent}/{name}` 的 openat 语义逐级打开（`O_NOFOLLOW` 防 symlink 跟随、`O_NONBLOCK` 防挂起），并通过 `/proc/self/fdinfo/{fd}` 解析 `mnt_id` 与根目录比对——若子节点落在不同 mount 边界（`outsideRootMount`），则跳过该子树。这样即使 Agent 在容器内 rename/替换了目录项，也无法把 fchown/fchmod 重定向到 fd 之外；`readDirectory` 同样通过已打开的 fd 进行，agent 伪造的完成标记无法在镜像回滚后保留不安全权限（每次启动强制重扫）。\n\nSources: [session-permissions-watcher.mjs](container/session-permissions-watcher.mjs#L56-L74), [session-permissions-watcher.mjs](container/session-permissions-watcher.mjs#L226-L281), [session-permissions-watcher.mjs](container/session-permissions-watcher.mjs#L358-L404), [session-permissions-mount.mjs](container/session-permissions-mount.mjs#L1-L22)\n\n权限模式采用"mirror 不扩散"策略：对 `/workspace/group`、`/workspace/extra` 等共享挂载根，`mirrorMode` 只把 owner 的 rwx 位复制到 group 位（`(ownerBits << 6) | (ownerBits << 3)`），**永不给 other 任何权限**；对 `.claude`、IPC 等 managed 根使用固定模式 `0o2770`/`0o660`。目录被替换为 symlink（`ELOOP`/`ENOENT`）时视为消失并跳过，不会跟随链接。会话结束时，`happyclaw_stop_session_permission_watcher` 执行最终扫描，把 Claude Code 生成的 0600 文件（如 `settings.json`）修复为宿主后端可读。\n\nSources: [session-permissions-watcher.mjs](container/session-permissions-watcher.mjs#L192-L224), [session-permissions.sh](container/session-permissions.sh#L242-L292)\n\n## 浏览 API 与前端编辑：可导航不等于可挂载\n\n宿主目录选择器（`/api/browse/directories?purpose=mount`）在权限上独立收紧：仅 active 管理员可使用 mount 用途，且白名单不可用时返回 503。路径遍历在 `isHostMountNavigationPathAllowed` 的约束下进行——它允许浏览受保护路径的**祖先**（例如 HOME 本身），但一旦进入受保护路径或其子路径即被拒绝，同时过滤含 socket 组件的路径。列表项的 `selectable` 字段由 `validateMount` 实时判定，因此前端可以呈现"可导航但不可选"的灰色项：用户能看到目录树全貌，但只有真正通过完整验证管线的目录才可被选中挂载。\n\nSources: [routes/browse.ts](src/routes/browse.ts#L105-L143), [mount-security.ts](src/mount-security.ts#L294-L301), [routes/browse.ts](src/routes/browse.ts#L55-L96)\n\n前端 `HostDirectoryMountEditor` 在提交前做第一层表单校验，与后端规则逐条对齐：最多 8 个挂载、宿主路径必须绝对路径、容器路径必须是安全相对后缀（无 `/` 前缀/后缀、无 `\\`/`:`/控制字符、无 `.`/`..`/保留组件 `.npm-global`）、containerPath 重复或相互嵌套即时报错。这层校验的目的不是替代后端，而是把后端拒绝的常见原因（形状、冲突、保留目录）前置到用户体验层——`frontend-host-directory-mount.test.ts` 以契约测试锁定前后端规则一致性。\n\nSources: [HostDirectoryMountEditor.tsx](web/src/components/chat/HostDirectoryMountEditor.tsx#L38-L113), [frontend-host-directory-mount.test.ts](tests/frontend-host-directory-mount.test.ts#L43-L97)\n\n## 文件校验：上传、下载与文本提取的净化边界\n\n文件校验与挂载安全共享同一安全哲学：**所有路径与内容在进入 Agent prompt 或宿主机文件系统前必须经过显式净化，且净化失败时 fail-closed**。\n\n工作区文件上传（`routes/files.ts`）执行五道检查：大小上限（`MAX_FILE_SIZE`，默认 50MB，可经 `MAX_FILE_SIZE_MB` 配置）；文件名禁止含 `..` 或以 `/` 开头；禁止写入系统路径（`isSystemPath`：`logs`、`CLAUDE.md`、`.claude`、`conversations`，在 macOS/Windows 等大小写不敏感文件系统上做 lowercase 比较防止 `Logs/` 绕过）；`validateAndResolvePath` 双重防遍历——先 `path.relative` 判定越界，再沿路径向上寻找最近已存在祖先并比对 `realpath`，拦截"父级是 symlink 且末级尚不存在"的绕过；最终以 `O_NOFOLLOW` 标志写入，防止校验与写入之间的 leaf TOCTOU（校验通过后文件被替换为指向系统敏感路径的 symlink）。\n\nSources: [file-manager.ts](src/file-manager.ts#L54-L94), [file-manager.ts](src/file-manager.ts#L105-L120), [routes/files.ts](src/routes/files.ts#L325-L392), [config.ts](src/config.ts#L30-L35)\n\nIM 渠道下载文件（`im-downloader.ts`）的净化重点在**文件名**，因为返回的相对路径会被直接插入 Agent prompt：`sanitizeImFilename` 先 `path.basename` 剥离目录部分，再系统性替换控制字符（ASCII C0+DEL、C1、行/段分隔符）、bidi 控制符、零宽字符、反引号、box-drawing 整段（防终端逃逸）以及半角/全角中括号（防 `[文件: …]` 围栏被攻破），折叠空白并截断到 200 字符。写入使用 `O_EXCL` 原子创建加递增后缀处理同秒并发冲突，杜绝静默覆写。上传侧另有 `http-upload-policy.ts` 对头像（3MB）与 Skill 压缩包（10MB）设置更严格的 body limit，multipart 开销预留 256KB，防止 Hono 在内存中缓冲不受信的大请求体。\n\nSources: [im-downloader.ts](src/im-downloader.ts#L32-L51), [im-downloader.ts](src/im-downloader.ts#L71-L149), [http-upload-policy.ts](src/http-upload-policy.ts#L1-L26)\n\n文本提取（`file-text-extractor.ts`）是"内容注入 prompt"的最后一环：白名单扩展名（`.txt/.md/.csv/.json/.yml/.xml/.html` 等直接 `fs.readFile`，PDF 走 `pdftotext`，Office 走 `textutil`/`pandoc`），所有输出统一截断到 20KB，截断边界回退到合法 UTF-8 字符边界并附加截断标记；提取失败（缺二进制、超时、不可读）时返回 `null`，调用方回退为仅引用文件路径——**绝不把部分提取或未净化的原始内容当作成功结果**。图片附件则通过 `image-detector` 的 magic bytes 严格检测 MIME，声明值与检测值不一致时以检测值为准，防止伪造 MIME 类型的内容混入 Agent 视觉上下文。\n\nSources: [file-text-extractor.ts](src/file-text-extractor.ts#L28-L56), [file-text-extractor.ts](src/file-text-extractor.ts#L86-L152), [message-attachments.ts](src/message-attachments.ts#L43-L77)\n\n## 测试保障：安全属性以契约锁定\n\n挂载安全与文件校验的安全属性由三层测试锁定：`mount-security-strict.test.ts` 覆盖验证管线的全部拒绝分支（形状畸形、特权路径硬拒绝、只读根拒绝读写、宿主路径含冒号/控制字符、批量挂载原子性、白名单热收紧、目录删除后重校验失败、目录被替换为逃逸 symlink 后重校验失败）；`container-runner-host-mount.test.ts` 覆盖运行时授权契约（owner 降级/禁用立即失效、畸形持久化形状拒绝、原始损坏 DB 配置作为运行时安全阻断保留）；`container-session-permissions.test.ts` 覆盖身份探测契约与 entrypoint 权限脚本的静态契约（含"无 world-permission fallback"与"mount 元数据解析无唯一合法 mnt_id 即 fail-closed"）。这三层分别对应宿主机侧路径策略、运行时授权、容器内侧身份归一化，任何一层的回归都会在 CI 中被拦截。\n\nSources: [mount-security-strict.test.ts](tests/mount-security-strict.test.ts#L86-L271), [container-runner-host-mount.test.ts](tests/container-runner-host-mount.test.ts#L139-L206), [container-session-permissions.test.ts](tests/container-session-permissions.test.ts#L252-L284)\n\n## 下一步阅读\n\n挂载安全与文件校验是 RBAC 权限模型的延伸——挂载权限的授予主体（active 管理员）与资源隔离规则详见 [RBAC 权限模型与资源隔离](24-rbac-quan-xian-mo-xing-yu-zi-yuan-ge-chi)；容器内侧的权限 watcher 运行于 [Host 与 Container 双执行模式](7-host-yu-container-shuang-zhi-xing-mo-shi) 描述的容器生命周期之内；文件校验与提取的产物最终经 [StreamEvent 流式事件体系与类型同步](11-streamevent-liu-shi-shi-jian-ti-xi-yu-lei-xing-tong-bu) 流入 Agent 上下文。若关注容器内安全上下文的完整装配流程（含环境变量遮蔽与 npm 全局目录隔离），可回溯 [主服务架构与核心模块划分](9-zhu-fu-wu-jia-gou-yu-he-xin-mo-kuai-hua-fen) 与 [数据目录结构与一致性备份恢复](23-shu-ju-mu-lu-jie-gou-yu-zhi-xing-bei-fen-hui-fu)。';export{t as default};

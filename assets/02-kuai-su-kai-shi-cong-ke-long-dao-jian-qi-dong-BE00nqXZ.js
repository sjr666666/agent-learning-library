const n='本页面向第一次接触 Miniclaw 的开发者，目标是让你在十分钟内完成「克隆仓库 → 一键启动 → 登录工作台」的完整流程。内容覆盖前置环境检查、`make dev` 的内部机制、首次启动的管理员初始化向导、开发模式与生产模式的端口差异，以及新手最常见的几类故障排查。更深入的架构原理请阅读后续目录页，本页只聚焦"跑起来"这一件事。\n\n## 环境准备：开始前需要装什么\n\n在敲下第一行命令之前，先确认本机满足以下条件。Miniclaw 是一个 TypeScript 全栈项目，由 Backend、Web 前端和 Agent Runner 三个子项目组成，因此依赖检查比单包项目多一步，但全部工具都是常见标配：\n\n| 工具 | 版本要求 | 是否必需 | 用途说明 |\n| --- | --- | --- | --- |\n| Node.js | ≥ 20 | ✅ 必需 | 后端以原生 Node 工具链运行（npm/npx/tsx/node） |\n| npm | 随 Node 附带 | ✅ 必需 | 安装根目录、`web/`、`container/agent-runner/` 三处依赖 |\n| GNU Make | 任意稳定版 | ✅ 必需 | 执行 `make dev` 等一键目标 |\n| Git | 任意稳定版 | ✅ 必需 | 克隆仓库 |\n| Docker | 任意近期版本 | ⭕ 可选 | 仅"容器执行模式"需要；没有 Docker 也能用 Host 模式运行 Agent |\n\nNode 版本下限写在 `package.json` 的 `engines` 字段中（`node >= 20`），README 徽章同样标注了这一要求。特别提醒：**不要用 Bun 替代 Node 运行本项目**——主服务的 WebSocket 依赖 `ws` 包配合 `@hono/node-server` 的 `server.on(\'upgrade\')` 握手，该模式在 Bun 的 HTTP server 下不触发，会导致所有实时流式推送失效（HTTP 接口看似正常，但前端流式卡片全挂），Makefile 注释中明确记录了这一约束。\n\nSources: [package.json](package.json#L76-L78), [README.md](README.md#L74-L76), [Makefile](Makefile#L7-L11)\n\n## 一键启动：三条命令\n\n满足环境要求后，完整启动只需三条命令：\n\n```bash\ngit clone https://github.com/helsome/miniclaw.git\ncd miniclaw\nmake dev\n```\n\n`make dev` 会自动安装三端依赖并启动 Backend（`:3000`）与 Vite Web Client（`:5173`）。看到启动日志后，打开 <http://localhost:5173>，完成管理员初始化即可进入工作台；Provider 与消息渠道配置可以选「稍后设置」，之后随时在设置页补齐——这意味着你不需要提前准备任何 API Key 就能把系统跑起来。\n\nSources: [README.md](README.md#L78-L86)\n\n## make dev 背后发生了什么\n\n对初学者来说，理解这条命令做了什么比背下它更重要。`make dev` 并非黑盒，而是一条精心编排的自检流水线，其执行顺序如下图所示：\n\n```mermaid\nflowchart TD\n    A["git clone + cd miniclaw"] --> B["make dev"]\n    B --> C{"三处依赖是否缺失或过期？"}\n    C -->|"是"| D["自动安装依赖<br/>根目录 / web / container/agent-runner"]\n    C -->|"否"| E["物化内置 Skills"]\n    D --> E\n    E --> F{"检测到 docker 命令？"}\n    F -->|"是"| G["拉取 Agent 容器镜像<br/>helsome/miniclaw-agent:latest"]\n    F -->|"否"| H["跳过，后续仅 Host 模式"]\n    G --> I["编译 agent-runner"]\n    H --> I\n    I --> J["npm run dev:all<br/>concurrently 同时拉起 Backend 与 Vite"]\n    J --> K["浏览器打开 http://localhost:5173"]\n```\n\n第一步是**依赖新鲜度检查**：只要任一处 `node_modules` 缺失，或对应的 `package.json`/lockfile 比 `node_modules` 更新，就重新安装。第二步通过 `_ensure-builtin-skills` 校验固定版本内置 Skills 目录，缺失时调用脚本自动物化——这是 Skills 能力体系的地基。第三步仅在检测到 `docker` 命令时才拉取镜像，所以**没装 Docker 的机器不会在这里报错中断**。最后通过 `dev:all` 脚本用 `concurrently` 同时启动两个进程：后端以 `tsx src/index.ts` 直接热运行 TypeScript 源码，前端进入 Vite 开发服务器。\n\nSources: [Makefile](Makefile#L24-L30), [Makefile](Makefile#L80-L86), [Makefile](Makefile#L175-L178), [package.json](package.json#L21)\n\n## 首次启动：管理员初始化向导\n\n打开 `http://localhost:5173` 后，你会先进入初始化向导而不是登录页。这个流程由后端的认证路由严格把关：\n\n```mermaid\nflowchart LR\n    A["访问 localhost:5173"] --> B{GET /api/auth/status<br/>已有用户？}\n    B -->|"无任何用户"| C["SetupPage 初始化向导<br/>设置管理员用户名与密码"]\n    B -->|"已初始化"| D["LoginPage 登录"]\n    C --> E["创建 admin 用户<br/>+ admin Home Group<br/>+ 自动写入 Session Cookie"]\n    E --> F["Provider / 渠道配置页<br/>（可跳过，稍后在设置页补齐）"]\n    F --> G["进入工作台"]\n    D --> G\n```\n\n关键机制有三点值得注意。其一，`POST /api/auth/setup` 是公开路由，但**只在系统中不存在任何用户时有效**，重复调用会返回 `System already initialized`，因此不存在被旁路重放的风险。其二，完成 setup 时后端会顺带为新管理员创建一个 Home Group（`web:main`，Host 模式），这就是你在工作台看到的第一个默认工作区。其三，在系统未初始化前，其他认证接口会被强制拦下并返回 403（提示「系统尚未初始化，请先完成管理员设置」），保证首个账号必然经由向导创建。\n\n前端侧对应的页面组件为 `SetupPage.tsx`、`SetupProvidersPage.tsx` 与 `SetupChannelsPage.tsx` 三步向导，Provider 与渠道两步均可跳过延后配置。\n\nSources: [auth.ts](src/routes/auth.ts#L139-L142), [auth.ts](src/routes/auth.ts#L170-L207), [auth.ts](src/routes/auth.ts#L361-L378)\n\n## 启动后的端口与进程拓扑\n\n开发模式下机器上会有两个服务端口协同工作，理解它们的分工能帮你看懂浏览器地址栏为什么是 5173 而不是 3000：\n\n```mermaid\nflowchart LR\n    subgraph 浏览器\n        UI["Web 工作台 UI"]\n    end\n    UI -->|"http://localhost:5173"| Vite["Vite Dev Server :5173"]\n    Vite -->|"/api 请求反向代理"| Backend["Hono Backend :3000"]\n    Vite -->|"/ws WebSocket 反向代理"| Backend\n    Backend --> DB[("SQLite<br/>data/db/messages.db")]\n    Backend --> Runner["Pi Agent Runner<br/>Host / Docker 双模式"]\n```\n\nVite 开发服务器固定监听 `5173` 端口，并把 `/api` 与 `/ws` 两类请求透明转发到 `127.0.0.1:3000` 的后端，所以前端代码可以直接用相对路径调 API，无需关心跨域。后端端口由 `WEB_PORT` 环境变量控制，默认 `3000`；运行时数据统一落在项目根的 `data/` 目录下（数据库在 `data/db/`，工作区文件夹在 `data/groups/`）。生产模式的拓扑则完全不同——后端会直接托管编译产物 `web/dist` 静态文件，**整个系统收缩为单一 `:3000` 端口**，不再需要 Vite。\n\nSources: [vite.config.ts](web/vite.config.ts#L23-L38), [config.ts](src/config.ts#L33-L35), [config.ts](src/config.ts#L60), [web.ts](src/web.ts#L1262-L1311)\n\n## 手动等价路径与生产启动\n\n如果不想用 Make 目标（例如在 CI 或受限 shell 中），可以手动执行完全等价的步骤：\n\n```bash\nnpm install                            # 根目录后端依赖\nnpm --prefix web install               # Web 前端依赖\nnpm --prefix container/agent-runner install  # Agent Runner 依赖\n\nnpm run build:all   # 构建 Backend、Web 与 Agent Runner\nnpm start           # 生产式本地服务，默认 http://127.0.0.1:3000\n```\n\n开发热更新则对应 `make dev` 底层实际调用的 `npm run dev:all`。三种启动方式的差异如下表：\n\n| 方式 | 命令 | 进程形态 | 访问地址 | 适用场景 |\n| --- | --- | --- | --- | --- |\n| 开发模式 | `make dev` / `npm run dev:all` | tsx 直跑源码 + Vite 热更新，双进程双端口 | `http://localhost:5173` | 日常开发调试 |\n| 生产模式 | `make start` | 编译后 `node dist/index.js`，单进程单端口 | `http://127.0.0.1:3000` | 本地长期自托管 |\n| 手动生产 | `npm run build:all && npm start` | 同上，但跳过 Make 的自动检查 | `http://127.0.0.1:3000` | CI / 受限环境 |\n\n推荐初学者日常使用 `make dev`；想模拟真实部署时用 `make start`——它会先检查端口占用（被占用则拒绝启动并提示 `make stop`），再按依赖新鲜度按需重建三端产物，最后以前台阻塞方式运行编译后的 Node 进程。若还想体验桌面客户端，在前两者运行的基础上追加 `npm run desktop:dev` 即可拉起 Electron Shell。\n\nSources: [README.md](README.md#L88-L106), [Makefile](Makefile#L52-L69), [README.md](README.md#L174-L188)\n\n## 配置：环境变量与 .env 文件\n\nMiniclaw 的设计哲学是**所有环境变量都有默认值**——零配置即可启动，`.env` 文件只在需要覆盖默认行为时才创建（文件不存在属于正常情况，后端会静默跳过加载）。快速开始阶段最可能用到的变量如下：\n\n| 环境变量 | 用途 | 默认值 |\n| --- | --- | --- |\n| `WEB_PORT` | Backend 监听端口（Makefile 与 config 共用） | `3000` |\n| `MINICLAW_CONTAINER_IMAGE` | Agent 容器执行模式使用的镜像 | `helsome/miniclaw-agent:latest` |\n| `CONTAINER_IMAGE` | 上者的兼容别名 | 同上 |\n| `MINICLAW_SERVER_URL` | Electron 桌面端要连接的 Backend 地址 | `http://127.0.0.1:3000` |\n| `HTTPS_PROXY` / `HTTP_PROXY` | 让后端 server-side fetch 走系统代理 | 未配置则直连 |\n\n`.env` 通过 Node 原生的 `process.loadEnvFile()` 在所有模块读取环境变量之前加载（这也是它在 `index.ts` 中必须是第一个 import 的原因）。另一个对国内用户实用的细节：一旦检测到代理变量，后端会用 undici 的 `EnvHttpProxyAgent` 让全局 `fetch` 遵守 `HTTP(S)_PROXY` 与 `NO_PROXY`，避免官方 Provider OAuth 交换因裸出口 IP 被拒。安全红线只有一条：**不要把 API Key、Session Cookie 写进命令行历史或提交进仓库**。\n\nSources: [load-env.ts](src/load-env.ts#L1-L12), [load-env.ts](src/load-env.ts#L14-L44), [README.md](README.md#L155-L163)\n\n## 常见问题排查\n\n初次启动遇到问题时，绝大多数都能在下表中对症解决：\n\n| 症状 | 原因 | 解决办法 |\n| --- | --- | --- |\n| 启动报错「端口已被占用」 | 旧的 Miniclaw 进程仍在运行 | 执行 `make stop`（按端口杀进程），或用 `PORT=4000 make dev` 换端口 |\n| 忘记管理员密码 | 密码不可找回 | `npm run reset:admin -- <用户名> <新密码≥8位>` 重置 |\n| 没装 Docker，`make dev` 卡在拉镜像？ | 不会卡——无 docker 命令时镜像步骤自动跳过 | 无需处理；Agent 将以 Host 模式执行 |\n| 页面正常但流式回复不实时 | 使用了 Bun 运行，WebSocket 握手失败 | 改用 Node 运行（`make dev` 已内置此保障） |\n| 想确认服务是否健康 | — | `make status` 会请求 `/api/health` 并汇报进程、日志、容器三类状态 |\n| 日志去哪看 | 前台模式直接打印终端 | 后台运行时可 `make start > /tmp/miniclaw.log 2>&1 &` 后用 `make logs` 跟踪 |\n\n其中 `reset-admin` 脚本要求新密码至少 8 个字符，且会把目标账号恢复为激活状态的 admin 角色；如果想把系统彻底打回首装状态（清空数据库、配置、工作区、记忆与会话），可执行 `make reset-init`——这是破坏性操作，执行前务必确认 `data/` 目录中没有需要保留的数据。\n\nSources: [Makefile](Makefile#L124-L146), [reset-admin.ts](src/reset-admin.ts#L13-L16), [Makefile](Makefile#L236-L238)\n\n## 下一步阅读建议\n\n到这里，你已经拥有一个可登录、可对话的 Miniclaw 实例。建议按照以下顺序继续深入：先读 [项目概览：Miniclaw 是什么](1-xiang-mu-gai-lan-miniclaw-shi-shi-yao) 建立 Agent-first 产品模型的认知框架，再到 [开发工作流与常用命令速查](3-kai-fa-gong-zuo-liu-yu-chang-yong-ming-ling-su-cha) 补齐日常 typecheck/test/format 的肌肉记忆；当你准备修改代码时，[目录导航与模块地图：从 src 到 container 再到 web](6-mu-lu-dao-hang-yu-mo-kuai-di-tu-cong-src-dao-container-zai-dao-web) 会带你定位每个功能所在的模块，而 [整体架构：Backend、Pi Runner、Workspace 与客户端的边界划分](4-zheng-ti-jia-gou-backend-pi-runner-workspace-yu-ke-hu-duan-de-bian-jie-hua-fen) 则解释了本页出现的 Backend、Runner、Workspace 各自承担的职责边界。';export{n as default};

const e='飞书渠道的会话模型在 HappyClaw 中经历了一次关键的概念分层：**会话语义**（这条消息在哪个逻辑上下文中、是否被激活）与**话题路由**（消息递送到哪个 Agent 会话）被拆成两个独立决策维度。本页剖析飞书 `p2p` / `group` / `topic` 三种会话形态的识别机制、激活模式（activation mode）、确定性会话计划解析器 `resolveFeishuConversationPlan`，以及原生话题（thread/topic）如何映射为独立的会话代理（conversation agent）并持久化到 `im_context_bindings`。贯穿始终的安全准则是：识别不了就 fail-closed，绝不猜测。\n\n## 飞书会话形态：opaque 标识与元数据权威性\n\n飞书用同一套 `oc_*` 前缀标识私聊（p2p）与群聊（group），JID 本身无法区分会话类型，因此会话分类必须依赖实时元数据。`resolveChannelConversationKind` 对 `feishu:` 前缀的 JID 强制要求 `chat_mode` 或 `feishu_chat_mode` 字段：`p2p` 判定为 `direct`，`group`/`topic` 判定为 `group`，两者皆无则返回 `unknown` 并拒绝绑定——注释明确写着"durable/live metadata is authoritative and the JID alone must never be guessed"。这种 fail-closed 设计同时约束了绑定方向：工作区绑定只接受 `group`，会话绑定只接受 `direct`，`unknown` 一律报错。\n\n```mermaid\nflowchart LR\n    JID["feishu:oc_xxx"] --> K{resolveChannelConversationKind}\n    K -->|chat_mode = p2p| D["direct → 会话绑定"]\n    K -->|chat_mode = group| G["group → 工作区绑定"]\n    K -->|chat_mode = topic| T["topic → group + 原生话题容器"]\n    K -->|无元数据| U["unknown → fail-closed 拒绝"]\n```\n\n元数据的两个来源汇入同一判定：实时的 `chat.get`/`chat.list` 响应携带 `chat_mode` 与 `group_message_type`（`thread` 表示群开启了话题模式），缓存在 `chatInfoById`；持久化的 `feishu_chat_mode`/`feishu_group_message_type` 则落在 `registered_groups` 上。`isNativeContextContainer` 在两者之上叠加激活模式判断：Feishu 群若 `chat_mode === \'group\'`，只有 `require_mention` 语义开启时才被视为话题容器（`thread_map`），因为"每次 @ 开启一个独立会话"本身就是一种隐式话题映射。\n\nSources: [channel-conversation-kind.ts](src/channel-conversation-kind.ts#L12-L74)、[feishu.ts](src/feishu.ts#L3652-L3680)、[channel-mount-service.ts](src/channel-mount-service.ts#L125-L156)\n\n## 激活模式：谁有资格触发 Agent\n\n飞书群聊的触发资格由 `FeishuActivationMode` 五态模型控制，私聊不受此约束（永远放行）。`requiresMention` 纯函数给出判定：`when_mentioned`/`owner_mentioned` 强制要求 @，`always`/`disabled` 免 @，`auto` 则回退到历史 `require_mention` 布尔标志。\n\n| 模式 | 群消息免 @？ | 语义 |\n| --- | --- | --- |\n| `auto` | 取决于 `require_mention` | 兼容旧配置，显式 flag 兜底 |\n| `always` | 是 | 群内所有消息进入共享会话 |\n| `when_mentioned` | 否 | 仅 @ 机器人时处理，@ 后开启独立上下文 |\n| `owner_mentioned` | 否（且仅群主） | @ 机器人且发送者是登记的 owner 才处理 |\n| `disabled` | 硬停止 | 即使 @ 也无法绕过（mention gate 直接拒绝） |\n\n`feishu-mention-gate.ts` 的 `evaluateMentionGate` 是纯函数实现，其行为表锁定了三个关键边界：`botOpenId` 未知时 fail-closed（`bot_open_id_missing`），区别于未 @（`not_mentioned`）；`conversationPlan.disabled` 在任何检查之前短路；`conversationPlan.allowWithoutMention` 存在时取代旧的 `shouldProcessGroupMessage` 回调——这正是"已激活话题内的后续消息无需再次 @" 得以成立的机制。注意观众策略（audience policy）是独立于 @/话题激活的身份边界，在命令与 mention 门控之前执行，对私聊和已激活话题同样生效。\n\nSources: [feishu-conversation-policy.ts](src/feishu-conversation-policy.ts#L1-L60)、[feishu-mention-gate.ts](src/feishu-mention-gate.ts#L60-L162)、[feishu.ts](src/feishu.ts#L1950-L1966)\n\n## 会话计划解析器：确定性决策流\n\n`resolveFeishuConversationPlan` 把"激活判定 + 会话隔离"合并为一次确定性决策，输出 `FeishuConversationPlan`。设计上的关键分层（文件头注释）是：**投递位置（reply_in_thread）与会话身份刻意分离**——always-on 的普通群可以在既有飞书话题内回复，但仍共享该群唯一的工作区会话。\n\n```mermaid\nflowchart TD\n    A[输入: chatType/chatMode/activationMode/threadId/rootId/activeContext] --> B{activationMode = disabled?}\n    B -->|是| X1[disabled 硬停止]\n    B -->|否| C{chatType = p2p?}\n    C -->|是| X2[direct 免 @ 共享会话]\n    C -->|否| D{activeContext 且<br/>topic 或需要 @ 且带 threadId?}\n    D -->|是| X3[active_context 沿用持久绑定<br/>免 @ 独立上下文]\n    D -->|否| E{topicChat?}\n    E -->|是| F{需要 @ 且未 @?}\n    F -->|是| X4[mention_required]\n    F -->|否| X5[new_native_topic<br/>contextId = threadId/rootId/messageId]\n    E -->|否| G{需要 @ 且带 threadId?}\n    G -->|是| H{未 @?}\n    H -->|是| X6[mention_required]\n    H -->|否| X7[new_native_topic<br/>contextId = threadId]\n    G -->|否| I{免 @?}\n    I -->|是| X8[shared_chat 共享会话]\n    I -->|否| J{未 @?}\n    J -->|是| K{有 activeContext?}\n    K -->|是| X9[active_context 免 @ 沿用话题]\n    K -->|否| X10[mention_required]\n    J -->|否| X11[new_mention_context<br/>锚定本条消息自身]\n```\n\n七种 `reason` 语义表：\n\n| reason | 免 @ | 独立上下文 | contextId 来源 | 典型场景 |\n| --- | --- | --- | --- | --- |\n| `disabled` | 否 | 否 | — | 配置禁用，@ 也无法绕过 |\n| `direct` | 是 | 否 | — | p2p 私聊共享一个会话 |\n| `shared_chat` | 是 | 否 | — | always-on 普通群共享工作区会话 |\n| `new_mention_context` | 否 | 是 | `messageId` 自身 | 普通群内顶楼 @，开启新会话 |\n| `new_native_topic` | 视模式 | 是 | `threadId`（或 root/message） | 真实飞书话题的首次激活 |\n| `active_context` | 是 | 是 | 持久绑定中的 contextId | 已激活话题内的后续消息 |\n| `mention_required` | 否 | 否 | — | 未 @，被门控拒绝 |\n\n两个值得注意的防御性分支：其一，`active_context` 分支只在"话题群"或"需要 @ 且带 threadId"时触发，且要求输入中已存在 `activeContext`——这保证已建立的话题内再次 @ 不会嵌套创建新的会话代理；其二，普通群中的 `rootId` 可能只是普通回复链（reply chain）而非真实话题，若继承它会把新请求错误并入旧会话，因此 `new_mention_context` 始终锚定触发消息自身，旧链只作为有界引用上下文保留。测试 `feishu-conversation-policy.test.ts` 用 16 个用例完整锁定了这张决策表。\n\nSources: [feishu-conversation-policy.ts](src/feishu-conversation-policy.ts#L74-L214)、[tests/feishu-conversation-policy.test.ts](tests/feishu-conversation-policy.test.ts#L19-L229)\n\n## 原生话题到会话代理的映射（thread_map）\n\n话题群的会话路由走 `thread_map` 模式：每个飞书原生话题（`thread_id`）被映射为一个 `kind: \'conversation\'` 的子代理，挂在群所绑定工作区之下。`resolveOrCreateNativeThreadAgent` 是这条映射的核心：先按 `contextId` 查 `im_context_bindings`，命中且代理仍归属同一工作区则复用（仅刷新时间戳），否则新建代理并持久化绑定。\n\n```mermaid\nsequenceDiagram\n    participant F as feishu.ts 入站\n    participant P as resolveFeishuConversationPlan\n    participant R as resolveEffectiveChatJid\n    participant A as resolveOrCreateNativeThreadAgent\n    participant DB as im_context_bindings\n\n    F->>P: chatJid + messageMeta(threadId/rootId)\n    P-->>F: plan(contextId, rootMessageId, independentContext)\n    F->>F: 构造 nativeContextType=\'thread\' 的 meta\n    F->>R: resolveAdmittedChannelRoute\n    R->>A: mount.routing_mode === \'thread_map\' 且有 nativeThread\n    A->>DB: getImContextBinding(sourceJid, \'thread\', contextId)\n    alt 无绑定或代理失效\n        A->>A: 创建 SubAgent(source_kind=\'native_thread\')\n        A->>DB: upsertImContextBinding 持久化\n    else 已存在\n        A->>DB: touchImContextBindingActivity 轻量刷新\n    end\n    A-->>R: effectiveJid = workspaceJid#agent:agentId\n```\n\n新建代理携带完整的原生话题身份字段：`source_kind: \'native_thread\'`、`thread_id`、`root_message_id`、`title_source: \'native_root\'`，并在工作区内创建虚拟聊天 `workspaceJid#agent:agentId`。`im_context_bindings` 表以 `(source_jid, context_type, context_id)` 为主键，`root_message_id` 与 `title` 用于后续的身份回溯与前端展示。绑定一旦建立，"该话题的后续消息"通过 `findActiveFeishuContext` 的候选 ID 查找（`contextId` → `threadId` → `rootId` 依次尝试）直接命中同一代理，不再需要新的 @。\n\nSources: [index.ts](src/index.ts#L18318-L18417)、[db.ts](src/db.ts#L705-L719)、[channel-mount-service.ts](src/channel-mount-service.ts#L306-L334)\n\n## 路由 JID 编码与账户作用域\n\n飞书出站目标被编码进路由 JID：`feishu:oc_123#thread:omt_xxx#root:om_root`。`parseFeishuRouteTarget`/`buildFeishuRouteTarget` 负责解析与组装，`replyInThread` 由 `rootMessageId` 是否存在推导。入站侧，`deliveryRootMessageId` 的计算遵循三分支：独立上下文用 `plan.rootMessageId`；否则有 `threadId` 用 `rootMessageId`（`threadId || rootId || messageId`）；都没有才用 `rootId`。\n\n账户作用域是这条编码链上踩过坑后形成的硬约束：`target.raw` 来自原始 `chat_id`，不带账户作用域，直接发射会导致 `getRegisteredGroup`（按 JID 精确匹配）查不到账号作用域化后的群记录，整个 turn fail-closed——没有出站路由、没有 warm-session 准入、连尾部中断通知都发不出。因此 `feishuRouteToJid` 与 `buildNativeThreadRouteJid` 都在已规范化的 base JID 上追加 `#thread:`/`#root:` 片段，`scopeChannelJid` 保留这些 provider 原生片段而只替换 `account:` 片段。\n\n```mermaid\nflowchart LR\n    B["feishu:oc_123#account:bot-a"] --> A\n    R["thread:omt_xxx + root:om_root"] --> A[buildNativeThreadRouteJid]\n    A --> J["feishu:oc_123#account:bot-a#thread:omt_xxx#root:om_root"]\n    J --> G{getRegisteredGroup 精确匹配}\n    G -->|命中 base 群记录| OK["路由到 thread_map 工作区"]\n```\n\n出站回复时，`replyToFeishuMessage` 以 `reply_in_thread: true` 调用 SDK；若飞书返回 `230071`/`230072`（该会话不支持话题内回复），仅对"线程回复"这一物理步骤降级重试为普通回复，上传与已发送的附件不受影响。普通群目标永远创建顶楼消息，绝不继承群里最近的活跃话题——`resolveFeishuMessageAnchor` 只为 p2p 使用 `lastMessageId`。\n\nSources: [feishu.ts](src/feishu.ts#L536-L700)、[feishu.ts](src/feishu.ts#L1661-L1730)、[channel-address.ts](src/channel-address.ts#L27-L93)、[channel-native-context.ts](src/channel-native-context.ts#L40-L47)\n\n## 上下文身份查找与源 JID 隔离\n\n`findActiveFeishuContext` 提供三条查找路径：先遍历 `[contextId, threadId, rootId]` 候选集逐个查询 `im_context_bindings`（校验代理仍归属期望工作区），再按 `root_message_id` 反查——这覆盖了飞书偶发"thread_id 出现前消息先带 root_id"的时序问题。身份隔离的根基在 `im_context_bindings` 的复合主键：`source_jid` 含账号作用域，因此**不同渠道/不同账号下的相同 contextId 永不相交**——`native-context-db.test.ts` 明确验证了 Feishu 与 Telegram 各自持有 `same-context` 时互不干扰。解绑清理同理只删除精确匹配 `source_jid` 的回复路由，不影响同 contextId 的其他账号。\n\nSources: [index.ts](src/index.ts#L18431-L18473)、[tests/native-context-db.test.ts](tests/native-context-db.test.ts#L84-L165)\n\n## 工作区导航与前端呈现\n\n话题映射的副作用延伸到工作区元数据：`markThreadMapWorkspace` 会把被话题容器挂载的工作区升级为 `conversation_source: \'native_thread\'`（保留历史 `feishu_thread` 标记）+ `conversation_nav_mode: \'vertical_threads\'`，前端据此切换为垂直话题导航视图。解绑时 `buildDetachedWorkspaceUpdate` 将导航模式还原为 `manual`/`horizontal`，但**不删除**话题期间创建的会话与上下文绑定——重新绑定同一话题渠道即可恢复历史。\n\nSources: [channel-mount-service.ts](src/channel-mount-service.ts#L623-L652)、[index.ts](src/index.ts#L1962-L1978)、[ChatView.tsx](web/src/components/chat/ChatView.tsx#L367-L374)\n\n## 安全边界与 fail-closed 语义汇总\n\n| 边界 | 机制 | 后果 |\n| --- | --- | --- |\n| 会话类型无法判定 | `ChannelConversationKind = \'unknown\'` | 拒绝绑定，不猜测 |\n| `botOpenId` 未知 | mention gate 返回 `bot_open_id_missing` | 拒绝而非放行 |\n| 话题容器绑定固定会话 | `upgradeNativeContextChannelMount` 返回 `conflict: bound_to_session` | 丢弃消息 |\n| 路由 JID 缺账户作用域 | `getRegisteredGroup` 精确匹配失败 | turn 整体 fail-closed |\n| 话题内回复不支持 | 错误码 230071/230072 单步降级 | 仅该物理发送重试为普通回复 |\n| 已激活话题内再次 @ | `active_context` 分支 | 不嵌套新建会话代理 |\n\nSources: [channel-mount-service.ts](src/channel-mount-service.ts#L300-L334)、[feishu.ts](src/feishu.ts#L613-L632)、[feishu-conversation-policy.ts](src/feishu-conversation-policy.ts#L108-L123)\n\n## 延伸阅读\n\n- 话题路由的可靠性依托于渠道可靠性状态机：[渠道可靠性状态机：Inbox、Turn 与 Outbox](16-qu-dao-ke-kao-xing-zhuang-tai-ji-inbox-turn-yu-outbox)\n- 话题代理是会话代理（`kind: \'conversation\'`）的一种，其生命周期与挂载边界见[渠道挂载与会话绑定边界](14-qu-dao-gua-zai-yu-hui-hua-bang-ding-bian-jie)\n- 会话代理的并发与排队语义见[会话队列与并发控制](18-hui-hua-dui-lie-yu-bing-fa-kong-zhi)\n- 多账号连接池中账号作用域化 JID 的构造见[IM 渠道架构与多账号连接池](13-im-qu-dao-jia-gou-yu-duo-zhang-hao-lian-jie-chi)';export{e as default};

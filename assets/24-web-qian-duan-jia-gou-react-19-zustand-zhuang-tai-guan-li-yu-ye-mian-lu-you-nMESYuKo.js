const e='Miniclaw 的 Web 前端（`web/` 目录）是一个独立的 Vite + React 19 单页应用，承担产品的工作台角色：会话交互、智能体配置、任务监控与系统管理全部经由它完成。本文从第一性原理出发，拆解该前端的三根支柱——**以 React Router v7 数据路由器为核心的页面路由体系**、**以 Zustand 5 为骨架的全局状态层**、以及支撑流式对话体验的**数据获取与实时事件管线**，并说明代码组织如何服务于"高频流式更新下的渲染性能"这一核心约束。\n\n## 技术栈总览与分层架构\n\n前端的依赖选型高度克制：核心运行时依赖仅十余个，UI 组件基于 shadcn/ui（radix-nova 风格）+ Tailwind CSS 4 构建，Markdown 渲染链条（remark/rehype 插件族、highlight.js、KaTeX、mermaid）则是体量最大的部分——这也直接催生了后文的路由级懒加载策略。\n\n| 层次 | 技术选型 | 职责 |\n|------|---------|------|\n| 视图层 | React 19 + JSX 函数组件 | 页面与组件渲染，`lazy`/`Suspense` 分片 |\n| 路由层 | react-router-dom 7 | `createBrowserRouter` / `createHashRouter` 双模式数据路由 |\n| 状态层 | zustand 5 | 16 个领域 store，纯 `create` 用法、零中间件 |\n| 数据层 | 原生 fetch 封装 + WebSocket 单例 | REST 请求超时控制 + 实时事件分发 |\n| UI 基建 | shadcn/ui、Tailwind 4、lucide-react、sonner | 无头组件、原子化样式、Toast |\n| 构建链 | Vite 6 + TypeScript 5.7 + @tailwindcss/vite | 开发代理、路径别名、生产构建 |\n| E2E | Playwright（Pixel 7 移动仿真项目） | 移动端聊天场景回归 |\n\nSources: [package.json](web/package.json#L1-L67), [components.json](web/components.json#L1-L24), [playwright.config.ts](web/playwright.config.ts#L1-L42)\n\n整体分层关系如下图所示。值得注意的是，状态层并不直接耦合视图：WebSocket 事件既可由页面组件订阅后写入 store，也可由布局组件在全局层面直接通过 `useXStore.getState()` 写入，形成"事件 → store → 订阅组件"的单向数据流。\n\n```mermaid\nflowchart TB\n    subgraph Entry["入口层"]\n        HTML["index.html<br/>认证预热 + 主题引导"]\n        Main["main.tsx<br/>StrictMode 挂载"]\n    end\n    subgraph Router["路由层 App.tsx"]\n        Guard["AuthGuard 守卫"]\n        Layout["AppLayout 布局路由"]\n        Pages["19 个懒加载页面"]\n    end\n    subgraph State["状态层 stores/"]\n        Chat["chat.ts (~4400 行)"]\n        Auth["auth.ts"]\n        Others["其余 14 个领域 store"]\n    end\n    subgraph Data["数据层 api/"]\n        REST["client.ts<br/>apiFetch + 超时/401 处理"]\n        WS["ws.ts<br/>WsManager 重连单例"]\n    end\n    Backend["Hono 后端 :3000"]\n\n    HTML --> Main --> Router\n    Guard --> Layout --> Pages\n    Pages -->|"窄 selector"| State\n    State --> REST\n    Pages -->|"WS 订阅"| WS\n    Layout -->|"getState() 全局写入"| State\n    REST --> Backend\n    WS --> Backend\n```\n\n## 应用入口：HTML 解析期的两段关键优化\n\n启动时序的设计目标只有一个——压缩首屏可用时间。`index.html` 在 HTML 解析阶段就完成了两件通常由 JS 框架负责的事。\n\n第一是**认证预热**。由于 `AuthGuard` 在认证检查完成前不渲染任何已登录子树，如果等入口 JS 下载执行后才发 `/api/auth/me`，这一次 RTT 会与分片下载串行叠加。因此内联脚本在解析期就发起请求并存入 `window.__authPrewarm`，随后 `stores/auth.ts` 的 `checkAuth` 会一次性消费这个 Promise，把整跳串行压缩为并行。\n\n第二是**无闪烁主题引导**。另一段内联脚本在 `<div id="root">` 渲染前读取 localStorage 中的主题、色系与字体偏好，同步为 `<html>` 添加 `dark` / `theme-orange` 等 class，避免暗色用户看到白屏闪烁。\n\nSources: [index.html](web/index.html#L44-L54), [index.html](web/index.html#L57-L73), [stores/auth.ts](web/stores/auth.ts#L185-L210)\n\n`main.tsx` 则完成三件事：根据设备环境计算哈希路由开关写入 `window.__MINICLAW_HASH_ROUTER__`；拦截 iOS Safari 专有的 `gesturestart`/`gesturechange` 事件阻止双指缩放；清理旧版本遗留的 Service Worker 注册与 Cache Storage（Miniclaw 已退役 PWA Service Worker，但老用户的浏览器中仍有残留）。最终以 `createRoot` + `StrictMode` 挂载 `<App />`。\n\nSources: [main.tsx](web/src/main.tsx#L1-L30)\n\n## 页面路由：React Router v7 数据路由器与三级访问控制\n\n### 路由表结构\n\n`App.tsx` 使用 `createRoutesFromElements` 以 JSX 形式声明整棵路由树，分为三个安全区：\n\n| 区域 | 路径 | 保护机制 |\n|------|------|---------|\n| 公开区 | `/login`、`/register`、`/setup` | 无守卫 |\n| 引导区 | `/setup/providers`、`/setup/channels` | 仅需登录 |\n| 应用区 | `/chat/:groupFolder?`、`/agent-profiles`、`/tasks`、`/usage`、`/billing`、`/memory`、`/capabilities/:section?`、`/settings` | `AuthGuard` + `AppLayout` 嵌套布局 |\n| 受限区 | `/monitor`（需 `manage_system_config`）、`/users`(需 `manage_users`/`manage_invites`/`view_audit_log` 任一) | `AuthGuard` 权限参数 |\n\n路由表还承载了**产品语义迁移**：旧路径 `/groups`、`/skills`、`/mcp-servers`、`/plugins` 均以 `<Navigate replace>` 重定向到 Agent-first 新地址（如 `/capabilities/skills`），保证老书签不断链；兜底的 `path="*"` 也指向 `/chat`。\n\nSources: [App.tsx](web/src/App.tsx#L97-L255)\n\n### 路由级代码分割与 ChatPage 特殊预热\n\n除登录/注册/引导三页外，所有页面均为 `React.lazy` 动态导入，每个路由配独立中文文案的 `<Suspense>` 骨架。这一策略有明确的量化动机——源码注释记录道：`CapabilitiesPage` 曾静态拉入 `SkillsPage → MarkdownRenderer → KaTeX + highlight.js` 整条依赖链，把入口 chunk 抬到约 1.74MB（gzip 后 517KB），连 `/login` 都要下载它。\n\n但全量懒加载引入了新问题：默认落地页 `/chat` 的首屏会多一次分片往返。解决方案是对 `ChatPage` 做**手动预热**——模块顶层的 `chatPagePromise ??= import(...)` 将加载 Promise 记忆化，入口脚本执行时立即调用 `shouldPreloadChatRoute()` 判断当前 URL（含哈希路由形态）是否为 `/` 或 `/chat/*`，命中则提前触发加载。这样懒加载的收益保留给低频页面，而高频首页的分片延迟被前置到与入口 bundle 并行。\n\nSources: [App.tsx](web/src/App.tsx#L13-L76), [chat-route-preload.ts](web/src/utils/chat-route-preload.ts#L1-L21)\n\n### 双路由器模式：iOS PWA 的历史 API 兼容\n\n`createAppRouter` 根据 `shouldUseHashRouter()` 在 `createBrowserRouter` 与 `createHashRouter` 之间二选一，判定条件精确到设备形态组合：**仅当同时处于 standalone 模式且为 iOS 设备时才启用哈希路由**（`isStandaloneMode() && isIOSDevice()`）。这是针对 iOS 主屏 PWA 场景下浏览器 History API 行为差异的防御性设计。配套的 `replaceInApp()` 工具函数在两种模式下分别生成正确的重定向目标——哈希模式将完整路径（含 APP_BASE）放入 hash，普通模式直接替换 pathname。此外 `APP_BASE`（来自 `VITE_BASE_PATH`）作为 router basename 注入，支持反向代理子路径部署。\n\nSources: [App.tsx](web/src/App.tsx#L257-L281), [utils/url.ts](web/src/utils/url.ts#L30-L59)\n\n### AuthGuard：声明式的认证状态机\n\n`AuthGuard` 是一个包裹型守卫组件，内部实现了一条完整的决策链。其流转逻辑如下：\n\n```mermaid\nflowchart TD\n    A["挂载: checkAuth()<br/>ref 保证只执行一次"] --> B{"checking?"}\n    B -->|"是, 超12秒"| C["超时卡片:<br/>刷新 / 去登录"]\n    B -->|"是"| D["LogoLoading"]\n    B -->|"否"| E{"initialized === false?"}\n    E -->|"是"| F["→ /setup"]\n    E -->|"否"| G{"未认证?"}\n    G -->|"是"| H["→ /login<br/>携带 state.from 来源"]\n    G -->|"否"| I{"必须改密?"}\n    I -->|"是且非/settings"| J["→ /settings"]\n    I -->|"否"| K{"admin 且 needsSetup?"}\n    K -->|"是"| L["→ /setup/providers"]\n    K -->|"否"| M{"权限校验"}\n    M -->|"不足"| N["→ /chat 兜底"]\n    M -->|"通过"| O["渲染 children"]\n```\n\n三个细节体现了工程成熟度：其一，`checkedRef` 确保 React StrictMode 下双重挂载也只发一次认证请求，且 `checkAuth` 内部用模块级 `checkAuthInFlight` 单例 Promise 做请求去重；其二，12 秒超时 UI 针对"后端刚启动或浏览器缓存异常"给出自愈指引而非无限 loading；其三，权限检查支持三种粒度——`requireAdmin`（角色）、`requiredPermission`（单一权限点）、`requiredAnyPermissions`（任一满足）。\n\nSources: [components/auth/AuthGuard.tsx](web/src/components/auth/AuthGuard.tsx#L1-L109), [stores/auth.ts](web/src/stores/auth.ts#L92-L95)\n\n### URL 即状态：`/chat/:groupFolder?` 的双向同步\n\n会话页采用可选动态段承载当前工作区。`ChatPage` 中 URL 参数是**唯一事实来源**：`routeGroupJid` 通过 folder 名在 groups 映射中反查 jid，`useEffect` 监听其变化调用 `selectGroup` 同步进 store；反向地，store 里找不到对应 group 时会先刷新一次 groups（兼容任务新建工作区的竞态），仍失败则以 `replace` 回退到 `/chat` 列表页。移动端返回手势（`useSwipeBack`）也直接导航回列表，保持 URL 与视图一致。可选启用的 `useRouteRestore` 则在 PWA 冷启动落回 `/chat` 时恢复上次深链接。\n\nSources: [pages/ChatPage.tsx](web/src/pages/ChatPage.tsx#L46-L58), [pages/ChatPage.tsx](web/src/pages/ChatPage.tsx#L107-L140), [hooks/useRouteRestore.ts](web/src/hooks/useRouteRestore.ts#L1-L38)\n\n## Zustand 状态管理：16 个领域 Store 的组织纪律\n\n### Store 家族全景\n\n`src/stores/` 下共 16 个 store（合计约 8800 行），每个对应一个后端资源域：\n\n| Store | 体量 | 核心职责 |\n|-------|------|---------|\n| `chat.ts` | ~4435 行 | 会话域全集：消息分页、流式状态、思考缓存、草稿、未读、follow-up 队列 |\n| `usage.ts` | ~800 行 | 用量统计聚合 |\n| `auth.ts` | 322 行 | 认证、用户档案、外观配置、权限判断 |\n| `billing.ts` / `users.ts` / `tasks.ts` | 200~300 行 | 计费、用户管理、定时任务 |\n| `agent-profiles.ts` / `channel-accounts.ts` / `skills.ts` / `plugins.ts` / `mcp-servers.ts` | 各 <200 行 | 能力配置域 |\n| `groups.ts` / `files.ts` / `monitor.ts` / `container-env.ts` | 小型 | 轻量查询状态 |\n| `run-lifecycle.ts` | 107 行 | **零 store**——纯函数集合 |\n\nSources: [stores/groups.ts](web/src/stores/groups.ts#L18-L61)\n\n### 统一的创建模式与跨 Store 协作\n\n全部 store 采用同一种极简写法：`create<State>((set, get) => ({ ...state, ...actions }))`。整个代码库**没有使用任何 Zustand 中间件**（无 `persist`、无 `devtools`、无 `shallow`）——持久化需求（主题、路由记忆）直接操作 localStorage，保持了状态层的纯粹性。\n\n跨 store 协作走两条路。声明式路径用于组件内联动，例如登录成功后 `useUsageStore.getState().reset()` 清空用量缓存；命令式路径用于事件回调——这正是关键设计：WS 事件处理器注册在组件的 `useEffect` 中，但回调体内一律通过 `useXxxStore.getState()` 拿取最新 action，避免闭包捕获过期 state，也让监听器无需依赖 store 实例变化而重建。\n\nSources: [stores/chat.ts](web/src/stores/chat.ts#L1624-L1646), [stores/auth.ts](web/src/stores/auth.ts#L108-L135)\n\n### 以 chat.ts 为例：按 JID 键控的扁平 Map 结构\n\n`ChatState` 接口揭示了多会话并存的状态建模思路——几乎所有字段都是 `Record<string, T>` 形态，以 chatJid（或 `jid#agent:agentId` 运行时 JID）为键：`messages`、`streaming`、`waiting`、`drafts`、`unreadReplies`、`activeRuns` 各自独立成 map，互不嵌套。这使得切换会话只需改一个 `currentGroup` 字符串，其他会话的状态原样驻留内存，回到会话即恢复现场。\n\n```mermaid\nflowchart LR\n    subgraph ChatStore["useChatStore"]\n        direction TB\n        G["groups: Record&#8203;<jid, GroupInfo>"]\n        M["messages: Record&#8203;<jid, Message[]>"]\n        S["streaming: Record&#8203;<jid, StreamingState>"]\n        R["activeRuns: Record&#8203;<runtimeJid, ClientActiveRun>"]\n        W["waiting / drafts / unreadReplies ..."]\n    end\n    JID["chatJid 或<br/>jid#agent:xxx"] --- G & M & S & R & W\n```\n\nSources: [stores/chat.ts](web/src/stores/chat.ts#L323-L430)\n\n### 请求去重：In-Flight Promise 缓存\n\n高频入口存在天然的重复请求风险——桌面端侧边栏和 ChatPage 各会调一次 `loadGroups`，首屏有两个入口各发一次 `loadMessages`。chat store 用两个模块级缓存解决：`loadGroupsInFlight` 单例 Promise，以及 `loadMessagesInFlight` 以 `${jid}\\0${before}` 为键的 Map，同一目标的在途请求直接复用，`finally` 中清理。这与 auth store 的 `checkAuthInFlight` 是同一模式的三次复用。\n\nSources: [stores/chat.ts](web/src/stores/chat.ts#L1638-L1672), [stores/chat.ts](web/src/stores/chat.ts#L1675-L1720)\n\n### 窄 Selector 性能纪律\n\n流式对话意味着每秒数十次 state 更新，Zustand 默认的引用相等性检查下，粗粒度订阅会导致大面积无效重渲染。代码库将其上升为显式规范——`ChatPage` 中的注释直言："窄 selector：整 store 订阅会让整个页面跟着流式输出每帧重渲染"。实践中所有组件均使用 `useChatStore((s) => s.groups)` 这类单字段选择器，甚至派生值（如未读总数求和）也在选择器内完成再交给 `document.title` 副作用。\n\nSources: [pages/ChatPage.tsx](web/src/pages/ChatPage.tsx#L24-L33), [components/layout/AppLayout.tsx](web/src/components/layout/AppLayout.tsx#L112-L122)\n\n## 数据获取层：REST 客户端与 WebSocket 单例\n\n### apiFetch：带超时与认证旁路的 fetch 封装\n\n`apiFetch` 用 `AbortController` 实现**分级超时**：JSON 请求默认 8 秒；FormData 上传放宽至 120 秒；大文件上传按字节数动态推算（保守带宽 20KB/s，下限 120 秒上限 10 分钟，与后端 requestTimeout 对齐）。认证旁路逻辑内聚在客户端：401 统一重定向 `/login`（已在登录页则跳过防循环）；403 且错误码为 `PASSWORD_CHANGE_REQUIRED` 时重定向 `/settings`，与 AuthGuard 的改密强制流程形成前后端闭环。\n\nSources: [api/client.ts](web/src/api/client.ts#L3-L68), [api/client.ts](web/src/api/client.ts#L72-L126)\n\n### WsManager：指数退避 + 网络感知的重连单例\n\n`wsManager` 是模块级实例化的 `WsManager` 类，承担全站唯一的 WebSocket 连接。事件按 `type` 字段路由到 `Map<string, Set<handler>>`，`on()` 返回退订函数，天然契合 `useEffect` 清理语义。重连策略有三个分支：常规断线按指数退避重试（1 秒起步、30 秒封顶）；**认证失败**（close code 1008 或自定义 4001）直接跳转登录页、放弃重试；`online`/`offline` 事件分别触发立即重连（并重置退避）与暂停重试。\n\nSources: [api/ws.ts](web/src/api/ws.ts#L1-L126)\n\n### 两级 WS 订阅拓扑\n\nWS 事件的消费按作用域分两层，职责边界清晰：\n\n| 订阅层级 | 位置 | 监听事件 | 写入方式 |\n|---------|------|---------|---------|\n| 全局层 | `AppLayout` | `runner_state`、`run_started`、`run_finished`、`active_run_snapshot`、`billing_update`、`group_created`、`agent_status` | `getState()` 直写 chat/groups/billing/tasks store |\n| 会话层 | `ChatView` | `stream_event`、`new_message`、`ws_error`、`stream_snapshot`、`follow_up_update` | 先过滤 `data.chatJid === groupJid` 再写 store |\n\n运行生命周期事件放在全局层的理由写在注释里：会话进程可能在轮次间保持温热，`run_started`/`active_run_snapshot` 是即时显示与断线恢复的权威信号，必须在用户离开会话页时依然生效（例如未读计数、标题栏角标）。`run-lifecycle.ts` 把这组判定抽成了**纯函数**——`applyRunStarted`（同 JID 新 run 取代旧 run）、`applyRunFinished`（只移除终态事件指名的那次尝试）、`shouldApplyRunScopedPayload`（流式载荷必须归属当前活跃 run，否则丢弃）——使并发时序逻辑可以脱离 Zustand 独立做单元测试。\n\nSources: [components/layout/AppLayout.tsx](web/src/components/layout/AppLayout.tsx#L58-L110), [components/chat/ChatView.tsx](web/src/components/chat/ChatView.tsx#L494-L551), [stores/run-lifecycle.ts](web/src/stores/run-lifecycle.ts#L17-L107)\n\n## 流式渲染管线：rAF 批量提交对抗高频 Delta\n\n流式输出的性能瓶颈在于 delta 事件频率远超浏览器渲染帧率。`handleStreamEvent` 入口处先做两道闸门：`context_audit` 事件因可能包含主机路径与 prompt 接线信息被直接丢弃（不出现在用户会话状态）；随后用 `shouldApplyRunScopedPayload` 校验载荷归属，不属于当前活跃 run 的迟到流被 fail-closed 丢弃。\n\n通过闸门后的 `text_delta` / `thinking_delta` 进入 **requestAnimationFrame 合帧**：模块级 `pendingDeltas` Map 以 `main:{jid}` 或 `agent:{agentId}` 为键持有待刷缓冲，首个 delta 到达时注册 rAF 回调，同帧内后续 delta 只追加文本不入队；换 run 时先取消旧 rAF 再开新缓冲，防止串话。这保证了无论模型吐字多快，React 每帧最多提交一次 setState。\n\n```mermaid\nsequenceDiagram\n    participant WS as WsManager\n    participant CV as ChatView (会话层订阅)\n    participant CS as chat store handleStreamEvent\n    participant RAF as pendingDeltas + rAF\n    participant DOM as React 渲染\n\n    WS->>CV: stream_event {chatJid, event, runId}\n    CV->>CS: 过滤 chatJid 后转发\n    CS->>CS: context_audit? 丢弃<br/>runId 不匹配? 丢弃\n    alt text_delta / thinking_delta\n        CS->>RAF: 追加文本缓冲<br/>(已有 rAF 则只累积)\n        Note over RAF: 每动画帧最多一次\n        RAF->>DOM: flushPendingDelta → set()\n    else 其他事件类型\n        CS->>DOM: 直接 set() 更新时间线/任务态\n    end\n    WS->>CS: stream_snapshot (WS 重连)\n    CS->>DOM: 恢复 partialText/activeTools/recentEvents\n```\n\nSources: [stores/chat.ts](web/src/stores/chat.ts#L2503-L2560), [components/chat/ChatView.tsx](web/src/components/chat/ChatView.tsx#L520-L536)\n\n## 布局系统：一套路由树适配桌面与移动\n\n`AppLayout` 作为应用区所有路由的父布局，用 `Outlet` 渲染子路由，并通过响应式断点切换外壳形态：≥1024px 条件挂载 `UnifiedSidebar`（仅在 `/chat` 路由可展开，其余路由强制折叠），窄屏则渲染 `BottomTabBar`。源码注释解释了条件挂载而非 CSS 隐藏的原因——手机端此前虽然视觉隐藏但仍会挂载整棵侧边栏（含每个工作区一组 DropdownMenu/Tooltip）并触发数据加载。\n\n布局层同时是**全局副作用的宿主**：建立 WebSocket 连接、注册上表所列的全局事件监听、应用持久化主题、驱动 `document.title` 未读角标。内容区包一层 `ErrorBoundary` 并以 `resetKeys={[location.pathname]}` 实现路由切换时自动重置错误态。\n\nSources: [components/layout/AppLayout.tsx](web/src/components/layout/AppLayout.tsx#L18-L56), [components/layout/AppLayout.tsx](web/src/components/layout/AppLayout.tsx#L155-L188)\n\n## 工程化约定：Vite 配置与目录分层\n\n开发环境下 Vite 将 `/api` 与 `/ws` 代理到本地后端（默认 `127.0.0.1:3000`，可经 `VITE_API_PROXY_TARGET` 覆盖），`strictPort` 固定 5173 端口并为 VS Code Remote 端口转发显式指定 HMR clientPort。构建产物支持 `VITE_BASE_PATH` 子路径部署，与运行时的 `APP_BASE` 判定逻辑共用同一套归一化规则。\n\n目录分层遵循清晰的职责半径：\n\n| 目录 | 内容 | 判定标准 |\n|------|------|---------|\n| `pages/` | 19 个路由级页面 | 与路由表一一对应 |\n| `components/<domain>/` | 133 个组件按 14 个业务域分组 | 可复用但有领域语境 |\n| `components/ui/` | shadcn/ui 生成的 18 个无头组件封装 | 无业务语义 |\n| `features/workspace-memory/` | 特性模块（model.ts + 同文件测试） | 自洽的功能闭环 |\n| `hooks/` | 14 个跨页面 hooks（手势、滚动、媒体查询等） | 有状态的行为复用 |\n| `lib/` | 消息时间线、交互模式等纯展示逻辑 | 无 React 依赖的可测函数 |\n| `stores/` / `api/` / `utils/` | 状态、传输、工具 | 如前文所述 |\n\nSources: [vite.config.ts](web/vite.config.ts#L1-L49), [playwright.config.ts](web/playwright.config.ts#L1-L42)\n\n## 总结：三条贯穿始终的设计主线\n\n回看整个前端，有三条主线反复出现。**第一是"URL 为纲"**：路由参数承载会话选择，守卫组件承载认证与权限的全部分支判断，重定向规则同时服务产品迁移与异常兜底。**第二是"事件归属精确化"**：无论是 run-scoped 流式载荷校验还是两级 WS 订阅拓扑，每个实时事件都能追溯到明确的所有者，迟到的、错配的事件一律丢弃而非污染状态。**第三是"重复工作一律去重"**：认证预热消费预发请求、in-flight Promise 缓存、rAF 合帧、条件挂载代替视觉隐藏——性能优化不是事后补丁，而是写进了每一层的数据结构选择里。\n\n若想继续深入相邻主题，推荐阅读路径：\n- 流式事件的类型契约与三端同步机制，见 [StreamEvent 流式契约：三端共享类型与同步机制](25-streamevent-liu-shi-qi-yue-san-duan-gong-xiang-lei-xing-yu-tong-bi-zhi)；\n- 本前端背后的 HTTP/WS 服务端实现，见 [Hono Web 服务：路由族、Cookie 认证与 WebSocket](8-hono-web-fu-wu-lu-you-zu-cookie-ren-zheng-yu-websocket)；\n- 桌面端受限壳与 Preload IPC 桥接如何复用这套前端，见 [Electron 受限 Shell：contextIsolation 与 Preload IPC 桥接](26-electron-shou-xian-shell-contextisolation-yu-preload-ipc-qiao-jie)；\n- 前端测试的组织方式（含 Playwright 移动仿真），见 [测试体系：单元测试、契约测试与 E2E 场景的组织方式](27-ce-shi-ti-xi-dan-yuan-ce-shi-qi-yue-ce-shi-yu-e2e-chang-jing-de-zu-zhi-fang-shi)。';export{e as default};

const t='自动压缩（Auto-Compact）是 MiniCode 上下文管理体系中**唯一依赖 LLM 的全局压缩手段**：当会话上下文逼近模型窗口上限时，它调用模型将早期对话改写为结构化摘要，以换取后续轮次的可用空间。与裁剪压缩、微压缩等确定性手段不同，自动压缩存在模型调用失败的风险，因此其设计围绕两个核心决策轴展开：**触发阈值**（何时值得付费调用模型压缩）与**失败熔断**（连续失败后如何避免在每次轮询中重复消耗资源）。本页聚焦这两条决策路径的完整实现。\n\n## 一、定位：压缩阶梯中的最后防线\n\nMiniCode 的上下文治理并非单一机制，而是按利用率从低到高排列的一整套阶梯。每个层级在上一层级释放空间不足时接力，自动压缩位于阶梯顶端，仅在上下文进入危急状态时才介入：\n\n| 利用率阈值 | 机制 | 性质 | 常量定义 |\n|---|---|---|---|\n| 0.50 | 微压缩（microcompact） | 确定性，清理旧工具结果占位 | `THRESHOLDS.MICROCOMPACT_UTILIZATION` |\n| 0.70 | 裁剪压缩（snip compact） | 确定性，安全移除中段历史 | `SNIP_COMPACT_THRESHOLD` |\n| 0.75 | 上下文折叠（context collapse） | 模型辅助，可摘要区间替换 | `CONTEXT_COLLAPSE_UTILIZATION` |\n| **0.85** | **自动压缩（auto-compact）** | **LLM 摘要，全局重写** | `THRESHOLDS.AUTOCOMPACT_UTILIZATION` |\n| 0.95 | 阻塞（blocked） | 状态标记，拒绝继续执行 | `THRESHOLDS.BLOCKED_UTILIZATION` |\n\nSources: [constants.ts](src/compact/constants.ts#L1-L5)、[snipCompact.ts](src/compact/snipCompact.ts#L402-L419)、[context-collapse.ts](src/compact/context-collapse.ts#L614-L629)\n\n利用率（utilization）的定义是 `accounting.totalTokens / window.effectiveInput`，其中 `effectiveInput = contextWindow - outputReserve`，即扣除模型输出预留后真正可用的输入空间（[token-estimator.ts](src/utils/token-estimator.ts#L177-L206)、[model-context.ts](src/utils/model-context.ts#L132-L149)）。\n\n在代理循环中，阶梯的各层并非每次都全量执行——**自动压缩只在每轮（turn）的第 0 步运行一次**，且前面先让 snip、microcompact、collapse 依次尝试释放空间；若它们已把利用率压回阈值之下，自动压缩便不会触发：\n\n```mermaid\nflowchart TD\n    A[step 0 开始] --> B[computeContextStats]\n    B --> C[snipCompact 裁剪<br/>阈值 0.70]\n    C --> D[microcompact 微压缩<br/>阈值 0.50]\n    D --> E[contextCollapse 折叠<br/>阈值 0.75]\n    E --> F{warningLevel<br/>critical / blocked?<br/>>= 0.85}\n    F -->|是| G[autoCompact LLM 摘要]\n    F -->|否| H[直接调用模型]\n    G -->|成功| I[替换消息 + 重置折叠状态<br/>重算 stats]\n    G -->|失败| H\n    I --> H\n    H --> J[model.next]\n```\n\nSources: [agent-loop.ts](src/agent-loop.ts#L178-L236)\n\n## 二、触发条件：利用率阈值与最小有效输入双重门槛\n\n`autoCompact` 入口函数依次设置三道闸门，任何一道未通过都直接返回 `null`（[auto-compact.ts](src/compact/auto-compact.ts#L45-L82)）：\n\n**闸门一：熔断开关。** 模块级单例 `state.disabled` 一旦为真，函数在第一条语句即短路返回——这是对"已判定模型不可用"状态的快速路径。\n\n**闸门二：最小有效输入保护。** `window.effectiveInput < LIMITS.MIN_EFFECTIVE_INPUT_FOR_AUTOCOMPACT (20_000)` 时放弃。这是对**小型上下文模型**的保护：若模型有效输入窗口本身就很小，摘要请求（系统提示 + 待压缩对话全文）可能挤占掉本就紧张的空间，压缩反而加速上下文耗尽。所有已知模型规则的 `effectiveInput` 均远超 2 万，该闸门主要拦截未知模型或未来可能出现的小窗口模型（[model-context.ts](src/utils/model-context.ts#L13-L17)）。\n\n**闸门三：利用率阈值。** `shouldAutoCompact` 基于 `computeContextStats` 计算 `stats.utilization >= THRESHOLDS.AUTOCOMPACT_UTILIZATION (0.85)`（[auto-compact.ts](src/compact/auto-compact.ts#L33-L43)）。关键点在于：**这条判断的输入不是朴素字符估算，而是供应商用量优先的记账结果**——`tokenCountWithEstimation` 从消息尾部向前扫描，找到最近一条携带未过期 `providerUsage` 的 assistant 消息，以它的 `totalTokens` 为基准，仅对尾随消息做估算补足；若全程无可用用量数据，才退化为纯估算（[token-estimator.ts](src/utils/token-estimator.ts#L125-L156)）。这意味着自动压缩的触发以模型厂商返回的真实计费令牌为准，估算只承担"尾巴"部分，`shouldAutoCompact` 对 provider 用量高度敏感：\n\n```mermaid\nflowchart TD\n    A[tokenCountWithEstimation] --> B{从尾部向前<br/>找到带 providerUsage 的消息?}\n    B -->|是| C[totalTokens = usage.totalTokens<br/>+ 尾随消息估算]\n    B -->|否| D[totalTokens = 全量估算<br/>标记 estimate_only]\n    C --> E[utilization = totalTokens / effectiveInput]\n    D --> E\n    E --> F{utilization >= 0.85?}\n    F -->|是| G[触发压缩]\n    F -->|否| H[不触发]\n```\n\nSources: [auto-compact.ts](src/compact/auto-compact.ts#L33-L43)、[token-estimator.ts](src/utils/token-estimator.ts#L125-L156)\n\n在代理循环侧，调用方还叠加了第四层约束：**仅在 `warningLevel === \'critical\' || \'blocked\'` 时才调用 autoCompact**（[agent-loop.ts](src/agent-loop.ts#L221-L236)）。`warningLevel` 与阈值常量同源——`>= 0.95` 为 blocked、`>= 0.85` 为 critical（[token-estimator.ts](src/utils/token-estimator.ts#L185-L194)）——因此内外两层判断等价，是同一阈值的双重校验。此外 `step === 0` 的限制意味着：即使后续步骤中上下文继续膨胀，自动压缩也不会在单轮内重复执行，避免为同一次会话重复支付摘要成本。TUI 顶栏的 `ctx` 徽标通过 `renderContextBadge` 以绿/黄/红/亮红四色实时呈现这四个状态（[chrome.ts](src/tui/chrome.ts#L214-L247)）。\n\n调试时可通过 `MINI_CODE_DEBUG_AUTOCOMPACT=1` 环境变量开启 stderr 日志，输出记账来源（`provider_usage` / `estimate_only`）、provider 用量、估算值、利用率和阈值对比（[auto-compact.ts](src/compact/auto-compact.ts#L18-L22)）。\n\n## 三、压缩执行：从保留边界到结构化摘要\n\n通过全部闸门后，`autoCompact` 委托 `compactConversation` 完成实际压缩（[compact.ts](src/compact/compact.ts#L124-L198)）。执行管线如下：\n\n**1. 边界计算。** `findRetentionBoundary` 从消息尾部向前扫描并累计令牌：一旦累计超过 `RETENTION.MAX_KEEP_TOKENS (40_000)` 即停止，同时保证至少保留 `RETENTION.MIN_KEEP_MESSAGES (6)` 条消息；若发现几乎全部都要保留，则强制退化为只保留最近 6 条（[compact.ts](src/compact/compact.ts#L58-L86)）。\n\n**2. API 轮次对齐。** 原始边界可能落在一次"thinking → tool_call → tool_result"组合的中间，切割会破坏工具调用的完整性。`groupMessagesByApiRound` 先将消息按 API 轮次分组，`alignBoundaryToApiRound` 再把边界前移到所属组的起点，确保保留区从完整轮次开始（[compact.ts](src/compact/compact.ts#L11-L56)）。\n\n**3. 摘要生成。** 边界之前的所有消息经 `messagesToText` 序列化为纯文本（工具结果截断至 500 字符、错误标记 `ERROR`、思考块仅保留占位说明），拼入 `buildCompactSummaryPrompt`。该提示要求模型按六个固定章节输出——Primary Request / Key Decisions / Files Modified / Errors Encountered / Current State / Pending Tasks——并强制使用 `<summary>` 标签包裹最终结果（[prompt.ts](src/compact/prompt.ts#L1-L21)）。摘要请求通过 `modelAdapter.next` 独立发往模型，而非走主对话流。\n\n**4. 结果解析与重组。** `parseSummaryFromResponse` 依次尝试 `<summary>` 标签、`<analysis>` 标签、纯文本三种解析策略（[prompt.ts](src/compact/prompt.ts#L23-L38)）。成功后构造 `context_summary` 消息（记录 `compressedCount` 与时间戳），新消息序列为 `system + summary + 保留区`。\n\n**5. 用量标记失效。** 保留区消息的 `providerUsage` 一律经 `markProviderUsageStale` 标记为 stale，原因注明"conversation was compacted after this provider usage was recorded"（[compact.ts](src/compact/compact.ts#L141-L148)）。这是**记账正确性的关键**：压缩后 token 布局已变，旧用量不能继续充当全量基准，必须让 `tokenCountWithEstimation` 回退到估算模式。\n\n```mermaid\nsequenceDiagram\n    participant AC as autoCompact\n    participant C as compactConversation\n    participant M as modelAdapter\n    participant S as session.ts\n    AC->>C: compactConversation(messages, adapter)\n    C->>C: findRetentionBoundary + API 轮次对齐\n    C->>M: next(summaryRequestMessages)\n    M-->>C: assistant 响应\n    C->>C: parseSummaryFromResponse → context_summary\n    C->>C: markProviderUsageStale(保留区)\n    C-->>AC: CompressionResult{tokensBefore/After}\n    AC-->>AC: consecutiveFailures = 0\n    AC-->>C: 返回 result\n    Note over S: tty-app 调用 appendCompactBoundary<br/>写入 compact_boundary 事件\n```\n\nSources: [compact.ts](src/compact/compact.ts#L124-L198)、[tty-app.ts](src/tty-app.ts#L1403-L1427)\n\n## 四、失败熔断：连续失败计数与全局禁用\n\n自动压缩是唯一会"永久"自我关闭的压缩机制，原因是它依赖模型调用——若模型持续失败，每个轮次的第一条消息前都会重复发起摘要请求，造成无谓的延迟与费用消耗。熔断器是一个三态状态机：\n\n```mermaid\nstateDiagram-v2\n    [*] --> Active: resetAutoCompactState / 初始\n    Active --> Active: 压缩成功<br/>consecutiveFailures 归零\n    Active --> Counting: 一次失败<br/>consecutiveFailures = 1\n    Counting --> Counting: 再次失败<br/>consecutiveFailures++\n    Counting --> Disabled: consecutiveFailures >= 3\n    Disabled --> Active: /compact 手动压缩成功\n    Disabled --> [*]: 会话终止\n```\n\nSources: [auto-compact.ts](src/compact/auto-compact.ts#L8-L27)、[manual-compact.ts](src/compact/manual-compact.ts#L6-L15)\n\n**失败的定义覆盖两条路径**：`compactConversation` 返回 `null`（消息过少、无可用保留区、模型返回空响应、摘要解析失败等）与抛出异常（API 错误），二者统一进入 `state.consecutiveFailures++` 分支（[auto-compact.ts](src/compact/auto-compact.ts#L63-L81)）。计数达到 `LIMITS.MAX_AUTOCOMPACT_FAILURES (3)` 时置 `disabled = true`。\n\n状态转移规则如下：\n\n| 事件 | consecutiveFailures | disabled | 结果 |\n|---|---|---|---|\n| 压缩成功 | 归 0 | 保持 false | 返回 `CompressionResult` |\n| 压缩返回 null 或抛异常（第 1 次） | 1 | false | 返回 null，下次仍可尝试 |\n| 连续第 2 次失败 | 2 | false | 返回 null，下次仍可尝试 |\n| 连续第 3 次失败 | 3 | **true** | 返回 null，**后续全部短路** |\n| 熔断后任意调用 | 不变 | true | 第一条语句即返回 null |\n| `/compact` 手动压缩成功 | 归 0 | **false** | 熔断解除 |\n\nSources: [auto-compact.ts](src/compact/auto-compact.ts#L45-L82)、[test/auto-compact.test.ts](test/auto-compact.test.ts#L58-L90)\n\n值得注意的细节：\n\n- **"连续"语义。** 一次成功立即清零计数（[auto-compact.ts](src/compact/auto-compact.ts#L63-L68)）。这意味着偶发失败不会累积，只有模型持续不可用才会触发熔断——这正是"熔断"而非"限流"的定位：它在保护系统不被反复失败的昂贵调用拖垮。\n- **手动压缩是唯一的解除通道。** `manualCompact` 在成功时调用 `resetAutoCompactState`（[manual-compact.ts](src/compact/manual-compact.ts#L6-L15)），对应 TUI 的 `/compact` 斜杠命令（[tty-app.ts](src/tty-app.ts#L1133-L1180)）。语义上自洽：用户主动发起压缩并成功，说明模型已恢复可用，熔断器应当重置。\n- **测试可观测性。** `getAutoCompactState` 暴露只读快照，测试据此断言失败计数与禁用状态；`resetAutoCompactState` 在测试 `beforeEach` 中清除残留状态（[test/auto-compact.test.ts](test/auto-compact.test.ts#L30-L33)）。\n\n## 五、调用侧集成与持久化\n\n自动压缩的成功结果在主循环与 UI 层产生三处联动：\n\n1. **消息替换与状态重置。** `messages = result.messages` 覆盖当前上下文，同时 `replaceContextCollapseState(createContextCollapseState())` 清空折叠状态——因为折叠区间依赖的具体消息可能已被摘要替换，保留旧区间会产生悬空引用（[agent-loop.ts](src/agent-loop.ts#L227-L234)）。\n2. **UI 反馈。** `onAutoCompact` 回调计算压缩百分比与节省令牌数，写入 `state.compressionStatus` 显示 5 秒，并向终端转录追加"Context auto-compressed: N messages summarized"（[tty-app.ts](src/tty-app.ts#L1403-L1427)）。\n3. **会话持久化。** `appendCompactBoundary` 向 JSONL 事件日志写入一条 `compact_boundary` 事件（携带 `trigger: \'auto\' | \'manual\'`、压缩前后令牌数），随后写入摘要用户事件，最后按 `retainedMessagesAfterCompact` 过滤（剔除 system 与 summary）逐条写入保留区消息，形成可恢复的压缩边界链（[session.ts](src/session.ts#L315-L366)、[tty-app.ts](src/tty-app.ts#L877-L881)）。\n\n## 六、测试覆盖\n\n`test/auto-compact.test.ts` 围绕本节两条主线建立回归保障：熔断测试验证三次连续失败后 `disabled` 置真且后续调用短路返回 null；成功测试验证失败计数归零；记账测试则分别用"provider usage 达到 11.1 万令牌"与"provider usage + 尾随大段估算"两种场景确认 `shouldAutoCompact` 以供应商用量为触发基准（[test/auto-compact.test.ts](test/auto-compact.test.ts#L136-L171)）。这组测试直接固化了本文档描述的阈值语义，任何改动若破坏阈值或熔断行为都会在此暴露。\n\n## 下一步\n\n- 自动压缩依赖的保留边界与 API 轮次对齐是确定性裁剪的前置知识：[确定性裁剪压缩：保护编辑与错误轮次的安全删除](13-que-ding-xing-cai-jian-ya-suo-bao-hu-bian-ji-yu-cuo-wu-lun-ci-de-an-quan-shan-chu)\n- 压缩后 provider 用量被标记 stale，相关记账细节见：[供应商用量优先的上下文记账与令牌估算](11-gong-ying-shang-yong-liang-you-xian-de-shang-xia-wen-ji-zhang-yu-ling-pai-gu-suan)\n- 压缩边界作为会话事件持久化的完整链路：[追加式 JSONL 事件日志与断点恢复](19-zhui-jia-shi-jsonl-shi-jian-ri-zhi-yu-duan-dian-hui-fu)';export{t as default};

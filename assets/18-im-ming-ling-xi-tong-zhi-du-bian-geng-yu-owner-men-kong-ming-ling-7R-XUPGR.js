const n="Miniclaw 的 IM 渠道（飞书、Telegram、微信、QQ、钉钉、Discord、WhatsApp）共享一套以 `/` 开头的斜杠命令系统。这套系统的核心设计问题是：**命令在什么时机被拦截、哪些命令允许任何人执行、哪些命令必须验证发送者是工作区 Owner，以及命令执行失败后如何保证不产生重复副作用**。本文剖析内置斜杠命令的拦截架构、三分类权限模型、Owner 身份生命周期与飞书渠道的持久化检查点机制。\n\n## 双层命令体系：内置拦截与插件展开的分叉点\n\nMiniclaw 实际上存在两层\"斜杠命令\"。第一层是**内置命令**：各渠道适配器在消息进入正常处理管线之前，用正则 `/^\\/(\\S+)/` 检测文本，命中即调用 `onCommand` 回调短路整个流程——命令永远不会到达 Agent Runner。第二层是**插件贡献的 DMI 命令**（`disable-model-invocation: true`）：它们作为普通消息穿过管线，在投递给 Agent 前由插件展开器替换为完整渲染的 Prompt 正文，未命中的返回 `{ kind: 'miss' }` 原样透传。\n\n```mermaid\nflowchart TD\n    A[IM 渠道消息] --> B{渠道适配器<br/>/^\\/(\\S+)/ 检测}\n    B -->|命中| C[onCommand 回调]\n    C --> D[handleCommand 分发]\n    D --> E[同步回复文本<br/>不进入 Agent 管线]\n    B -->|未命中| F[正常消息管线]\n    F --> G[队列调度]\n    G --> H{插件 DMI 命令?<br/>expandMessagesIfNeeded}\n    H -->|expanded| I[替换为 Prompt 后交给 Agent]\n    H -->|miss| J[原样交给 Agent]\n```\n\n所有渠道适配器都遵循同一检测模式：飞书在 [feishu.ts](src/feishu.ts#L1991-L1993)、Telegram 在 [telegram.ts](src/telegram.ts#L720-L738)、钉钉在 [dingtalk.ts](src/dingtalk.ts#L1893-L1899)、QQ 在 [qq.ts](src/qq.ts#L1627-L1638)、微信在 [wechat.ts](src/wechat.ts#L896-L902)。Telegram 的实现还带一个细节考量——斜杠检测必须发生在路由解析之前，因为 `/status` 这类命令不应在新话题中触发\"为尚未确认是命令的消息创建会话 Agent\"这类副作用。多账号场景下，`im-manager` 会用 `scope(jid)` 包装回调并施加 `inboundAllowed()` 准入门控，保证命令只会路由到当前活跃账号的作用域内。\n\nSources: [plugin-expander-core.ts](src/plugin-expander-core.ts#L1-L44), [index.ts](src/index.ts#L5689-L5714), [im-manager.ts](src/im-manager.ts#L388-L395)\n\n## 命令分发中枢：handleCommand 与统一门控入口\n\n所有渠道的 `onCommand` 最终汇聚到主进程的 `handleCommand`（[index.ts](src/index.ts#L3548-L3556)）。它先做小写归一化和参数切分，然后在函数顶部执行两步鉴权前置逻辑：DM 自动认领检查和统一 Owner 门控检查，之后才进入 `switch` 分发到各具体处理器。未知命令返回 `null`，由调用方决定是否静默降级为普通消息。\n\n| 命令 | 别名 | 分类 | 行为摘要 |\n|------|------|------|----------|\n| `/clear` | — | 变更 · Owner 门控 | 重置会话上下文 |\n| `/list` | `/ls` | 只读 | 列出可访问工作区与会话短 ID |\n| `/status` | — | 只读 | 展示位置、运行状态与容器负载 |\n| `/recall` | `/rc` | 只读 | 调用 LLM 总结最近对话 |\n| `/where` | — | 只读 | 显示当前绑定位置与回复策略 |\n| `/bind <ws>[/<会话>]` | — | 变更 · Owner 门控 | 切换绑定到工作区或具体会话 |\n| `/unbind` | — | 变更 · Owner 门控 | 恢复 Bot 默认工作区 |\n| `/new <名称>` | — | 变更 · Owner 门控 | 创建新工作区并绑定 |\n| `/sw <任务>` | `/spawn` | 变更 · Owner 门控 | 创建并行子 Agent 任务 |\n| `/release_owner` | — | 变更 · Owner 门控 | 当前 Owner 主动释放身份 |\n| `/owner_mention` | — | 自我门控 | 认领 Owner（唯一引导路径） |\n| `/require_mention true\\|false` | — | 自我门控 | 切换 @激活策略 |\n| `/allow` / `/disallow @成员` | — | 自我门控 | 维护发言白名单（仅 Owner） |\n| `/allowlist` | — | 只读 | 查看白名单 |\n\nSources: [index.ts](src/index.ts#L3587-L3628), [index.ts](src/index.ts#L3793-L3823), [index.ts](src/index.ts#L4509-L4524)\n\n## 三分类权限模型与 OWNER_REQUIRED_IM_COMMANDS\n\n权限模型的核心是一个纯数据集合 `OWNER_REQUIRED_IM_COMMANDS`，定义在 [im-command-utils.ts](src/im-command-utils.ts#L270-L280)：包含 `clear`、`bind`、`unbind`、`new`、`sw`、`spawn`、`release_owner` 七个变更类命令。集合之外的命令直接放行；集合之内的命令走 `checkImOwnerCommand` 三段校验——先检查通道是否能提供发送者身份，再检查群组是否已有 Owner，最后比对 `senderImId === group.owner_im_id`。\n\n刻意排除在中央门控之外的命令各有理由（注释中逐条说明）：`/owner_mention` 是无主群组的**唯一引导路径**，若加门控新群组将永远无法认领；`/require_mention` 是设置开关且自带守卫——当 `owner_im_id` 已存在时非 Owner 一律拒绝修改，防止任意成员把群从受限模式翻成 `always` 造成聊天提权；`/allow`、`/disallow` 在各自处理器内部强制 `sender === owner_im_id`；其余均为只读工具无需防护。这份契约被 [tests/im-owner-gate.test.ts](tests/im-owner-gate.test.ts#L15-L48) 以快照式断言锁定。\n\n```mermaid\nflowchart TD\n    S[收到命令 cmd] --> R{cmd ∈<br/>OWNER_REQUIRED_IM_COMMANDS?}\n    R -->|否| OK1[放行]\n    R -->|是| D{senderImId 存在?}\n    D -->|否| X1['该通道暂不支持此命令']\n    D -->|是| O{group.owner_im_id<br/>已设置?}\n    O -->|否| X2['请先由 Owner 发送 /owner_mention 认领']\n    O -->|是| M{senderImId ==<br/>owner_im_id?}\n    M -->|否| X3['只有工作区 Owner 才能执行此命令']\n    M -->|是| OK2[放行]\n```\n\nSources: [im-command-utils.ts](src/im-command-utils.ts#L282-L313), [im-command-utils.ts](src/im-command-utils.ts#L255-L269)\n\n## DM 自动认领：结构性 JID 判定的一向失败设计\n\n群聊中强制先发 `/owner_mention` 合理（第一个发命令的人未必是主人），但在一对一私聊里这就是纯粹的摩擦。因此 `handleCommand` 在门控前插入一段自动认领逻辑：若命令属于 Owner 必需集合、群组尚无 Owner 且未被凭据转移隔离、而当前 JID 是私聊，则直接把发送者认领为 Owner（[index.ts](src/index.ts#L3561-L3585)）。关键在于\"如何判断一个 JID 是私聊\"——`isDirectMessageJid` 采用**纯结构判定**，因为每个自动注册私聊的渠道都会把 DM 属性编码进 JID 格式本身：\n\n| 渠道 | 私聊 JID 形态 | 群聊 JID 形态 |\n|------|--------------|--------------|\n| QQ | `qq:c2c:{openid}` | `qq:group:{id}` |\n| 钉钉 | `dingtalk:c2c:{staffId}` | `dingtalk:{cid}` |\n| Discord | `discord:dm:{userId}` | `discord:{channelId}` |\n| WhatsApp | `…@s.whatsapp.net` | `…@g.us` |\n| Telegram | 正数 chatId | 负数 chatId（Bot API 保证） |\n| 微信 | 仅支持私聊 `wechat:{userId}` | 无群聊 |\n| 飞书 | 无法从 JID 区分 → 恒判 false | 走 DM owner-learn 自动回填 |\n\n这个设计的失败方向是**单向安全**的：群组永远不可能被误读为私聊（群内不会自动授权），最坏情况只是真实私聊被误判为群聊、用户退回手动 `/owner_mention`。\n\nSources: [im-command-utils.ts](src/im-command-utils.ts#L315-L345), [tests/im-owner-gate.test.ts](tests/im-owner-gate.test.ts#L107-L130)\n\n## Owner 身份生命周期：两条不变量的单一归属点\n\nOwner 的认领与释放曾经散落在 8 个以上的手写 `setRegisteredGroup` 调用点，两条关键不变量在复制粘贴中极易漂移，因此收敛到纯函数模块 [group-owner.ts](src/group-owner.ts#L1-L41)：\n\n1. **不变量一（释放侧）**：释放 Owner 必须把 `activation_mode: 'owner_mentioned'` 降级为 `when_mentioned`，否则 `isGroupOwnerMessage()` 再也匹配不到任何人，Bot 在群里永久沉默。同时清空 `sender_allowlist`，避免新 Owner 被旧白名单锁死后连 `/allow` 都无法自救。\n2. **不变量二（认领侧）**：认领 Owner 绝不触碰 `activation_mode`——认领是身份锚定而非激活策略变更，避免引导过程意外把群切成\"仅 Owner 响应\"。\n\n`claimOwnerFromMention` 还处理两个边界：`owner_claim_source === 'transfer_reset'` 的凭据转移隔离态不可覆盖；已存在的更强信任来源（`configured`/`trusted_direct`）不被幂等重发降级为普通 `explicit`。所有状态迁移通过 `persistGroupUpdate` 一步完成\"写库 + 同步内存缓存\"，杜绝只写一半导致轮询器读到陈旧状态的经典错误。\n\nSources: [group-owner.ts](src/group-owner.ts#L42-L98), [group-owner.ts](src/group-owner.ts#L100-L147), [index.ts](src/index.ts#L4082-L4132)\n\n## 变更命令的执行语义\n\n**`/clear` 的五步重置**是理解变更类命令副作用范围的最好样本。[commands.ts](src/commands.ts#L37-L108) 中的 `executeSessionReset` 依次：停止队列进程（Agent 级只停该虚拟 JID，主会话级停同 folder 全部兄弟 JID）→ 清理 `.claude/` 会话文件（保留 settings.json）→ 从 DB 删除会话并清 Owner 缓存 → 插入 `context_reset` 系统分隔消息并广播 → 推进 `lastAgentTimestamp` 游标防止重置前的旧消息被下一个全新会话重复消费。目标定位由纯函数 `resolveBoundChatTarget` 完成——非主工作区使用随机 `web:<uuid>` JID，命令绝不能从 `folder` 反推目标（[im-command-utils.ts](src/im-command-utils.ts#L162-L215)）。\n\n**`/sw` 并行任务**则在 Owner 主工作区创建一条 `kind: 'spawn'` 的 SubAgent 记录和虚拟会话 `${homeChatJid}#agent:${agentId}`，把用户消息写入虚拟会话并入队执行；IM 来源的任务还会镜像一条 `source_kind='user_command'` 的命令记录到 Web 会话流，让网页端可见但不会被轮询循环误拾取（[index.ts](src/index.ts#L4541-L4645)）。**`/bind`** 则按渠道能力分流：飞书话题群只能绑工作区（thread_map），普通群和私聊只能绑具体会话，错配直接拒绝并提示正确用法（[index.ts](src/index.ts#L3900-L3946)）。\n\nSources: [commands.ts](src/commands.ts#L37-L108), [index.ts](src/index.ts#L4541-L4629)\n\n## 可靠性：飞书斜杠命令的持久化检查点\n\n命令是有副作用的（停进程、删文件、建工作区），崩溃后盲目重试会导致重复执行。飞书适配器为此实现了**检查点状态机**：命令执行前后把状态持久化到 Channel Inbox 的 `normalizedPayload` 字段，恢复时据此决策。\n\n```mermaid\nstateDiagram-v2\n    [*] --> executing : 写入检查点后调 onCommand\n    executing --> pending_reply : 执行完成, 回复落盘\n    pending_reply --> sending_reply : 开始投递回复\n    sending_reply --> reply_acknowledged : 收到送达 ACK\n    reply_acknowledged --> [*]\n    executing --> ⚠️人工对账 : 崩溃恢复\n    pending_reply --> ✅续投 : 崩溃恢复\n    sending_reply --> ⚠️人工对账 : 崩溃恢复\n```\n\n恢复语义刻意保守（[feishu.ts](src/feishu.ts#L2017-L2050)）：停在 `pending_reply` 可以安全续投（结果已在盘上）；停在 `executing` 或 `sending_reply` 则**停止自动重试**并发送人工核对通知——前者可能已执行任意副作用，后者可能已发出回复但未确认，重跑任一步都不安全。此外恢复时若发现检查点记录的命令与本次输入不一致，直接判定为异常输入予以拒绝（[feishu.ts](src/feishu.ts#L2006-L2016)）。\n\n| 检查点状态 | 含义 | 恢复动作 |\n|-----------|------|----------|\n| `executing` | 处理器执行中 | 拒绝重跑 → 人工对账 |\n| `pending_reply` | 结果已落盘待投递 | 用存量文本续投 |\n| `sending_reply` | 投递已开始 | 拒绝重发 → 人工对账 |\n| `reply_acknowledged` | 已确认送达 | 直接完结 |\n\nSources: [feishu.ts](src/feishu.ts#L267-L309), [feishu.ts](src/feishu.ts#L2055-L2113)\n\n## 与 Web 通道的一致性\n\n同一套命令语义在 Web 端保持对齐：Web 消息端点的 `/clear` 同样在进入管线前被 `isClearCommand` 拦截（精确匹配、大小写不敏感、容忍空白、拒绝全角斜杠），但鉴权换用 Web 侧的 `canModifyGroup` ACL——注释明确指出这与 `reset-session` 路由对齐，因为两者破坏力相同（清会话文件、停兄弟容器、丢历史）（[web.ts](src/web.ts#L320-L334)）。IM 与 Web 各自用符合自身信任模型的鉴权原语保护同一个高危操作，这是\"命令语义统一、鉴权按通道分治\"的典型体现。\n\nSources: [web.ts](src/web.ts#L320-L334), [commands.test.ts](tests/commands.test.ts#L32-L57)\n\n## 小结与延伸阅读\n\nMiniclaw 的 IM 命令系统展示了三个值得借鉴的工程决策：**用纯数据集合声明权限面**使契约可测试；**用结构性 JID 判定实现一向失败的自动认领**避免了配置驱动的复杂度；**用持久化检查点区分\"可安全续做\"与\"必须人工对账\"**在可靠性与安全性之间划出了清晰界线。命令系统与消息准入、可靠性投递紧密协作——继续阅读推荐：\n\n- 了解命令回复如何可靠送达 IM：[可靠性状态机：Inbox、Turn、Outbox 与 Streaming Card 的持久投递](17-ke-kao-xing-zhuang-tai-ji-inbox-turn-outbox-yu-streaming-card-de-chi-jiu-tou-di)\n- 理解命令操作的工作区对象模型：[Agent-first 产品模型：Agent Profile、Workspace 与 Session 层级](5-agent-first-chan-pin-mo-xing-agent-profile-workspace-yu-session-ceng-ji)\n- 命令背后的队列与串行约束：[会话串行队列：Runner 生命周期、重试退避与卡死恢复](14-hui-hua-chuan-xing-dui-lie-runner-sheng-ming-zhou-qi-zhong-shi-tui-bi-yu-qia-si-hui-fu)";export{n as default};

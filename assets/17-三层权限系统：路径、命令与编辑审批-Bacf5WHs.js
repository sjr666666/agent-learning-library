const s='MiniCode 在把执行权交给模型之前，设置了三条递进的防线：**路径访问控制**决定"能碰哪些文件"、**命令审批**决定"能跑哪些 shell 命令"、**编辑审批**决定"能把哪些改动写进磁盘"。三者共用同一套决策引擎：一个 `PermissionManager` 实例持有全部状态，通过统一的 `PermissionRequest` 交互协议向 TUI 挂起审批界面，并把用户的每个决定落到"持久 / 会话 / 回合"三个作用域之一。本文从决策模型出发，逐层拆解三条防线的实现细节与协同方式。\n\n## 一、统一决策模型：三类请求 × 七种决定 × 三级作用域\n\n三层权限共享一个核心设计：**先查清单、再问用户、最后才放行**。三类请求被建模为同一个 `PermissionRequest` 结构，仅靠 `kind: \'path\' | \'command\' | \'edit\'` 区分；每类请求都携带 `summary`、`details`、`scope` 和一组 `choices`，由 TUI 渲染成一致的审批面板（[permissions.ts](src/permissions.ts#L30-L40)）。\n\n用户的所有决定收敛为七种 `PermissionDecision`。其中 `allow_once`、`deny_once` 只影响当前会话（内存 `Set`）；`allow_always`、`deny_always` 写入 `~/.mini-code/permissions.json` 成为持久规则；`allow_turn`、`allow_all_turn` 是编辑层专属的回合级放行，随 `beginTurn()`/`endTurn()` 清空（[permissions.ts](src/permissions.ts#L6-L13)、[permissions.ts](src/permissions.ts#L212-L220)）。\n\n```mermaid\nflowchart TD\n    subgraph Tools["工具层"]\n        A["list_files / read_file / grep_files<br/>write_file / edit_file / patch_file / modify_file"]\n        B["run_command"]\n    end\n\n    subgraph Gates["三层防线 (PermissionManager)"]\n        L1["第一层: ensurePathAccess<br/>kind=\'path\' · 4 种选择"]\n        L2["第二层: ensureCommand<br/>kind=\'command\' · 4 种选择"]\n        L3["第三层: ensureEdit<br/>kind=\'edit\' · 7 种选择"]\n    end\n\n    subgraph Stores["状态与持久化"]\n        S1["会话级 Set<br/>sessionAllowed*/Denied*"]\n        S2["回合级 Set<br/>turnAllowedEdits / turnAllowAllEdits"]\n        S3["持久规则<br/>~/.mini-code/permissions.json"]\n    end\n\n    subgraph UI["审批交互"]\n        U["TUI 审批面板<br/>createPermissionPromptHandler"]\n    end\n\n    A -->|"resolveToolPath 按 intent 分流"| L1\n    A -->|"applyReviewedFileChange 生成 diff"| L3\n    B -->|"先校验 cwd 路径"| L1\n    B -->|"再校验命令签名"| L2\n\n    L1 --> S1\n    L1 --> S3\n    L2 --> S1\n    L2 --> S3\n    L3 --> S1\n    L3 --> S2\n    L3 --> S3\n\n    L1 -->|"未命中清单"| U\n    L2 -->|"未命中清单"| U\n    L3 -->|"未命中清单"| U\n```\n\n每一层都遵循**拒绝优先、允许次之、提示兜底**的判定顺序：先查 deny 清单（会话级 + 持久级），命中即抛错终止；再查 allow 清单（会话级 + 持久级），命中即放行；两者皆未命中且存在交互式提示处理器时，才挂起审批面板等待用户决定（[permissions.ts](src/permissions.ts#L261-L330) 的路径层即典型体现）。若无提示处理器（如非 TTY 的批处理模式），则直接抛错拒绝，提示"Start minicode in TTY mode to approve it"（[permissions.ts](src/permissions.ts#L283-L287)）。\n\nSources: [permissions.ts](src/permissions.ts#L6-L52)、[permissions.ts](src/permissions.ts#L212-L248)、[index.ts](src/index.ts#L79-L80)\n\n## 二、第一层：路径访问控制（ensurePathAccess）\n\n路径层是所有文件类工具的必经关卡。`workspace.ts` 中的 `resolveToolPath` 是唯一入口：它把模型给出的相对路径解析为绝对路径后，按工具的 `intent`（`read` / `write` / `list` / `search`）调用 `ensurePathAccess`（[workspace.ts](src/workspace.ts#L7-L27)）。`run_command` 的 `cwd` 参数也走同一通道，intent 为 `command_cwd`（[run-command.ts](src/tools/run-command.ts#L175-L178)）。\n\n`ensurePathAccess` 的核心逻辑包含一个**工作区快速路径**：只要目标路径位于 `workspaceRoot` 之内（`isWithinDirectory` 通过 `path.relative` 判定），就无条件放行、完全不需要审批。只有越出 cwd 的访问才会进入清单查询与提示流程（[permissions.ts](src/permissions.ts#L59-L67)、[permissions.ts](src/permissions.ts#L261-L267)）。\n\n越界访问的提示有一个值得注意的细节——**scope 目录的计算因 intent 而异**：`list` 与 `command_cwd` 直接把目标路径本身当作审批范围（因为列举目录或以此为工作目录意味着递归访问其全部内容），其余 intent 则取 `path.dirname(target)`（[permissions.ts](src/permissions.ts#L289-L292)）。选 `allow this directory` 时持久化的是这个 scope 目录前缀，后续对该目录内任意文件的访问都会命中 `matchesDirectoryPrefix` 而直接放行（[permissions.ts](src/permissions.ts#L69-L80)、[permissions.ts](src/permissions.ts#L316-L319)）。\n\n路径层的提示提供四个选项：`y` 允许一次、`a` 允许整个目录（持久化）、`n` 拒绝一次、`d` 拒绝该目录（持久化）。`deny_always` 会把目录写入持久 deny 前缀表并同步落盘（[permissions.ts](src/permissions.ts#L303-L309)、[permissions.ts](src/permissions.ts#L322-L327)）。\n\nSources: [workspace.ts](src/workspace.ts#L7-L27)、[permissions.ts](src/permissions.ts#L261-L330)\n\n## 三、第二层：命令审批（ensureCommand）\n\n命令层是三层中逻辑最丰富的一层，因为它在 `ensureCommand` 之外还承担了**前置分类**。`run_command` 工具把模型输入归一化为 `command + args`（支持把 `"git status"` 这样的单字符串拆成命令与参数，[run-command.ts](src/tools/run-command.ts#L94-L120)），然后判定命令类别：\n\n| 类别 | 命令集合 | 审批行为 |\n|------|----------|----------|\n| 只读命令 | `pwd` `ls` `find` `rg` `grep` `cat` `head` `tail` `wc` `sed` `echo` `df` `du` `free` `uname` `uptime` `whoami` | 直接放行，不触发审批 |\n| 开发命令 | `git` `npm` `node` `python3` `pytest` `bash` `sh` `bun` | 进入 `classifyDangerousCommand` 判定 |\n| 未知命令 | 不在以上两个集合 | 强制提示（`forcePromptReason`） |\n| Shell 片段 | 含 `\\|&;<>()$\\`` 等字符的整串输入 | 改由 `bash -lc` 执行，按 bash 危险类别审批 |\n\n其中"Shell 片段"走的是另一条通道：`looksLikeShellSnippet` 检测到管道、重定向、变量展开等语法后，实际执行命令被替换为 `bash -lc <原串>`，此时 `command` 变成 `bash`，自然落入"可执行任意本地代码"的危险类别（[run-command.ts](src/tools/run-command.ts#L122-L137)、[run-command.ts](src/tools/run-command.ts#L188-L209)）。\n\n`classifyDangerousCommand` 对危险模式做**启发式识别**，命中即返回理由字符串作为提示原因。其规则覆盖四类风险（[permissions.ts](src/permissions.ts#L86-L135)）：\n\n| 危险模式 | 风险描述 |\n|----------|----------|\n| `git reset --hard` | 丢弃本地修改 |\n| `git clean` | 删除未跟踪文件 |\n| `git checkout --` / `git restore --source` | 覆盖工作区文件 |\n| `git push --force` / `-f` | 改写远端历史 |\n| `npm publish` | 影响本机之外的 registry |\n| `node` / `python3` / `bun` / `bash` / `sh` | 可执行任意本地代码 |\n\n命令审批在进入危险判定前还有一个**前置门**：`ensureCommand` 首先对 `commandCwd` 调用 `ensurePathAccess(commandCwd, \'command_cwd\')`，即命令的工作目录本身必须已获得路径层许可（[permissions.ts](src/permissions.ts#L340)）。这保证了一条命令即使签名被允许，也不能被用于在未授权的目录里运行。命令的审批粒度是完整签名 `command args...` 拼接成的字符串，精确匹配、不做模糊前缀匹配（[permissions.ts](src/permissions.ts#L82-L84)）。\n\nSources: [run-command.ts](src/tools/run-command.ts#L14-L50)、[run-command.ts](src/tools/run-command.ts#L122-L209)、[permissions.ts](src/permissions.ts#L332-L407)\n\n## 四、第三层：编辑审批（ensureEdit）\n\n编辑层与文件写入深度绑定：四个写工具 `write_file`、`edit_file`、`patch_file`、`modify_file` 最终都汇入 `file-review.ts` 的 `applyReviewedFileChange`（[write-file.ts](src/tools/write-file.ts#L26-L30)、[edit-file.ts](src/tools/edit-file.ts#L33-L50)、[patch-file.ts](src/tools/patch-file.ts#L50-L84)、[modify-file.ts](src/tools/modify-file.ts#L26-L30)）。该函数在写入前做三件事：读取旧内容、用 `createTwoFilesPatch` 生成带 3 行上下文的 unified diff、然后调用 `ensureEdit(targetPath, diff)` 请求审批——**用户看到的是 diff 而不是整文件**（[file-review.ts](src/file-review.ts#L7-L32)、[file-review.ts](src/file-review.ts#L46-L70)）。\n\n编辑审批的选择项是三层中最多的，共 7 个，体现了"文件编辑需要更细的信任粒度"：\n\n| 按键 | 决定 | 作用域 |\n|------|------|--------|\n| `1` | apply once | 会话级 `sessionAllowedEdits` |\n| `2` | allow this file in this turn | 回合级 `turnAllowedEdits` |\n| `3` | allow all edits in this turn | 回合级 `turnAllowAllEdits` 标志 |\n| `4` | always allow this file | 持久 `allowedEditPatterns` |\n| `5` | reject once | 会话级 deny |\n| `6` | reject and send guidance to model | 抛错携带用户反馈 |\n| `7` | always reject this file | 持久 deny |\n\n其中 `2`、`3` 两个回合级选项是编辑层独有的：`beginTurn()` 在每轮模型对话开始前清空 `turnAllowedEdits` 并复位 `turnAllowAllEdits`，`endTurn()` 在轮次结束时再次清理，保证"本回合放行"不会泄漏到下一回合（[permissions.ts](src/permissions.ts#L409-L428)、[tty-app.ts](src/tty-app.ts#L1388)、[tty-app.ts](src/tty-app.ts#L1615)）。这样用户可以用 `3` 对一轮中模型连续修多个文件的场景一次性放行，同时保持回合边界的安全隔离。\n\n`deny_with_feedback`（`6`）是编辑层独有的**反馈回路**：选择后进入输入模式，用户的指导文本被拼进抛出的错误消息 `Edit denied: ...\\nUser guidance: ...`。这个错误作为 `isError: true` 的 tool_result 回流给模型，让模型在下一次尝试中根据用户指导调整方案（[permissions.ts](src/permissions.ts#L477-L486)、[agent-loop.ts](src/agent-loop.ts#L380-L399)）。\n\nSources: [file-review.ts](src/file-review.ts#L7-L70)、[permissions.ts](src/permissions.ts#L409-L496)、[agent-loop.ts](src/agent-loop.ts#L380-L399)\n\n## 五、作用域对比与持久化\n\n三层权限共享同一套"会话 / 回合 / 持久"作用域模型，但各层的具体组合不同：\n\n| 维度 | 路径层 | 命令层 | 编辑层 |\n|------|--------|--------|--------|\n| 会话级允许 | `sessionAllowedPaths` | `sessionAllowedCommands` | `sessionAllowedEdits` |\n| 会话级拒绝 | `sessionDeniedPaths` | `sessionDeniedCommands` | `sessionDeniedEdits` |\n| 回合级 | 无 | 无 | `turnAllowedEdits` + `turnAllowAllEdits` |\n| 持久允许 | `allowedDirectoryPrefixes` | `allowedCommandPatterns` | `allowedEditPatterns` |\n| 持久拒绝 | `deniedDirectoryPrefixes` | `deniedCommandPatterns` | `deniedEditPatterns` |\n| 持久化粒度 | 目录前缀 | 完整命令签名 | 文件路径 |\n\n持久状态统一存放在 `~/.mini-code/permissions.json`（`MINI_CODE_PERMISSIONS_PATH`，[config.ts](src/config.ts#L41)），结构为六组字符串数组。`PermissionManager` 构造时异步加载该文件，`whenReady()` 保证加载完成前不对外服务；`allow_always` / `deny_always` 触发 `persist()` 时先 `mkdir` 再原子写 JSON，并保持两次写入间内存与磁盘一致（[permissions.ts](src/permissions.ts#L113-L130)、[permissions.ts](src/permissions.ts#L149-L206)、[permissions.ts](src/permissions.ts#L250-L259)）。\n\n```mermaid\nflowchart LR\n    A["PermissionManager 构造"] --> B["readPermissionStore<br/>加载 permissions.json"]\n    B --> C["内存 Set 集合<br/>allowlist / denylist × 三层"]\n    C --> D["ensure* 判定时查询"]\n    D -->|"allow_always / deny_always"| E["persist()<br/>写回 permissions.json"]\n```\n\n一个容易被忽略的设计是**拒绝记忆优先于允许记忆**：在三层判定中，deny 清单（无论会话级还是持久级）都先于 allow 清单被检查。这意味着即使某个路径同时出现在 allow 与 deny 前缀中，deny 也会胜出——这是一个"默认安全"的取舍（[permissions.ts](src/permissions.ts#L269-L281) 的路径层即为典型）。\n\nSources: [config.ts](src/config.ts#L36-L44)、[permissions.ts](src/permissions.ts#L113-L130)、[permissions.ts](src/permissions.ts#L250-L259)\n\n## 六、TUI 审批交互：挂起、渲染与键盘\n\n审批不是一个同步弹窗，而是一个**异步 Promise 挂起机制**。`createPermissionPromptHandler` 返回的处理器收到 `PermissionRequest` 后，把 `{ request, resolve }` 存入 `state.pendingApproval` 并返回一个未决的 Promise——模型所在的代理循环被挂起，TUI 进入"Waiting for approval..."状态；用户在面板上做出选择后 `resolve()` 被调用，Promise 兑现，被挂起的工具调用继续执行（[tty-app.ts](src/tty-app.ts#L1626-L1644)）。\n\n审批面板由 `chrome.ts` 的 `renderPermissionPrompt` 渲染：折叠态最多显示 16 行详情，超出部分提示 `Ctrl+O` 展开；编辑类请求的 diff 会经 `colorizeEditPermissionDetails` 做行级语法着色，并用 `computeChangedRange` 高亮增删行的实际差异区间（[chrome.ts](src/tui/chrome.ts#L431-L557)、[chrome.ts](src/tui/chrome.ts#L739-L746)）。键盘交互覆盖了从选择到滚动到反馈输入的完整操作面（[tty-app.ts](src/tty-app.ts#L1798-L1958)）：\n\n| 按键 | 行为 |\n|------|------|\n| `↑` / `↓` | 在选项间移动（展开态 Alt+方向键用于滚动） |\n| `Enter` | 确认当前选中项 |\n| `Esc` | 退出反馈输入模式；否则视为 `deny_once` |\n| `Ctrl+O` | 展开 / 折叠完整 diff |\n| 滚轮 / `PgUp` / `PgDn` | 滚动 diff 详情 |\n| 数字键 `1`-`7` | 直接触发编辑层对应决定 |\n| 文本输入 + `Enter` | `deny_with_feedback` 模式下提交指导文本 |\n\n注意一个细节：`deny_with_feedback` 既可以被 `6` 键直接触发，也可以先通过方向键选中该选项再按 `Enter` 进入——两条路径最终都进入 `feedbackMode`，输入 Esc 可退回选项列表（[tty-app.ts](src/tty-app.ts#L1875-L1957)）。\n\nSources: [tty-app.ts](src/tty-app.ts#L1626-L1644)、[tty-app.ts](src/tty-app.ts#L1798-L1958)、[chrome.ts](src/tui/chrome.ts#L498-L557)\n\n## 七、与系统提示词的集成\n\n权限状态不只是运行时约束，还会以**提示词上下文**的形式告知模型。`PermissionManager.getSummary()` 输出当前 cwd、额外允许的目录、危险命令 allowlist 与受信编辑目标，`buildSystemPrompt` 将其拼入 `Permission context:` 段落。系统提示同时声明"你可以访问 cwd 之外，但工具权限可能先暂停等待审批"——这让模型在规划阶段就能预判哪些操作会触发审批，从而避免无效的越界尝试（[permissions.ts](src/permissions.ts#L222-L248)、[prompt.ts](src/prompt.ts#L5-L35)）。\n\n非 TTY 模式下（如脚本或管道调用），`PermissionManager` 不携带提示处理器，三层校验命中未决情况时统一抛错而非静默放行——**审批能力缺失 = 直接拒绝**，这是与 Claude Code 等工具一致的安全默认（[index.ts](src/index.ts#L79-L80)、[permissions.ts](src/permissions.ts#L283-L287)）。\n\nSources: [permissions.ts](src/permissions.ts#L222-L248)、[prompt.ts](src/prompt.ts#L13-L35)、[index.ts](src/index.ts#L79-L80)\n\n## 八、总结与延伸阅读\n\n三层权限系统用一个 `PermissionManager` 统一了路径、命令、编辑三类安全边界：路径层以目录前缀为持久化粒度拦截越界访问，命令层以完整签名为单位审批危险与未知命令，编辑层在写入前用 diff 换取用户对每次改动的显式确认。三个层次共享七种决策、三种作用域和同一套 TUI 挂起协议，形成了从"看得到"到"跑得动"再到"改得进"的完整信任链。\n\n- 编辑审批的 diff 生成与 `applyReviewedFileChange` 是 [写前审阅流程与统一 diff 生成](18-xie-qian-shen-yue-liu-cheng-yu-tong-diff-sheng-cheng) 的主题，建议紧接着阅读。\n- 权限校验是工具执行管线的一环，可回看 [统一工具协议：注册、Schema 校验与集中执行](8-tong-gong-ju-xie-yi-zhu-ce-schema-xiao-yan-yu-ji-zhong-zhi-xing) 了解工具注册与 `ToolContext` 的注入方式。\n- 命令审批与后台任务的交互（`&` 后缀的 `backgroundShell`）在 [后台 shell 任务：前台工具与后台进程的区分](27-hou-tai-shell-ren-wu-qian-tai-gong-ju-yu-hou-tai-jin-cheng-de-qu-fen) 中展开。\n- 审批期间代理循环如何挂起与恢复，可参考 [代理循环：model → tool → model 的多步执行流程](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng)。';export{s as default};

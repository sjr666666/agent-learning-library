const e='本页面向自托管运维场景，覆盖三块核心内容：**环境变量体系**（加载机制、分类清单与公网部署要点）、**Agent Docker 镜像**（构建参数、多架构发布流水线与容器入口行为）、**备份与恢复**（一致性快照、安全校验不变量与恢复流程）。操作命令均以 `make` 为核心入口，与 [快速开始：环境要求与一键启动](2-kuai-su-kai-shi-huan-jing-yao-qiu-yu-jian-qi-dong) 形成衔接。\n\n```mermaid\nflowchart LR\n    subgraph Host["宿主机（Node 主服务）"]\n        ENV[".env + Web 设置"] --> Core["HappyClaw Node Service"]\n        Core --> DB[("data/db/messages.db<br/>SQLite WAL")]\n        Core --> DATA[("data/ 其余组件")]\n    end\n    subgraph Registry["Docker Hub"]\n        IMG["riba2534/happyclaw-agent<br/>amd64 + arm64 manifest"]\n    end\n    subgraph Container["任务容器（一次性）"]\n        AR["agent-runner + Chromium + 工具链"]\n    end\n    Core -->|docker run| Container\n    IMG -->|make docker-pull| Core\n    Core -->|make backup| ARCHIVE["happyclaw-backup-*.tar.gz"]\n    ARCHIVE -->|make restore| DATA\n```\n\n主服务始终以 Node.js 运行，Docker 仅用于隔离智能体执行环境，不承载主服务本身——这是理解整套部署拓扑的前提。Sources: [README.md](README.md#L372)\n\n## 环境变量体系\n\n### 加载机制与代理支持\n\n项目在 `src/load-env.ts` 中实现了一套**最早的加载时机**：该模块被强制作为 `index.ts` 的第一个 import，在其它任何模块读取 `process.env` 之前，先通过 Node 20.12+ 的 `process.loadEnvFile()` 将项目根目录的 `.env` 载入环境变量。缺少 `.env` 文件属正常情况（所有变量均有默认值），静默忽略。自托管部署用 `.env` 配置 `CORS_ALLOWED_ORIGINS`（公网域名白名单）等变量。Sources: [load-env.ts](src/load-env.ts#L1-L9)\n\n同一模块还解决了一个隐蔽的运维问题：undici（Node 全局 fetch）默认**不读取** `HTTP_PROXY`/`HTTPS_PROXY`，导致 server-side fetch（如 Claude OAuth 令牌交换、连通性测试）使用裸出口 IP 直连，在大陆网络下会被 Anthropic 以 403 拒绝。`load-env.ts` 在最早时机设置 `EnvHttpProxyAgent`，并遵守 `NO_PROXY`。值得注意的是它**刻意不检查 `ALL_PROXY`**——undici 不支持 SOCKS，若把 `ALL_PROXY` 纳入守卫，仅配置该变量的用户会得到一个不做任何代理的 no-op agent，却看到"已启用代理"的误导日志。代理串中的凭据会在日志输出前脱敏。Sources: [load-env.ts](src/load-env.ts#L11-L45)\n\n### 核心变量分类清单\n\nHappyClaw 遵循"Web 设置优先"的配置哲学，不要求维护庞大环境变量集合；环境变量主要用于**启动期参数**与**运行时兜底**。系统设置存在三级优先级：Web 设置文件 > 环境变量 > 硬编码默认值。Sources: [runtime-config.ts](src/runtime-config.ts#L4259-L4277)\n\n| 类别 | 变量 | 默认值 | 说明 |\n|---|---|---|---|\n| Web/端口 | `WEB_PORT` | `3000` | Web、REST API 与 WebSocket 共用端口 |\n| Web/安全 | `WEB_SESSION_SECRET` | 自动生成并持久化 | 登录会话签名密钥（见下文优先级） |\n| Web/公网 | `CORS_ALLOWED_ORIGINS` | 仅 localhost | 逗号分隔的跨站 Origin 白名单，或 `*` |\n| Web/公网 | `CORS_ALLOW_LOCALHOST` | `true` | 是否放行 localhost/127.0.0.1 任意端口 |\n| Web/代理 | `TRUST_PROXY` | `false` | 位于可信反向代理后设为 `true` |\n| 容器 | `CONTAINER_IMAGE` | `riba2534/happyclaw-agent:latest` | 智能体镜像引用 |\n| 容器 | `CONTAINER_TIMEOUT` | `1800000` | 容器硬超时（毫秒） |\n| 容器 | `IDLE_TIMEOUT` | `1800000` | 容器空闲保活时间（毫秒） |\n| 容器 | `CONTAINER_MAX_OUTPUT_SIZE` | `10485760` | 单次容器输出上限（字节，约 10 MiB） |\n| 容量 | `MAX_CONCURRENT_CONTAINERS` | `20` | 最大并发容器数 |\n| 文件 | `MAX_FILE_SIZE_MB` | `50` | Web 与 IM 入站文件大小上限 |\n| 安全 | `ADMIN_HOST_ONLY_MODE` | `false` | 管理员工作区/任务强制宿主机模式 |\n| 时区 | `TZ` | 系统时区 | 日志与定时任务时区（回落 `Asia/Shanghai`） |\n| 安全 | `ALLOW_LEGACY_UNSIGNED_COOKIE` | `false` | 旧版无签名 cookie 兼容升级开关 |\n\nSources: [config.ts](src/config.ts#L26-L55) · [config.ts](src/config.ts#L97) · [runtime-config.ts](src/runtime-config.ts#L3969-L3971) · [runtime-config.ts](src/runtime-config.ts#L4222-L4247) · [README.md](README.md#L306-L325) · [auth.ts](src/auth.ts#L49-L57)\n\nProvider 与渠道凭据（`ANTHROPIC_BASE_URL`、`ANTHROPIC_AUTH_TOKEN`、`CLAUDE_CODE_OAUTH_TOKEN`、`FEISHU_APP_ID/SECRET`、`TELEGRAM_BOT_TOKEN/PROXY_URL` 等）虽然支持环境变量注入，但官方推荐只在 Web 设置中填写——它们使用 AES-256-GCM 加密存储，API 只返回"是否已配置"而不回传明文。系统设置类变量（`FALLBACK_MODEL`、`PLUGIN_AUTO_SCAN`、`BILLING_*`、`EXTERNAL_CLAUDE_DIR` 等）均可通过 `buildEnvFallbackSettings()` 从环境变量兜底。Sources: [runtime-config.ts](src/runtime-config.ts#L1811-L1816) · [runtime-config.ts](src/runtime-config.ts#L1852-L1853) · [runtime-config.ts](src/runtime-config.ts#L1947-L1948) · [runtime-config.ts](src/runtime-config.ts#L4222-L4247)\n\n### Web 会话密钥的三级优先级\n\n`WEB_SESSION_SECRET` 按"环境变量 → 文件 → 生成"三级解析：环境变量优先级最高（允许容器/运维覆盖）；否则读取 `data/config/session-secret.key`（以 `0o600` 权限持久化，重启后保持同一密钥）；若文件不存在则随机生成 32 字节并写入。这意味着**备份中必须包含 `config/` 组件**，否则恢复后所有登录 cookie 失效。Sources: [config.ts](src/config.ts#L64-L79)\n\n## Docker Agent 镜像\n\n### 构建参数与工具链\n\nAgent 镜像只在 GitHub Actions 中构建发布，用户机器不执行 `docker build`，`make dev`/`make start` 均从 Docker Hub 拉取。Dockerfile 基于 `node:24-slim`，预装 Chromium、Python 工具链、编译工具、文档处理（pandoc/poppler）与多数据库客户端，并通过 7 个构建参数实现**默认最新、可精确回滚**的版本策略：\n\n| Build arg | 默认值 | 作用 |\n|---|---|---|\n| `TOOL_REFRESH` | `manual` | 写入首个层，使下游 "latest" 安装层随 main 推送整体失效 |\n| `CLAUDE_AGENT_SDK_VERSION` | `latest` | Agent SDK 版本 |\n| `CLAUDE_CODE_VERSION` | `latest` | Claude Code CLI 版本 |\n| `AGENT_BROWSER_VERSION` | `latest` | agent-browser 版本 |\n| `HEADROOM_VERSION` | `latest` | headroom-ai（仅 `[code,mcp]` extras，避免 torch/CUDA 依赖） |\n| `FEISHU_CLI_VERSION` | `latest` | feishu-cli（从 GitHub Releases 下载并校验 checksums） |\n| `OH_MY_ZSH_REF` | `master` | oh-my-zsh 快照引用 |\n\nSDK/CLI/agent-browser 以 `--no-save --package-lock=false` 安装，**刻意不重写提交的 lockfile**；运行时工具默认取构建时最新版，精确版本留作回滚手段。镜像内保留 `/usr/local/share/happyclaw-tool-versions.txt` 审计所有动态解析版本，无需依赖易失的 CI 日志即可核对。Sources: [Dockerfile](container/Dockerfile#L8-L14) · [Dockerfile](container/Dockerfile#L128-L140) · [Dockerfile](container/Dockerfile#L150-L160) · [Dockerfile](container/Dockerfile#L164-L200) · [Dockerfile](container/Dockerfile#L245-L258) · [README.md](README.md#L272-L290)\n\n### 多架构发布流水线\n\n镜像发布采用**双原生架构、digest 先行、签名后提升**的流水线，不使用 QEMU 模拟：\n\n```mermaid\nflowchart TD\n    PUSH["main 分支 push"] --> B1["ubuntu-24.04<br/>构建 linux/amd64 digest"]\n    PUSH --> B2["ubuntu-24.04-arm<br/>构建 linux/arm64 digest"]\n    B1 --> S1["冒烟：真实入口启动容器<br/>HTTP 探测 127.0.0.1:9222/json/version"]\n    B2 --> S2["冒烟：真实入口启动容器<br/>HTTP 探测 CDP 端点"]\n    S1 -->|通过| D1["上传已验证 digest"]\n    S2 -->|通过| D2["上传已验证 digest"]\n    D1 --> PROMOTE["promote job"]\n    D2 --> PROMOTE\n    PROMOTE --> MANIFEST["imagetools 合并为<br/>git-&lt;sha&gt; manifest"]\n    MANIFEST --> SIGN["cosign 签名 + 验证<br/>（OIDC 身份绑定）"]\n    SIGN --> LATEST["提升至 latest<br/>读后写验证 ×12 重试"]\n```\n\n关键设计：先以 `push-by-digest=true` 推送**无用户标签**的候选镜像，两个平台各自通过真实容器冒烟后才合并——保证任何用户可见标签不会在验证前移动。发布时 `TOOL_REFRESH=${{ github.sha }}` 写入镜像首层，每次 main 推送都会强制刷新 Debian 安全补丁与工具链。`latest` 提升后执行最多 12 次（间隔 5 秒）的读后写一致性验证，容忍 Docker Hub 的标签传播延迟。Sources: [docker-publish.yml](.github/workflows/docker-publish.yml#L1-L88) · [docker-publish.yml](.github/workflows/docker-publish.yml#L119-L199)\n\n### 入口脚本与容器身份模型\n\n`entrypoint.sh` 以 root 启动、随后降权到 node 用户执行 agent-runner。启动序列依次完成：按 `HAPPYCLAW_HOST_IDENTITY_MODE` 对齐节点身份 → 为挂载目录设置 `git safe.directory`（规避 CVE-2022-24765 属主校验）→ 从 `/workspace/env-dir/env` 注入宿主机生成的运行环境（同时**局部遮蔽**所有 root 控制变量，防止宿主机侧 denylist 被绕过）→ 将 npm 全局前缀指向 `/workspace/extra/.npm-global`（使 `npm install -g` 的包在 `docker run --rm` 销毁后仍持久化）→ 重建 Skills 清单 → 启动受管 Chromium 并等待 CDP 就绪 → 将 stdin JSON 缓冲到文件后以 node 身份运行。退出 trap 会停止权限 watcher 与 Chromium，并修复 Claude Code 生成的 `0600` 文件权限。Sources: [entrypoint.sh](container/entrypoint.sh#L1-L43) · [entrypoint.sh](container/entrypoint.sh#L58-L104) · [entrypoint.sh](container/entrypoint.sh#L140-L174)\n\n容器身份模式是部署安全的关键决策点，由宿主机传入：\n\n| 模式 | 语义 | 适用场景 |\n|---|---|---|\n| `direct` | 容器 node 用户对齐宿主机非 root uid/gid | 原生 Linux 部署 |\n| `rootless` | 信任 Docker rootless 的 uid 0 映射桥 | rootless Docker |\n| `host-root` | 保留 node 身份，仅归一化授权挂载 | 宿主 root 调用 |\n| `virtualized` | 保留 Docker Desktop 的 node 属主语义 | macOS/Windows |\n| `userns` / `unknown` | **拒绝启动**（fail-closed） | 无安全身份桥的根因保护 |\n\nSources: [session-permissions.sh](container/session-permissions.sh#L40-L130)\n\n## 备份与恢复\n\n### 备份流程：一致性快照 + 安全树处理\n\n`make backup` 将运行时数据打包为 `happyclaw-backup-{日期}.tar.gz`（默认写入仓库根目录，可用 `BACKUP_DIR=` 覆盖）。核心设计是**在线一致性**：数据库快照通过 better-sqlite3 的 online backup API 生成，读取事务一致快照且包含 WAL 中已提交页，**无需停止服务**；随后对快照执行 `quick_check` 校验并 `chmod 600`。Sources: [sqlite-snapshot.mjs](scripts/sqlite-snapshot.mjs#L20-L84) · [Makefile](Makefile#L244-L285)\n\n```mermaid\nflowchart TD\n    A["make backup"] --> B["sqlite-snapshot.mjs<br/>SQLite online backup API 一致性快照"]\n    B --> C["复制 config/groups/sessions/...<br/>11 个受管组件"]\n    C --> D["prepare-backup-tree.mjs<br/>符号链接/硬链接/特殊文件扫描"]\n    D --> E{"存在不安全条目?"}\n    E -->|是| X["拒绝创建备份（fail fast）"]\n    E -->|否| F["剔除 groups/*/*/logs<br/>生成 backup-manifest.json v2"]\n    F --> G["tar -czf + chmod 600"]\n```\n\n备份内容由 `backup-manifest.mjs` 以 `formatVersion: 2` 记录：受管组件固定为 `config / groups / sessions / skills / mcp-servers / plugins / memory / avatars / extra / builtin-skills / db` 共 11 项，瞬态组件（`ipc / env / streaming-buffer / logs`）明确排除——`env` 含宿主机侧 denylist 等机密，`ipc` 是运行时管道。Sources: [backup-manifest.mjs](scripts/backup-manifest.mjs#L7-L47)\n\n### 备份树的三重安全防御\n\n`prepare-backup-tree.mjs` 在打包前对暂存树执行严格扫描，任何失败都会**在备份创建时**终止（而非恢复时才发现不可恢复）：\n\n1. **运行时可重建的链接**：`sessions/*/.claude` 下由运行时生成的符号链接直接删除（重启可重建），保留周边会话数据；\n2. **无法安全迁移的链接**：`groups/` 下指向数据目录外部的链接删除并计数提示；指向数据目录内部的相对链接**记录到 `backup-symlinks.json`（v1）后解除**，恢复时按元数据重建；\n3. **硬性拒绝**：绝对路径链接、逃逸出备份根的链接、非文件/目录/链接的特殊文件（FIFO 等）直接报错；**硬链接文件（nlink>1）也被拒绝**——tar 会将其存储为 link-type 条目，而恢复端拒绝该类型，放行会产生"备份成功但永远无法恢复"的归档。\n\n此外，`groups/*/*/logs` 目录在打包前被剪除。Sources: [prepare-backup-tree.mjs](scripts/prepare-backup-tree.mjs#L12-L82) · [Makefile](Makefile#L262-L278) · [backup-restore-safety.test.ts](tests/backup-restore-safety.test.ts#L27-L71)\n\n### 恢复流程：fail-closed 的安全校验链\n\n`make restore` 要求服务已停止（`assert-port-free` 在 Makefile 预检与脚本内紧邻文件操作前**双重执行**，收窄提示到恢复之间的竞态窗口）。恢复器 `restore-backup.mjs` 的校验链层层递进：\n\n```mermaid\nflowchart TD\n    A["make restore FILE=xxx.tar.gz"] --> B["端口空闲检查（双阶段）"]\n    B --> C["归档条目预检<br/>仅允许 -/d 类型；拒绝绝对路径/../非 data 前缀"]\n    C --> D["获取 PID 锁<br/>.happyclaw-restore.lock（fail-closed）"]\n    D --> E["解压到 staging<br/>--no-same-owner/--no-same-permissions"]\n    E --> F["解压树复检<br/>拒绝符号链接/特殊文件"]\n    F --> G["manifest v2 校验<br/>+ 敏感组件权限加固 0700/0600"]\n    G --> H["SQLite integrity_check<br/>清除陈旧 -wal/-shm 侧车文件"]\n    H --> I["按元数据重建安全符号链接<br/>（目标不得逃逸）"]\n    I --> J["逐组件原子替换<br/>失败回滚 + 清理孤儿 staging"]\n```\n\n恢复期的关键机制包括：**PID 戳记排他锁**（`.happyclaw-restore.lock`，`wx` 标志原子创建；陈旧锁**绝不自动回收**——便携文件 API 无法实现无竞态的 compare-and-delete，破坏性操作必须人工确认）；**staging + 回滚目录**（每组件先 rename 旧数据到 `rollback/` 再安装新数据，任一步失败按逆序回滚）；**孤儿 staging 清理时机**（仅在本次恢复完整验证并成功应用后才清扫历史残留——若提前清扫，遇到本次归档损坏时将摧毁最后一次可恢复副本）。Sources: [restore-backup.mjs](scripts/restore-backup.mjs#L86-L133) · [restore-backup.mjs](scripts/restore-backup.mjs#L300-L365) · [restore-backup.mjs](scripts/restore-backup.mjs#L367-L398) · [restore-backup.mjs](scripts/restore-backup.mjs#L414-L462) · [restore-backup.mjs](scripts/restore-backup.mjs#L476-L527)\n\n### 数据库迁移备份\n\n除运维备份外，数据库升级路径自带一道保险：`initDatabase()` 在 schema 迁移（v39 及以上，或检测到遗留 `memory_chunks` 表）前通过 `VACUUM INTO` 创建自包含快照，路径默认为 `data/db/migration-backups/`，可用 `HAPPYCLAW_MIGRATION_BACKUP_DIR` 覆盖。备份失败会**中止启动**（拒绝迁移），快照同样执行 `quick_check` 并 `chmod 600`。WAL 模式在迁移备份闸门通过后才开启，`WAL + synchronous=NORMAL` 是文档化的安全配对。Sources: [db.ts](src/db.ts#L382-L429) · [db.ts](src/db.ts#L484-L490)\n\n## 运维与监控\n\n### 健康检查与常用命令\n\n- **`GET /api/health`**（无认证，挂载于 `/api`）：返回 `{ status, checks: { database, queue, uptime } }`，数据库连通性与队列可用性任一失败即返回 `503 unhealthy`——适合接入反向代理的存活探针。Sources: [monitor.ts](src/routes/monitor.ts#L198-L236) · [web.ts](src/web.ts#L280)\n- **`make status`**：汇总端口占用、`/api/health` 健康状态、日志文件与 Docker 容器列表；**`make logs`** 跟踪 `/tmp/happyclaw.log`；**`make stop`** 按端口终止进程。Sources: [Makefile](Makefile#L148-L174)\n\n### 公网部署要点\n\n对外暴露时需理解两层 Origin 防御：HTTP API 走 CORS 中间件，但 **WebSocket 不经过 CORS**（浏览器不发 preflight），因此 `setupWebSocket` 在 upgrade 时独立执行同源校验 + 白名单校验，拒绝的 Origin 返回 403 并只 warn 一次。同源请求（Origin host == Host header）始终放行，跨站来源需配置 `CORS_ALLOWED_ORIGINS`。配合 `SameSite=Strict` cookie 形成纵深防御（CSWSH）。反向代理部署时设 `TRUST_PROXY=true`；生产建议 HTTPS + 强密码 + 关闭开放注册，并定期备份 `data/`。Sources: [web.ts](src/web.ts#L201-L243) · [web.ts](src/web.ts#L1390-L1430) · [README.md](README.md#L387-L396)\n\n## 下一步阅读\n\n- 容器执行模式的完整语义与挂载边界：[Host 与 Container 双执行模式](7-host-yu-container-shuang-zhi-xing-mo-shi)\n- 数据目录结构与备份恢复的深层机制：[数据目录结构与一致性备份恢复](23-shu-ju-mu-lu-jie-gou-yu-zhi-xing-bei-fen-hui-fu)\n- 镜像 CI 发布与工程质量约束：[CI 流水线与工程规范](30-ci-liu-shui-xian-yu-gong-cheng-gui-fan)\n- 凭据加密存储与密钥管理：[密钥加密与凭据管理](25-mi-yao-jia-mi-yu-ping-ju-guan-li)';export{e as default};

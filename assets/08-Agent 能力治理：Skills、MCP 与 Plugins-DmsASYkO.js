const e='Agent 的能力面由三条相互独立的通道构成：**Skills**（技能：可复用的指令与工作流）、**MCP**（Model Context Protocol：外部工具与数据源接入）、**Plugins**（Claude Code 插件：以 Marketplace 形态打包的命令/技能/钩子集合）。这三者共享同一套治理范式——**来源层叠 → 策略筛选 → 运行时物化 → 变更静默生效**。本文聚焦这一治理模型本身：各通道的来源与优先级、Agent Profile 的运行时策略（`runtime_policy`）、以及保证变更一致性的运行时静默机制。\n\n## 统一治理范式：三层模型\n\n无论是 Skill、MCP 还是 Plugin，HappyClaw 都遵循同一套三层架构：\n\n```mermaid\nflowchart LR\n    subgraph L1["① 来源层（Layer）"]\n        A1["builtin / project / workspace<br/>managed(user) / host / plugin"]\n        A2["Claude 原生上下文<br/>settings.json / .mcp.json"]\n        A3["Plugin Catalog<br/>不可变快照树"]\n    end\n    subgraph L2["② 策略层（Policy）"]\n        B1["AgentProfileRuntimePolicy<br/>mode: inherit / custom / disabled<br/>ids: string[]"]\n    end\n    subgraph L3["③ 物化层（Materialize）"]\n        C1["effective-skills 只读挂载<br/>/home/node/.claude/skills"]\n        C2["settings.json<br/>replaceMcpServers: true"]\n        C3["runtime/snapshots 独立 inode<br/>--plugin-dir 指向"]\n    end\n    L1 --> L2 --> L3\n    L3 --> D["Agent Runner<br/>（Host / Container）"]\n```\n\n三层各自回答一个问题：**来源层**决定「能力从哪里来」，**策略层**决定「哪个 Agent 能用哪些能力」，**物化层**决定「以何种安全形态注入运行会话」。三条通道的差异集中在来源层的组织方式与物化层的注入路径上。\n\n| 维度 | Skills | MCP | Plugins |\n|---|---|---|---|\n| 来源组织 | 目录树（`SKILL.md` 即技能标识） | 作用域文件（system/user 两份 store） | 不可变快照目录（内容哈希命名） |\n| 冲突语义 | 层叠优先级，高优先级遮蔽低优先级 | 后加载者覆盖同名（settings 语义） | 快照隔离，命令索引检测命名冲突 |\n| 策略模式 | `inherit / custom / disabled` | `inherit / custom / disabled` | 启用引用（pin 快照版本） |\n| 物化形态 | 逐技能只读挂载 + 符号链接 | 会话 `settings.json` 全量替换 | 独立 inode 拷贝 + `--plugin-dir` |\n| 变更副作用 | 停止引用该技能的会话 Runner | 停止引用该 MCP 的会话 Runner | 重建运行时树 + 失效命令索引 |\n\nSources: [types.ts](src/types.ts#L355-L395)、[effective-skill-resolver.ts](src/effective-skill-resolver.ts#L24-L60)、[plugin-catalog.ts](src/plugin-catalog.ts#L20-L60)\n\n## Skills：六层来源与优先级解析\n\nSkills 是所有能力通道中来源最复杂的一个。HappyClaw 为每个会话按**从低到高**的固定顺序解析六层来源：`builtin`（内置技能，随数据目录物化）→ `host`（管理员原生 `~/.claude/skills`，仅当策略显式请求）→ `project`（`container/skills`，随仓库分发）→ `managed`（用户经 UI/API 安装的技能）→ `workspace`（工作区自带的 `.claude/skills`）→ `plugin`（每个启用插件自带的 `skills/` 目录，以 `plugin:skill` 限定名加入）。\n\n```mermaid\nflowchart TB\n    subgraph Layers["层叠顺序（低 → 高）"]\n        L1["builtin<br/>data/builtin-skills"]\n        L2["host<br/>~/.claude/skills（管理员）"]\n        L3["project<br/>container/skills"]\n        L4["managed<br/>data/skills/{userId}"]\n        L5["workspace<br/>groups/{folder}/.claude/skills"]\n        L6["plugin<br/>runtime/.../{plugin}/skills"]\n    end\n    Policy["AgentProfileRuntimePolicy.skills<br/>mode: inherit / custom / disabled"]\n    Resolver["resolveEffectiveSkills()<br/>禁用即移除 → 优先级裁决 → 冲突登记"]\n    Reconcile["reconcileSessionSkills()<br/>隔离旧目录 → 重建 → 符号链接"]\n    Layers --> Resolver\n    Policy --> Resolver\n    Resolver --> Reconcile\n    Reconcile --> Mount["逐技能只读挂载<br/>/workspace/effective-skills/{id}"]\n```\n\n解析器 `resolveEffectiveSkills()` 的关键规则是**禁用定义不构成墓碑**：一个被策略排除（`disabled`/`custom` 未选中）的技能会直接从候选集中移除，因此低层级的同名技能仍可生效——这与「禁用即删除」的直觉一致，避免策略变更意外遮蔽更底层的默认技能。当多个层级提供同名技能时，最高优先级者胜出，其余被标记为 `shadowed`，冲突清单进入 manifest 供审计与 UI 提示。\n\n**内容哈希**是整个治理体系一致性的基石。`hashSkillDirectory()` 对技能目录做全量 SHA-256（遍历文件、目录、符号链接目标，排除 `.git`/`node_modules`/`__pycache__` 等），因此策略或内容任何一处变化都会反映到 manifest 的顶层 `hash` 字段——会话是否需要重建、Runner 是否需要重启，都由该哈希驱动。\n\n物化层 `reconcileSessionSkills()` 保证会话目录的**可重建性**：真实文件/目录先被移入 `orphaned-skills` 隔离区（而非删除），再以 staging → previous 两段式重建，最后为每个选中技能建立符号链接。容器模式下 `entrypoint.sh` 先清空会话 `skills` 目录，再为宿主侧只读挂载的 `/workspace/effective-skills/*` 逐个执行 `--link-skill`——彻底杜绝「上一个 Agent 在容器里写入的技能目录在重启后存活」的脏状态。\n\nSources: [effective-skill-resolver.ts](src/effective-skill-resolver.ts#L122-L240)、[effective-skill-resolver.ts](src/effective-skill-resolver.ts#L280-L341)、[claude-context-resolver.ts](src/claude-context-resolver.ts#L266-L300)、[container/entrypoint.sh](container/entrypoint.sh#L67-L80)、[container-runner.ts](src/container-runner.ts#L1249-L1265)\n\n### 技能导入与内置目录\n\n用户侧技能通过两条通道导入：**Git 仓库**（`POST /api/skills/import/git`）与 **ZIP 压缩包**（`POST /api/skills/import/archive`）。两者都受严格的资源配额约束——Git 仓库上限 64MB、ZIP 展开上限 25MB、条目上限 1000、扫描深度 5 层；Git 导入还会先验证 URL 为安全 HTTPS 且域名解析到公网地址，并用目录配额监视器在子进程超限时强制终止。\n\n内置技能目录 `data/builtin-skills` 由 `scripts/builtin-skill-catalog.mjs` 物化：它写入 `.catalog.json` 标记文件（含 feishu-cli 固定版本、源 SHA-256、内容 payloadHash 与技能 ID 列表），`Makefile` 的 `_ensure-builtin-skills` 目标会在每次构建前校验该标记，缺失即调用 `install-host-tools.sh skills` 重新物化。这保证了 Host 与 Container 两种执行模式共享同一份**固定版本**的内置技能。\n\nSources: [skill-import-service.ts](src/skill-import-service.ts#L1-L30)、[skill-import-service.ts](src/skill-import-service.ts#L301-L370)、[scripts/builtin-skill-catalog.mjs](scripts/builtin-skill-catalog.mjs#L15-L40)、[Makefile](Makefile#L80-L85)、[routes/skills.ts](src/routes/skills.ts#L767-L830)\n\n## MCP：作用域、来源合并与秘密边界\n\nMCP 通道的核心治理对象是**服务器定义**与**机密**（env 与 headers），它们被拆分为两个存储文件（`servers.json` 与 `secrets.json`），并引入**双作用域**：`system` 作用域由管理员维护、默认仅管理员可见（`memberAccess: \'admin_only\'`，可显式改为 `shared` 共享给成员）；`user` 作用域属于单个用户。\n\n```mermaid\nflowchart LR\n    subgraph Sources["运行时 MCP 来源合并（mergeMcpServerLayers）"]\n        C1["Claude 原生上下文<br/>host settings.json<br/>.claude.json"]\n        C2["项目上下文<br/>.mcp.json<br/>.claude/settings*.json"]\n        C3["托管层<br/>system（admin_only/shared）<br/>+ user"]\n        C4["插件 MCP<br/>.mcp.json / mcp-servers/*.json<br/>plugin:{mp}/{plugin}:{server}"]\n    end\n    Policy["Agent McpPolicy<br/>inherit / custom / disabled"]\n    Merge["合并后<br/>buildEffectiveMcpManifest()<br/>只暴露 serverIds + 哈希"]\n    C1 --> Merge\n    C2 --> Merge\n    C3 --> Merge\n    C4 --> Policy\n    Policy --> Merge\n    Merge --> Settings["会话 settings.json<br/>replaceMcpServers: true"]\n```\n\n合并遵循 Claude 原生的设置优先级：宿主设置 → 项目 `.mcp.json` → 项目 settings → 项目 settings.local → **托管层**（HappyClaw 管理的 system + user 服务器，作为加法最终层）。`mergeMcpServerLayers()` 的后覆盖语义意味着托管层可以确定性地覆盖同名服务器。运行时 manifest 只暴露 `serverIds` 与内容哈希，**绝不携带命令或机密**——哈希用于审计与变更检测，机密只存在于独立的 secrets store。\n\n**Agent 策略的三种模式**在 `resolveManagedMcpPolicy()` 中实现：`inherit` 合并全部 system（共享部分）+ user 服务器；`disabled` 返回空集；`custom` 按引用列表精确筛选，引用格式为 `system:{id}` / `user:{id}`（裸 ID 作为历史兼容视为 user 作用域）。若 custom 引用了已被删除/禁用的服务器，容器构建会抛出 `agent_profile_unavailable`——这是确定性错误而非瞬时故障，避免 Runner 带着残缺配置启动。`sync-host` 端点（管理员专用）从宿主 Claude 目录**一次性拷贝**服务器定义到托管层，已存在的定义不会被覆盖，因此它是「导入」而非「实时同步」。\n\n**秘密边界**贯穿始终：`StoredMcpServerSecrets` 只存 `env`/`headers` 两个键，API 返回的摘要仅暴露键名（`envKeys`/`headerKeys`）与存在性标记，取值永不回传；system 级服务器对非管理员成员在运行层面直接被过滤（`restrictedSystemIds`），即使共享标记未开启。\n\nSources: [mcp-context.ts](src/mcp-context.ts#L50-L89)、[mcp-utils.ts](src/mcp-utils.ts#L410-L474)、[effective-mcp-manifest.ts](src/effective-mcp-manifest.ts#L30-L95)、[container-runner.ts](src/container-runner.ts#L690-L760)、[routes/mcp-servers.ts](src/routes/mcp-servers.ts#L812-L870)\n\n## Plugins：不可变快照与命令展开\n\nPlugins 通道的治理核心是**内容寻址的不可变快照**。宿主 Claude 的 Marketplace（`~/.claude/plugins/marketplaces/` 物理目录 + `known_marketplaces.json` 注册项）经导入器扫描后，每个插件被拷贝为 `versions/{contentHash}/` 快照：写入一次性完成（`rename(2)` 原子落盘，绝不覆盖既有快照），索引 `index.json` 以 tmp+rename 原子更新，模块级互斥锁保证并发扫描不丢失。\n\n```mermaid\nflowchart TB\n    Import["scanHostMarketplaces()<br/>内容哈希（排除 .git/node_modules）"]\n    Catalog["不可变 Catalog<br/>marketplaces/{mp}/plugins/{plugin}/versions/{hash}/"]\n    Enable["用户启用<br/>PATCH /enabled/:fullId<br/>plugins.json pin snapshot"]\n    Materialize["materializeUserRuntime()<br/>copyTreeIsolated → 独立 inode<br/>@happyclaw-runtime-markers"]\n    Index["buildCommandIndex()<br/>/name 短名 + /plugin:name 限定名"]\n    Expander["DMI 命令展开器<br/>expandMessagesIfNeeded()"]\n    Import --> Catalog --> Enable --> Materialize\n    Materialize --> Index --> Expander\n    Materialize --> Mount["只读挂载 /workspace/plugins<br/>或 host --plugin-dir"]\n```\n\n`materializeUserRuntime()` 的关键不变量是**独立 inode**：每个启用插件的运行时树都通过逐文件 `copyFileSync(..., COPYFILE_FICLONE)`（APFS/btrfs/xfs 上为 COW reflink）从 Catalog 拷贝而来，**绝不使用硬链接或符号链接**。这是因为宿主模式 Agent 以 `bypassPermissions` 运行并持有运行时绝对路径，任何写入必须落在隔离树内，不能反向污染共享 Catalog；符号链接则会向容器内泄漏宿主绝对路径，破坏只读挂载边界。快照根部的 `@happyclaw-runtime-markers` 目录（`@` 被名称段正则拒绝，天然不可伪造）记录物化版本，下次物化可证明隔离性而跳过拷贝；被仍在运行的 Agent 挂载的快照由 GC 判定为「在用」而保留。\n\n**命令索引与展开器**解决的是 SDK 模式下的功能缺口：标记 `disable-model-invocation: true`（DMI）的插件斜杠命令在 CLI 里由人输入并展开，但 SDK 模式没有该展开器，所以 HappyClaw 自建索引——每个 `commands/{name}.md` 注册 `/name` 短名与 `/plugin:name` 限定名两个别名，与内置命令（`clear`/`status`/`list` 等）冲突时丢弃短名保住内置处理器的优先权。`plugin-expander-core.ts` 实现三态结果：`miss`（非插件命令，原样透传给 Agent）、`expanded`（渲染完成的 prompt）、`reply`（带内系统回复）。展开管线对 `!`\\`bash\\`\\` 内联命令在**宿主或容器内**执行并把 stdout 拼回 prompt；`$ARGUMENTS` 与 `$1..$n` 一律经由环境/位置参数注入，**绝不做 JS 字符串插值**；代码围栏块原样保留。展开成功后写入 `plugin_expansion` sentinel 到消息附件，崩溃恢复据此跳过重复执行（crash-safety 契约）。\n\n**依赖预检**是纯静态分析（不执行 shell）：从命令/技能的 frontmatter 提取 `Bash(node:*)` 类工具声明，对照容器内已知二进制集合与 `config/plugin-deps-override.json` 的手动覆盖（如 `codex@openai-codex` 需要宿主 PATH 上有 `codex`），缺失项以非阻塞 warning 形式随插件列表返回，让用户在上线前知道该插件在容器模式会缺什么。\n\nSources: [plugin-catalog.ts](src/plugin-catalog.ts#L145-L175)、[plugin-importer.ts](src/plugin-importer.ts#L40-L130)、[plugin-materializer.ts](src/plugin-materializer.ts#L1-L80)、[plugin-utils.ts](src/plugin-utils.ts#L40-L90)、[plugin-command-index.ts](src/plugin-command-index.ts#L40-L120)、[plugin-expander-core.ts](src/plugin-expander-core.ts#L20-L50)、[plugin-inline-bash.ts](src/plugin-inline-bash.ts#L1-L45)、[config/plugin-deps-override.json](config/plugin-deps-override.json#L1-L6)\n\n## 运行时静默：让能力变更安全生效\n\n能力治理的最后一个环节是**变更如何安全地到达正在运行的 Agent**。三条通道的变更（技能安装/重装、MCP 增删改、插件启停）都经由同一套运行时静默机制：\n\n```mermaid\nsequenceDiagram\n    participant API as 能力路由<br/>(skills/mcp/plugins)\n    participant Lock as withCapabilityScopeLocks<br/>(user / system 键)\n    participant Quiesce as mutateCapabilityAroundRuntimeQuiesce\n    participant Runner as 受影响工作区 Runner\n    API->>Lock: 获取作用域锁（按键字典序，防死锁）\n    Lock->>Quiesce: 提交变更 + 失效会话\n    Quiesce->>Runner: 计算受影响目标（profileUsesCapability）\n    Quiesce->>Quiesce: 队列变更暂停 + 停止 Runner\n    Quiesce->>API: 提交（写 store / plugins.json）\n    API->>API: 失败 → runtime-safety gate<br/>队列无法以陈旧能力重启\n```\n\n`capability-lock.ts` 实现了可重入的串行化锁：键为 `user:{id}` 或 `system`，多键操作按字典序获取（避免系统+用户跨作用域操作与单作用域操作死锁）。锁之上是 `mutateCapabilityAroundRuntimeQuiesce()`——它先计算**受影响的运行时目标**：遍历该用户所有 Agent Profile，凡策略（`inherit`，或 `custom` 且引用包含受影响 ID）可能观测到该能力的 Profile 所挂工作区都会被停止，且会话目录被删除，这样陈旧的能力快照（旧 MCP JSON、旧技能链接）不可能在重启后残留。\n\n提交结果有三种失败语义，路由层据此返回可重试的 503：`WorkspaceRuntimeQuiesceError`（明确区分「已保存但清理失败」与「未保存」）、`CapabilityRuntimeCommitError`（提交结果不确定，必须重试以完成 fail-closed 清理）、以及确定性 `agent_profile_unavailable`（配置本身残缺，重试无意义）。这套机制与[会话队列与并发控制](18-hui-hua-dui-lie-yu-bing-fa-kong-zhi)页面的队列暂停机制协同：静默期间队列拒绝新任务，避免任务落在「能力已变更但 Runner 未重启」的中间态。\n\nSources: [capability-lock.ts](src/capability-lock.ts#L5-L60)、[capability-runtime-mutation.ts](src/capability-runtime-mutation.ts#L35-L75)、[capability-runtime-mutation.ts](src/capability-runtime-mutation.ts#L100-L120)、[routes/skills.ts](src/routes/skills.ts#L651-L675)\n\n## 策略即配置：Agent Profile 运行时策略\n\n三条通道在 Agent Profile 层汇合为一份 `runtime_policy`，它是「哪个 Agent 拥有哪些能力」的唯一事实来源。除三通道策略外，它还承载两个跨通道的宿主上下文开关：`context.source`（`managed` / `host_claude`，决定是否注入管理员原生 `~/.claude` 上下文）与独立的 `skills.host` 子策略（历史 Profile 缺省该字段时，按 context 来源推导：`host_claude` → inherit，否则 disabled——这是明确的兼容哨兵）。\n\n| 策略字段 | 模式 | 语义 |\n|---|---|---|\n| `skills.mode` | inherit / custom / disabled | inherit 全量托管技能；custom 按 ID 白名单；disabled 关闭 |\n| `skills.host` | inherit / custom / disabled | 管理员宿主原生技能，独立于托管层 |\n| `mcp.mode` | inherit / custom / disabled | 对应 `resolveManagedMcpPolicy` 三态 |\n| `context.source` | managed / host_claude | 是否注入管理员原生 Claude 上下文（仅 admin 可用） |\n\n前端的「能力配置」体验由 `CapabilitiesPage`（导航 Skills / MCP / Plugins 三个分区）与 `AgentSkillsPolicyEditor` / `EffectiveCapabilitiesPreview` 组件构成，页面文案明确区分两种能力来源：**可复用资源**（能力库中安装，按智能体决定是否启用）与**工作区自带上下文**（CLAUDE.md、`.claude/skills`、项目 MCP——不在此处分配）。`agent-capability-preview.ts` 在提交前渲染生效预览（含来源、遮蔽关系、`system_admin_only` 不可用原因与 manifest 哈希），让策略编辑的后果在保存前可见。宿主模式还有一层工具预检（`agent-capabilities.ts`）：探测 `claude` / `feishu-cli` / `agent-browser` / `uv` 等二进制是否可用，`claude` 优先使用 SDK 内置二进制以避免 PATH 包装器劫持。\n\nSources: [types.ts](src/types.ts#L355-L395)、[agent-profile-policy.ts](src/agent-profile-policy.ts#L19-L40)、[agent-capability-preview.ts](src/agent-capability-preview.ts#L30-L70)、[agent-capabilities.ts](src/agent-capabilities.ts#L40-L80)、[web/src/pages/CapabilitiesPage.tsx](web/src/pages/CapabilitiesPage.tsx#L1-L50)\n\n## 小结\n\nSkills、MCP、Plugins 三通道共享的治理范式可概括为：**来源层用内容哈希保证可审计性，策略层用三态模式（inherit/custom/disabled）保证可配置性，物化层用隔离拷贝与只读挂载保证安全性，变更层用作用域锁 + 运行时静默保证一致性**。差异只在来源的组织形态——Skills 是层叠目录、MCP 是双作用域合并、Plugins 是不可变快照——以及物化的注入路径。若想深入，可继续阅读 [Agent Runner 与 IPC 通信协议](10-agent-runner-yu-ipc-tong-xin-xie-yi)（能力如何注入运行会话）、[RBAC 权限模型与资源隔离](24-rbac-quan-xian-mo-xing-yu-zi-yuan-ge-chi)（admin/member 对 system MCP 与宿主上下文的访问边界）、[Host 与 Container 双执行模式](7-host-yu-container-shuang-zhi-xing-mo-shi)（两条物化路径的差异）。';export{e as default};

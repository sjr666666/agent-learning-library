const s='Miniclaw 把几乎所有运行时状态——消息、工作区、Agent Profile、渠道可靠性队列、定时任务、计费用量、长期记忆——都收敛进单个 SQLite 文件 `data/db/messages.db`，由一个超过 1.4 万行的 `src/db.ts` 统一治理。本文剖析这条持久化内核的三条主线：**双运行时兼容层与 PRAGMA 调优**、**以版本号为轴心的三段式迁移策略**，以及支撑整个产品模型的**核心表族拓扑**。理解这一页是读懂后续可靠性状态机、调度器与记忆系统等章节的地基。\n\nSources: [db.ts](src/db.ts#L100-L106), [config.ts](src/config.ts#L33-L35)\n\n## 存储引擎与双运行时兼容层\n\nMiniclaw 同时支持 Bun 与 Node.js 两种运行时，SQLite 驱动的差异被隔离在一个仅 23 行的兼容层里：Bun 环境走内置的 `bun:sqlite`，Node.js 环境走 `better-sqlite3`。两者在 `prepare/run/get/all/exec/transaction` 这些核心 API 上几乎完全一致，唯一的差异是 import 路径和 `pragma()` 方法——Bun 没有该方法，需要退化为 `exec`。动态拼接模块名字符串（`\'bun:sqlite\'`）则是为了阻止 tsc 在 Node 编译路径上尝试解析这个不存在的模块。这层抽象让上层 `db.ts` 完全不必感知运行时差异。\n\nSources: [sqlite-compat.ts](src/sqlite-compat.ts#L1-L23)\n\n数据库文件的落盘位置由 `config.ts` 决定：项目根目录下的 `data/db/messages.db`（`STORE_DIR` 即 `DATA_DIR/db`），`initDatabase()` 在打开连接前会递归创建目录。这个布局也是后续备份恢复工作流的锚点。\n\nSources: [config.ts](src/config.ts#L33-L35), [db.ts](src/db.ts#L468-L472)\n\n## 启动初始化管线：PRAGMA 调优与外键治理\n\n`initDatabase()` 是整条持久化管线的入口，其执行顺序经过刻意编排——每一步都依赖前一步建立的安全前提。下面的流程图概括了从打开连接到写入版本头的完整序列：\n\n```mermaid\nflowchart TD\n    A["打开 messages.db"] --> B["busy_timeout = 5000"]\n    B --> C["读取 schema_version<br/>（任何写操作之前）"]\n    C --> D{"需要升级？"}\n    D -- "是（v≥39 或存在遗留表）" --> E["VACUUM INTO 预迁移备份<br/>quick_check 校验"]\n    E -- "备份失败" --> X["抛错中止启动"]\n    D -- "否 / 备份成功" --> F["开启 WAL + synchronous=NORMAL<br/>temp_store=MEMORY"]\n    F --> G["报告已知外键孤儿"]\n    G --> H["foreign_keys=ON<br/>+ foreign_key_check"]\n    H -- "messages→chats 孤儿" --> I["自动补完删除级联"]\n    H -- "其他违规" --> J["降级回 OFF 并告警"]\n    I & J --> K["幂等 DDL 重放<br/>38 张 CREATE TABLE IF NOT EXISTS"]\n    K --> L["ensureColumn 列补齐"]\n    L --> M["版本门控数据迁移<br/>db.transaction 包裹"]\n    M --> N["assertSchema 结构断言"]\n    N --> O["写入 schema_version = 69"]\n```\n\nSources: [db.ts](src/db.ts#L468-L493)\n\nPRAGMA 组合体现了明确的工程权衡。`busy_timeout = 5000` 让并发写竞争等待而非立即报错；`journal_mode = WAL` 提升读写并发，但其开启被有意安排在升级备份门控完成之后，确保备份读到的源库未被本进程改动过；`synchronous = NORMAL` 是 WAL 的官方推荐配对——提交不再逐事务 fsync WAL（代码注释记录了该驱动上实测 21 倍的写入提升），断电最坏情况只是丢失最后瞬间的已提交数据，绝不会损坏数据库；`temp_store = MEMORY` 则让排序与临时表驻留内存。\n\nSources: [db.ts](src/db.ts#L474-L492)\n\n外键治理是启动管线中最能体现"防御性数据工程"的部分。SQLite 出于向后兼容默认关闭外键强制，所有 FK 声明在没有 `PRAGMA foreign_keys = ON` 时都是静默空转。Miniclaw 开启后会立即执行 `PRAGMA foreign_key_check` 全量扫描：若唯一违规类别是 `messages → chats` 孤儿（历史上被中断的聊天删除级联残留），直接补完级联删除并保持强制开启；若是其他类别——例如为了操作员审查而**故意保留**的计费审计行——则降级回 OFF 并输出告警，因为带着现存违规开启强制的下一个写入必然被拒绝。在此之前，`reportKnownForeignKeyOrphans` 还会对用户、订阅、余额等关键父子关系逐一计数并记录告警日志，让孤儿问题可观测但不阻塞启动。\n\nSources: [db.ts](src/db.ts#L494-L539), [db.ts](src/db.ts#L314-L346)\n\n## Schema 版本化机制：router_state 与版本头\n\n版本号的存取围绕一张极简的键值表 `router_state(key TEXT PRIMARY KEY, value TEXT)` 展开。`CURRENT_SCHEMA_VERSION = 69` 是当前版本头，它被显式导出——注释解释了原因：如果每个迁移测试各自硬编码这个数字，每次版本升级都要改动十几个不相干的测试文件，这种噪声会掩盖真正的断言变更；导出常量让测试表达"旧库必须到达最新版"这一语义即可。\n\nSources: [db.ts](src/db.ts#L106-L110), [db.ts](src/db.ts#L660-L664)\n\n版本生命周期有三个关键时刻。**读取**发生在打开连接之后、任何写操作之前（`rawSchemaVersionBeforeInit`），后续多个迁移块依赖这个"原始版本"判断是否执行；**校验**阶段若发现数据库版本大于代码支持的版本，直接抛出"refusing downgrade"拒绝启动——新版本数据库在旧代码上运行可能因缺失列而静默损坏数据；**写入**则在 `initDatabase()` 的最后一行，用 `INSERT OR REPLACE` 把版本号推进到当前头，标志本次迁移全部完成。\n\nSources: [db.ts](src/db.ts#L454-L465), [db.ts](src/db.ts#L2484-L2486)\n\n## 三层迁移策略\n\nMiniclaw 没有引入独立的迁移框架，而是在每次启动时重放一段精心分层的过程式迁移代码。三个层次各司其职：\n\n| 层次 | 机制 | 执行条件 | 典型手段 | 失败语义 |\n| --- | --- | --- | --- | --- |\n| 幂等 DDL 重放 | `CREATE TABLE IF NOT EXISTS` × 38 张 + `ensureColumn` | 每次启动 | 补建缺失表与列 | 已存在即跳过 |\n| 版本门控数据迁移 | 读 `schema_version` 比较 + `db.transaction` | 仅当 `curVer < N` | 行级数据改写、表重建、DROP | 事务原子回滚 |\n| 结构断言 | `assertSchema(必需列, 禁止列)` | 每次启动 | fail-fast 抛错 | 中止启动，提示重建 |\n\nSources: [db.ts](src/db.ts#L263-L293)\n\n**第一层：幂等 DDL。** 所有表的建表语句都以 `IF NOT EXISTS` 书写，因此老库与新库都会完整走过一遍 DDL，差异只在于是否真正生效。列级演进靠 `ensureColumn(table, column, ddl)`：内部先用 `hasColumn` 检查 `PRAGMA table_info`，缺列才执行 `ALTER TABLE ADD COLUMN`。这段代码长达近百个调用（users 表的头像字段群、scheduled_tasks 的租约与软删除字段群、task_runs 的通知租约字段群……），本质上是把"加列"这类无损变更从版本门控中解放出来，让它们可以安全地重复执行。\n\nSources: [db.ts](src/db.ts#L256-L272), [db.ts](src/db.ts#L1236-L1315)\n\n**第二层：版本门控的数据迁移。** 凡涉及行级改写、无法简单幂等的变更，都用 `getRouterStateInternal(\'schema_version\')` 与目标版本比较后包进事务执行。几个代表性样本勾勒出这套机制的谱系：\n\n| 版本跃迁 | 变更内容 | 技术要点 |\n| --- | --- | --- |\n| v16→v17 | `sessions` 单列主键重建为 `(group_folder, agent_id)` 复合主键 | 先探测 `table_info` 确认旧结构，再建新表→拷贝→DROP→RENAME |\n| v23→v24 | 计费系统初始化 | 为存量用户批量补默认套餐、余额与活跃订阅 |\n| v47→v48 | 一体式 Agent Prompt 拆分为 IDENTITY/SOUL/AGENTS/TOOLS 四段 | 无损搬移旧 prompt 至 AGENTS，重算 identity_hash 并写入版本快照 |\n| v66→v67 | DROP 遗留 `memory_chunks` 与 FTS 影子表 | 注释记录其占生产库 65% 体积、数月零写入 |\n| v68→v69 | Agent 推理力度归入 runtime_policy JSON | 仅归一化 JSON，刻意不动 identity/version 元数据 |\n\nSources: [db.ts](src/db.ts#L1642-L1671), [db.ts](src/db.ts#L1705-L1754), [db.ts](src/db.ts#L2046-L2097), [db.ts](src/db.ts#L2464-L2482), [db.ts](src/db.ts#L1996-L2005)\n\n值得注意的是版本读取的来源差异：有的迁移块使用启动早期捕获的 `rawSchemaVersionBeforeInit`（如 v68→v69），有的在执行点现场再读一次（如 v47→v48 的 `promptSchemaVersion`）——后者意味着前面的迁移块已经推进了某些中间状态时仍能读到准确值。而 v66→v67 这类破坏性 DROP 则展示了另一个原则：先探测 `tableExists(\'memory_chunks\')` 再删除，只有真的删掉了东西才打日志，全新数据库不会产生误导性的迁移噪音。\n\nSources: [db.ts](src/db.ts#L2010-L2012), [db.ts](src/db.ts#L2468-L2482)\n\n**第三层：结构断言。** 迁移完成后，`assertSchema` 对核心表声明必需列与禁止列清单，任何偏差都抛出带明确指引的错误（"请删除 data/db/messages.db 后重启"）。这是一种 fail-fast 设计哲学的体现：与其带着残缺 Schema 继续运行并在业务路径上随机爆炸，不如在启动边界就拦截。禁止列检查同样有实战意义——例如 `registered_groups` 明确禁止 `trigger_pattern` 与 `requires_requires` 这类已被移除特性的残留列。\n\nSources: [db.ts](src/db.ts#L274-L293), [db.ts](src/db.ts#L1489-L1543)\n\n## 迁移前备份门控：不可协商的安全网\n\n所有针对 v39 及以上版本的升级都会先经历一道强制备份门控。`createPreMigrationBackup` 使用 `VACUUM INTO` 生成自包含的一致性快照——该命令穿透 SQLite 的事务层读取，已提交的 WAL 页也会包含在内——随后对新文件独立开连接执行 `pragma quick_check` 校验完整性，再收紧权限到 0600。备份文件名自带版本区间与时间戳（`messages-v50-to-v69-{timestamp}-{pid}.db`），目录可通过 `MINICLAW_MIGRATION_BACKUP_DIR` 覆盖，默认落在数据库同级的 `migration-backups/` 下。\n\nSources: [db.ts](src/db.ts#L379-L438)\n\n门控的失败语义同样严格：备份或校验任一环节失败，函数会清理半成品文件（含 `-wal`/`-shm` 伴生文件），然后抛出让启动中止的错误——注释强调"清理绝不能掩盖导致迁移被阻塞的那个备份失败"。两个边界情形也有明确裁决：无版本标记的史前安装若携带遗留 `memory_chunks` 表，按 v0 处理照样备份（因为 v67 会 DROP 它们）；而真正全新的数据库两张表都没有，不应产生空的误导性备份。\n\nSources: [db.ts](src/db.ts#L416-L437), [db.ts](src/db.ts#L440-L466)\n\n## 核心表族全景\n\n`db.ts` 内联定义了 38 张表，加上三个子模块（渠道可靠性 5 张、Workspace Memory 9 张、Owner Profile 1 张），全库约 53 张表。下表按业务域归组：\n\n| 表族 | 代表表 | 业务域 | 定义位置 |\n| --- | --- | --- | --- |\n| 消息与会话 | `chats`, `messages`, `im_context_bindings`, `workspace_runtime_sessions` | IM 消息底账、上下文绑定、SDK 恢复元数据 | [db.ts](src/db.ts#L540-L572) |\n| 工作区与 Agent | `workspaces`, `registered_groups`, `agent_profiles`, `workspace_agent_profiles` | Agent-first 产品模型的四大实体 | [db.ts](src/db.ts#L736-L995) |\n| 身份认证 | `users`, `user_sessions`, `invite_codes`, `auth_audit_log` | 账户、会话、审计 | [db.ts](src/db.ts#L796-L859) |\n| 渠道接入 | `channel_accounts`, `channel_mounts`, `agent_channel_mounts` | 多账号绑定（凭据经 `secret_ref` 外置） | [db.ts](src/db.ts#L680-L784) |\n| 可靠性投递 | `channel_inbox`, `turn_runs`, `channel_outbox`, `streaming_cards`, `channel_cursors` | 消息持久投递状态机 | [channel-reliability-store.ts](src/channel-reliability-store.ts#L287-L437) |\n| 定时任务 | `scheduled_tasks`, `task_runs`, `task_run_logs` | Cron 调度、租约执行、通知投递 | [db.ts](src/db.ts#L574-L657) |\n| 计费与用量 | `usage_records`, `usage_events`, `billing_plans`, `user_balances` | Token 台账、订阅钱包 | [db.ts](src/db.ts#L1000-L1215) |\n| 长期记忆 | `workspace_memory_*` 九表 | 知识条目、修订史、墓碑、审计 | [memory-store.ts](src/memory-store.ts#L494-L641) |\n\nSources: [db.ts](src/db.ts#L540-L657), [db.ts](src/db.ts#L680-L995)\n\n有两个架构细节值得展开。其一是**新旧实体的共存与更替**：`agents` 表存放的是工作区内的对话/任务/派生 Agent，而 `agent_profiles` 才是顶层运行身份，代码用注释显式警告不要混淆两者；同理，旧的 `sessions` 表被 `workspace_runtime_sessions` 取代，后者只承载 SDK/Provider 的续接元数据，产品意义上的 Session 归 `agents` 管——命名上的克制（"deliberately not named sessions"）防止了概念污染。其二是**子 Schema 的模块化组装**：可靠性、记忆、Owner Profile 三族的建表与访问函数封装在各自模块中，通过 `createXxxSchema(db)` + `bindXxxDatabase(db)` 两步注入共享连接；`closeDatabase()` 关闭连接前先把这三个绑定解为 `null`，杜绝悬垂句柄。创建顺序亦有讲究——Workspace Memory 的建表被安排在"幽灵工作区清理"之前，使清理过程连带清除其记忆数据。\n\nSources: [db.ts](src/db.ts#L889-L918), [db.ts](src/db.ts#L748-L764), [db.ts](src/db.ts#L787-L792), [db.ts](src/db.ts#L14404-L14417)\n\n### 渠道可靠性表族：一条用外键串起的状态机\n\n可靠性五表是全库中外键与部分索引运用最密集的一族，构成 Inbox → Turn → Outbox 的持久投递链路：\n\n```mermaid\nerDiagram\n    channel_inbox ||--o| turn_runs : "inbox_id (UNIQUE)"\n    turn_runs ||--o{ channel_outbox : "turn_run_id CASCADE"\n    turn_runs ||--o{ streaming_cards : "turn_run_id CASCADE"\n    channel_cursors {\n        text provider_pk\n        text cursor\n        int position\n    }\n    channel_inbox {\n        text id_PK\n        text provider\n        text account_id\n        text external_message_id\n        text status\n        int attempt\n    }\n    turn_runs {\n        text id_PK\n        text status\n        int lease_token\n        text lease_expires_at\n    }\n    channel_outbox {\n        text id_PK\n        int ordinal\n        text payload_hash\n        text provider_message_id\n    }\n    streaming_cards {\n        text id_PK\n        int version\n        text snapshot\n    }\n```\n\n去重不变量全部下沉到数据库约束层：`channel_inbox` 以 `UNIQUE(provider, account_id, external_message_id)` 保证同一条外部消息至多入库一次；`turn_runs.inbox_id` 上的部分唯一索引（`WHERE inbox_id IS NOT NULL`）保证一条收件至多产生一次执行；`channel_outbox` 以 `UNIQUE(turn_run_id, ordinal)` 固定输出顺序并以 `payload_hash`（SHA-256）支撑幂等比对。三张队列表共享同一套租约字段模式（`lease_owner/lease_token/lease_expires_at/attempt`），这是崩溃恢复与卡死回收的物理基础。\n\nSources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L287-L437)\n\n### 定时任务表族：乐观锁与部分索引的不变量\n\n`scheduled_tasks` 携带 `revision`（乐观锁版本号）、`updated_at` 与 `deleted_at`（软删除）三件套，配合 `updateTaskWithRevision` 系列函数实现并发安全的任务编辑；`task_runs` 则以 `occurrence_key UNIQUE` 实现调度幂等——同一次触达至多产生一次运行记录。两条部分唯一索引把业务不变量直接焊进索引层：`(task_id, idempotency_key) WHERE idempotency_key IS NOT NULL` 管住手动触发去重，`(task_id) WHERE status IN (\'queued\',\'running\',\'retry_wait\')` 则保证单个任务同时至多一条非终态运行。通知子系统拥有独立的租约字段群（`notification_lease_*`、`notification_generation`），使"任务执行"与"结果通知投递"成为两个可独立重试的阶段。\n\nSources: [db.ts](src/db.ts#L574-L657), [db.ts](src/db.ts#L4618-L4690)\n\n### 用量表族：事件台账与聚合层的分离\n\n用量体系采用"明细台账 + 日汇总"双层结构：`usage_events` 记录每次请求的原始事件（含缓存读写与推理 token 分类），`usage_records` 是面向查询的事实表，`usage_daily_summary` 按 `(user_id, model, date)` 唯一键预聚合。v51 引入的事件台账列展示了加列与加索引的正确次序——先 `ensureColumn` 补齐 `event_id` 等四列，再创建 `(event_id, model) WHERE event_id IS NOT NULL` 的部分唯一索引，否则已有数据的旧库会在建索引时失败。\n\nSources: [db.ts](src/db.ts#L1134-L1234)\n\n### Workspace Memory 表族：CHECK 约束承载领域规则\n\n九张 `workspace_memory_*` 表把大量领域不变量编码为 SQLite CHECK 约束：知识类型限定 `\'fact\'|\'decision\'|\'lesson\'|\'open_loop\'`，状态机限定五态，重要度与置信度限定 [0,1] 区间，修订号必须为正。修订历史以 `(item_id, revision)` 复合主键承载，溯源表 `workspace_memory_provenance` 直接以外键引用这个复合键实现级联；墓碑表与幂等请求表（`PRIMARY KEY(store_id, idempotency_key)`）分别服务遗忘语义与并发写去重。这套设计是该表族所属章节的主题，此处只需指出：它的 Schema 由 `memory-store.ts` 自治管理，经 `db.ts` 在固定时点装配。\n\nSources: [memory-store.ts](src/memory-store.ts#L494-L641)\n\n## 测试如何守护迁移正确性\n\n迁移代码最容易腐烂的方式是"没人敢动也没人验证"。Miniclaw 用三层测试对抗这一点。首先是**版本头钉死测试**：`expect(db.CURRENT_SCHEMA_VERSION).toBe(69)` 故意使用字面量而非常量——注释坦诚地解释了原因：其他迁移测试都与导出常量比较，而 `initDatabase` 写入的正是这个常量，单独看每个测试都是同义反复；唯有这个字面量断言会在版本头移动时失败，强迫升级者在同一次提交里确认配套迁移块和覆盖测试真实落地。\n\nSources: [db-upgrade-safety.test.ts](tests/db-upgrade-safety.test.ts#L166-L177)\n\n其次是**逐版本迁移回归**：仓库中 `schema-v46-migration.test.ts` 直到 `schema-v69-agent-effort.test.ts` 的十余个测试文件，每个对应一次版本跃迁，模式统一——用 better-sqlite3 手工搭建一个旧版本的最小数据库（例如 v69 测试先创建 `router_state` 并插入 `schema_version = \'68\'`，再造出旧结构的 users/sessions/agent_profiles 表和种子数据），然后调用真实的 `initDatabase()`，断言迁移后的结构与数据。这种"真启动"测试比 mock DDL 更可信，因为它验证的是完整管线的行为。\n\nSources: [schema-v69-agent-effort.test.ts](tests/schema-v69-agent-effort.test.ts#L59-L100)\n\n第三层是**升级安全门控本身的端到端验证**：测试先把一个正常初始化的库篡改为 v39 并植入遗留 `group_members` 表与一条引用已删用户的计费审计孤儿，再触发升级——断言备份目录恰好出现一份内容为 v39 快照（含孤儿行）的备份文件；随后把备份目录指向一个普通文件迫使备份失败，断言 `initDatabase()` 抛出"pre-migration backup failed"且源库版本纹丝未动、遗留表原样保留；最后恢复目录重试成功，且再次启动不会产生重复备份。事务原子性则由独立的 `db-transactions.test.ts` 以"外部只读连接探针"的方式验证月度与日用量的跨表原子累加。\n\nSources: [db-upgrade-safety.test.ts](tests/db-upgrade-safety.test.ts#L42-L163), [db-transactions.test.ts](tests/db-transactions.test.ts#L30-L52)\n\n## 运维视角：在线快照与备份工作流\n\n迁移备份解决的是"升级瞬间"的安全，日常备份则由 `make backup` 承担。其核心是 `scripts/sqlite-snapshot.mjs`：以只读模式打开运行中的数据库，调用 better-sqlite3 的 online backup API——它从 SQLite 自身读取事务一致性快照，WAL 中已提交的页自动包含，全程无需停止 Miniclaw 进程；产出文件同样过 `quick_check` 校验并 chmod 600。外围的 Makefile 编排还加入了两道防御：发现运行时数据含硬链接文件即拒绝打包（tar 会将其存为无法恢复的不完整链接），打包前再用 `prepare-backup-tree.mjs` 扫描符号链接与特殊文件，最后生成清单并压缩。这些细节共同保证"拿到的备份一定可恢复"。\n\nSources: [sqlite-snapshot.mjs](scripts/sqlite-snapshot.mjs#L38-L59), [Makefile](Makefile#L251-L287)\n\n## 小结与延伸阅读\n\nMiniclaw 的持久化内核可以浓缩为四个设计决断：**单文件收敛**降低部署与备份复杂度；**版本头 + 三层迁移**让每次启动都成为一次幂等的"向最新版对齐"；**预迁移备份门控**把破坏性升级的风险前置化解；**约束下沉**（UNIQUE/CHECK/FK/部分索引）把业务不变量交给数据库引擎而非应用代码。当你继续深入以下章节时，本页的表族地图会不断被引用：\n\n- 消息如何沿 Inbox/Turn/Outbox 链路可靠送达，见[可靠性状态机：Inbox、Turn、Outbox 与 Streaming Card 的持久投递](17-ke-kao-xing-zhuang-tai-ji-inbox-turn-outbox-yu-streaming-card-de-chi-jiu-tou-di)\n- 定时任务如何消费 `scheduled_tasks`/`task_runs` 两张表，见[定时任务调度器：Cron、固定间隔、一次性任务与重启补跑](21-ding-shi-ren-wu-diao-du-qi-cron-gu-ding-jian-ge-ci-xing-ren-wu-yu-zhong-qi-bu-pao)\n- 记忆九表的领域语义，见[Workspace Memory v2：知识类型、修订历史与并发安全](22-workspace-memory-v2-zhi-shi-lei-xing-xiu-ding-li-shi-yu-bing-an-quan)\n- 备份恢复的完整操作手册，见[备份恢复与日常运维：数据目录布局与 make 工作流](29-bei-fen-hui-fu-yu-ri-chang-yun-wei-shu-ju-mu-lu-bu-ju-yu-make-gong-zuo-liu)';export{s as default};

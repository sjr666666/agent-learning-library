const e='本文是 HappyClaw 维基的**第四步**，面向已完成 `make start` 启动、正准备让系统真正"跑起来"的初学者。读完本页，你将知道：首次访问时如何创建管理员账户、系统如何判断"模型是否已配置"、官方与第三方 Provider 各有哪些接入方式、如何可选地接入 7 种消息渠道，以及配置出错时如何应急找回管理员权限。上一页 [快速开始：环境要求与一键启动](2-kuai-su-kai-shi-huan-jing-yao-qiu-yu-jian-qi-dong) 只介绍了向导的概貌，本页展开全部实现细节。\n\nSources: [README.md](README.md#L239-L243)\n\n## 向导总览：从浏览器到工作台的三步路径\n\n首次访问 <http://localhost:3000> 时，HappyClaw 会依据"系统是否已初始化"和"模型是否已配置"两个状态，把用户导向不同的页面。整个向导的完整流程如下：\n\n```mermaid\nflowchart TD\n    A["打开 http://localhost:3000"] --> B{"GET /api/auth/status<br/>系统中是否存在用户?"}\n    B -- "否" --> C["/setup<br/>创建管理员账户"]\n    C --> D["POST /api/auth/setup<br/>建号 + 建 Home 工作区 + 自动登录"]\n    D --> E["/setup/providers<br/>STEP 2/2 系统接入初始化"]\n    B -- "是" --> F["/login 登录"]\n    F --> G{"管理员且 needsSetup?"}\n    G -- "是" --> E\n    G -- "否" --> H["/chat 工作台<br/>（未配渠道时顶部出现黄色横幅）"]\n    E --> I{"配置 Provider 后<br/>setupStatus.needsSetup 是否变 false?"}\n    I -- "是" --> J["/settings?tab=claude<br/>正式后台"]\n    I -- "否" --> E\n```\n\nSources: [src/web.ts](src/web.ts#L265-L284)、[web/src/App.tsx](web/src/App.tsx#L105-L126)、[web/src/components/auth/AuthGuard.tsx](web/src/components/auth/AuthGuard.tsx#L75-L92)\n\n三个关键判断点值得先建立认知：其一，**`GET /api/auth/status` 只回答"有没有用户"**，返回 `{ initialized: boolean }`，没有任何用户时前端把入口导向 `/setup`；其二，**`setupStatus` 才是"模型是否可用"的权威来源**，登录响应、`/setup` 响应和 `checkAuth` 都会携带它，管理员在 `needsSetup === true` 期间无论访问哪个受保护页面都会被 `AuthGuard` 强制重定向到 `/setup/providers`；其三，渠道接入是**完全可跳过**的步骤，是否配置渠道不影响进入工作台，只影响 IM 消息能否与 Web 对话互通。\n\nSources: [src/routes/auth.ts](src/routes/auth.ts#L133-L137)、[src/routes/auth.ts](src/routes/auth.ts#L106-L129)、[web/src/components/auth/AuthGuard.tsx](web/src/components/auth/AuthGuard.tsx#L89-L92)、[web/src/pages/SetupPage.tsx](web/src/pages/SetupPage.tsx#L25-L77)\n\n## 第一步：创建管理员账户\n\n当系统检测到没有任何用户时，浏览器会进入 `/setup` 页面，展示一个"创建管理员账号"表单。该页面明确提示"先创建管理员账号，完成后进入后台继续配置飞书 Token 与 Claude Key"，提交按钮文案是"创建账号并下一步"。\n\nSources: [web/src/pages/SetupPage.tsx](web/src/pages/SetupPage.tsx#L52-L77)\n\n### 后端校验与唯一性保护\n\n表单提交后调用 `POST /api/auth/setup`。后端做三层防护：**用户名格式**（3-32 位字母、数字或下划线，统一小写存储，与登录/注册路径保持一致，避免 mixed-case 产生登录不到的死账号）、**密码强度**（8-128 位）、以及最关键的一层——**"仅当系统中不存在任何用户"的原子性检查**。这个检查在 SQLite 事务内完成：事务先 `SELECT COUNT(*)` 统计用户数，若已有用户则返回 `already_initialized`（HTTP 403），否则才插入首条用户记录，因此并发请求也无法绕过。\n\nSources: [src/routes/auth.ts](src/routes/auth.ts#L139-L182)、[src/db.ts](src/db.ts#L11953-L11978)、[src/auth.ts](src/auth.ts#L107-L124)\n\n### 建号时自动完成的四件事\n\n创建首个管理员不是简单的插一行用户数据，`/setup` 路由成功后会顺带完成四件事：\n\n| 动作 | 实现 | 说明 |\n| --- | --- | --- |\n| **写入审计日志** | `logAuthEvent`，`source: \'setup_wizard\'` | 记录来源、IP 与 User-Agent，供审计查询 |\n| **创建 Home 工作区** | `ensureUserHomeGroup(userId, \'admin\', username)` | 首个管理员继承遗留的 `web:main`（文件夹 `main`，**宿主机执行模式**）；后续账号使用 `web:home-<userId>` |\n| **绑定默认智能体** | `assignWorkspaceAgentProfile` + `getOrCreateDefaultAgentProfile` | Home 工作区自动归属内置 HappyClaw 的默认 Profile |\n| **自动登录** | `generateSessionToken` + `createUserSession` + `Set-Cookie` | 建号即登录，无需二次输入密码 |\n\nSources: [src/routes/auth.ts](src/routes/auth.ts#L184-L230)、[src/db.ts](src/db.ts#L11087-L11129)\n\n需要强调的是"Home 工作区"的概念：它是**不可删除的系统工作区**，永久归属内置 HappyClaw 智能体，承担平台级对话与主人身份识别。首次唤醒时，内置 HappyClaw 只会在 Home 工作区中面向经过宿主验证的实际 owner 询问称呼（one-shot onboarding lease，重启不重复表演）——这是后续 [智能体-工作区-会话三层模型](6-zhi-neng-ti-gong-zuo-qu-hui-hua-san-ceng-mo-xing) 中的平台级机制，本页只需记住"首个管理员天然拥有它"。\n\nSources: [src/happyclaw-bootstrap.ts](src/happyclaw-bootstrap.ts#L8-L22)、[README.md](README.md#L87-L89)\n\n### 忘记密码怎么办：reset:admin 应急通道\n\n如果管理员密码遗失，不需要重装系统。项目提供了命令行重置脚本，逻辑与 `/setup` 一致：用户名同样小写归一化，密码同样要求至少 8 位。若用户已存在则原地重置为管理员（`role: admin, status: active`，清除 `must_change_password` 标记并删除该用户全部会话，强制重新登录）；若不存在则直接以管理员身份创建。\n\n```bash\nnpm run reset:admin -- <username> <new_password>\n```\n\nSources: [src/reset-admin.ts](src/reset-admin.ts#L10-L56)\n\n## 第二步：配置模型提供商\n\n创建管理员后自动进入 `/setup/providers`（页面标注"STEP 2 / 2"）。这一页是**系统全局默认配置**：保存的 Provider 会成为所有继承型 Agent 的默认模型来源。页面上同时提供"飞书配置（可选）"和"Claude Code 配置（二选一）"两个区块。\n\nSources: [web/src/pages/SetupProvidersPage.tsx](web/src/pages/SetupProvidersPage.tsx#L259-L307)\n\n### setupStatus：系统如何判断"模型已配置"\n\n`buildSetupStatus()` 是向导完成度的唯一裁决者。它**遍历所有启用的 Provider**（而不是只看第一个——V3→V4 迁移可能产生排在真实 Provider 前面的空配置，只看第一个会误判），任一 Provider 满足以下条件即视为 `claudeConfigured`：\n\n| 判断维度 | 满足条件 |\n| --- | --- |\n| **官方凭据** | `claudeCodeOauthToken` 非空 或 `claudeOAuthCredentials` 存在 或 `anthropicApiKey` 非空 |\n| **第三方凭据** | `anthropicBaseUrl` 与 `anthropicAuthToken` 同时非空 |\n\n`needsSetup = !claudeConfigured`。登录响应、`/setup` 响应都会携带这个状态；前端在保存 Provider 后调用 `checkAuth()` 刷新它，只有确认 `needsSetup` 变为 `false` 才放行到 `/settings?tab=claude`，否则提示"配置已保存但验证未通过"。\n\nSources: [src/routes/auth.ts](src/routes/auth.ts#L106-L129)、[src/routes/auth.ts](src/routes/auth.ts#L339-L346)、[web/src/pages/SetupProvidersPage.tsx](web/src/pages/SetupProvidersPage.tsx#L244-L251)\n\n### 官方渠道的三种接入方式\n\n官方渠道对应 Anthropic 官方账号，向导提供三种方式（前端以 Tab 切换）：\n\n| 方式 | 操作路径 | 后端行为 |\n| --- | --- | --- |\n| **OAuth 一键登录（推荐）** | 点击"一键登录 Claude"→ 打开 claude.ai 授权页 → 粘贴授权码 → 确认 | PKCE 流程：`POST /api/config/claude/oauth/start` 生成 `state`/`code_verifier`/`code_challenge` 并返回授权 URL；`POST /api/config/claude/oauth/callback` 用授权码换 token，创建或更新官方 Provider 并写入 `.credentials.json` |\n| **Setup Token / .credentials.json** | 在终端 `claude login` 后执行 `cat ~/.claude/.credentials.json` 或 `claude setup-token`，粘贴输出 | 若粘贴内容是 JSON 则解析 `claudeAiOauth` 字段写入 `claudeOAuthCredentials`；否则作为 `claudeCodeOauthToken` 创建 Provider |\n| **API Key** | 直接粘贴 Anthropic API Key | 创建 `type: \'official\'` 的 Provider，仅填 `anthropicApiKey` |\n\nOAuth 流程的授权码在服务端内存中暂存（`oauthFlows` Map，带 TTL），换取 token 时校验 state 与 PKCE challenge；换得的 token 若含 refresh_token 则保存为 `claudeOAuthCredentials`（含过期时间与 scopes），并立即 `updateAllSessionCredentials` 同步到所有会话的 `.credentials.json`。\n\nSources: [src/routes/config.ts](src/routes/config.ts#L1454-L1493)、[src/routes/config.ts](src/routes/config.ts#L1495-L1623)、[web/src/pages/SetupProvidersPage.tsx](web/src/pages/SetupProvidersPage.tsx#L180-L228)\n\n### 第三方兼容端点\n\n第三方模式只需填写三件套：**API Endpoint（`anthropicBaseUrl`）**、**Auth Token（`anthropicAuthToken`）**、**模型名称（`anthropicModel`）**，其中 Base URL 与 Auth Token 为必填（Zod schema 的 `superRefine` 强制）。可选的高级设置允许追加自定义环境变量行，但系统保留 `ANTHROPIC_BASE_URL`、`ANTHROPIC_AUTH_TOKEN`、`ANTHROPIC_API_KEY`、`CLAUDE_CODE_OAUTH_TOKEN`、`ANTHROPIC_MODEL` 五个字段，不允许用户覆盖；自定义 Key 必须符合 `[A-Z_][A-Z0-9_]*` 且不重复。\n\nSources: [src/schemas.ts](src/schemas.ts#L1157-L1183)、[web/src/pages/SetupProvidersPage.tsx](web/src/pages/SetupProvidersPage.tsx#L17-L46)\n\n第三方 Provider 创建后，运行时通过 `providerToConfig()` 把字段投影为 Claude Code 兼容配置，再由容器/宿主机 Runner 预填主模型映射、上下文窗口、自动压缩、超时等环境变量。Base URL 会经过 `normalizeBaseUrl` 规范化（必须是 HTTP(S)，去除尾斜杠差异），密钥字段经过 `normalizeSecret` 去首尾空白并拒绝换行。\n\nSources: [src/runtime-config.ts](src/runtime-config.ts#L1674-L1686)、[src/runtime-config.ts](src/runtime-config.ts#L537-L552)、[README.md](README.md#L181-L190)\n\n### Provider 管理 API 一览\n\n向导保存后，后续配置都在 **设置 → 模型与提供商** 完成。所有 Provider 路由都要求登录且 `systemConfigMiddleware`（系统配置权限），密钥字段在响应中一律**掩码**（`toPublicProvider` 只返回 `hasXxx` 布尔值与 `xxxMasked` 掩码串，绝不返回明文）：\n\n| 方法 / 路径 | 作用 |\n| --- | --- |\n| `GET /api/config/claude/providers` | 列出全部 Provider，附带健康状态与负载均衡配置 |\n| `POST /api/config/claude/providers` | 创建 Provider（向导使用） |\n| `PATCH /api/config/claude/providers/:id` | 更新非密钥字段（名称、Base URL、模型、自定义环境变量、权重） |\n| `PUT /api/config/claude/providers/:id/secrets` | 单独更新/清除密钥（支持 `clearXxx` 显式清除） |\n| `PUT /api/config/claude/default` | 设置所有继承型 Agent 的默认 Provider |\n| `DELETE /api/config/claude/providers/:id` | 删除 Provider（仍被智能体引用时拒绝） |\n| `PUT /api/config/claude/balancing` | 更新负载均衡策略与健康阈值 |\n\nSources: [src/routes/config.ts](src/routes/config.ts#L845-L883)、[src/routes/config.ts](src/routes/config.ts#L961-L992)、[src/routes/config.ts](src/routes/config.ts#L1134-L1237)、[src/runtime-config.ts](src/runtime-config.ts#L1689-L1715)\n\n### 负载均衡与健康状态（向导后的基础认知）\n\n配置多个 Provider 后，新会话由 `ProviderPool` 按策略选择，支持三种策略：**round-robin**（健康成员间轮询）、**weighted-round-robin**（按 1-100 权重分配）、**failover**（优先第一个，故障才切换）。健康状态纯内存管理：连续失败达到阈值（默认 3 次）标记为 unhealthy 并从候选池摘除，经过恢复窗口（默认 5 分钟）重新探测；已开始的会话保持 Provider 一致（会话粘性），避免上下文在模型间漂移。详细算法属于 [Provider 池与负载均衡策略](20-provider-chi-yu-fu-zai-jun-heng-ce-lue) 的范畴。\n\nSources: [src/provider-pool.ts](src/provider-pool.ts#L47-L77)、[src/provider-pool.ts](src/provider-pool.ts#L87-L158)、[src/provider-pool.ts](src/provider-pool.ts#L162-L192)\n\n## 第三步：接入消息渠道（可选）\n\nProvider 配置通过后，正式入口 `GET /setup/channels` 打开"接入消息渠道（可选）"页面。它**刻意复用设置页的 `ChannelAccountsManager` 组件**，而不是维护第二套遗留表单——官方注释明确说明这是为了避免 QR 与配对协议在 onboarding 之后与产品分裂。页面底部提供"稍后设置"与"完成并进入 HappyClaw"两个按钮，渠道接入**可以随时跳过、随时回来补**。\n\nSources: [web/src/pages/SetupChannelsPage.tsx](web/src/pages/SetupChannelsPage.tsx#L9-L63)\n\n### 七种渠道与三种认证模式\n\n每个渠道账号的接入协议各不相同，但统一收敛为三种认证模式：\n\n| 渠道 | 认证模式 | 必填凭证 | 连接测试 | 配对码 |\n| --- | --- | --- | --- | --- |\n| **飞书** | `credentials` | App ID + App Secret | ✅ | ❌（首次私聊自动识别 Owner） |\n| **Telegram** | `bot_token` | Bot Token（可选代理 URL） | ✅ | ✅ `/pair <配对码>` |\n| **QQ** | `credentials` | 机器人 AppID + AppSecret | ✅ | ✅ |\n| **钉钉** | `credentials` | Client ID（AppKey）+ Client Secret | ✅ | ✅ |\n| **Discord** | `bot_token` | Bot Token | ✅ | ✅ |\n| **微信** | `qr_session` | 无（扫码授权） | ✅ | ✅ |\n| **WhatsApp** | `qr_session` | 无（扫码关联设备） | ❌ | ✅ |\n\nSources: [web/src/utils/channel-accounts.ts](web/src/utils/channel-accounts.ts#L45-L263)\n\n每个渠道都内置了**分步接入引导**（`setupGuide`），例如飞书指引"创建企业自建应用 → 启用机器人 → 事件订阅选择长连接并添加 `im.message.receive_v1` → 发布应用"，并附带开放平台直达链接；Telegram 指引用 `@BotFather` 执行 `/newbot`；微信与 WhatsApp 则是"创建账号 → 生成二维码 → 手机扫码 → 配对码授权具体会话"。\n\nSources: [web/src/utils/channel-accounts.ts](web/src/utils/channel-accounts.ts#L66-L82)\n\n### 账号创建与凭据安全\n\n创建账号走 `POST /api/channel-accounts`，后端做四道检查：**目标校验**（默认工作区必须存在且属于当前用户）、**凭证完整性**（按渠道要求的必填字段逐个核对）、**QQ 身份冲突**（同一 AppID 不能创建第二个账号，返回 409）、**格式校验**（`ChannelAccountCreateSchema`）。创建流程分两阶段：先把凭据加密落盘（`saveChannelAccountSecret`），再写账号行；若写行失败会回滚删除已加密的凭据文件。\n\nSources: [src/routes/channel-accounts.ts](src/routes/channel-accounts.ts#L515-L599)、[src/schemas.ts](src/schemas.ts#L26-L41)\n\n凭据使用 **AES-256-GCM 加密**：加密密钥首次使用时生成并原子发布到 `data/config/claude-provider.key`（权限 0600），每个账号的凭据以 `iv + authTag + ciphertext` 结构独立落盘到 `data/config/channel-accounts/`，API 层只暴露"是否已配置"与掩码，不返回明文。完整的加密方案见 [密钥加密与凭据管理](25-mi-yao-jia-mi-yu-ping-ju-guan-li)。\n\nSources: [src/channel-account-secrets.ts](src/channel-account-secrets.ts#L21-L52)、[src/channel-account-secrets.ts](src/channel-account-secrets.ts#L76-L98)\n\n### 连接、测试与配对\n\n账号创建后根据认证模式分流：**凭证类渠道**（飞书/Telegram/QQ/钉钉/Discord）创建时若启用则立即尝试连接，失败会把 `auth_status` 标为 `error`；**QR 类渠道**（微信/WhatsApp）初始为 `draft`，需要进入 `POST /:id/onboarding` 发起扫码会话，服务端通过 WebSocket `channel_account_status` 事件把二维码状态实时推给前端。配对码类渠道在连接成功后通过 `POST /:id/pairing-code` 生成一次性配对码，用户在 Bot 私聊或目标群聊中发送 `/pair <配对码>` 完成会话授权。WebSocket 状态事件、账号生命周期与连接池细节见 [IM 渠道架构与多账号连接池](13-im-qu-dao-jia-gou-yu-duo-zhang-hao-lian-jie-chi)。\n\nSources: [src/routes/channel-accounts.ts](src/routes/channel-accounts.ts#L753-L765)、[src/routes/channel-accounts.ts](src/routes/channel-accounts.ts#L1024-L1035)、[web/src/components/settings/ChannelAccountsManager.tsx](web/src/components/settings/ChannelAccountsManager.tsx#L88-L109)\n\n### 跳过后的入口：工作台横幅提醒\n\n如果选择"稍后设置"，进入 `/chat` 后，只要当前 Home 容器下没有任何 IM 渠道处于启用状态，工作台顶部就会显示黄色横幅："未配置消息渠道（飞书 / Telegram / Discord / QQ / 微信 / 钉钉 / WhatsApp），消息无法与 HappyClaw 的直接对话互通"，并提供"去配置"按钮直达 `/setup/channels`。横幅可手动关闭（记忆在 localStorage），渠道配置完成后不再出现。\n\nSources: [web/src/components/chat/ChatView.tsx](web/src/components/chat/ChatView.tsx#L1113-L1140)\n\n## 后续用户与注册策略\n\n首个管理员就绪后，其他成员通过 `/register` 页面注册。`GET /api/auth/register/status` 会返回注册开关状态；**在管理员创建完成之前，该接口强制返回 `allowRegistration: false, requireInviteCode: true`**，把所有人挡在 `/setup` 之外。初始化完成后，管理员可在 **设置 → 注册策略** 选择三种模式：开放注册、邀请码注册（注册时必填 `invite_code`）或关闭注册；注册接口还有基于 IP 的速率限制（沿用登录的 `maxLoginAttempts` / `loginLockoutMinutes` 设置）。用户名同样小写归一化，避免 `admin` / `Admin` 被当成三个账号。\n\nSources: [src/routes/auth.ts](src/routes/auth.ts#L358-L372)、[src/routes/auth.ts](src/routes/auth.ts#L374-L458)、[web/src/components/settings/RegistrationSection.tsx](web/src/components/settings/RegistrationSection.tsx#L36-L45)\n\n## 下一步阅读\n\n你已经完成了 HappyClaw 从"空系统"到"可对话工作台"的全部初始化步骤。按照目录结构，建议的阅读顺序是：\n\n- [智能体-工作区-会话三层模型](6-zhi-neng-ti-gong-zuo-qu-hui-hua-san-ceng-mo-xing)——理解创建完的智能体、工作区与会话之间如何组织\n- [部署与运维：环境变量、Docker 镜像与备份恢复](5-bu-shu-yu-yun-wei-huan-jing-bian-liang-docker-jing-xiang-yu-bei-fen-hui-fu)——公网部署、反向代理、环境变量全集与 `make backup` / `make restore`\n- [Provider 池与负载均衡策略](20-provider-chi-yu-fu-zai-jun-heng-ce-lue)——多 Provider 的选路算法、健康探测与会话粘性实现\n- [IM 渠道架构与多账号连接池](13-im-qu-dao-jia-gou-yu-duo-zhang-hao-lian-jie-chi)——渠道账号生命周期、连接池与状态事件体系\n- 如果还没有阅读起点页，建议先回到 [项目概览：自托管多用户 Claude Code 智能体工作台](1-xiang-mu-gai-lan-zi-tuo-guan-duo-yong-hu-claude-code-zhi-neng-ti-gong-zuo-tai) 建立整体认知';export{e as default};

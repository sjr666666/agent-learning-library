const n='Miniclaw 的配置不是一个单一的配置文件，而是一个**分层解析系统**：同一个配置项可能同时存在于代码默认值、`.env` 环境变量和 Web 后台持久化文件中。理解"哪一层生效、什么时候生效"是排障和部署的关键。本文面向初学者，拆解这套优先级机制的完整链路——从进程启动的第一行代码，到最终注入 Agent 容器的每一个环境变量。\n\n## 全局图景：四条配置流水线\n\nMiniclaw 的配置读取发生在四个不同时机，每个时机对应不同的优先级规则。先通过一张架构图建立整体认知：\n\n```mermaid\nflowchart TD\n    A["进程启动<br/>src/index.ts"] --> B["第〇层：load-env.ts<br/>process.loadEnvFile() 把 .env 注入 process.env"]\n    B --> C["第一层：config.ts 模块求值<br/>环境变量 → 代码默认值（只算一次）"]\n    C --> D["第二层：runtime-config.ts<br/>Web 后台写入的 JSON 文件"]\n\n    D --> E{"system-settings.json<br/>存在？"}\n    E -->|"是"| F["文件值生效<br/>（环境变量被整体忽略）"]\n    E -->|"否"| G{"相关环境变量<br/>有值？"}\n    G -->|"是"| H["环境变量作为回退"]\n    G -->|"否"| I["DEFAULT_SYSTEM_SETTINGS 代码默认值"]\n\n    F & H & I --> J["第三层：工作区覆盖<br/>mergeClaudeEnvConfig + 安全过滤"]\n    J --> K["注入容器环境变量"]\n```\n\n注意一个反直觉的设计：**系统设置的优先级是"文件 > 环境变量"，而会话密钥却是"环境变量 > 文件"**。后文会解释这个差异背后的原因。\n\nSources: [index.ts](src/index.ts#L1-L19)、[runtime-config.ts](src/runtime-config.ts#L4208-L4245)\n\n## 第〇层：`.env` 的加载时机决定一切\n\n`src/load-env.ts` 是整个应用的**第一个 import**，它调用 Node.js 内置的 `process.loadEnvFile()` 把项目根目录下的 `.env` 文件读进 `process.env`。这一步必须在所有其他模块之前完成，因为 `config.ts` 和 `web.ts` 在被 import 时就会立即读取环境变量——如果 `.env` 加载晚了，那些模块拿到的将是空值。\n\n```mermaid\nsequenceDiagram\n    participant Node as Node.js 进程\n    participant LE as load-env.ts\n    participant CF as config.ts\n    participant RT as runtime-config.ts\n    Node->>LE: import \'./load-env.js\'（第一个 import）\n    LE->>LE: process.loadEnvFile() 读取 .env\n    Node->>CF: import \'./config.js\'\n    CF->>CF: 此刻求值：读 process.env 并固化导出常量\n    Node->>RT: 后续任意时刻调用 getSystemSettings()\n    RT->>RT: 动态读取：每次调用都检查文件是否更新\n```\n\n两个对初学者重要的细节：其一，缺少 `.env` 文件是完全正常的情况（注释明确说明"所有 env 均有默认值"），加载失败会被静默忽略；其二，该模块还顺带处理了代理——undici 的全局 fetch 默认**不读** `HTTP_PROXY/HTTPS_PROXY`，这里在最早时机用 `EnvHttpProxyAgent` 补上，避免 server-side fetch（如 OAuth 令牌交换）绕过代理直连。\n\nSources: [load-env.ts](src/load-env.ts#L1-L12)、[load-env.ts](src/load-env.ts#L14-L44)\n\n## 第一层：启动常量——环境变量覆盖代码默认值\n\n`src/config.ts` 定义了一组**进程生命周期内不变的导出常量**。它们在模块被 import 时求值一次，之后不再变化。典型的模式有两种：\n\n**模式一：新旧变量名兼容**。`envWithLegacyAlias(current, legacy, fallback)` 先查新名，再查旧名，最后回退默认值：\n\n| 配置项 | 新变量名 | 旧变量名（兼容） | 默认值 |\n|---|---|---|---|\n| `ASSISTANT_NAME` | `MINICLAW_ASSISTANT_NAME` | `ASSISTANT_NAME` | `\'Miniclaw\'` |\n| `CONTAINER_IMAGE` | `MINICLAW_CONTAINER_IMAGE` | `CONTAINER_IMAGE` | `\'helsome/miniclaw-agent:latest\'` |\n\n**模式二：解析加校验**。以文件上传上限为例——先 `parseInt`，若结果非有限正数则回退默认 50MB。这意味着设置 `MAX_FILE_SIZE_MB=abc` 不会崩溃，只会静默回到默认值：\n\n| 配置项 | 环境变量 | 默认值 | 说明 |\n|---|---|---|---|\n| `WEB_PORT` | `WEB_PORT` | `3000` | Web 服务监听端口 |\n| `MAX_FILE_SIZE_MB` | `MAX_FILE_SIZE_MB` | `50`（非法值回退） | Web 上传与 IM 收文件共用上限 |\n| `TIMEZONE` | `TZ` | 系统 时区 → `\'Asia/Shanghai\'` | 定时任务 cron 表达式使用 |\n| `TRUST_PROXY` | `TRUST_PROXY`（需恰好为 `\'true\'`） | `false` | 反向代理场景必开 |\n| `CORS_ALLOWED_ORIGINS` | 同名，逗号分隔白名单或 `\'*\'` | 空（拒绝跨站） | 自托管公网域名放行 |\n\n其中 `CORS_ALLOWED_ORIGINS` 直接在 `web.ts` 中读取，用于防御跨站请求；如需放行跨站来源，在 `.env` 里配置逗号分隔的域名白名单即可。\n\nSources: [config.ts](src/config.ts#L7-L17)、[config.ts](src/config.ts#L42-L61)、[config.ts](src/config.ts#L104-L106)、[web.ts](src/web.ts#L203-L245)\n\n## 第二层：Web 设置——文件优先于环境变量\n\n这是整个体系中最精巧的部分。管理员在 Web 后台修改的系统设置（容器超时、并发上限、计费开关等）由 `runtime-config.ts` 管理，持久化为 `data/config/system-settings.json`。它的读取函数 `getSystemSettings()` 实现了严格的两段式回退：\n\n1. **文件存在 → 文件值全面生效**。此时环境变量被*整体忽略*，而不是逐字段合并；\n2. **文件不存在 → 构造一份纯环境变量的回退配置**，缺失字段再落到 `DEFAULT_SYSTEM_SETTINGS` 硬编码默认值。\n\n| 配置族 | 文件路径（均在 `DATA_DIR/config/` 下） | 环境变量回退示例 |\n|---|---|---|\n| 系统设置 SystemSettings | `system-settings.json` | `CONTAINER_TIMEOUT`、`MAX_CONCURRENT_CONTAINERS`、`BILLING_ENABLED` |\n| Claude Provider | `claude-provider.json` | `ANTHROPIC_BASE_URL`、`ANTHROPIC_API_KEY`、`ANTHROPIC_MODEL` |\n| 飞书凭据 | `feishu-provider.json`（AES-256-GCM 加密） | `FEISHU_APP_ID`、`FEISHU_APP_SECRET` |\n| Telegram 凭据 | `telegram-provider.json`（加密） | `TELEGRAM_BOT_TOKEN`、`TELEGRAM_PROXY_URL` |\n| 注册策略 | `registration.json`（无 env 层） | — |\n| 外观品牌 | `appearance.json`（无直接 env 层） | — |\n\n为什么文件要压过环境变量？因为环境变量在这里的角色是**首次部署的引导配置**（bootstrap）：运维者可以不打开网页就通过 Docker 环境变量预填渠道凭据，但一旦管理员在 Web 后台保存了正式配置，文件就成为唯一权威——否则重启时环境变量会悄悄覆盖掉界面上的修改，造成"我明明改了却变回去"的经典排障噩梦。\n\nSources: [runtime-config.ts](src/runtime-config.ts#L4185-L4245)、[runtime-config.ts](src/runtime-config.ts#L1807-L1830)、[runtime-config.ts](src/runtime-config.ts#L1860-L1880)\n\n### 类型归一化：环境变量的字符串世界 vs 代码的类型世界\n\n环境变量天生是字符串，而 `SystemSettings` 接口要求数字和布尔值。`normalizeSystemSettings(raw, source)` 统一做归一化，且**只有 `env` 来源允许宽松解析**：\n\n- 数字字段：仅当 `source === \'env\'` 时才接受字符串形式（如 `"300000"`），并用 `Math.min/max` 钳制到合法区间；\n- 布尔字段：仅接受真正的 boolean 或字符串 `\'true\'`/`\'false\'`，其他一律判非法并回退默认值；\n- 所有被判非法的字段都会带上来源标记记入日志（`Normalized invalid system settings`），方便定位是哪个环节传了脏数据。\n\n归一化同时是一道安全防线。登录锁定时长被硬性钳制在最大 1440 分钟——注释解释了原因：认证模块按固定 24 小时 TTL 回收登录尝试记录，如果允许更大的锁定窗口，清理定时器会在锁定中途删掉记录、重置计数器，攻击者就能通过暂停约一天来恢复暴力破解。因此**每一条读取路径都钳制**，不给环境变量或手改文件绕过上限的机会。\n\nSources: [runtime-config.ts](src/runtime-config.ts#L3984-L4029)、[runtime-config.ts](src/runtime-config.ts#L4164-L4171)、[runtime-config.ts](src/runtime-config.ts#L3956-L3962)\n\n### 缓存与原子写：热路径的性能保障\n\n`getSystemSettings()` 被调用在高频路径上（容器 stdout 数据处理器、队列容量检查），因此实现了一个基于文件 `mtimeMs` 的内存缓存：命中缓存时只需一次 `statSync` 对比修改时间，文件未变就直接返回缓存对象。而 `saveSystemSettings()` 采用"写临时文件 + rename 原子替换"的模式，写入后立即更新缓存，避免读到半写状态。含敏感数据的配置（IM 凭据密文）则统一走 `writeSecretFile()`，临时文件以 `0o600` 权限创建，防止同主机其他本地账号读到密文。\n\nSources: [runtime-config.ts](src/runtime-config.ts#L4173-L4183)、[runtime-config.ts](src/runtime-config.ts#L4253-L4273)、[runtime-config.ts](src/runtime-config.ts#L22-L57)\n\n### 一个间接的联动：环境变量如何成为 Web 界面的默认值\n\n外观配置（应用名、AI 名字、头像）本身没有专属的环境变量层，但它的默认值取自 `ASSISTANT_NAME`——也就是 `MINICLAW_ASSISTANT_NAME` 环境变量。这形成了一条隐蔽的传导链：你在 `.env` 设置 `MINICLAW_ASSISTANT_NAME=小爪`，Web 界面的默认品牌名就变成"小爪"；一旦管理员在后台保存了 `appearance.json`，文件值便接管一切。飞书和 Telegram 的读取函数更进一步，显式返回 `source: \'runtime\' | \'env\' | \'none\'` 标记，前端据此向用户展示"当前配置来自环境变量还是后台保存"。\n\nSources: [runtime-config.ts](src/runtime-config.ts#L3302-L3309)、[runtime-config.ts](src/runtime-config.ts#L1860-L1880)、[runtime-config.ts](src/runtime-config.ts#L1911-L1923)\n\n## 第三层：工作区级容器环境变量——最细粒度的覆盖\n\n前三层解决的是"全局用什么模型、什么凭据"。第三层回答的是"某个 Workspace 能不能用自己的端点和密钥"。每个工作区可以有一份 `ContainerEnvConfig`（存放在按 folder 隔离的独立文件中），合并规则由 `mergeClaudeEnvConfig` 定义：**工作区字段非空则胜出，否则继承全局 Provider 配置**。一个安全细节：一旦工作区设置了自定义 `anthropicBaseUrl`（第三方中转），OAuth 凭据会被强制剥离——否则 SDK 会尝试走 OAuth 认证路径、跳过标准 Bearer 头，在 Kimi 等非 Anthropic 端点上产生 404。\n\n```mermaid\nflowchart LR\n    A["全局 Provider 配置<br/>claude-provider.json"] --> M["mergeClaudeEnvConfig<br/>工作区非空字段胜出"]\n    B["工作区覆盖<br/>ContainerEnvConfig"] --> M\n    M --> BL["buildContainerEnvLines"]\n    P["Agent Profile 自定义 env"] --> BL\n    W["工作区 customEnv 键值对"] --> BL\n    BL --> F{"三道安全过滤"}\n    F -->|"键名合法?"| F1["ENV_KEY_RE 正则"]\n    F -->|"不在危险名单?"| F2["DANGEROUS_ENV_VARS"]\n    F -->|"不是保留键?"| F3["RESERVED / 托管键"]\n    F1 & F2 & F3 --> OUT["最终容器环境变量"]\n```\n\n`buildContainerEnvLines` 是所有环境变量进入容器前的最后一道闸门，过滤规则值得初学者认真理解：\n\n| 过滤器 | 作用 | 示例被拦截的键 |\n|---|---|---|\n| `ENV_KEY_RE` | 键名必须是合法环境变量格式 | 含连字符或特殊字符的自定义键 |\n| `DANGEROUS_ENV_VARS` | 阻断代码执行/路径劫持/凭据泄露向量 | `LD_PRELOAD`、`NODE_OPTIONS`、`PATH`、`BASH_ENV`、`HOME` |\n| `RESERVED_CLAUDE_ENV_KEYS` | 端点与模型由平台托管，工作区不可改 | `ANTHROPIC_BASE_URL`、`MINICLAW_FALLBACK_MODEL` |\n| `THIRD_PARTY_CONFIGURABLE_ENV_KEYS` | 第三方模式下这些键只能由 Provider 高级设置改，工作区禁止覆盖 | `CLAUDE_CODE_EFFORT_LEVEL`、`API_TIMEOUT_MS` |\n\n此外还有一个**不可伪造的权威标记**：`MINICLAW_CLAUDE_ENDPOINT_KIND` 由是否存在 `anthropicBaseUrl` 推导得出（custom 或 official），即使工作区试图在 customEnv 里塞入相反值也会被忽略——契约测试明确验证了这一点。第三方 Provider 还会获得一组运行时默认值（如 `CLAUDE_CODE_EFFORT_LEVEL=max`、`API_TIMEOUT_MS=3000000`），但 Provider 高级设置中的同名值可以覆盖它们。\n\nSources: [runtime-config.ts](src/runtime-config.ts#L2939-L2965)、[runtime-config.ts](src/runtime-config.ts#L3032-L3079)、[runtime-config.ts](src/runtime-config.ts#L146-L190)、[tests/runtime-config-env.test.ts](tests/runtime-config-env.test.ts#L103-L123)、[tests/runtime-config-env.test.ts](tests/runtime-config-env.test.ts#L174-L189)\n\n## 特例剖析：会话密钥为何环境变量最高\n\n`WEB_SESSION_SECRET` 的三级优先序与前文系统设置正好相反：① 环境变量最高（注释原话："allows container/operator override"，让容器编排者显式掌控）；② 其次是 `data/config/session-secret.key` 持久化文件（保证重启后会话不失效）；③ 都没有则现场随机生成 32 字节十六进制并尝试落盘。这里的逻辑在于：会话密钥是纯运维资产而非业务配置，没有对应的 Web 管理界面，环境变量是运维者表达意志的唯一通道，理应拥有最高话语权。\n\nSources: [config.ts](src/config.ts#L62-L102)\n\n## 排障速查表：为什么我的环境变量"不生效"\n\n| 症状 | 根因 | 解决方向 |\n|---|---|---|\n| 设了 `CONTAINER_TIMEOUT` 但没效果 | `system-settings.json` 已存在，文件整体压过环境变量 | 在 Web 后台修改，或删除该文件让 env 引导生效 |\n| 改了 `.env` 重启后行为不变 | 启动常量（端口、镜像名等）仅在模块求值时读取一次 | 属预期行为；确认改动的是动态读取类配置还是启动常量 |\n| `MAX_FILE_SIZE_MB=abc` 未报错也未生效 | 解析失败静默回退默认 50 | 使用合法正整数 |\n| 工作区 customEnv 里的 `PATH`/`NODE_OPTIONS` 消失了 | 被 `DANGEROUS_ENV_VARS` 安全过滤器拦截 | 属安全设计，换用非危险键名 |\n| 第三方模式下工作区改 `CLAUDE_CODE_EFFORT_LEVEL` 无效 | 该键属 Provider 托管，仅 Provider 高级设置可改 | 到 Provider 设置页调整 |\n| 登录锁定时间设得比 24h 还长却不生效 | 被钳制在 1440 分钟上限 | 上限即安全约束，不可绕过 |\n\n## 关键要点回顾\n\n把全文浓缩成一句话：**启动常量看环境变量，运行时设置看文件，环境变量只是文件的引导垫脚石，而工作区覆盖永远过不了安全过滤器这道闸**。三条铁律——`.env` 必须最早加载（`load-env.ts` 的首要地位）、文件写入必须原子（tmp + rename）、数值必须归一化钳制（env 来源也不例外）——贯穿了整个配置系统。\n\n想继续深入，建议按以下路径阅读：理解这些配置在双执行模式中如何消费（容器超时、并发数），请看 [Host 与 Container 双执行模式：容量、超时与暖 Runner](11-host-yu-container-shuang-zhi-xing-mo-shi-rong-liang-chao-shi-yu-nuan-runner)；了解挂载白名单这类文件型安全配置，请看 [容器隔离与挂载安全：非 root 运行、白名单与密钥加密边界](20-rong-qi-ge-chi-yu-gua-zai-an-quan-fei-root-yun-xing-bai-ming-dan-yu-mi-yao-jia-mi-bian-jie)；掌握配置落地后的日常维护手段，请看 [备份恢复与日常运维：数据目录布局与 make 工作流](29-bei-fen-hui-fu-yu-ri-chang-yun-wei-shu-ju-mu-lu-bu-ju-yu-make-gong-zuo-liu)。';export{n as default};

const n='HappyClaw 采用**宿主主进程 + 独立 Agent Runner 进程**的双进程执行模型。Agent Runner 承载 Claude Code SDK 的全部执行逻辑（模型调用、工具执行、流式输出），宿主进程负责编排、持久化与 IM 分发。两者之间的通信由一套**基于文件系统的 IPC 协议**承担：输入走"stdin 启动 + 文件注入"，输出走"stdout 帧 + 文件回执"。本文从目录命名空间、输入/输出协议、可靠性机制三个层次拆解这套协议。\n\n## 双进程模型与 IPC 的定位\n\n宿主进程（`src/index.ts`、`src/container-runner.ts`）与 Runner（`container/agent-runner/src/index.ts`）在物理上隔离：容器模式下 Runner 运行在 Docker 容器内，宿主机模式下则是独立 Node 子进程。`container-runner.ts` 的注释给出了核心定位："Spawns agent execution in Docker container and handles IPC"——Runner 的启动、挂载与 IPC 目录创建均由宿主完成。\n\nRunner 内部使用 `@anthropic-ai/claude-agent-sdk` 0.3.220 驱动 Claude Code，其工具集通过进程内 SDK MCP Server 暴露（`mcp__happyclaw__*`）。这意味着**渠道感知能力（send_message、send_image、schedule_task 等）不是模型原生能力，而是经由 IPC 文件回到宿主进程后由宿主代为执行的**——IPC 是 Runner 与宿主之间唯一的能力通道。\n\n```mermaid\nflowchart LR\n    subgraph Host["宿主进程 (src/index.ts)"]\n        Q[GroupQueue 会话队列]\n        W[IpcWatcherManager<br/>fs.watch + 5s 轮询]\n        R[routeHostIpcOutput<br/>turn 投递协调]\n    end\n\n    subgraph Runner["Agent Runner (container/agent-runner)"]\n        SDK[Claude Agent SDK]\n        MCP[MCP Server<br/>send_message / send_image / schedule_task]\n        LOOP[主循环<br/>query → waitForIpcMessage → query]\n    end\n\n    subgraph FS["共享文件系统（IPC 命名空间）"]\n        INPUT["ipc/{group}/input/*.json<br/>+ _close/_drain/_interrupt"]\n        OUT["ipc/{group}/messages/<br/>ipc/{group}/tasks/"]\n        RESULT["ipc/{group}/message-results/<br/>send_message_result_{id}.json"]\n    end\n\n    Q -- "原子写 JSON + receipt" --> INPUT\n    LOOP -- "fs.watch 读取" --> INPUT\n    MCP -- "写请求文件" --> OUT\n    W -- "事件驱动扫描" --> OUT\n    W -- "写回执（原子写）" --> RESULT\n    MCP -- "500ms 轮询回执" --> RESULT\n    SDK -- "流式事件" --> LOOP\n    LOOP -- "OUTPUT_START/END 帧" --> Host\n```\n\nSources: [src/container-runner.ts](src/container-runner.ts#L1-L4)、[container/agent-runner/package.json](container/agent-runner/package.json#L11-L17)、[container/agent-runner/src/index.ts](container/agent-runner/src/index.ts#L1-L15)\n\n## IPC 目录命名空间：按组、代理、任务三层隔离\n\n每个工作区（group）拥有独立的 IPC 根目录 `DATA_DIR/ipc/{groupFolder}`，并根据执行实体在根目录下展开**三级命名空间**：\n\n| 命名空间 | 路径 | 适用场景 |\n|---|---|---|\n| 主会话 | `ipc/{groupFolder}/` | 普通用户交互 turn |\n| 子代理 | `ipc/{groupFolder}/agents/{agentId}/` | 会话子代理 / conversation agent |\n| 隔离任务 | `ipc/{groupFolder}/tasks-run/{taskRunId}/` | 定时任务（每次运行独立命名空间） |\n\n每个命名空间内固定包含四个子目录：`messages/`（文本/图片外发）、`tasks/`（任务类工具请求）、`input/`（宿主注入的后续消息）、`agents/`（嵌套代理命名空间）。容器模式下该根目录以读写方式挂载到容器的 `/workspace/ipc`；宿主机模式则通过环境变量 `HAPPYCLAW_WORKSPACE_IPC` 注入（`container-runner.ts` 中 host 模式设置 `hostEnv[\'HAPPYCLAW_WORKSPACE_IPC\'] = groupIpcDir`）。容器内 Runner 读取该环境变量并降级到默认路径 `/workspace/ipc`。\n\n这一命名空间设计同时解决了两个问题：**会话隔离**（子代理与主代理的输入输出互不串扰）与**回执归属**（任务类工具必须把回执写回"请求文件被读出的那个 tasksDir"，而非按 folder 重算的主 root——`src/index.ts` 中明确注释了会话子代理与隔离任务各有嵌套 IPC root，写到主 root 会导致容器侧空等超时）。\n\n```mermaid\nflowchart TD\n    ROOT["data/ipc/{groupFolder}/"] --> M["messages/"]\n    ROOT --> T["tasks/"]\n    ROOT --> I["input/"]\n    ROOT --> A["agents/"]\n    A --> A1["agents/{agentId}/"]\n    A1 --> A1M["messages/ · tasks/ · input/"]\n    ROOT --> TR["tasks-run/{taskRunId}/"]\n    TR --> TRM["messages/ · tasks/ · input/"]\n    M --> MR["message-results/（回执目录）"]\n```\n\nSources: [src/container-runner.ts](src/container-runner.ts#L1333-L1357)、[src/container-runner.ts](src/container-runner.ts#L2400-L2423)、[src/container-runner.ts](src/container-runner.ts#L2684-L2685)、[container/agent-runner/src/index.ts](container/agent-runner/src/index.ts#L151-L166)、[src/index.ts](src/index.ts#L11505-L11510)\n\n## 输入协议：stdin 冷启动 + input 文件热注入\n\n### 冷启动：stdin 一次性输入\n\n容器启动时，`entrypoint.sh` 将 stdin 完整缓冲到 `/tmp/input.json`，随后降权执行 `node /tmp/dist/index.js < /tmp/input.json`——**stdin 以 EOF 为边界，携带完整的 `ContainerInput` JSON**（prompt、sessionId、channelContext、agentProfile、taskRunId 等）。宿主侧通过 `spawn(\'docker\', containerArgs)` 写入 stdin 后立即关闭管道。\n\n### 热注入：input 目录文件\n\nRunner 启动后进入 `query → waitForIpcMessage → query` 主循环。后续用户消息由宿主 `GroupQueue.sendMessage()` 以**原子写（tmp 文件 + rename）**方式写入 `input/{timestamp}-{random}.json`，文件内容为：\n\n```json\n{\n  "type": "message",\n  "text": "...",\n  "images": [],\n  "queryRunId": "…",\n  "sourceJid": "qq:c2c:…",\n  "channelContext": { "schemaVersion": 1, "provider": "qq", "sourceJid": "…" },\n  "taskId": "…",\n  "receipt": {\n    "deliveryId": "…",\n    "chatJid": "…",\n    "coveredCursors": [{ "timestamp": "…", "id": "…" }],\n    "cursor": { "timestamp": "…", "id": "…" }\n  }\n}\n```\n\n`receipt` 是这套协议可靠性的核心：宿主在写入文件的同时在内存中登记 `pendingIpcDeliveries`，并在同一同步关键区（rename + 注册）内完成投递登记——`rename + in-memory delivery registration are one synchronous critical section`，杜绝了"文件已写但恢复元数据缺失"的竞态窗口。Runner 侧 `drainIpcInput()` 读取全部 JSON 后调用 `orderIpcInputMessages()` **按 receipt 的持久化 DB 游标（cursor.timestamp + cursor.id）恢复真实到达顺序**——因为 requeue 消息的文件名可能比排队的消息更新，文件名顺序不可靠。\n\n### 信号量文件：进程生命周期控制\n\n除消息文件外，`input/` 目录还承载三个生命周期信号量：\n\n| 信号量 | 语义 | 处理时机 |\n|---|---|---|\n| `_close` | 会话结束，空闲等待时退出 | `waitForIpcMessage` 空闲轮询 |\n| `_drain` | 完成当前 query 后退出（一问一答语义） | 查询完成后轮询；结果到达后视为 close |\n| `_interrupt` | 优雅中断当前 query（stop/steer） | 查询进行中轮询；带 10s 宽限期 |\n\n信号量是"读后即删"的：`shouldClose()/shouldInterrupt()/shouldDrain()` 检测到文件存在就 unlink 并返回 true。查询进行中的轮询由 `pollIpcDuringQuery()` 执行，它会在检测到 `_interrupt` 时调用 `ipcDeliveryTracker.cancelCurrentTurn()`——**只取消当前被中断 SDK query 拥有的输入 turn，后续已接受的 turn 保持未确认并可重新排队**，避免 warm runner 初始消息被错误重放。\n\nSources: [container/entrypoint.sh](container/entrypoint.sh#L168-L173)、[src/group-queue.ts](src/group-queue.ts#L1629-L1691)、[src/group-queue.ts](src/group-queue.ts#L1694-L1708)、[container/agent-runner/src/ipc-delivery.ts](container/agent-runner/src/ipc-delivery.ts#L22-L46)、[container/agent-runner/src/index.ts](container/agent-runner/src/index.ts#L1284-L1376)、[container/agent-runner/src/index.ts](container/agent-runner/src/index.ts#L2057-L2101)\n\n## 输出协议：stdout 帧 + 文件外发 + 回执轮询\n\nRunner 的输出分两条路径返回宿主：\n\n### 路径一：stdout 帧协议\n\n每次查询结果以 `---HAPPYCLAW_OUTPUT_START---` / `---HAPPYCLAW_OUTPUT_END---` 标记包裹的 JSON（`ContainerOutput`）写入 stdout，一次运行可输出多帧（status 依次为 stream → success/error/closed）。宿主侧 `agent-output-parser.ts` 实现字符串感知的 `findJsonObjectEnd()` 花括号深度匹配，即使帧内 JSON 字符串包含标记文本也不会误切，可容忍同一 buffer 中尾随第二帧。\n\n`ContainerOutput` 的关键字段包括：`status`、`result`、`newSessionId`、`inputTurnId`（**不可变的用户输入 turn 身份**，IPC turn 用持久化 deliveryId）、`ipcReceipts`（本 turn 完成时确认的 receipt 数组）、`finalizationReason`（completed/interrupted/error/truncated）、`pendingBgTasks`、`queryIdle` 等。\n\n### 路径二：文件外发（MCP 工具）\n\n`send_message`、`send_image`、`schedule_task`、`list_tasks` 等 MCP 工具**不直接触达 IM/DB**，而是把请求写入 `messages/` 或 `tasks/` 目录的 JSON 文件，然后 `pollIpcResult()` 以 500ms 间隔轮询回执文件，默认 30s（消息类 120s）超时。宿主侧 `IpcWatcherManager` 对 `messages/` 与 `tasks/` 目录做事件驱动 `fs.watch` + 5s 兜底轮询，100ms 防抖后处理。\n\n`send_message` 的文件载荷由 `buildSendMessageData()` 统一构建：**总是盖章 `chatJid`、`groupFolder`、`timestamp`，条件性盖章 `isScheduledTask`、`taskId`、`inputTurnId`**——`taskId` 键是否存在直接决定宿主走"普通用户 turn 回复"还是"任务广播"分支。同时写入 `interactionMode`（assistant/proactive）冻结交付契约，宿主不得在写入后因工作区模式变更而重新解释已落盘的副作用。\n\n宿主消费消息文件后，把 `send_message_result_{requestId}.json` 原子写回 `message-results/` 目录（该目录在 messages/ 之外，**防止宿主 watcher 消费自己的回执**）。回执载荷携带 `disposition`（staged_progress / staged_final / delivered_separately），Runner 侧据此向模型返回语义化确认文本（如"Final answer staged on the active reply"）。\n\n```mermaid\nsequenceDiagram\n    participant Runner as Agent Runner\n    participant FS as IPC 文件系统\n    participant Host as 宿主进程\n    Runner->>FS: 写 messages/{ts}.json (type=message, requestId)\n    Host->>FS: fs.watch 触发 → 读取 → routeHostIpcOutput\n    Host->>FS: 原子写 message-results/send_message_result_{id}.json\n    loop 500ms 轮询\n        Runner->>FS: readFileSync 回执\n    end\n    Runner->>Runner: 返回模型语义化确认\n```\n\nSources: [container/agent-runner/src/index.ts](container/agent-runner/src/index.ts#L801-L814)、[src/agent-output-parser.ts](src/agent-output-parser.ts#L13-L63)、[container/agent-runner/src/mcp-tools.ts](container/agent-runner/src/mcp-tools.ts#L60-L119)、[container/agent-runner/src/mcp-tools.ts](container/agent-runner/src/mcp-tools.ts#L437-L473)、[src/index.ts](src/index.ts#L1084-L1205)、[src/index.ts](src/index.ts#L11436-L11468)\n\n## 宿主侧投递路由与去重\n\n宿主消费 `messages/` 文件时通过 `routeHostIpcOutput()` 选择投递通道，输出三条路径：\n\n| 路径 | 条件 | 语义 |\n|---|---|---|\n| `primary_projection` | 授权 + 显式 progress/final + assistant 模式 | 文本 **stage 进当前活动主回复**（不产生第二条消息），由 SDK Result 定稿 |\n| `separate_provider` | 定时任务 / legacy / 显式 separate / proactive 模式 | 独立原生消息，走 Turn Outbox 与渠道连接器 |\n| `rejected` | 未授权 | 拒绝投递，但不落盘 |\n\n`primary_projection` 通过 `ActiveTurnOutputRegistry.stage()` 与活动 turn 的 `inputTurnId` 精确匹配——即使 stage 失败也**消费该请求**，防止其落入 legacy 路径产生第二条 provider 消息。proactive 模式则先 `recordAttemptedFinal()` 记录"已尝试最终投递"（供 proactive-final-classifier 在模型未发最终消息时兜底发布 SDK final）。\n\n重试去重由 `ipc-send-dedup.ts` 承担：以 `{sourceGroup}|{chatJid}|MD5(text)` 为键，10 分钟 TTL、上限 500 条。**只有确认送达的投递才被记录**——replay 命中本身就是"先前尝试已触达用户"的证据，失败的尝试绝不记录，避免掩盖真实失败。\n\nSources: [src/host-ipc-output-router.ts](src/host-ipc-output-router.ts#L84-L165)、[src/ipc-send-dedup.ts](src/ipc-send-dedup.ts#L1-L66)、[src/index.ts](src/index.ts#L10383-L10399)\n\n## 投递可靠性：回执、恢复与隔离任务清理\n\n### Turn 级 ACK：IpcTurnDeliveryTracker\n\n容器侧 `IpcTurnDeliveryTracker` 把每个已接受的 IPC 批次与"恰好一个健康 SDK result"绑定：`completeNextTurn()` 只在健康完成时被调用，错误、中断、后台任务未结清、截断流都不调用——**失败 turn 的消息保持可重放**。输出关联由 `IpcTurnOutputCorrelation` 完成：`inputTurnId` 在 turn 开始时快照到每一帧，后续被接受的 steer 不会轮换已执行 turn 的身份。\n\n### 启动恢复：discardStartupTypedIpcDeliveries\n\n宿主启动时执行 `discardStartupTypedIpcDeliveries()` 全量扫描所有 `input/` 目录（主会话 + agents 子命名空间），解析带 receipt 的投递文件并**先持久化回滚再删除唯一崩溃证据**——若回滚回调失败或进程崩溃，文件保留供下次启动重试。隔离任务命名空间被明确排除，因为任务投递不是 DB 聊天消息，绝不能走游标回滚协议。\n\n### 隔离任务生命周期：.run-complete 标记\n\n定时任务运行结束后，`task-scheduler.ts` 调用 `markIsolatedTaskRunIpcComplete()` 在任务命名空间**旁边**（而非内部，防止不可信容器伪造生产者完成）写 `.run-complete-{taskRunId}.json` 标记。宿主 watcher 只有在 `messages/` 与 `tasks/` 目录**完全清空**（每个文件的副作用已确认并 unlink）后才允许清理整个命名空间——"空目录即持久化 ACK"。若进程在清理前崩溃，标记与文件都幸存，启动扫描会先恢复投递再清理。\n\nSources: [container/agent-runner/src/ipc-delivery.ts](container/agent-runner/src/ipc-delivery.ts#L150-L223)、[container/agent-runner/src/ipc-delivery.ts](container/agent-runner/src/ipc-delivery.ts#L225-L262)、[src/ipc-delivery-recovery.ts](src/ipc-delivery-recovery.ts#L85-L122)、[src/isolated-task-ipc.ts](src/isolated-task-ipc.ts#L4-L20)、[src/isolated-task-ipc.ts](src/isolated-task-ipc.ts#L102-L140)、[src/task-scheduler.ts](src/task-scheduler.ts#L391-L424)\n\n## 与 StreamEvent 体系的关系\n\nRunner 内部 `StreamEventProcessor` 把 SDK 原始流事件（text_delta、tool_use_start、task_updated 等）转换为统一 `StreamEvent`，经 `ContainerOutput.streamEvent` 字段随 stdout 帧输出。`StreamEvent` 类型定义以 [shared/stream-event.ts](shared/stream-event.ts) 为唯一权威源，构建时同步复制到 `src/`、`container/agent-runner/src/` 与 `web/src/` 四处，并由 `check-stream-event-sync.sh` 在 CI 中校验一致性。**IPC 负责控制面（消息注入、外发、任务调度），StreamEvent 负责数据面（流式渲染）**，两者通过 `inputTurnId` / `deliveryId` 关联到同一用户输入。\n\nSources: [shared/stream-event.ts](shared/stream-event.ts#L1-L10)、[scripts/check-stream-event-sync.sh](scripts/check-stream-event-sync.sh#L27-L31)、[container/agent-runner/src/stream-processor.ts](container/agent-runner/src/stream-processor.ts#L1-L10)\n\n## 阅读建议\n\n- 掌握 IPC 帧格式与 StreamEvent 完整类型体系：[StreamEvent 流式事件体系与类型同步](11-streamevent-liu-shi-shi-jian-ti-xi-yu-lei-xing-tong-bu)\n- 理解 IPC 注入的排队与并发前提：[会话队列与并发控制](18-hui-hua-dui-lie-yu-bing-fa-kong-zhi)\n- 了解 Runner 进程的启动/退出/卡死检测闭环：[Runner 生命周期、卡死检测与故障恢复](21-runner-sheng-ming-zhou-qi-qia-si-jian-ce-yu-gu-zhang-hui-fu)\n- 定时任务如何借助隔离 IPC 命名空间执行：[定时任务调度与异常恢复](19-ding-shi-ren-wu-diao-du-yu-yi-chang-hui-fu)';export{n as default};

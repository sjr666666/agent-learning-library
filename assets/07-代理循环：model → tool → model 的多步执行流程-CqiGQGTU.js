const t='MiniCode 的核心执行引擎是一个**单文件、单入口、无递归**的代理循环。它不依赖任何编排框架，全部逻辑收敛在 `src/agent-loop.ts` 的 `runAgentTurn()` 函数中——这个函数定义了"模型思考 → 调用工具 → 拿回结果 → 再次交给模型"的完整迭代，并内嵌了上下文预算管理、空响应恢复、思考块保留等防御性机制。理解这个循环，是理解 MiniCode 其余一切（工具注册、压缩策略、会话持久化）的枢纽。\n\n## 循环骨架：一个 for 循环驱动的状态机\n\n代理循环的本质是一个以 `maxSteps` 为界的 `for` 循环。每一次迭代（step）内，循环只做两件事：**向模型请求下一步**（`model.next()`），然后**根据返回的 `AgentStep` 分派到不同的收尾路径**。整个函数是纯函数式的——不修改传入的 `messages` 数组，而是每次通过展开运算符构造新数组。\n\n```mermaid\nflowchart TD\n    A[用户输入入队 messages] --> B{step < maxSteps?}\n    B -- 否 --> Z[返回 maxSteps 兜底消息]\n    B -- 是 --> C[上下文预算管理<br/>snip / microcompact / collapse / autocompact]\n    C --> D[model.next messages]\n    D --> E{AgentStep.type}\n    E -- assistant --> F{空响应?}\n    F -- 是（重试 < 2） --> G[push 续写提示<br/>continue]\n    F -- 是（耗尽） --> H[返回兜底 assistant 消息]\n    F -- 否 --> I{<final>/<progress>?}\n    I -- progress --> J[存为 assistant_progress<br/>push 续写提示<br/>continue]\n    I -- final --> K[返回 assistant 消息<br/>附 providerUsage]\n    E -- tool_calls --> L[逐条执行 ToolRegistry.execute]\n    L --> M[大结果落盘 + 批次预算]\n    M --> N[追加 assistant_tool_call + tool_result]\n    N --> O{存在 awaitUser?}\n    O -- 是 --> P[返回，等待用户回复]\n    O -- 否 --> B\n```\n\n`AgentStep` 是模型适配器与循环之间的唯一契约，只有两种形态（见 [types.ts](src/types.ts#L69-L86)）：\n\n| AgentStep 类型 | 语义 | 循环行为 |\n|---|---|---|\n| `assistant` | 模型给出文本回答 | 若是 `<final>`/`[FINAL]` 标记 → 结束回合并返回；若为空 → 按重试策略处理 |\n| `tool_calls` | 模型请求执行工具 | 执行 `calls[]` 中的每个工具，将调用与结果入队后进入下一轮迭代 |\n\n`runAgentTurn` 的入参通过一个参数对象传递，其中 `model`（`ModelAdapter`）与 `tools`（`ToolRegistry`）是两大核心依赖，其余均为可选的回调与持久化状态（[agent-loop.ts](src/agent-loop.ts#L112-L130)）。`maxSteps` 为 `null` 时循环无上限；TUI 与 CLI 两个入口分别以不同方式提供该参数。\n\nSources: [agent-loop.ts](src/agent-loop.ts#L112-L130)、[types.ts](src/types.ts#L69-L86)\n\n## 回合开始前的上下文预算管理\n\n每次调用 `model.next()` 之前，循环会先执行一条**分层压缩流水线**——它按"成本从低到高、破坏性从弱到强"的顺序尝试控制上下文体积。这一层是代理循环区别于简单 while-loop 的关键设计：工具调用越多，历史越长，越需要在不打断模型视角的前提下压缩。\n\n```mermaid\nflowchart LR\n    A[原始 messages] --> B[snipCompact<br/>阈值 0.70<br/>每回合仅一次]\n    B --> C[microcompact<br/>阈值 0.50<br/>清空旧工具结果]\n    C --> D[context collapse<br/>阈值 0.75<br/>摘要投影]\n    D --> E[autoCompact<br/>阈值 0.85/0.95<br/>LLM 摘要压缩<br/>仅 step 0]\n    E --> F[model.next]\n```\n\n| 层级 | 触发阈值 | 作用范围 | 行为 | 关键常量 |\n|---|---|---|---|---|\n| Snip Compact | 利用率 ≥ 0.70 | 最早的消息区间 | 删除旧区间并插入 `snip_boundary` 占位，目标降至 0.60；每回合由 `snippedThisTurn` 守卫只执行一次 | [constants.ts](src/compact/constants.ts#L7-L11) |\n| Microcompact | 利用率 ≥ 0.50 | 旧 `tool_result` 消息 | 将内容替换为 `CLEAR_MARKER`，仅保留最近 3 条完整结果 | [constants.ts](src/compact/constants.ts#L20-L25)、[microcompact.ts](src/compact/microcompact.ts#L6-L50) |\n| Context Collapse | 利用率 ≥ 0.75 | 可摘要的对话区间 | 调用模型生成投影摘要，原消息保留在侧，目标降至 0.65 | [constants.ts](src/compact/constants.ts#L13-L18) |\n| AutoCompact | ≥ 0.85（critical）或 ≥ 0.95（blocked） | 整个会话 | LLM 生成 `context_summary`，**仅第一个 step 执行**，并有失败熔断 | [agent-loop.ts](src/agent-loop.ts#L221-L236) |\n\n这条流水线在代码中的执行顺序非常清晰：先算上下文统计（`computeContextStats`），再依次尝试 snip → microcompact → collapse（每步后重新统计并通知 UI），最后在 `step === 0` 时检查是否达到 autoCompact 的临界线（[agent-loop.ts](src/agent-loop.ts#L174-L236)）。利用率阈值统一来自 `computeContextStats` 的 `warningLevel` 分级：0.50 为 warning、0.85 为 critical、0.95 为 blocked（[token-estimator.ts](src/utils/token-estimator.ts#L185-L194)）。\n\nSources: [agent-loop.ts](src/agent-loop.ts#L174-L236)、[compact/constants.ts](src/compact/constants.ts#L1-L33)、[utils/token-estimator.ts](src/utils/token-estimator.ts#L177-L205)\n\n## assistant 分支：空响应恢复、进度标记与思考块保留\n\n当 `model.next()` 返回 `type: \'assistant\'` 时，循环进入一段精心设计的**终止判定逻辑**。它按照"空响应重试 → 思考阶段中断恢复 → 正常返回"的优先级处理，确保任何异常出口都有兜底消息。\n\n### 空响应与思考中断的重试策略\n\n模型返回空文本并不罕见——token 耗尽、`pause_turn`、thinking 块截断都会导致空响应。循环为此设计了三种独立的重试计数器：\n\n| 场景 | 判定条件 | 重试上限 | 续写提示内容 |\n|---|---|---|---|\n| 空响应 | `content.trim().length === 0` | 2 次 | 根据本回合是否已用工具选择不同的续写指令 |\n| 可恢复的 thinking 中断 | 空响应 + `stop_reason` 为 `pause_turn`/`max_tokens` + 包含 thinking 块 | 3 次 | 明确要求"不要重复之前的计划，继续下一步" |\n| 空响应耗尽 | 重试次数用尽 | — | 生成兜底 assistant 消息，附上 `stop_reason` 与块类型诊断信息 |\n\n空响应的判定函数 `isEmptyAssistantResponse` 只做简单的 trim 检查（[agent-loop.ts](src/agent-loop.ts#L31-L33)）；可恢复 thinking 中断的判定 `isRecoverableThinkingStop` 则要求**同时满足**空响应、特定的 stop_reason 以及 thinking 块存在三个条件（[agent-loop.ts](src/agent-loop.ts#L92-L110)）。重试通过 `pushContinuationPrompt` 注入一条用户角色的续写指令，`continue` 进入下一轮迭代——注意这不会消耗新的工具步数。\n\n### progress / final 标记协议\n\n模型可以通过 `<progress>` / `[PROGRESS]` 与 `<final>` / `[FINAL]` 两种前缀明确声明自己的输出是"中间进度"还是"最终答案"。适配器在 `parseAssistantText` 中解析这些标记并剥离闭合标签（[anthropic-adapter.ts](src/anthropic-adapter.ts#L153-L187)）。循环根据标记决定消息去向：\n\n- **progress** → 以 `assistant_progress` 角色入队，并追加续写提示强制模型继续；\n- **final** → 直接作为最终 assistant 消息返回，回合结束；\n- **无标记但本回合已用过工具** → 按 `shouldTreatAssistantAsProgress` 的规则（该函数当前恒返回 `false`，即不再自动降级）视为最终回答。\n\n### 思考块的往返保留\n\nthinking 块是 Anthropic 系模型的推理内容，包含签名信息，必须在多步工具调用中**原样带回**下一轮请求，否则部分供应商会报错。循环通过 `appendThinkingBlocks` 将其以 `assistant_thinking` 角色插入消息队列（[agent-loop.ts](src/agent-loop.ts#L163-L172)），适配器再在 `toAnthropicMessages` 中把该角色还原为原始 thinking 块（[anthropic-adapter.ts](src/anthropic-adapter.ts#L253-L258)）。`test/anthropic-thinking-roundtrip.test.ts` 专门验证了这一往返：第二次请求的 assistant 消息内容块类型必须严格为 `[\'thinking\', \'tool_use\']`（[测试文件](test/anthropic-thinking-roundtrip.test.ts#L85-L103)）。\n\n### providerUsage 记账\n\n每条 assistant 角色的消息都会通过 `withProviderUsage` 附带供应商返回的 token 用量（[agent-loop.ts](src/agent-loop.ts#L35-L48)），作为后续令牌估算的"权威数据源"——这正是"供应商用量优先"记账策略的落点（详见 [供应商用量优先的上下文记账与令牌估算](11-gong-ying-shang-yong-liang-you-xian-de-shang-xia-wen-ji-zhang-yu-ling-pai-gu-suan)）。\n\nSources: [agent-loop.ts](src/agent-loop.ts#L240-L341)、[anthropic-adapter.ts](src/anthropic-adapter.ts#L153-L187)、[test/anthropic-thinking-roundtrip.test.ts](test/anthropic-thinking-roundtrip.test.ts#L85-L103)\n\n## tool_calls 分支：执行、落盘、预算与续环\n\n`tool_calls` 分支是循环的"发动机"——模型在这里产出一批工具调用，循环负责执行并把结果拼回消息队列，让下一轮迭代得以继续。\n\n### 工具执行的容错边界\n\n`ToolRegistry.execute` 是工具执行的唯一入口，它为每一次调用提供三层容错：未知工具名返回 `Unknown tool` 错误；Zod schema 校验失败返回校验错误消息；`run()` 抛出的异常被捕获并转为 `ok: false` 的结果。无论成败，调用都以 `ToolResult` 形式返回，不会向循环抛出异常（[tool.ts](src/tool.ts#L95-L124)）。循环内部还维护 `toolErrorCount`，当空响应兜底时用它决定提示文案的措辞（[agent-loop.ts](src/agent-loop.ts#L308-L313)）。\n\n```mermaid\nsequenceDiagram\n    participant Loop as runAgentTurn\n    participant Reg as ToolRegistry\n    participant Tool as 具体工具\n    participant Store as tool-result-storage\n\n    Loop->>Reg: execute(name, input, {cwd, permissions})\n    Reg->>Reg: find + schema.safeParse\n    Reg->>Tool: run(parsed, context)\n    Tool-->>Reg: ToolResult {ok, output, awaitUser?}\n    Reg-->>Loop: ToolResult\n    Loop->>Store: replaceLargeToolResult (>50K chars → 落盘)\n    Loop->>Store: applyToolResultBudget (批次 ≤ 200K chars)\n    Loop->>Loop: 追加 assistant_tool_call + tool_result\n```\n\n### 大结果落盘与批次预算\n\n工具输出可能极大（如 `grep_files` 全库扫描）。循环在把结果写回消息队列前执行两道防线：`replaceLargeToolResult` 将超过 50,000 字符的结果持久化到磁盘，消息中只保留 2,000 字符的预览与 `<persisted-output>` 标记（[tool-result-storage.ts](src/utils/tool-result-storage.ts#L11-L13)）；`applyToolResultBudget` 再对**同一批次**的结果总量施加 200,000 字符上限，超限的旧结果同样被替换为预览（[agent-loop.ts](src/agent-loop.ts#L393-L414)）。这两步保证单轮工具结果不会撑爆后续请求的上下文。\n\n### 并行工具调用的消息编排\n\n一次 `tool_calls` 可以携带多个调用。循环**顺序执行**这些调用（`for...of`），但将全部 `assistant_tool_call` 与 `tool_result` 一次性追加到消息队列，保持与模型返回时的调用顺序一致。`providerUsage` 只挂载在**最后一个** tool_call 消息上，避免重复记账（[agent-loop.ts](src/agent-loop.ts#L416-L437)）。测试 `anthropic-thinking-roundtrip` 验证了并行工具调用与思考块共存的场景（[测试文件](test/anthropic-thinking-roundtrip.test.ts#L106-L120)）。\n\n### ask_user 的人机交互暂停\n\n`ask_user` 工具返回 `awaitUser: true`，循环检测到后会把问题以 assistant 消息呈现给用户并**立即返回**，等待用户回复后由外层入口开启新回合（[agent-loop.ts](src/agent-loop.ts#L439-L453)、[ask-user.ts](src/tools/ask-user.ts#L22-L30)）。这是循环中唯一打破"model → tool → model"自动续环的出口——它把控制权交还给人类。\n\nSources: [tool.ts](src/tool.ts#L95-L124)、[agent-loop.ts](src/agent-loop.ts#L374-L453)、[utils/tool-result-storage.ts](src/utils/tool-result-storage.ts#L11-L13)、[tools/ask-user.ts](src/tools/ask-user.ts#L8-L30)\n\n## 回合的三种出口与调用方视角\n\n循环不是无限运转的——它只有三个正常出口，外加一个异常兜底：\n\n| 出口 | 触发条件 | 返回内容 |\n|---|---|---|\n| 最终回答 | assistant 消息（final 或非空且未用工具） | 完整的消息队列，末尾为 assistant 消息 |\n| 等待用户 | `ask_user` 返回 `awaitUser` | 消息队列 + 问题作为 assistant 消息 |\n| 步数耗尽 | `step` 达到 `maxSteps` | 消息队列 + "达到最大工具步数限制" 兜底消息 |\n\n两个调用方展示了不同的消费方式：**CLI 模式**（`index.ts`）只关心循环返回后最后一条 assistant 消息并打印（[index.ts](src/index.ts#L238-L272)）；**TUI 模式**（`tty-app.ts`）则通过 `onAssistantMessage`、`onToolStart`、`onAutoCompact` 等回调实时驱动终端渲染与状态栏（[tty-app.ts](src/tty-app.ts#L1390-L1454)）。两个入口都以 `permissions.beginTurn()` / `endTurn()` 包裹循环调用，为权限系统划定回合边界。\n\nSources: [agent-loop.ts](src/agent-loop.ts#L456-L465)、[index.ts](src/index.ts#L238-L272)、[tty-app.ts](src/tty-app.ts#L1388-L1454)\n\n## 设计要点总结\n\n- **单一职责**：`runAgentTurn` 只做编排，不关心模型协议（适配器负责）与工具细节（注册表负责）；`ModelAdapter.next()` 与 `ToolRegistry.execute()` 两个接口让循环对任何供应商、任何工具透明。\n- **防御性迭代**：空响应重试、thinking 中断恢复、工具错误计数、步数上限共同构成了一张"无论模型多不配合都不会挂死"的安全网。\n- **上下文预算前置**：压缩流水线在每次 `model.next()` 之前运行，且按 snip → microcompact → collapse → autoCompact 的成本递增顺序排列，把最昂贵的 LLM 压缩保留到最危急的时刻。\n- **状态不变性**：每次迭代都构造新的 `messages` 数组，`messages` 作为返回值成为会话持久化的唯一事实来源——这为 [追加式 JSONL 事件日志与断点恢复](19-zhui-jia-shi-jsonl-shi-jian-ri-zhi-yu-duan-dian-hui-fu) 提供了干净的接入点。\n\n## 继续深入\n\n- [统一工具协议：注册、Schema 校验与集中执行](8-tong-gong-ju-xie-yi-zhu-ce-schema-xiao-yan-yu-ji-zhong-zhi-xing) — 本页中 `ToolRegistry.execute` 的完整机制\n- [结构化消息模型：角色类型与工具调用编排](9-jie-gou-hua-xiao-xi-mo-xing-jiao-se-lei-xing-yu-gong-ju-diao-yong-bian-pai) — `ChatMessage` 七种角色与 `assistant_tool_call`/`tool_result` 的配对规则\n- [Anthropic 适配器：重试退避、限流与思考块保留](10-anthropic-gua-pei-qi-zhong-shi-tui-bi-xian-liu-yu-si-kao-kuai-bao-liu) — `model.next()` 的供应商侧实现细节';export{t as default};

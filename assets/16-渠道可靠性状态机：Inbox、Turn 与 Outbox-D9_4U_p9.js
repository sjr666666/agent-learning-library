const n='## 引言：渠道可靠性的核心矛盾\n\nIM 渠道（飞书、QQ、微信、WhatsApp）与 HTTP 服务有着本质差异：消息投递的 ACK 语义弱、存在网络闪断与进程崩溃窗口、且同一逻辑输入可能被 WebSocket 重连或回填机制重复投递。若不做持久化，一次 `Agent` 执行可能产生两种灾难性后果——**重复输出**（两条相同的用户可见消息）或**丢失输出**（发送后进程崩溃，用户永远等不到回复）。\n\nhappyclaw 用一套**供应商无关的持久化状态机**解决这个问题：`channel-reliability-store` 模块专门拥有持久化与栅栏（fencing）能力，而 Provider I/O、路由决策与重试策略留在渠道/运行时层。这一边界刻意保持狭窄，使同一套 Inbox/Turn/Outbox 模型可被飞书、QQ、微信及未来新传输复用。该模块注释直接定义了这条边界：*"This module deliberately owns only persistence and fencing. Provider I/O, routing decisions and retry policy remain in the channel/runtime layers."*\n\nSources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L3-L10)\n\n## 三层持久化流水线\n\n整个可靠性模型由五个 SQLite 表构成，形成一条**入站 → 执行 → 出站**的持久化流水线。schema v60 迁移在 v59 之上一次性创建全部五张表，且重启幂等：\n\n| 表 | 角色 | 核心唯一性约束 | 生命周期归属 |\n|---|---|---|---|\n| `channel_inbox` | 入站消息去重与租约 | `UNIQUE(provider, account_id, external_message_id)` | 每条外部消息一行 |\n| `channel_cursors` | 回填游标（防回退） | `PRIMARY KEY(provider, account_id, scope, chat_id)` | 每聊天一个单调位置 |\n| `turn_runs` | 一次逻辑输入的执行记录 | `idempotency_key` 唯一 + `inbox_id` 唯一索引 | 每个逻辑 Turn 一行 |\n| `channel_outbox` | 出站副作用幂等 | `idempotency_key` 唯一 + `UNIQUE(turn_run_id, ordinal)` | 每个物理输出一行 |\n| `streaming_cards` | 流式卡片的持久化投影 | 确定性 `id`（由 runId 派生） | 每个 Turn 至多一张主卡 |\n\nSources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L287-L436)\n\n```mermaid\nflowchart LR\n    subgraph Provider["IM Provider (Feishu/QQ/WeChat)"]\n        WS[WebSocket 事件] --> IN\n        BK[回填分页] --> IN\n        OUT[物理发送] --> PROVACK[Provider ACK]\n    end\n\n    subgraph Durable["持久化层 (channel-reliability-store)"]\n        IN[channel_inbox<br/>received/admitted/queued/processing] --> TURN[turn_runs<br/>queued/running/finalizing]\n        TURN --> OB[channel_outbox<br/>pending/claimed/sending/delivered]\n        TURN --> CARD[streaming_cards<br/>creating/streaming/recovering]\n        CURSOR[channel_cursors<br/>position + tie_breaker] -.回填推进.-> IN\n    end\n\n    subgraph Runtime["运行时层 (feishu.ts / index.ts)"]\n        CLAIM[claimChannelInboxById] --> PROC[processClaimedIncomingMessage]\n        PROC --> RT[ChannelTurnRuntime.start]\n        RT --> DEL[deliverChannelOutboxItem]\n        DEL --> OUT\n    end\n\n    IN --> CLAIM\n    TURN --> RT\n```\n\n设计要点：**每层之间通过数据库外键级联**——`turn_runs.inbox_id` 引用 `channel_inbox(id) ON DELETE SET NULL`（保证 Inbox 清理后 Turn 仍可审计），`channel_outbox.turn_run_id` 与 `streaming_cards.turn_run_id` 引用 `turn_runs(id) ON DELETE CASCADE`（删除父 Turn 时级联清除其输出记录）。Sources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L334-L435)\n\n## Inbox：入站唯一性与租约栅栏\n\n### 状态机\n\n`ChannelInboxStatus` 定义了七种状态，其中 `processed`、`ignored`、`failed` 为终态：\n\n```mermaid\nstateDiagram-v2\n    [*] --> received: 消息到达\n    received --> admitted: 准入校验\n    admitted --> queued: 入队\n    queued --> processing: 租约认领 (claim)\n    received --> processing: 直接认领\n    admitted --> processing: 直接认领\n    processing --> processed: 执行完成\n    processing --> ignored: 判定忽略\n    processing --> failed: 明确失败\n    processing --> processing: 租约续期 (renew)\n    processing --> queued: 失败重试\n    processed --> [*]\n    ignored --> [*]\n    failed --> [*]\n```\n\n### 去重与租约\n\n**唯一性**是 Inbox 的第一道防线：`recordChannelInbox` 以 `INSERT OR IGNORE` 写入，冲突时返回已存在行（`created: false`），调用方可据此判断是首次到达还是重复投递。Sources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L663-L709)\n\n**租约**是第二道防线：`claimChannelInbox` 在事务内先以 `SELECT` 选出候选（`status = \'queued\'` 或租约已过期的 `processing` 行），再用条件 UPDATE 原子抢占——`lease_token + 1` 递增、`lease_owner` 写入、`lease_expires_at` 设置。过期认领行（`lease_expires_at <= now`）可被任何持有者回收，这保证了崩溃后消息不会被永久卡死。Sources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L774-L851)\n\n飞书集成中的实际参数：`FEISHU_INBOX_LEASE_MS = 5 * 60 * 1000`（5 分钟租约），`handleIncomingMessage` 先 `recordChannelInbox` 再 `claimChannelInboxById`；若持久化失败则**拒绝无栅栏执行**——注释明确警告：没有 Inbox 唯一性栅栏，两个 WS 客户端或一次重连可能启动同一个 Agent Turn。认领成功后由 `startInboxHeartbeat` 周期性 `renewChannelInboxClaim` 续期。Sources: [feishu.ts](src/feishu.ts#L221-L225)、[feishu.ts](src/feishu.ts#L1761-L1819)、[feishu.ts](src/feishu.ts#L1297-L1302)\n\n### 回填游标\n\n`channel_cursors` 表解决回填分页的幂等问题：`advanceChannelCursor` 只在 `excluded.position > current.position` 或同位置下 `tie_breaker` 更大时才推进，保证游标**单调不后退**；`deleteChannelCursor` 则要求显式 CAS（position + tieBreaker 匹配）才能删除，用于退役传输。飞书回填先按 `createTimeMs` 升序排序每页消息再逐条 `handleIncomingMessage`，保证单聊内的处理顺序。Sources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L484-L537)、[feishu.ts](src/feishu.ts#L2965-L2973)\n\n## Turn：逻辑输入执行状态机\n\n### 状态机与幂等键\n\n`ChannelTurnRunStatus` 定义了九种状态，`completed / failed / interrupted / cancelled` 为终态：\n\n```mermaid\nstateDiagram-v2\n    [*] --> queued: 创建 (createChannelTurnRun)\n    queued --> running: 租约认领 (claim)\n    running --> retry_wait: 可重试失败 (retry)\n    retry_wait --> running: 下次尝试重新认领\n    running --> waiting_user: 等待用户输入 (卡片)\n    waiting_user --> queued: 用户回复恢复 (resume)\n    running --> finalizing: 输出完成前锁定 (markFinalizing)\n    finalizing --> completed: 全部输出已投递\n    running --> interrupted: 已投递副作用/手动中断\n    finalizing --> interrupted: 已投递副作用/手动中断\n    queued --> interrupted: 启动期栅栏\n    retry_wait --> interrupted: 启动期栅栏\n    waiting_user --> interrupted: 启动期栅栏\n    running --> failed: 明确失败\n    finalizing --> failed: 明确失败\n    running --> cancelled: 取消\n    finalizing --> cancelled: 取消\n    completed --> [*]\n    failed --> [*]\n    interrupted --> [*]\n    cancelled --> [*]\n```\n\nTurn 的**幂等键**由逻辑输入派生而非随机 UUID：`channel-turn-v1:provider:accountId:externalMessageId:agentId`，`runId = turn_<sha256 前 32 位>`。同一逻辑输入（同消息 + 同 Agent）在崩溃重放时永远解析到同一行，`attempt` 递增但 `idempotency_key` 不变，因此 provider 幂等历史得以保留。Sources: [channel-turn-runtime.ts](src/channel-turn-runtime.ts#L50-L58)、[channel-turn-runtime.ts](src/channel-turn-runtime.ts#L81-L91)\n\n### 执行处置（executionDisposition）\n\n`ChannelTurnRuntime.start()` 是一个**确定性重放入口**：无论进程已死还是存活，只要携带同一逻辑输入，它都会命中同一 `turn_runs` 行。start 后通过 `executionDisposition` 将调用方引导到四种路径：\n\n| 处置 | 条件 | 调用方行为 |\n|---|---|---|\n| `execute` | 成功认领租约 | 正常执行 Agent 循环 |\n| `defer` | 行非终态但租约被他人持有 | 等待下次尝试 |\n| `skip_terminal` | 行已是终态 | 直接跳过，返回已有结果 |\n| `manual_reconciliation` | 行被标记为需人工对账 | 发送对账通知，绝不自动重放 |\n\nSources: [channel-turn-runtime.ts](src/channel-turn-runtime.ts#L93-L129)、[channel-turn-runtime.ts](src/channel-turn-runtime.ts#L148-L160)\n\n### 已投递副作用栅栏（Delivered-Effect Fence）\n\n这是整个状态机最关键的崩溃安全设计。`interruptChannelTurnRunWithDeliveredEffect` 在**每次新认领之前**检查：若该 Turn 已存在 `channel_outbox.status = \'delivered\'` 或 `streaming_cards.status = \'completed\'` 的行，则立即将 Turn 置为 `interrupted` 并写入 `[manual_reconciliation]` 前缀的错误——因为"Provider ACK 已持久化但 Turn 尚未完成"意味着崩溃发生在 ACK 写入与完成事务之间，此时重放 Agent 循环会产生**同一条消息的兄弟副本**。Sources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L1152-L1193)\n\n启动期的批量版本 `interruptChannelTurnRunsWithDeliveredEffects` 只处理**租约已过期**的行（未过期的 running/finalizing 行可能仍由第二个服务实例持有，不得覆盖）。Sources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L1195-L1230)\n\n### 完成条件与等待用户\n\n`completeChannelTurnRun` 对 `completed` 状态有硬性前置：该 Turn 下不得存在任何非终态 Outbox 行（`pending / claimed / retry_wait / uploading / uploaded / sending / uncertain`）。换句话说，**Turn 只有在全部输出都落定后才能宣告完成**——这保证 `completed` 是用户可见结果的可靠证明。Sources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L1433-L1480)\n\n流式卡片场景下，Turn 还支持 `waiting_user` 挂起（卡片等待用户输入）与 `resumeWaitingChannelTurn` 恢复（按 `revision` CAS），恢复后重新认领租约继续执行。Sources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L1344-L1387)\n\n## Outbox：出站幂等与不确定投递\n\n### 状态机\n\n`ChannelOutboxStatus` 是三层中最精细的状态机，其核心目标是区分"安全重试"与"结果未知"：\n\n```mermaid\nstateDiagram-v2\n    [*] --> pending: 入队 (enqueue)\n    pending --> claimed: 租约认领\n    claimed --> uploading: 上传开始 (upload_then_send)\n    uploading --> uploaded: 上传完成\n    uploaded --> sending: 发送开始\n    claimed --> sending: 单阶段直接发送\n    sending --> delivered: Provider ACK 持久化\n    claimed --> retry_wait: 明确拒绝 (可重试)\n    uploading --> retry_wait: 明确拒绝\n    uploaded --> retry_wait: 明确拒绝\n    sending --> failed: 明确拒绝 (不可重试)\n    sending --> uncertain: 普通错误/ACK 丢失\n    claimed --> retry_wait: 租约过期回收\n    uploading --> retry_wait: 租约过期回收\n    uploaded --> retry_wait: 租约过期回收\n    sending --> uncertain: 租约过期回收\n    delivered --> [*]\n    failed --> [*]\n    cancelled --> [*]\n    uncertain --> delivered: 人工裁决已送达\n    uncertain --> failed: 人工裁决未送达\n```\n\nSources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L51-L63)\n\n### 语义幂等身份\n\nOutbox 的幂等键**刻意排除 `requestId`**：模型/工具重试会分配新的 UUID，若以它为键则重试必然产生重复副作用。`semanticChannelOutboxIdentity` 从可见副作用本身推导身份——路由 + kind + payload 的稳定 SHA-256 哈希；`stableChannelOutboxOrdinal` 再从该身份派生出确定性的正整型 `ordinal`（SQLite 安全、跨重试稳定）。Sources: [channel-outbox-runtime-scope.ts](src/channel-outbox-runtime-scope.ts#L132-L163)\n\n飞书能力（feishu capability）的每条 mutation 即一行语义 Outbox：`deliverFeishuCapabilityMutation` 将 operation+params 打包为 payload，同一语义操作的重试（新 requestId）命中同一行。Sources: [feishu-capability-outbox.ts](src/feishu-capability-outbox.ts#L47-L64)\n\n### 物理投递事务\n\n`deliverChannelOutboxItem` 将一次物理发送拆为**逐阶段持久化**的检查点事务：enqueue → claim →（uploading → uploaded）→ sending → delivered。每个阶段之间 `notifyPersisted` 钩子使故障注入测试可模拟任意崩溃点。两个关键错误类：\n\n- **`DefinitiveChannelDeliveryError`**：Provider 显式拒绝（如 4xx、明确错误码），已知不会产生可见消息，安全地进入 `retry_wait`（带 `retryAt`）或 `failed`。\n- **普通错误（sending 之后）**：超时/断连无法证明 Provider 未接受消息，一律栅栏为 `uncertain`，**绝不自动重试**——注释原文：*"Only an explicit rejection is safe to retry/fail without creating duplicate visible output."*\n\nSources: [channel-outbox-delivery.ts](src/channel-outbox-delivery.ts#L68-L87)、[channel-outbox-delivery.ts](src/channel-outbox-delivery.ts#L383-L403)\n\n### uncertain 的整 Turn 栅栏\n\n一条 `uncertain` 行会**栅栏整个 Turn**：`getUncertainChannelOutboxForTurn` 被 `ChannelTurnRuntime.complete`、`deliverScopedChannelOutput`、`deliverChannelOutboxItem` 三处共同调用——任何新兄弟输出在 uncertain 对账前都被拒绝，因为 Provider 可能已经接受了那条消息。同时 `runtime.interrupt()` 立即失效租约 token 并置 `interrupted`，防止后续任何自动重放。Sources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L1685-L1725)、[channel-turn-runtime.ts](src/channel-turn-runtime.ts#L227-L248)\n\n### 人工对账出口\n\nuncertain 行的唯一释放路径是**人工对账**。监控路由提供两个端点：`GET /api/status/channel-outbox/uncertain` 列出待对账项（刻意省略 payload 以保护用户内容），`POST /api/status/channel-outbox/:id/resolve` 接受 `delivered`（需提供 providerMessageId）或 `failed` 裁决，且必须携带 `expectedRevision` 做 CAS——裁决失败返回 409 要求重读当前 revision，绝不盲目重试。Sources: [routes/monitor.ts](src/routes/monitor.ts#L361-L480)\n\n## Streaming Card：Turn 的流式投影\n\n流式卡片是 Turn 的一个**持久化投影**：`streaming_cards` 表记录卡片的创建、流式更新与终结，使卡片生命周期可跨进程恢复。关键设计：\n\n- **确定性保留**：主卡片 id 由 `stream_<digest(runId:primary)>` 派生，`reserveStreamingCard` 在 Provider 创建前先落库；若已有进程保留了同一卡片，调用方不得再创建第二张活动卡。\n- **回滚保护**：`rollbackUnpublishedStreamingCardReservation` 只允许回滚**未到达 Provider** 的本地保留；一旦发现 `messageId`/`cardId` 已存在，拒绝回滚并保留现场供对账。\n- **恢复分类**：恢复器对无 Provider 身份（既无 messageId 也无 cardId）的卡片直接终态化为 `failed` 并中断对应 Turn（`missing_provider_identity` → 人工对账）；有身份的则调用 `reconcileStreamingCard` 按 `cardkit` 或 `message_patch` 方法收尾为 `aborted`。\n\nSources: [channel-turn-runtime.ts](src/channel-turn-runtime.ts#L162-L217)、[channel-reliability-recovery.ts](src/channel-reliability-recovery.ts#L82-L156)\n\n## 崩溃恢复与启动编排\n\n### 启动顺序\n\n恢复顺序是**正确性边界**，注释明确为：*"exact Bot account ready → close cards left live by the old process → invalidate expired execution fences → only then resume Agent work"*——提前启动消息循环会在 Provider 仍显示旧卡片运行时创建第二张活动卡：\n\n```mermaid\nsequenceDiagram\n    participant M as imManager\n    participant R as Recovery\n    participant S as Store\n    participant Q as Queue/MessageLoop\n\n    M->>M: deferInbound()（入站暂停）\n    M->>M: reload 所有渠道账号\n    M->>R: reconcileChannelReliabilityOnStartup\n    R->>S: 关闭遗留流式卡片（reconcileStreamingCard）\n    R->>S: interruptChannelTurnRunsWithDeliveredEffects（启动批量栅栏）\n    R->>S: interruptExpiredChannelTurnRuns（过期执行租约）\n    M->>S: reconcileChannelOutboxDeliveries（claimed→retry_wait, sending→uncertain）\n    M->>R: startChannelReliabilityRecoveryLoop（15s 周期）\n    M->>M: resumeDeferredInbound()（恢复入站）\n    M->>Q: 恢复消息循环\n```\n\nSources: [index.ts](src/index.ts#L21340-L21382)\n\n### 恢复规则\n\n崩溃恢复将每张表的非终态行按"崩溃点语义"归类：\n\n| 崩溃点证据 | 恢复动作 | 依据 |\n|---|---|---|\n| Inbox `processing` 且租约过期 | 可被重新认领（attempt+1） | [channel-reliability-store.ts](src/channel-reliability-store.ts#L802-L803) |\n| Outbox `claimed/uploading/uploaded` 且租约过期 | → `retry_wait`（Provider 未收到，安全重试） | [channel-reliability-store.ts](src/channel-reliability-store.ts#L1951-L1985) |\n| Outbox `sending` 且租约过期 | → `uncertain`（发送已开始，结果不可知） | 同上 |\n| Turn `running/finalizing` 且租约过期 | → `interrupted` + 人工对账（副作用不可知，不自动重放） | [channel-reliability-store.ts](src/channel-reliability-store.ts#L1483-L1501) |\n| Turn 存在已投递副作用 | → `interrupted`（防兄弟副本） | [channel-reliability-store.ts](src/channel-reliability-store.ts#L1195-L1230) |\n\n### 启动与周期恢复的边界\n\n启动恢复（`mode: \'startup\'`）有权处理全部遗留行；周期恢复（`mode: \'live\'`，15 秒间隔）则受 **boot epoch 栅栏**约束——只处理启动前创建的行（`createdBefore`），避免正常 30 秒以上的长响应被 15 秒恢复定时器误判为崩溃。同时，任何持有**未过期租约**的行在两种模式下都被 defer（`hasActiveLease` 检查），租约过期后才会被回收。Sources: [channel-reliability-recovery.ts](src/channel-reliability-recovery.ts#L50-L69)、[channel-reliability-recovery.ts](src/channel-reliability-recovery.ts#L224-L261)\n\n## 进程内协调层\n\n持久化层之外，还有两个**进程本地**注册表负责将"正在执行的运行时对象"与"持久化行"绑定：\n\n- **`ActiveChannelTurnRegistry`**：Host 持有的当前输入 Turn 注册表，Runner IPC 认领仅在 correlationId 匹配时被接受，防止旧 IPC 消息借用新 Turn 的上下文。Sources: [channel-turn-registry.ts](src/channel-turn-registry.ts#L16-L54)\n- **`ActiveChannelOutboxScopeRegistry`**：进程内 Outbox 作用域桥。每个 scope 绑定携带随机 `token`，`unbind` 是 **token CAS**——迟到的 finally 永远不会误删复用同一运行键的新 Turn 作用域。`deliverScopedChannelOutput` 在 Turn 进入终态后拒绝一切迟到副作用（"output window is closed"）。Sources: [channel-outbox-runtime-scope.ts](src/channel-outbox-runtime-scope.ts#L25-L30)、[index.ts](src/index.ts#L2558-L2611)\n- **输出相关性**：`resolveContainerOutputInputTurnId` 按 `inputTurnId → 最后一个 IPC receipt.deliveryId → coldStartInputTurnId` 的优先级解析不可变输入身份，绝不查询可变的 "latest" 状态。Sources: [channel-output-correlation.ts](src/channel-output-correlation.ts#L6-L16)\n\n## 运维与数据治理\n\n两个运维工具封闭了状态机的生命周期：\n\n- **`scanChannelReliabilityNonterminal`**：跨四张表扫描全部非终态行（Inbox/Turn/Outbox/卡片），支撑健康检查与审计。\n- **`cleanupChannelReliability`**：两阶段治理——先对终态行**脱敏**大体积负载（Inbox 原始/归一化 payload、Outbox payload、Turn result、卡片 snapshot 置 NULL，保留幂等收据）；再按时间窗**删除**终态行（`recordsBefore` 应超过 Provider 重放窗口），删除时显式跳过仍挂有非终态 Outbox/卡片的 Turn，并依赖外键级联清理子行。\n\nSources: [channel-reliability-store.ts](src/channel-reliability-store.ts#L2338-L2482)\n\n## 总结与延伸阅读\n\n渠道可靠性状态机的本质可以浓缩为一句话：**Inbox 保证"每条输入只执行一次"，Turn 保证"每次执行可确定性重放、终态可信"，Outbox 保证"每个可见输出要么有 ACK、要么被人工裁决"**。三者通过外键级联与 `[manual_reconciliation]` 错误码串联，将 IM 渠道的弱 ACK 语义转化为可审计、可恢复、可人工干预的强状态。\n\n- 状态机落库的迁移机制见 [SQLite Schema 与数据库迁移机制](12-sqlite-schema-yu-shu-ju-ku-qian-yi-ji-zhi)\n- 飞书会话如何产生 Inbox 输入见 [飞书会话语义与原生话题映射](15-fei-shu-hui-hua-yu-yi-yu-yuan-sheng-hua-ti-ying-she)\n- Turn 如何进入队列执行见 [会话队列与并发控制](18-hui-hua-dui-lie-yu-bing-fa-kong-zhi)\n- Runner 崩溃后的恢复编排见 [Runner 生命周期、卡死检测与故障恢复](21-runner-sheng-ming-zhou-qi-qia-si-jian-ce-yu-gu-zhang-hui-fu)\n- 状态机的契约测试体系见 [测试体系：单元测试、契约测试与真实模型冒烟](29-ce-shi-ti-xi-dan-yuan-ce-shi-qi-yue-ce-shi-yu-zhen-shi-mo-xing-mou-yan)';export{n as default};

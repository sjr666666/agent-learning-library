const e='HappyClaw 作为自托管多用户系统，管理着四类敏感凭据：Claude Provider 密钥（Anthropic API Key、OAuth Token）、IM 渠道账号凭据（飞书 AppSecret、Telegram Bot Token 等）、MCP 服务器密钥（env / headers）以及 Web 会话凭据。本文剖析其"单主密钥 + AES-256-GCM 静态加密 + 文件权限纪律 + API 永不回读 + 日志脱敏"的分层防护体系，并说明凭据如何安全投递到容器运行时。\n\n## 凭据全景：共享主密钥与三类密文\n\n系统只有一个加密主密钥 `claude-provider.key`（32 字节随机 hex），由 `runtime-config.ts`（Provider 密钥与 IM 通道配置）和 `channel-account-secrets.ts`（渠道账号密钥）共同持有；Web 会话签名使用独立的 `session-secret.key`。MCP 服务器密钥与容器运行时 env 则走"明文 JSON + 0o600 权限"的差异化路线——它们是运行期必需、且无法用单一主密钥封装后仍保持透明可挂载的形态，因此以严格文件权限替代静态加密。\n\n```mermaid\ngraph TB\n    subgraph 密钥库["DATA_DIR/config（0o700）"]\n        KEY["claude-provider.key<br/>32B hex · 0o600"]\n        SKEY["session-secret.key<br/>0o600"]\n    end\n    subgraph 加密存储["AES-256-GCM 密文"]\n        P1["claude-provider.json<br/>Provider 密钥"]\n        P2["config/channel-accounts/*.json<br/>渠道账号密钥"]\n    end\n    subgraph 权限保护["明文 + 0o600"]\n        P3["mcp-servers/{uid}/secrets.json"]\n        P4["config/container-env/*.json"]\n    end\n    KEY --> P1\n    KEY --> P2\n    SKEY -->|HMAC-SHA256| SESSION["Session Cookie"]\n    P1 -->|decrypt| RUNTIME["容器 env / .credentials.json"]\n    P3 -->|读时合并| RUNTIME\n    P4 --> RUNTIME\n```\n\n各类凭据的存储形态与防护策略可归纳为下表：\n\n| 凭据类型 | 存储位置 | 静态加密 | 权限 | 读取时机 |\n|---|---|---|---|---|\n| Provider 密钥 | `config/claude-provider.json` | AES-256-GCM | 0o600 | 构建容器 env 时解密 |\n| 渠道账号密钥 | `config/channel-accounts/{id}.json` | AES-256-GCM | 0o600 | 账号连接/CLI 绑定时解密 |\n| MCP 密钥 | `mcp-servers/{userId}/secrets.json` | 明文（定义分离） | 0o600 | 读时合并进运行配置 |\n| 会话密钥 | `config/session-secret.key` | 明文 | 0o600 | 进程启动时加载 |\n\nSources: [channel-account-secrets.ts](src/channel-account-secrets.ts#L7-L13)、[runtime-config.ts](src/runtime-config.ts#L62-L76)、[mcp-utils.ts](src/mcp-utils.ts#L37-L67)\n\n## AES-256-GCM 加密存储格式\n\n所有加密载荷采用统一的 `EncryptedSecrets` 结构：随机 12 字节 IV（GCM 推荐 nonce 长度）、认证标签 tag 与密文 data 均以 base64 存储，并携带 `version` 字段以支持未来格式演进。解密时必须先 `setAuthTag(tag)` 再 `final()`——GCM 的认证标签保证密文在磁盘上被篡改（或备份被部分回滚）时解密直接抛错，而非产出损坏的明文 JSON。\n\n```typescript\ninterface EncryptedSecretFile {\n  version: 1;\n  iv: string;    // base64\n  tag: string;   // base64, AES-GCM auth tag\n  data: string;  // base64 ciphertext\n  updated_at: string;\n}\n```\n\n`saveChannelAccountSecret` 每次写入都生成全新 IV 与 tag，同一条目多次更新不会产生可关联的密文模式。写入路径遵循严格的权限纪律：先以 0o600 创建临时文件，`rename` 原子替换后再次 `chmodSync`——注释明确指出 APFS 上 mode 可能不随 inode 跟随，rename 后补一次 chmod 是必要的防御边角。`runtime-config.ts` 的 `writeSecretFile` 更进一步：先 unlink 残留 tmp 文件（避免 `fs.writeFileSync` 复用旧 mode 把新密文落到 0o644），再用 `fs.openSync(..., 0o600)` 强制创建。\n\nSources: [channel-account-secrets.ts](src/channel-account-secrets.ts#L61-L91)、[runtime-config.ts](src/runtime-config.ts#L22-L60)、[runtime-config.ts](src/runtime-config.ts#L705-L719)\n\n## 主密钥生命周期与并发安全\n\n主密钥的创建是双进程竞争的焦点：`getOrCreateKey` 先以 0o700 创建配置目录，若密钥文件不存在，则写入带 `wx` 标志（O_EXCL）的临时文件，再通过 `link(2)` 原子发布到最终路径。`link` 与 `rename` 的关键差异在于**不可覆盖**——若另一进程已抢先创建密钥，`link` 返回 `EEXIST`，当前进程转而读取已存在的密钥并复用。finally 块清理临时文件，确保任何失败路径都不残留明文密钥副本。\n\n会话密钥 `session-secret.key` 的创建遵循三级优先级：环境变量 `WEB_SESSION_SECRET`（允许容器/运维覆盖）→ 文件持久化密钥（跨重启存活）→ 进程内生成（写文件失败时不阻断启动，但密钥不跨重启）。三处密钥文件（`claude-provider.key`、`session-secret.key`、渠道账号密钥目录）统一落在 0o700 的 `config` 目录内，阻断同主机其他本地账号的读取。\n\n备份策略上，`backup-manifest.mjs` 将 `config` 作为整体组件纳入备份——这意味着**密钥文件与密文一起备份**。这是刻意的取舍：备份恢复后密文可解，运维无需单独保管密钥；对应的安全边界是"备份文件本身必须按敏感数据对待"。\n\nSources: [channel-account-secrets.ts](src/channel-account-secrets.ts#L18-L49)、[config.ts](src/config.ts#L55-L87)、[backup-manifest.mjs](scripts/backup-manifest.mjs#L5-L16)\n\n## 渠道账号与 Provider 密钥\n\n渠道账号密钥通过 `secret_ref`（形如 `channel-account:{accountId}`）间接引用，数据库仅存引用字符串而绝不落凭据。引用解析受 `SAFE_REF_RE` 严格校验（仅允许字母数字与 `-`/`_`），从根本上杜绝路径穿越。每个账号一个独立密文文件，账号登出时 `deleteChannelAccountSecret` 精准删除该账号凭据——测试验证了登出一个微信账号不会影响同 Provider 其他账号的凭据。\n\nProvider 密钥（`anthropicAuthToken`、`anthropicApiKey`、`claudeCodeOauthToken`、OAuth credentials）在 `claude-provider.json` 中整体加密。`normalizeSecret` 对输入做两层清洗：剥离非 ASCII 字符并压缩空白——`ANTHROPIC_AUTH_TOKEN` 允许 `Bearer ` 前缀（内部空白折叠为单空格），其余密钥类型删除全部空白，防止误粘贴的空格破坏认证。`PUT /api/config/claude/providers/:id/secrets` 采用"只增改、显式清除"的 API 语义：前端传新值则更新，传 `clearAnthropicApiKey: true` 等标志才清除对应字段，每次变更写入审计日志。\n\nAPI 响应用 `maskSecret` 脱敏：保留前 3 后 4 字符，中间以星号填充（短值保留末 2 位）；QQ 的 appSecret 另有独立的 `maskQQAppSecret`（前 4 后 4）。所有 GET 端点只返回 `hasXxx` 布尔与 masked 值，明文只存在于服务端内存。\n\nSources: [channel-account-secrets.ts](src/channel-account-secrets.ts#L14-L16)、[channel-account-migration.ts](src/channel-account-migration.ts#L42-L73)、[runtime-config.ts](src/runtime-config.ts#L515-L530)、[runtime-config.ts](src/runtime-config.ts#L2025-L2052)、[routes/config.ts](src/routes/config.ts#L1133-L1233)\n\n## MCP 密钥：定义与秘密分离\n\nMCP 服务器配置经历了从"env/headers 内嵌进 servers.json"到"定义与秘密分离"的迁移。当前形态下，`servers.json` 只含命令、URL、启停状态等非敏感定义；`secrets.json` 以 0o600 独立存放 env 与 headers。读取时 `readStoredUserMcpServers` 检测 legacy 内嵌字段并触发一次性迁移：新秘密文件先落盘并 chmod，之后才把旧值从定义文件中清除——**密文副本先于明文清除**，崩溃也只会产生临时重复，绝不丢失唯一副本。\n\n迁移由跨进程文件锁保护：`.secret-migration.lock` 记录 owner 的 token、PID 与进程启动时间，`isMigrationLockOwnerAlive` 通过 signal-0 探测 + `/proc/{pid}/stat` 启动时间比对（防 PID 复用误判），加锁前对 inode 指纹做双重快照比对，杜绝"把新锁误当旧锁回收"的经典 stale-lock 缺陷。迁移过程还强制修正 system 级 MCP 的 `memberAccess` 缺省值为 `admin_only`——旧或畸形的系统定义**绝不**仅凭其命令/URL/秘密内容对成员可见，这是 fail-closed 的显式策略持久化。\n\n运行时，`loadUserMcpServers` 把 secrets 合并进临时对象完成校验与投影，但**从不把秘密写回定义文件**。API 层 `toMcpServerSummary` 只暴露 `envKeys`/`headerKeys` 键名与 `hasEnvSecrets` 布尔，注释明确"Secret values are never returned"；system MCP 对非管理员只显示 enabled/type 等最小定义，键名都不泄露。\n\nSources: [mcp-utils.ts](src/mcp-utils.ts#L120-L299)、[mcp-utils.ts](src/mcp-utils.ts#L300-L362)、[routes/mcp-servers.ts](src/routes/mcp-servers.ts#L240-L290)、[routes/mcp-servers.ts](src/routes/mcp-servers.ts#L318-L345)\n\n## 会话凭据：bcrypt 与 HMAC 签名\n\n登录口令以 bcrypt 12 rounds 哈希存储（`hashPassword`/`verifyPassword`），配合双维度登录限流：per-username:ip 与全局 per-username 计数器（阈值 4 倍、窗口 1 小时），成功登录只清 per-IP 记录而保留全局计数，防止攻击者借已知 IP 重置限流。\n\n会话 token 为 `randomBytes(32)` hex，签名采用 HMAC-SHA256（密钥即 `WEB_SESSION_SECRET`），验证时用 `timingSafeEqual` 常数时间比较。Cookie 属性为 `HttpOnly; SameSite=Strict; Path=/`，HTTPS 下切换为 `__Host-` 前缀并要求 `Secure`（阻止 domain 前缀攻击与子域覆盖）。`ALLOW_LEGACY_UNSIGNED_COOKIE` 是默认关闭的迁移开关：仅接受 64 位 hex 形态的旧 cookie，验证后通过 Set-Cookie 透明升级为签名版。设计意图明确——数据库/备份/日志泄漏 raw token 后，未签名 cookie 使 HMAC 防护形同虚设。\n\nSources: [auth.ts](src/auth.ts#L11-L37)、[auth.ts](src/auth.ts#L43-L93)、[auth.ts](src/auth.ts#L134-L190)、[middleware/auth.ts](src/middleware/auth.ts#L50-L140)\n\n## 日志与 Bug 报告脱敏（防御纵深）\n\n日志系统是凭据泄漏的最后防线，分三层防御：\n\n**第一层 `redactLogMessage`（格式优先）**：先匹配 `Bearer/Basic` 方案值、`sk-`/`gh_` 前缀 token、JWT 三段式结构等已知凭据格式；再用关键词正则覆盖 `authorization|cookie|token|secret|api_key` 等赋值形态；最后处理 URL userinfo 中的密码。格式优先于关键词——否则 `Authorization: Bearer <token>` 会被关键词正则截到空格处，只遮住 `Bearer` 留下真 token。\n\n**第二层 `serializeErrorForLog`（白名单提取）**：Axios/Lark SDK 会把整个 HTTP 请求挂在 Error 上（含 `config.headers` 与 `request._header` 的原始 Authorization）。此函数只提取 status、code、provider 诊断字段与 stack，`config`、`request`、`response`、`headers` 对象**永不跨日志边界**；提取后的字段再过一遍 `redactLogMessage`。\n\n**第三层 `sanitizeLogObject` + pino redact paths**：结构化日志逐键递归清洗，`isSensitiveLogKey` 把 token/secret/apiKey 等键值直接替换为 `[REDACTED]`；pino 的 path redaction 作为纵深覆盖 `*.config.headers.authorization`、`*.response.request._header` 等深度路径。\n\nBug 报告通道独立实现 `sanitizeLogs`：先替换项目根/家目录绝对路径，再按行截断至 2000 字符（防止关键词正则在 ~100KB 超长行上 O(n²) 回溯卡死事件循环），同样采用"格式脱敏先行"策略，并允许未闭合引号内的截断值整体遮蔽——`"app_secret": "cli_abc` 这类切片中断的 JSON 值仍被完整掩码。\n\nSources: [logger.ts](src/logger.ts#L50-L79)、[logger.ts](src/logger.ts#L86-L152)、[logger.ts](src/logger.ts#L154-L280)、[routes/bug-report.ts](src/routes/bug-report.ts#L130-L215)\n\n## 运行时秘密投递\n\n容器运行时的凭据投递遵循"最小可见性"原则，三个机制各司其职：\n\n**容器 env 文件**：Provider 密钥解密后写入 `DATA_DIR/config/container-env/{folder}/env`，以 0o600 创建并以只读挂载进容器。注释点明动机——**把凭据移出进程列表**，因为环境变量会暴露在 `/proc/PID/environ` 中；env 文件按 group/agent/task/渠道账号维度隔离（`getContainerRuntimeEnvDir`），任务结束时 `cleanupContainerTaskRuntimeEnvDirs` 清理对应快照。env 写入前还会剔除 Agent 策略中已显式覆盖的键，保证策略权威。\n\n**OAuth `.credentials.json`**：写入已挂载的会话目录，切换第三方 Provider 时主动删除残留凭据，防止 SDK 误检测到上一轮的官方 OAuth token 而走错认证路径。\n\n**Workspace Memory 写能力（HMAC capability）**：`workspaceMemoryMutationSigningSecret` 在容器启动时经 stdin bootstrap 注入（明确禁止出现在环境变量、文件、日志或 IPC 中）。宿主侧 `issueWorkspaceMemoryWriteCapability` 生成随机 base64url 密钥并登记 admittedTurnId，`verifyWorkspaceMemoryCapability` 用 HMAC-SHA256 + `timingSafeEqual` 验证签名，且**消费型校验**记录已用签名防重放；runner 结束时按实例 ID 精确吊销，不影响并发的后继进程。feishu-cli 绑定则在解密凭据后校验禁入字符（NUL/换行），杜绝注入。\n\nSources: [container-runner.ts](src/container-runner.ts#L366-L375)、[container-runner.ts](src/container-runner.ts#L1359-L1430)、[container-runner.ts](src/container-runner.ts#L1428-L1445)、[workspace-memory-capability.ts](src/workspace-memory-capability.ts#L80-L87)、[workspace-memory-capability.ts](src/workspace-memory-capability.ts#L142-L190)、[feishu-cli-runtime.ts](src/feishu-cli-runtime.ts#L46-L48)\n\n## 边界验证与测试保障\n\n凭据管理的每条边界都有契约测试锚定，防止回归：\n\n| 测试 | 验证的边界 |\n|---|---|\n| `mcp-runtime-secret-boundary` | 成员运行时只收到显式共享的 system MCP；admin-only 系统密钥的键名都不泄露；custom 策略选择受限 MCP 时显式失败 |\n| `routes-mcp-server-secrets` | 首次读取原子迁移 legacy 内嵌密钥且幂等；`secrets.json` 权限恒为 0o600；API 响应与定义文件均不含明文 |\n| `channel-account-onboarding` | 微信 botToken 不出现在任何 API 响应；登出只撤销目标账号凭据；禁用后 QR 确认结果被拒收 |\n| `logger-security` | `Bearer`/关键词/URL 密码/嵌套错误对象中的凭据全部脱敏；`request._header` 等深度路径被清洗 |\n\nSources: [mcp-runtime-secret-boundary.test.ts](tests/mcp-runtime-secret-boundary.test.ts#L172-L396)、[routes-mcp-server-secrets.test.ts](tests/routes-mcp-server-secrets.test.ts#L35-L204)、[channel-account-onboarding.test.ts](tests/channel-account-onboarding.test.ts#L150-L270)、[logger-security.test.ts](tests/logger-security.test.ts#L1-L144)\n\n## 相关阅读\n\n- 凭据用于驱动渠道运行时，参见 [IM 渠道架构与多账号连接池](13-im-qu-dao-jia-gou-yu-duo-zhang-hao-lian-jie-chi)\n- 密钥文件与密文随数据目录备份，参见 [数据目录结构与一致性备份恢复](23-shu-ju-mu-lu-jie-gou-yu-zhi-xing-bei-fen-hui-fu)\n- 凭据权限受 RBAC 模型约束，参见 [RBAC 权限模型与资源隔离](24-rbac-quan-xian-mo-xing-yu-zi-yuan-ge-chi)\n- 容器凭据投递与 IPC 隔离，参见 [Host 与 Container 双执行模式](7-host-yu-container-shuang-zhi-xing-mo-shi)\n- 密钥轮换与审计日志记录，参见 [主服务架构与核心模块划分](9-zhu-fu-wu-jia-gou-yu-he-xin-mo-kuai-hua-fen)';export{e as default};

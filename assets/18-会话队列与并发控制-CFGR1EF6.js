const e='## 概述：为什么需要会话级串行化\n\nHappyClaw 是一个多租户、多渠道的智能体工作台：同一个 Workspace 可能同时被 Web 会话、飞书群、Telegram 话题等多个入口触达，而每个 Workspace 最终只对应一个主 Agent Runner（Docker 容器或宿主机进程）。如果并发消息同时涌入同一会话，就会产生重复回复、孤儿容器、乱序回答等一类问题。因此系统在消息分发层之下设置了一道**会话队列（GroupQueue）**——一个进程内的单例状态机，负责把同一会话家族（serialization family）的全部工作严格串行化，同时以容量门控与计费门控约束跨会话的并发度。\n\nGroupQueue 的核心设计目标是三件事：**同一工作区内绝不并发**、**不同工作区在容量内尽快并行**、**任何安全敏感变更可以原子地暂停整条工作流**。它以 3092 行的单体实现承载了消息冷启动、IPC 热注入、任务调度、卡死恢复与优雅关停等多条路径。\n\nSources: [group-queue.ts](src/group-queue.ts#L119-L193)、[index.ts](src/index.ts#L20639-L20647)\n\n## 核心模型：GroupState 与串行化键\n\n队列的最小单元是 `GroupState`——每个 JID（渠道会话 ID）一个实例，由 `groups: Map<string, GroupState>` 持有。它同时记录运行态（`active`、`process`、`containerName`、`runnerGeneration`）、待办态（`pendingMessages`、`pendingTasks[]`）、查询态（`queryInFlight`、`queryId`、`announcedQueryId`）、重试态（`retryCount`、`retryTimer`、`messageRetrySnapshot`）以及 IPC 投递簿记（`pendingIpcDeliveries`、`acknowledgedIpcDeliveryIds`），是理解整个并发模型的关键数据结构。\n\n```mermaid\nstateDiagram-v2\n    [*] --> Idle: 注册/GC 后\n    Idle --> Running: enqueueMessageCheck / enqueueTask\\n(通过三层准入检查)\n    Running --> Draining: processMessagesFn / task.fn 完成\\nfinally 块执行\n    Draining --> Running: drainGroup 发现 pending 工作\\n(任务优先，消息次之)\n    Draining --> Idle: 无 pending + 无 retryTimer\\n(虚拟 JID 此时被 GC)\n    Running --> Stopping: stopGroup / restartGroup / shutdown\n    Stopping --> Draining: teardown 握手完成\n    Stopping --> [*]: 强制 kill 超时\n    Running --> [*]: crash / SIGKILL\n```\n\n**串行化键（serialization key）**是并发控制的根基。`setSerializationKeyResolver` 把 JID 映射为文件夹级别的键：普通渠道 JID 直接映射到 `group.folder`，因此共享同一文件夹的 Web 会话与飞书群会命中同一个活跃 runner；`#agent:` 子代理与 `#task:` 定时任务虚拟 JID 则解析为独立的键，获得自己的 runner 生命周期。`findActiveRunnerFor` 遍历所有活跃状态寻找同键 runner，`resolveActiveState` 则优先返回自身状态、否则回退到同键兄弟——这套间接层让"多个 JID 共享一个进程"与"虚拟 JID 独立运行"两种拓扑共存于同一队列。\n\nSources: [group-queue.ts](src/group-queue.ts#L269-L310)、[group-queue.ts](src/group-queue.ts#L855-L906)、[index.ts](src/index.ts#L20659-L20679)\n\n## 三层并发控制\n\n`hasCapacityFor` 是每次启动新 runner 前的唯一准入函数，它按顺序叠加三道闸门：\n\n| 层次 | 闸门 | 规则 | 适用模式 |\n|---|---|---|---|\n| 第一层 | 串行化键互斥 | 同键已有活跃 runner 则拒绝，待办交给活跃 runner 的 finally → drainGroup | Host + Container |\n| 第二层 | 全局容器容量 | `activeContainerCount < maxConcurrentContainers`（默认 20） | 仅 Container |\n| 第三层 | 用户级计费限额 | `setUserConcurrentLimitChecker` 按 owner 统计活跃容器数（含任务虚拟 JID），超过 `getUserConcurrentContainerLimit` 则拒绝 | 仅 Container（计费开启时） |\n\n**Host 模式的特殊豁免**：`hasCapacityFor` 对 host 模式直接返回 `true`——它镜像 Claude Agent SDK 的进程模型，只以串行化键为唯一边界，不做应用级容量池；测试明确验证了即使 `maxConcurrentHostProcesses=1` 且计费检查返回 `allowed:false`，不同飞书话题的 host 会话也能立即并行启动，因为「计费容量是 Docker 分配问题，不应变成隐藏的 host 进程限制」。容器模式保留显式容量与计费限制，因为每次 turn 都消耗受管的 Docker 资源。\n\nSources: [group-queue.ts](src/group-queue.ts#L873-L895)、[runtime-config.ts](src/runtime-config.ts#L3968-L3973)、[index.ts](src/index.ts#L20730-L20747)、[group-queue-host-session-concurrency.test.ts](tests/group-queue-host-session-concurrency.test.ts#L52-L89)\n\n## 入队路径与优先级\n\n消息与任务走两条平行的入队入口，但决策树完全一致：**shutdown 拒绝 → 终态变更墓碑丢弃 → 变更暂停门排队 → 已有同键活跃 runner 排队（并写 drain 哨兵）→ 容量不足排队 → 立即启动**。\n\n```mermaid\nflowchart TD\n    A[新消息 / 新任务到达] --> B{shuttingDown?}\n    B -- 是 --> X[丢弃]\n    B -- 否 --> C{terminalDiscardMutationKeys?}\n    C -- 是 --> X\n    C -- 否 --> D{mutationPaused?}\n    D -- 是 --> Q1[挂起: pendingMessages=true 或 push pendingTasks<br/>加入 waitingGroups]\n    D -- 否 --> E{同串行化键有活跃 runner?}\n    E -- 是 --> Q2[排队 + 写 _drain 哨兵<br/>由活跃 runner finally 接管]\n    E -- 否 --> F{hasCapacityFor?}\n    F -- 否 --> Q3[排队等待容量释放]\n    F -- 是 --> G[立即 runForGroup / runTask]\n    Q1 --> H[变更恢复后 drainWaiting]\n    Q2 --> I[活跃 runner 退出后 drainGroup]\n    Q3 --> J[其他 runner 释放后 drainWaiting]\n```\n\n两条入队路径的差异点：`enqueueTask` 额外做 **taskId 去重**（同 JID 下相同任务不重复入队），并携带 `allowInactive`（手动运行不受任务配置暂停影响）与 `onDropped`（排队中被丢弃时释放调用方持有的资源）两个选项。而 `drainGroup` 与 `drainWaiting` 在派发时都遵循**任务优先于消息**的策略——因为定时任务不会像消息那样从 SQLite 被重新发现，且派发前会用 `getTaskById` 核对任务是否仍为 `active`，已取消/删除的调度任务直接跳过并触发 `onDropped`，而动态任务（如 `#agent:` 会话代理）无 DB 记录则始终放行。\n\nSources: [group-queue.ts](src/group-queue.ts#L1331-L1389)、[group-queue.ts](src/group-queue.ts#L1391-L1487)、[group-queue.ts](src/group-queue.ts#L2773-L2849)、[group-queue.ts](src/group-queue.ts#L2856-L2914)\n\n## 变更暂停门：Mutation Pause\n\n这是全系统最精妙的一层并发控制。当发生 Agent 能力变更（Skills/MCP/Plugins 增删改）、凭据刷新或工作区删除等**安全敏感操作**时，`pauseGroupsForMutation` 在变更的第一次 await 之前**同步原子地**把整个串行化家族的所有工作停住：按 `mutationKey`（通常是文件夹的串行化键，同时维护 `mutationBaseKeyAliases` 映射以覆盖虚拟 JID）计数暂停，返回一个 `MutationPauseToken`。暂停期间新到达的消息与任务照常入队但绝不启动；`sendMessage` 热注入也会因 `isMutationPaused` 直接返回 `no_active`，从源头杜绝"旧运行时代理收到新策略消息"的窗口。\n\n| 方法 | 语义 | 队列处置 |\n|---|---|---|\n| `pauseGroupsForMutation` | 原子暂停一批家族 | 计数 +1，全部工作原地挂起 |\n| `resumeGroupsAfterMutation` | 计数 -1，清零后放行 | 保留的待办重新加入 waitingGroups 并 drain |\n| `discardGroupsAfterMutation` | 终态删除（工作区已不存在） | 丢弃待办、清 retry、写入**进程级墓碑**，墓碑在计数归零后依然拒绝迟到的工作 |\n\n`runtimeSafetyBlocks` 是另一层**故障关闭（fail-closed）**门：当安全敏感变更已提交但旧运行时无法确认停止时，按 source 独立记账阻止重启，任何单一子系统都无法解除其他子系统的封锁。墓碑与别名映射特意跨过最终 token 释放存活到进程结束，确保通过了旧 DB 检查的迟到调度回调仍被拒绝。\n\nSources: [group-queue.ts](src/group-queue.ts#L358-L399)、[group-queue.ts](src/group-queue.ts#L446-L522)、[group-queue.ts](src/group-queue.ts#L528-L568)、[group-queue-mutation-pause.test.ts](tests/group-queue-mutation-pause.test.ts#L49-L120)\n\n## 重试与指数退避\n\n`runForGroup` 调用 `processMessagesFn` 失败（返回 `false` 或抛异常）且 `canScheduleRetry`（非关停、非 stop、非 restart）成立时进入 `scheduleRetry`：`retryCount` 递增，延迟为 `5000 × 2^(retryCount-1)` 毫秒（5s → 10s → 20s → 40s → 80s），超过 `MAX_RETRIES = 5` 后调用 `onMaxRetriesExceededFn` 并向用户发送失败通知。三个关键细节：\n\n1. **不可变批次围栏**：`setMessageRetrySnapshot` 把重试边界钉死在本次尝试读到的 DB 游标批次上；重试耗尽回调只消费该批内的精确消息，游标推进也以批终值为界，之后到达的消息保持 pending——防止一条新消息被错误标记为失败批次的一部分。\n2. **上下文溢出跳过**：`markContextOverflow` 标记的组直接跳过退避（agent 内部已自行重试过 3 次），下次运行即清标记。\n3. **中断不归零**：`interruptQuery` 只 `cancelRetryTimer` 不重置 `retryCount`，否则 N 次失败后退避中的 runner 一被中断就回到 retry=0，会死循环重试同一失败请求；而 `runForGroup` 的新消息启动只 `cancelRetryTimer` 保留计数，成功后才归零。\n\nSources: [group-queue.ts](src/group-queue.ts#L115-L117)、[group-queue.ts](src/group-queue.ts#L622-L637)、[group-queue.ts](src/group-queue.ts#L2675-L2771)、[group-queue.ts](src/group-queue.ts#L2046-L2055)\n\n## 卡死检测与恢复管线\n\n主消息循环每 `POLL_INTERVAL` 轮询一次消息，每 15 次轮询（`STUCK_RUNNER_CHECK_INTERVAL_POLLS`）执行一次 `recoverStuckPendingGroups`。恢复管线是**快照 → CPU 探测 → 重新校验 → 决策**的四段式，每一段都在收窄误杀窗口：\n\n```mermaid\nsequenceDiagram\n    participant Loop as 主消息循环\n    participant Q as GroupQueue\n    participant Probe as CPU 探测器\n    participant Docker as 容器/进程\n\n    Loop->>Q: getStuckPendingGroups(3min, 10min)\n    Q-->>Loop: StuckRecoveryCandidate[]\n    Loop->>Probe: resolveRunnerCpuActivity(candidate)\n    Probe-->>Loop: active / inactive / unknown / unavailable\n    Loop->>Q: revalidateStuckRecoveryCandidate(candidate)\n    Q-->>Loop: 身份一致? candidate : null\n    Loop->>Loop: decideStuckRunnerRecovery\n    alt restart\n        Loop->>Q: restartGroup(jid)\n        Q->>Docker: _close 哨兵 → docker stop → 超时则 kill\n    else defer\n        Loop->>Loop: 记录 defer 原因\n    end\n```\n\n候选生成的条件是：活跃、非任务 runner、非 restarting、存在 `pendingMessages` 或有未清偿的 IPC 债务（`ipcOwedSinceAt` + `queryInFlight`），且 `lastActivityAt` 静默超过阈值。决策规则如下：\n\n| 条件 | 决策 |\n|---|---|\n| IPC 债务 ≥ 10min（绝对上限，普通输出无法刷新） | `restart / absolute_ipc_ceiling` |\n| 空闲 ≥ 10min（硬上限） | `restart / idle_ceiling` |\n| Container 运行时（`ps` 无法观察容器内 CPU） | `defer / container_grace` |\n| Host 且 CPU 活跃（后代进程 >0.5%） | `defer / cpu_active` |\n| Host 且 CPU 探测失败 | `defer / cpu_unknown` |\n| Host 且 CPU 空闲 | `restart / idle` |\n\n**TOCTOU 防护**是这段管线的点睛之笔：CPU 探测是异步的，期间旧 turn 可能完成、新 turn 可能启动；`revalidateStuckRecoveryCandidate` 在决策前同步重新快照，逐一比对 `runnerGeneration`、`queryId`、`runtime`、`runnerPid`、`reason`、`ipcOwedSinceAt`，任何身份漂移都拒绝旧决策，从根上杜绝"旧恢复决策杀死新健康 turn"。\n\nSources: [group-queue.ts](src/group-queue.ts#L1165-L1205)、[group-queue.ts](src/group-queue.ts#L1207-L1259)、[stuck-runner-recovery.ts](src/stuck-runner-recovery.ts#L142-L172)、[index.ts](src/index.ts#L17387-L17466)、[index.ts](src/index.ts#L1235-L1240)\n\n## Runner 活性与生命周期超时\n\nWarm runner（空闲窗口内保持存活、通过 IPC 管道接收新消息）依赖两套独立的时钟，`resolveRunnerLivenessTimeouts` 确保它们严格有序：`idleCloseMs = min(executionTimeoutMs, idleTimeoutMs)` 是**优雅回收线**（agent 主动收尾），`watchdogMs = max(executionTimeoutMs, idleCloseMs) + 15s` 是**外层看门狗**。历史教训是两者默认同为 30 分钟，而 stdout 看门狗在宿主完成投影输出前重置、空闲计时器在之后重置，导致看门狗必然抢先触发、把健康会话误报为超时——现在的设计让优雅回收永远领先看门狗一拍。\n\n`sendMessage` 是 warm 路径的入口：活跃 runner 存在且身份兼容（飞书 Bot 凭据是容器不可变环境，跨 Bot 身份注入被拒绝并改为 drain 冷启动）时，消息写成 IPC JSON 文件；`queryInFlight` 期间到达的消息在 `waitForIpcMessage → drainIpcInput` 处自然合并为单次 prompt（飞书转发+评论场景的聚合）。若活跃 runner 是调度任务（`activeRunnerIsTask`），用户消息一律拒绝注入返回 `no_active`，等待任务结束后冷启动，避免任务容器静默吞掉用户上下文。\n\nSources: [runner-liveness.ts](src/runner-liveness.ts#L1-L34)、[group-queue.ts](src/group-queue.ts#L1549-L1615)、[group-queue.ts](src/group-queue.ts#L1617-L1627)\n\n## 停止、重启、中断与优雅关停\n\n四个生命周期操作覆盖了从单次查询到整机下线的全部终止语义：\n\n| 操作 | 机制 | 语义 |\n|---|---|---|\n| `interruptQuery` | 写 `_interrupt` 哨兵，agent-runner 调用 `query.interrupt()` | 中断当前查询，容器存活可继续接收消息；不归零退避 |\n| `stopGroup` | 优雅 SIGTERM/docker stop → 等待 finally 握手（15s）→ 超时 SIGKILL/docker kill | 清空待办与 retry；`preserveQueuedWork` 模式保留待办供变更后重放 |\n| `restartGroup` | 写 `_close` 哨兵 → 等 2s 优雅退出 → docker stop（15s）→ 超时 kill → `enqueueMessageCheck` 重新拉起 | 卡死恢复专用；先 `clearRetryTimer` 防旧定时器与替换 runner 打架 |\n| `shutdown(grace)` | 清 retry 与待办 → 全员写 `_close` 哨兵 → 轮询 activeCount 归零 → 超时强制 stop | 全局下线的最后防线 |\n\n`stopGroup` 的一个关键设计是**等待 finally 握手而非猜测**：`teardownWaiters` 让 stop 调用方在 `runForGroup`/`runTask` 的 finally 块真正重置 `state.active` 后才返回；`recentlyStoppedFolders` 30 秒窗口则防止停止后 IPC 注入消息的重新武装把容器瞬间又拉起来，同时让 OOM 计数不会把用户主动 SIGKILL（退出码 137）误判为真 OOM。`runForGroup` 的 finally 块还负责清理 IPC 哨兵、恢复未确认/未消费的 IPC 投递，并在 `hasIpcInjectedMessages` 时重新武装 pending——除非 30 秒内刚被用户停止。\n\nSources: [group-queue.ts](src/group-queue.ts#L2046-L2075)、[group-queue.ts](src/group-queue.ts#L2082-L2211)、[group-queue.ts](src/group-queue.ts#L2216-L2310)、[group-queue.ts](src/group-queue.ts#L2976-L3090)、[group-queue.ts](src/group-queue.ts#L411-L439)\n\n## 外围并发防线\n\n会话队列是主串行化层，但事件摄入与策略变更还需要更轻量的锁来堵住队列之外的缝隙：\n\n| 防线 | 位置 | 作用 |\n|---|---|---|\n| `ProcessingLock` | QQ/Discord/Telegram 事件摄入 | 短 TTL（5min）内存锁，覆盖"事件尚未提交到去重 LRU"的在途窗口；60s 周期清扫 |\n| `isStale` | 各渠道入站过滤 | 30 分钟全局丢弃兜底，防 webhook 重试 1 小时后陈旧消息混入 |\n| `withCapabilityScopeLocks` | Agent 策略校验/提交 | 按 key 字典序获取锁避免死锁，串行化系统+用户双作用域的变更 |\n| `ExactAsyncIndicatorRegistry` | provider 资源持有 | 按精确输入消息 ID 注册（512 条上限，LRU 式淘汰最旧并释放句柄），保证资源释放恰好一次 |\n\n这些防线与 GroupQueue 的层次关系可以这样理解：ProcessingLock 挡的是"同一条事件被并发投递"，GroupQueue 挡的是"同一会话的 turn 并发执行"——前者在渠道边界、后者在工作区边界，互不替代。\n\nSources: [processing-lock.ts](src/im-safety/processing-lock.ts#L1-L51)、[stale-detector.ts](src/im-safety/stale-detector.ts#L1-L24)、[capability-lock.ts](src/capability-lock.ts#L14-L60)、[processing-indicator.ts](src/processing-indicator.ts#L21-L116)\n\n## 数据流全景\n\n```mermaid\nflowchart LR\n    subgraph Channels[渠道层]\n        FE[飞书] --> PL[ProcessingLock + isStale]\n        TG[Telegram] --> PL\n        QQ[QQ] --> PL\n        WB[Web] --> MSG[消息表/游标]\n    end\n    PL --> ML[主消息循环]\n    MSG --> ML\n    ML -->|sendMessage 热注入| Q\n    ML -->|no_active 冷启动| Q[(GroupQueue)]\n    Q -->|准入三层| R[Runner 容器/进程]\n    R -->|query idle 回调| FQ[dispatchQueuedFollowUpFamily]\n    R -->|stdout 事件| SB[StreamEvent 总线]\n    Q -->|卡死候选| RC[recoverStuckPendingGroups]\n    RC -->|restartGroup| Q\n    S[task-scheduler] -->|enqueueTask| Q\n    M[Mutation 变更] -->|pause/resume/discard| Q\n```\n\n主消息循环的完整路径是：读取 `lastAgentTimestamp` 之后的未处理消息 → 插件命令展开 → 尝试 `sendMessage` 热注入（成功则仅推进 next-pull 游标）→ 失败则 `queue.enqueueMessageCheck` 冷启动；启动恢复阶段（`recoverPendingMessages`）还会把 next-pull 游标回退到已提交游标并清除陈旧 session，防止"session ghost"——被 SIGKILL 中断的 SDK 会话恢复后继续完成旧工作而非处理新消息。\n\nSources: [index.ts](src/index.ts#L17350-L17395)、[index.ts](src/index.ts#L17484-L17536)、[index.ts](src/index.ts#L5595-L5627)\n\n## 延伸阅读\n\n- 本页聚焦队列串行化与并发门控；Runner 进程的完整生命周期（IDLE_TIMEOUT 循环、卡死恢复细节）见 [Runner 生命周期、卡死检测与故障恢复](21-runner-sheng-ming-zhou-qi-qia-si-jian-ce-yu-gu-zhang-hui-fu)\n- 定时任务如何经由 `enqueueTask` 进入本队列见 [定时任务调度与异常恢复](19-ding-shi-ren-wu-diao-du-yi-chang-hui-fu)\n- 渠道消息如何汇聚到消息表与游标见 [IM 命令系统与消息分发](17-im-ming-ling-xi-tong-yu-xiao-xi-fen-fa) 与 [渠道可靠性状态机：Inbox、Turn 与 Outbox](16-qu-dao-ke-kao-xing-zhuang-tai-ji-inbox-turn-yu-outbox)\n- Host/Container 双执行模式对本队列准入策略的影响见 [Host 与 Container 双执行模式](7-host-yu-container-shuang-zhi-xing-mo-shi)\n- GroupQueue 在整体服务中的位置见 [主服务架构与核心模块划分](9-zhu-fu-wu-jia-gou-yu-he-xin-mo-kuai-hua-fen)';export{e as default};

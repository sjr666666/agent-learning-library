const e='Miniclaw 的授权体系建立在一条反直觉的原则上：**admin 角色不是万能钥匙**。系统能力（管理用户、配置系统）与资源归属（工作区属于谁）是两条独立判定的维度，前者允许角色隐含权限，后者严格按 `created_by` 归属隔离，任何角色都不提供跨用户旁路。本文拆解这套矩阵的结构、实现与设计权衡。\n\nSources: [ACL-MATRIX.md](docs/ACL-MATRIX.md#L11-L36)\n\n## 双维度授权模型：系统能力 ≠ 资源归属\n\n理解整个 ACL 体系的起点，是区分两个正交的判定维度。**第一维是系统能力维度**：`src/permissions.ts` 定义了全部六个系统权限——`manage_system_config`、`manage_group_env`、`manage_users`、`manage_invites`、`view_audit_log`、`manage_billing`——并通过四套预置模板（管理员全权限、普通成员、运维管理员、用户管理员）批量授予。`hasPermission()` 中存在一个显式的角色捷径：`user.role === \'admin\'` 直接返回 true，即 admin 角色隐含全部系统能力。这是整个系统中唯一一处"角色即通行证"的设计，且被刻意限定在系统级检查内。\n\nSources: [permissions.ts](src/permissions.ts#L3-L10) [permissions.ts](src/permissions.ts#L12-L45) [permissions.ts](src/permissions.ts#L77-L83)\n\n**第二维是资源归属维度**，也是本文标题所指的核心。`canAccessGroup()`、`canModifyGroup()`、`canDeleteGroup()` 三个纯函数构成工作区资源的全部判定逻辑，它们的签名接受 `{ id, role }` 两个参数——但函数体从未读取过 `role` 字段。这个细节是最强力的结构性证据：admin 在工作区层面没有任何代码路径可以绕过 `created_by` 比较。Home Workspace 只允许 `created_by` 本人，Web Workspace 只允许创建者，IM Chat 优先比对自身 `created_by`。\n\nSources: [web-context.ts](src/web-context.ts#L351-L407)\n\n```mermaid\nflowchart LR\n    subgraph S1["系统能力维度（有角色捷径）"]\n        R["UserRole: admin / member"] --> P["permissions 列表"]\n        P --> H{"hasPermission()"}\n        H -->|"admin 直接通过"| OK1["系统级路由放行"]\n    end\n    subgraph S2["资源归属维度（无任何角色捷径）"]\n        U["当前用户 id"] --> C{"canAccessGroup()<br/>canModifyGroup()"}\n        G["工作区行 created_by"] --> C\n        C -->|"仅 id 相等"| OK2["资源操作放行"]\n    end\n```\n\n## 层次化权限栈：从 Public 到 IM Owner\n\n单个请求在到达业务逻辑前要穿过一组有序的关卡。`authMiddleware` 负责 Login 层：解析多套 Cookie 名（含新旧签名变体迁移场景）、校验 HMAC 会话令牌、拒绝 disabled/deleted 账号、并在 `must_change_password` 时只放行改密相关路径。通过后用户身份（含角色与权限列表）被固定到请求上下文。在此之上，`requirePermission()` 及其衍生的具名中间件（`systemConfigMiddleware`、`usersManageMiddleware` 等）承担系统权限层。\n\nSources: [middleware/auth.ts](src/middleware/auth.ts#L63-L150) [middleware/auth.ts](src/middleware/auth.ts#L152-L190)\n\n完整的层次结构如下表所示。注意两个关键点：其一，Access 层对所有角色使用同一规则；其二，Host 执行权限是**额外边界**——拥有工作区 Modify 权限不代表可以执行宿主机操作，反之 admin 有 Host 权限也不代表可以进入他人的工作区。\n\n| 层次 | 代码入口 | 判定含义 |\n| --- | --- | --- |\n| Public | 无 `authMiddleware` | 无需登录的白名单接口 |\n| Login | `authMiddleware` | 有效登录 Session |\n| Access | `canAccessGroup` | 可查看和使用该工作区 |\n| Modify | `canModifyGroup` | 当前用户是工作区 owner |\n| Delete | `canDeleteGroup` | 是 owner 且非 Home Workspace |\n| Host | `hasHostExecutionPermission` | 仅 admin 角色 |\n| System | `systemConfigMiddleware` 等 | 对应 `manage_*` 权限位 |\n| IM Owner | `owner_im_id` 比对 | 渠道原生 sender 是记录的主人 |\n\nSources: [ACL-MATRIX.md](docs/ACL-MATRIX.md#L13-L26) [web-context.ts](src/web-context.ts#L342-L349)\n\n```mermaid\nflowchart TD\n    REQ["任意入口：HTTP / WebSocket / IM / MCP-IPC"] --> GW{"是否 Public 白名单？"}\n    GW -->|"否"| LOGIN["authMiddleware：Cookie 会话校验"]\n    GW -->|"是"| DONE["直接处理"]\n    LOGIN --> RES["资源级 ACL：canAccess / canModify"]\n    RES --> SYSPERM["系统权限中间件：requirePermission"]\n    SYSPERM --> HOST["Host 边界：hasHostExecutionPermission"]\n    HOST --> IMOWN["IM 面：checkImOwnerCommand（无 Cookie 平行通道）"]\n```\n\n## 资源级三函数：Access、Modify、Delete 的归属判定\n\n三个函数共享同一套归属模型，差异仅在收紧程度。`canAccessGroup` 处理三类工作区：Home 组直接比较 `created_by`；IM 组若 `created_by` 缺失（旧数据），则回退到同 `folder` 下兄弟 Home Workspace 解析 owner——解析失败**默认拒绝**而非默认放行；Web 组同样只认创建者。`canModifyGroup` 是 owner-only 的收紧版，保留同一条兄弟回退链。`canDeleteGroup` 再加一道不变量：`is_home` 组对任何人返回 false——Home Workspace 在模型上是用户的根容器，不可删除。\n\nSources: [web-context.ts](src/web-context.ts#L376-L419)\n\n这组判定不仅保护单点操作，也定义了**列表可见性**。工作区列表接口在投影阶段逐条过滤：其他用户的 Home 组直接从侧边栏隐藏，Host 模式组除非是自己所属否则要求 admin，最后统一经过 `canAccessGroup` 兜底。列表同时输出 `can_modify` 投影字段，前端据此渲染编辑能力而无需二次猜测。\n\nSources: [routes/groups.ts](src/routes/groups.ts#L330-L354) [routes/groups.ts](src/routes/groups.ts#L363-L394)\n\n| 函数 | Home Workspace | Web Workspace | IM Chat | 失败语义 |\n| --- | --- | --- | --- | --- |\n| `canAccessGroup` | 仅 `created_by` | 仅创建者 | 自身 owner，旧数据走兄弟 Home 回退 | 回退失败默认拒绝 |\n| `canModifyGroup` | 仅 `created_by` | 仅创建者 | 同上（owner-only 收紧） | 回退失败默认拒绝 |\n| `canDeleteGroup` | 永远 false | 需 Modify | 需 Modify | Home 不可删除是不变量 |\n\nSources: [web-context.ts](src/web-context.ts#L358-L380)\n\n## 无 admin 旁路：原则、实现与刻意的例外\n\n"admin 不自动绕过工作区所有权"写在仓库权威文档的核心原则区，但真正让它成立的是三处互相印证的实现。最直白的示范是环境变量路由：`PUT /api/groups/:jid/env` 依次执行四层检查——先 `canAccessGroup`（失败返回 404），再对 Host 模式组要求 admin 角色，然后非 admin 必须同时满足 owner（`canModifyGroup`）与 `manage_group_env` 权限位。注释明确解释了动机：环境变量包含 `anthropicAuthToken` 这类能让 Agent 全部流量被劫持的字段，跨用户共享工作区中持有系统权限的非 owner 不得改写 owner 的密钥；admin 虽然豁免后两层，但**第一层工作区 Access 无法豁免**。\n\nSources: [routes/groups.ts](src/routes/groups.ts#L2512-L2546) [ACL-MATRIX.md](docs/ACL-MATRIX.md#L122-L129)\n\n第二处是 Workspace Memory v2 的服务层授权。`authorizeWorkspace()` 在读操作走 `canAccessGroup`、写操作走 `canModifyGroup` 之后，对未授权情况抛出与"资源不存在"完全相同的错误，注释原话是 *"Deliberately hide existence. Admin follows the same owner-only policy."*——连错误信息都不给 admin 区分对待。\n\nSources: [memory-service.ts](src/memory-service.ts#L192-L216)\n\n第三处最有分析价值：定时任务的 IPC/MCP 面。`task-acl.ts` 的模块文档用整段注释论证了为什么 `isAdminHome` 被**刻意排除**在工作区范围判定之外：admin Home Agent 本质是一个接触不可信内容的 LLM，而任务的 `prompt` 会按计划重放、以 Bot 身份在目标会话发言、并计费到目标工作区——若存在全局旁路，注入内容就能在其他租户的工作区里植入或立即运行 cron 任务。因此该面只允许两条路径：目标就是 Agent 自身工作区目录，或该工作区属主同样拥有目标群组，判定复用与 REST 面同一个 `canAccessGroup`，保证同一行数据在任何入口得到同一裁决。\n\nSources: [task-acl.ts](src/task-acl.ts#L3-L21) [task-acl.ts](src/task-acl.ts#L41-L75)\n\n这里存在一个容易被误读为矛盾的对比：`cross-group-acl.ts` 的 `canSendCrossGroupMessage()` 第一条分支恰恰是 `isAdminHome` 直接放行任意目标。两者的区别在于**能力与范围的分界**——跨群消息发送是 Agent 运行时回复消息的路由能力（admin Home 作为宿主级受信执行体可以向任何群发言）；而定时任务是把一段会被反复重放的用户内容写入他人领地。`task-acl.ts` 文档对此有精确表述：script 任务与 Host 执行模式要求 admin 属于"能调度什么"的能力检查，留在调用点；它们不回答"落在谁的工作区"。理解这条分界线，就理解了整套矩阵的取舍逻辑。\n\nSources: [cross-group-acl.ts](src/cross-group-acl.ts#L13-L35) [tests/can-send-cross-group-message.test.ts](tests/can-send-cross-group-message.test.ts#L1-L16)\n\nadmin 并非完全没有干预手段，唯一的 break-glass 是 `POST /api/groups/:jid/reset-owner`：当 IM owner 离群或换号导致 owner-only 命令永久锁死时，平台 admin 可强制释放 owner 锚点，后续由新 owner 通过 `/owner_mention` 重新认领。注意该接口的角色检查发生在目标资源存在性校验之前，但它只是清除认领状态，并不授予对工作区内容的任何访问。\n\nSources: [routes/groups.ts](src/routes/groups.ts#L1609-L1643) [ACL-MATRIX.md](docs/ACL-MATRIX.md#L247-L253)\n\n## 404 反枚举：让"不存在"与"无权访问"不可区分\n\n传统 RBAC 系统常用 401/403 区分认证与授权失败，但状态码本身会泄露信息：收到 403 即证明资源存在。Miniclaw 的资源级路由统一采用 404 掩蔽策略——工作区不存在和无权访问都返回 `{ error: \'Group not found\' }`，使攻击者无法通过响应差异枚举其他用户的工作区。这一策略贯穿各资源面：环境变量 GET/PUT、Workspace Memory 服务层、Sub-Agent CRUD 测试断言均一致。契约测试甚至把这一点写成显式验收标准："non-owner → all routes return 404 (group hidden by canAccessGroup)"。\n\nSources: [routes/groups.ts](src/routes/groups.ts#L2515-L2521) [ACL-MATRIX.md](docs/ACL-MATRIX.md#L130-L143) [tests/routes-workspace-config-acl.test.ts](tests/routes-workspace-config-acl.test.ts#L1-L10)\n\n## 无 Cookie 的平行世界：IM 命令 Owner Gate 与响应对象\n\nIM 渠道消息不经过 Web 中间件，因此需要一套平行的授权机制，锚点是渠道原生 sender ID 与 `owner_im_id` 的比对。变更类命令集中在 `OWNER_REQUIRED_IM_COMMANDS` 集合（`/clear`、`/bind`、`/unbind`、`/new`、`/sw`、`/spawn`、`/release_owner`），`checkImOwnerCommand()` 统一执行"sender 必须等于当前 owner"判定；未认领的群不会让第一条命令的发送者自动成为 owner，必须走 `/owner_mention` 显式认领——唯一例外是 1:1 私聊，因为私聊里 sender 身份无歧义，强制认领纯属摩擦，且 JID 判定是结构性单向的：群永远不会被误读为私聊。\n\nSources: [im-command-utils.ts](src/im-command-utils.ts#L255-L313) [im-command-utils.ts](src/im-command-utils.ts#L315-L345)\n\n在命令门控之前还有一道更早的闸门：响应对象策略。每条渠道消息先按 `audience_mode` 过滤（`everyone` 放行所有人，`owner_only` 只允许 `owner_im_id` 匹配者），再做 @/话题激活判断，两个维度相互独立。与之配套的是运行时 Owner Gate——当 owner 账号被禁用或删除后，其名下群组必须立即停止响应，防止 Bot 持续开火到下次重启。广播投递侧同样 fail-closed：已声明绑定的群如果解析不出目标工作区，宁可拒发也不回退到渠道账号原始 owner，避免把 A 工作区的对话泄露给 B 账号的旧 Home 会话。\n\nSources: [im-audience-policy.ts](src/im-audience-policy.ts#L20-L32) [owner-gate.ts](src/owner-gate.ts#L1-L29) [group-broadcast-acl.ts](src/group-broadcast-acl.ts#L31-L48)\n\n| IM 面 Gate | 判定依据 | 覆盖动作 |\n| --- | --- | --- |\n| 响应对象 | `audience_mode` vs sender | 所有入站消息的第一道过滤 |\n| 命令 Owner Gate | `sender_im_id === owner_im_id` | 全部变更类斜杠命令 |\n| 私聊自动认领 | 结构性 DM JID 判定 | 仅 1:1，群聊永不自动认领 |\n| 运行时 Owner Gate | owner 用户 status | owner 被禁用后全群停摆 |\n| 广播归属解析 | 绑定关系解析 owner | 输出投递可见性 |\n\nSources: [im-command-utils.ts](src/im-command-utils.ts#L270-L280)\n\n## 同一动作的多面一致性：REST、WebSocket、IM 与 MCP/IPC\n\nMiniclaw 的同一操作往往存在多个入口，矩阵要求它们对同一行数据给出相同裁决。WebSocket 面是典型样本：`send_message` 要求 Access 且 Host 组叠加 Host 权限，其中的 `/clear` 分支注释直接写明 "aligned with HTTP /clear" 并调用 `canModifyGroup`；终端操作则演示了**授权与所有权分离**的两段设计——`terminal_start` 校验 `canAccessGroup`，成功后在 `terminalOwners` 映射中登记 WebSocket 连接为终端所有者，此后的 `terminal_input`/resize/stop 不再重复资源判定，而是校验"必须是当前连接拥有的终端"，连接关闭时清理映射是协议的一部分。\n\nSources: [web.ts](src/web.ts#L1685-L1700) [web.ts](src/web.ts#L1758-L1770) [web.ts](src/web.ts#L1879-L1906) [web.ts](src/web.ts#L1950-L1994) [ACL-MATRIX.md](docs/ACL-MATRIX.md#L216-L230)\n\nMCP/IPC 面的一致性由 `task-acl.ts` 的双函数保证：`canIpcActorAccessGroup` 与 `canIpcActorManageTask` 分别镜像 REST 面 `canAccessGroup` 和 `canViewTask` 的形状，模块文档将此上升为自洽性要求——"This surface must reach the same verdict for the same row, otherwise one rule contradicts itself depending on which entrypoint asks"。主进程侧的任务更新处理器同样复用该判定，并留下注释"走 REST 面同一套 canAccessGroup，admin home 不再是全局旁路"。\n\nSources: [task-acl.ts](src/task-acl.ts#L5-L11) [index.ts](src/index.ts#L12788-L12795)\n\n| 入口面 | 身份载体 | 变更类判定 | 一致性锚点 |\n| --- | --- | --- | --- |\n| REST | Cookie Session | `canModifyGroup` | `routes/*` 中间件栈 |\n| WebSocket | Upgrade 时固定的 Session | 同 HTTP（如 `/clear`） | 注释级对齐约定 |\n| IM 命令 | 渠道原生 sender ID | `checkImOwnerCommand` | `owner_im_id` 比对 |\n| MCP/IPC | 主进程签发的运行上下文 | `canIpcActorManageTask` | 复用 `canAccessGroup` |\n\nSources: [ACL-MATRIX.md](docs/ACL-MATRIX.md#L275-L287)\n\n## 修改 ACL 的测试纪律\n\n仓库把 ACL 变更的验证要求固化为八条清单，其中第三条直接针对本文主题："admin 是否应该绕过必须显式测试；默认不绕过"。其余维度覆盖 owner 成功、非 owner 拒绝、Host/Container 分支、404 语义、多渠道凭据不串用、四入口一致性以及失败路径不留半写状态。现有测试按此组织成可定位的文件族：`routes-*-acl.test.ts` 系列（agents、groups-owner、messages、workspace-config、stop-interrupt）、`owner-gate.test.ts`、`im-owner-gate.test.ts`、`mcp-runtime-secret-boundary.test.ts` 等，每个文件的头部注释都声明了覆盖矩阵与镜像关系。\n\nSources: [ACL-MATRIX.md](docs/ACL-MATRIX.md#L310-L333) [tests/routes-agents-acl.test.ts](tests/routes-agents-acl.test.ts#L1-L10) [tests/routes-stop-interrupt-acl.test.ts](tests/routes-stop-interrupt-acl.test.ts#L1-L5)\n\n这套纪律的价值在于把"无 admin 旁路"从文档承诺变成回归资产：任何人引入新的绕过分支，都会立刻撞上显式断言 admin 行为的测试用例。对于要在类似系统中落地多租户隔离的读者，值得借鉴的不是具体规则，而是这种"判定函数纯化（依赖注入保持可单测）+ 入口裁决同源 + 测试维度清单化"的三层工程结构。\n\nSources: [task-acl.ts](src/task-acl.ts#L29-L39)\n\n## 小结与延伸阅读\n\nMiniclaw 的 ACL 矩阵可以用一句话收束：**系统能力看权限位（admin 隐含全部），资源归属看 `created_by`（无人豁免）**。三函数判定模型、404 反枚举、IM 面平行 Owner Gate 与四入口裁决一致性共同构成纵深防御，而 `task-acl.ts` 的设计注释展示了每个例外背后的威胁模型推理。建议按以下路径继续深入：\n\n- 了解工作区与 Session 的产品模型背景：[Agent-first 产品模型：Agent Profile、Workspace 与 Session 层级](5-agent-first-chan-pin-mo-xing-agent-profile-workspace-yu-session-ceng-ji)\n- 理解承载这些中间件的 Web 服务骨架：[Hono Web 服务：路由族、Cookie 认证与 WebSocket](8-hono-web-fu-wu-lu-you-zu-cookie-ren-zheng-yu-websocket)\n- 进入下一层的容器与挂载安全边界：[容器隔离与挂载安全：非 root 运行、白名单与密钥加密边界](20-rong-qi-ge-chi-yu-gua-zai-an-quan-fei-root-yun-xing-bai-ming-dan-yu-mi-yao-jia-mi-bian-jie)\n- 展开 IM 命令系统的完整清单：[IM 命令系统：只读、变更与 Owner 门控命令](18-im-ming-ling-xi-tong-zhi-du-bian-geng-yu-owner-men-kong-ming-ling)\n- 深挖任务调度为何成为旁路攻防焦点：[定时任务调度器：Cron、固定间隔、一次性任务与重启补跑](21-ding-shi-ren-wu-diao-du-qi-cron-gu-ding-jian-ge-ci-xing-ren-wu-yu-zhong-qi-bu-pao)';export{e as default};

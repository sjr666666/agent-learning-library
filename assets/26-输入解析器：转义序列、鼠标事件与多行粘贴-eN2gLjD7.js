const t='上一页 [TUI 架构：状态机驱动的终端界面](25-tui-jia-gou-zhuang-tai-ji-qu-dong-de-zhong-duan-jie-mian) 勾勒了"原始字节流进、结构化事件出"的整体管道。本页深入这条管道的第一个环节——`src/tui/input-parser.ts`（291 行），回答三个核心问题：**终端把按键编码成什么**（转义序列协议）、**解析器如何在任意 chunk 边界下重组这些序列**（状态化 remainder）、**以及如何区分"用户按了一次回车"与"用户粘贴了五段代码"**（多行粘贴判定）。同时覆盖解析结果在 `src/tty-app.ts` 中的消费链路：鼠标框选、滚轮滚动、Ctrl 快捷键与剪贴板复制。\n\n## 事件模型：四种结构化事件的统一契约\n\n解析器对外只暴露一个入口 `parseInputChunk(previousRest, chunk)` 和一个联合类型 `ParsedInputEvent`。终端的字节流形态千差万别——方向键是 `ESC[A`、SGR 鼠标是 `ESC[<0;10;20M`、普通字符就是它本身——但解析器一律归一为四类事件：\n\n| 事件种类 | 载荷字段 | 典型来源 |\n|---|---|---|\n| `key` | `name`（14 个命名键）+ `ctrl`/`meta` | 方向键、Home/End、Tab、退格、回车、Esc |\n| `text` | `text` 字符串 + `ctrl`/`meta` | 可打印字符、Ctrl 组合键、粘贴内容、Meta 键 |\n| `wheel` | `direction`（up/down） | 鼠标滚轮（SGR/X10 协议） |\n| `mouse` | `x`/`y`（0-based）+ `button` + `action` | 鼠标按下/释放/拖动 |\n\n`key` 与 `text` 的区分是有意为之：**需要状态机语义的按键**（如 `return` 触发提交、`up` 导航历史）走 `key`，**需要原样进入输入缓冲区的字符**（如粘贴的代码、Meta+字母）走 `text`。这一契约是上层 `handleEvent` 做模式分派的基础——审批模式、会话选择模式与普通输入模式各自从这四类事件中挑选自己关心的子集。\n\nSources: [input-parser.ts](src/tui/input-parser.ts#L1-L44)、[input-parser.ts](src/tui/input-parser.ts#L290)\n\n## 状态化管线：跨 chunk 缓冲与事件串行化\n\n终端并不保证一个逻辑输入在一个数据包内送达。SGR 鼠标事件 `ESC[<0;10;20M` 可能被拆成 `ESC[<0;` 和 `10;20M` 两段；粘贴的大文本也可能被内核按缓冲区边界切开。因此解析器必须**有状态**：`tty-app.ts` 在 `runTtyApp` 的 Promise 体内维护两个闭包变量——`inputRemainder`（未消费的字节残片）与 `eventChain`（事件处理串行化链条）：\n\n```mermaid\nsequenceDiagram\n    participant T as 终端\n    participant S as process.stdin\n    participant P as parseInputChunk\n    participant R as inputRemainder\n    participant H as handleEvent\n    T->>S: ESC[<0;10;20M（被拆散）\n    S->>P: chunk1 = "ESC[<0;"\n    P->>R: 序列不完整，rest="ESC[<0;"\n    S->>P: chunk2 = "10;20M"\n    P->>P: input="ESC[<0;10;20M" → 命中 SGR 正则\n    P->>H: {kind:\'mouse\', x:9, y:19, button:\'left\', action:\'press\'}\n    Note over P,R: 若某次解析无法完成，rest 暂存<br/>等待下一个 data 事件拼接\n```\n\n`onData` 回调的接线方式是关键：每次 `data` 事件到达，先把 `inputRemainder` 与当前 `chunk` 拼接，再交给 `parseInputChunk`；返回的 `rest` 写回 `inputRemainder`，返回的 `events` 则被追加到 `eventChain` 这个 Promise 链上逐条 `await handleEvent(event)`。这个设计同时解决了两个问题：**转义序列跨 chunk 重组**，以及**事件处理的竞态**——`handleEvent` 内部可能触发异步流程（如 `return` 提交进入 agent 回合），若不串行化，两个连续 chunk 的事件会交错执行导致状态错乱。\n\n```mermaid\nflowchart LR\n    TTY["终端 (raw mode)"] -->|"data 事件<br/>任意 chunk 边界"| CONCAT["inputRemainder + chunk"]\n    CONCAT --> PARSE["parseInputChunk<br/>逐字符主循环"]\n    PARSE -->|"events[]"| CHAIN["eventChain Promise 链<br/>逐条 await"]\n    PARSE -->|"rest"| REMAINDER["inputRemainder 闭包缓冲"]\n    CHAIN --> HANDLER["handleEvent 模式分派"]\n    HANDLER --> STATE["ScreenState 状态变更"]\n```\n\n管道的另一端是终端初始化的对称操作：`runTtyApp` 启动时 `enterAlternateScreen()` 进入备选屏幕并启用鼠标追踪，`setRawMode(true)` 关闭行缓冲与信号处理（Ctrl+C 不再产生 SIGINT，而是作为字节 `\\u0003` 进入解析器）；退出时 `cleanup` 反向执行 `setRawMode(false)`、退出备选屏幕、`process.stdin.pause()`。注意 `inputRemainder` 和 `eventChain` 的生命周期与这个 Promise 体一致，随 `finish()` 一起消亡。\n\nSources: [tty-app.ts](src/tty-app.ts#L1754-L1758)、[tty-app.ts](src/tty-app.ts#L2409-L2427)、[tty-app.ts](src/tty-app.ts#L1646-L1651)、[tty-app.ts](src/tty-app.ts#L1775-L1789)\n\n## 转义序列识别：防阻塞的前瞻策略\n\n`parseInputChunk` 的主循环（L221-L285）是一个逐字符扫描器：`index` 指向当前消费位置，`remaining` 是未消费的尾部。遇到 `ESC`（`\\u001b`）时，并不立即尝试匹配，而是先调用 `maybeNeedMoreForEscapeSequence` 做**前瞻判断**——如果当前残片是一个"已知不完整的转义序列前缀"，就立即返回 `{ events, rest: remaining }`，把残片交给 `inputRemainder` 等待下一 chunk：\n\n| 残片形态 | 判定 | 处理 |\n|---|---|---|\n| 孤立的 `ESC` | 可能是 ESC 键，也可能是序列前缀 | 等待更多数据 |\n| `ESC[` | CSI 引入符后无内容 | 等待 |\n| `ESC[<数字;?]*` | CSI 中间段只有 `<`、数字、`;`、`?` | 等待 |\n| `ESC` + `O`（长度 < 3） | SS3 应用键区前缀 | 等待 |\n| `ESC[` + 异常字符（如 `\\r`） | 不可能是合法 CSI | **立即放行**，交给回退逻辑 |\n\n这个设计有一个明确取舍：**宁可延迟**也不误判。孤立的 `ESC` 被保留在 remainder 中，直到后续字节到来才能确定它是 ESC 键还是某个序列的开头——这是转义序列协议的固有歧义，无法在字节层面消除。但代码注释明确约束了等待范围：只对"已知不完整的 CSI 前缀"等待，一旦出现异常字符（如 `ESC[\\r`）立即放行，由 `parseEscapeSequence` 的回退路径兜底（最终落到"孤立 ESC → escape 键，长度 1"），避免解析器被垃圾字节永久阻塞。这是整个解析器最值得注意的防呆细节。\n\n`parseEscapeSequence` 是一个**按优先级串联的正则匹配器**，每个模式命中即返回 `{event, length}` 并退出；所有模式都不命中时回退为裸 ESC 键。匹配顺序即优先级：\n\n```mermaid\nflowchart TD\n    A["remaining[0] === ESC?"] -->|"否"| B["普通字符分支<br/>换行 / 制表 / 控制符 / 文本"]\n    A -->|"是"| C["maybeNeedMoreForEscapeSequence 前瞻"]\n    C -->|"前缀不完整"| D["返回 rest，等待下一 chunk"]\n    C -->|"可解析"| E["按优先级尝试正则"]\n    E --> E1["① SGR 鼠标 ESC[&lt;b;x;yM/m"]\n    E --> E2["② X10 滚轮 ESC[M + 3 字节"]\n    E --> E3["③ CSI 方向键 ESC[1;5A"]\n    E --> E4["④ CSI 功能键 ESC[3~"]\n    E --> E5["⑤ SS3 键区 ESC-O-A"]\n    E --> E6["⑥ Meta 回退 ESC + 单字符"]\n    E --> E7["⑦ 兜底：孤立 ESC → escape 键"]\n```\n\n注意 `parseEscapeSequence` 可能返回 `{event: null, length}`——即**消费了字节但不上报事件**。这在 X10 非滚轮鼠标、未知的 `ESC[n~` 功能码（如 `ESC[2~` Insert）时发生：字节被吞掉以免污染后续解析，但没有对应的事件语义。\n\nSources: [input-parser.ts](src/tui/input-parser.ts#L61-L77)、[input-parser.ts](src/tui/input-parser.ts#L79-L82)、[input-parser.ts](src/tui/input-parser.ts#L205-L208)、[input-parser.ts](src/tui/input-parser.ts#L224-L235)\n\n## SGR 鼠标协议：位掩码解码\n\n主鼠标通道是 XTerm 的 **SGR 扩展模式**（`ESC[?1006h` 启用，见 `screen.ts` 的 `ENABLE_MOUSE_TRACKING`）。其报文格式为 `ESC[<b;x;yM`（按下）或 `ESC[<b;x;ym`（释放），其中 `b` 是**位叠加的按钮码**，`x`/`y` 是 1-based 坐标（解析后各减 1 转为 0-based）。解码的核心是两组位掩码：\n\n| 掩码 | 位段 | 含义 |\n|---|---|---|\n| `0x43` | bit 0-1 + bit 6 | 按钮号（0=左, 1=中, 2=右）与滚轮标志（64=上, 65=下） |\n| `0x20` | bit 5 | 拖动/移动标志（motion） |\n| — | bit 2-3、bit 4 | Shift/Alt/Ctrl 修饰，解码时**忽略** |\n\n`0x43`（二进制 `0100 0011`）是一个精心挑选的掩码：它同时保留了低位按钮号和高位滚轮标志，而把中间的修饰位全部抹掉。因此 `(b & 0x43) === 0x40` 判定滚轮向上、`=== 0x41` 判定滚轮向下，无论叠加了多少修饰键；否则 `btnCode = b & 0x43` 得到 0/1/2 对应左/中/右键。`action` 的推导优先级是：后缀 `m` 表示 `release` → `b & 0x20` 表示 `drag` → 否则 `press`。终端还同时启用了 `ESC[?1000h`（X10 基础追踪）与 `ESC[?1002h`（按钮事件/拖动追踪），其中 1000 模式作为回退通道：`ESC[M` 后跟三个 `值+32` 的字节，解析器只从中提取滚轮事件，其他 X10 点击/拖动事件被消费但不生成事件（`event: null`）——因为 SGR 才是坐标精确的主通道。\n\nSources: [input-parser.ts](src/tui/input-parser.ts#L83-L110)、[input-parser.ts](src/tui/input-parser.ts#L112-L122)、[screen.ts](src/tui/screen.ts#L9-L16)、[screen.ts](src/tui/screen.ts#L30-L40)\n\n## CSI/SS3 按键序列与修饰键\n\n键盘导航键走两条协议族。**CSI**（`ESC[`）方向键序列支持带修饰符的变体 `ESC[1;<mod>;<letter>`——代码从 `(?:1;(\\d+))?` 可选组中提取修饰码，但只识别两个值：`3`（Alt/Meta）与 `5`（Ctrl），其余修饰（Shift、组合键）一律按普通键处理。`meta` 与 `ctrl` 布尔值直接成为 `key` 事件的字段，供上层区分"上下方向键切换斜杠菜单"与"Meta+上下滚动转录面板"。**SS3**（`ESC` + `O`）应用键区序列没有修饰符变体，直接映射同一组名称。\n\n| 协议形态 | 示例 | 事件结果 |\n|---|---|---|\n| CSI 方向/导航 | `ESC[1;5A`（Ctrl+上）、`ESC[1;3C`（Alt+右）、`ESC[H` | `key` + `ctrl`/`meta` |\n| CSI 功能键 | `ESC[3~`（Delete）、`ESC[5~`（PageUp）、`ESC[1~`/`ESC[7~`（Home） | `key`，无修饰 |\n| SS3 键区 | `ESCOA`/`ESCOB`/`ESCOC`/`ESCOD`/`ESCOH`/`ESCOF` | `key`，无修饰 |\n| Meta 组合 | `ESC` + 任意非 `[`/`O`/`\\t` 字符 | `text` + `meta:true` |\n| Meta+Tab | `ESC` + `\\t` | `key` tab + `meta:true` |\n\n`ESC[n~` 功能键表的别名值得注意：`1` 和 `7` 都映射 `home`，`4` 和 `8` 都映射 `end`——这是为了兼容不同终端对老式 `ESC[n~` 编码的差异。未登记的 `n`（如 `2`=Insert）返回 `event: null` 但照常消费长度。Meta 回退路径则解释了 Alt 键的编码本质：终端把 Alt+字母编码为 `ESC` + 字母，解析器无法与"ESC 键后跟一个字母"区分，因此统一解释为 Meta 文本事件。\n\nSources: [input-parser.ts](src/tui/input-parser.ts#L124-L146)、[input-parser.ts](src/tui/input-parser.ts#L148-L165)、[input-parser.ts](src/tui/input-parser.ts#L167-L193)、[input-parser.ts](src/tui/input-parser.ts#L195-L203)\n\n## 多行粘贴判定：基于 chunk 而非累计输入\n\n解析器最精妙的设计是**多行粘贴检测**。`isMultilinePasteChunk` 的判定极简：当前 chunk 是否"同时含有换行符与非换行符"——`/[\\r\\n]/.test(input) && /[^\\r\\n]/.test(input)`。判定结果 `treatNewlinesAsText` 在进入主循环前一次性决定，随后主循环对换行的处理分岔：\n\n| 场景 | chunk 内容 | 判定 | 换行处理 |\n|---|---|---|---|\n| 用户按回车 | `\\r` | 只有换行，无其他字符 → 非粘贴 | 发射 `return` 键，触发提交 |\n| 粘贴多行代码 | `test1\\r\\ntest2\\r\\n...` | 含换行 **且** 含非换行 → 粘贴 | 每个换行发射 `\\n` 文本，**绝不触发提交** |\n| 双击回车 | `\\r\\r` | 只有换行 → 非粘贴 | 两个 `return` 键 |\n\n这个判定刻意**只看当前 chunk**，不看累计输入——因为终端粘贴通常在一个 data 事件内整体送达，而按键是逐字节事件。判定为粘贴后，`\\r\\n` 与 `\\n\\r` 组合被归一化为**一个** `\\n` 文本事件（`index += 2`），且统一转成 `\\n` 而非保留 `\\r`，保证粘贴内容进入输入缓冲区后与编辑器内部的换行表示一致。对应测试 `input-parser.test.ts` 验证了"五段 `\\r\\n` 分隔的文本不产生任何 `return` 键、且文本完整保留"这一核心不变量。\n\n非粘贴路径下，单个 `\\r` 或 `\\n` 都归一化为 `return` 键——这正是"按一次回车只提交一次"的保障。\n\nSources: [input-parser.ts](src/tui/input-parser.ts#L46-L48)、[input-parser.ts](src/tui/input-parser.ts#L215-L218)、[input-parser.ts](src/tui/input-parser.ts#L240-L255)、[input-parser.test.ts](test/input-parser.test.ts#L6-L28)\n\n## 控制字符映射与按键归一化\n\n主循环对三类"特殊字节"做归一化。**控制字符**（`\\u0001`–`\\u001a`，即 Ctrl+A 到 Ctrl+Z 在 raw mode 下的原始形态）查 `CTRL_CHAR_TO_NAME` 映射表——只有七个键有语义，映射为 `{kind:\'text\', text:名, ctrl:true}`；未登记的控制字符被**静默跳过**（不产生事件，防止垃圾字节流入输入缓冲区）：\n\n| 字节 | 控制键 | 映射名 | 上层行为 |\n|---|---|---|---|\n| `\\u0001` | Ctrl+A | `a` | 光标移到行首（无输入时跳转录顶部） |\n| `\\u0003` | Ctrl+C | `c` | 退出应用（raw mode 下不产生 SIGINT） |\n| `\\u0005` | Ctrl+E | `e` | 光标移到行尾 |\n| `\\u000e` / `\\u0010` | Ctrl+N / Ctrl+P | `n` / `p` | 历史下/上导航 |\n| `\\u000f` | Ctrl+O | `o` | 展开/折叠审批详情 |\n| `\\u0015` | Ctrl+U | `u` | 清空输入 |\n\n**退格归一化**同样值得注意：终端对退格键有两种编码——`\\u007f`（DEL，多数现代终端）与 `\\b`（`\\u0008`，老式终端），解析器把两者都归一为 `backspace` 键事件。**制表符** `\\t` 单独归一为 `tab` 键。剩余可打印字符（`char >= \' \'`）直接发射 `text` 事件；而小于空格的其他控制字节（如 `\\u0000`、`\\u001b` 之后的杂散控制码）被 `char < \' \'` 分支静默消费，保证输入流干净。\n\nSources: [input-parser.ts](src/tui/input-parser.ts#L50-L59)、[input-parser.ts](src/tui/input-parser.ts#L257-L281)、[input-parser.ts](src/tui/input-parser.ts#L283-L284)\n\n## 事件消费链路：从字节到 UI 行为\n\n解析器只负责"翻译"，不负责"决策"。四类事件在 `tty-app.ts` 的 `handleEvent` 中被三个模式分支（审批 → 会话选择 → 普通输入）按优先级消费。普通输入模式下的事件语义映射：\n\n| 事件 | 消费逻辑 |\n|---|---|\n| `key: return` | 有斜杠命令候选则补全为选中命令；否则清空输入并提交 `handleInput`，`submitInFlight` 防重入 |\n| `key: up/down` | 有斜杠命令候选时在菜单间循环；`meta` 时滚动转录面板；否则 `historyUp/Down` 导航历史 |\n| `key: left/right` | 移动 `cursorOffset`（带边界钳制） |\n| `key: backspace/delete` | 按 `cursorOffset` 位置删除前/后一个字符 |\n| `key: escape` | 清空输入与斜杠索引 |\n| `key: pageup/pagedown` | 转录面板按 8 行翻页滚动 |\n| `wheel` | 转录面板按 3 行滚动 |\n| `mouse` | 三段式框选：press 记录 `mouseDown` → drag 更新 `selection` → release 提取文本并复制 |\n\n鼠标链路是解析器与 UI 协作的典范。`mouse` 事件的 `x`/`y` 是终端坐标，消费端先通过 `screenToAbsoluteLineIndex` 把屏幕行号换算成 transcript 逻辑行索引（基于 `transcriptBodyStartY` 布局缓存与滚动偏移），再减 3 列得到内容列坐标。释放时 `extractSelectedText` 从 transcript 渲染行中剥离 ANSI 序列、按显示宽度切片（CJK 全角字符按 2 列处理），最终 `copyToClipboard` 按平台选择 `pbcopy`/`xclip`/`clip` 子进程写入系统剪贴板（Windows 下先编码为带 BOM 的 UTF-16LE）。`keepSelectionAfterMouseRelease` 当前是恒等函数——释放后保留高亮选区直到下次按下。\n\nSources: [tty-app.ts](src/tty-app.ts#L2099-L2108)、[tty-app.ts](src/tty-app.ts#L2110-L2167)、[tty-app.ts](src/tty-app.ts#L2171-L2234)、[tty-app.ts](src/tty-app.ts#L2236-L2342)、[tty-app.ts](src/tty-app.ts#L2378-L2384)、[tty-app.ts](src/tty-app.ts#L330-L350)、[transcript.ts](src/tui/transcript.ts#L282-L303)、[tty-app.ts](src/tty-app.ts#L352-L379)\n\n## 测试验证\n\n解析器的行为被三层测试覆盖。**解析层**：`input-parser.test.ts` 验证多行粘贴不产生提交键且文本完整保留。**选区层**：`transcript-cjk-selection.test.ts` 验证 CJK 全角字符在按显示列高亮与文本提取时的一致性（`你A` 中选中第 2-4 列恰好提取出 `你`）；`mouse-release-selection.test.ts` 验证释放后选区保留的不变量。**剪贴板层**：`windows-clipboard-encoding.test.ts` 验证 Windows 平台下剪贴板载荷以 `0xFF 0xFE` BOM 开头且正文为 UTF-16LE。这些测试共同锚定了"字节翻译"与"坐标换算"两条最容易回归的边界。\n\nSources: [input-parser.test.ts](test/input-parser.test.ts#L6-L28)、[transcript-cjk-selection.test.ts](test/transcript-cjk-selection.test.ts#L30-L64)、[mouse-release-selection.test.ts](test/mouse-release-selection.test.ts#L7-L34)、[windows-clipboard-encoding.test.ts](test/windows-clipboard-encoding.test.ts#L6-L23)\n\n## 延伸阅读\n\n- 输入事件如何在三种模式间分派、`ScreenState` 如何承载编辑器与选择状态，见 [TUI 架构：状态机驱动的终端界面](25-tui-jia-gou-zhuang-tai-ji-qu-dong-de-zhong-duan-jie-mian)\n- `return` 提交后进入的 agent 回合与 `submitInFlight` 防重入机制，见 [代理循环：model → tool → model 的多步执行流程](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng)\n- 审批模式下 Ctrl+O 展开与方向键导航的完整语义，见 [三层权限系统：路径、命令与编辑审批](17-san-ceng-quan-xian-xi-tong-lu-jing-ming-ling-yu-bian-ji-shen-pi)\n- 解析器与鼠标选区的自动化测试在整个测试体系中的位置，见 [测试体系：从单元测试到集成回归](28-ce-shi-ti-xi-cong-dan-yuan-ce-shi-dao-ji-cheng-hui-gui)';export{t as default};

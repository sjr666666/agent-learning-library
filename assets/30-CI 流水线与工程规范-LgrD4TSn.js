const e='HappyClaw 用两条 GitHub Actions 流水线守护代码质量与镜像分发：**`ci.yml` 是合并门槛**，在每个 PR 和 main 分支 push 上执行 11 步验证；**`docker-publish.yml` 是镜像发布链**，只在 main 分支上构建、冒烟、签名并发布双平台 Agent 镜像。本篇回答三个问题：CI 每一步在验证什么、背后遵循哪些工程规范、以及你提交代码前应该在本地先过哪些关卡。测试体系的完整分类见 [测试体系：单元测试、契约测试与真实模型冒烟](29-ce-shi-ti-xi-dan-yuan-ce-shi-qi-yue-ce-shi-yu-zhen-shi-mo-xing-mou-yan)，常用命令速查见 [开发模式与常用命令](3-kai-fa-mo-shi-yu-chang-yong-ming-ling)。\n\n## 双流水线分工总览\n\n两条流水线的触发条件、职责和验收标准完全不同，理解它们的分工是理解整个工程规范的前提。`ci.yml` 对**每一次代码变更**负责，追求快而全的静态与动态验证；`docker-publish.yml` 只对 **main 分支的发布事件**负责，追求"验证过的镜像才允许打标签"的交付纪律。\n\n```mermaid\nflowchart TB\n    subgraph PR["Pull Request / push 任意分支"]\n        E1["触发 ci.yml validate 任务"]\n    end\n    subgraph Main["push 到 main 分支"]\n        E2["触发 ci.yml（变更验证）"]\n        E3["触发 docker-publish.yml（镜像发布）"]\n    end\n    E1 --> C1["安装依赖 → 格式 → 类型同步 → 类型检查<br/>→ 单元测试 → 构建 → E2E → Runner 自检"]\n    E2 --> C1\n    E3 --> C2["build-and-smoke × 2<br/>amd64 原生 runner + arm64 原生 runner"]\n    C2 --> C3["promote 任务<br/>合并双平台 digest → cosign 签名 → 打 :latest"]\n```\n\n| 维度 | `ci.yml` | `docker-publish.yml` |\n| --- | --- | --- |\n| 触发时机 | `pull_request` + push 到 `main` | 仅 push 到 `main` + 手动 `workflow_dispatch` |\n| 运行平台 | 单个 `ubuntu-24.04` | `ubuntu-24.04`（amd64）与 `ubuntu-24.04-arm`（arm64）双原生 runner |\n| 超时 | 20 分钟 | 构建冒烟 45 分钟 / 提升 10 分钟 |\n| 权限 | `contents: read`（最小权限） | `contents: read`；promote 任务额外 `id-token: write`（用于签名） |\n| 验收标准 | 类型、格式、测试、构建、E2E 全绿 | 双平台镜像都通过真实 CDP 探测、签名校验后才发布 |\n\nSources: [.github/workflows/ci.yml](.github/workflows/ci.yml#L1-L13) · [.github/workflows/docker-publish.yml](.github/workflows/docker-publish.yml#L1-L16) · [.github/workflows/docker-publish.yml](.github/workflows/docker-publish.yml#L22-L33)\n\n## ci.yml：合并门槛流水线\n\n`ci.yml` 只有一个名为 `validate` 的任务，内部按"**从快到慢、从静态到动态**"的顺序串联 11 个步骤。顺序不是随意的：格式检查几十秒就能完成，而 E2E 和构建需要几分钟，把廉价检查放在前面可以让失败的 PR 尽早暴露，减少无谓的等待。\n\n```mermaid\nflowchart TD\n    S1["checkout<br/>fetch-depth: 0 拉全量历史"] --> S2["setup-node<br/>Node 24 + npm cache 三份 lockfile"]\n    S2 --> S3["npm ci × 3<br/>根 / web / agent-runner"]\n    S3 --> S4["安装 Playwright Chromium"]\n    S4 --> S5["format:check<br/>仅检查改动文件（FORMAT_BASE_REF）"]\n    S5 --> S6["make sync-types<br/>+ git diff --exit-code 校验生成物"]\n    S6 --> S7["make typecheck<br/>三端 tsc + 3 个静态检查脚本"]\n    S7 --> S8["npm test -- --run<br/>全量 Vitest"]\n    S8 --> S9["npm run build:all<br/>三端生产构建"]\n    S9 --> S10["Web E2E<br/>Playwright 移动端 Chromium"]\n    S10 --> S11["self-test:agent-runner<br/>后台任务汇总 incident 回放"]\n```\n\n**Step 1–2：完整历史与依赖缓存。** `fetch-depth: 0` 拉取完整 Git 历史，这不是冗余——后面的格式检查需要计算"本次改动相对基准提交的 diff"，必须能访问历史提交。`setup-node` 用 `cache: npm` 缓存三份 lockfile（根、`web/`、`container/agent-runner/`）对应的依赖，加速后续安装。\n\n**Step 3：三端可复现安装。** 项目由三个独立的 Node 工程组成，CI 对每个工程都执行 `npm ci`。`npm ci` 与 `npm install` 的关键区别是：它**严格按照 lockfile 安装**并删除 `node_modules` 后重装，任何 package.json 与 lockfile 的漂移都会直接失败——这正是"可复现构建"规范的第一道防线。\n\n**Step 5：只检查改动文件的格式。** CI 通过 `FORMAT_BASE_REF` 环境变量告诉检查脚本基准提交：PR 用 `github.event.pull_request.base.sha`（目标分支当前 HEAD），push 用 `github.event.before`（上一次提交）。`check-format-changed.mjs` 基于 `merge-base` 计算 diff，只把本次改动的文件交给 Prettier 校验，而不是全仓库扫描——既快又能避免历史遗留的格式问题阻塞新代码。\n\n**Step 6：生成物必须可复现且已提交。** 这一步执行 `make sync-types` 重新生成共享类型副本，然后用 `git diff --exit-code` 断言**没有产生任何差异**。含义是：如果开发者改了 `shared/stream-event.ts` 却没有运行同步、没有提交副本，CI 会当场失败——生成物不是"CI 现场生成"的，而是必须作为普通代码提交进仓库。\n\n**Step 7：typecheck 不只是 tsc。** `make typecheck` 在三个 `tsc --noEmit` 之外还串联了三个静态检查脚本（共享类型一致性、Prompt 引用完整性、文档链接），它们把本应在运行时或容器启动时才暴露的错误提前到 CI 拦截。详见下文"失败向左移"一节。\n\n**Step 8–11：动态验证。** 全量单元测试（`--run` 单次模式，避免 Vitest watch 挂起）、三端生产构建、Playwright 移动端 E2E、以及 Agent Runner 自检（不依赖真实模型，回放历史 incident 验证后台任务汇总判定逻辑）。四层验证覆盖了"逻辑正确、能编译、前端交互正常、Runner 行为正确"四个维度。\n\nSources: [.github/workflows/ci.yml](.github/workflows/ci.yml#L16-L18) · [.github/workflows/ci.yml](.github/workflows/ci.yml#L20-L27) · [.github/workflows/ci.yml](.github/workflows/ci.yml#L29-L33) · [.github/workflows/ci.yml](.github/workflows/ci.yml#L39-L42) · [.github/workflows/ci.yml](.github/workflows/ci.yml#L44-L48) · [.github/workflows/ci.yml](.github/workflows/ci.yml#L50-L63) · [scripts/check-format-changed.mjs](scripts/check-format-changed.mjs#L41-L57)\n\n## 工程规范一：可复现构建\n\nCI 的第一条工程规范可以概括为一句话：**任何一次构建的依赖版本和工具版本都必须可追溯、可复现**。它体现在四个层面，且每一条都有契约测试守护（见后文）。\n\n**Lockfile 是构建输入，不是构建产物。** `.gitignore` 明确注释了"npm lockfiles are build inputs and must be version-controlled"，三个 Node 工程的 lockfile 全部提交入库，禁止用 `bun.lock` 替代。契约测试进一步断言：lockfile 中不存在 `git+ssh:` 形式的依赖（避免依赖解析依赖网络与私钥），Makefile 的 `install` 目标和 CI 都只使用 `npm ci`，**全仓库不允许出现裸的 `npm install`**。\n\n**GitHub Actions 一律按 commit SHA 固定版本。** 两个 workflow 中所有第三方 Action（checkout、setup-node、buildx、login、build-push-action、cosign-installer 等）都钉在 40 位完整 commit SHA 上，`# v4` 只是人类可读注释。契约测试用正则断言每个 `uses:` 都必须是 SHA 格式——这样即使上游 Action 发布新版本或仓库被篡改，构建行为也不会漂移。\n\n**最小权限原则。** `ci.yml` 声明 `permissions: contents: read`，即 workflow 只获得读取仓库内容的权限；`docker-publish.yml` 的 build-and-smoke 任务同样只读，只有负责签名的 promote 任务才追加 `id-token: write`（cosign 无密钥签名需要 OIDC token）。镜像发布只用 GitHub Secrets 中的 `DOCKERHUB_USERNAME` / `DOCKERHUB_TOKEN`，契约测试还专门断言工作流中**不会出现**拼接出的 Docker PAT 形字符串。\n\n**原生 runner，不用模拟器。** 镜像构建矩阵使用 `ubuntu-24.04`（amd64）和 `ubuntu-24.04-arm`（arm64）两个**原生架构 runner**，而不是在一个 x86 runner 上用 QEMU 模拟 arm64。契约测试显式断言 workflow 中不出现 `docker/setup-qemu-action`——因为模拟构建可能掩盖真实 arm64 环境才出现的二进制兼容问题。\n\nSources: [.gitignore](.gitignore#L36-L38) · [tests/reproducible-build-contract.test.ts](tests/reproducible-build-contract.test.ts#L22-L58) · [.github/workflows/ci.yml](.github/workflows/ci.yml#L8-L9) · [.github/workflows/docker-publish.yml](.github/workflows/docker-publish.yml#L22-L33) · [tests/docker-publish-workflow-contract.test.ts](tests/docker-publish-workflow-contract.test.ts#L46-L54)\n\n## 工程规范二：单一事实来源与生成物纪律\n\nHappyClaw 存在大量**跨工程共享的类型定义**：StreamEvent 事件类型在 `src/`、`web/`、`container/agent-runner/` 各有一份副本，image-detector 与 channel-prefixes 各有两份。如果放任各端手改副本，三端的类型契约迟早会漂移——流式事件在运行时会解析失败或静默丢字段。\n\n规范的核心是：**`shared/` 目录是唯一真相源，副本是受控生成物**。`make sync-types` 执行 `scripts/sync-stream-event.sh`，把 `shared/` 下的三个源文件按内容复制到各副本位置；脚本特意只在内容不同时才复制（`cmp -s` 比较），避免无谓的时间戳变化触发冗余增量构建。\n\n```mermaid\nflowchart LR\n    CANON["shared/stream-event.ts<br/>shared/image-detector.ts<br/>shared/channel-prefixes.ts"]\n    subgraph Copies["受控副本（必须提交）"]\n        B1["src/stream-event.types.ts"]\n        W1["web/src/stream-event.types.ts"]\n        A1["container/agent-runner/src/stream-event.types.ts"]\n    end\n    CANON -- "make sync-types<br/>内容不同才复制" --> Copies\n    Copies -- "check-stream-event-sync.sh<br/>内容级 diff 校验" --> CANON\n```\n\n同步脚本负责"写"，校验脚本负责"查"。`check-stream-event-sync.sh` 对每个副本做**内容级 diff**（不是时间戳比较），任何缺失或差异都会让 `make typecheck` 失败，并提示 `Run \'make sync-types\' to re-sync`。CI 的 Step 6 则把校验推到更严格的位置：重新同步后用 `git diff --exit-code` 断言仓库里提交的副本与生成结果完全一致——**生成物必须入库，且入库的必须是最新的**。\n\nSources: [scripts/sync-stream-event.sh](scripts/sync-stream-event.sh#L1-L46) · [scripts/check-stream-event-sync.sh](scripts/check-stream-event-sync.sh#L1-L50) · [.github/workflows/ci.yml](.github/workflows/ci.yml#L44-L48) · [Makefile](Makefile#L185-L188)\n\n## 工程规范三：失败向左移（Shift-Left）\n\n"失败向左移"指**把错误发现的时间点尽量提前**：能在类型检查阶段拦截的，就不要等运行时；能在 CI 拦截的，就不要等容器启动。HappyClaw 用三个检查脚本把三类"迟到的失败"硬生生拽到了 `make typecheck` 这一道门里。\n\n**Prompt 引用完整性。** agent-runner 在模块加载时用 `readFileSync` 同步读取 `.md` Prompt 文件——如果源码引用了一个不存在的文件，容器**启动即崩溃**（`ENOENT \'/tmp/prompts/<name>.md\'`），而且只在真正运行容器时才暴露。`check-agent-runner-prompts.sh` 用 grep 模式抓取源码中所有 `loadPrompt(...)`、`path.join` 和字符串拼接形式的 `.md` 引用，逐一验证文件存在。脚本还内置一道防回退保险：如果源码中出现**任何**未被模式捕获的 `.md` 字面量，构建直接失败并提示"要么补模式、要么声明为非 Prompt"——防止新调用风格悄悄逃过检查。\n\n**文档即代码。** `check-docs.mjs` 把仓库内所有 Markdown 文档当成代码来校验：① 所有本地链接目标必须真实存在（跳过代码块与行内代码中的内容）；② 指定的核心文档中，所有反引号包裹的仓库路径必须存在；③ README/CLAUDE.md 中引用的每个 `make <target>` 必须在 Makefile 中真实定义；④ `docs/API.md` 必须索引 `src/routes/` 下的每个路由模块。文档悬空链接不再等到读者发现，提交前就会被拦下。\n\n**格式检查只面向改动。** 与全仓库格式化不同，`check-format-changed.mjs` 只对本次改动文件运行 Prettier（`--check` 只读模式），本地可用 `--write` 模式自动修复。CI 通过 `FORMAT_BASE_REF` 显式指定基准，让"改动集"的定义在 PR 与 push 场景下都准确。\n\n```mermaid\nflowchart LR\n    A["改动发生"] --> B{"哪类改动？"}\n    B -- "Prompt 引用" --> C["typecheck 阶段<br/>check-agent-runner-prompts.sh 拦截"]\n    B -- "文档链接 / make 目标" --> D["typecheck 阶段<br/>check-docs.mjs 拦截"]\n    B -- "共享类型" --> E["typecheck + CI Step 6<br/>双重复核"]\n    C -- "漏检" --> F["容器启动 ENOENT 崩溃"]\n    D -- "漏检" --> G["读者发现悬空链接"]\n    E -- "漏检" --> H["跨端类型契约漂移"]\n```\n\nSources: [scripts/check-agent-runner-prompts.sh](scripts/check-agent-runner-prompts.sh#L1-L22) · [scripts/check-agent-runner-prompts.sh](scripts/check-agent-runner-prompts.sh#L94-L157) · [scripts/check-docs.mjs](scripts/check-docs.mjs#L55-L82) · [scripts/check-docs.mjs](scripts/check-docs.mjs#L94-L141) · [scripts/check-format-changed.mjs](scripts/check-format-changed.mjs#L62-L82) · [Makefile](Makefile#L148-L153)\n\n## docker-publish.yml：镜像发布流水线\n\n镜像发布流水线是"验证过的镜像才允许打标签"这一纪律的完整实现。它由两个任务组成：`build-and-smoke`（矩阵双平台）和 `promote`（合并、签名、提升）。\n\n**为什么先按 digest 构建，而不是直接打标签？** Docker 镜像的 digest 是内容的加密指纹，不可变；而 `:latest` 之类的标签是可变的、会移动的。如果 workflow 先给镜像打 `:latest` 再冒烟，一旦冒烟失败，一个"已发布但未验证"的标签就已经暴露给用户了。因此 workflow 分两步：先用 `push-by-digest=true` 推送**不带标签的候选镜像**，等两个原生 runner 各自通过冒烟测试、记录并上传 verified digest 之后，promote 任务才用 `docker buildx imagetools create` 把两个 digest 合并成多平台清单并打上 `git-<sha>` 标签。\n\n```mermaid\nsequenceDiagram\n    participant A as build-and-smoke<br/>linux/amd64 (ubuntu-24.04)\n    participant B as build-and-smoke<br/>linux/arm64 (ubuntu-24.04-arm)\n    participant P as promote (ubuntu-24.04)\n    A->>A: buildx 构建 + push-by-digest（无标签）\n    B->>B: buildx 构建 + push-by-digest（无标签）\n    A->>A: smoke-agent-image.sh 真实 CDP HTTP 探测\n    B->>B: smoke-agent-image.sh 真实 CDP HTTP 探测\n    A-->>P: 上传 verified digest artifact\n    B-->>P: 上传 verified digest artifact\n    P->>P: imagetools create → git-<sha> 标签\n    P->>P: cosign 无密钥签名 + 校验平台清单\n    P->>P: 打 :latest 标签 + 读后写重试校验\n```\n\n**TOOL_REFRESH：缓存失效的利器。** 镜像里的运行时工具（Claude Agent SDK、CLI、agent-browser、headroom、feishu-cli 等）默认安装最新版，Docker 层缓存会让旧镜像迟迟不刷新。workflow 把 `TOOL_REFRESH=${{ github.sha }}` 作为 build-arg 写入镜像**第一个层**（`/etc/happyclaw-tool-refresh`），每次 main push 都强制使所有下游层缓存失效，同时保留精确版本 ARG 供回滚。构建完成后镜像内会生成 `happyclaw-tool-versions.txt` 审计文件，记录本次构建实际解析到的每个工具版本，不依赖易失的 Actions 日志。\n\n**冒烟测试必须走真实启动路径。** `scripts/smoke-agent-image.sh` 用 `docker run --detach --interactive` 启动容器——**不覆盖 `--entrypoint`**，让生产 entrypoint 完整执行（先启动 Chromium 再等待 stdin 任务 JSON）；然后循环探测容器内 `http://127.0.0.1:9222/json/version`，用 jq 校验返回的 Browser 版本与 WebSocket URL 字段，最多等待 120 秒。脚本还会校验拉取的镜像架构与预期矩阵架构一致，并按宿主 uid/gid 传入 `HAPPYCLAW_HOST_IDENTITY_MODE` 身份契约。\n\n**无密钥签名与读后写校验。** promote 任务用 `cosign sign` 基于 GitHub OIDC token 做**无密钥签名**（身份即 workflow 的证书身份），随后用 `cosign verify` 按证书身份和 OIDC issuer 反向验证签名；签名前还会用 jq 断言多平台清单恰好包含 `linux/amd64` 和 `linux/arm64` 两个平台，防止"双平台发布"退化成单平台。打 `:latest` 标签后，由于 Docker Hub 的标签变更存在传播延迟，脚本用最多 12 次、每次 5 秒的重试循环做**读后写校验**：反复 `imagetools inspect :latest` 直到 digest 与已签名清单一致——宁可等待，也不把"已发布"误报为失败。\n\nSources: [.github/workflows/docker-publish.yml](.github/workflows/docker-publish.yml#L46-L48) · [.github/workflows/docker-publish.yml](.github/workflows/docker-publish.yml#L49-L90) · [.github/workflows/docker-publish.yml](.github/workflows/docker-publish.yml#L120-L166) · [.github/workflows/docker-publish.yml](.github/workflows/docker-publish.yml#L168-L198) · [container/Dockerfile](container/Dockerfile#L4-L10) · [container/Dockerfile](container/Dockerfile#L243-L258) · [scripts/smoke-agent-image.sh](scripts/smoke-agent-image.sh#L21-L74) · [scripts/smoke-agent-image.sh](scripts/smoke-agent-image.sh#L76-L104)\n\n## 契约测试：CI 自己也在被测试\n\n工程规范最特别的一点是：**CI 工作流本身不是"配置文件不可测"的，而是被 Vitest 契约测试锁死的**。`tests/` 下有三个测试文件专门守护 CI 与工程规范，任何试图绕过规范的手改都会在单元测试阶段被拦截。\n\n| 契约测试 | 守护对象 | 代表性断言 |\n| --- | --- | --- |\n| `docker-publish-workflow-contract.test.ts` | 镜像分发链路 | 双平台矩阵存在、先 smoke 后 latest（按字符串位置断言）、所有 Action 钉 SHA、无 QEMU、无 Docker PAT 字面量、本地入口点（Makefile/scripts/container）不允许出现 `docker build` |\n| `reproducible-build-contract.test.ts` | 可复现构建 | 三份 lockfile 已提交且未被 gitignore、无 `git+ssh:` 依赖、Makefile 与 CI 只用 `npm ci`、checkout/setup-node 钉 SHA、StreamEvent 四份副本内容一致且通过 Prettier、Dockerfile 工具刷新 ARG 完整 |\n| `makefile-runtime-contract.test.ts` | 运行时命令契约 | `make start` 是唯一生产启动路径、无 pm2、备份走 SQLite 快照、恢复前必须 `assert-port-free` |\n\n其中 `docker-publish-workflow-contract.test.ts` 最值得注意：它用 `workflow.indexOf(smoke) < workflow.indexOf(latest)` 断言"冒烟步骤必须出现在打 latest 标签之前"——**发布顺序被写成测试**，防止未来有人调整步骤顺序时无意破坏"先验证后发布"的安全边界。它同时断言 Makefile 与 monitor 路由**不允许**出现本地 `docker build` 能力：镜像只能由 GitHub Actions 构建，用户机器只 `docker pull`。\n\nSources: [tests/docker-publish-workflow-contract.test.ts](tests/docker-publish-workflow-contract.test.ts#L8-L62) · [tests/docker-publish-workflow-contract.test.ts](tests/docker-publish-workflow-contract.test.ts#L64-L106) · [tests/reproducible-build-contract.test.ts](tests/reproducible-build-contract.test.ts#L22-L97) · [tests/makefile-runtime-contract.test.ts](tests/makefile-runtime-contract.test.ts#L7-L31)\n\n## 提交前本地门槛\n\nCI 流水线的每一步都有对应的本地命令——**CI 通过不是"提交后再碰运气"，而是把同样的门槛在本地先跑一遍**。CLAUDE.md 的"提交前门槛"一节给出了官方清单，建议按以下顺序执行：\n\n| 顺序 | 命令 | 对应 CI 验证 | 说明 |\n| --- | --- | --- | --- |\n| 1 | `npm run format:changed` | 格式检查 | `--write` 模式自动修复改动文件 |\n| 2 | `npm run docs:check` | typecheck 中的文档校验 | 悬空链接、make 目标、API 路由索引 |\n| 3 | `make typecheck` | 类型 + 静态检查 | 三端 tsc + 共享类型同步 + Prompt 引用 + 文档 |\n| 4 | `make test` | 单元测试 | 含守护 CI 自身的契约测试 |\n| 5 | `make build` | 生产构建 | 先 `sync-types` 再并发构建三端 |\n| 6 | `git diff --check` | — | 检查空白错误 |\n\n两个注意点：一是**改了 `shared/` 下任何类型后必须运行 `make sync-types`** 并提交生成的副本，否则 CI 的 `git diff --exit-code` 会失败；二是 **SDK/CLI 升级必须走显式的 `make update-sdk`**，生产启动路径（`make start`）故意不触碰依赖图，防止隐式升级引入未验证的运行时变化。真实模型冒烟测试 `npm run test:real-model` 会产生真实请求与费用，且严禁把凭据、Endpoint 或未脱敏回复写入日志——它不属于常规提交门槛，只在需要验证真实 Provider 连通性时手动执行。\n\nSources: [CLAUDE.md](CLAUDE.md#L299-L348) · [Makefile](Makefile#L148-L171) · [.github/workflows/ci.yml](.github/workflows/ci.yml#L44-L48) · [package.json](package.json#L11-L24)\n\n## 下一步阅读\n\n- 想知道 CI 里的单元测试、契约测试与 E2E 各自覆盖什么，读 [测试体系：单元测试、契约测试与真实模型冒烟](29-ce-shi-ti-xi-dan-yuan-ce-shi-qi-yue-ce-shi-yu-zhen-shi-mo-xing-mou-yan)\n- 想理解 CI 校验的共享类型副本为什么存在、如何流动，读 [StreamEvent 流式事件体系与类型同步](11-streamevent-liu-shi-shi-jian-ti-xi-yu-lei-xing-tong-bu)\n- 想了解 Agent 镜像在运行时的使用方式（拉取、挂载、执行模式），读 [Host 与 Container 双执行模式](7-host-yu-container-shuang-zhi-xing-mo-shi)\n- 生产部署时如何配置环境变量、拉取镜像与备份恢复，读 [部署与运维：环境变量、Docker 镜像与备份恢复](5-bu-shu-yu-yun-wei-huan-jing-bian-liang-docker-jing-xiang-yu-bei-fen-hui-fu)';export{e as default};

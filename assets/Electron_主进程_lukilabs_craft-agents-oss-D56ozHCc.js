const n="# Electron 主进程 | lukilabs/craft-agents-oss\n\n\n---\n在 Craft Agents 中，Electron 主进程充当**桌面环境与 Agent 运行时之间的桥梁**。它负责管理应用程序的生命周期、窗口管理、WebSocket RPC 服务器引导、原生操作系统集成（通知、菜单、深度链接、自动更新），以及 Agent 浏览器工具所使用的浏览器窗格子系统。每个渲染器窗口都通过传输层与后端进行通信，而不是直接使用 IPC，这使得该架构能够在 Electron 和无头模式之间轻松移植。\n\n来源：[index.ts](apps/electron/src/main/index.ts#L1-L1008)\n\n## 架构概述[](#架构概述)\n\n主进程遵循分层初始化模式：环境引导 → 服务器引导 → 窗口创建 → 运行时服务。它可以在三种不同的模式下运行——**完整模式**（本地嵌入式服务器）、**仅客户端模式**（连接到远程 `CRAFT_SERVER_URL` 的瘦客户端）和**无头模式**（无 UI 的服务器）——每种模式会禁用不同的子系统。\n\n来源：[index.ts](apps/electron/src/main/index.ts#L20-L175), [index.ts](apps/electron/src/main/index.ts#L352-L897)\n\n## 运行模式[](#运行模式)\n\n主进程支持三种运行模式，由环境变量控制。这些变量决定了初始化哪些子系统，以及渲染器如何连接到后端。\n\n模式\n\n触发条件\n\n服务器\n\n窗口\n\n核心差异\n\n**完整模式**\n\n默认（无环境变量）\n\n嵌入式本地 WS 服务器\n\n创建\n\n所有系统均激活\n\n**仅客户端模式**\n\n设置了 `CRAFT_SERVER_URL`\n\n无（连接到远程）\n\n创建\n\n跳过 `SessionManager`、模型刷新、平台注入\n\n**无头模式**\n\n设置了 `CRAFT_HEADLESS`\n\n嵌入式本地 WS 服务器\n\n不创建\n\n作为后台服务器运行以供远程客户端连接\n\n模式检查发生在 `app.whenReady()` 内部，即在 `WindowManager` 和 `BrowserPaneManager` 初始化之后、服务器引导之前。在仅客户端模式下，预加载脚本会创建一个指向远程 URL 的单一 `WsRpcClient`；在完整模式下，它会创建一个 `RoutedClient`，该客户端可以在工作区切换时在本地和远程服务器之间切换。\n\n来源：[index.ts](apps/electron/src/main/index.ts#L435-L449), [bootstrap.ts](apps/electron/src/preload/bootstrap.ts#L60-L154)\n\n## 环境引导[](#环境引导)\n\n在 Electron 的 `app.whenReady()` 触发之前，主进程会执行关键的环境配置。首先在模块顶层通过 `loadShellEnv()` 加载 Shell 环境，以确保所有后续的子进程生成操作都能继承用户的 Shell 配置文件（[shell-env.ts](apps/electron/src/main/shell-env.ts)）。然后，根据应用程序是否已打包来解析资源路径，并通过 `delimiter` 将二进制工具目录（`uv`、文档转换脚本、包装器命令）前置到 `PATH` 环境变量中。\n\n在引导阶段建立的关键环境变量包括 `CRAFT_IS_PACKAGED`、`CRAFT_RESOURCES_BASE`、`CRAFT_UV`（指向捆绑的 `uv` 二进制文件或后备文件）、`CRAFT_BUN`、`CRAFT_SCRIPTS`、`CRAFT_COMMANDS_ENTRY` 和 `CRAFT_AGENT_VERSION`。捆绑的运行时检查会验证 `uv` 二进制文件是否存在，如果缺失则记录警告。Sentry SDK 使用 `beforeSend` 钩子进行初始化，该钩子会从错误报告中清除授权头、Cookie、API 密钥以及类似凭据的值。机器身份由 `hostname() + homedir()` 的 SHA-256 哈希值生成。\n\n来源：[index.ts](apps/electron/src/main/index.ts#L3-L175)\n\n## 服务器引导[](#服务器引导)\n\n核心后端通过 `@craft-agent/server-core/bootstrap` 中的 `bootstrapServer()` 进行引导，该函数会创建 WebSocket RPC 服务器、`SessionManager`、`OAuthFlowStore`，并连接所有 RPC 处理程序。主进程提供一个 `platformFactory`，用于从 Electron API（app、`nativeImage`、shell、`nativeTheme`）构建 `PlatformServices`，并提供一个 `applyPlatformToSubsystems` 回调，将平台功能注入到模型获取器、会话管理器、搜索服务和图像处理器中。\n\n当通过 `embeddedServerConfig` 启用**服务器模式**时，RPC 服务器会绑定到可配置的主机/端口，并可选启用 TLS。主进程会注册特定于 Electron 的 RPC 处理程序用于服务器配置（`GET_SERVER_CONFIG`、`SET_SERVER_CONFIG`、`GET_SERVER_STATUS`），并验证 TLS 证书路径。当服务器模式在没有 TLS 的情况下绑定到网络地址时，会发出安全警告，因为身份验证令牌将以明文形式发送。\n\n在 Windows 上，引导过程还会验证持久化的 Git Bash 路径，并检查 VC++ 可再发行组件包（`onnxruntime`/`markitdown` 进行文档转换所必需的），设置环境变量以便渲染器后续将其显示为可操作的 Toast 通知。\n\n来源：[index.ts](apps/electron/src/main/index.ts#L522-L805)\n\n## WindowManager[](#windowmanager)\n\n`WindowManager` 类是所有 `BrowserWindow` 生命周期操作的中央控制机构。它维护一个以 `webContentsId` 为键的 `Map<number, ManagedWindow>`，其中每个 `ManagedWindow` 将一个 `BrowserWindow` 与一个 `workspaceId` 配对。该类提供了用于创建、聚焦、关闭和查询窗口的方法，以及工作区重新绑定和对同一工作区的多窗口支持。\n\n窗口创建会遵循来自 `window-state.ts` 的保存状态（边界、专注模式、路由）。每个窗口加载 Vite 开发服务器 URL 或打包后的 `dist/index.html`，查询参数中编码了工作区 ID、深度链接目标和专注模式标志。窗口管理器支持**分层关闭拦截**：当渲染器有打开的模态框时，关闭请求将被延迟，并带有后备超时机制，而不是立即销毁窗口。`setAppQuitting(true)` 方法会在 `Cmd+Q` 期间绕过此拦截，以允许干净地关闭。\n\n事件推送在可用时使用 RPC 事件接收器，为了向后兼容，会回退到 `webContents.send`。管理器还会跟踪每个窗口的工作区 ID，以将推送事件路由到正确的客户端。\n\n来源：[window-manager.ts](apps/electron/src/main/window-manager.ts#L37-L646)\n\n## BrowserPaneManager[](#browserpanemanager)\n\n`BrowserPaneManager` 是主进程中最大的子系统之一（3171 行），它实现了 Agent 浏览器工具所使用的 `IBrowserPaneManager` 接口。它使用 Electron 的 `BrowserWindow` 和 `BrowserView` 管理隔离的浏览器实例，每个实例都有自己的会话分区（`persist:browser-pane`）、工具栏视图、页面视图以及用于 Agent 控制指示器的原生叠加视图。\n\n每个浏览器实例支持基于 CDP 的交互：无障碍树快照、元素点击（带有基于引用的定向）、文本填充、键盘输入、滚动、文件上传以及带可选标注的屏幕截图捕获。屏幕截图系统包含针对隐藏表面的复杂恢复逻辑（`displaySurfaceUnavailableError`）、带有绘制延迟的重试机制，以及网络空闲等待。主题颜色提取使用了类似 Safari 26 的启发式算法——检查 `meta[theme-color]`、用于固定/粘性头部的 `elementsFromPoint`，以及 `body/html` 背景——并通过 `MutationObserver` 进行实时更新。\n\n实例可以绑定到会话（`bindSession`）以进行自动生命周期管理。管理器还通过将弹出窗口（OAuth 重定向、target=\"\\_blank\"）重新挂载到主浏览器窗口中来处理它们，并使用注册表跟踪父子关系以便在销毁时进行清理。\n\n来源：[browser-pane-manager.ts](apps/electron/src/main/browser-pane-manager.ts#L133-L3170)\n\n## 预加载脚本与传输层[](#预加载脚本与传输层)\n\n预加载脚本（[bootstrap.ts](apps/electron/src/preload/bootstrap.ts)）是渲染器与主/后端进程之间的**唯一桥梁**。它通过 `contextBridge.exposeInMainWorld('electronAPI', api)` 暴露完整的 API 接口，其中 `api` 是通过 `buildClientApi()` 使用通道映射构建的，该映射将友好的方法名称映射到 RPC 通道字符串。\n\n在**正常模式**下，预加载脚本会创建一个 `RoutedClient`，它维护两个 `WsRpcClient` 实例：一个用于本地嵌入式服务器，另一个用于当前工作区的服务器（可能是远程的）。`RoutedClient` 会根据 `isLocalOnly()` 谓词检查每个 RPC 通道，以决定使用哪个连接——`window:*` 等通道是仅本地的，而会话通道则是 `REMOTE_ELIGIBLE`。工作区切换会触发客户端交换：旧的工作区客户端断开连接，并通过工厂函数创建一个新的客户端。\n\n在**仅客户端模式**下，单个 `WsRpcClient` 直接连接到远程 URL。明确拒绝到非本地主机的未加密 `ws://` 连接，以防止令牌泄露。\n\n预加载脚本还直接处理 **OAuth 编排**，因为 OAuth 回调服务器必须在用户的机器上运行。`performOAuth`、`startClaudeOAuth` 和 `startChatGptOAuth` 方法遵循多步模式：启动本地回调服务器 → 要求服务器准备流程（PKCE） → 打开浏览器 → 等待重定向 → 完成令牌交换。\n\n传输连接状态变更通过 `ipcRenderer.send('__transport:status', ...)` 报告给主进程，以便故障信息能出现在终端/main.log 中，而不仅仅是渲染器控制台。\n\n来源：[bootstrap.ts](apps/electron/src/preload/bootstrap.ts#L44-L420), [routed-client.ts](apps/electron/src/transport/routed-client.ts#L40-L252)\n\n`RoutedClient` 工作区切换机制是 Craft Agents 混合架构的关键。当用户从本地工作区切换到远程工作区时，预加载脚本会透明地交换活动的 `WsRpcClient`，而无需渲染器感知。仅本地通道（窗口管理、原生菜单）始终路由到嵌入式服务器，无论哪个工作区处于活动状态。此设计记录在[传输与 RPC 层](/lukilabs/craft-agents-oss/17-transport-and-rpc-layer) 中。\n\n## 原生操作系统集成[](#原生操作系统集成)\n\n### 深度链接[](#深度链接)\n\n应用程序注册了 `craftagents://` URL 方案（可通过 `CRAFT_DEEPLINK_SCHEME` 配置以用于多实例开发）。深度链接被解析为包含工作区 ID、视图路由、操作、窗口模式和侧边栏参数的结构化目标。在 macOS 上，链接通过 `open-url` 事件到达；在 Windows/Linux 上，当另一个进程尝试启动时，它们通过 `second-instance` 到达。冷启动链接存储在 `pendingDeepLink` 中，并在初始化完成后处理。\n\n来源：[deep-link.ts](apps/electron/src/main/deep-link.ts#L43-L343), [index.ts](apps/electron/src/main/index.ts#L259-L296)\n\n### 通知与徽章计数[](#通知与徽章计数)\n\n通知服务使用 Electron 的 `Notification` API 来显示从服务器推送的操作系统级通知。点击时，通知会聚焦正确的窗口并导航到关联的会话。徽章计数（未读会话指示器）在不同平台上的渲染方式有所不同：macOS 使用画布合成的 Dock 图标叠加层，Windows 使用 `setOverlayIcon`，Linux 使用 `app.setBadgeCount`。在开发过程中，多实例徽章会在 Dock 图标上显示实例编号。\n\n来源：[notifications.ts](apps/electron/src/main/notifications.ts#L31-L279)\n\n### 应用程序菜单[](#应用程序菜单)\n\n原生菜单基于 `menu-schema.ts` 中的共享模式构建，该模式以与平台无关的格式定义菜单项，然后转换为 Electron 的 `MenuItemConstructorOptions`。菜单事件（新建聊天、设置、快捷键、专注模式、侧边栏切换）通过 RPC 事件接收器推送到聚焦的渲染器。当有可用更新时（以显示“安装更新...”）和语言更改时（为了支持 i18n），菜单会动态重建。\n\n来源：[menu.ts](apps/electron/src/main/menu.ts#L1-L295)\n\n### 电源管理[](#电源管理)\n\n电源管理器使用 Electron 的 `powerSaveBlocker` 来防止系统在 Agent 会话主动处理时进入睡眠状态。它跟踪活动会话的计数，并且仅在启用该设置且至少有一个会话正在运行时才阻止睡眠。\n\n来源：[power-manager.ts](apps/electron/src/main/power-manager.ts#L1-L109)\n\n### 自动更新[](#自动更新)\n\n自动更新使用 `electron-updater`，并启用了自动下载和退出时自动安装。在打包构建中，更新检查在启动时立即运行。系统维护一个 `UpdateInfo` 状态，通过 RPC 事件接收器广播到所有渲染器窗口。通过 electron-updater 的内部状态和文件系统检查，可以检测到在之前会话中被中断的已下载更新。已忽略版本机制允许用户跳过特定更新，同时仍允许手动触发“立即检查”。\n\n来源：[auto-update.ts](apps/electron/src/main/auto-update.ts#L17-L438)\n\n### 网络代理[](#网络代理)\n\n代理设置在两个级别应用：自定义的 `ProtocolProxyDispatcher`（扩展自 undici 的 `Dispatcher`）通过配置的代理路由 Node.js HTTP/HTTPS 请求，并支持 `NO_PROXY` 绕过；而 Electron 的会话代理配置则覆盖渲染器和浏览器窗格的流量。`applyConfiguredProxySettings()` 函数可以安全地在 `app.whenReady()` 之前调用——Electron 会话设置会延迟到应用程序准备就绪。\n\n来源：[network-proxy.ts](apps/electron/src/main/network-proxy.ts#L17-L175)\n\n## 关机序列[](#关机序列)\n\n`before-quit` 事件触发精心排序的清理操作：窗口管理器保存窗口状态，会话管理器刷新所有待处理的写入，浏览器窗格实例被销毁，OAuth 流程存储的清理计时器停止，模型刷新计时器停止，电源阻止器被释放。如果更新正在进行（`isUpdating()`），进程会将控制权交给 `electron-updater` 而不是强制退出。重入保护（`isQuitting` 标志）可防止在 `app.exit(0)` 再次触发事件时进行双重清理。\n\n来源：[index.ts](apps/electron/src/main/index.ts#L929-L994)\n\n## 模块依赖关系图[](#模块依赖关系图)\n\n模块\n\n职责\n\n依赖于\n\n`index.ts`\n\n入口点，生命周期编排\n\n所有其他主模块\n\n`window-manager.ts`\n\nBrowserWindow 生命周期\n\n`window-state.ts`，logger\n\n`browser-pane-manager.ts`\n\nAgent 浏览器工具，BrowserView 管理\n\n`browser-cdp.ts`，`WindowManager`\n\n`handlers/index.ts`\n\nRPC 处理程序注册（核心 + GUI）\n\n`handler-deps.ts`，所有处理程序文件\n\n`deep-link.ts`\n\nURL 方案解析与路由\n\n`WindowManager`，RPC 通道\n\n`network-proxy.ts`\n\nNode + Electron 的 HTTP/HTTPS 代理\n\nundici，共享配置\n\n`auto-update.ts`\n\nelectron-updater 集成\n\nRPC 事件接收器，共享配置\n\n`menu.ts`\n\n原生应用程序菜单\n\n`menu-schema.ts`，`WindowManager`\n\n`notifications.ts`\n\n操作系统通知 + 徽章\n\n`WindowManager`，原生图像\n\n`power-manager.ts`\n\n防止系统睡眠\n\nElectron powerSaveBlocker\n\n`thumbnail-protocol.ts`\n\n自定义 `thumbnail://` 协议\n\n—\n\n`onboarding.ts`\n\n首次运行设置流程\n\n—\n\n## 后续步骤[](#后续步骤)\n\n本页介绍了 Electron 主进程架构。要继续探索桌面应用程序堆栈：\n\n-   **[渲染器 UI 组件](/lukilabs/craft-agents-oss/16-renderer-ui-components)** — React 渲染器如何使用 `electronAPI` 接口并管理 UI 状态\n-   **[传输与 RPC 层](/lukilabs/craft-agents-oss/17-transport-and-rpc-layer)** — 深入探讨 WebSocket 协议、消息信封格式、编解码器和通道路由\n-   **[会话与工作区模型](/lukilabs/craft-agents-oss/18-session-and-workspace-model)** — 会话和工作区的结构化、持久化和同步方式";export{n as default};

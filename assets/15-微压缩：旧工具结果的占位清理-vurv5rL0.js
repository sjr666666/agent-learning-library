const n='微压缩（microcompact）是 MiniCode 长会话上下文管理体系中**成本最低、触发最早**的一级压缩机制。它不调用 LLM、不删除任何消息、不改变会话结构，只做一件事：当上下文利用率越过警戒线时，将**较早的、可重跑工具的结果内容**替换为一个固定占位标记，从而在不破坏对话因果链的前提下释放大量令牌。\n\n## 定位：压缩阶梯中的第一道防线\n\nMiniCode 的代理循环在每一轮发给模型之前，会依次尝试多种压缩手段。它们按成本递增、触发阈值递增的顺序排列，构成一条"阶梯"：能便宜解决的绝不动用昂贵的 LLM 总结。\n\n```mermaid\nflowchart TD\n    A[代理循环每步开始<br/>agent-loop.ts L174] --> B[computeContextStats<br/>计算利用率]\n    B --> C{snipCompact<br/>阈值 0.70}\n    C -->|裁剪中间对话段| D{仍可压缩?}\n    C -->|未触发| E{microcompact<br/>阈值 0.50}\n    D --> E\n    E -->|替换旧工具结果为占位符| F{contextCollapse<br/>阈值 0.75}\n    E -->|未触发| F\n    F -->|LLM 摘要替换区间| G{autoCompact<br/>step 0 且 critical/blocked}\n    F -->|未触发| G\n    G --> H[发送给模型 model.next]\n```\n\n微压缩在这个阶梯中的位置非常特殊：它的触发阈值是 **0.50**，是所有策略中**最低**的——恰好与上下文统计的 `warning` 警戒等级对齐。也就是说，一旦会话进入"警告"区域，第一反应不是删除中间对话、不是调用 LLM 总结，而是先做一次零成本的"清场"：把那些又大又旧、模型大概率不会再看的工具输出换成一句话占位符。Sources: [microcompact.ts](src/compact/microcompact.ts#L6-L13), [constants.ts](src/compact/constants.ts#L1-L5), [token-estimator.ts](src/utils/token-estimator.ts#L177-L206)\n\n## 触发条件：利用率门槛 0.50\n\n微压缩是一个纯函数 `microcompact(messages, model)`，没有任何异步操作与副作用。它首先调用 `computeContextStats` 计算当前上下文的利用率：\n\n```\nutilization = totalTokens / effectiveInput\n```\n\n其中 `effectiveInput` 是模型上下文窗口减去输出预留后的有效输入容量（如 `deepseek-chat` 为 128K − 4K = 124K，Claude Sonnet 4.6 为 200K − 16K = 184K）。只有当 `utilization >= 0.50` 时流程才继续；否则原样返回消息数组（**返回同一引用**，测试对此有专门断言）。Sources: [microcompact.ts](src/compact/microcompact.ts#L10-L13), [token-estimator.ts](src/utils/token-estimator.ts#L177-L206), [model-context.ts](src/utils/model-context.ts#L132-L149)\n\n> 为什么是 0.50？因为 `computeContextStats` 在 0.50 时恰好将 `warningLevel` 置为 `warning`（0.85 为 `critical`，0.95 为 `blocked`）。微压缩的触发点与状态栏告警起点完全一致——用户看到警告的同时，系统已自动开始回收空间。\n\n## 清理范围：可重跑的只读工具\n\n不是所有工具结果都值得清理。微压缩只针对 `COMPACTABLE_TOOLS` 集合内的工具结果，这些工具的共同特征是**只读、确定性输出、可随时重跑**：\n\n| 工具 | 归类理由 | 清理后模型能否自愈 |\n|---|---|---|\n| `read_file` | 文件内容读取，输出可能上万字符 | ✅ 可重新读取同一路径 |\n| `run_command` | 命令输出（构建日志、测试报告等） | ✅ 可重跑命令 |\n| `search_files` | 搜索命中列表 | ✅ 可重新搜索 |\n| `list_files` | 目录列表 | ✅ 可重新列出 |\n| `web_fetch` | 网页抓取内容 | ✅ 可重新抓取 |\n\nSources: [context.ts](src/utils/context.ts#L118-L124)\n\n**被排除的恰恰是状态类工具**：`edit_file`、`write_file`、`patch_file`、`modify_file` 等编辑工具的结果承载了"文件当前是什么状态"这一关键事实，清理后模型将失去对已编辑内容的理解。测试 `preserves non-compactable tool results` 专门验证了 `edit_file` 的大结果在利用率超限时也**不会被清除**。Sources: [microcompact.test.ts](test/microcompact.test.ts#L64-L81)\n\n扫描完成后，代码收集所有满足条件的 `tool_result` 消息下标，并应用保留策略：`RETENTION.KEEP_RECENT_TOOL_RESULTS = 3`——**最近的 3 条可压缩工具结果原样保留**，更早的全部列入清除名单。这个"最近三条"的直觉是：模型刚看到的工具输出仍在推理链中活跃，而更早的输出已随时间衰减为背景噪音。Sources: [microcompact.ts](src/compact/microcompact.ts#L15-L28), [constants.ts](src/compact/constants.ts#L20-L25)\n\n## 占位替换：CLEAR_MARKER 的语义与账本\n\n清除操作不是删除消息，而是把 `content` 替换为固定哨兵字符串：\n\n```\n[Output cleared for context space]\n```\n\n```mermaid\nflowchart LR\n    subgraph 清除前\n        A1["assistant_tool_call<br/>read_file id-1 input"] --> A2["tool_result<br/>80,000 字符内容<br/>≈ 40,000 tokens"]\n    end\n    subgraph 清除后\n        B1["assistant_tool_call<br/>read_file id-1 input（原样保留）"] --> B2["tool_result<br/>[Output cleared for context space]<br/>≈ 17 tokens"]\n    end\n    A2 -. "microcompact" .-> B2\n```\n\n替换保留了消息的**全部结构字段**（`role`、`toolUseId`、`toolName`、`isError`），只改写 `content`。这带来两个关键性质：\n\n- **因果链完整**：模型仍能看到"哪个工具、用什么参数、何时被调用"，只是结果被截断。当它需要旧数据时，可以依据保留的 `toolUseId`/`toolName` 重新发起调用。\n- **账本可预期**：工具结果的估算比率是 2.0 字符/令牌（`CHARS_PER_TOKEN[\'tool_result\'] = 2.0`）。一个 80K 字符的 `read_file` 输出约占 40K 令牌，而 34 字符的占位标记仅约 17 令牌——每清除一条即可释放近 4 万令牌，这是全系统中"性价比"最高的回收手段。Sources: [token-estimator.ts](src/utils/token-estimator.ts#L34-L46), [token-estimator.ts](src/utils/token-estimator.ts#L78-L82)\n\n替换过程还是**幂等且不可变**的：已是指标的消息原样透传、不计入 `changed`；只有实际发生内容改写时才返回新数组，否则返回原引用。原始数组绝不被就地修改。Sources: [microcompact.ts](src/compact/microcompact.ts#L30-L49)\n\n## 执行位置：每步调用、先便宜后昂贵\n\n微压缩在代理循环中的挂载点位于 `snipCompactConversation` 之后、`applyContextCollapseIfNeeded` 之前，且**每一步都会尝试**（不限于第一步），也不受 `snippedThisTurn` 状态门控。Sources: [agent-loop.ts](src/agent-loop.ts#L174-L218)\n\n这个顺序的工程含义有三层：\n\n1. **先剪后清**：`snipCompact`（硬删除中间对话段）是结构性手术，触发条件更苛刻（需满足最少可删消息数与最少释放令牌数）；它失败或未触发时，微压缩仍能兜底清理。\n2. **清了再折叠**：`contextCollapse` 是 LLM 参与的摘要操作，成本高。微压缩先把最肥的旧工具结果瘦身，让后续 LLM 折叠面对的是已经瘦身后的上下文——连折叠操作本身的输入令牌都被省下来了。\n3. **每步重算**：当微压缩真的改写了消息，循环会重新 `computeContextStats` 并通过 `onContextStats` 回调推送最新利用率，状态栏的警告等级可能随之从 `warning` 回落到 `normal`。Sources: [agent-loop.ts](src/agent-loop.ts#L196-L201)\n\n把四层策略放在同一张表里对比，微压缩的"轻"就一目了然：\n\n| 维度 | 微压缩 microcompact | 裁剪 snipCompact | 上下文折叠 contextCollapse | 自动压缩 autoCompact |\n|---|---|---|---|---|\n| 触发阈值 | 0.50 | 0.70 | 0.75 | 0.85 / 0.95 |\n| 是否调用 LLM | ❌ 纯本地 | ❌ 纯本地 | ✅ 摘要生成 | ✅ 全局总结 |\n| 是否删除消息 | ❌ 仅替换内容 | ✅ 删除中间段 | ✅ 区间替换为摘要 | ✅ 保留尾部、压缩前置 |\n| 消息数量是否变化 | ❌ 严格不变 | ✅ 减少 | ✅ 减少 | ✅ 减少 |\n| 调用频率 | 每步 | 每步（每轮限一次） | 每步 | 仅 step 0 |\n\nSources: [constants.ts](src/compact/constants.ts#L1-L25), [agent-loop.ts](src/agent-loop.ts#L221-L236)\n\n## 不变式与测试保障\n\n微压缩的设计可以浓缩为四条不可违背的不变式，`test/microcompact.test.ts` 与 `test/snip-compact.test.ts` 对每条都有直接覆盖：\n\n| 不变式 | 含义 | 验证测试 |\n|---|---|---|\n| 低利用率零开销 | 利用率 < 0.50 时返回同一数组引用 | `returns messages unchanged when utilization is low` |\n| 消息数量守恒 | 清除只替换内容，绝不增删消息 | `assert.equal(result.length, messages.length)` |\n| 原数组不可变 | 函数不就地修改输入 | `does not modify the original array` |\n| 越界工具不碰 | 非 `COMPACTABLE_TOOLS` 结果原样保留 | `preserves non-compactable tool results` |\n| 幂等收敛 | 已清除的消息不产生新变更，返回原引用 | `returns same reference if all tool results are already cleared` |\n| 不碰折叠边界 | `snip_boundary` 消息不受影响 | `microcompact does not clear snip_boundary messages` |\n\nSources: [microcompact.test.ts](test/microcompact.test.ts#L24-L125), [snip-compact.test.ts](test/snip-compact.test.ts#L440-L471)\n\n最后一条尤其值得注意：`snip_boundary` 是裁剪压缩留下的历史段标记，它本身已经非常短小，微压缩对它无操作，体现了各策略之间**互不侵犯**的边界纪律。\n\n## 边界与权衡\n\n微压缩并非没有代价，它是一组明确的取舍：\n\n- **模型会看到占位符**：哨兵文本会被原样送入 API。它依赖模型的常识理解"输出已被清除"，而不是系统提示中的显式指令（代码中没有任何针对该标记的 prompt 说明）。这对主流模型有效，但语义完全靠模型自觉。\n- **只适用于"可重跑"工具**：如果某个 `run_command` 输出是后续决策的唯一依据且命令不可重复执行（如一次性迁移脚本），清除后信息将永久丢失。系统选择信任"模型需要时会主动重跑"。\n- **阈值基于利用率而非绝对量**：0.50 的门槛意味着在超大上下文模型（如 1M 窗口的 Gemini）上，微压缩可能要到会话积累数十万令牌才首次触发；它保护的是"相对紧张"，而非"绝对浪费"。\n\n## 延伸阅读\n\n微压缩是 MiniCode 上下文管理四件套中最轻的一环，建议沿以下路径继续深入：\n\n- 它清理的是**"旧工具结果"**，而 [大工具结果落盘：预览替换与稳定记账](16-da-gong-ju-jie-guo-luo-pan-yu-lan-ti-huan-yu-wen-ding-ji-zhang) 处理的是**"新工具结果"**写入时的超限问题——两者一前一后，共同覆盖工具输出的全生命周期。\n- 它之后的 [自动压缩：触发阈值与失败熔断](12-zi-dong-ya-suo-hong-fa-yu-zhi-yu-shi-bai-rong-duan) 是唯一调用 LLM 的全局手段，阈值更高、成本更重。\n- 它与 [确定性裁剪压缩：保护编辑与错误轮次的安全删除](13-que-ding-xing-cai-jian-ya-suo-bao-hu-bian-ji-yu-cuo-wu-lun-ci-de-an-quan-shan-chu) 共享"不碰编辑工具"的防护哲学，但裁剪是删除消息、微压缩是替换内容。\n- 它所属的整体记账体系见 [供应商用量优先的上下文记账与令牌估算](11-gong-ying-shang-yong-liang-you-xian-de-shang-xia-wen-ji-zhang-yu-ling-pai-gu-suan)。';export{n as default};

const n='HappyClaw 的授权体系不是单一的角色判断，而是一个**双轴正交模型**：第一轴是面向系统管理面的 RBAC（角色 + 细粒度权限），第二轴是面向用户数据的资源级 ACL（以 `created_by` 为核心的工作区所有权）。两条轴交叉处是第三条边界——Host 执行权限。理解这个模型的关键在于一个反直觉的设计：**admin 角色在第一轴拥有全局豁免，但在第二轴不自动绕过任何资源所有权**。系统文档在 ACL-MATRIX 中明确声明了这一原则："admin 不自动绕过工作区所有权"。\n\n```mermaid\ngraph TD\n    subgraph 第一轴:系统管理面\n        A[请求] --> B[authMiddleware 会话认证]\n        B --> C{requirePermission}\n        C -->|manage_system_config| D[系统配置]\n        C -->|manage_users| E[用户管理]\n        C -->|manage_invites| F[邀请码]\n        C -->|view_audit_log| G[审计日志]\n        C -->|manage_billing| H[计费]\n    end\n\n    subgraph 第二轴:资源隔离面\n        I[工作区请求] --> J{canAccessGroup}\n        J -->|created_by 匹配| K[读取/发送]\n        J -->|不匹配| L[404 防枚举]\n        M{canModifyGroup} --> N[owner-only 变更]\n        O{canDeleteGroup} --> P[owner + 非 Home]\n    end\n\n    subgraph 第三边界:Host 执行\n        Q[host 工作区] --> R{hasHostExecutionPermission}\n        R -->|admin| S[允许]\n        R -->|member| T[拒绝]\n    end\n\n    B -.->|固定角色与权限到 Session| I\n```\n\nSources: [docs/ACL-MATRIX.md](docs/ACL-MATRIX.md#L1-L30)、[src/web-context.ts](src/web-context.ts#L340-L420)\n\n## 第一轴：角色、细粒度权限与权限模板\n\n授权链的起点是登录会话。`authMiddleware` 验证 HMAC 签名的 Session Cookie，将用户 ID、角色、状态和权限列表固定到请求上下文，并在会话有效期内动态读取最新用户记录（禁用/删除的账号即时返回 403）[src/middleware/auth.ts](src/middleware/auth.ts#L60-L130)。会话令牌本身使用 HMAC-SHA256 签名，防止数据库或日志泄漏 raw token 后被冒名登录；旧版未签名 Cookie 默认拒绝，仅允许通过 `ALLOW_LEGACY_UNSIGNED_COOKIE` 临时开启一次性升级路径 [src/auth.ts](src/auth.ts#L36-L100)。\n\n系统面共定义 6 项细粒度权限，类型定义与白名单校验都收敛在 `src/types.ts` 与 `src/permissions.ts`：\n\n| 权限 | 覆盖范围 |\n| --- | --- |\n| `manage_system_config` | Provider、系统容量、Host 集成、注册策略、系统外观、Docker pull |\n| `manage_group_env` | 工作区环境变量（仍需 owner + Access） |\n| `manage_users` | 用户创建、禁用、恢复、角色与权限变更 |\n| `manage_invites` | 邀请码管理 |\n| `view_audit_log` | 审计日志查看与导出 |\n| `manage_billing` | 计费管理 |\n\n`hasPermission()` 的判定逻辑是：`admin` 角色直接返回 `true`，否则检查 `permissions` 数组是否包含目标权限 [src/permissions.ts](src/permissions.ts#L60-L84)。这意味着角色是权限的**上限豁免**，而不是替代品——`admin` 天然拥有全部 6 项权限，普通成员则完全依赖细粒度授权。\n\n为简化管理，系统提供了 4 个内置权限模板，供管理员一键套用：\n\n| 模板 | 角色 | 权限集合 | 典型场景 |\n| --- | --- | --- | --- |\n| `admin_full` | admin | 全部 6 项 | 管理员 |\n| `member_basic` | member | 无 | 普通成员 |\n| `ops_manager` | member | `manage_system_config` + `manage_group_env` | 运维管理员 |\n| `user_admin` | member | `manage_users` + `manage_invites` + `view_audit_log` | 用户管理员 |\n\n权限输入经过 `normalizePermissions()` 白名单过滤，未知字符串被丢弃，无法注入非标准权限 [src/permissions.ts](src/permissions.ts#L40-L58)。中间件层将权限检查封装为可组合的 Hono 中间件：`requirePermission`（单权限）、`requireAnyPermission`（任一权限）、`adminRoleMiddleware`（纯角色检查），以及基于它们派生的 `systemConfigMiddleware`、`usersManageMiddleware`、`inviteManageMiddleware`、`auditViewMiddleware` [src/middleware/auth.ts](src/middleware/auth.ts#L150-L187)。\n\nSources: [src/permissions.ts](src/permissions.ts#L1-L84)、[src/middleware/auth.ts](src/middleware/auth.ts#L150-L187)、[docs/ACL-MATRIX.md](docs/ACL-MATRIX.md#L200-L230)\n\n## 第二轴：资源级 ACL 与工作区隔离\n\n资源级授权是这套模型的安全重心。三个核心判定函数 `canAccessGroup`、`canModifyGroup`、`canDeleteGroup` 都以**同一套 owner 规则**应用于所有角色——注意参数只接收 `id` 和 `role`，实现内部从不读取 `permissions`，admin 在此轴没有特殊分支 [src/web-context.ts](src/web-context.ts#L358-L420)。\n\n```mermaid\nflowchart TD\n    A[请求目标工作区] --> B{是 Home?}\n    B -->|是| C[created_by === user.id]\n    B -->|否| D{JID 前缀 web:?}\n    D -->|是 Web Workspace| C\n    D -->|否 IM Chat| E{created_by 存在?}\n    E -->|存在| F[created_by === user.id]\n    E -->|不存在 legacy| G[同 folder 找 sibling Home]\n    G -->|找到| H[用 Home 的 created_by 比对]\n    G -->|找不到| I[fail-closed 拒绝]\n```\n\n三种工作区类型的隔离语义：\n\n- **Home Workspace**：仅创建者本人可访问，永远不可删除。\n- **Web Workspace**：仅创建者本人；`editable`、`deletable` 投影由 owner 判定得出 [src/routes/groups.ts](src/routes/groups.ts#L345-L400)。\n- **IM Chat**：优先使用自身 `created_by`；历史遗留行（无 owner）只允许通过同 folder 的 Home Workspace 解析 owner，解析失败默认拒绝（fail-closed）。\n\n三个函数的分工：`canAccessGroup` 控制读取与发送（列表投影、消息/文件读取、Runtime Session 查看）；`canModifyGroup` 是 owner-only，控制所有变更操作（重命名、切 Agent、stop/interrupt、会话创建删除、Skills/MCP 写入、群聊绑定）；`canDeleteGroup` 在 Modify 之上额外禁止删除 Home Workspace [docs/ACL-MATRIX.md](docs/ACL-MATRIX.md#L30-L60)。\n\n两个值得注意的实现细节：\n\n1. **404 防枚举**：资源不存在与无权访问返回相同的 `404 Group not found`，客户端无法通过响应差异枚举其他用户的工作区 [src/routes/groups.ts](src/routes/groups.ts#L1879-L1885)。\n2. **广播归属动态解析**：`getGroupAllowedUserIds()` 在每次广播时重新解析工作区归属，不缓存 raw-JID 映射，因为归属与绑定是可变的敏感状态——已绑定 IM Chat 的受众以绑定目标工作区的 owner 为准，绑定失效（stale binding）时 fail-closed 返回空集，防止把另一个工作区的对话投递给账号原 owner [src/group-broadcast-acl.ts](src/group-broadcast-acl.ts#L1-L65)。\n\n### 工作区环境变量的叠加检查\n\n`GET|PUT /api/groups/:jid/env` 是双轴叠加的典型示例：必须通过 `canAccessGroup`，Host 工作区必须 admin，非 admin 还必须同时满足 owner + `manage_group_env`。**admin 仍需先通过工作区 Access，不能借角色读取其他用户工作区** [docs/ACL-MATRIX.md](docs/ACL-MATRIX.md#L100-L110)。\n\nSources: [src/web-context.ts](src/web-context.ts#L358-L420)、[src/group-broadcast-acl.ts](src/group-broadcast-acl.ts#L1-L65)、[src/routes/groups.ts](src/routes/groups.ts#L345-L400)\n\n## 第三边界：Host 执行权限与实时撤销\n\nHost 执行模式是独立于工作区所有权的额外边界：**有工作区权限不代表可以执行 Host 操作**。`hasHostExecutionPermission()` 仅看角色是否 admin [src/web-context.ts](src/web-context.ts#L343-L356)，但运行时判定 `canExecuteOnHost()` 更加严格——它要求当前数据库中的用户记录满足三点：角色为 admin、状态为 active、且不在 `revokingHostPrivilegeUserIds` 撤销集合中 [src/host-execution-policy.ts](src/host-execution-policy.ts#L1-L30)。\n\n```mermaid\nsequenceDiagram\n    participant R as Runtime\n    participant DB as SQLite\n    participant H as Host Execution\n    R->>DB: 解析当前 owner 记录\n    DB-->>R: role/status\n    R->>H: canExecuteOnHost(owner)\n    alt admin + active + 未撤销\n        H-->>R: true → 允许 Host\n    else 角色降级 / 禁用 / 正在撤销\n        H-->>R: false → 拒绝\n    end\n```\n\n撤销集合机制的意义在于：Host 权限是**实时特权**而非工作区行的历史属性，管理员降级或禁用用户后，正在进行的 Host 操作会被立即阻断，而不是等下一轮状态加载。代码注释明确要求调用方"在执行点从数据库解析用户并传入当前记录"。\n\n与之配套的是 `admin-host-only-runtime` 的 fail-closed 清理机制：当启用 `adminHostOnlyMode`（仅 admin 可配置，开启后所有工作区强制 Host、Container 写入入口全部拒绝）时，系统在暂停 mutation 之前持久化待清理文件夹标记；若进程在清理中途重启，标记会被恢复并重新阻塞对应工作区队列，直到精确重试完成。标记损坏时保守地阻塞**所有**工作区 [src/admin-host-only-runtime.ts](src/admin-host-only-runtime.ts#L10-L70)。\n\nSources: [src/host-execution-policy.ts](src/host-execution-policy.ts#L1-L30)、[src/web-context.ts](src/web-context.ts#L343-L356)、[src/admin-host-only-runtime.ts](src/admin-host-only-runtime.ts#L10-L70)\n\n## IM Owner Gate：渠道侧的身份锚点\n\nIM 渠道没有 Cookie 和 Web Session，授权锚点是渠道原生的 `owner_im_id`。命令级 Owner Gate `checkImOwnerCommand()` 要求发送者规范化 ID 与记录的 owner 完全一致，涉及变更的命令集合为 `clear`、`bind`、`unbind`、`new`、`sw`、`spawn`、`release_owner` [src/im-command-utils.ts](src/im-command-utils.ts#L255-L330)。\n\nOwner 认领遵循严格的 bootstrap 规则，防止"第一个发命令的人"自动成为群主：\n\n- **1:1 私聊**：可以从第一条持久化人类消息学习 owner（结构上 DM 的发送者无歧义，`isDirectMessageJid()` 通过 JID 结构识别，群聊永远不自动认领）。\n- **群聊**：必须由 owner 显式发送 `/owner_mention` 自我认领，这是唯一被排除在 Owner Gate 之外的 bootstrap 路径——否则无主群聊会永久锁死。\n- **`/release_owner`**：清除 owner 与 allowlist，并把 legacy `owner_mentioned` 降级为 `when_mentioned`，避免 bot 群组静默。\n- **admin break-glass**：`POST /api/groups/:jid/reset-owner` 是 owner 离群/换号后的强制释放通道，admin 角色可触发，但仍要求目标资源存在；释放后下一人通过 `/owner_mention` 重新认领 [src/routes/groups.ts](src/routes/groups.ts#L1610-L1650)。\n\n响应对象与激活方式是**两个独立维度**：`audience_mode` 决定谁能触发（`everyone` 或 `owner_only`），`activation_mode` 决定是否需要 @（`always`、`when_mentioned`、`disabled`）。Legacy `owner_mentioned` 读取时规范化为 `owner_only + when_mentioned` 组合 [src/im-audience-policy.ts](src/im-audience-policy.ts#L1-L55)。\n\n一个容易被忽视的安全细节：**不同 Provider 的原生 sender ID namespace 不得混用**。QQ 的 C2C 与 Group 使用不同 ID 空间，owner 比对必须使用渠道适配器传入的规范化 ID。`ownerImIdFromPersistedSender()` 按 Provider 分别解析格式，无法识别的格式 fail-closed，且"绝不允许后缀匹配" [src/agent-builder-turn-auth.ts](src/agent-builder-turn-auth.ts#L130-L200)。\n\nSources: [src/im-command-utils.ts](src/im-command-utils.ts#L255-L346)、[src/im-audience-policy.ts](src/im-audience-policy.ts#L1-L55)、[src/routes/groups.ts](src/routes/groups.ts#L1610-L1650)\n\n## Agent/MCP 侧的授权一致性\n\nAgent 的 MCP 调用没有 Cookie，但主进程为每个运行注入权威上下文：`ownerUserId`、Workspace/Session 身份、IPC 目录、`ChannelTurnContext` 与能力 Manifest。**跨工作区操作必须经过主进程 owner 检查，不能根据 Agent 文本参数自行扩大范围** [docs/ACL-MATRIX.md](docs/ACL-MATRIX.md#L230-L250)。\n\n定时任务的 IPC/MCP 授权与 Web 面**同源同判**：`task-acl.ts` 的 `canIpcActorAccessGroup()` 允许两条路径——目标是 Agent 自身工作区目录，或该工作区属主同样拥有目标群组。这里刻意不引入 admin bypass，注释给出了完整推理：admin Home Agent 是接触不可信内容的 LLM，而任务 `prompt` 会被按计划重放、以 Bot 身份在目标会话发言并计费到目标工作区——全局 bypass 会让注入内容在其他租户工作区种植或改写 cron 任务 [src/task-acl.ts](src/task-acl.ts#L1-L76)。\n\n```mermaid\ngraph LR\n    A[Scheduled Run] --> B[IPC 授权层]\n    C[SDK Sub-Agent] --> B\n    B --> D{canIpcActorAccessGroup}\n    D -->|目标 = 自身 folder| E[允许]\n    D -->|owner 拥有目标群组| E\n    D -->|其他| F[拒绝]\n    B --> G{Workspace Memory 写?}\n    G -->|scheduled / task / spawn| H[只读]\n    G -->|interactive 顶层 turn| I[按 Workspace ACL]\n```\n\nWorkspace Memory 的写入限制是双层防护：`workspaceMemoryWriteRejection()` 在 IPC 层拦截三类非法写入——非 conversation 类型 Agent、scheduled turn、非 interactive turn，且 mutation 操作（create/update/delete）必须在 MUTATIONS 集合中；`buildWorkspaceMemoryUpdateInput()` 则从架构上保证攻击者控制的 principal 字段永远被权威字段覆盖 [src/workspace-memory-ipc.ts](src/workspace-memory-ipc.ts#L1-L114)。\n\nAgent Builder（turn 级能力）也遵循同样的上下文隔离：scheduled/isolated 任务不可用，非 conversation 类型 Agent 不可用，非默认 HappyClaw 不可用，且每个 conversation session 拥有独立命名空间防止并发会话互相借用确认 [src/agent-builder-turn-auth.ts](src/agent-builder-turn-auth.ts#L20-L55)。\n\n跨组消息授权 `canSendCrossGroupMessage()` 的规则链：admin Home 可发任意组 → 同 folder 组互通 → member Home 可发给同 `created_by` 的组 → 绑定到源工作区的 IM 渠道可达。四条规则依次降级，默认拒绝 [src/cross-group-acl.ts](src/cross-group-acl.ts#L1-L36)。\n\nSources: [src/task-acl.ts](src/task-acl.ts#L1-L76)、[src/workspace-memory-ipc.ts](src/workspace-memory-ipc.ts#L1-L114)、[src/cross-group-acl.ts](src/cross-group-acl.ts#L1-L36)\n\n## 特权升级防护\n\n持有 `manage_users` 的非 admin 账号理论上可以管理其他用户，因此系统在用户管理路由内置了 `denyEscalationToTarget()`：**非 admin actor 不能对持有自己未持有权限的目标做破坏性操作**（改密码、改状态、改/撤 session、删除/恢复、改权限）[src/routes/admin.ts](src/routes/admin.ts#L230-L252)。\n\n该防护最精妙之处在于对"两步绕过"的预判：\n\n1. `PATCH permissions=[]` 把 target 权限抹平 → 此时 `denyEscalationToTarget` 会比较通过。\n2. `PATCH password=X` 重置密码接管账号。\n\n因此触发条件（`triggersPrivCheck`）将 `password`、`status`、`permissions` 三者全部纳入——只要任何一个字段出现，就先做权限集比较 [src/routes/admin.ts](src/routes/admin.ts#L420-L460)。除此之外还有四道硬约束：\n\n- 非 admin 不能管理 admin 目标（`target.role === \'admin\'` 早拦截）。\n- 非 admin 不能变更角色。\n- 不能移除自己的 admin 角色。\n- 不能禁用/删除自己的账号。\n\nOwner 运行时侧还有一道兜底闸门：`checkOwnerActive()` 是一个纯函数，群组创建者（`created_by`）被禁用或删除后，消息处理必须立即停止，而不是等下一次服务重启——因为 `loadState` 只在启动时过滤非 active 用户，重启时序与缓存滞后都会造成窗口期 [src/owner-gate.ts](src/owner-gate.ts#L1-L30)。\n\nSources: [src/routes/admin.ts](src/routes/admin.ts#L220-L260)、[src/routes/admin.ts](src/routes/admin.ts#L395-L470)、[src/owner-gate.ts](src/owner-gate.ts#L1-L30)\n\n## 安全设计模式总结\n\n| 模式 | 实现位置 | 目的 |\n| --- | --- | --- |\n| 双轴正交 | permissions.ts + web-context.ts | 系统权限与资源所有权互不替代 |\n| admin 无资源旁路 | canAccessGroup/ModifyGroup/DeleteGroup | 防止管理角色越权读取租户数据 |\n| 404 防枚举 | routes/groups.ts 各分支 | 资源不存在与无权访问同响应 |\n| fail-closed 解析 | canAccessGroup、group-broadcast-acl | 无法解析 owner 时默认拒绝 |\n| 实时特权 | host-execution-policy.ts | Host 权限不继承自历史行，可即时撤销 |\n| 动态归属解析 | group-broadcast-acl.ts | 每次广播重算，不缓存可变绑定 |\n| 权限提升防护 | routes/admin.ts denyEscalationToTarget | 非 admin 不能获得比自己高的权限 |\n| 写面收窄 | workspace-memory-ipc.ts | scheduled/task 上下文只读 |\n| bootstrap 例外最小化 | im-command-utils.ts | 仅 owner_mention 豁免 Gate |\n| 持久化 fail-closed 标记 | admin-host-only-runtime.ts | 清理中断后重启仍保持阻塞 |\n\n## 验证要求与测试覆盖\n\nACL-MATRIX 对任何 ACL 修改规定了 8 项验证维度：owner 成功、非 owner 拒绝、admin 是否绕过必须显式测试（默认不绕过）、Host/Container 分支、资源不存在与跨用户返回值、多渠道账号凭据隔离、HTTP/WebSocket/IM/MCP 同一动作一致性、失败路径不留半写状态 [docs/ACL-MATRIX.md](docs/ACL-MATRIX.md#L280-L310)。\n\n对应测试分布：`tests/routes-workspaces.test.ts`（工作区 ACL 契约）、`tests/owner-gate.test.ts` 与 `tests/im-owner-gate.test.ts`（IM Owner Gate）、`tests/im-audience-policy.test.ts`（响应对象）、`tests/routes-groups-owner-acl.test.ts`（群组 owner 路由）、`tests/channel-binding-rest-contract.test.ts`（绑定原子性）、`tests/mcp-runtime-secret-boundary.test.ts`（MCP 密钥边界）、`tests/host-execution-policy.test.ts`（Host 实时权限）。\n\nSources: [docs/ACL-MATRIX.md](docs/ACL-MATRIX.md#L280-L333)\n\n## 下一步阅读\n\nRBAC 模型中的"资源隔离"依赖于工作区数据的物理存储与备份一致性，可继续阅读 [数据目录结构与一致性备份恢复](23-shu-ju-mu-lu-jie-gou-yu-zhi-xing-bei-fen-hui-fu)；管理面权限对应的系统配置与用户管理后端属于 [主服务架构与核心模块划分](9-zhu-fu-wu-jia-gou-yu-he-xin-mo-kuai-hua-fen)；IM Owner Gate 与响应对象在消息分发中的完整链路见 [IM 命令系统与消息分发](17-im-ming-ling-xi-tong-yu-xiao-xi-fen-fa)。';export{n as default};

const e='Miniclaw 的一次对话回复不是一次性返回的完整文本，而是由数十到数百个**流式事件**（StreamEvent）驱动的过程：文字逐段浮现、工具调用逐个展开、Token 用量在回合末尾落账。这些事件诞生于容器内的 Agent Runner，穿越后端的记账与围栏，最终同时呈现在 Web 聊天界面和 IM 流式卡片上。支撑这条链路的前提是：三个互相独立构建的 TypeScript 子项目对"什么是事件、事件长什么样"持有**完全一致**的定义。本页解析这份契约的类型结构，以及仓库用怎样的工程机制保证三份拷贝永不漂移。\n\n## 契约的形态：单一事实源与三份受控拷贝\n\nMiniclaw 没有为共享类型建立 npm workspace 或发布内部包，而是采用了更朴素的**复制式共享**：`shared/stream-event.ts` 是唯一允许手工编辑的规范源（canonical source），文件头注释明确声明它会由构建步骤复制到 `container/agent-runner/src/stream-event.types.ts`、`src/stream-event.types.ts`、`web/src/stream-event.types.ts` 三个位置，并且"禁止直接编辑副本——请编辑本文件然后执行 `make build`"。选择复制而非包引用有其结构性原因：Agent Runner 运行在独立容器内，拥有自己的 `package.json` 和编译配置，无法依赖宿主仓库的模块解析。\n\nSources: [stream-event.ts](shared/stream-event.ts#L1-L10)\n\n三个子项目在这份契约中扮演的角色截然不同，构成一条单向数据流：\n\n| 端 | 持有的拷贝 | 角色定位 | 关键消费文件 |\n| --- | --- | --- | --- |\n| Agent Runner（容器） | `container/agent-runner/src/stream-event.types.ts` | **生产者**：把 SDK 原始消息转译为 StreamEvent | [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L1-L10) |\n| 后端服务 | `src/stream-event.types.ts` | **编排者**：记账、门控、过期围栏、多路分发 | `src/index.ts`、`src/web.ts` |\n| Web 前端 | `web/src/stream-event.types.ts` | **渲染消费者**：事件归约成聊天 UI 状态 | [chat.ts](web/src/stores/chat.ts#L111-L112) |\n\n值得注意的是，跨端一致性纪律并不止于生成类型。`stream-processor.ts` 中有一段注释专门警告：SDK 任务终态集合在 `web/src/stores/chat.ts`、`src/web.ts`、`src/index.ts` 各有一份等价映射，新增终态时必须人工同步检查，否则漏判会导致关流被永久推迟。这说明契约的维护是"生成类型 + 人工约定"的双重体系。\n\nSources: [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L27-L30)\n\n## 类型全景：24 种事件与信封字段\n\n契约的核心是两个层次。第一层是 `StreamEventType` 联合类型，枚举了 24 种事件，覆盖从文本增量到上下文审计的全部运行时信号；第二层是单一的大而全的信封接口 `StreamEvent`——它不使用可辨识联合（discriminated union），而是把所有可能载荷作为可选字段平铺在一个接口里，由各端按 `eventType` 自行取用。\n\nSources: [stream-event.ts](shared/stream-event.ts#L12-L36)、[stream-event.ts](shared/stream-event.ts#L289-L396)\n\n| 类别 | eventType | 关键载荷字段 |\n| --- | --- | --- |\n| 文本流 | `text_delta`、`thinking_delta` | `text` |\n| 工具生命周期 | `tool_use_start` / `tool_use_end` / `tool_progress` / `tool_result` | `toolName`、`toolUseId`、`parentToolUseId`、`toolResult` |\n| Hook 信号 | `hook_started` / `hook_progress` / `hook_response` | `hookName`、`hookEvent`、`hookOutcome` |\n| 任务与工作流 | `task_start` / `task_progress` / `task_updated` / `task_notification` | `taskId`、`taskStatus`、`workflowRun`（完整工作流快照） |\n| 上下文与记忆 | `context_audit`、`memory_recall`、`compact_boundary` | `contextAudit`（含 Prompt/Skills/MCP 全量审计） |\n| 用户可见通知 | `notification`、`prompt_suggestion`、`permission_denied`、`todo_update` | `summary`、`todos`、`permissionDenied` |\n| 计量与状态 | `usage`、`status`、`init` | `usage`（含 `eventId` 幂等键）、`statusText` |\n| 调试透传 | `raw_sdk_event` | `rawType`、`rawEvent` |\n\n信封上有几个字段值得特别关注，它们承担着**跨端关联**职责：`turnId` 把一个用户回合产生的所有事件串成一条流；`queryRunId` 标记产生该事件的具体 GroupQueue 查询尝试，是后端丢弃过期尝试事件的依据；`agentScope` 区分 `main` / `task` / `subagent` / `system` 四种产出主体；`displayLevel` 则是三级显示优先级——`primary` 内联呈现、`detail` 进追踪面板、`debug` 仅开发者可见。\n\nSources: [stream-event.ts](shared/stream-event.ts#L290-L304)、[stream-event.ts](shared/stream-event.ts#L69-L84)\n\n## 同步机制：构建期同步、校验期围栏与 CI 终审\n\n理解了"为什么要同步"，再看"如何保证同步"。Miniclaw 为此部署了三道相互独立的防线，任何一道单独失效都不会让漂移进入主干。同步脚本的核心细节是**内容比对后才复制**：只有目标文件不存在或 `cmp -s` 判定内容不同时才执行 `cp`，刻意避免无谓的时间戳更新触发下游增量构建。同一脚本还顺带同步另外两组共享文件——`image-detector.ts` 与 `channel-prefixes.ts` 各自的两份拷贝（Web 前端不需要这两个模块）。\n\nSources: [sync-stream-event.sh](scripts/sync-stream-event.sh#L12-L27)、[sync-stream-event.sh](scripts/sync-stream-event.sh#L29-L45)\n\n| 防线 | 触发时机 | 执行载体 | 失败行为 |\n| --- | --- | --- | --- |\n| 第一道：构建期同步 | `make build`、`make sync-types` | [sync-stream-event.sh](scripts/sync-stream-event.sh#L1-L7) | 无声修复：以规范源覆盖副本 |\n| 第二道：本地校验 | `make typecheck`、`make start` | [check-stream-event-sync.sh](scripts/check-stream-event-sync.sh#L11-L25) + Makefile `_check-sync` | 报告 `MISSING`/`OUT OF SYNC` 并输出 diff，非零退出提示执行 `make sync-types` |\n| 第三道：CI 终审 | 每次 PR 与 main 推送 | ci.yml「Verify generated type sources」步骤 | 同步后 `git diff --exit-code`，提交的拷贝若不同步直接挂掉 |\n\n第二道防线实际包含两种互补策略：`make typecheck` 在完成三端 tsc 之后运行 diff 校验脚本，属于"事后拦截"；而 `make start` 通过内部的 `_check-sync` 目标用 `find shared/ -newer <target>` 按 mtime 探测规范源是否比任一副本新，发现漂移就自动补一次同步再继续启动，属于"事前自愈"。第三道防线的精妙之处在于它校验的不是"当前工作区是否同步"，而是"**已提交进仓库的四份文件是否本来就一致**"——先跑 `make sync-types`，再用 `git diff --exit-code` 断言没有产生任何变更，配合 prettier 格式检查，彻底杜绝"在我机器上是同步的"这类争辩。\n\n```mermaid\nflowchart LR\n    SRC["shared/stream-event.ts\\n单一事实源"] -->|"sync-stream-event.sh\\n内容比对后复制"| AR["container/agent-runner/\\nstream-event.types.ts"]\n    SRC --> BE["src/\\nstream-event.types.ts"]\n    SRC --> WEB["web/src/\\nstream-event.types.ts"]\n    CHK["make typecheck:\\ncheck-stream-event-sync.sh"] -.diff 校验.-> AR\n    MKS["make start:\\n_check-sync mtime 探测"] -.自动补同步.-> SRC\n    CI["CI:\\nsync 后 git diff --exit-code\\n+ prettier --check"] -.终审.-> BE\n```\n\nSources: [Makefile](Makefile#L40)、[Makefile](Makefile#L73-L78)、[Makefile](Makefile#L150-L153)、[Makefile](Makefile#L185-L188)、[ci.yml](.github/workflows/ci.yml#L44-L48)\n\n## 生产端剖析：从 SDK 消息到 stdout 标记帧\n\n契约的生产侧封装在 `StreamEventProcessor` 类中，它从原始的 `runQuery()` 中抽离出全部流式处理逻辑：主线程与嵌套任务的文本/思考缓冲与冲刷、顶层与 Skill/Task 工具的状态跟踪、子 Agent 消息向 StreamEvent 的转换。每产出一个事件，`emitStreamEvent()` 都会把它包装进统一信封 `ContainerOutput`——设置 `status: \'stream\'`、`result: null`，事件本体挂在 `streamEvent` 字段上。信封本身也是跨端契约的一部分：`inputTurnId`、`turnId`、`sessionId` 等关联字段与四种 `status`（`success`/`error`/`stream`/`closed`）共同定义了 Runner 对外的全部输出语义。\n\nSources: [stream-processor.ts](container/agent-runner/src/stream-processor.ts#L1-L10)、[stream-processor.ts](container/agent-runner/src/stream-processor.ts#L212-L219)、[types.ts](container/agent-runner/src/types.ts#L289-L330)\n\n序列化环节采用**标记帧协议**而非裸 JSON 行：`writeOutput()` 先打印 `---MINICLAW_OUTPUT_START---`，然后是一行完整的 `JSON.stringify(correlatedOutput)`，最后以 `---MINICLAW_OUTPUT_END---` 收束。文件头注释说明这是有意设计——一次查询可以发出多个结果帧（例如 Agent Teams 场景）。宿主侧的 `agent-output-parser.ts` 持有完全相同的两个标记常量来切割 stdout，这又是一处需要两端严格一致的字符串级契约。\n\nSources: [index.ts](container/agent-runner/src/index.ts#L11-L14)、[index.ts](container/agent-runner/src/index.ts#L809-L823)、[agent-output-parser.ts](src/agent-output-parser.ts#L14-L15)\n\n## 编排端剖析：记账、门控与过期围栏\n\n后端收到 Runner 输出后并不做无脑转发，而是依次经过四道处理。首先是**用量记账**：`usage` 事件被送入 `writeUsageRecords` 写入持久化账本，以 `eventId` 保证重放安全（幂等），并按 `batchIndex`/`batchCount` 合并分批到达的账目，只有收到最后一批才继续向下传递合并后的完整用量。其次是**答案投影**：回合输出协调器的 `reduceStreamEvent()` 从事件流中计算出对外可见的回答文本，供 IM 卡片等消费方取用。\n\nSources: [index.ts](src/index.ts#L15380-L15420)、[index.ts](src/index.ts#L15421-L15436)\n\n第三道是**交互模式门控**：`shouldBroadcastSdkStreamEvent()` 决定哪些事件有资格离开服务端——`assistant` 模式全量放行；`proactive` 模式下模型思绪、工具、用量一律视为隐私，仅放行 `requesting`、`idle`、`interrupted` 三种生命周期状态以维持公共 spinner 的启停。第四道是**过期围栏** `RunStreamFence`：当查询尝试 A 被替换为 B 后，A 的迟到回调不能被误标为 B 的输出。带 `queryRunId` 的事件走 `observeExact()` 精确比对活跃 run；遗留路径走 `observe()` 用 `turnId` 归属推断，每个 JID 维护最多 64 个 turn 归属的有界缓存。被判定为过期的事件在 debug 日志后被静默丢弃。\n\nSources: [workspace-interaction-runtime.ts](src/workspace-interaction-runtime.ts#L141-L157)、[run-stream-fence.ts](src/run-stream-fence.ts#L27-L45)、[run-stream-fence.ts](src/run-stream-fence.ts#L67-L84)、[web.ts](src/web.ts#L2925-L2944)\n\n通过围栏的事件最终兵分两路。一路进入 `broadcastStreamEvent()`：这里有一个容易被忽略的安全设计——`context_audit` 事件在此被**截留在服务端**，其携带的主机路径、Prompt 装配细节只写入运维诊断快照与告警日志，绝不发往浏览器；其余事件被包装为 `{type: \'stream_event\', chatJid, event, agentId?, runId}` 的 WS 出站消息广播给订阅用户，同时累积进服务端流式快照用于断线恢复与停机持久化。另一路则喂给 IM 渠道的流式卡片会话。\n\nSources: [web.ts](src/web.ts#L2899-L2923)、[web.ts](src/web.ts#L2945-L2963)、[run-context-snapshot.ts](src/run-context-snapshot.ts#L1-L1)\n\n## 消费端剖析：Web Store 的批处理渲染与 IM 卡片投影\n\nWeb 端的消费入口是 `ChatView` 中的 WebSocket 订阅：`wsManager.on(\'stream_event\', ...)` 过滤出当前会话的 JID 后，把 `(event, agentId, runId)` 三元组交给 Zustand store 的 `handleStreamEvent`。Store 层的第一组动作是复刻后端的纪律：跳过清空历史窗口期、再次忽略 `context_audit`、用 `${chatJid}#agent:${agentId}` 构造运行时 JID 并以 `runId` 做 run 级作用域校验——前后端围栏语义保持对称。\n\nSources: [ChatView.tsx](web/src/components/chat/ChatView.tsx#L495-L501)、[chat.ts](web/src/stores/chat.ts#L2503-L2514)\n\n面对高频 `text_delta`/`thinking_delta`，store 不逐条 setState，而是把增量文本攒进按 `agent:`/`main:` 键控的 pending 缓冲，用 **requestAnimationFrame 合帧**后在下一帧统一冲刷——每个动画帧至多触发一次状态更新，这是长回答不卡顿的关键。随后 `applyStreamEvent` 作为纯函数归约器按 `eventType` 分派：文本追加受最大长度截断保护、工具调用按 `toolUseId` 维护活跃列表并在结束时计算耗时、带 `parentToolUseId` 的子 Agent 事件被路由进独立的任务面板而不污染主流。`displayLevel` 在此落地为可见性规则——例如 `raw_sdk_event` 只有 `primary` 级别才会进入最近事件列表。\n\nSources: [chat.ts](web/src/stores/chat.ts#L2516-L2549)、[chat.ts](web/src/stores/chat.ts#L1341-L1367)、[chat.ts](web/src/stores/chat.ts#L1368-L1420)、[chat.ts](web/src/stores/chat.ts#L1602-L1613)\n\nIM 卡片是同一事件流的第二个投影面。`feedStreamEventToCard()` 将事件类型映射到 `StreamingSession` 的卡片操作：`text_delta` 触发整卡文本追加、`tool_use_start` 同时拉起 thinking 态与工具条目并推送"正在使用某工具"的系统状态、`tool_result` 把脱敏截断后的工具输出写进时间线。这里同样贯彻跨端对齐原则——带 `parentToolUseId` 的子 Agent 思考被排除在主卡思考面板之外，注释明确指出这与 Web 端 `applyStreamEvent` 及服务端快照的处理逻辑三方对齐。\n\nSources: [index.ts](src/index.ts#L591-L650)、[index.ts](src/index.ts#L15438-L15475)\n\n```mermaid\nflowchart LR\n    SP["StreamEventProcessor\\nagent-runner 生产端"] -->|"emitStreamEvent"| CO["ContainerOutput\\nstatus:\'stream\'"]\n    CO -->|"stdout 标记帧"| HP["宿主解析\\nagent-output-parser"]\n    HP --> UL["usage 账本\\neventId 幂等 + 分批合并"]\n    UL --> RED["reduceStreamEvent\\n可见答案投影"]\n    RED --> GATE{"交互模式门控"}\n    GATE -->|"proactive 只放行生命周期状态"| FENCE["RunStreamFence\\n过期 run 丢弃"]\n    GATE -->|"assistant 全量"| FENCE\n    FENCE -->|"context_audit 截留"| SNAP["服务端诊断快照"]\n    FENCE --> WSMSG["WS 广播\\ntype:\'stream_event\'"]\n    FENCE --> CARD["feedStreamEventToCard\\nIM 流式卡片"]\n    WSMSG --> STORE["chat store\\nrAF 合帧 + applyStreamEvent"]\n```\n\n上图 prerequisite：阅读前需知道 ContainerOutput 是 Runner 输出的统一信封（见生产端一节），RunStreamFence 是后端丢弃过期查询尝试事件的围栏（见编排端一节）。\n\n## 延伸阅读\n\nStreamEvent 契约是贯穿 Miniclaw 多条主线的枢纽：想了解事件的**源头协议**——Runner 如何经 stdin/stdout 与宿主通信——请阅读 [Pi Agent Runner 协议：stdin/stdout 结构化结果与 IPC 通道](10-pi-agent-runner-xie-yi-stdin-stdout-jie-gou-hua-jie-guo-yu-ipc-tong-dao)；关心事件在投递链路上的**可靠性与断线恢复**（本页提到的 streaming 快照只是入口）请转向 [可靠性状态机：Inbox、Turn、Outbox 与 Streaming Card 的持久投递](17-ke-kao-xing-zhuang-tai-ji-inbox-turn-outbox-yu-streaming-card-de-chi-jiu-tou-di)；想看消费端 store 所处的整体前端架构请参考 [Web 前端架构：React 19、Zustand 状态管理与页面路由](24-web-qian-duan-jia-gou-react-19-zustand-zhuang-tai-guan-li-yu-ye-mian-lu-you)。若你想动手修改这份契约，记住唯一的正确路径：编辑 `shared/stream-event.ts`，执行 `make build`（或 `make sync-types`），并让 CI 的 `git diff --exit-code` 替你确认三份拷贝已同步。';export{e as default};

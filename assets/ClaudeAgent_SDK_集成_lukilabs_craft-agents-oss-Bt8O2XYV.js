const e="# ClaudeAgent SDK 集成 | lukilabs/craft-agents-oss\n\n\n---\n**ClaudeAgent** 是 Craft Agents 中的主要 AI 后端，它将 Anthropic 的 `@anthropic-ai/claude-agent-sdk` 封装为一个支持流式传输、具备权限感知且连接来源的 Agent。它继承 `BaseAgent` 以复用共享基础设施（权限管理、来源追踪、会话恢复、技能解析），同时实现 Claude 特有的关注点：SDK 子进程生命周期、Claude Code 工具预设、自适应思考、OAuth 凭据注入以及 SDK 错误分类。本页将追踪从 SDK 选项构建、事件流式传输到清理的整个集成架构。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L1-L1)、[base-agent.ts](packages/shared/src/agent/base-agent.ts#L160-L270)、[backend/types.ts](packages/shared/src/agent/backend/types.ts#L307-L571)\n\n## SDK 接口与导入契约[](#sdk-接口与导入契约)\n\nClaudeAgent 消耗了 Anthropic Claude Agent SDK 中一个精简的子集。[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L1) 顶部的导入语句揭示了确切的接口：`query`、`createSdkMcpServer`、`tool`、`AbortError` 以及几个仅导入类型的声明（`Query`、`SDKUserMessage`、`SDKAssistantMessageError`、`Options`、`SDKResultSuccess`）。这是整个 Monorepo 中对 Claude Agent SDK 的唯一依赖——所有其他包完全通过 [backend/types.ts](packages/shared/src/agent/backend/types.ts#L307-L571) 中定义的与提供商无关的 `AgentBackend` 接口与 `ClaudeAgent` 交互。\n\n`query` 函数是 SDK 的主要入口点：它生成或恢复 Claude Code 子进程，向回流式传输 `SDKResultSuccess` 事件，并管理 `cwd` 目录下的对话记录持久化。`createSdkMcpServer` 和 `tool` 用于注册进程内工具服务器以及 SDK 子进程回调调用的单个工具定义。`AbortError` 是 SDK 查询被中止时抛出的异常类型，`ClaudeAgent` 会捕获该异常并将其转换为干净的流终止，而不是未处理的错误。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L1)、[backend/types.ts](packages/shared/src/agent/backend/types.ts#L307-L571)\n\n## 架构概述[](#架构概述)\n\nClaudeAgent 集成遵循分层架构，其中 SDK 子进程处理核心的 Agent 循环（模型推理、工具调度、对话管理），而 Node.js 宿主进程提供横切关注点：权限执行、来源连接、会话状态和 UI 事件转换。\n\nSyntax error in textmermaid version 11.6.0\n\nSDK 子进程作为一个黑盒 Agent 循环运行：它接收用户消息，决定调用哪些工具，并发出流式事件。宿主进程通过 SDK 钩子（`PreToolUse`/`PostToolUse`）拦截工具执行，以执行权限策略、防止超大图像进入、追踪前置读取，并注入引导消息——所有这些都不需要修改 SDK 的内部决策逻辑。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L771-L950)、[base-agent.ts](packages/shared/src/agent/base-agent.ts#L186-L269)\n\n## 构建与配置流水线[](#构建与配置流水线)\n\n[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L534-L627) 中的 `ClaudeAgent` 构造函数执行多阶段初始化，将 Claude 特有的 `ClaudeAgentConfig` 接口与 `BaseAgent` 消耗的与提供商无关的 `BackendConfig` 连接起来。\n\n**阶段 1 — 模型解析**：通过优先考虑 `session.model` 而非 `config.model` 来解析有效模型，确保通过 UI 在会话中途进行的模型切换优先生效。从模型注册表（`getModelContextWindow`）中查找上下文窗口大小，默认情况下 Opus 4.6 为 1,000,000，其他模型为 200,000。\n\n**阶段 2 — BackendConfig 构建**：组装一个 `BackendConfig` 对象，包含提供商 `'anthropic'`、所有会话/回调引用、工作区配置以及可选的基础设施引用（MCP 池、自动化系统、环境覆盖）。此对象被传递给 `super(backendConfig, DEFAULT_MODEL, CLAUDE_CONTEXT_WINDOW)`，后者会初始化所有核心模块：`PermissionManager`、`SourceManager`、`PromptBuilder`、`PathProcessor`、`UsageTracker` 和 `PrerequisiteManager`。\n\n**阶段 3 — Claude 特有初始化**：实例化事件适配器，从 `session.sdkSessionId` 捕获 SDK 会话 ID（如果正在恢复），并存储分支参数（`branchFromSdkSessionId`、`branchFromSdkCwd`、`branchFromSdkTurnId`）以用于 SDK 级别的分叉操作。通过 `initializeModeState` 初始化权限模式状态，并带有同步 `safeMode` 标志和在模式更改时通知 UI 的回调。\n\n**阶段 4 — 会话工具注册**：`registerSessionScopedToolCallbacks` 为 `SubmitPlan`、认证请求工具、`call_llm` 和 `spawn_session` 接入进程内回调——这些工具在 SDK 子进程内执行，但需要与宿主进程通信。\n\n**阶段 5 — 配置监视器**：在非无头模式下，启动配置文件监视器，以热重载来源配置更改而无需重启会话。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L534-L627)、[base-agent.ts](packages/shared/src/agent/base-agent.ts#L220-L270)\n\n## 通过 `postInit()` 注入认证[](#通过-postinit-注入认证)\n\n与同步运行且无法执行异步凭据查找的构造函数不同，[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L629-L681) 中的 `postInit()` 处理必须在第一次 `chat()` 调用之前完成的异步认证注入。SDK 子进程在第一次 `query()` 调用时延迟生成，因此 `postInit()` 有一个机会窗口来设置所需的环境变量。\n\n该方法调用 `getToken()`，后者通过优先级链尝试解析凭据：(1) 通过 `getValidClaudeOAuthToken()` 获取 Claude OAuth 令牌，(2) 通过 `getCredentialManager()` 查找凭据管理器，(3) 直接读取 `ANTHROPIC_API_KEY` 环境变量。一旦获得令牌，[llm-connections.ts](packages/shared/src/config/llm-connections.ts) 中的 `resolveAuthEnvVars()` 会设置 `ANTHROPIC_API_KEY` 并清除任何可能导致 SDK 子进程误路由到错误提供商端点的过时 Bedrock/Vertex 路由变量。\n\n返回的 `PostInitResult` 指示认证是否成功注入，并可选择性地暴露带有严重性级别的警告（例如，OAuth 令牌过期），以便 UI 可以适当地渲染。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L629-L681)\n\n## `chatImpl` 核心循环[](#chatimpl-核心循环)\n\n[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L771-L900) 中的 `chatImpl` 方法是 ClaudeAgent 集成的核心——一个异步生成器，负责编排用户与 Claude SDK 单次交互的完整生命周期。\n\n### SDK 选项组装[](#sdk-选项组装)\n\n在调用 `query()` 之前，该方法会构建一个全面的 `Options` 对象，用于配置 SDK 子进程行为的各个方面：\n\n选项\n\n用途\n\n关键细节\n\n`model`\n\n活动模型标识符\n\n可能会追加 `[1m]` 后缀以选择加入 1M 上下文\n\n`systemPrompt`\n\n带有 Claude Code 预设的系统提示词\n\nMini Agent 改用精简的自定义提示词\n\n`tools`\n\n可用工具集\n\n完整模式使用预设的 `claude_code`，Mini Agent 使用显式列表\n\n`mcpServers`\n\nMCP 服务器配置\n\n会话工具、文档服务器、按来源的代理服务器\n\n`permissionMode`\n\nSDK 权限绕过\n\n`'bypassPermissions'` + `allowDangerouslySkipPermissions`\n\n`hooks`\n\nPreToolUse/PostToolUse 回调\n\n权限检查、图像守卫、前置追踪、自动化\n\n`cwd`\n\n用于存储对话记录的工作目录\n\n使用 `sdkCwd` 保持一致性；分支分叉时使用父级的 cwd\n\n`thinking`\n\n扩展思考配置\n\n面向 Anthropic 的自适应 effort，面向兼容提供商的 Token 预算\n\n`stderr`\n\n子进程 stderr 捕获\n\n保留最后 20 行用于诊断\n\n`permissionMode: 'bypassPermissions'` 设置至关重要：它告诉 SDK 跳过其内置的权限 UI，并通过 `PreToolUse` 钩子将所有权限决策委托给宿主进程。这使得 Craft Agents 能够实现更丰富的权限模型（安全模式、白名单、审批 TTL、命令哈希），超越了 SDK 原生的允许/拒绝语义。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L900-L1000)\n\n### 1M 上下文窗口选择加入[](#1m-上下文窗口选择加入)\n\n即使对于支持 1M 的模型，Anthropic 的 API 也默认使用 200K 上下文。集成通过在模型标识符后附加 `[1m]` 后缀（例如 `claude-sonnet-4-20250514[1m]`）来选择加入扩展窗口。这种方法适用于 API 密钥和 OAuth 认证路径。拦截器还会剥离任何 SDK 注入的 Beta 请求头以防止冲突，如 [claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L929-L935) 中所述。此行为受 `enable1MContext` 配置标志控制（默认：`true`）。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L929-L935)\n\n### Mini Agent 模式[](#mini-agent-模式)\n\nMini Agent 是一种为快速配置编辑优化的轻量级执行模式。当 `systemPromptPreset` 为 `'mini'` 时，Agent 会激活受限配置：仅 6 个工具（`Read`、`Edit`、`Write`、`Glob`、`Grep`、`Bash`）、单个 MCP 服务器（`session`）、禁用扩展思考，以及不带 Claude Code 预设的精简系统提示词。如 [claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L942-L953) 中所述，与完整模式相比，这减少了约 70% 的每轮 Token 消耗。\n\nMini Agent 配置集中在 `BaseAgent.getMiniAgentConfig()` 中，使用共享常量 `MINI_AGENT_TOOLS` 和 `MINI_AGENT_MCP_KEYS`，确保 Claude 和 Codex 后端应用相同的限制。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L822-L953)、[base-agent.ts](packages/shared/src/agent/base-agent.ts#L656-L704)\n\n### 来源代理服务器[](#来源代理服务器)\n\nClaudeAgent 不是从 SDK 子进程直接连接每个 MCP 来源（这会在并发会话中导致连接重复），而是使用 [claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L400-L441) 中的 `createSourceProxyServers()` 创建**代理服务器**。集中式 `McpClientPool` 中的每个已连接来源 slug 都会获得一个进程内的 `createSdkMcpServer` 包装器，将工具调用路由到该池。SDK 自动生成的 `mcp__{serverKey}__` 前缀命名随后会产生正确的工具名称，如 `mcp__linear__createIssue`。\n\n这种代理模式意味着 MCP 池拥有主进程中的所有连接，而 SDK 子进程通过池的 HTTP 端点或进程内代理与来源交互——消除了每会话的连接开销，并实现了集中式的连接生命周期管理。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L400-L441)、[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L834-L838)\n\n## SDK 钩子系统[](#sdk-钩子系统)\n\n钩子系统是 ClaudeAgent 在不修改 SDK 本身的情况下，将宿主进程逻辑注入 SDK Agent 循环的地方。钩子在 [claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L955-L1049) 中作为包含 `PreToolUse` 回调匹配器的 `Options.hooks` 对象进行组装。\n\n### PreToolUse 钩子链[](#pretooluse-钩子链)\n\n`PreToolUse` 钩子在每次工具执行前触发，并实现顺序检查链：\n\n1.  **前置追踪**：如果工具是 `Read`，文件路径会注册到 `PrerequisiteManager` 中，以追踪在允许其他工具之前是否已读取技能指南文件。\n\n2.  **图像大小守卫**：对于针对图像文件的 `Read` 调用（通过扩展名检测），钩子会根据 `IMAGE_LIMITS.MAX_RAW_SIZE`（约 3.5MB 原始大小 / 5MB base64）检查文件大小。超大的图像会通过 `config.onImageResize` 触发异步调整大小（Electron 提供原生图像缩放），或者使用描述性错误阻止工具。此守卫在所有其他检查之前运行，因为进入对话历史的超大图像会永久破坏会话——API 会以 400 错误拒绝后续轮次。\n\n3.  **工具使用前检查**：`runPreToolUseChecks()` 处理安全模式执行（阻止所有变更工具）、危险命令检测以及针对 `PermissionManager` 的命令白名单检查。\n\n4.  **权限门控**：`checkToolPermission()` 根据当前权限模式、命令危险性分类以及任何现有的白名单条目，评估工具是否需要用户批准。如果需要权限，它会触发 `onPermissionRequest` 并暂停，直到用户通过 `respondToPermission()` 响应。\n\n5.  **引导注入**：如果存在 `pendingSteerMessage`（由 `redirect()` 设置），它将作为 `additionalContext` 注入并在传递后清除。\n\n6.  **自动化钩子**：来自 `automations.json` 的用户自定义钩子通过 `automationSystem.buildSdkHooks()` 合并，允许工作区级别的自定义行为注入。\n\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L955-L1100)\n\n### PostToolUse 钩子[](#posttooluse-钩子)\n\n工具使用后钩子由 `automationSystem.buildSdkHooks()` 生成，可能包括在 `automations.json` 中定义的日志记录、指标收集或自定义自动化触发器。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L955-L960)\n\n## 事件流式传输与转换[](#事件流式传输与转换)\n\nSDK 的 `query()` 函数返回一个低级 SDK 事件流。ClaudeAgent 通过 `ClaudeEventAdapter`（位于 [backend/claude/event-adapter.ts](packages/shared/src/agent/backend/claude/event-adapter.ts)）将这些事件管道化，将其转换为 `@craft-agent/core` 中定义的与提供商无关的 `AgentEvent` 类型。这种适配器模式隔离了 Claude SDK 事件与会话层和 UI 使用的统一事件协议之间的格式差异。\n\n关键的事件转换包括将 SDK `assistant` 消息块转换为 `text` 事件，将 SDK `tool_use` 块转换为 `tool_start`/`tool_result` 事件，以及将 SDK 错误转换为带有用户友好消息的类型化 `typed_error` 事件。适配器在构造函数中实例化，带有用于调试日志和 SDK 错误映射的回调，使其可以独立测试。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L109-L109)、[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L532-L533)\n\n## 思考级别集成[](#思考级别集成)\n\nClaudeAgent 通过 [claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L128-L157) 中的 `resolveClaudeThinkingOptions()` 将 Craft Agents 的五级思考规模（`off`、`low`、`medium`、`high`、`max`）映射到 Anthropic 的思考配置。该映射是感知提供商的：\n\n-   **真正的 Anthropic 后端**：使用自适应思考（`type: 'adaptive'`），并将 `effort` 参数从 `THINKING_TO_EFFORT` 映射（例如，`medium` → `'medium'`）。这让模型可以动态决定思考的深度。\n-   **Anthropic 兼容提供商**（自定义 Base URL）：回退到显式的 `maxThinkingTokens` 预算，因为兼容 API 可能不支持自适应思考。\n-   **非 Claude 模型**：完全禁用思考，因为扩展思考是 Claude 特有的能力。\n\nToken 预算因模型系列而异——与默认模型（4K–32K）相比，Haiku 模型获得较小的预算（2K–8K），定义在 [thinking-levels.ts](packages/shared/src/agent/thinking-levels.ts#L67-L82) 中。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L128-L157)、[thinking-levels.ts](packages/shared/src/agent/thinking-levels.ts#L18-L82)\n\n## 权限系统集成[](#权限系统集成)\n\nClaudeAgent 中的权限系统通过双层架构运行。SDK 的原生权限系统被显式绕过（`permissionMode: 'bypassPermissions'`），而所有执行都通过委托给 `PermissionManager`（继承自 `BaseAgent`）的 `PreToolUse` 钩子链进行。\n\n危险命令——包括 `rm`、`sudo`、`git push`、`curl`、`wget` 等在 [claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L217-L224) 中定义的硬编码集合——无论当前的权限模式如何，始终需要明确的用户批准。权限请求负载包含丰富的元数据：用于去重的命令哈希、用于限时白名单的审批 TTL、影响描述以及应用名称上下文。\n\n全局权限系统（`setGlobalPermissionHandler`、`requestToolPermission`、`resolveGlobalPermission`）提供了一种模块级机制，允许在外部进程中运行的 MCP 工具通过相同的 UI 流程请求权限。这些与用于 SDK 原生工具调用的实例级 `pendingPermissions` 映射是不同的。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L207-L297)、[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L683-L765)\n\n## 会话恢复与分支分叉[](#会话恢复与分支分叉)\n\nClaudeAgent 实现了两种处理不同故障模式的会话连续性机制：\n\n**恢复**（由恢复失败触发）：当 SDK 找不到之前的会话记录时，`BaseAgent` 中的 `buildRecoveryContext()` 会将最后 N 条对话消息格式化为 `<conversation_recovery>` XML 块，并预置到用户消息中。这使得 Agent 能够在丢失 SDK 会话状态的情况下自然继续。[base-agent.ts](packages/shared/src/agent/base-agent.ts#L717-L745) 中的实现将每条消息截断为 1000 个字符以防止上下文膨胀。\n\n**分支分叉**（由 UI 分支操作触发）：尝试两种策略。首先，SDK 级别的分叉使用 `branchFromSdkSessionId` 和 `branchFromSdkTurnId` 让 SDK 在特定轮次原生分叉对话。如果失败（例如跨工作目录分支），回退策略通过 [claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L2612-L2627) 中的 `generateBranchFallbackContext()` 生成一个迷你摘要上下文，并将其作为 `<branch_seed_context>` 块注入。第三种策略——种子分支会话——通过 `buildBranchSeedContext()` 注入多达 24 条原始父消息，每条限制在 1200 个字符。\n\n跨不同工作目录（例如 git worktree ↔ 主仓库）的分支分叉需要将父级的 `sdkCwd` 作为 `cwd` 选项传递。SDK 将记录存储在 `~/.claude/projects/{cwd-hash}/` 下，因此不匹配的 CWD 会导致“No conversation found”错误。ClaudeAgent 通过在 [claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L920-L925) 的分叉尝试期间有条件地使用 `branchFromSdkCwd` 来处理此问题。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L2612-L2668)、[base-agent.ts](packages/shared/src/agent/base-agent.ts#L717-L783)、[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L2565-L2572)\n\n## 错误分类与映射[](#错误分类与映射)\n\nSDK 错误通过 [claude-sdk-error-mapper.ts](packages/shared/src/agent/claude-sdk-error-mapper.ts#L1-L280) 中实现的三层错误映射流水线进行分类：\n\n1.  **SDK 错误码**：来自 SDK 子进程的原始 `SDKAssistantMessageError`。\n2.  **调试日志解析**：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L2152-L2213) 中的 `parseApiErrorFromDebugLog()` 方法读取 Claude Code 调试日志文件以提取结构化的 API 错误详情（错误类型、消息、请求 ID）。\n3.  **拦截器捕获的错误**：统一的网络拦截器捕获 SDK 可能未暴露的 HTTP 级别错误元数据（状态码、响应体）。\n\n`classifyFailure()` 函数将错误分为三类：`provider`（服务器端问题，如过载、500 错误）、`network`（连接失败、DNS 问题、超时）和 `unknown`。每种类型都映射到不同的用户友好错误，并提供可操作的建议——例如，提供商错误建议稍后重试，而网络错误建议检查连接。\n\n来源：[claude-sdk-error-mapper.ts](packages/shared/src/agent/claude-sdk-error-mapper.ts#L88-L280)、[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L2152-L2310)\n\n## SDK 子进程环境[](#sdk-子进程环境)\n\n子进程环境由 [options.ts](packages/shared/src/agent/options.ts#L208-L258) 中的 `getDefaultOptions()` 和 [options.ts](packages/shared/src/agent/options.ts#L187-L206) 中的 `buildClaudeSubprocessEnv()` 构建。关键的环境配置包括：\n\n-   **可执行文件解析**：`setPathToClaudeCodeExecutable()` 覆盖 SDK 对捆绑环境（Electron）的自动检测。`setExecutable()` 覆盖 Node/Bun 运行时路径。\n-   **网络拦截器**：`setInterceptorPath()` 注入一个 `--require` 标志，将拦截器包加载到 SDK 子进程中，用于 API 错误捕获和 MCP 模式增强。\n-   **代理支持**：[proxy-env.ts](packages/shared/src/config/proxy-env.ts) 中的 `getProxyEnvVars()` 注入 `HTTP_PROXY`、`HTTPS_PROXY` 和 `NO_PROXY` 变量。\n-   **每会话覆盖**：`envOverrides`（来自 `ClaudeAgentConfig`）最后合并，允许特定的连接值（如自定义 `ANTHROPIC_BASE_URL`）避免被并发会话覆盖。\n\n[options.ts](packages/shared/src/agent/options.ts#L38-L115) 中的 `ensureClaudeConfig()` 函数会写入一个 Claude Code 配置文件（带有用于 Windows 兼容性的 BOM 处理），SDK 子进程在启动时读取该文件。它包含代理设置和其他 SDK 级别的配置。\n\n来源：[options.ts](packages/shared/src/agent/options.ts#L38-L258)\n\n## 重定向与引导[](#重定向与引导)\n\nClaudeAgent 通过 [claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L2326-L2347) 中的 `redirect()` 覆盖支持流中消息注入。与具有原生引导功能的后端（PiAgent）不同，ClaudeAgent 将重定向消息存储为 `pendingSteerMessage`，并在下一次 `PreToolUse` 钩子调用时将其作为 `additionalContext` 注入。如果在轮次结束前没有工具触发，生成器会产出一个 `steer_undelivered` 事件，以便会话层知道重新排队该消息。这种务实的方法避免了 SDK 级别对话操作的复杂性，同时为轮次中修正提供了合理的用户体验。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L2326-L2347)\n\n## 清理与拆解[](#清理与拆解)\n\n[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L2486-L2515) 中的 `destroy()` 方法执行级联清理序列。它首先调用 `super.destroy()`（这会停止配置监视器、清除权限白名单、重置来源/前置/使用状态，并断开 MCP 池连接），然后处理 Claude 特有的清理：通过 `cleanupModeState()` 重置模式状态，注销会话范围的工具回调，以及清理会话范围的工具状态。`close()` 方法还会将内部状态引用置空。\n\n`super.destroy()` 中的 MCP 池断开连接是触发即忘的（`.catch()` 吞没错误）。这是有意为之——连接拆解不应抛出异常并可能掩盖真正的清理失败。然而，这意味着连接泄漏可能会悄无声息。如果在调试 MCP 连接耗尽问题，请检查调试日志中的“Failed to disconnect MCP pool”消息。\n\n来源：[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L2486-L2515)、[base-agent.ts](packages/shared/src/agent/base-agent.ts#L845-L859)\n\n## 会话范围工具系统[](#会话范围工具系统)\n\n需要跨越 SDK 子进程边界的工具使用 [session-scoped-tools.ts](packages/shared/src/agent/session-scoped-tools.ts) 中定义的会话范围工具回调注册表。这些工具（`SubmitPlan`、`source_oauth_trigger`、`call_llm`、`spawn_session`、`update_user_preferences`、`transform_data`）在 SDK 子进程内执行，但通过以会话 ID 为键的进程内回调映射将结果传回宿主进程。\n\n这在架构上不同于其他后端处理会话工具的方式。如 [base-agent.ts](packages/shared/src/agent/base-agent.ts#L360-L376) 中所述，ClaudeAgent 的会话范围工具通过 Claude Agent SDK 在进程内运行（回调注册表可以直接工作，因为工具和回调都在同一进程中）。像 CodexAgent 这样的后端必须从其事件流中检测工具完成情况并调用 `handleSessionMcpToolCompletion()`，因为它们的会话工具运行在具有独立隔离内存的单独 MCP 服务器子进程中。\n\n来源：[session-scoped-tools.ts](packages/shared/src/agent/session-scoped-tools.ts#L1-L1)、[base-agent.ts](packages/shared/src/agent/base-agent.ts#L360-L423)\n\n## ClaudeContext：会话工具外观[](#claudecontext会话工具外观)\n\n[claude-context.ts](packages/shared/src/agent/claude-context.ts#L96-L329) 中的 `createClaudeContext()` 函数创建一个 `SessionToolContext` 对象，该对象实现了 `@craft-agent/session-tools-core` 中的接口。此上下文为会话范围工具提供对配置验证、来源管理、凭据操作、MCP 连接测试和自动化验证的访问——所有这些都限定在当前会话和工作区范围内。\n\n该上下文是一个外观，将工具操作路由到适当的共享模块：配置验证通过 `validators.ts`，来源操作通过 `sources/storage.ts`，MCP 验证通过 `mcp/validation.ts`，凭据通过凭据管理器。这将会话工具实现与特定的存储和验证实现解耦，同时为测试提供了干净的依赖注入表面。\n\n来源：[claude-context.ts](packages/shared/src/agent/claude-context.ts#L78-L329)\n\n## 后端工厂集成[](#后端工厂集成)\n\nClaudeAgent 通过 [backend/factory.ts](packages/shared/src/agent/backend/factory.ts) 中的后端工厂基于 `BackendConfig` 中的 `provider` 字段进行实例化。当 `provider` 为 `'anthropic'` 时，工厂使用解析后的配置创建一个 `ClaudeAgent`。`BackendConfig.providerType` 字段携带更丰富的提供商信息（例如 `'anthropic'`、`'anthropic_bedrock'`、`'anthropic_vertex'`、`'anthropic_compat'`），ClaudeAgent 使用这些信息来实现感知提供商的行为，如思考级别映射和错误分类。\n\n[backend/internal/drivers/anthropic.ts](packages/shared/src/agent/backend/internal/drivers/anthropic.ts) 中的驱动注册表提供了 Anthropic 特有的驱动实现，而 [backend/internal/runtime-resolver.ts](packages/shared/src/agent/backend/internal/runtime-resolver.ts) 中的运行时解析器将 `BackendHostRuntimeContext` 映射到 `options.ts` 所需的可执行文件路径和拦截器包位置。\n\n来源：[backend/factory.ts](packages/shared/src/agent/backend/factory.ts#L1-L1)、[backend/types.ts](packages/shared/src/agent/backend/types.ts#L576-L623)、[claude-agent.ts](packages/shared/src/agent/claude-agent.ts#L128-L157)\n\n## 后续步骤[](#后续步骤)\n\n为了加深你对 ClaudeAgent 在更广泛架构中集成的理解，请继续阅读 Agent 后端系统中的相邻页面：\n\n-   [BaseAgent 抽象](/lukilabs/craft-agents-oss/9-baseagent-abstraction) — ClaudeAgent 继承的共享基类，涵盖模板方法模式、核心模块初始化以及所有后端必须满足的抽象契约。\n-   [PiAgent 子进程协议](/lukilabs/craft-agents-oss/11-piagent-subprocess-protocol) — 展示了不同的子进程协议（Pi Coding Agent）如何以根本不同的引导和会话管理语义实现相同 `AgentBackend` 接口的替代后端。\n\n对于 ClaudeAgent 依赖的基础设施：\n\n-   [MCP 客户端池](/lukilabs/craft-agents-oss/12-mcp-client-pool) — ClaudeAgent 的来源代理服务器委托给的集中式连接池。\n-   [权限模式系统](/lukilabs/craft-agents-oss/13-permission-mode-system) — ClaudeAgent 的 PreToolUse 钩子链实现的多模式权限执行。\n-   [自动化引擎](/lukilabs/craft-agents-oss/14-automation-engine) — 工作区级别的自动化系统，其 `buildSdkHooks()` 的输出被合并到 ClaudeAgent 的钩子配置中。";export{e as default};

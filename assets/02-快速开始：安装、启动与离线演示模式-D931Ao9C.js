const n='本页是 MiniCode 的入门第一站。你将学会三件事：如何通过三步命令完成本地安装、如何用三种方式启动程序，以及如何在不配置任何 API 凭据的情况下，用内置的 Mock 模型离线体验完整的代理循环。本页只覆盖"装得上、起得来、能离线试"这条最短路径，配置文件的细节、交互界面和斜杠命令的完整用法，会在文末给出后续阅读入口。\n\n在开始之前，你只需要准备：一台装有 Node.js 与 npm 的电脑（项目通过 `tsx` 直接运行 TypeScript 源码，编译目标为 ES2022）、一个终端，以及可选的 Anthropic API 密钥——**离线演示模式不需要任何密钥**。\n\nSources: [package.json](package.json#L6-L15), [tsconfig.json](tsconfig.json#L2-L13)\n\n## 第一步：安装 MiniCode\n\n安装只有三条命令。进入仓库目录后，先安装依赖，再运行交互式安装器：\n\n```bash\ncd mini-code\nnpm install\nnpm run install-local\n```\n\n其中 `npm run install-local` 实际执行的是 `tsx src/install.ts`，也就是直接运行 TypeScript 源码中的安装器，而不是把项目编译成二进制再安装。整个安装流程可以概括为下图：\n\n```mermaid\nflowchart TD\n    A["cd mini-code"] --> B["npm install<br/>安装依赖（diff、zod、tsx 等）"]\n    B --> C["npm run install-local<br/>运行 src/install.ts"]\n    C --> D["安装器读取现有配置作为默认值"]\n    D --> E["提问 Model name"]\n    D --> F["提问 ANTHROPIC_BASE_URL"]\n    D --> G["提问 ANTHROPIC_AUTH_TOKEN"]\n    E --> H["写入 ~/.mini-code/settings.json"]\n    F --> H\n    G --> H\n    H --> I["生成启动器 ~/.local/bin/minicode"]\n    I --> J{"PATH 已包含目标目录？"}\n    J -->|是| K["任意终端输入 minicode 即可启动"]\n    J -->|否| L["提示把 export PATH 加入 ~/.bashrc 或 ~/.zshrc"]\n```\n\n值得注意的是，安装器在提问前会先加载现有的有效配置作为默认值（预填在提示符的 `[默认值]` 中），这意味着**重复运行安装器可以安全地修改或升级已有配置**，不会从零开始覆盖。配置会被写入独立的 `~/.mini-code/` 目录，与其它本地工具（如 Claude Code 的 `~/.claude/`）互不干扰。\n\nSources: [README.zh-CN.md](README.zh-CN.md#L136-L150), [src/install.ts](src/install.ts#L52-L58)\n\n## 安装器会问你什么\n\n安装器依次询问三个问题，回答后按回车确认。下表说明了每个问题的用途：\n\n| 提示词 | 默认值 | 用途 | 能否留空 |\n|---|---|---|---|\n| `Model name` | 已有配置中的模型名（首次为空） | 设定默认模型，会同时写入 `ANTHROPIC_MODEL` 环境变量 | 不能，为空会反复提示 |\n| `ANTHROPIC_BASE_URL` | `https://api.anthropic.com` | API 网关地址，可指向代理或兼容服务 | 不能 |\n| `ANTHROPIC_AUTH_TOKEN` | 已有配置中的 token（显示为 `[saved]`） | API 认证令牌，输入时以明文回显 | **不能**，为空直接报错终止 |\n\n回答完成后，安装器会把三项内容以 `env` 块的形式写入 `~/.mini-code/settings.json`，同时把模型名写到顶层 `model` 字段。注意一个关键细节：**安装器要求 `ANTHROPIC_AUTH_TOKEN` 非空**——这是安装阶段的门槛，但并不意味着启动阶段一定需要它，因为离线演示模式会绕开真实 API。\n\nSources: [src/install.ts](src/install.ts#L60-L86)\n\n## 安装后：文件落在哪里\n\n安装完成会输出两个关键路径：配置文件位置和启动命令位置。两者的默认位置都可以通过环境变量自定义：\n\n| 产物 | 默认路径 | 自定义方式 |\n|---|---|---|\n| 配置文件 | `~/.mini-code/settings.json` | 设置 `MINI_CODE_HOME` 可整体迁移 `~/.mini-code/` 目录 |\n| 用户级 MCP 配置 | `~/.mini-code/mcp.json` | 同上，随 `MINI_CODE_HOME` 一起移动 |\n| 启动器脚本 | `~/.local/bin/minicode` | 设置 `MINI_CODE_BIN_DIR` 可指定其它目录 |\n\n启动器本身是一个极简的 bash 脚本：它不做任何逻辑，只是把参数透传给仓库内的 `bin/minicode`，再由后者调用 `tsx` 去执行 `src/index.ts`。把整个调用链展开就是：**终端输入 `minicode` → `~/.local/bin/minicode` → 仓库 `bin/minicode` → `node` 执行 `tsx` → `src/index.ts`**。\n\n安装器最后会检查目标目录是否已加入 `PATH`。如果不在，会提示你把 `export PATH="$HOME/.local/bin:$PATH"` 加入 `~/.bashrc` 或 `~/.zshrc`；如果在，则直接提示"现在你可以在任意终端输入 `minicode` 启动"。\n\nSources: [src/config.ts](src/config.ts#L36-L45), [src/install.ts](src/install.ts#L88-L117), [bin/minicode](bin/minicode#L1-L8)\n\n## 三种启动方式\n\n安装完成后，你有三种启动方式，适用场景各不相同：\n\n| 方式 | 命令 | 适用场景 |\n|---|---|---|\n| 正式启动 | `minicode` | 安装完成后日常使用，任意目录下可用 |\n| 本地开发 | `npm run dev` | 在仓库内直接以源码方式运行，等价于 `tsx src/index.ts` |\n| 离线演示 | `MINI_CODE_MODEL_MODE=mock npm run dev` | 不配置任何 API 凭据，用 Mock 模型体验完整流程 |\n\n前两种方式走真实 API，需要你在安装时配置过认证信息；第三种方式通过一个环境变量把模型适配器切换成内置的 Mock 实现，完全不需要网络和密钥。如果你已经完成安装，也可以直接运行 `MINI_CODE_MODEL_MODE=mock minicode` 进入离线演示。\n\nSources: [README.zh-CN.md](README.zh-CN.md#L151-L157), [package.json](package.json#L8-L15)\n\n## 启动时发生了什么\n\n无论从哪个入口进入，最终都会汇聚到 `src/index.ts` 的主函数。启动管线按顺序执行以下步骤：\n\n```mermaid\nflowchart LR\n    subgraph Entry["入口"]\n        CMD["minicode 命令"]\n        DEV["npm run dev"]\n    end\n    subgraph Boot["src/index.ts 启动管线"]\n        ARGS["解析 --resume / --fork 参数"]\n        MGMT["管理命令分流<br/>minicode mcp / skills"]\n        CFG["加载运行时配置<br/>失败时降级为 null"]\n        TOOLS["构建工具注册表 + MCP 注入<br/>（MCP 失败不影响启动）"]\n        PERM["初始化权限管理器"]\n        MODEL{"MINI_CODE_MODEL_MODE<br/>是否为 mock？"}\n        MOCK["MockModelAdapter"]\n        ANTH["AnthropicModelAdapter"]\n        TTY{"标准输入输出是 TTY？"}\n        TUI["全屏交互 TUI"]\n        FALLBACK["非交互 readline 回退"]\n    end\n    CMD --> ARGS\n    DEV --> ARGS\n    ARGS --> MGMT\n    MGMT --> CFG\n    CFG --> TOOLS\n    TOOLS --> PERM\n    PERM --> MODEL\n    MODEL -->|是| MOCK\n    MODEL -->|否| ANTH\n    MOCK --> TTY\n    ANTH --> TTY\n    TTY -->|是| TUI\n    TTY -->|否| FALLBACK\n```\n\n这个管线里有三个值得初学者注意的"韧性"设计：\n\n1. **配置加载失败不致命**：`loadRuntimeConfig()` 被包在 try/catch 中，失败时 `runtime` 降级为 `null`，程序继续启动（在 Mock 模式下这是常态——没有配置也能跑）。\n2. **MCP 注入失败不致命**：`hydrateMcpTools()` 的失败被吞掉，个别 MCP 服务器连不上不会阻塞整个会话。\n3. **模型选择只有一处开关**：`process.env.MINI_CODE_MODEL_MODE === \'mock\'` 这一行决定了走 Mock 还是真实 API，这也是离线演示模式的全部秘密。\n\nSources: [src/index.ts](src/index.ts#L55-L102)\n\n## 离线演示模式：零配置体验完整代理循环\n\n离线演示模式是 MiniCode 最友好的入门方式。它的核心是 `src/mock-model.ts` 中的 `MockModelAdapter`——一个**确定性的、不发起任何网络请求的模型适配器**。它实现与真实 Anthropic 适配器相同的 `ModelAdapter` 接口，因此能驱动完整的 `model → tool → model` 代理循环：识别你的指令 → 发起工具调用 → 拿到工具结果 → 生成回复。\n\nMock 模型识别一组专用指令，并映射到真实的内置工具上：\n\n| 在 Mock 模式下输入 | 触发的工具调用 | 效果 |\n|---|---|---|\n| `/tools` | 无（直接回复） | 列出 7 个可用工具名 |\n| `/ls` 或 `/ls <目录>` | `list_files` | 列出当前目录或指定目录内容 |\n| `/grep 关键词::路径` | `grep_files` | 按模式搜索，路径可省略 |\n| `/read <文件>` | `read_file` | 读取文件内容 |\n| `/cmd <命令> <参数...>` | `run_command` | 执行 shell 命令 |\n| `/write 路径::内容` | `write_file` | 写入文件（无内容分隔符时会提示用法） |\n| `/edit 路径::查找::替换` | `edit_file` | 替换文件中的文本片段 |\n\n工具结果返回后，Mock 模型会根据工具类型生成固定的中文回复：`list_files` 回复"目录内容如下：…"，`read_file` 回复"文件内容如下：…"，写入/编辑类直接回显工具输出。如果你输入一个不认识的指令，它会返回一段"最小骨架版本"提示，把上面 7 个可试指令全部列出来——这实际上就是一份内置的入门菜单。\n\n因为 Mock 模型完全不访问网络，你**不需要 API 密钥、不需要配置 `settings.json`、也不需要关心额度消耗**，就能观察多步工具调用如何串起来。这对理解代理循环的骨架、以及后续阅读 [代理循环：model → tool → model 的多步执行流程](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng) 是最直观的铺垫。\n\nSources: [src/mock-model.ts](src/mock-model.ts#L24-L178), [src/index.ts](src/index.ts#L82-L86)\n\n## 常见问题排查\n\n| 现象 | 原因 | 解决办法 |\n|---|---|---|\n| 安装器提示 `ANTHROPIC_AUTH_TOKEN 不能为空` | 令牌输入为空且无旧值可复用 | 重新输入有效令牌，或按回车复用 `[saved]` 的旧值 |\n| 启动时报 `No model configured` | `settings.json` 与进程环境都没有模型名 | 重跑安装器，或设置 `MINI_CODE_MODEL` / `ANTHROPIC_MODEL` 环境变量 |\n| 启动时报 `No auth configured` | 缺少 `ANTHROPIC_AUTH_TOKEN` 或 `ANTHROPIC_API_KEY` | 补全凭据；**离线演示可完全绕过此问题** |\n| 终端提示 `minicode: command not found` | `~/.local/bin` 不在 `PATH` 中 | 把 `export PATH="$HOME/.local/bin:$PATH"` 加入 `~/.bashrc` 或 `~/.zshrc` 后重新打开终端 |\n| 个别 MCP 服务器连不上导致启动缓慢 | MCP 注入失败被容错吞掉，但握手仍在进行 | 启动流程不中断；可用 `minicode mcp list` 检查连接状态并移除故障服务器 |\n\n其中关于配置优先级还有一个常用技巧：`loadRuntimeConfig` 的解析顺序是 `~/.mini-code/settings.json` → `~/.claude/settings.json` → 进程环境变量，环境变量优先级最高。这也是为什么 `MINI_CODE_MODEL_MODE=mock` 这类环境变量能一键覆盖默认行为。\n\nSources: [src/config.ts](src/config.ts#L200-L252), [src/install.ts](src/install.ts#L109-L117)\n\n## 下一步\n\n你已经能安装、启动并离线体验 MiniCode 了。建议按目录顺序继续阅读：\n\n- 在**安装与环境**分组中，先看 [配置文件体系：settings.json、mcp.json 与目录覆盖](3-pei-zhi-wen-jian-ti-xi-settings-json-mcp-json-yu-mu-lu-fu-gai)，理解配置从哪来、优先级如何叠加；再看 [交互式界面与斜杠命令速览](4-jiao-hu-shi-jie-mian-yu-xie-gang-ming-ling-su-lan)，认识你即将面对的全屏 TUI。\n- 在**日常使用**分组中，[内置工具与本地快捷指令](5-nei-zhi-gong-ju-yu-ben-di-kuai-jie-zhi-ling) 会告诉你刚才在 Mock 模式下试用的 `/ls`、`/read` 等指令在真实模式下如何工作。\n- 如果你想深入了解离线演示背后那条执行链路，直接进入 [代理循环：model → tool → model 的多步执行流程](7-dai-li-xun-huan-model-tool-model-de-duo-bu-zhi-xing-liu-cheng)。\n\n详细的安装细节与命令清单还可以随时查阅 [USAGE_ZH.md](USAGE_ZH.md#L85-L160) 中的"安装细节"和"快速开始"章节。';export{n as default};

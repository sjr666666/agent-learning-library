const e="分层记忆（layered memory）是 MiniCode 将「项目约定」注入模型上下文的机制：它不依赖注册表或插件，而是约定一组**指令文件**（`MINI.md`、`CLAUDE.md`、`.mini-code/rules/*.md` 等）的命名与放置规则，在每次构建系统提示词时从用户全局层与项目目录树中自动收集、解析 `@include` 引用、按内容去重，最后以受控的令牌预算渲染进提示词。它回答的核心问题是：*如何让模型无需用户显式说明，就知道这个仓库的编码约定、验证命令与工作流边界。*\n\n本页聚焦三个机制：**目录遍历**（哪些文件、按什么顺序被找到）、**@include 展开**（指令文件如何组合与安全防护）以及**内容去重**（重复指令如何收敛）。渲染预算与运行时集成作为收尾，帮助你理解这套机制在完整系统提示词中的位置。\n\n## 数据模型与容量常量\n\n记忆模块的对外类型极简：`ContextFile` 只有 `path` 与 `content` 两个字段，是所有发现/去重/渲染流程的统一载体；`MemoryFileInfo` 在其上追加 `scope`（`'global' | 'project' | 'rules'`）、`lineCount`、`charCount` 与 `preview`，专供报告类输出使用（Sources: [memory.ts](src/memory.ts#L6-L16)）。\n\n容量控制由两个常量承载：`MAX_PER_FILE_CHARS = 8_000`（单文件截断上限）与 `MAX_TOTAL_CHARS = 20_000`（全部指令总预算）。这两个数字决定了分层记忆在最坏情况下对上下文的侵占上限——无论发现多少文件，渲染阶段都会被压缩到 20K 字符以内（Sources: [memory.ts](src/memory.ts#L18-L19)）。\n\n## 目录遍历：候选文件、rules 目录与向上行走\n\n`discoverInstructionFiles(cwd, homeDir?, scanRoot?)` 是整套发现的入口。它把文件来源分为**三个层级**，按固定顺序收集：\n\n### 层级一：用户全局层（home）\n\n全局层读取 `homeDir`（默认 `MINI_CODE_DIR`，即 `~/.mini-code`，可由 `MINI_CODE_HOME` 环境变量覆盖）下的 `MINI.md` 与 `CLAUDE.md`——但**只取第一个找到的**（`break`），即用户全局指令遵循「MINI.md 优先于 CLAUDE.md」的单一文件语义（Sources: [memory.ts](src/memory.ts#L150-L161)、[config.ts](src/config.ts#L36-L38)）。\n\n紧接着，`~/.mini-code/rules/` 目录下的所有 `.md` 文件按文件名 `localeCompare` 排序后依次加入（Sources: [memory.ts](src/memory.ts#L163-L168)、[memory.ts](src/memory.ts#L52-L62)）。\n\n### 层级二：项目祖先目录链\n\n核心的向上行走逻辑首先从 `cwd` 出发逐级收集祖先目录（遇到 `scanRoot` 或文件系统根目录停止），然后**反转数组**，使遍历顺序为「根 → 最近子目录」（Sources: [memory.ts](src/memory.ts#L135-L145)）。\n\n对每个目录，系统检查一组候选文件名（`CANDIDATES_PER_DIR`）：\n\n| 顺序 | 候选文件 | 语义 |\n|---|---|---|\n| 1 | `MINI.md` | MiniCode 原生指令 |\n| 2 | `MINI.local.md` | 本地覆盖（不提交仓库） |\n| 3 | `.mini-code/MINI.md` | 目录化指令 |\n| 4 | `CLAUDE.md` | Claude Code 兼容 |\n| 5 | `CLAUDE.local.md` | Claude Code 本地覆盖兼容 |\n| 6 | `.claude/CLAUDE.md` | Claude Code 目录化兼容 |\n\n（Sources: [memory.ts](src/memory.ts#L21-L28)）\n\n与全局层「只取一个」不同，项目层的候选文件**全部收集**（无 `break`）——测试证实同一目录下 `MINI.md` 与 `CLAUDE.md` 会同时载入，`MINI.local.md` 会跟在 `MINI.md` 之后（Sources: [memory.test.ts](test/memory.test.ts#L207-L233)）。每个目录还会额外扫描 `.mini-code/rules/*.md`，作为该目录的规则片段（Sources: [memory.ts](src/memory.ts#L171-L186)）。\n\n```mermaid\nflowchart TD\n    A[discoverInstructionFiles cwd] --> B[全局层<br/>home/MINI.md 或 home/CLAUDE.md<br/>+ home/rules/*.md]\n    A --> C[祖先目录链 root→cwd]\n    C --> D[每目录候选<br/>MINI / MINI.local / .mini-code/MINI<br/>CLAUDE / CLAUDE.local / .claude/CLAUDE]\n    C --> E[每目录 .mini-code/rules/*.md]\n    B --> F[resolveIncludes 逐文件]\n    D --> F\n    E --> F\n    F --> G[dedupe 内容去重]\n    G --> H[ContextFile[]]\n```\n\n空文件（trim 后为空字符串）在 `tryRead` 阶段即被丢弃，不会进入结果集（Sources: [memory.ts](src/memory.ts#L43-L50)）。结果顺序因此是确定性的：**全局文件 → 根目录文件 → … → cwd 目录文件**，且同一目录内按候选数组顺序、rules 按文件名排序（Sources: [memory.test.ts](test/memory.test.ts#L133-L148)、[memory.test.ts](test/memory.test.ts#L235-L252)）。\n\n## @include：组合指令文件的展开协议\n\n分层记忆的第二个机制是文件内引用。任何指令文件中，**独占一行、trim 后恰好形如 `@<path>`** 的行（正则 `^@([^\\s]+)\\s*$`）会被识别为 include 指令，在收集阶段展开为被引用文件的内容（Sources: [memory.ts](src/memory.ts#L30)）。\n\n`resolveIncludes(content, fromFile, visited)` 是递归展开器，路径解析基准是**源文件所在目录**（`path.dirname(fromFile)`），而非 cwd（Sources: [memory.ts](src/memory.ts#L70-L115)）。展开结果用 HTML 注释包裹：\n\n```\n<!-- included from docs/workflow.md -->\nworkflow rules\n<!-- end include docs/workflow.md -->\n```\n\n### 三道防线\n\n| 场景 | 判定逻辑 | 输出标记 |\n|---|---|---|\n| 不安全路径 | 绝对路径，或含 `..` 段（`isUnsafeIncludePath`） | `<!-- include skipped: unsafe path … -->` |\n| 循环引用 | 目标路径已在 `visited` 集合中 | `<!-- include skipped: cycle detected … -->` |\n| 文件缺失 | `tryRead` 返回 null | `<!-- include skipped: not found … -->` |\n\n（Sources: [memory.ts](src/memory.ts#L64-L68)、[memory.ts](src/memory.ts#L86-L102)）\n\n循环检测采用**回溯式 visited 集合**：进入递归前 `add`、返回后 `delete`，因此同一文件可以在不同分支被重复引用，只有同一引用链上的自引用/互引才会触发标记（Sources: [memory.ts](src/memory.ts#L104-L106)）。测试覆盖了 `MINI.md → a.md → b.md → a.md` 的环，最终内容同时包含 `a`、`b` 与 `cycle detected a.md` 标记（Sources: [memory.test.ts](test/memory.test.ts#L286-L301)）。\n\n```mermaid\nsequenceDiagram\n    participant D as discoverInstructionFiles\n    participant R as resolveIncludes\n    participant F as 文件系统\n    D->>R: content + 源文件路径 + visited{源文件}\n    loop 逐行扫描\n        R->>R: 命中 @path ?\n        alt 不安全(绝对/..)\n            R->>R: 插入 unsafe 注释\n        else 已在 visited\n            R->>R: 插入 cycle 注释\n        else 文件不存在\n            R->>R: 插入 not found 注释\n        else 正常\n            R->>F: 读取被引用文件\n            R->>R: visited.add → 递归展开 → visited.delete\n            R->>R: 插入 included/end 注释块\n        end\n    end\n    R-->>D: 渲染后的完整文本\n```\n\n一个值得注意的安全语义：**include 无法逃逸出源文件目录**——`@../outside.md` 这类父目录引用会被直接拒绝并留下标记，而不是静默解析（Sources: [memory.test.ts](test/memory.test.ts#L303-L317)）。\n\n## 内容去重：SHA-256 哈希与「就近胜出」\n\n当同一份指令内容出现在多个层级（例如根目录与子目录各有一份相同内容的 `MINI.md`），`dedupe` 按内容收敛为一份。算法刻意选择了**内容哈希而非路径比较**：`contentHash` 对 trim 后的文本计算 SHA-256，意味着只有「内容完全一致」才判重——相同路径不同内容、相同内容不同路径都不会误删（Sources: [memory.ts](src/memory.ts#L32-L35)、[memory.ts](src/memory.ts#L117-L128)）。\n\n去重的胜出规则是 **「就近胜出」（cwd-closest wins）**：遍历顺序反转（从数组尾部向前），首次出现的哈希保留，随后 `unshift` 回原序。由于收集顺序是「全局 → 根 → … → cwd」，数组末尾恰是离 cwd 最近的目录——因此**根目录的重复指令会被 cwd 目录的版本覆盖**，而全局层重复则会被任何项目层版本覆盖（Sources: [memory.ts](src/memory.ts#L120-L126)）。\n\n测试验证了这一语义：根目录与 `apps/web` 子目录各有一份 `same rules`，去重后保留的是 `apps/web` 的那份（Sources: [memory.test.ts](test/memory.test.ts#L150-L164)）。\n\n## 渲染与预算：loadMemory 的容量治理\n\n`loadMemory(cwd, homeDir?, scanRoot?)` 把 `ContextFile[]` 转成可注入系统提示词的 Markdown 文本，是整个流程的「最终出口」：\n\n1. 无文件时返回空字符串，调用方直接跳过（Sources: [memory.ts](src/memory.ts#L234-L237)）；\n2. 有文件时以 `# Instructions` 开头，每个文件渲染为一个 `## <文件名> (scope: <目录>)` 小节（`renderScope` 用 basename + 父目录描述来源）（Sources: [memory.ts](src/memory.ts#L238-L249)、[memory.ts](src/memory.ts#L228-L232)）；\n3. 预算治理是**两级截断**：单文件超过 `MIN_PER_FILE_CHARS`（8K）时 `truncateTo` 截断并追加 `[truncated]` 标记；累计消费超过 `MAX_TOTAL_CHARS`（20K）时停止后续文件，追加 `_Additional instruction content omitted after reaching the prompt budget._`（Sources: [memory.ts](src/memory.ts#L37-L41)、[memory.ts](src/memory.ts#L241-L250)）。\n\n两个细节值得注意：预算按**截断后的实际长度**递减（`remaining -= truncated.length`），因此不会出现「看似 20K 实际溢出」的偏差；且渲染顺序继承收集顺序——全局与根目录指令永远优先占预算，离 cwd 越近的文件越可能被截断（Sources: [memory.ts](src/memory.ts#L247-L249)）。测试确认 10K 单文件被截断、两个 12K 文件的组合被压缩到 30K 以下（Sources: [memory.test.ts](test/memory.test.ts#L367-L395)）。\n\n## 运行时集成：三个消费入口\n\n`src/memory.ts` 本身不含任何 UI 逻辑，它通过三个独立入口被消费：\n\n| 入口 | 位置 | 行为 |\n|---|---|---|\n| 系统提示词 | [prompt.ts](src/prompt.ts#L92-L95) | `buildSystemPrompt` 调用 `loadMemory(cwd)`，非空时追加为提示词独立段落 |\n| `/memory` 命令 | [cli-commands.ts](src/cli-commands.ts#L265-L267) | `discoverInstructionFiles` + `renderMemoryReport`，输出每文件的 scope/lines/chars/preview |\n| TUI 启动横幅 | [tty-app.ts](src/tty-app.ts#L1706-L1721) | 启动时发现指令文件，若有则向 transcript 推送「Memory: N instruction file(s) loaded」清单 |\n\n（命令定义与注册见 [cli-commands.ts](src/cli-commands.ts#L151-L154)，提示词刷新链路 `refreshSystemPrompt` 见 [tty-app.ts](src/tty-app.ts#L867-L870)）\n\n其中提示词注入是最核心的路径：每次系统提示词刷新（启动、权限摘要变化、技能/MCP 状态更新）都会重新走一遍「发现 → include 展开 → 去重 → 预算渲染」，保证模型始终看到**当前 cwd 对应的最新指令集**（Sources: [tty-app.ts](src/tty-app.ts#L964)、[tty-app.ts](src/tty-app.ts#L1287)、[tty-app.ts](src/tty-app.ts#L1371)）。\n\n## 设计要点与边界\n\n- **约定优于配置**：无清单文件、无注册表，文件存在即指令；读取失败被 `tryRead` 静默吞掉，单个损坏文件不影响整体发现（Sources: [memory.ts](src/memory.ts#L43-L50)）。\n- **Claude Code 兼容层**：`CLAUDE.md`/`CLAUDE.local.md`/`.claude/CLAUDE.md` 全部纳入候选，但优先级低于同名 MiniCode 原生文件，迁移成本趋近于零。\n- **全局单文件 vs 项目多文件**：全局层刻意「只取一个」防止用户级指令互相覆盖产生歧义；项目层刻意「全部收集」以承载分目录的渐进式约定。\n- **去重的粒度是「文件」而非「行」**：完全相同的内容只保留离 cwd 最近的一份；若两份指令仅部分重复，它们会原样共存——这是刻意取舍，避免复杂 diff 合并引入语义错误。\n- **include 是「渲染期展开」而非「运行时读取」**：展开发生在提示词构建阶段，模型看到的永远是展开后的最终文本；`<!-- included from … -->` 注释同时起到来源标注作用。\n\n与相邻机制的关系：技能系统（`SKILL.md`）提供「按需加载的工作流」，而分层记忆提供「常驻的项目约定」——前者只向模型暴露一行摘要，后者则把全文（预算内）注入提示词（Sources: [skills.ts](src/skills.ts#L24-L47)）；`/init` 命令负责生成 `MINI.md` 骨架与 `.mini-code/` 目录，是分层记忆的「供给侧」（Sources: [init.ts](src/init.ts#L302-L325)）。\n\n继续探索扩展机制，建议下一步阅读 [项目初始化：/init 脚手架与仓库自动检测](24-xiang-mu-chu-shi-hua-init-jiao-shou-jia-yu-cang-ku-zi-dong-jian-ce)（了解 `MINI.md` 如何被脚手架生成），或对比 [技能系统：SKILL.md 的多级发现与加载](21-ji-neng-xi-tong-skill-md-de-duo-ji-fa-xian-yu-jia-zai)（两者在多级发现与去重语义上的异同）。";export{e as default};

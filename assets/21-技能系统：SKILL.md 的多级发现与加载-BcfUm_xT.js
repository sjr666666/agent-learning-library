const s='技能系统是 MiniCode 扩展机制的第一层：它不引入插件平台，而是约定「一个技能 = 一个目录下的 `SKILL.md` 文件」。系统在启动时扫描**四个固定根目录**，将每个技能的摘要注入系统提示词，并在代理循环中提供一个 `load_skill` 工具，让模型在需要时按名拉取完整工作流内容。这套设计继承自 Claude Code 的 skills 约定，同时用 `.mini-code` 命名空间保留了自身的托管目录（Sources: [skills.ts](src/skills.ts#L6-L15)、[ARCHITECTURE_ZH.md](ARCHITECTURE_ZH.md#L51)）。\n\n## 多级发现：四个来源根与优先级\n\n技能发现的核心是一个固定的根目录数组，按优先级从高到低排列。`getSkillRoots(cwd)` 返回四个 `SkillSourceRoot`，每个根都携带一个 `source` 标签，用于标识技能的归属层级：\n\n| 优先级 | 根目录 | source 标签 | 用途 |\n|---|---|---|---|\n| 1（最高） | `{cwd}/.mini-code/skills` | `project` | 项目级托管技能 |\n| 2 | `~/.mini-code/skills` | `user` | 用户级托管技能 |\n| 3 | `{cwd}/.claude/skills` | `compat_project` | Claude Code 兼容（项目） |\n| 4（最低） | `~/.claude/skills` | `compat_user` | Claude Code 兼容（用户） |\n\n（Sources: [skills.ts](src/skills.ts#L49-L68)）\n\n每个技能目录的结构约定是 `<技能名>/SKILL.md`。`listSkillDirs` 遍历根目录下的所有子目录，尝试读取每个子目录中的 `SKILL.md`；若读取失败（目录不存在、文件缺失或内容损坏），该条目被静默跳过，不影响其他技能（Sources: [skills.ts](src/skills.ts#L76-L106)）。\n\n```mermaid\nflowchart TD\n    A[discoverSkills cwd] --> B[项目级 .mini-code/skills]\n    A --> C[用户级 ~/.mini-code/skills]\n    A --> D[兼容级项目 .claude/skills]\n    A --> E[兼容级用户 ~/.claude/skills]\n    B --> F{byName 去重}\n    C --> F\n    D --> F\n    E --> F\n    F --> G[SkillSummary 列表<br/>name/description/path/source]\n    G --> H[注入 ToolRegistry 元数据]\n```\n\n**去重语义是「首见胜出」（first-wins）**：`discoverSkills` 按优先级顺序遍历四个根，用一个 `Map<string, LoadedSkill>` 按技能名去重——先被写入 Map 的条目保留，后出现的同名技能被丢弃（Sources: [skills.ts](src/skills.ts#L108-L126)）。这意味着项目级技能可以覆盖（shadow）用户级同名技能，形成一个自然的层级覆盖模型：项目定制优先于个人偏好，托管目录优先于兼容目录。\n\n## 描述提取：SKILL.md 如何变成一行摘要\n\n技能发现阶段只向模型暴露**摘要**而非全文——这是控制令牌开销的关键决策。摘要的提取逻辑 `extractDescription` 遵循一个简单启发式（Sources: [skills.ts](src/skills.ts#L24-L47)）：\n\n1. 将 `\\r\\n` 归一化为 `\\n`，按空行切分为段落块；\n2. 跳过以 `#` 开头的块（即 Markdown 标题区）；\n3. 在第一个非标题块中，取第一行非空且不以 `#` 开头的文本行；\n4. 去除该行中的反引号字符；\n5. 若全程无匹配，回退为 `\'No description provided.\'`。\n\n实践中这通常提取到 `SKILL.md` 开头第一段正文，即技能的「一句话说明」。它随后出现在系统提示词的技能清单和 `/skills` 命令的输出中。**注意**：摘要与全文是分离的——发现阶段只产出摘要，全文留在磁盘上，直到模型显式调用 `load_skill` 才读取。\n\n## 按名加载：load_skill 工具\n\n`createLoadSkillTool` 将技能加载能力封装为标准工具 `load_skill`，注册进内置工具集（Sources: [tools/load-skill.ts](src/tools/load-skill.ts#L9-L45)、[tools/index.ts](src/tools/index.ts#L42-L66)）。它的行为与 `discoverSkills` 的优先级一致：`loadSkill(cwd, name)` 同样按四个根的优先级顺序逐个尝试读取 `{root}/{name}/SKILL.md`，返回第一个命中；全部失败则返回 `null`（Sources: [skills.ts](src/skills.ts#L128-L154)）。\n\n```mermaid\nsequenceDiagram\n    participant M as 模型\n    participant T as load_skill 工具\n    participant S as skills.ts\n    M->>T: load_skill(name="frontend-dev")\n    T->>S: loadSkill(cwd, name)\n    loop 四个根按优先级\n        S->>S: 尝试读取 {root}/frontend-dev/SKILL.md\n    end\n    S-->>T: LoadedSkill 或 null\n    T-->>M: SKILL:/SOURCE:/PATH: + 全文<br/>或 "Unknown skill: name"\n```\n\n工具输出采用带标签的多行格式：依次输出 `SKILL:`（技能名）、`SOURCE:`（来源标签）、`PATH:`（绝对路径），空行后附上 `SKILL.md` 全文。这样模型既能确认技能的身份与来源，又能直接获取工作流指令（Sources: [tools/load-skill.ts](src/tools/load-skill.ts#L33-L42)）。\n\n在提示词层面，系统明确约束了调用时机：*"If the user names a skill or clearly asks for a workflow that matches a listed skill, call load_skill before following it."*——用户点名技能或请求与技能匹配的工作流时，必须先加载再执行（Sources: [prompt.ts](src/prompt.ts#L24)）。\n\n## 提示词注入与运行时集成\n\n技能系统贯穿启动链路的三个环节：\n\n1. **注册中心元数据**：`createDefaultToolRegistry` 在创建时调用 `discoverSkills(args.cwd)`，把 `SkillSummary[]` 存入 `ToolRegistry` 的 `metadataStore`，通过 `getSkills()` 暴露（Sources: [tools/index.ts](src/tools/index.ts#L46-L65)、[tool.ts](src/tool.ts#L35-L63)）。\n2. **系统提示词**：`buildSystemPrompt` 读取 `getSkills()`，若发现技能则渲染为 `Available skills:` 清单（每项 `- name: description`），否则渲染 `- none discovered`；同时携带 `load_skill` 调用规则（Sources: [prompt.ts](src/prompt.ts#L37-L46)、[index.ts](src/index.ts#L85-L93)）。\n3. **模型工具列表**：`AnthropicModelAdapter` 在请求体中把注册中心所有工具（含 `load_skill`）映射为 name/description/input_schema 三要素，因此模型在每一轮都能看到该工具（Sources: [anthropic-adapter.ts](src/anthropic-adapter.ts#L330-L340)）。\n\n```mermaid\nflowchart LR\n    A[skills.ts<br/>发现+加载] -->|SkillSummary[]| B[ToolRegistry<br/>metadataStore]\n    B -->|getSkills| C[buildSystemPrompt<br/>Available skills 清单]\n    B -->|工具列表| D[Anthropic 请求<br/>load_skill 定义]\n    C --> E[模型]\n    D --> E\n    E -->|load_skill| A\n```\n\n会话中，用户可通过 `/skills` 斜杠命令实时查看当前发现到的技能及其来源标签，输出格式为 `name  description  [source]`；空结果时命令会返回包含四个根目录路径的引导文案（Sources: [cli-commands.ts](src/cli-commands.ts#L212-L224)）。TUI 的状态栏与启动横幅也会展示技能数量（Sources: [tty-app.ts](src/tty-app.ts#L246)、[index.ts](src/index.ts#L143)）。\n\n## 技能管理：minicode skills 命令\n\n除自动发现外，系统提供 CLI 管理命令 `minicode skills`，支持三个子命令（Sources: [manage-cli.ts](src/manage-cli.ts#L13-L25)）：\n\n| 子命令 | 作用 | 关键参数 |\n|---|---|---|\n| `minicode skills list` | 列出全部已发现技能（含路径） | 无 |\n| `minicode skills add <path>` | 安装技能到托管目录 | `--name` 重命名、`--project` 项目级 |\n| `minicode skills remove <name>` | 从托管目录删除技能 | `--project` 项目级 |\n\n`installSkill` 接受**两种输入形态**：一个包含 `SKILL.md` 的目录，或一个直接的 `SKILL.md` 文件路径。目录形态以目录 basename 推断技能名，文件形态以父目录 basename 推断；`--name` 可覆盖推断结果。安装目标由 scope 决定——默认写入 `~/.mini-code/skills/{name}/SKILL.md`（用户级），`--project` 则写入 `{cwd}/.mini-code/skills/{name}/SKILL.md`，目录递归创建（Sources: [skills.ts](src/skills.ts#L156-L199)、[manage-cli.ts](src/manage-cli.ts#L27-L38)）。\n\n`removeManagedSkill` 只从**托管目录**（`.mini-code`）删除，绝不触碰 `.claude` 兼容目录——这保证了兼容层只读，用户自己的 Claude Code 技能目录不被 MiniCode 意外修改（Sources: [skills.ts](src/skills.ts#L202-L218)）。删除时若目录不存在，返回 `removed: false` 并打印提示，而非报错。\n\n## 设计要点与边界\n\n技能系统刻意保持轻量，几个值得注意的设计决策：\n\n- **约定优于配置**：不做技能注册表或清单文件，目录存在 + `SKILL.md` 存在即视为技能；损坏的技能被静默忽略，保证发现的健壮性（Sources: [skills.ts](src/skills.ts#L97-L99)）。\n- **摘要与全文分离**：启动阶段只扫描摘要，全文按需加载，避免把大量工作流文本永久塞进上下文——这与本页相邻的[上下文折叠投影](14-shang-xia-wen-zhe-die-tou-ying-ke-zhai-yao-qu-jian-shi-bie-yu-ti-huan)等长会话治理机制形成配合。\n- **兼容但可托管**：`.claude/skills` 作为只读兼容层，`.mini-code/skills` 是唯一可写托管层；优先级上托管层压过兼容层，项目级压过用户级。\n- **技能 vs MCP 的分工**：技能承载「怎么做」的工作流约定，MCP 承载「能做什么」的外部能力；两者可组合使用——技能塑造执行方式，MCP 提供搜索、图像理解等远程能力（Sources: [USAGE.md](USAGE.md#L426-L434)）。\n\n想继续深入扩展机制，建议依次阅读 [MCP 集成：stdio/HTTP 启动、协议协商与工具注入](22-mcp-ji-cheng-stdio-http-qi-dong-xie-yi-xie-shang-yu-gong-ju-zhu-ru)（对比技能与外部工具的接入差异）和 [分层记忆：指令文件遍历、@include 与去重](23-fen-ceng-ji-yi-zhi-ling-wen-jian-bian-li-atinclude-yu-qu-zhong)（另一种基于文件遍历的上下文注入机制）。';export{s as default};

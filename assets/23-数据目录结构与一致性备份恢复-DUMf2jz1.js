const s='本页深入剖析 HappyClaw 的**数据在磁盘上的组织方式**与**备份/恢复的一致性保证**。与 [部署与运维：环境变量、Docker 镜像与备份恢复](5-bu-shu-yu-yun-wei-huan-jing-bian-liang-docker-jing-xiang-yu-bei-fen-hui-fu) 中的操作视角不同，本页聚焦三个架构问题：哪些数据值得备份、为什么这些数据可以安全地在线备份、以及恢复过程如何保证"要么完整恢复、要么原样保留"。\n\n## 数据目录全景：受管组件与瞬态组件的分界\n\n数据根目录由 `DATA_DIR` 定义，默认解析为项目根下的 `data/`，并派生三个关键子路径：`STORE_DIR = DATA_DIR/db`（SQLite 数据库目录）、`GROUPS_DIR = DATA_DIR/groups`（工作区根目录）。运行期可用 `RUNTIME_DATA_DIR` 覆盖（`make backup`/`make restore` 的目标目录），容器挂载场景下该覆盖是隔离的基础。Sources: [config.ts](src/config.ts#L23-L25)、[Makefile](Makefile#L17-L18)\n\n备份系统以 **backup-manifest.json 的受管组件清单**为唯一真相源：`config / groups / sessions / skills / mcp-servers / plugins / memory / avatars / extra / builtin-skills / db` 共 11 项为**受管组件**（backup 时全部捕获），`ipc / env / streaming-buffer / logs` 为**瞬态组件**（显式排除）。这个分界不是任意的——它精确对应"重启后可重建"与"重建即丢失"两类数据的分界线：瞬态组件要么是运行时管道（`ipc`）、要么是宿主机侧机密（`env`）、要么是临时缓冲（`streaming-buffer`）、要么是可再生成的日志（`logs`）。Sources: [backup-manifest.mjs](scripts/backup-manifest.mjs#L7-L47)\n\n```mermaid\nflowchart TB\n    subgraph DATA["data/（DATA_DIR，默认 data/）"]\n        direction TB\n        subgraph Managed["受管组件 ×11 — 全部进入备份"]\n            DB["db/<br/>messages.db + WAL<br/>migration-backups/"]\n            CFG["config/<br/>密钥 · 凭据 · provider 配置"]\n            GRP["groups/<br/>工作区：CLAUDE.md · conversations/ · 用户文件"]\n            SES["sessions/<br/>Claude 配置目录"]\n            SK["skills/ · mcp-servers/ · plugins/"]\n            MEM["memory/（遗留 v1）"]\n            AV["avatars/ · extra/ · builtin-skills/"]\n        end\n        subgraph Transient["瞬态组件 ×4 — 备份时排除"]\n            IPC["ipc/ — 运行时管道"]\n            ENV["env/ — 宿主机侧 denylist"]\n            SB["streaming-buffer/ — 流式缓冲"]\n            LOG["logs/ — 运行日志"]\n        end\n    end\n    Backup["make backup"] -->|sqlite-snapshot + prepare-backup-tree| Managed\n    Backup -.不触碰.-> Transient\n```\n\n## 受管组件逐项解析\n\n**`db/` 是唯一必须存在的组件**——恢复器硬性要求归档包含 `data/db/messages.db`，否则拒绝恢复。它承载全部业务数据：核心表族（会话、消息、用户、计费、用量）、渠道可靠性表族、Workspace Memory v2 表族与 Owner Profile 表族，外加 schema 版本状态。数据库以 WAL 模式运行，因此 `db/` 目录在运行期还会出现 `messages.db-wal` 与 `messages.db-shm` 两个 sidecar。`db/migration-backups/` 是迁移保险的落点（详见下文一致性机制）。Sources: [restore-backup.mjs](scripts/restore-backup.mjs#L449-L455)、[db.ts](src/db.ts#L484-L490)、[db.ts](src/db.ts#L386-L389)\n\n**`config/` 是敏感度最高的组件**，权限按目录 `0o700`、文件 `0o600` 加固。它的内容横跨四个安全域：`session-secret.key`（登录 cookie 签名密钥，缺失则恢复后所有会话失效，`make restore` 会专门提示）；`claude-provider.key`（渠道凭据加密主密钥）与 `channel-accounts/`（各渠道账号的 AES-GCM 加密凭据文件）；`claude-provider.json` 及各家 provider 配置（`claude-custom-env.json`、`feishu-provider.json`、`telegram-provider.json` 等）；`container-env/` 与 `user-im/` 子目录分别存放容器环境与 IM 用户配置。此外 `container-claude-json.json` 是为容器模式生成的精简 `.claude.json`。Sources: [config.ts](src/config.ts#L55)、[channel-account-secrets.ts](src/channel-account-secrets.ts#L16-L25)、[runtime-config.ts](src/runtime-config.ts#L59-L65)、[container-runner.ts](src/container-runner.ts#L156-L183)\n\n**`groups/` 是工作区（Workspace）的文件系统真相**。每个工作区对应 `groups/{folder}/`，folder 名受 `[A-Za-z0-9][A-Za-z0-9._-]{0,127}` 正则约束——这是"持久化标识符而非任意路径"的安全契约。工作区内混合三类内容：**系统保留路径**（`logs`、`CLAUDE.md`、`.claude`、`conversations`，在文件面板中被保护不可删除）；**Agent 产物**（`conversations/` 是 agent-runner 在上下文压缩前归档完整会话转录的目录，`CLAUDE.md` 是项目记忆文件）；**用户文件**（通过文件面板、IM 下载等途径进入的任意文件，也可是 `git clone` 初始化的代码仓库）。`logs/` 在备份打包前被整目录剪除。Sources: [file-manager.ts](src/file-manager.ts#L30-L31)、[workspace-folder.ts](src/workspace-folder.ts#L9-L16)、[agent-runner/src/index.ts](container/agent-runner/src/index.ts#L1085-L1095)、[Makefile](Makefile#L262-L264)\n\n**`sessions/` 是 Claude Code 的配置目录集合**：`sessions/{folder}/.claude/`（顶层会话）与 `sessions/{folder}/agents/{agentId}/.claude/`（Agent 会话）分别对应不同粒度的 CLI 状态。备份系统对其中有特殊处理——`.claude` 下的符号链接（如指向宿主技能目录的 `host-skill`）被视为**可在运行时重建的生成物**，打包时直接忽略而非记录。Sources: [session-files.ts](src/session-files.ts#L15-L20)、[prepare-backup-tree.mjs](scripts/prepare-backup-tree.mjs#L35-L42)\n\n**能力类组件按用户维度分片**：`skills/{userId}/` 是技能树；`mcp-servers/{userId}/` 含 `servers.json` 与 `secrets.json` 两个文件；`plugins/` 分三层——`catalog/`（市场索引 + 只写一次的 `versions/{contentHash}/` 快照树）、`users/{userId}/plugins.json`（启用清单）、`runtime/{userId}/snapshots/`（物化后的运行时树）。这些目录的写入普遍遵循"临时目录构建 + 原子 rename 发布"模式。Sources: [mcp-utils.ts](src/mcp-utils.ts#L59-L69)、[plugin-catalog.ts](src/plugin-catalog.ts#L77-L100)、[plugin-utils.ts](src/plugin-utils.ts#L48-L52)\n\n**其余三个组件的定位**：`memory/` 是 Workspace Memory v1 的遗留目录——v2 全面迁移到 SQLite 后，该目录"只保留用于备份、离线导出或显式迁移，不读写"；`avatars/` 存放用户与 AI 头像（`{userId}-user-`/`{userId}-ai-` 前缀分片）；`extra/{groupFolder}/` 是容器模式下每个工作区的持久化附加目录（`/workspace/extra/`，容纳 `npm install -g` 的全局包）；`builtin-skills/` 是内置技能缓存，由 `install-host-tools.sh` 以"staging 构建 + 同文件系统 swap"方式原子刷新。Sources: [workspace-memory-v2.md](docs/workspace-memory-v2.md#L141-L150)、[routes/auth.ts](src/routes/auth.ts#L762-L820)、[container-runner.ts](src/container-runner.ts#L1515-L1530)、[install-host-tools.sh](scripts/install-host-tools.sh#L72-L100)\n\n## 瞬态组件与排除语义\n\n四个瞬态组件各有明确的排除理由：**`ipc/`** 是主服务与容器 runner 之间的任务/消息管道（`ipc/{folder}/input`、`tasks-run/`、`agents/`），每次运行都会重建；**`env/`** 存放容器环境配置，其中包含宿主机侧 denylist 等机密——备份它等于把安全边界本身打包带走；**`streaming-buffer/`** 是流式输出的临时缓冲；**`logs/`** 是工作区运行日志。这些目录**不进入归档**，也不被恢复器管理。Sources: [backup-manifest.mjs](scripts/backup-manifest.mjs#L38-L42)、[container-runner.ts](src/container-runner.ts#L1064)、[container-runner.ts](src/container-runner.ts#L1338-L1341)、[index.ts](src/index.ts#L9895)\n\n## 一致性机制：WAL 模式与事务一致快照\n\n在线备份的一致性核心是 **SQLite WAL 模式 + online backup API** 的组合。运行期的 `messages.db` 以 `journal_mode = WAL` + `synchronous = NORMAL` 运行——这是文档化的安全配对：WAL 让读操作不阻塞写操作，`NORMAL` 同步级别牺牲"最后瞬间的已提交事务"以换取约 21 倍的同步写提速，且最坏情况只是丢失而非损坏。备份脚本 `sqlite-snapshot.mjs` 通过 better-sqlite3 的 `source.backup(snapshotPath)` 读取**事务一致快照**：即使主进程正在写入、已提交页仍驻留在 WAL 中，快照也完整包含它们——因此 `make backup` **无需停止服务**。快照生成后立即执行 `quick_check` 校验并 `chmod 600`。Sources: [db.ts](src/db.ts#L484-L490)、[sqlite-snapshot.mjs](scripts/sqlite-snapshot.mjs#L20-L84)\n\n需要明确一致性边界：**事务一致只保证数据库文件本身**。备份管线对文件组件采用"运行期逐目录 `cp -a`"而非快照，因此工作区文件与数据库之间存在理论上的时间漂移。这个设计是有意为之——数据库中的文件引用是相对路径（如 `groups/{folder}/...`），文件树的部分滞后不会产生悬空引用；而真正的强一致场景（schema 迁移）则由独立的 `VACUUM INTO` 机制覆盖：`initDatabase()` 在迁移前通过 `VACUUM INTO` 生成自包含快照到 `db/migration-backups/`（路径可用 `HAPPYCLAW_MIGRATION_BACKUP_DIR` 覆盖），备份失败即中止启动。Sources: [Makefile](Makefile#L252-L262)、[db.ts](src/db.ts#L382-L429)\n\n```mermaid\nflowchart LR\n    subgraph Live["运行期 data/db/"]\n        MDB["messages.db（WAL 模式）"]\n        WAL["messages.db-wal（已提交但未 checkpoint 的页）"]\n    end\n    MDB -->|"online backup API<br/>读取事务一致快照（含 WAL 页）"| SNAP["备份暂存区 data/db/messages.db"]\n    WAL --> SNAP\n    SNAP -->|"quick_check + chmod 600"| OK["一致性快照 ✓"]\n    MDB -->|"VACUUM INTO（迁移前）"| MB["db/migration-backups/<br/>messages-v{from}-to-v{to}-{ts}.db"]\n```\n\n## 备份树准备：三重安全防御\n\n`make backup` 先把 11 个受管组件复制到临时暂存根（`mktemp -d`），再交给 `prepare-backup-tree.mjs` 做**符号链接与特殊文件扫描**——任何不安全项都在备份创建时 fail-fast，而非恢复时才发现不可恢复。扫描把符号链接分为三类处理：\n\n| 链接类型 | 处理策略 | 理由 |\n|---|---|---|\n| 会话 `.claude` 下的生成链接 | **直接删除**并计数提示 | 运行时按需重建，备份它只会引入逃逸风险 |\n| `groups/` 下指向数据目录外部的链接 | **直接删除**并计数提示 | 目标不在备份根内，恢复后必然悬空 |\n| 指向数据目录内部的相对链接 | **记录到 `backup-symlinks.json`（v1）后解除**，恢复时重建 | tar 会保留链接语义，但恢复端拒绝链接类型条目——记录后重建是唯一可验证的路径 |\n\n**硬性拒绝**的类别包括：绝对路径链接、逃逸出备份根的链接、非文件/目录/链接的特殊文件（FIFO、设备等）、以及**硬链接文件（`nlink > 1`）**——tar 会把硬链接存为 link-type 条目指向首次出现的兄弟文件，而恢复端拒绝该类型，放行将产生"备份报告成功但永远无法恢复"的归档。扫描结束后，Makefile 再剪除 `groups/*/*/logs`，最后由 `backup-manifest.mjs` 写入 `backup-manifest.json`（`formatVersion: 2`，含受管清单、实际存在清单与排除清单，权限 `0o600`），整个 `data` 树以 `tar -czf` 打包。Sources: [prepare-backup-tree.mjs](scripts/prepare-backup-tree.mjs#L12-L117)、[Makefile](Makefile#L244-L285)、[backup-manifest.mjs](scripts/backup-manifest.mjs#L20-L47)\n\n## 恢复管线：fail-closed 校验链\n\n恢复器 `restore-backup.mjs` 的设计哲学是**每个校验失败都拒绝任何文件系统变更**（fail-closed）。完整管线如下：\n\n```mermaid\nflowchart TB\n    A["make restore FILE=xxx.tar.gz"] --> B["端口空闲检查<br/>assert-port-free（Makefile 预检 + 脚本内双执行）"]\n    B --> C["归档条目校验<br/>tar -tzf 路径包含性 + tar -tvzf 条目类型"]\n    C -->|"拒绝 / 绝对路径 / .. / 非 data 前缀 / 链接设备 FIFO"| X1["中止，data/ 未被触碰"]\n    C --> D["获取 PID 戳记排他锁<br/>.happyclaw-restore.lock（wx 原子创建）"]\n    D --> E["解压到 .happyclaw-restore-* 暂存目录<br/>--no-same-owner --no-same-permissions"]\n    E --> F["解压后树校验<br/>拒绝符号链接与特殊条目"]\n    F --> G["读取并校验 backup-manifest.json"]\n    G --> H["敏感组件权限加固<br/>config/sessions/db → 0o700 目录 + 0o600 文件"]\n    H --> I["数据库校验<br/>integrity_check + 清除 WAL/SHM sidecar"]\n    I --> J["符号链接元数据校验与重建"]\n    J --> K["replaceComponents<br/>逐组件 rename 旧数据到 rollback/ → 安装新数据"]\n    K -->|"任一步失败"| R["按逆序回滚 rename"]\n    K -->|"全部成功"| L["清扫孤儿暂存目录 + 释放锁"]\n```\n\n各层校验的具体机制：**归档条目校验**在解压前执行——`-tzf` 验证每条路径必须为 `data/` 前缀且无 `..`/绝对路径，`-tvzf` 验证条目类型只允许普通文件（`-`）与目录（`d`），从而阻断归档内嵌链接把写入重定向到暂存目录之外；**解压后树校验**再对落地文件系统做一次 lstat 扫描（防御 tar 实现差异）；**权限加固**把 `config/`、`sessions/`、`db/` 三个敏感组件整体收敛为 0o700/0o600；**数据库校验**以只读连接执行 `integrity_check` 并删除校验产生的空 WAL/SHM sidecar；**符号链接重建**先全量验证元数据（路径在受管组件内、target 非绝对、解析后不逃逸恢复根、无嵌套、无冲突），再统一 `symlinkSync`。Sources: [restore-backup.mjs](scripts/restore-backup.mjs#L86-L133)、[restore-backup.mjs](scripts/restore-backup.mjs#L135-L196)、[restore-backup.mjs](scripts/restore-backup.mjs#L198-L292)\n\n**组件替换**阶段是恢复的原子性核心：对每个受管组件，先把现有数据 `rename` 到 `rollback/`，再 `rename` 新数据就位；任一步失败按逆序回滚——恢复失败时 `data/` 保持原状。**孤儿暂存目录清扫**的时机经过精心设计：被 SIGKILL/OOM 中断的恢复会遗留 `.happyclaw-restore-*` 目录，其中 `rollback/` 子树可能是旧数据唯一幸存副本，因此清扫**只在本次恢复完整验证并成功应用后**执行——提前清扫会在本次归档损坏时摧毁最后可恢复副本。**排他锁**以 `wx` 标志原子创建、PID 戳记；陈旧锁**绝不自动回收**（便携文件 API 无法实现无竞态的 compare-and-delete，破坏性操作必须人工确认），遇到活锁直接报错。Sources: [restore-backup.mjs](scripts/restore-backup.mjs#L351-L398)、[restore-backup.mjs](scripts/restore-backup.mjs#L400-L462)、[restore-backup.mjs](scripts/restore-backup.mjs#L464-L497)\n\n## 一致性不变量与测试契约\n\n备份/恢复系统的一致性由 `tests/backup-restore-safety.test.ts`（818 行）以集成测试形式固化，可归纳为四组不变量：\n\n| 不变量 | 实现机制 | 测试覆盖 |\n|---|---|---|\n| 备份必须可恢复 | 生成期拒绝硬链接、绝对链接、逃逸链接、特殊文件 | 符号链接/硬链接拒绝、外部链接忽略 |\n| 归档不可越权 | 解压前类型校验 + 路径包含校验 + 解压后树校验 | 恶意归档、伪造逃逸元数据拒绝 |\n| 恢复失败不破坏现状 | staging + rollback rename 逆序回滚、校验先于变更 | 损坏数据库中止、孤儿保留 |\n| 并发恢复串行化 | PID 锁 fail-closed、陈旧锁人工清理 | 活锁拒绝、mkdtemp 失败释放锁、陈旧锁人工移除 |\n\n两个边界案例尤其值得注意：**孤儿 rollback 在本次恢复失败时必须幸存**（测试显式构造"损坏归档 + 遗留孤儿"组合，断言孤儿内容不被触碰）；**暂存目录创建失败不得遗留锁**（注入 ENOSPC，断言锁已释放且重试可立即成功）。Sources: [backup-restore-safety.test.ts](tests/backup-restore-safety.test.ts#L37-L180)、[backup-restore-safety.test.ts](tests/backup-restore-safety.test.ts#L380-L560)\n\n**下一步阅读建议**：本页的一致性机制依赖 SQLite 的 WAL 与迁移管线，详见 [SQLite Schema 与数据库迁移机制](12-sqlite-schema-yu-shu-ju-ku-qian-yi-ji-zhi)；数据目录中的 `groups/` 与 `sessions/` 是 [智能体-工作区-会话三层模型](6-zhi-neng-ti-gong-zuo-qu-hui-hua-san-ceng-mo-xing) 的落盘形态；`config/` 的密钥体系在 [密钥加密与凭据管理](25-mi-yao-jia-mi-yu-ping-ju-guan-li) 中有完整展开；运维操作入口（`make backup`/`make restore` 的具体命令）见 [部署与运维](5-bu-shu-yu-yun-wei-huan-jing-bian-liang-docker-jing-xiang-yu-bei-fen-hui-fu)。';export{s as default};
